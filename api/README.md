# Introduction

install global npm modules and typings
```javascript
npm i typings -g
npm i gulp -g
```
Search Typings
```javascript
typings search <typings name>
```

Add / Update typings
```javascript
typings i <typings name>  --global --save
```
