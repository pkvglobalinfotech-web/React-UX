import { BaseRequest } from '../../../../Common/Index';
//import * as moment from 'moment';

export class ServiceItemFormat {
    public static heading: any = [];
    public async ServiceItemExport(req: BaseRequest): Promise<any[]> {
        let AllServiceItemFormat: any = [];
        try {
            console.log('asdfadsf');
        } catch (e) {
            console.log('error : ' + e);
        }
        return AllServiceItemFormat;
    }

}
