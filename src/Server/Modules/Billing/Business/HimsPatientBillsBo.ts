import * as SStatic from 'sequelize';
import { BaseBo } from '../../Base/Index';
import { WhereOptions, IncludeOptions, Report, FileInfo, Template } from '../../../Core/Index';
import { ApiResponse, BaseRequest, ApiRequest } from '../../../Common/Index';
import { BoFactory } from '../../Base/Business/Index';
import * as bo from '../../Billing/Business/Index';
import * as regbo from '../../Registration/Business/Index';
import * as emrbo from '../../EMR/Business/Index';
import * as encbo from '../../Visit/Business/Index';
import * as appbo from '../../SystemSettings/Business/Index';
import * as billingBO from '../../Billing/Business/Index';
import * as inpatientBO from '../../IPManagement/Business/Index';
import * as inventoryBO from '../../Pharmacy/Business/Index';
import * as apnmtBO from '../../Appointment/Business/Index';
import {
    PatientBillsInstance, PatientBillsAttributes,
    PatientReturnsAttributes
} from '../Model/Interface/Index';
import { Sequence, SequenceKeys } from '../../General/Common/Sequence.s';
import {
    PatientBillsFilters, PatientBillDetailsFilters, PatientReturnsFilters,
    PatientPaymentDetailsFilters, PatientRefundFilters, PatientBillSummaryFilters,
    PatientReturnDetailsFilters, PatientBillPackageSummaryFilters,
    PatientDoctorShareDetailsFilters
} from '../Common/Filters.e';
import { BedOccupancyHistoryFilters } from '../../IPManagement/Common/Filters.e';
import { ItemMasterFilters } from '../../Pharmacy/Common/Filters.e';
import {
    EncounterFilters
} from '../../Visit/Common/Filters.e';
import {
    PatientFilters
} from '../../Registration/Common/Filters.e';
import { SurgeryEntryFilters } from '../../OtManagement/Common/Filters.e';
import { DoctorShareFilters } from '../../Billing/Common/Filters.e';

import {
    PatientOrderDetailFilters, ProcedureOrderDetailFilters,
    PrescriptionDetailFilters, PatientDietOrderDetailFilters, PatientOrderFilters,
    TreatmentPlanDetailFilters, TreatmentPlanFilters
} from '../../EMR/Common/Filters.e';
import {
    ServiceItemFilters,
    ServiceItemPackageMapFilters,
    ServiceItemAliasFilters
} from '../../ClinicalMaster/Common/Filters.e';
import * as clinicalmasterBO from '../../ClinicalMaster/Business/Index';
import * as moment from 'moment';
import * as generalMasterBO from '../../GeneralMaster/Business/Index';
import * as generalBO from '../../General/Business/Index';
import * as userbo from '../../SystemSettings/Business/Index';
import { UserFilters } from '../../SystemSettings/Common/Filters.e';
import { join } from 'path';
import * as _ from 'lodash';
import * as invbo from '../../Pharmacy/Business/Index';
import * as otmbo from '../../OtManagement/Business/Index';
import * as generalMasterBo from '../../GeneralMaster/Business/Index';
import { ReferenceValueFilters } from '../../SystemSettings/Common/Filters.e';
import { UserAttributes } from '../../SystemSettings/Model/Interface/Index';
import { PatientAttributes } from '../../Registration/Model/Interface/Index';
import * as StoreBo from '../../Pharmacy/Business/Index';
import {
    StoreMasterFilters
} from '../../Pharmacy/Common/Filters.e';
import {
    ReferralFilters,
} from '../../GeneralMaster/Common/Filters.e';
import { PatientGuarantorFilters } from '../../Registration/Common/Filters.e';
import * as vhcareBo from '../../VirtualHealthcare/Business/Index';
import { VirtualOrderFilters, VirtualOrderDetailFilters } from '../../VirtualHealthcare/Common/Filters.e';
// import * as docbo from '../../DoctorInvoice/Business/Index';
// import { DoctorPaymentFilters } from '../../DoctorInvoice/Common/Filters.e';
import * as BillingBo from './Index';
import { NotificationService } from '../../../Notification/OneSignalNotification';
// import * as facilityBo from '../../SystemSettings/Business/Index';
import { WhatsappNotificationService } from '../../../WhatsappNotification/WhatsappNotification';
// const logger = require('../../../Core/Util/Logger').logger;
export class PatientBillsBo extends BaseBo<PatientBillsInstance, PatientBillsAttributes> {
    public async AddPatientBills(req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 1) {
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }
            req.Data.Header.BillNumber = null;
            if (!req.Data.Header.BillDateTime) {
                req.Data.Header.BillDateTime = new Date();
            }
        }
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptNumber = null; // await Sequence.Next(SequenceKeys.ReceiptNrId);
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
                req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;
            }
        }
        if (req.Data.Header.EncounterTypeId === 4) {
            let encounterbo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
            let item = req.Data.Header;
            let encounter: any = {
                Data: {
                    PatientId: item.PatientId,
                    EncounterTypeId: item.EncounterTypeId,
                    PatientMrn: item.MRN,
                    DoctorId: item.DoctorId,
                    DepartmentId: item.DepartmentId,
                    ReferralId: item.ReferralId,
                    GuarantorId: item.GuarantorId,
                    FacilityId: item.FacilityId
                }
            };
            req.Data.Header.EncounterId = await encounterbo.AddEncounter(encounter);
        }

        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);


        // if (req.Data.encounter.IsPackageAssigned) {
        //     if(req.Data.Header.IsInclusionItem){

        //     }
        //     let patientbillpackagesummarybo = BoFactory.GetBo(BillingBo.PatientBillPackageSummaryBo, this.Request);
        //     let item = req.Data.encounterippackage;
        //     let encounterippackage: any = {
        //         Data: {
        //             EncounterId: item.EncounterId,
        //             IPPackageId: item.IPPackageId,
        //             EncounterIPPackageId: item.Id,
        //             DoctorId: item.DoctorId,
        //             PackageName: item.PackageName,
        //             ServiceGroupId: item.ServiceGroupId,
        //             ServiceCategoryId: item.ServiceCategoryId,
        //             ExclusionAmount: item.FacilityId
        //         }
        //     };
        //     req.Data.Header.EncounterId = await encounterbo.AddEncounter(encounter);
        // }
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let PatientBillId = result.dataValues.Id;

            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                for (let ii = 0, len1 = req.Data.Details.length; ii < len1; ii++) {
                    req.Data.Details[ii].OrderStatusId = 1;
                    req.Data.Details[ii].BillDateTime = req.Data.Header.BillDateTime;
                }
            }

            if (req.Data.Header.IsFromIPBill) {
                let IPDetails: any = [];
                for (let ipindx in req.Data.Details) {
                    let detail = req.Data.Details[ipindx];
                    let IPDetail = {
                        Id: detail.Id,
                        IPBillDetailId: detail.Id,
                        Rate: 0,
                        Quantity: 0,
                        DiscountAmount: 0,
                        Amount: 0,
                        GrossAmount: 0,
                        NetAmount: 0
                    };
                    IPDetails.push(IPDetail);
                }
                if (IPDetails.length > 0) {
                    await detailBO.UpdateIPBillTOOPBillDetails(IPDetails);
                }
                if (req.Data.Header.BillTypeId === 3) {
                    await this.PopulateInpatientBills(req);
                }


                for (let ipindx in req.Data.Details) {
                    req.Data.Details[ipindx].Id = 0;
                    req.Data.Details[ipindx].BillDateTime = new Date();
                }
            }

            await this.setAliasName(req);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                try {
                    await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                } catch (ex) { throw { message: 'Error in Generating Receipt' }; }

                // let TransactionId = Number(PatientBillId);
                // let groupInstance = await PaymentdetailBO.Find({
                //     where: { PatientBillId: TransactionId }
                // });
                // if (!groupInstance) {
                //     throw { message: 'Error in Generating Receipt' };
                // }
                if (req.Data.Header.BillTypeId === 1) {
                    await this.sendOPBillReceiptSMS(PatientBillId, req);
                }
                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.BillTypeId === 1) {
                    if (req.Data.adjustmentDetail.length > 0) {
                        IsAdvanceAdjusting = 1;
                        AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

                        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                            if (req.Data.paymentDetail[i].Id > 0) {
                                for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                    req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                }
                            }
                        }

                        let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                        await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                    } else {
                        PaidAgainstBill = Number(req.Data.Header.PaidAmount);
                        DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                    }
                    let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                    let patientaccount = {};
                    patientaccount = {
                        Id: 0,
                        PatientId: req.Data.Header.PatientId,
                        EncounterId: req.Data.Header.EncounterId,
                        VisitNumber: req.Data.Header.VisitNumber,
                        TransactionTypeId: 2,
                        TransactionId: PatientBillId,
                        TransactionNumber: null, // req.Data.Header.BillNumber,
                        TransactionDate: new Date(),
                        BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
                        PaidAmount: PaidAgainstBill,
                        IsAdvanceAdjusted: IsAdvanceAdjusting,
                        AdjustedAmount: AdjustedAgainstBill,
                        UnAdjustedAmount: 0,
                        DueAmount: DueTowardsBill,
                        DebitAmount: 0,
                        CreditAmount: 0
                    };
                    await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                }
                if (!req.Data.Header.IsDietBill) {
                    if (req.Data.Header.LoadFrom && req.Data.Header.PatientBillStatusId === 3) {
                        if (req.Data.Header.LoadFrom.toLowerCase() === 'orderbills') {
                            await this.CreateOrUpdateOrder(PatientBillId, req.Data, req.Data.Header.BillNumber);
                        }
                    }
                }
            }

            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                let facilityId = (req.Data.Header.FacilityId) ? req.Data.Header.FacilityId : -1;
                if (req.Data.Header.BillTypeId === 1) {
                    // billIdentifier = this.getSequenceIdentifier(SequenceKeys.OPBillNumberId);
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.OPBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 2) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPFinalBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 5) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.DGBillNumberId, facilityId);
                }
            }

            let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
            let billingPreferencesData =
                await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
            let isopcreditseq = 0;
            try {
                isopcreditseq = parseInt(billingPreferencesData.opcreditseq);
            } catch (ex) { isopcreditseq = 0; }
            if (isopcreditseq) {
                if (req.Data.Header.BillTypeId === 1 && req.Data.Header.GuarantorTypeId > 1) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.OPCreditBillNumberId, req.Data.Header.FacilityId);
                }
            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            updateOrderInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdateOrderInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdateOrderInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdatePatientAccountsInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdatePatientAccountsInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            SendBillDiscountSMS: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.BillNum = code;
                                    await bo.SendBillDiscountSMS(request);
                                } catch (error) {
                                    console.error('Error in SendBillDiscountSMS:', error);
                                    throw error;  // Handle the error as needed
                                }
                            }
                        };
                    })(this, req, PatientBillId);
                };

                // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                //     [
                //         afterO().updateOrderInfo,
                //         afterO().updatePatientAccountsInfo,
                //         afterO().SendBillDiscountSMS,
                //     ]);
                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().updateOrderInfo,
                                afterO().updatePatientAccountsInfo,
                                afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                handleDeferredExecution();

            }

            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.EncounterId) {
                    let encounterBO = BoFactory.GetBo(encbo.EncounterBo, this.Request);
                    await encounterBO.UpdateIsBillCompleted(1, req.Data.Header.EncounterId);
                }
            }
            const patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
            const patient: any = await patientBO.GetById(req.Data.Header.PatientId);

            if (patient && (patient.NotificationToken)) {
                /* tslint:disable-next-line */
                const pushMessage: string = 'Dear ' + patient.FirstName + ', ' + ' Your OP bill is created,have look in DrHMS' + '.';
                const notificationService: any = new NotificationService();
                const body = {
                    type: 'appoinment_booking',
                };
                const pushTokens: string[] = [];
                if (patient.NotificationToken) {
                    pushTokens.push(patient.NotificationToken);
                }
                await notificationService.sendNotification(pushMessage, pushTokens, body);
                console.log('*************************pushMessage**********************', pushMessage);
                console.log('*************************pushTokens**********************', pushTokens);
            }
            return PatientBillId;
        }

        return 0;
    }

    public async AddIPPatientBills(req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 1) {
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }
            req.Data.Header.BillNumber = null;
            // req.Data.Header.BillDateTime = new Date();
            if (!req.Data.Header.BillDateTime) {
                req.Data.Header.BillDateTime = new Date();
            }
        }
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptNumber = null; // await Sequence.Next(SequenceKeys.ReceiptNrId);
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
                req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;
            }
        }
        if (req.Data.Header.EncounterTypeId === 4) {
            let encounterbo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
            let item = req.Data.Header;
            let encounter: any = {
                Data: {
                    PatientId: item.PatientId,
                    EncounterTypeId: item.EncounterTypeId,
                    PatientMrn: item.MRN,
                    DoctorId: item.DoctorId,
                    DepartmentId: item.DepartmentId,
                    ReferralId: item.ReferralId,
                    GuarantorId: item.GuarantorId,
                    FacilityId: item.FacilityId
                }
            };
            req.Data.Header.EncounterId = await encounterbo.AddEncounter(encounter);
        }
        if (!req.Data.Header.allowDuplicate)
            if (await this.IsAlreadyExist(req) <= -1) return -1;

        // if (req.Data.Header && req.Data.Header.GuarantorId === 1) {
        //     if (await this.IsBillCashExceed(req.Data.Header.EncounterId) <= -1) return -1;
        // }
        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);
        // if (req.Data.encounter.IsPackageAssigned) {
        //     if(req.Data.Header.IsInclusionItem){

        //     }
        //     let patientbillpackagesummarybo = BoFactory.GetBo(BillingBo.PatientBillPackageSummaryBo, this.Request);
        //     let item = req.Data.encounterippackage;
        //     let encounterippackage: any = {
        //         Data: {
        //             EncounterId: item.EncounterId,
        //             IPPackageId: item.IPPackageId,
        //             EncounterIPPackageId: item.Id,
        //             DoctorId: item.DoctorId,
        //             PackageName: item.PackageName,
        //             ServiceGroupId: item.ServiceGroupId,
        //             ServiceCategoryId: item.ServiceCategoryId,
        //             ExclusionAmount: item.FacilityId
        //         }
        //     };
        //     req.Data.Header.EncounterId = await encounterbo.AddEncounter(encounter);
        // }
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let PatientBillId = result.dataValues.Id;

            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                for (let ii = 0, len1 = req.Data.Details.length; ii < len1; ii++) {
                    req.Data.Details[ii].OrderStatusId = 1;
                    req.Data.Details[ii].BillDateTime = req.Data.Header.BillDateTime;
                }
            }

            if (req.Data.Header.IsFromIPBill) {
                let IPDetails: any = [];
                for (let ipindx in req.Data.Details) {
                    let detail = req.Data.Details[ipindx];
                    let IPDetail = {
                        Id: detail.Id,
                        IPBillDetailId: detail.Id,
                        Rate: 0,
                        Quantity: 0,
                        DiscountAmount: 0,
                        Amount: 0,
                        GrossAmount: 0,
                        NetAmount: 0
                    };
                    IPDetails.push(IPDetail);
                }
                if (IPDetails.length > 0) {
                    await detailBO.UpdateIPBillTOOPBillDetails(IPDetails);
                }
                // if (req.Data.Header.BillTypeId === 3) {
                //     await this.PopulateInpatientBills(req);
                // }
                for (let ipindx in req.Data.Details) {
                    req.Data.Details[ipindx].Id = 0;
                    req.Data.Details[ipindx].BillDateTime = new Date();
                }
            }

            if (req.Data.Header.updateOccupancy && req.Data.Header.updateOccupancy > 0) {
                let OccupancyBo = BoFactory.GetBo(inpatientBO.BedOccupancyHistoryBo, this.Request);
                let OccupancyData: any = {
                    Id: req.Data.Header.updateOccupancy,
                    PatientBillId: PatientBillId
                };
                await OccupancyBo.Update(OccupancyData);
            }

            await this.setAliasName(req);
            await detailBO.ManageIPatientBillDetails(PatientBillId, req.Data.Details);
            let encId = req.Data.Header.EncounterId;
            let IsPatientTransfer = false;
            IsPatientTransfer = req.Data.Header.IsPatientTransfer;
            if (!req.Data.Header.IsDayCare) {
                if (req.Data.Header.IsDayCare === 0)
                    await billSummBo.ManagePatBillSummary(IsPatientTransfer, encId, req.Data.Details);
            }

            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                if (req.Data.Header.BillTypeId === 1) {
                    await this.sendOPBillReceiptSMS(PatientBillId, req);
                }
                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.BillTypeId === 1) {
                    if (req.Data.adjustmentDetail.length > 0) {
                        IsAdvanceAdjusting = 1;
                        AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

                        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                            if (req.Data.paymentDetail[i].Id > 0) {
                                for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                    req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                }
                            }
                        }

                        let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                        await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                    } else {
                        PaidAgainstBill = Number(req.Data.Header.PaidAmount);
                        DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                    }
                    let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                    let patientaccount = {};
                    patientaccount = {
                        Id: 0,
                        PatientId: req.Data.Header.PatientId,
                        EncounterId: req.Data.Header.EncounterId,
                        VisitNumber: req.Data.Header.VisitNumber,
                        TransactionTypeId: 2,
                        TransactionId: PatientBillId,
                        TransactionNumber: null, // req.Data.Header.BillNumber,
                        TransactionDate: new Date(),
                        BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
                        PaidAmount: PaidAgainstBill,
                        IsAdvanceAdjusted: IsAdvanceAdjusting,
                        AdjustedAmount: AdjustedAgainstBill,
                        UnAdjustedAmount: 0,
                        DueAmount: DueTowardsBill,
                        DebitAmount: 0,
                        CreditAmount: 0
                    };
                    await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                }
                if (!req.Data.Header.IsDietBill) {
                    await this.CreateOrUpdateOrder(PatientBillId, req.Data, req.Data.Header.BillNumber);
                }
            }

            let billIdentifier: any = null;
            let facilityId = (req.Data.Header.FacilityId) ? req.Data.Header.FacilityId : -1;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 1) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.OPBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 2) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPFinalBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 5) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.DGBillNumberId, facilityId);
                }
            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            updateOrderInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdateOrderInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdateOrderInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdatePatientAccountsInfo(request);
                                } catch (error) {
                                    console.error('Error in updatePatientAccountsInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            SendBillDiscountSMS: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.BillNum = code;
                                    await bo.SendBillDiscountSMS(request);
                                } catch (error) {
                                    console.error('Error in SendBillDiscountSMS:', error);
                                    throw error;  // Handle the error as needed
                                }
                            }
                        };
                    })(this, req, PatientBillId);
                };

                // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                //     [
                //         afterO().updateOrderInfo,
                //         afterO().updatePatientAccountsInfo,
                //         afterO().SendBillDiscountSMS,
                //     ]);

                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().updateOrderInfo,
                                afterO().updatePatientAccountsInfo,
                                afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                handleDeferredExecution();
            }

            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.EncounterId) {
                    let encounterBO = BoFactory.GetBo(encbo.EncounterBo, this.Request);
                    await encounterBO.UpdateIsBillCompleted(1, req.Data.Header.EncounterId);
                }
            }

            if (req.Data.Header && req.Data.Header.updateBillLock && req.Data.Header.updateBillLock === 1) {
                let encounterbo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
                let IsbillLock: any = { IsBillLock: true, IsAutoBillLock: true };
                await encounterbo.Update(IsbillLock, {
                    fields: ['IsBillLock', 'IsAutoBillLock'],
                    where: {
                        Id: req.Data.Header.EncounterId
                    }
                });
            }

            return PatientBillId;
        }

        return 0;
    }

    public async AddBedChargePatientBills(req: BaseRequest): Promise<number> {
        console.log('*********Bed Charges**************');
        console.log(req.Data.Header.Id);
        req.Data.Header.Id = 0;
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 1) {
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }
            req.Data.Header.BillNumber = null;
            // req.Data.Header.BillDateTime = new Date();
            if (!req.Data.Header.BillDateTime) {
                req.Data.Header.BillDateTime = new Date();
            }
        }
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptNumber = null; // await Sequence.Next(SequenceKeys.ReceiptNrId);
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
                req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;
            }
        }
        // if (req.Data.Header.EncounterTypeId === 4) {
        //     let encounterbo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        //     let item = req.Data.Header;
        //     let encounter: any = {
        //         Data: {
        //             PatientId: item.PatientId,
        //             EncounterTypeId: item.EncounterTypeId,
        //             PatientMrn: item.MRN,
        //             DoctorId: item.DoctorId,
        //             DepartmentId: item.DepartmentId,
        //             ReferralId: item.ReferralId,
        //             GuarantorId: item.GuarantorId,
        //             FacilityId: item.FacilityId
        //         }
        //     };
        //     req.Data.Header.EncounterId = await encounterbo.AddEncounter(encounter);
        // }
        if (!req.Data.Header.allowDuplicate)
            if (await this.IsAlreadyExist(req) <= -1) return -1;
        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);

        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let PatientBillId = result.dataValues.Id;

            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                for (let ii = 0, len1 = req.Data.Details.length; ii < len1; ii++) {
                    console.log('************BIll DateTime*********');
                    console.log(req.Data.Header.BillDateTime);
                    req.Data.Details[ii].OrderStatusId = 1;
                    req.Data.Details[ii].BillDateTime = req.Data.Header.BillDateTime;
                    req.Data.Details[ii].Id = 0;
                }
            }

            // if (req.Data.Header.IsFromIPBill) {
            //     let IPDetails: any = [];
            //     for (let ipindx in req.Data.Details) {
            //         let detail = req.Data.Details[ipindx];
            //         let IPDetail = {
            //             Id: detail.Id,
            //             IPBillDetailId: detail.Id,
            //             Rate: 0,
            //             Quantity: 0,
            //             DiscountAmount: 0,
            //             Amount: 0,
            //             GrossAmount: 0,
            //             NetAmount: 0
            //         };
            //         IPDetails.push(IPDetail);
            //     }
            //     if (IPDetails.length > 0) {
            //         await detailBO.UpdateIPBillTOOPBillDetails(IPDetails);
            //     }
            //     // if (req.Data.Header.BillTypeId === 3) {
            //     //     await this.PopulateInpatientBills(req);
            //     // }
            //     for (let ipindx in req.Data.Details) {
            //         req.Data.Details[ipindx].Id = 0;
            //         req.Data.Details[ipindx].BillDateTime = new Date();
            //     }
            // }

            if (req.Data.Header.updateOccupancy && req.Data.Header.updateOccupancy > 0) {
                let OccupancyBo = BoFactory.GetBo(inpatientBO.BedOccupancyHistoryBo, this.Request);
                let OccupancyData: any = {
                    Id: req.Data.Header.updateOccupancy,
                    PatientBillId: PatientBillId
                };
                await OccupancyBo.Update(OccupancyData);
            }

            // await this.setAliasName(req);
            await detailBO.ManagePatientClinicBillDetails(PatientBillId, req.Data.Details);
            let encId = req.Data.Header.EncounterId;
            let IsPatientTransfer = false;
            IsPatientTransfer = req.Data.Header.IsPatientTransfer;
            await billSummBo.ManagePatBillSummary(IsPatientTransfer, encId, req.Data.Details);
            // if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
            //     let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
            //     await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
            //     if (req.Data.Header.BillTypeId === 1) {
            //         await this.sendOPBillReceiptSMS(PatientBillId, req);
            //     }
            //     let IsAdvanceAdjusting = 0;
            //     let PaidAgainstBill = 0;
            //     let AdjustedAgainstBill = 0;
            //     let DueTowardsBill = 0;
            //     if (req.Data.Header.BillTypeId === 1) {
            //         if (req.Data.adjustmentDetail.length > 0) {
            //             IsAdvanceAdjusting = 1;
            //             AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

            //             for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            //                 if (req.Data.paymentDetail[i].Id > 0) {
            //                     for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
            //                         req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
            //                     }
            //                 }
            //             }

            //             let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
            //             await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
            //         } else {
            //             PaidAgainstBill = Number(req.Data.Header.PaidAmount);
            //             DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
            //         }
            //         let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
            //         let patientaccount = {};
            //         patientaccount = {
            //             Id: 0,
            //             PatientId: req.Data.Header.PatientId,
            //             EncounterId: req.Data.Header.EncounterId,
            //             VisitNumber: req.Data.Header.VisitNumber,
            //             TransactionTypeId: 2,
            //             TransactionId: PatientBillId,
            //             TransactionNumber: null, // req.Data.Header.BillNumber,
            //             TransactionDate: new Date(),
            //             BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
            //             PaidAmount: PaidAgainstBill,
            //             IsAdvanceAdjusted: IsAdvanceAdjusting,
            //             AdjustedAmount: AdjustedAgainstBill,
            //             UnAdjustedAmount: 0,
            //             DueAmount: DueTowardsBill,
            //             DebitAmount: 0,
            //             CreditAmount: 0
            //         };
            //         await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
            //     }
            //     if (!req.Data.Header.IsDietBill) {
            //         await this.CreateOrUpdateOrder(PatientBillId, req.Data, req.Data.Header.BillNumber);
            //     }
            // }

            let billIdentifier: any = null;
            let facilityId = (req.Data.Header.FacilityId) ? req.Data.Header.FacilityId : -1;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 1) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.OPBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 2) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPFinalBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId, facilityId);
                } else if (req.Data.Header.BillTypeId === 5) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.DGBillNumberId, facilityId);
                }
            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            updateOrderInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdateOrderInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdateOrderInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdatePatientAccountsInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdatePatientAccountsInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            SendBillDiscountSMS: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.BillNum = code;
                                    await bo.SendBillDiscountSMS(request);
                                } catch (error) {
                                    console.error('Error in SendBillDiscountSMS:', error);
                                    throw error;  // Handle the error as needed
                                }
                            }
                        };
                    })(this, req, PatientBillId);
                };

                // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                //     [
                //         afterO().updateOrderInfo,
                //         afterO().updatePatientAccountsInfo,
                //         afterO().SendBillDiscountSMS,
                //     ]);

                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().updateOrderInfo,
                                afterO().updatePatientAccountsInfo,
                                afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                handleDeferredExecution();
            }

            // if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
            //     if (req.Data.Header.EncounterId) {
            //         let encounterBO = BoFactory.GetBo(encbo.EncounterBo, this.Request);
            //         await encounterBO.UpdateIsBillCompleted(1, req.Data.Header.EncounterId);
            //     }
            // }

            return PatientBillId;
        }

        return 0;
    }

    public async AddClinicDraftBills(req: BaseRequest): Promise<any> {
        let apnmntInfo = req.Data;
        let virtualOrderData: any;
        let Pbilldata: any;
        let apiReqvirtualorder = {
            Id: 0,
            PageContext: { PageSize: 50, PageNumber: 1 },
            Params: [
                { Key: VirtualOrderFilters.Id, Value: req.Data.VirtualOrderId },
            ]
        };
        let VhorderBO = BoFactory.GetBo(vhcareBo.VirtualOrderBo, this.Request);
        let virtualOrderlist = await VhorderBO.GetVirtualOrders(apiReqvirtualorder);
        virtualOrderData = virtualOrderlist.Data[0];
        let billData: any = {
            Data: {
                Id: 0,
                BillNumber: '',
                BillDateTime: new Date(),
                BillAmount: virtualOrderData.OrderTotal,
                PaidAmount: 0.000,
                IsPaidFully: false,
                OutStandingAmount: virtualOrderData.OrderTotal,
                BillGeneratedBy: this.Session.UserId,
                OrganizationId: this.Session.OrganizationId,
                FacilityId: this.Session.FacilityId,
                PatientId: apnmntInfo.PatientId,
                PatientName: apnmntInfo.PatientName,
                PatientMrn: apnmntInfo.PatientMrn,
                DoctorId: apnmntInfo.DoctorId,
                DoctorName: apnmntInfo.DoctorName,
                PatientBillStatusId: 1,
                IsVirtualOrders: true,
                AppointmentId: apnmntInfo.Id,
                IsClinicalBills: true
            }
        };
        Pbilldata = await this.Save(billData.Data);
        let pbillId = Pbilldata.Id;
        let BillDetails: any = [];

        let apiReqvirtualorderDet = {
            Id: 0,
            PageContext: { PageSize: 50, PageNumber: 1 },
            Params: [
                { Key: VirtualOrderDetailFilters.VirtualOrderId, Value: req.Data.VirtualOrderId },
            ]
        };
        let VhorderDetBO = BoFactory.GetBo(vhcareBo.VirtualOrderDetailBo, this.Request);
        let virtualOrderDetlist = await VhorderDetBO.GetVirtualOrderDetails(apiReqvirtualorderDet);

        for (let jdx in virtualOrderDetlist.Data) {
            let vdetails = virtualOrderDetlist.Data[jdx];
            let detData: any = {
                Id: 0,
                PatientBillId: pbillId,
                BillDateTime: new Date(),
                ServiceId: vdetails.ServiceId,
                ServiceName: vdetails.ServiceName,
                PatientBillStatusId: 1,
                Quantity: vdetails.Quantity,
                Rate: virtualOrderData.OrderTotal,
                Amount: virtualOrderData.OrderTotal,
                GrossAmount: virtualOrderData.OrderTotal,
                NetAmount: virtualOrderData.OrderTotal,
                ReceivedAmount: 0.00,
            };
            BillDetails.push(detData);
        }
        let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        await detailBO.ManagePatientClinicBillDetails(pbillId, BillDetails);
        return pbillId;
    }

    public async UpdatePatientBedCharges(req: BaseRequest): Promise<boolean> {
        let result = await this.Update(req.Data.Header);
        let PatientBillId = req.Data.Header.Id;
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let encId = req.Data.Header.EncounterId;
            await billSummBo.ManageAutoPatBillSummary(encId, req.Data.Details);
        }
        return true;
    }

    public async UpdateBillDiscount(req: BaseRequest): Promise<boolean> {
        let result = await this.Update(req.Data.Header);
        let PatientBillId = req.Data.Header.Id;

        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            // let detailReq = {
            //     Id: 0,
            //     PageContext: { PageSize: -1, PageNumber: 1 },
            //     Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: PatientBillId },
            //     { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
            // };
            // let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
            // let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
            // if (PatientPaymentDetailsData.Data.length > 0) {
            //     for (let idx in PatientPaymentDetailsData.Data) {
            //         let payData = PatientPaymentDetailsData.Data[idx];
            //         let updatepayInfo: any = {
            //             Id: payData.Id,
            //             AmountPaid: req.Data.Header.PaidAmount
            //         };
            //         await PatientPaymentDetailsBo.Update(updatepayInfo);
            //     }
            // }
            if (req.Data.Header.IsRefund) {
                let refundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
                let refundDetails: any = [];
                refundDetails.push({
                    Id: 0,
                    RefundDetailsDateTime: new Date(),
                    // PatientRefundId: number;
                    FacilityId: req.Data.Header.FacilityId,
                    // OrganizationId: number;
                    PatientId: req.Data.Header.PatientId,
                    EncounterId: req.Data.Header.EncounterId,
                    EncounterTypeId: req.Data.Header.EncounterTypeId,
                    RefundAmount: req.Data.Header.ReturnedAmount,
                    // DepartmentID: number;
                    // PaymentcounterID: number;
                    // DoctorId: number;
                    // ServiceId: number;
                    // ServiceName: string;
                    PatientBillId: PatientBillId,
                    // PharmacyReturnId: number;
                    // Comments: string;
                    // RoundOffValue: number;
                });
                let refundInfo: any = {
                    Data: {
                        Header: {
                            Id: 0,
                            RefundDateTime: new Date(),
                            // RefundIdentifier: '',
                            FacilityId: req.Data.Header.FacilityId,
                            //OrganizationId: number;
                            PatientId: req.Data.Header.PatientId,
                            RefundTypeId: 4,
                            RefundStatusId: 1,
                            EncounterId: req.Data.Header.EncounterId,
                            EncounterTypeId: req.Data.Header.EncounterTypeId,
                            PatientName: req.Data.Header.PatientName,
                            // PatientReceiptId: number;
                            RefundAmount: req.Data.Header.ReturnedAmount,
                            // DepartmentID: number;
                            //  PaymentcounterID: number;
                            // GuarantorId: number;
                            // GuarantorTypeId: number;
                            RefundGeneratedById: req.Data.Header.BillGeneratedBy,
                            RefundApprovedById: req.Data.Header.BillGeneratedBy,
                            // DoctorId: req.Dat;
                            PatientBillId: PatientBillId,
                            PaymentTypeId: 1
                            //PharmacyReturnId: number;
                            // CardNumber: paymentInfo.CardNumber,
                            // // CardDateTime: paymentInfo.CardDateTime,
                            // //CardExpiryDate: paymentInfo.CardExpiryDate,
                            // BankId: paymentInfo.BankId,
                            // CardTypeId: paymentInfo.CardTypeId,
                            // TerminalNoId: paymentInfo.TerminalNoId,
                            // //PatientCreditNoteId: paymentInfo.PatientCreditNoteId,
                            // // CardHolderName: string;
                            // // AuthorizeNumber: number;
                            // // GurantorName: string;
                            // ChequeNo: paymentInfo.ChequeNo,
                            // ChequeDate: paymentInfo.ChequeDate,
                            // DDNumber: paymentInfo.DDNumber,
                            // DDDate: paymentInfo.DDDate,
                            // WireTransferId: paymentInfo.WireTransferId,
                            // WireTransferDate: paymentInfo.WireTransferDate,
                            // // Comments: string;
                            // //CancelReason: string;
                            // RefundStatusId: 1,

                            // PaymentTypeId: paymentInfo.PaymentTypeId
                            // RoundOffValue: number;
                            //DebitNoteId: number;
                            // Status: number;
                            //  Rev: number;
                            //  CreatedBy: number;
                            // CreatedAt: Date;
                            // UpdatedBy: number;
                            // UpdatedAt: Date; },

                        },
                        Details: refundDetails
                    }
                };
                await refundBo.AddPatientRefund(refundInfo);
                // let refInfo: any = {
                //     Id: 0,
                //     RefundDateTime: new Date(),
                //     RefundIdentifier: '',
                //     FacilityId: req.Data.Header.FacilityId,
                //     RefundTypeId: 4,
                //     PatientId: req.Data.Header.PatientId,
                //     EncounterId: req.Data.Header.EncounterId,
                //     EncounterTypeId: req.Data.Header.EncounterTypeId,
                //     PatientName: req.Data.Header.PatientName,
                //     RefundAmount: req.Data.Header.ReturnedAmount,
                //     RefundGeneratedById: this.Session.UserId,
                //     PatientBillId: PatientBillId,
                //     RefundStatusId: 1,
                //     PaymentTypeId: 1
                // };
                // await PatientRefundBo.Save(refInfo);
            }
        }
        return true;
    }

    public async AddPatientStaffBills(req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 6) {
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
            if (req.Data.Header.EncounterTypeId && req.Data.Header.PharmacySaleTypeId) {
                if (req.Data.Header.EncounterTypeId === 2
                    && req.Data.Header.PharmacySaleTypeId === 6) {
                    req.Data.Header.ToStoreId = req.Data.Header.StoreMasterId;
                    let detailnew: any = [];
                    let perscreq: any = {
                        Data: {
                            Header: req.Data.Header,
                            Details: detailnew
                        }
                    };
                    let details = req.Data.Details;
                    for (let i = 0, len = details.length; i < len; i++) {
                        if (details[i].SubCategoryId) {
                            if (details[i].SubCategoryId === 1) {
                                details[i].RequestedQuantity = details[i].Quantity;
                                details[i].DrugId = details[i].ItemMasterId;
                                perscreq.Data.Details.push(details[i]);
                            }
                        }
                    }
                    if (perscreq.Data.Details.length > 0) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.CreatePrescription(perscreq);
                        for (let pre = 0, len = req.Data.Details.length; pre < len; pre++) {
                            req.Data.Details[pre].Id = 0;
                        }
                    }
                }
            }
        }
        let BillPaymentTypeId = -1;
        let result = await this.Save(req.Data.Header);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {

                if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {
                    if (req.Data.Details.length > 0) {
                        for (let k = 0, len = req.Data.Details.length; k < len; k++) {
                            if (req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
                                req.Data.Details.splice(k, 1);
                            }
                        }
                    }

                    let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
                    let stockResult = await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);
                    //console.log(stockResult);
                    if (stockResult && stockResult.length > 0) {
                        let errorMessages: any = [];
                        _.forEach(stockResult, (item: any) => { errorMessages.push(item.error.name); });
                        throw { message: 'Stock Changes Happened for ' + errorMessages.join('$,$') };
                    }

                    // let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
                    // await stockmovementBO.ManageStockMovements(21, PatientBillId, req.Data);

                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                    if (req.Data.Header.StaffId > 0) {
                        let userbo = BoFactory.GetBo(appbo.UserBo, this.Request);
                        let UserInfo = await userbo.GetUserById({ Id: req.Data.Header.StaffId });
                        let paymentRequest: any = {
                            Data: {
                                Id: UserInfo.Id,
                                // PatientId: req.Data.Header.PatientId,
                                BillAmount: (UserInfo.BillAmount) + (req.Data.Header.BillAmount),
                                PaidAmount: (UserInfo.PaidAmount) + (req.Data.Header.PaidAmount),
                                OutStandingAmount: (UserInfo.OutStandingAmount) + (req.Data.Header.OutStandingAmount),

                            }
                        };
                        await userbo.UpdateUserStaffBills(paymentRequest);
                    }

                }
            }

            if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;

            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 6) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId, req.Data.Header.FacilityId);
                }


                let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let billingPreferencesData =
                    await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                let iPharmacyPaymodeSeqNr = 0;
                try {
                    iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
                if (iPharmacyPaymodeSeqNr) {
                    if (BillPaymentTypeId > 0) {
                        if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                req.Data.Header.FacilityId);
                        } else {
                            if (req.Data.Header.PharmacySaleTypeId === 4) { //Manual-Sales - MAN10001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyManualBill,
                                    req.Data.Header.FacilityId);
                            } else { //Cash-Sales - CAS10001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCashBill,
                                    req.Data.Header.FacilityId);
                            }
                        }
                    } else { //Credit-Sales- (outstanding) - CRT10001
                        billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                            req.Data.Header.FacilityId);
                    }
                }

            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateMovementInfo: async (code: string) => {
                                request.Data.Header.TransactionId = patientBillId;
                                request.Data.Header.TransactionNumber = code;
                                await bo.UpdateMovementInfo(request);
                            },
                            // updatePatientAccountsInfo: async (code: string) => {
                            //     request.Data.Header.PatientBillId = patientBillId;
                            //     request.Data.Header.BillNumber = code;
                            //     await bo.UpdatePatientAccountsInfo(request);
                            // },
                            SendBillDiscountSMS: async (code: string) => {
                                request.Data.Header.PatientBillId = patientBillId;
                                request.Data.BillNum = code;
                                await bo.SendBillDiscountSMS(request);
                            }
                        };
                    })(this, req, PatientBillId);
                };

                this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                    [
                        afterO().UpdateMovementInfo,
                        // afterO().updatePatientAccountsInfo,
                        afterO().SendBillDiscountSMS,
                    ]);

            }
            return PatientBillId;
        }
        return 0;
    }

    public async UpdatePatientStaffBills(req: BaseRequest): Promise<boolean> {
        if (req.Data.Header.Id > 0 && !req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 6) {
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
        }
        let BillPaymentTypeId = -1;
        if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 2) {
            if (req.Data.Header.CancelReason)
                var CancelReason: string = req.Data.Header.CancelReason;
            req.Data.Header = await this.GetPatientBillsById({ Id: req.Data.Header.Id });
            req.Data.Header.PatientBillStatusId = 2;
            if (CancelReason)
                req.Data.Header.CancelReason = CancelReason;
        }
        let result = await this.Update(req.Data.Header);
        let PatientBillId = req.Data.Header.Id;
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {

                    let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
                    let stockResult = await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);
                    //console.log(stockResult);
                    if (stockResult && stockResult.length > 0) {
                        let errorMessages: any = [];
                        _.forEach(stockResult, (item: any) => { errorMessages.push(item.error.ItemMasterId + ':' + item.error.name); });
                        throw { message: 'Stock Changes Happened for ' + errorMessages.join('$,$') };
                    }

                    // let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
                    // await stockmovementBO.ManageStockMovements(21, PatientBillId, req.Data);

                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                }
            }

            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 6) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId, req.Data.Header.FacilityId);
                }


                let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let billingPreferencesData =
                    await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                let iPharmacyPaymodeSeqNr = 0;
                try {
                    iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
                if (iPharmacyPaymodeSeqNr) {
                    if (BillPaymentTypeId > 0) {
                        if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                req.Data.Header.FacilityId);
                        } else {
                            if (req.Data.Header.PharmacySaleTypeId === 4) { //Direct(Manual)-Sales - MAN0001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyManualBill,
                                    req.Data.Header.FacilityId);
                            } else { //Cash-Sales - CAS0001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCashBill,
                                    req.Data.Header.FacilityId);
                            }
                        }
                    } else {
                        if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                req.Data.Header.FacilityId
                            );
                        }
                    }
                }
            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateMovementInfo: async (code: string) => {
                                request.Data.Header.TransactionId = patientBillId;
                                request.Data.Header.TransactionNumber = code;
                                await bo.UpdateMovementInfo(request);
                            },
                            // updatePatientAccountsInfo: async (code: string) => {
                            //     request.Data.Header.PatientBillId = patientBillId;
                            //     request.Data.Header.BillNumber = code;
                            //     await bo.UpdatePatientAccountsInfo(request);
                            // },
                        };
                    })(this, req, PatientBillId);
                };

                this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                    [
                        afterO().UpdateMovementInfo,
                        // afterO().updatePatientAccountsInfo,
                    ]);

            }

            return PatientBillId;
        }
        return result;
    }
    public async GetInsuranceCreditSummary(req: BaseRequest): Promise<any> {
        let result: any = [];
        result.push({ Key: 1, Value: await this.InsurancePaymentDetails(req) });
        return result;
    }
    public async InsurancePaymentDetails(req: BaseRequest): Promise<any> {
        let GuarantorGroup: { [id: number]: any[] } = {};
        let GuarantorGroupJoin: any = {
            model: this.Models.Guarantor, as: 'GuarantorMaster',
            attributes: ['GuarantorName'],
            required: true,
        };
        if (req.Data.GuarantorId > 0) {
            let opcreditamountInstance: any = await this.FindAll({
                attributes: ['PaidAmount', 'GuarantorId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [1, 5] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$eq': req.Data.GuarantorId },
                    FacilityId: req.Data.FacilityId
                },
                include: [GuarantorGroupJoin]
            });
            if (opcreditamountInstance) {
                let groupbills = _.groupBy(opcreditamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let PaidAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        PaidAmount += bills.PaidAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'OPCredit': PaidAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        } else if (req.Data.GuarantorId === 0) {
            let opcreditamountInstance: any = await this.FindAll({
                attributes: ['PaidAmount', 'GuarantorId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [1, 5] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': req.Data.GuarantorId },
                    FacilityId: req.Data.FacilityId
                },
                include: [GuarantorGroupJoin]
            });
            if (opcreditamountInstance) {
                let groupbills = _.groupBy(opcreditamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let PaidAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        PaidAmount += bills.PaidAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'OPCredit': PaidAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        }
        if (req.Data.GuarantorId > 0) {
            let ipcreditamountInstance: any = await this.FindAll({
                attributes: ['PaidAmount', 'GuarantorId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$eq': req.Data.GuarantorId },
                    FacilityId: req.Data.FacilityId
                },
                include: [GuarantorGroupJoin]
            });
            if (ipcreditamountInstance) {
                let groupbills = _.groupBy(ipcreditamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let PaidAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        PaidAmount += bills.PaidAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'IPCredit': PaidAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        } else if (req.Data.GuarantorId === 0) {
            let ipcreditamountInstance: any = await this.FindAll({
                attributes: ['PaidAmount', 'GuarantorId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': req.Data.GuarantorId },
                    FacilityId: req.Data.FacilityId
                },
                include: [GuarantorGroupJoin]
            });
            if (ipcreditamountInstance) {
                let groupbills = _.groupBy(ipcreditamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let PaidAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        PaidAmount += bills.PaidAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'IPCredit': PaidAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        }
        if (req.Data.GuarantorId > 0) {
            let totaloutstandingamountInstance: any = await this.FindAll({
                attributes: ['PaidAmount', 'GuarantorId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [1, 2, 5] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$eq': req.Data.GuarantorId },
                    FacilityId: req.Data.FacilityId
                },
                include: [GuarantorGroupJoin]
            });

            if (totaloutstandingamountInstance) {
                let groupbills = _.groupBy(totaloutstandingamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'TotalOutstanding': OutStandingAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        } else if (req.Data.GuarantorId === 0) {
            let totaloutstandingamountInstance: any = await this.FindAll({
                attributes: ['PaidAmount', 'GuarantorId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [1, 2, 5] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': req.Data.GuarantorId },
                    FacilityId: req.Data.FacilityId
                },
                include: [GuarantorGroupJoin]
            });

            if (totaloutstandingamountInstance) {
                let groupbills = _.groupBy(totaloutstandingamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'TotalOutstanding': OutStandingAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        }
        return GuarantorGroup;
    }
    public async GetInsuranceOutstandingSummary(req: BaseRequest): Promise<any> {
        let result: any = [];
        result.push({ Key: 1, Value: await this.InsuranceOutstandingDetails(req) });
        return result;
    }
    public async InsuranceOutstandingDetails(req: BaseRequest): Promise<any> {
        let GuarantorGroup: { [id: number]: any[] } = {};
        let GuarantorGroupJoin: any = {
            model: this.Models.Guarantor, as: 'GuarantorMaster',
            attributes: ['GuarantorName', 'GuarantorTypeId'],
            required: true,
        };
        let GuarantorTypeJoin: any = this.GetReference('GuarantorType');
        if (req.Data.GuarantorTypeId > -1) {
            let opoutstandingamountInstance: any = await this.FindAll({
                attributes: ['GuarantorId', 'GuarantorTypeId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [1, 5] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': 0 },
                    FacilityId: req.Data.FacilityId,
                    // IsPaidFully: false,
                    GuarantorTypeId: { '$eq': req.Data.GuarantorTypeId }
                },
                include: [GuarantorGroupJoin, GuarantorTypeJoin]
            });
            if (opoutstandingamountInstance) {
                let groupbills = _.groupBy(opoutstandingamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    let GuarantorType: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorType = bills.GuarantorType.Description;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'OPOutstanding': OutStandingAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName,
                        'GuarantorType': GuarantorType,
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        } else if (req.Data.GuarantorTypeId === -1) {
            let opoutstandingamountInstance: any = await this.FindAll({
                attributes: ['GuarantorId', 'OutStandingAmount', 'GuarantorTypeId'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [1, 5] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': 0 },
                    FacilityId: req.Data.FacilityId,
                    // IsPaidFully: false,
                    GuarantorTypeId: { '$gt': 1 }

                },
                include: [GuarantorGroupJoin, GuarantorTypeJoin]
            });
            if (opoutstandingamountInstance) {
                let groupbills = _.groupBy(opoutstandingamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    let GuarantorType: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorType = bills.GuarantorType.Description;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'OPOutstanding': OutStandingAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName,
                        'GuarantorType': GuarantorType,
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        }
        if (req.Data.GuarantorTypeId > -1) {
            let ipoutstandingamountInstance: any = await this.FindAll({
                attributes: ['GuarantorId', 'GuarantorTypeId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': 0 },
                    FacilityId: req.Data.FacilityId,
                    // IsPaidFully: false,
                    GuarantorTypeId: { '$eq': req.Data.GuarantorTypeId }
                },
                include: [GuarantorGroupJoin, GuarantorTypeJoin]
            });
            if (ipoutstandingamountInstance) {
                let groupbills = _.groupBy(ipoutstandingamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    let GuarantorType: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorType = bills.GuarantorType.Description;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'IPOutstanding': OutStandingAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName,
                        'GuarantorType': GuarantorType,
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        } else if (req.Data.GuarantorTypeId === -1) {
            let ipoutstandingamountInstance: any = await this.FindAll({
                attributes: ['GuarantorId', 'GuarantorTypeId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': 0 },
                    FacilityId: req.Data.FacilityId,
                    // IsPaidFully: false,
                    GuarantorTypeId: { '$gt': 1 }
                },
                include: [GuarantorGroupJoin, GuarantorTypeJoin]
            });
            if (ipoutstandingamountInstance) {
                let groupbills = _.groupBy(ipoutstandingamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    let GuarantorType: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorType = bills.GuarantorType.Description;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'IPOutstanding': OutStandingAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName,
                        'GuarantorType': GuarantorType,
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        }
        if (req.Data.GuarantorTypeId > -1) {
            let totaloutstandingamountInstance: any = await this.FindAll({
                attributes: ['GuarantorId', 'GuarantorTypeId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [1, 2, 5] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': 0 },
                    FacilityId: req.Data.FacilityId,
                    // IsPaidFully: false,
                    GuarantorTypeId: { '$eq': req.Data.GuarantorTypeId }
                },
                include: [GuarantorGroupJoin, GuarantorTypeJoin]
            });

            if (totaloutstandingamountInstance) {
                let groupbills = _.groupBy(totaloutstandingamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    let GuarantorType: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorType = bills.GuarantorType.Description;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'TotalOutstanding': OutStandingAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName,
                        'GuarantorType': GuarantorType,
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        } else if (req.Data.GuarantorTypeId === -1) {
            let totaloutstandingamountInstance: any = await this.FindAll({
                attributes: ['GuarantorId', 'GuarantorTypeId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [1, 2, 5] } },
                    IsPharmacyBill: 0,
                    PatientBillStatusId: 3,
                    GuarantorId: { '$gt': 0 },
                    FacilityId: req.Data.FacilityId,
                    // IsPaidFully: false,
                    GuarantorTypeId: { '$gt': 1 }
                },
                include: [GuarantorGroupJoin, GuarantorTypeJoin]
            });

            if (totaloutstandingamountInstance) {
                let groupbills = _.groupBy(totaloutstandingamountInstance, 'GuarantorId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let GuarantorId: number = 0;
                    let GuarantorName: string = '';
                    let GuarantorType: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        GuarantorId = bills.GuarantorId;
                        GuarantorName = bills.GuarantorMaster.GuarantorName;
                        GuarantorType = bills.GuarantorType.Description;
                        GuarantorGroup[GuarantorId] = GuarantorGroup[GuarantorId] || [];
                    }
                    let info = {
                        'TotalOutstanding': OutStandingAmount,
                        'GuarantorId': GuarantorId,
                        'GuarantorName': GuarantorName,
                        'GuarantorType': GuarantorType,
                    };
                    GuarantorGroup[GuarantorId].push(info);
                }
            }
        }
        return GuarantorGroup;
    }

    public async GetStaffCreditSummary(req: BaseRequest): Promise<any> {
        let result: any = [];
        result.push({ Key: 1, Value: await this.StaffPaymentDetails(req) });
        return result;
    }
    public async StaffPaymentDetails(req: BaseRequest): Promise<any> {
        let StaffGroup: { [id: number]: any[] } = {};
        let StaffGroupJoin: any = {
            model: this.Models.User, as: 'StaffUser',
            attributes: ['FirstName', 'LastName', 'UserName'],
            required: true,
            include: [
                this.GetReference('Title')
            ]
        };
        if (req.Data.StaffId > 0) {
            let billcreditamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'PaidAmount', 'StaffId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [6] } },
                    IsPharmacyBill: 1,
                    PatientBillStatusId: 3,
                    StaffId: { '$eq': req.Data.StaffId },
                    FacilityId: req.Data.FacilityId,
                    Staff: 1
                },
                include: [StaffGroupJoin]
            });
            if (billcreditamountInstance) {
                let groupbills = _.groupBy(billcreditamountInstance, 'StaffId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let StaffId: number = 0;
                    let StaffName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        StaffId = bills.StaffId;
                        StaffName = bills.StaffUser;
                        StaffGroup[StaffId] = StaffGroup[StaffId] || [];
                    }
                    let info = {
                        'BillAmount': BillAmount,
                        'StaffId': StaffId,
                        'StaffName': StaffName
                    };
                    StaffGroup[StaffId].push(info);
                }
            }
        } else if (req.Data.StaffId === 0) {
            let billcreditamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'PaidAmount', 'StaffId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [6] } },
                    IsPharmacyBill: 1,
                    PatientBillStatusId: 3,
                    StaffId: { '$gt': req.Data.StaffId },
                    FacilityId: req.Data.FacilityId,
                    Staff: 1
                },
                include: [StaffGroupJoin]
            });
            if (billcreditamountInstance) {
                let groupbills = _.groupBy(billcreditamountInstance, 'StaffId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let StaffId: number = 0;
                    let StaffName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        StaffId = bills.StaffId;
                        StaffName = bills.StaffUser;
                        StaffGroup[StaffId] = StaffGroup[StaffId] || [];
                    }
                    let info = {
                        'BillAmount': BillAmount,
                        'StaffId': StaffId,
                        'StaffName': StaffName
                    };
                    StaffGroup[StaffId].push(info);
                }
            }
        }
        if (req.Data.StaffId > 0) {
            let paidcreditamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'PaidAmount', 'StaffId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [6] } },
                    IsPharmacyBill: 1,
                    PatientBillStatusId: 3,
                    StaffId: { '$eq': req.Data.StaffId },
                    FacilityId: req.Data.FacilityId,
                    Staff: 1
                },
                include: [StaffGroupJoin]
            });
            if (paidcreditamountInstance) {
                let groupbills = _.groupBy(paidcreditamountInstance, 'StaffId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let PaidAmount: number = 0;
                    let StaffId: number = 0;
                    let StaffName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        PaidAmount += bills.PaidAmount;
                        StaffId = bills.StaffId;
                        StaffName = bills.StaffUser;
                        StaffGroup[StaffId] = StaffGroup[StaffId] || [];
                    }
                    let info = {
                        'PaidAmount': PaidAmount,
                        'StaffId': StaffId,
                        'StaffName': StaffName
                    };
                    StaffGroup[StaffId].push(info);
                }
            }
        } else if (req.Data.StaffId === 0) {
            let paidcreditamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'PaidAmount', 'StaffId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [6] } },
                    IsPharmacyBill: 1,
                    PatientBillStatusId: 3,
                    StaffId: { '$gt': req.Data.StaffId },
                    FacilityId: req.Data.FacilityId,
                    Staff: 1
                },
                include: [StaffGroupJoin]
            });
            if (paidcreditamountInstance) {
                let groupbills = _.groupBy(paidcreditamountInstance, 'StaffId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let PaidAmount: number = 0;
                    let StaffId: number = 0;
                    let StaffName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        PaidAmount += bills.PaidAmount;
                        StaffId = bills.StaffId;
                        StaffName = bills.StaffUser;
                        StaffGroup[StaffId] = StaffGroup[StaffId] || [];
                    }
                    let info = {
                        'PaidAmount': PaidAmount,
                        'StaffId': StaffId,
                        'StaffName': StaffName
                    };
                    StaffGroup[StaffId].push(info);
                }
            }
        }
        if (req.Data.StaffId > 0) {
            let totaloutstandingamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'PaidAmount', 'StaffId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [6] } },
                    IsPharmacyBill: 1,
                    PatientBillStatusId: 3,
                    StaffId: { '$eq': req.Data.StaffId },
                    FacilityId: req.Data.FacilityId,
                    Staff: 1
                },
                include: [StaffGroupJoin]
            });

            if (totaloutstandingamountInstance) {
                let groupbills = _.groupBy(totaloutstandingamountInstance, 'StaffId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let StaffId: number = 0;
                    let StaffName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        StaffId = bills.StaffId;
                        StaffName = bills.StaffUser;
                        StaffGroup[StaffId] = StaffGroup[StaffId] || [];
                    }
                    let info = {
                        'TotalOutstanding': OutStandingAmount,
                        'StaffId': StaffId,
                        'StaffName': StaffName
                    };
                    StaffGroup[StaffId].push(info);
                }
            }
        } else if (req.Data.StaffId === 0) {
            let totaloutstandingamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'PaidAmount', 'StaffId', 'OutStandingAmount'],
                where: {
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    // EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                    BillTypeId: { '$or': { '$eq': null, '$in': [6] } },
                    IsPharmacyBill: 1,
                    PatientBillStatusId: 3,
                    StaffId: { '$gt': req.Data.StaffId },
                    FacilityId: req.Data.FacilityId,
                    Staff: 1
                },
                include: [StaffGroupJoin]
            });

            if (totaloutstandingamountInstance) {
                let groupbills = _.groupBy(totaloutstandingamountInstance, 'StaffId');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let OutStandingAmount: number = 0;
                    let StaffId: number = 0;
                    let StaffName: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        OutStandingAmount += bills.OutStandingAmount;
                        StaffId = bills.StaffId;
                        StaffName = bills.StaffUser;
                        StaffGroup[StaffId] = StaffGroup[StaffId] || [];
                    }
                    let info = {
                        'TotalOutstanding': OutStandingAmount,
                        'StaffId': StaffId,
                        'StaffName': StaffName
                    };
                    StaffGroup[StaffId].push(info);
                }
            }
        }
        return StaffGroup;
    }

    public async PrintDailyBillReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let UserName = apiReq.Data.UserName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            UserName: UserName
        };
        let pdfOption: any = null;
        let key = 'dailybillreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintOpticalDailybillReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let UserName = apiReq.Data.UserName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            UserName: UserName
        };
        let pdfOption: any = null;
        let key = 'opticaldailybillreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintDiscountReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let DiscountApprover = apiReq.Data.DiscountApprover;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalDisAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            TotalDisAmount += item.BillDiscount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            DiscountApprover: DiscountApprover,
            TotalDisAmount: TotalDisAmount,
        };
        let pdfOption: any = null;
        let key = 'discountreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintIPDiscountReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let DiscountApprover = apiReq.Data.DiscountApprover;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalDisAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            TotalDisAmount += item.BillDiscount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            DiscountApprover: DiscountApprover,
            TotalDisAmount: TotalDisAmount,
        };
        let pdfOption: any = null;
        let key = 'ipdiscountreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintOutstandingReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let DueApproval = apiReq.Data.DueApproval;
        let GuarantorName = apiReq.Data.GuarantorName;
        let PatientBillsData = data.Data[0];
        let TotalDueAmount: number = 0;
        for (let idx in PatientBills) {
            let item = PatientBills[idx];
            TotalDueAmount += item.OutStandingAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalDueAmount: TotalDueAmount,
            DueApproval: DueApproval,
            GuarantorName: GuarantorName

        };
        let pdfOption: any = null;
        let key = 'outstandingreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintIPDueReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let DueApproval = apiReq.Data.DueApproval;
        let GuarantorName = apiReq.Data.GuarantorName;
        let PatientBillsData = data.Data[0];
        let TotalDueAmount: number = 0;
        for (let idx in PatientBills) {
            let item = PatientBills[idx];
            TotalDueAmount += item.OutStandingAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalDueAmount: TotalDueAmount,
            DueApproval: DueApproval,
            GuarantorName: GuarantorName

        };
        let pdfOption: any = null;
        let key = 'ipduereport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintPharmacyBillDetailReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let StoreName = apiReq.Data.StoreName;
        let UserName = apiReq.Data.UserName;
        // let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        let TotalDueAmount: number = 0;
        let TotalGSTAmount: number = 0;
        let TotalCGSTAmount: number = 0;
        let TotalSGSTAmount: number = 0;

        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            BillData.PatientInfo = '';
            if (Detail.Patient) {
                if (Detail.Patient.Title)
                    BillData.PatientInfo = Detail.Patient.Title.Description;
                if (Detail.Patient.FirstName)
                    BillData.PatientInfo += ' ' + Detail.Patient.FirstName;
                if (Detail.Patient.LastName)
                    BillData.PatientInfo += ' ' + Detail.Patient.LastName;
                if (Detail.Patient.MRN)
                    BillData.PatientInfo += '/' + Detail.Patient.MRN;
                if (Detail.Patient.Age)
                    BillData.PatientInfo += '/' + Detail.Patient.Age;
                if (Detail.Patient.Gender)
                    BillData.PatientInfo += '/' + Detail.Patient.Gender.Description;
            } else if (!Detail.Patient) {
                BillData.PatientInfo = Detail.PatientName
                    + '/' + Detail.Age;
                if (Detail.Gender) {
                    let Gender = Detail.Gender.Description;
                    BillData.PatientInfo += '/' + Gender;
                }
            }
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];

            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
            TotalDueAmount += item.OutStandingAmount;
            TotalGSTAmount += item.GSTAmount;
            TotalCGSTAmount += item.CGstAmount;
            TotalSGSTAmount += item.SGstAmount;
        }
        let FacilityId = apiReq.Data.FacilityId;
        let StoreMasterId = apiReq.Data.StoreMasterId;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(FacilityId, StoreMasterId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            StorePreferences: printStoreData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            UserName: UserName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            TotalDueAmount: TotalDueAmount,
            TotalGSTAmount: TotalGSTAmount,
            TotalCGSTAmount: TotalCGSTAmount,
            TotalSGSTAmount: TotalSGSTAmount,
            StoreName: StoreName

        };
        let pdfOption: any = null;
        let key = 'pharmacybilldetailreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintPharmacyDiscountReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let StoreName = apiReq.Data.StoreName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        let TotalDueAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            BillData.PatientInfo = '';
            if (Detail.Patient) {
                if (Detail.Patient.Title)
                    BillData.PatientInfo = Detail.Patient.Title.Description;
                if (Detail.Patient.FirstName)
                    BillData.PatientInfo += ' ' + Detail.Patient.FirstName;
                if (Detail.Patient.LastName)
                    BillData.PatientInfo += ' ' + Detail.Patient.LastName;
                if (Detail.Patient.MRN)
                    BillData.PatientInfo += '/' + Detail.Patient.MRN;
                if (Detail.Patient.Age)
                    BillData.PatientInfo += '/' + Detail.Patient.Age;
                if (Detail.Patient.Gender)
                    BillData.PatientInfo += '/' + Detail.Patient.Gender.Description;
            } else if (!Detail.Patient) {
                BillData.PatientInfo = Detail.PatientName + '/' + Detail.Age + '/' + Detail.Gender.Description;
            }
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
            TotalDueAmount += item.OutStandingAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBillsData.FacilityId, PatientBillsData.StoreMasterId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            StorePreferences: printStoreData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            TotalDueAmount: TotalDueAmount,
            StoreName: StoreName

        };
        let pdfOption: any = null;
        let key = 'pharmacydiscountreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintPharmacyDueReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let StoreName = apiReq.Data.StoreName;
        let DueApproval = apiReq.Data.DueApproval;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        let TotalDueAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            BillData.PatientInfo = '';
            if (Detail.Patient) {
                if (Detail.Patient.Title)
                    BillData.PatientInfo = Detail.Patient.Title.Description;
                if (Detail.Patient.FirstName)
                    BillData.PatientInfo += ' ' + Detail.Patient.FirstName;
                if (Detail.Patient.LastName)
                    BillData.PatientInfo += ' ' + Detail.Patient.LastName;
                if (Detail.Patient.MRN)
                    BillData.PatientInfo += '/' + Detail.Patient.MRN;
                if (Detail.Patient.Age)
                    BillData.PatientInfo += '/' + Detail.Patient.Age;
                if (Detail.Patient.Gender)
                    BillData.PatientInfo += '/' + Detail.Patient.Gender.Description;
            } else if (!Detail.Patient) {
                BillData.PatientInfo = Detail.PatientName + '/' + Detail.Age + '/' + Detail.Gender.Description;
            }
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
            TotalDueAmount += item.OutStandingAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBillsData.FacilityId, PatientBillsData.StoreMasterId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            StorePreferences: printStoreData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            TotalDueAmount: TotalDueAmount,
            DueApproval: DueApproval,
            StoreName: StoreName

        };
        let pdfOption: any = null;
        let key = 'pharmacyduereport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintOPBillReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let DoctorName = apiReq.Data.DoctorName;
        let GuarantorName = apiReq.Data.GuarantorName;
        let DepartmentName = apiReq.Data.DepartmentName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        let TotalDueAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            item.PaymentType = '';
            if (item.PatientPaymentDetails.length > 0) {
                let index = item.PatientPaymentDetails.length - 1;
                let PayDetails = item.PatientPaymentDetails[index];
                item.PaymentType = PayDetails.PaymentType.Description;

            }
            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
            TotalDueAmount += item.OutStandingAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            TotalDueAmount: TotalDueAmount,
            DoctorName: DoctorName,
            GuarantorName: GuarantorName,
            DepartmentName: DepartmentName

        };
        let pdfOption: any = null;
        let key = 'opbillreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintReferralDoctorRevenueDetailsReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let DoctorName = apiReq.Data.DoctorName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            DoctorName: DoctorName,

        };
        let pdfOption: any = null;
        let key = 'referraldoctorrevenuedetailsreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintIPBillReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let DoctorName = apiReq.Data.DoctorName;
        let GuarantorName = apiReq.Data.GuarantorName;
        let WardName = apiReq.Data.WardName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        let TotalDueAmount: number = 0;
        let TotalRefundAmount: number = 0;
        let TotaltobeRefAmount: number = 0;
        let TotalTdsAmount: number = 0;
        let TotalDisallowedAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
            TotalDueAmount += item.OutStandingAmount;
            TotalRefundAmount += item.RefundAmount;
            TotaltobeRefAmount += item.ToBeRefunded;
            TotalTdsAmount += item.TDSAmount;
            TotalDisallowedAmount += item.Disallowed;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            TotalDueAmount: TotalDueAmount,
            TotalRefundAmount: TotalRefundAmount,
            TotaltobeRefAmount: TotaltobeRefAmount,
            TotalTdsAmount: TotalTdsAmount,
            TotalDisallowedAmount: TotalDisallowedAmount,
            DoctorName: DoctorName,
            GuarantorName: GuarantorName,
            WardName: WardName

        };
        let pdfOption: any = null;
        let key = 'ipbillreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintRevenueSummaryDoctorReport(req: BaseRequest): Promise<any> {
        let FromDate = req.Data.FromDate;
        let ToDate = req.Data.ToDate;
        let FacilityName = req.Data.FacilityName;
        let DoctorName = req.Data.DoctorName;
        let DoctorId = req.Data.DoctorId;
        let DoctorData: any = [];

        let opTotNetAmt: number = 0;
        let opTotBillAmt: number = 0;
        let opTotDisAmt: number = 0;
        let opTotnetrevenue: number = 0;

        let doctor: Array<any> = [];
        let Totdoctor: any = [];
        let docName: any = '';

        let DoctorReq = req;
        DoctorData = await this.GetRevenueDoctorSummary(DoctorReq);
        // DoctorData = DoctorData;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(DoctorReq.Data.FacilityId);

        let opcollection = [];
        let ipcollection = [];
        let directcollection = [];

        if (DoctorData.length > 0)
            opcollection = DoctorData[0].Value;


        if (DoctorData.length > 1)
            ipcollection = DoctorData[1].Value;

        if (DoctorData.length > 2)
            directcollection = DoctorData[2].Value;


        for (let idx in opcollection) {
            let coll = opcollection[idx];
            let NetAmt = 0;
            let BillAmt = 0;
            let DisAmt = 0;
            let NetRevenue = 0;
            let key = '';
            for (let idx in coll) {
                docName = '';
                if (coll[idx].DoctorName.Title)
                    docName = coll[idx].DoctorName.Title.Description;
                if (coll[idx].DoctorName.FirstName)
                    docName += ' ' + coll[idx].DoctorName.FirstName;
                if (coll[idx].DoctorName.LastName)
                    docName += ' ' + coll[idx].DoctorName.LastName;

                key = docName;
                NetAmt += coll[idx].BillAmount;
                BillAmt += coll[idx].GrossAmount;
                DisAmt += coll[idx].BillDiscount;
                NetRevenue += coll[idx].NetRevenue;
            }
            opTotNetAmt += NetAmt;
            opTotBillAmt += BillAmt;
            opTotDisAmt += DisAmt;
            opTotnetrevenue += NetRevenue;
            doctor.push({
                'Key': key,
                'Value': {
                    'OP': NetAmt,
                    'OPBill': BillAmt,
                    'OPDis': DisAmt,
                    'OPRev': NetRevenue,
                    'OPIP': NetRevenue,
                    'IP': 0.00,
                    'IPBill': 0.00,
                    'IPDis': 0.00,
                }
            });
        }
        let ipTotNetAmt = 0;
        let ipTotBillAmt = 0;
        let ipTotDisAmt = 0;
        for (let idx in ipcollection) {
            let coll = ipcollection[idx];
            let NetAmt = 0;
            let BillAmt = 0;
            let DisAmt = 0;
            let key = '';
            for (let idx in coll) {
                docName = '';
                if (coll[idx].DoctorName.Title)
                    docName = coll[idx].DoctorName.Title.Description;
                if (coll[idx].DoctorName.FirstName)
                    docName += ' ' + coll[idx].DoctorName.FirstName;
                if (coll[idx].DoctorName.LastName)
                    docName += ' ' + coll[idx].DoctorName.LastName;

                key = docName;
                NetAmt += coll[idx].BillAmount;
                BillAmt += coll[idx].GrossAmount;
                DisAmt += coll[idx].BillDiscount;
            }
            ipTotNetAmt += NetAmt;
            ipTotBillAmt += BillAmt;
            ipTotDisAmt += DisAmt;

            let valappended = 0;
            doctor.forEach((val) => {
                if (key === val.Key) {
                    val.Value.IP = NetAmt;
                    val.Value.IPBill = BillAmt;
                    val.Value.IPDis = DisAmt;
                    val.Value.OPIP = val.Value.OPRev + val.Value.IP;
                    valappended = 1;
                }
            });

            if (valappended === 0)
                doctor.push({
                    'Key': key,
                    'Value': {
                        'OP': 0.00,
                        'OPBill': 0.00,
                        'OPDis': 0.00,
                        'OPRev': 0.00,
                        'IP': NetAmt,
                        'IPBill': BillAmt,
                        'IPDis': DisAmt,
                        'OPIP': NetAmt

                    }
                });

        }

        let directNetAmt = 0;
        let directBillAmt = 0;
        let directDisAmt = 0;
        let directnetrevenue = 0;
        let opwithdirectBillamt = 0;
        let opwithdirectDisamt = 0;
        let opwithdirectNetamt = 0;
        let opwithdirectnetrevenue = 0;
        if (DoctorId === -1 || !DoctorId) {
            for (let idx in directcollection) {
                let coll = directcollection[idx];
                let NetAmt = 0;
                let BillAmt = 0;
                let DisAmt = 0;
                let NetRevenue = 0;
                let key = '';
                for (let idx in coll) {
                    docName = '';
                    if (coll[idx].DoctorName)
                        docName = coll[idx].DoctorName;

                    key = docName;
                    NetAmt += coll[idx].BillAmount;
                    BillAmt += coll[idx].GrossAmount;
                    DisAmt += coll[idx].BillDiscount;
                    NetRevenue += coll[idx].NetRevenue;
                }
                directNetAmt += NetAmt;
                directBillAmt += BillAmt;
                directDisAmt += DisAmt;
                directnetrevenue += NetRevenue;

                doctor.push({
                    'Key': key,
                    'Value': {
                        'OP': NetAmt,
                        'OPBill': BillAmt,
                        'OPDis': DisAmt,
                        'OPRev': NetRevenue,
                        'OPIP': NetRevenue,
                        'IP': 0.00,
                        'IPBill': 0.00,
                        'IPDis': 0.00,
                    }
                });
            }
        }
        opwithdirectBillamt = opTotBillAmt + directBillAmt;
        opwithdirectDisamt = opTotDisAmt + directDisAmt;
        opwithdirectNetamt = opTotNetAmt + directNetAmt;
        opwithdirectnetrevenue = opTotnetrevenue + directnetrevenue;

        let netgross = opwithdirectBillamt + ipTotBillAmt;
        let netbill = opwithdirectNetamt + ipTotNetAmt;
        let netdis = opwithdirectDisamt + ipTotDisAmt;
        let netrev = opwithdirectnetrevenue + ipTotNetAmt;
        Totdoctor.push({
            'Key': 'Total',
            'TotOpNet': opwithdirectNetamt,
            'TotOpBill': opwithdirectBillamt,
            'TotOpDis': opwithdirectDisamt,
            'TotOpNetRev': opwithdirectnetrevenue,
            'TotIPNet': ipTotNetAmt,
            'TotIPBill': ipTotBillAmt,
            'TotIPDis': ipTotDisAmt,
            'NetGross': netgross,
            'NetBill': netbill,
            'NetDis': netdis,
            'NetRev': netrev,
        });
        let info = {
            Preferences: printPreferencesData,
            doctor: doctor,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            Totdoctor: Totdoctor,
            DoctorName: DoctorName
        };
        let pdfOption: any = null;
        let key = 'revenuesummarydoctorreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintRevenueSummaryDepartmentReport(req: BaseRequest): Promise<any> {
        let FromDate = req.Data.FromDate;
        let ToDate = req.Data.ToDate;
        let FacilityName = req.Data.FacilityName;
        let DepartmentName = req.Data.DepartmentName;
        let DepartmentData: any = [];
        // let FacilityInfo: any = [];
        let opTotNetAmt: number = 0;
        let opdepartment: any = [];
        let department: Array<any> = [];
        let Totdepartment: any = [];
        let totopdepartment: any = [];
        let ipdepartment: any = [];
        let totipdepartment: any = [];

        let DepartmentReq = req;
        DepartmentData = await this.GetRevenueDepartmentSummary(DepartmentReq);
        // DoctorData = DoctorData;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(DepartmentReq.Data.FacilityId);

        let opcollection = [];
        let ipcollection = [];
        let directcollection = [];

        if (DepartmentData.length > 0)
            opcollection = DepartmentData[0].Value;


        if (DepartmentData.length > 1)
            ipcollection = DepartmentData[1].Value;

        if (DepartmentData.length > 2)
            directcollection = DepartmentData[2].Value;


        for (let idx in opcollection) {
            let coll = opcollection[idx];
            let NetAmt = 0;
            let key = '';
            for (let idx in coll) {
                key = coll[idx].DepartmentName;
                NetAmt += coll[idx].BillAmount;
            }
            opTotNetAmt += NetAmt;
            opdepartment.push({
                'Key': key,
                'Value': NetAmt
            });
            department.push({
                'Key': key,
                'Value': {
                    'OP': NetAmt,
                    'IP': 0.00,
                    'OPIP': NetAmt
                }
            });
        }
        totopdepartment.push({
            'Key': 'Total',
            'Value': opTotNetAmt
        });
        let ipTotNetAmt = 0;
        for (let idx in ipcollection) {
            let coll = ipcollection[idx];
            let NetAmt = 0;
            let key = '';
            for (let idx in coll) {
                key = coll[idx].DepartmentName;
                NetAmt += coll[idx].BillAmount;
            }
            ipTotNetAmt += NetAmt;
            ipdepartment.push({
                'Key': key,
                'Value': NetAmt
            });

            let valappended = 0;
            department.forEach((val) => {
                if (key === val.Key) {
                    val.Value.IP = NetAmt;
                    val.Value.OPIP = val.Value.OP + val.Value.IP;
                    valappended = 1;
                }
            });

            if (valappended === 0)
                department.push({
                    'Key': key,
                    'Value': {
                        'OP': 0.00,
                        'IP': NetAmt,
                        'OPIP': NetAmt
                    }
                });

        }
        totipdepartment.push({
            'Key': 'Total',
            'Value': ipTotNetAmt
        });

        let directbillamt = 0;
        let opwithdirectamt = 0;
        for (let idx in directcollection) {
            let coll = directcollection[idx];
            let NetAmt = 0;
            let key = '';
            for (let idx in coll) {
                key = coll[idx].DepartmentName;
                NetAmt += coll[idx].BillAmount;
            }
            directbillamt += NetAmt;

            opdepartment.push({
                'Key': key,
                'Value': NetAmt
            });
            department.push({
                'Key': key,
                'Value': {
                    'OP': NetAmt,
                    'IP': 0.00
                }
            });
        }
        opwithdirectamt = opTotNetAmt + directbillamt;
        Totdepartment.push({
            'Key': 'Total',
            'Value': {
                'OP': opwithdirectamt || 0,
                'IP': ipTotNetAmt || 0,
                'OPIP': (opwithdirectamt + ipTotNetAmt) || 0
            }
        });
        let info = {
            Preferences: printPreferencesData,
            department: department,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            Totdepartment: Totdepartment,
            DepartmentName: DepartmentName
        };
        let pdfOption: any = null;
        let key = 'revenuesummarydepartmentreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintIPPharmacyIssueReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let StoreName = apiReq.Data.StoreName;
        let WardName = apiReq.Data.WardName;
        let PatientBillsData = data.Data[0];
        // let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalGST: number = 0;
        let TotalCGST: number = 0;
        let TotalSGST: number = 0;
        // PatientBills.forEach((Detail: any) => {
        //     let BillData = Detail;
        //     BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
        //     PatientDailyBills.push(BillData);
        // });
        for (let idx in PatientBills) {
            let item = PatientBills[idx];
            TotalBillAmount += item.BillAmount;
            TotalGST += item.GSTAmount;
            TotalCGST += item.CGstAmount;
            TotalSGST += item.SGstAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBillsData.FacilityId, PatientBillsData.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let info = {
            PatientBills: PatientBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalBillAmount: TotalBillAmount,
            TotalGST: TotalGST,
            TotalCGST: TotalCGST,
            TotalSGST: TotalSGST,
            StoreName: StoreName,
            WardName: WardName

        };
        let pdfOption: any = null;
        let key = 'ippharmacyissuevoucherreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintInsuranceAgingReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let GuarantorName = apiReq.Data.GuarantorName;
        let GuarantorType = apiReq.Data.GuarantorType;
        let insagingData: any = {};
        let InsAgeData: any = [];
        var insData = {
            InsuranceType: '',
            InsuranceName: '',
            InsuranceId: 0,
            Lessthan30Days: 0,
            Lessthan60Days: 0,
            Lessthan90Days: 0,
            Above90Days: 0
        };
        let BillData = data.Data[0];
        for (let idx in data.Data) {
            let less30 = 0;
            let less60 = 0;
            let less90 = 0;
            let above90 = 0;
            insagingData = data.Data[idx];
            let date1 = new Date(insagingData.BillDateTime);
            let To = new Date();
            let date2 = new Date(To);
            let difference_ms = date2.getTime() - date1.getTime();
            difference_ms = difference_ms / 1000;
            // let seconds = Math.floor(difference_ms % 60);
            difference_ms = difference_ms / 60;
            // let minutes = Math.floor(difference_ms % 60);
            difference_ms = difference_ms / 60;
            // let hours = Math.floor(difference_ms % 24);
            let days = Math.floor(difference_ms / 24);
            let NoofDays = days + 1;
            if (insagingData.GuarantorType) {
                insData.InsuranceType = insagingData.GuarantorType.Description;
            }
            if (insagingData.Guarantor) {
                insData.InsuranceName = insagingData.Guarantor.GuarantorName;
            }
            let InsuranceId = insagingData.GuarantorId;
            if (NoofDays <= 30) {
                less30 = insagingData.OutStandingAmount;
            }
            if (NoofDays > 30 && NoofDays <= 60) {
                less60 = insagingData.OutStandingAmount;
            }
            if (NoofDays > 60 && NoofDays <= 90) {
                less90 = insagingData.OutStandingAmount;
            }
            if (NoofDays > 90) {
                above90 = insagingData.OutStandingAmount;
            }
            var valappended = 0;
            if (InsAgeData.length > 0) {
                InsAgeData.forEach(function (item: any) {
                    if (insagingData.GuarantorId === item.InsuranceId) {
                        item.InsuranceType = insData.InsuranceType;
                        item.InsuranceName = insData.InsuranceName;
                        if (item.Lessthan30Days > 0) {
                            item.Lessthan30Days += less30;
                        } else {
                            item.Lessthan30Days = less30;
                        }
                        if (item.Lessthan60Days > 0) {
                            item.Lessthan60Days += less60;
                        } else {
                            item.Lessthan60Days = less60;
                        }
                        if (item.Lessthan90Days > 0) {
                            item.Lessthan90Days += less90;
                        } else {
                            item.Lessthan90Days = less90;
                        }
                        if (item.Above90Days > 0) {
                            item.Above90Days += above90;
                        } else {
                            item.Above90Days = above90;
                        }
                        valappended = 1;
                    }
                });
            }
            if (valappended === 0)
                InsAgeData.push({
                    'InsuranceType': insData.InsuranceType,
                    'InsuranceName': insData.InsuranceName,
                    'InsuranceId': InsuranceId,
                    'Lessthan30Days': less30,
                    'Lessthan60Days': less60,
                    'Lessthan90Days': less90,
                    'Above90Days': above90,
                });
        }
        let TotLessthan30Days = 0;
        let TotLessthan60Days = 0;
        let TotLessthan90Days = 0;
        let TotAbove90Days = 0;
        for (let idx in InsAgeData) {
            let OverallInsData = InsAgeData[idx];
            TotLessthan30Days = TotLessthan30Days + (OverallInsData.Lessthan30Days || 0);
            TotLessthan60Days = TotLessthan60Days + (OverallInsData.Lessthan60Days || 0);
            TotLessthan90Days = TotLessthan90Days + (OverallInsData.Lessthan90Days || 0);
            TotAbove90Days = TotAbove90Days + (OverallInsData.Above90Days || 0);
        }
        TotLessthan30Days = TotLessthan30Days;
        TotLessthan60Days = TotLessthan60Days;
        TotLessthan90Days = TotLessthan90Days;
        TotAbove90Days = TotAbove90Days;

        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(BillData.FacilityId);
        let info = {
            InsAgeData: InsAgeData,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            GuarantorName: GuarantorName,
            GuarantorType: GuarantorType,
            TotLessthan30Days: TotLessthan30Days,
            TotLessthan60Days: TotLessthan60Days,
            TotLessthan90Days: TotLessthan90Days,
            TotAbove90Days: TotAbove90Days,
        };
        let pdfOption: any = null;
        let key = 'insuranceagingreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintDirectBillReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        let TotalDueAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
            TotalDueAmount += item.OutStandingAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            TotalDueAmount: TotalDueAmount

        };
        let pdfOption: any = null;
        let key = 'directbillreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async UpdatePatientBillsFromCancel(req: BaseRequest): Promise<boolean> {
        if (req.Data.Header.Id > 0) {
            let result = await this.Update(req.Data.Header);
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(req.Data.Header.Id, req.Data.Details);
            return result;
        }
        return true;
    }
    public async UpdatePatientBillsCancel(req: BaseRequest): Promise<boolean> {
        if (req.Data.Header.Id > 0) {
            let result = await this.Update(req.Data.Header);
            return result;
        }
        return true;
    }
    public async UpdatePatientBillsInsurance(req: BaseRequest): Promise<boolean> {
        if (req.Data.Header.Id > 0) {
            let result = await this.Update(req.Data.Header);
            return result;
        }
        return true;
    }
    public async UpdatePatientBillsforDue(req: BaseRequest): Promise<boolean> {
        if (req.Data.Id > 0) {
            req.Data.DueApprovalDate = new Date();
            await this.Update(req.Data);
        }
        return true;
    }
    public async UpdatePatientBillsforDiscount(req: BaseRequest): Promise<boolean> {
        if (req.Data.Id > 0) {
            req.Data.DiscountApprovalDate = new Date();
            await this.Update(req.Data);
        }
        return true;
    }
    public async UpdatePatientBillsFromBedOccupancy(req: BaseRequest): Promise<boolean> {
        console.log('******req.Data*********', req.Data);
        if (req.Data.Id > 0) {
            let result = await this.Update(req.Data);
            console.log('******result*********', result);
            return result;
        }
        return true;
    }
    public async UpdatePatientBillsFromPlans(req: BaseRequest): Promise<boolean> {
        let receipttypeid = 3;
        if (req.Data.Header.Id > 0 && !req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            receipttypeid = 2;
            req.Data.Header.BillNumber = null; // await Sequence.Next(SequenceKeys.OPBillNumberId);
            if (!req.Data.Header.BillDateTime) {
                req.Data.Header.BillDateTime = new Date();
            }
            req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
        }
        if (req.Data.Header.IsMultiplePayment === true) {
            receipttypeid = 2;
        }
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptTypeId = receipttypeid;

                req.Data.paymentDetail[i].ReceiptNumber = null; //await Sequence.Next(SequenceKeys.ReceiptNrId);
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

                if (req.Data.Header.IsClaimed)
                    req.Data.paymentDetail[i].IsClaimed = req.Data.Header.IsClaimed;

            }
        }

        let result = await this.Update(req.Data.Header);
        let PatientBillId = req.Data.Header.Id;
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                if (req.Data.Header.BillTypeId === 1) {
                    await this.sendOPBillReceiptSMS(PatientBillId, req);
                }
                let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                let patientaccount = {};
                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.TotalDueAmount > 0) {
                    for (let j = 0, reclen = req.Data.paymentDetail.length; j < reclen; j++) {
                        if (req.Data.adjustmentDetail.length > 0) {
                            IsAdvanceAdjusting = 1;
                            AdjustedAgainstBill = Number(req.Data.Header.Received);

                            for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                if (req.Data.paymentDetail[i].Id > 0) {
                                    for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                        req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                    }
                                }
                            }

                            let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                            await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                        } else {
                            PaidAgainstBill = Number(req.Data.Header.Received);
                            DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                        }
                        patientaccount = {
                            Id: 0,
                            PatientId: req.Data.Header.PatientId,
                            EncounterId: req.Data.Header.EncounterId,
                            VisitNumber: req.Data.Header.VisitNumber,
                            TransactionTypeId: 2,
                            TransactionId: PatientBillId,
                            TransactionNumber: req.Data.paymentDetail[j].ReceiptNumber,
                            TransactionDate: new Date(),
                            BillAmount: 0,
                            PaidAmount: PaidAgainstBill,
                            IsAdvanceAdjusted: IsAdvanceAdjusting,
                            AdjustedAmount: AdjustedAgainstBill,
                            UnAdjustedAmount: 0,
                            DueAmount: DueTowardsBill,
                            DebitAmount: 0,
                            CreditAmount: 0,
                            IsDueCollection: 1
                        };
                        await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                    }
                } else {
                    for (let jj = 0, reclen1 = req.Data.paymentDetail.length; jj < reclen1; jj++) {
                        if (req.Data.adjustmentDetail.length > 0) {
                            IsAdvanceAdjusting = 1;
                            AdjustedAgainstBill = Number(req.Data.Header.Received);

                            for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                if (req.Data.paymentDetail[i].Id > 0) {
                                    for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                        req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                    }
                                }
                            }

                            let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                            await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                        } else {
                            PaidAgainstBill = Number(req.Data.Header.Received);
                            DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                        }
                        patientaccount = {
                            Id: 0,
                            PatientId: req.Data.Header.PatientId,
                            EncounterId: req.Data.Header.EncounterId,
                            VisitNumber: req.Data.Header.VisitNumber,
                            TransactionTypeId: 2,
                            TransactionId: PatientBillId,
                            TransactionNumber: null, // req.Data.paymentDetail[jj].ReceiptNumber,
                            TransactionDate: new Date(),
                            BillAmount: 0,
                            PaidAmount: PaidAgainstBill,
                            AdjustedAmount: AdjustedAgainstBill,
                            UnAdjustedAmount: 0,
                            DueAmount: DueTowardsBill,
                            DebitAmount: 0,
                            CreditAmount: 0
                        };
                        await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                    }
                }
                // if (req.Data.Header.LoadFrom) {
                //     if (req.Data.Header.LoadFrom.toLowerCase() === 'orderbills') {
                //         await this.CreateOrUpdateOrder(PatientBillId, req.Data, req.Data.Header.BillNumber);
                //     }
                // }
                if (receipttypeid === 2 && !req.Data.Header.BillNumber) {
                    const afterO: any = () => {
                        return ((bo, request, patientBillId) => {
                            return {
                                UpdatePlanInfo: async (code: string) => {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdatePlanInfo(request);
                                }
                            };
                        })(this, req, PatientBillId);
                    };

                    this.deferSequenceKey(PatientBillId, 'BillNumber',
                        this.getSequenceIdentifier(SequenceKeys.OPBillNumberId),
                        [
                            afterO().UpdatePlanInfo,
                        ]);

                }
            }


            let TreatmentPlanDetailBo = BoFactory.GetBo(emrbo.TreatmentPlanDetailBo, this.Request);
            let TreatmentPlanBo = BoFactory.GetBo(emrbo.TreatmentPlanBo, this.Request);
            let planDetailsApiReq = {
                Id: 0,
                PageContext: { PageSize: 1000, PageNumber: 1 },
                Params: [{ Key: TreatmentPlanDetailFilters.PatientBillId, Value: req.Data.Header.Id }
                ]
            };
            let TreatmentplanDetails = await TreatmentPlanDetailBo.GetTreatmentPlanDetails(planDetailsApiReq);
            if (TreatmentplanDetails.Data.length > 0) {
                for (let tx in TreatmentplanDetails.Data) {
                    var plndetData: any = {};
                    let planDetail = TreatmentplanDetails.Data[tx];
                    plndetData.PatientBillStatusId = 3;
                    plndetData.Id = planDetail.Id;
                    await TreatmentPlanDetailBo.Update(plndetData);
                }
            }

            let planApiReq = {
                Id: 0,
                PageContext: { PageSize: 1000, PageNumber: 1 },
                Params: [{ Key: TreatmentPlanFilters.PatientBillId, Value: req.Data.Header.Id }
                ]
            };
            let Treatmentplan = await TreatmentPlanBo.GetTreatmentPlans(planApiReq);

            if (Treatmentplan.Data.length > 0) {
                for (let px in Treatmentplan.Data) {
                    var planData: any = {};
                    let planDetail = Treatmentplan.Data[px];
                    planData.BillingStatusId = 3;
                    planData.Id = planDetail.Id;
                    await TreatmentPlanBo.Update(plndetData);
                }
            }
            return PatientBillId;
        }
        return result;
    }

    public async UpdatePatientBills(req: BaseRequest): Promise<boolean> {
        let receipttypeid = 3;
        if (req.Data.Header.Id > 0 && !req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            receipttypeid = 2;
            req.Data.Header.BillNumber = null; // await Sequence.Next(SequenceKeys.OPBillNumberId);
            if (!req.Data.Header.BillDateTime) {
                req.Data.Header.BillDateTime = new Date();
            }
            req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
        }
        if (req.Data.Header.IsMultiplePayment === true) {
            receipttypeid = 2;
        }
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptTypeId = receipttypeid;

                req.Data.paymentDetail[i].ReceiptNumber = null; //await Sequence.Next(SequenceKeys.ReceiptNrId);
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

                if (req.Data.Header.IsClaimed)
                    req.Data.paymentDetail[i].IsClaimed = req.Data.Header.IsClaimed;

            }
        }
        if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 2) {
            if (req.Data.Header.CancelReason)
                var CancelReason: string = req.Data.Header.CancelReason;
            let CancelledBy: number = req.Data.Header.CancelledBy;
            req.Data.Header = await this.GetPatientBillsById({ Id: req.Data.Header.Id });
            req.Data.Header.PatientBillStatusId = 2;
            if (CancelReason) {
                req.Data.Header.CancelReason = CancelReason;
                req.Data.Header.CancelledBy = CancelledBy;
            }
            let ordercancelBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
            await ordercancelBO.ManageFullOrderCanallation(req.Data.Header.Id);
        }
        let result = await this.Update(req.Data.Header);
        let PatientBillId = req.Data.Header.Id;
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                if (req.Data.Header.BillTypeId === 1) {
                    await this.sendOPBillReceiptSMS(PatientBillId, req);
                }
                let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                let patientaccount = {};
                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.TotalDueAmount > 0) {
                    for (let j = 0, reclen = req.Data.paymentDetail.length; j < reclen; j++) {
                        if (req.Data.adjustmentDetail.length > 0) {
                            IsAdvanceAdjusting = 1;
                            AdjustedAgainstBill = Number(req.Data.Header.Received);

                            for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                if (req.Data.paymentDetail[i].Id > 0) {
                                    for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                        req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                    }
                                }
                            }

                            let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                            await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                        } else {
                            PaidAgainstBill = Number(req.Data.Header.Received);
                            DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                        }
                        patientaccount = {
                            Id: 0,
                            PatientId: req.Data.Header.PatientId,
                            EncounterId: req.Data.Header.EncounterId,
                            VisitNumber: req.Data.Header.VisitNumber,
                            TransactionTypeId: 2,
                            TransactionId: PatientBillId,
                            TransactionNumber: req.Data.paymentDetail[j].ReceiptNumber,
                            TransactionDate: new Date(),
                            BillAmount: 0,
                            PaidAmount: PaidAgainstBill,
                            IsAdvanceAdjusted: IsAdvanceAdjusting,
                            AdjustedAmount: AdjustedAgainstBill,
                            UnAdjustedAmount: 0,
                            DueAmount: DueTowardsBill,
                            DebitAmount: 0,
                            CreditAmount: 0,
                            IsDueCollection: 1
                        };
                        await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                    }
                } else {
                    for (let jj = 0, reclen1 = req.Data.paymentDetail.length; jj < reclen1; jj++) {
                        if (req.Data.adjustmentDetail.length > 0) {
                            IsAdvanceAdjusting = 1;
                            AdjustedAgainstBill = Number(req.Data.Header.Received);

                            for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                if (req.Data.paymentDetail[i].Id > 0) {
                                    for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                        req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                    }
                                }
                            }

                            let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                            await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                        } else {
                            PaidAgainstBill = Number(req.Data.Header.Received);
                            DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                        }
                        patientaccount = {
                            Id: 0,
                            PatientId: req.Data.Header.PatientId,
                            EncounterId: req.Data.Header.EncounterId,
                            VisitNumber: req.Data.Header.VisitNumber,
                            TransactionTypeId: 2,
                            TransactionId: PatientBillId,
                            TransactionNumber: null, // req.Data.paymentDetail[jj].ReceiptNumber,
                            TransactionDate: new Date(),
                            BillAmount: 0,
                            PaidAmount: PaidAgainstBill,
                            AdjustedAmount: AdjustedAgainstBill,
                            UnAdjustedAmount: 0,
                            DueAmount: DueTowardsBill,
                            DebitAmount: 0,
                            CreditAmount: 0
                        };
                        await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                    }
                }
                if (req.Data.Header.LoadFrom && req.Data.Header.PatientBillStatusId === 3) {
                    if (req.Data.Header.LoadFrom.toLowerCase() === 'orderbills') {
                        await this.CreateOrUpdateOrder(PatientBillId, req.Data, req.Data.Header.BillNumber);
                    }
                }

                let billIdentifier: any = null;

                if (receipttypeid === 2 && !req.Data.Header.BillNumber) {

                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.OPBillNumberId, req.Data.Header.FacilityId);

                    const afterO: any = () => {
                        return ((bo, request, patientBillId) => {
                            return {
                                updateOrderInfo: async (code: string) => {
                                    try {
                                        request.Data.Header.PatientBillId = patientBillId;
                                        request.Data.Header.BillNumber = code;
                                        await bo.UpdateOrderInfo(request);
                                    } catch (error) {
                                        console.error('Error in UpdateOrderInfo:', error);
                                        throw error;  // Handle the error as needed
                                    }
                                }
                            };
                        })(this, req, PatientBillId);
                    };

                    // this.deferSequenceKey(PatientBillId, 'BillNumber',
                    //     this.getSequenceIdentifier(SequenceKeys.OPBillNumberId),
                    //     [
                    //         afterO().updateOrderInfo,
                    //     ]);
                    const handleDeferredExecution = async () => {
                        try {
                            // Assuming deferSequenceKey processes a list of functions
                            await this.deferSequenceKey(PatientBillId, 'BillNumber',
                                billIdentifier,
                                [
                                    afterO().updateOrderInfo,
                                    // afterO().updatePatientAccountsInfo,
                                    // afterO().SendBillDiscountSMS,
                                ]);
                        } catch (error) {
                            // console.error('Error in deferSequenceKey execution:', error);
                            throw error;
                        }
                    };

                    handleDeferredExecution();

                }
            }

            if (receipttypeid === 2) {
                if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 3) {
                    if (req.Data.Header.EncounterId) {
                        let encounterBO = BoFactory.GetBo(encbo.EncounterBo, this.Request);
                        await encounterBO.UpdateIsBillCompleted(1, req.Data.Header.EncounterId);
                    }
                }
            }

            return PatientBillId;
        }
        return result;
    }

    public async UpdateVirtualPatientBills(req: BaseRequest): Promise<boolean> {
        let receipttypeid = 3;
        if (req.Data.Header.Id > 0 && !req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            receipttypeid = 2;
            req.Data.Header.BillNumber = null; // await Sequence.Next(SequenceKeys.OPBillNumberId);
            if (!req.Data.Header.BillDateTime) {
                req.Data.Header.BillDateTime = new Date();
            }
            req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
        }
        if (req.Data.Header.IsMultiplePayment === true) {
            receipttypeid = 2;
        }
        if (req.Data.Header.PatientOrderId) {
            let patorderBo = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
            let PatientOrder = await patorderBo.GetPatientOrderById({ Id: req.Data.Header.PatientOrderId });
            PatientOrder.Id = req.Data.Header.PatientOrderId;
            PatientOrder.IsPaidFully = true;
            await patorderBo.Update(PatientOrder);
        }
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptTypeId = receipttypeid;

                req.Data.paymentDetail[i].ReceiptNumber = null; //await Sequence.Next(SequenceKeys.ReceiptNrId);
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

                if (req.Data.Header.IsClaimed)
                    req.Data.paymentDetail[i].IsClaimed = req.Data.Header.IsClaimed;

            }
        }
        if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 2) {
            if (req.Data.Header.CancelReason)
                var CancelReason: string = req.Data.Header.CancelReason;
            let CancelledBy: number = req.Data.Header.CancelledBy;
            req.Data.Header = await this.GetPatientBillsById({ Id: req.Data.Header.Id });
            req.Data.Header.PatientBillStatusId = 2;
            if (CancelReason) {
                req.Data.Header.CancelReason = CancelReason;
                req.Data.Header.CancelledBy = CancelledBy;
            }
            let ordercancelBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
            await ordercancelBO.ManageFullOrderCanallation(req.Data.Header.Id);
        }
        let result = await this.Update(req.Data.Header);
        let PatientBillId = req.Data.Header.Id;
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                if (req.Data.Header.BillTypeId === 1) {
                    await this.sendOPBillReceiptSMS(PatientBillId, req);
                }
                let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                let patientaccount = {};
                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.TotalDueAmount > 0) {
                    for (let j = 0, reclen = req.Data.paymentDetail.length; j < reclen; j++) {
                        if (req.Data.adjustmentDetail.length > 0) {
                            IsAdvanceAdjusting = 1;
                            AdjustedAgainstBill = Number(req.Data.Header.Received);

                            for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                if (req.Data.paymentDetail[i].Id > 0) {
                                    for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                        req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                    }
                                }
                            }

                            let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                            await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                        } else {
                            PaidAgainstBill = Number(req.Data.Header.Received);
                            DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                        }
                        patientaccount = {
                            Id: 0,
                            PatientId: req.Data.Header.PatientId,
                            EncounterId: req.Data.Header.EncounterId,
                            VisitNumber: req.Data.Header.VisitNumber,
                            TransactionTypeId: 2,
                            TransactionId: PatientBillId,
                            TransactionNumber: req.Data.paymentDetail[j].ReceiptNumber,
                            TransactionDate: new Date(),
                            BillAmount: 0,
                            PaidAmount: PaidAgainstBill,
                            IsAdvanceAdjusted: IsAdvanceAdjusting,
                            AdjustedAmount: AdjustedAgainstBill,
                            UnAdjustedAmount: 0,
                            DueAmount: DueTowardsBill,
                            DebitAmount: 0,
                            CreditAmount: 0,
                            IsDueCollection: 1
                        };
                        await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                    }
                } else {
                    for (let jj = 0, reclen1 = req.Data.paymentDetail.length; jj < reclen1; jj++) {
                        if (req.Data.adjustmentDetail.length > 0) {
                            IsAdvanceAdjusting = 1;
                            AdjustedAgainstBill = Number(req.Data.Header.Received);

                            for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                if (req.Data.paymentDetail[i].Id > 0) {
                                    for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                        req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                    }
                                }
                            }

                            let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                            await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                        } else {
                            PaidAgainstBill = Number(req.Data.Header.Received);
                            DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                        }
                        patientaccount = {
                            Id: 0,
                            PatientId: req.Data.Header.PatientId,
                            EncounterId: req.Data.Header.EncounterId,
                            VisitNumber: req.Data.Header.VisitNumber,
                            TransactionTypeId: 2,
                            TransactionId: PatientBillId,
                            TransactionNumber: null, // req.Data.paymentDetail[jj].ReceiptNumber,
                            TransactionDate: new Date(),
                            BillAmount: 0,
                            PaidAmount: PaidAgainstBill,
                            AdjustedAmount: AdjustedAgainstBill,
                            UnAdjustedAmount: 0,
                            DueAmount: DueTowardsBill,
                            DebitAmount: 0,
                            CreditAmount: 0
                        };
                        await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                    }
                }

                await this.CreateOrUpdateOrder(PatientBillId, req.Data, req.Data.Header.BillNumber);

                if (receipttypeid === 2) {
                    const afterO: any = () => {
                        return ((bo, request, patientBillId) => {
                            return {
                                updateOrderInfo: async (code: string) => {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdateOrderInfo(request);
                                }
                            };
                        })(this, req, PatientBillId);
                    };

                    this.deferSequenceKey(PatientBillId, 'BillNumber',
                        this.getSequenceIdentifier(SequenceKeys.OPBillNumberId),
                        [
                            afterO().updateOrderInfo,
                        ]);

                }
            }

            if (receipttypeid === 2) {
                if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 3) {
                    if (req.Data.Header.EncounterId) {
                        let encounterBO = BoFactory.GetBo(encbo.EncounterBo, this.Request);
                        await encounterBO.UpdateIsBillCompleted(1, req.Data.Header.EncounterId);
                    }
                }
            }

            return PatientBillId;
        }
        return result;
    }

    public async AddPatientB2BBills(req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            req.Data.Header.BillNumber = null;
            req.Data.Header.BillDateTime = new Date();
        }
        let result = await this.Save(req.Data.Header);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let PatientBillId = result.dataValues.Id;

            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                for (let ii = 0, len1 = req.Data.Details.length; ii < len1; ii++) {
                    req.Data.Details[ii].OrderStatusId = 1;
                }
            }

            //await this.setAliasName(req);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                await this.CreateOrUpdateOrder(PatientBillId, req.Data, req.Data.Header.BillNumber);
            }

            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                billIdentifier = this.getSequenceIdentifier(SequenceKeys.OPBillNumberId);
                /*
                if (req.Data.Header.BillTypeId === 1) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.OPBillNumberId);
                } else if (req.Data.Header.BillTypeId === 2) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.IPFinalBillNumberId);
                } else if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.IPBillNumberId);
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyBillNumberId);
                } else if (req.Data.Header.BillTypeId === 5) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.DGBillNumberId);
                }
                */
            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            updateOrderInfo: async (code: string) => {
                                request.Data.Header.PatientBillId = patientBillId;
                                request.Data.Header.BillNumber = code;
                                await bo.UpdateOrderInfo(request);
                            }
                        };
                    })(this, req, PatientBillId);
                };

                this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier, [afterO().updateOrderInfo]);

            }

            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.EncounterId) {
                    let encounterBO = BoFactory.GetBo(encbo.EncounterBo, this.Request);
                    await encounterBO.UpdateIsBillCompleted(1, req.Data.Header.EncounterId);
                }
            }


            return PatientBillId;
        }

        return 0;
    }

    public async SendBillDiscountSMS(req: BaseRequest): Promise<any> {
        if (req.Data.Header.BillDiscount) {
            let patientbillData = await this.GetPatientBillsById({ Id: req.Data.Header.PatientBillId });
            if (patientbillData.PatientBillStatusId === 3) {
                let patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
                let patientData = await patientBO.GetPatientById({ Id: patientbillData.PatientId });
                let facilityBO = BoFactory.GetBo(userbo.FacilityBo, this.Request);
                let facilitydata = await facilityBO.GetFacilityById({ Id: patientbillData.FacilityId });
                let BillType = await this.getBillType(patientbillData);
                let dateformat = 'DD/MM/YYYY';
                let timeformat = 'HH:mm:ss';
                let BillDate = moment(patientbillData.BillDateTime).format(dateformat);
                let BillTime = moment(patientbillData.BillDateTime).format(timeformat);
                if (facilitydata) {
                    if (facilitydata.Mobile) {
                        let smsProvider = this.GetSmsProvider();
                        let eventTemplateBO = BoFactory.GetBo(userbo.EventTemplateBo, this.Request);
                        let smsTemplateInfo = await eventTemplateBO.GetTemplateInfo('PatientDiscountBill', 'PatientDiscountBill', 1);
                        if (smsTemplateInfo) {
                            let vPatientName = '';
                            vPatientName = await this.getPatientName(patientData);
                            let vBillNum = '';
                            if (req.Data.BillNum) vBillNum = req.Data.BillNum;
                            if (req.Data.Header.BillNumber) vBillNum = req.Data.Header.BillNumber;
                            let smsmodel = {
                                numbers: [facilitydata.Mobile],
                                message: Template.Compile(smsTemplateInfo.TemplateContent,
                                    {
                                        patientName: vPatientName,
                                        billType: BillType,
                                        Mrn: patientData.MRN,
                                        discountamt: patientbillData.BillDiscount,
                                        displaydate: BillDate,
                                        displaytime: BillTime,
                                        billNum: vBillNum
                                    })
                            };
                            if (smsProvider) {
                                let SMSStatus = await smsProvider.send(smsmodel);
                                let eventDashboardOutboundBo = BoFactory.GetBo(userbo.EventDashboardBo, this.Request);
                                await eventDashboardOutboundBo.ManageSMSOutBound(SMSStatus,
                                    smsmodel.message + ' To : ' + facilitydata.Mobile);
                            }
                        }
                    }
                }
            }
            return true;
        }
    }

    public async getPatientName(patientData: PatientAttributes): Promise<string> {
        let vPatientName = '';
        if (patientData) {
            if (patientData.FirstName) vPatientName += patientData.FirstName;
            if (patientData.LastName) vPatientName += ' ' + patientData.LastName;
            let apiReqTitle = {
                Id: 0,
                PageContext: { PageSize: 50, PageNumber: 1 },
                Params: [
                    { Key: ReferenceValueFilters.ReferenceGroupCode, Value: 'Title' },
                    { Key: ReferenceValueFilters.ReferenceValueCodeId, Value: patientData.TitleId }
                ]
            };
            let vTitleName = '';
            let refTitleBo = BoFactory.GetBo(userbo.ReferenceValueBo, this.Request);
            let TitleData = await refTitleBo.GetReferenceValues(apiReqTitle);
            if (TitleData.Data) {
                if (TitleData.Data.length > 0) {
                    if (TitleData.Data[0].Description)
                        vTitleName = TitleData.Data[0].Description;
                }
            }
            if (vTitleName)
                vPatientName = vTitleName + '.' + vPatientName;
        }
        return vPatientName;
    }

    public async getBillType(patientbillData: PatientBillsAttributes): Promise<string> {
        let vbillType = '';
        let apiReqBillType = {
            Id: 0,
            PageContext: { PageSize: 50, PageNumber: 1 },
            Params: [
                { Key: ReferenceValueFilters.ReferenceGroupCode, Value: 'BillType' },
                { Key: ReferenceValueFilters.ReferenceValueCodeId, Value: patientbillData.BillTypeId }
            ]
        };
        let refbilltypeinfo = BoFactory.GetBo(userbo.ReferenceValueBo, this.Request);
        let billtypeData = await refbilltypeinfo.GetReferenceValues(apiReqBillType);
        if (billtypeData.Data) {
            if (billtypeData.Data.length > 0) {
                if (billtypeData.Data[0].Description)
                    vbillType = billtypeData.Data[0].Description;
            }
        }
        return vbillType;
    }

    public async setAliasName(req: BaseRequest): Promise<any> {
        for (let i = 0; i < req.Data.Details.length; i++) {
            try {
                let AliasId: any = null;
                let AliasName: any = null;
                let guarantorBO = BoFactory.GetBo(generalMasterBO.GuarantorBo, this.Request);
                let servItmAliasBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemAliasBo, this.Request);
                let patguarantordata: any = null;
                patguarantordata = await guarantorBO.GetGuarantorById({ Id: req.Data.Header.GuarantorId });
                let apiServAliasReq = {
                    Id: 0,
                    Params: [
                        { Key: ServiceItemAliasFilters.ServiceItemId, Value: req.Data.Details[i].ServiceId },
                        { Key: ServiceItemAliasFilters.ExternalProviderId, Value: req.Data.Header.GuarantorId }
                    ],
                    PageContext: { PageSize: -1, PageNumber: 1 }
                };
                let seritmaliasdata = await servItmAliasBo.GetServiceItemAliass(apiServAliasReq);
                if (seritmaliasdata.Data && seritmaliasdata.Data.length > 0) {
                    AliasId = seritmaliasdata.Data[0].AliasId;
                    AliasName = seritmaliasdata.Data[0].AliasName;
                }
                req.Data.Details[i].AliasId = AliasId;
                req.Data.Details[i].AliasName = AliasName;
            } catch (ex) { console.log(ex); }
        }
    }

    public async sendOPBillReceiptSMS(PatientBillId: number, req: BaseRequest): Promise<boolean> {
        let result = false;
        let PatientId = req.Data.Header.PatientId;
        let CurrentPaidAmt = 0;
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            try {
                CurrentPaidAmt += Number(req.Data.paymentDetail[i].AmountPaid);
            } catch (ex) { CurrentPaidAmt += 0; }
        }
        let patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let patientData = await patientBO.GetPatientById({ Id: PatientId });
        if (patientData && CurrentPaidAmt) {
            if (patientData.Mobile) {
                let smsProvider = this.GetSmsProvider();
                let eventTemplateBO = BoFactory.GetBo(userbo.EventTemplateBo, this.Request);
                let smsTemplateInfo = null;
                smsTemplateInfo =
                    await eventTemplateBO.GetTemplateInfo('OPBillReceipt', 'OPBillReceipt', 1);
                if (smsTemplateInfo) {
                    let smsmodel = {
                        numbers: [patientData.Mobile],
                        message: Template.Compile(smsTemplateInfo.TemplateContent,
                            {
                                displayPaidAmt: CurrentPaidAmt
                            })
                    };
                    if (smsProvider) {
                        let SMSStatus = await smsProvider.send(smsmodel);
                        let eventDashboardOutboundBo = BoFactory.GetBo(userbo.EventDashboardBo, this.Request);
                        await eventDashboardOutboundBo.ManageSMSOutBound(SMSStatus, smsmodel.message + ' To : ' + patientData.Mobile);
                    }
                }
                return true;
            }
        }

        return result;
    }

    public async CreateOrUpdateOrder(PatientBillId: number, request: any, v_BillNumber: string): Promise<any> {
        let OrderableSeparateSubDept = 0;
        try {
            let facilityprebo = BoFactory.GetBo(appbo.FacilityPreferenceBo, this.Request);
            let facilityPreferencesData =
                await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
            if (facilityPreferencesData && facilityPreferencesData.orderableseparatebysubdept) {
                try {
                    OrderableSeparateSubDept = parseInt(facilityPreferencesData.orderableseparatebysubdept);
                } catch (ex) { OrderableSeparateSubDept = 0; }
            }
        } catch (ex) { OrderableSeparateSubDept = 0; }

        let OrderableSeparateExternal = 0;
        try {
            let facilityprebo = BoFactory.GetBo(appbo.FacilityPreferenceBo, this.Request);
            let facilityPreferencesData =
                await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
            if (facilityPreferencesData && facilityPreferencesData.externaltestoredersplit) {
                try {
                    OrderableSeparateExternal = parseInt(facilityPreferencesData.externaltestoredersplit);
                } catch (ex) { OrderableSeparateExternal = 0; }
            }
        } catch (ex) { OrderableSeparateExternal = 0; }

        let patorderBo = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
        let group: { [id: number]: any[] } = {};
        let ServiceItemPackageBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemPackageMapBo, this.Request);
        let ServiceItemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
        await Promise.all(request.Details.map((DetailItem: any): Promise<void> => {
            return (async (detail): Promise<void> => {
                if (detail.IsOrderable && detail.Status === 1) {
                    detail.PatientId = request.Header.PatientId;
                    detail.PatientBillId = PatientBillId;
                    detail.PatientBillStatusId = request.Header.PatientBillStatusId;
                    if (detail.Id > 0) {
                        detail.PatientBillDetailId = detail.Id;
                        detail.Id = 0;
                    }
                    let testid = detail.TestTypeId;
                    if (OrderableSeparateSubDept && OrderableSeparateSubDept > 0) {
                        if (!detail.SubDepartmentId) testid += '-' + detail.PatientBillDetailId;
                        else testid += '-' + detail.SubDepartmentId;
                    }
                    if (OrderableSeparateExternal && OrderableSeparateExternal > 0) {
                        // if (!detail.IsExternalLab) testid += '-' + detail.PatientBillDetailId;
                        if (detail.IsExternalLab)
                            testid += '-' + detail.IsExternalLab;
                    }
                    group[testid] = group[testid] || [];
                    group[testid].push(detail);
                }
                if (detail.IsPackageItem) {
                    let PackageApiReq = {
                        Id: 0,
                        PageContext: { PageSize: 1000, PageNumber: 1 },
                        Params: [{ Key: ServiceItemPackageMapFilters.ServiceItemId, Value: detail.ServiceId }]
                    };
                    let PackageItems = await ServiceItemPackageBo.GetServiceItemPackageMaps(PackageApiReq);
                    if (PackageItems.Data.length > 0) {
                        await Promise.all(PackageItems.Data.map((PackageItem): Promise<void> => {
                            return (async (pack): Promise<void> => {
                                let ServiceItemApiReq = {
                                    Id: 0,
                                    PageContext: { PageSize: 1000, PageNumber: 1 },
                                    Params: [{ Key: ServiceItemFilters.Id, Value: pack.ServiceId }]
                                };
                                let ServiceItems = await ServiceItemBo.GetServiceItems(ServiceItemApiReq);
                                if (ServiceItems.Data.length > 0) {
                                    let ServiceItem: any = ServiceItems.Data[0];
                                    if (ServiceItem.IsOrderable) {
                                        let testid = ServiceItem.OrderTypeId;
                                        if (OrderableSeparateSubDept && OrderableSeparateSubDept > 0) {
                                            if (!ServiceItem.SubDepartmentId) testid += '-' + ServiceItem.Id;
                                            else testid += '-' + ServiceItem.SubDepartmentId;
                                        }

                                        group[testid] = group[testid] || [];
                                        let TestDetail = {
                                            BillDateTime: new Date(),
                                            CancelReason: 0,
                                            CancelledBy: 0,
                                            DepartmentId: ServiceItem.DepartmentId,
                                            DoctorId: request.Header.DoctorId,
                                            DoctorShare: 0,
                                            EncounterId: request.Header.EncounterId,
                                            EncounterTypeId: request.Header.EncounterTypeId,
                                            MasterTypeId: 1,
                                            OrderDateTime: new Date(),
                                            PackageId: detail.ServiceId,
                                            PackageName: detail.ServiceName,
                                            Quantity: pack.Quantity,
                                            RequestDate: new Date(), ServiceCategoryId: ServiceItem.ServiceCategoryId,
                                            ServiceCode: ServiceItem.ServiceCode,
                                            ServiceId: ServiceItem.Id,
                                            ServiceName: ServiceItem.ServiceName,
                                            Status: 1,
                                            SubDepartmentId: ServiceItem.SubDepartmentId,
                                            TestCode: ServiceItem.ItemCode,
                                            TestDescription: ServiceItem.Description,
                                            TestId: ServiceItem.MasterItemId,
                                            TestName: ServiceItem.MasterName,
                                            TestTypeId: ServiceItem.OrderTypeId,
                                            IsPackage: true,
                                            PatientId: request.Header.PatientId,
                                            PatientBillDetailId: detail.PatientBillDetailId,
                                            PatientBillId: detail.PatientBillId,
                                            PatientBillStatusId: detail.PatientBillStatusId,
                                            IsExternalLab: detail.IsExternalLab,
                                            OrderStatusId: 1
                                        };
                                        group[testid].push(TestDetail);
                                    }
                                }
                            })(PackageItem);
                        }));
                    }
                }
            })(DetailItem);
        }));

        for (let key in group) {
            // v_BillNumber = null;
            let details: any[] = group[key];
            let order = {
                PatientId: request.Header.PatientId,
                OrderTypeId: details[0].ServiceCategoryId,
                OrderStatusId: 1, // Credated
                OrderRequestDate: request.Header.BillDateTime,
                DoctorId: request.Header.DoctorId,
                DoctorName: request.Header.DoctorName,
                OrderFromId: request.Header.DepartmentId,
                // OrderToId: request.Header.DepartmentId,
                OrderToId: details[0].DepartmentId,
                OrderPriorityId: 1,
                ParentDeparementId: request.Header.DepartmentId,
                OrderLocationId: request.Header.DepartmentId,
                PatientMRN: request.Header.PatientMrn,
                BillingId: PatientBillId,
                BillAmount: request.Header.PaidAmount,
                BillDate: request.Header.BillDateTime,
                GroupId: 0,
                BillTypeId: request.Header.BillTypeId,
                EncounterId: request.Header.EncounterId,
                BillingStatusId: 2,
                EncounterTypeId: 1,
                TestTypeId: details[0].TestTypeId,
                SubDepartmentId: details[0].SubDepartmentId,
                PatientBillStatusId: request.Header.PatientBillStatusId,
                BillNumber: v_BillNumber,
                FacilityId: request.Header.FacilityId,
                PatientName: request.Header.PatientName,
                PatientMobile: request.Header.Mobile,
                ReferredId: request.Header.ReferralId,
                IsExternalLab: details[0].IsExternalLab
            };
            await patorderBo.ManagePatientOrder(order as any, details);
        }

        if (request.Header.PendingOrderId && request.Header.PendingOrderId > 0) {
            let PatientOrder = await patorderBo.GetPatientOrderById({ Id: request.Header.PendingOrderId });
            if (PatientOrder.IsExternalLab) {
                PatientOrder.ExternalOrderStatusId = 1;
            }
            PatientOrder.OrderStatusId = 3;
            await patorderBo.Update(PatientOrder);
        }
    }

    public async CreateAdditionalInPatientOrder(PatientBillId: number, request: any, v_BillNumber: string): Promise<any> {
        let OrderableSeparateSubDept = 0;
        try {
            let facilityprebo = BoFactory.GetBo(appbo.FacilityPreferenceBo, this.Request);
            let facilityPreferencesData =
                await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
            if (facilityPreferencesData && facilityPreferencesData.orderableseparatebysubdept) {
                try {
                    OrderableSeparateSubDept = parseInt(facilityPreferencesData.orderableseparatebysubdept);
                } catch (ex) { OrderableSeparateSubDept = 0; }
            }
        } catch (ex) { OrderableSeparateSubDept = 0; }

        let patorderBo = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
        let group: { [id: number]: any[] } = {};
        await Promise.all(request.Details.map((DetailItem: any): Promise<void> => {
            return (async (detail): Promise<void> => {
                if (detail.IsOrderable) {
                    detail.PatientId = request.Header.PatientId;
                    detail.PatientBillId = PatientBillId;
                    detail.PatientBillStatusId = request.Header.PatientBillStatusId;
                    if (detail.Id > 0) {
                        detail.PatientBillDetailId = detail.Id;
                        detail.Id = 0;
                    }
                    let testid = detail.TestTypeId;
                    if (OrderableSeparateSubDept && OrderableSeparateSubDept > 0) {
                        if (!detail.SubDepartmentId) testid += '-' + detail.PatientBillDetailId;
                        else testid += '-' + detail.SubDepartmentId;
                    }

                    group[testid] = group[testid] || [];
                    group[testid].push(detail);
                }
            })(DetailItem);
        }));

        for (let key in group) {
            v_BillNumber = null;
            let details: any[] = group[key];
            let order = {
                IsAdditionalVisit: true,
                PatientId: request.Header.PatientId,
                OrderTypeId: details[0].ServiceCategoryId,
                OrderStatusId: 1, // Credated
                OrderTotal: 0,
                OrderRequestDate: request.Header.BillDateTime,
                OrderScheduleDate: request.Header.BillDateTime,
                DoctorId: request.Header.DoctorId,
                DoctorName: request.Header.DoctorName,
                OrderFromId: request.Header.DepartmentId,
                OrderToId: request.Header.DepartmentId,
                OrderPriorityId: 1,
                ParentDeparementId: request.Header.DepartmentId,
                OrderLocationId: request.Header.DepartmentId,
                PatientMRN: request.Header.MRN,
                BillingId: PatientBillId,
                BillAmount: request.Header.PaidAmount,
                BillDate: request.Header.BillDateTime,
                GroupId: 0,
                BillTypeId: request.Header.BillTypeId,
                EncounterId: request.Header.EncounterId,
                BillingStatusId: 2,
                EncounterTypeId: request.Header.EncounterTypeId,
                TestTypeId: details[0].TestTypeId,
                SubDepartmentId: details[0].SubDepartmentId,
                PatientBillStatusId: request.Header.PatientBillStatusId,
                BillNumber: v_BillNumber,
                FacilityId: request.Header.FacilityId,
                WardId: request.Header.WardId,
                RoomId: request.Header.RoomId,
                BedId: request.Header.BedId,
            };
            await patorderBo.ManageAdditionalInPatientOrder(order as any, details);
        }

        if (request.Header.PendingOrderId && request.Header.PendingOrderId > 0) {
            let PatientOrder = await patorderBo.GetPatientOrderById({ Id: request.Header.PendingOrderId });
            PatientOrder.OrderStatusId = 3;
            await patorderBo.Update(PatientOrder);
        }
    }

    // public async AddPatientPharmacyBills(req: BaseRequest): Promise<number> {
    //     if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
    //         if (req.Data.Header.BillTypeId === 3) {
    //             /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
    //             //await this.PopulateInpatientBills(req);//Newly included 27/9/22
    //             req.Data.Header.CreditVocher = 0;
    //         } else if (req.Data.Header.BillTypeId === 4) {
    //             /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
    //             req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
    //         }

    //         req.Data.Header.BillDateTime = new Date();
    //         for (let i = 0, len = req.Data.Details.length; i < len; i++) {
    //             req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
    //         }
    //         if (req.Data.Header.EncounterTypeId && req.Data.Header.PharmacySaleTypeId) {
    //             if (req.Data.Header.EncounterTypeId === 2
    //                 && req.Data.Header.PharmacySaleTypeId === 6) {
    //                 req.Data.Header.ToStoreId = req.Data.Header.StoreMasterId;
    //                 let detailnew: any = [];
    //                 let perscreq: any = {
    //                     Data: {
    //                         Header: req.Data.Header,
    //                         Details: detailnew
    //                     }
    //                 };
    //                 let details = req.Data.Details;
    //                 for (let i = 0, len = details.length; i < len; i++) {
    //                     if (details[i].SubCategoryId) {
    //                         if (details[i].SubCategoryId === 1) {
    //                             details[i].RequestedQuantity = details[i].Quantity;
    //                             details[i].DrugId = details[i].ItemMasterId;
    //                             perscreq.Data.Details.push(details[i]);
    //                         }
    //                     }
    //                 }
    //                 if (perscreq.Data.Details.length > 0) {
    //                     let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
    //                     await prescriptionBO.CreatePrescription(perscreq);
    //                     for (let pre = 0, len = req.Data.Details.length; pre < len; pre++) {
    //                         req.Data.Details[pre].Id = 0;
    //                     }
    //                 }
    //             }
    //         }
    //     }
    //     let BillPaymentTypeId = -1;
    //     for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
    //         if (req.Data.paymentDetail[i].Id <= 0) {
    //             req.Data.paymentDetail[i].ReceiptNumber = null; /* await Sequence.Next(SequenceKeys.ReceiptNrId); */
    //             req.Data.paymentDetail[i].ReceiptDateTime = new Date();
    //             req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
    //             req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
    //             req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
    //             req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
    //             req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
    //             req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

    //             if (BillPaymentTypeId < 0)
    //                 BillPaymentTypeId = req.Data.paymentDetail[i].PaymentTypeId;
    //         }
    //     }
    //     let result = await this.Save(req.Data.Header);
    //     if (result) {
    //         let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
    //         let PatientBillId = result.dataValues.Id;
    //         await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
    //         if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
    //             let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
    //             await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);

    //             let IsAdvanceAdjusting = 0;
    //             let PaidAgainstBill = 0;
    //             let AdjustedAgainstBill = 0;
    //             let DueTowardsBill = 0;
    //             if (req.Data.Header.BillTypeId === 4) {
    //                 if (req.Data.adjustmentDetail.length > 0) {
    //                     IsAdvanceAdjusting = 1;
    //                     AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

    //                     for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
    //                         if (req.Data.paymentDetail[i].Id > 0) {
    //                             for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
    //                                 req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
    //                             }
    //                         }
    //                     }

    //                     let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
    //                     await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
    //                 } else {
    //                     PaidAgainstBill = Number(req.Data.Header.PaidAmount);
    //                     DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
    //                 }
    //                 let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
    //                 let patientaccount = {};
    //                 patientaccount = {
    //                     Id: 0,
    //                     PatientId: req.Data.Header.PatientId,
    //                     EncounterId: req.Data.Header.EncounterId,
    //                     VisitNumber: req.Data.Header.VisitNumber,
    //                     TransactionTypeId: 2,
    //                     TransactionId: PatientBillId,
    //                     TransactionNumber: null,
    //                     TransactionDate: new Date(),
    //                     BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
    //                     PaidAmount: PaidAgainstBill,
    //                     IsAdvanceAdjusted: IsAdvanceAdjusting,
    //                     AdjustedAmount: AdjustedAgainstBill,
    //                     UnAdjustedAmount: 0,
    //                     DueAmount: DueTowardsBill,
    //                     DebitAmount: 0,
    //                     CreditAmount: 0
    //                 };
    //                 await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
    //             }

    //             if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {
    //                 if (req.Data.Details.length > 0) {
    //                     for (let k = 0, len = req.Data.Details.length; k < len; k++) {
    //                         if (req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
    //                             req.Data.Details.splice(k, 1);
    //                         }
    //                     }
    //                 }

    //                 let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
    //                 let stockResult = await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);
    //                 console.log('stock result');
    //                 console.log(stockResult);
    //                 console.log('stock result');
    //                 if (stockResult && stockResult.length > 0) {
    //                     let errorMessages: any = [];
    //                     _.forEach(stockResult, (item: any) => { errorMessages.push(item.error.name); });
    //                     throw { message: 'Stock Changes Happened for ' + errorMessages.join(',') };
    //                 }

    //                 let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
    //                 await stockmovementBO.ManageStockMovements(21, PatientBillId, req.Data);

    //                 if (req.Data.Header.IsPrescription === 1) {
    //                     let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
    //                     await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
    //                 }
    //             }
    //         }

    //         if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;

    //         let billIdentifier: any = null;
    //         if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
    //             if (req.Data.Header.BillTypeId === 3) {
    //                 billIdentifier = this.getSequenceIdentifier(SequenceKeys.IPBillNumberId);
    //             } else if (req.Data.Header.BillTypeId === 4) {
    //                 if (req.Data.Header.EncounterTypeId === 2) {
    //                     billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyIPBillNumberId);
    //                 } else {
    //                     billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyBillNumberId);
    //                 }

    //             }


    //             let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
    //             let billingPreferencesData =
    //                 await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
    //             let iPharmacyPaymodeSeqNr = 0;
    //             try {
    //                 iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
    //             } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
    //             if (iPharmacyPaymodeSeqNr) {
    //                 if (BillPaymentTypeId > 0) {
    //                     if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
    //                         billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCreditBill);
    //                     } else {
    //                         if (req.Data.Header.PharmacySaleTypeId === 4) { //Manual-Sales - MAN10001
    //                             billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyManualBill);
    //                         } else { //Cash-Sales - CAS10001
    //                             billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCashBill);
    //                         }
    //                     }
    //                 } else { //Credit-Sales- (outstanding) - CRT10001
    //                     billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCreditBill);
    //                 }
    //             }

    //         }

    //         if (billIdentifier) {
    //             const afterO: any = () => {
    //                 return ((bo, request, patientBillId) => {
    //                     return {
    //                         UpdateMovementInfo: async (code: string) => {
    //                             request.Data.Header.TransactionId = patientBillId;
    //                             request.Data.Header.TransactionNumber = code;
    //                             await bo.UpdateMovementInfo(request);
    //                         },
    //                         updatePatientAccountsInfo: async (code: string) => {
    //                             request.Data.Header.PatientBillId = patientBillId;
    //                             request.Data.Header.BillNumber = code;
    //                             await bo.UpdatePatientAccountsInfo(request);
    //                         },
    //                         SendBillDiscountSMS: async (code: string) => {
    //                             request.Data.Header.PatientBillId = patientBillId;
    //                             request.Data.BillNum = code;
    //                             await bo.SendBillDiscountSMS(request);
    //                         }
    //                     };
    //                 })(this, req, PatientBillId);
    //             };

    //             this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
    //                 [
    //                     afterO().UpdateMovementInfo,
    //                     afterO().updatePatientAccountsInfo,
    //                     afterO().SendBillDiscountSMS,
    //                 ]);

    //         }

    //         /*
    //         if (req.Data.ClaimDetails.length > 0) {
    //             await this.AddPatientPharmacyClaimBills(req);
    //         }
    //         */


    //         return PatientBillId;
    //     }

    //     return 0;
    // }

    public async IsAlreadyExist(req: any): Promise<number> {
        let billDate = new Date();
        // let FromDate = billDate.setMinutes(billDate.getMinutes() - 2);
        // let ToDate = billDate.setMinutes(billDate.getMinutes() + 2);
        let FromDate = billDate.setSeconds(billDate.getSeconds() - 30);
        let ToDate = billDate.setSeconds(billDate.getSeconds() + 30);
        let frmDate = moment(FromDate);
        let todate = moment(ToDate);
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.PatientId, Value: req.Data.Header.PatientId },
            { Key: PatientBillsFilters.BillType, Value: req.Data.Header.BillTypeId },
            { Key: PatientBillsFilters.FromDate, Value: frmDate },
            { Key: PatientBillsFilters.ToDate, Value: todate }]
        };
        if (req.Data.Header.fromjssscreen && req.Data.Header.fromjssscreen === true) {
            apiReq.Params.push({
                Key: PatientBillsFilters.BillAmount, Value: req.Data.Header.BillAmount
            });
            // apiReq.Params.push({
            //     Key: PatientBillsFilters.EncounterId, Value: req.Data.Header.EncounterId
            // });
        }
        if (req.Data.Header.checkBill && req.Data.Header.checkBill === true) {
            apiReq.Params.push({
                Key: PatientBillsFilters.BillAmount, Value: req.Data.Header.BillAmount
            });
            apiReq.Params.push({
                Key: PatientBillsFilters.EncounterId, Value: req.Data.Header.EncounterId
            });
        }
        let data = await this.GetMinPatientBills(apiReq);
        if (data.Data && data.Data.length > 0) {
            return (data.Data.length * -1);
        }
        return 1;
    }

    public async IsFinalBillAlreadyExist(req: any): Promise<number> {
        // let billDate = new Date();
        // let FromDate = billDate.setMinutes(billDate.getMinutes() - 2);
        // let ToDate = billDate.setMinutes(billDate.getMinutes() + 2);
        // let FromDate = billDate.setSeconds(billDate.getSeconds() - 30);
        // let ToDate = billDate.setSeconds(billDate.getSeconds() + 30);
        // let frmDate = moment(FromDate);
        // let todate = moment(ToDate);
        req.Data.Header.checkBill = true;
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.PatientId, Value: req.Data.Header.PatientId },
            { Key: PatientBillsFilters.BillType, Value: req.Data.Header.BillTypeId },
            { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
                // { Key: PatientBillsFilters.FromDate, Value: frmDate },
                // { Key: PatientBillsFilters.ToDate, Value: todate }
            ]
        };
        if (req.Data.Header.checkBill && req.Data.Header.checkBill === true) {
            apiReq.Params.push({
                Key: PatientBillsFilters.BillAmount, Value: req.Data.Header.BillAmount
            });
            apiReq.Params.push({
                Key: PatientBillsFilters.EncounterId, Value: req.Data.Header.EncounterId
            });
        }
        let data = await this.GetMinPatientBills(apiReq);
        if (data.Data && data.Data.length > 0) {
            return (data.Data.length * -1);
        }
        return 1;
    }
    public async GetMinPatientBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let include: Array<IncludeOptions> = [];
        include.push(this.GetReference('PatientBillStatus'));
        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillAmount:
                        where['BillAmount'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientId:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.TransferEncounterId:
                        where['TransferEncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.BedId:
                        where['BedId'] = param.Value;
                        break;
                    case PatientBillsFilters.RoomId:
                        where['RoomId'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.BillDateTime:
                        console.log('*************PAram*********');
                        console.log(param.Value);
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    default:
                        console.log(param.Key);
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'], required: false,
            include: [
                this.GetReference('Title'),
                this.GetReference('Gender')
            ]
        });
        include.push({
            model: this.Models.StoreMaster, attributes: ['Id', 'ISSeparatePayCounter'], required: false,
            // include: [
            //     this.GetReference('Title')
            // ]
        });
        include.push({
            model: this.Models.Facility, attributes: ['FacilityName', 'AddressLine1', 'Mobile',
                'Email', 'GstNumber'
            ], required: false,
            // include: [
            //     this.GetReference('Title')
            // ]
        });
        include.push({
            model: this.Models.Referral, attributes: ['Id', 'ReferralName'
            ], required: false,
            // include: [
            //     this.GetReference('Title')
            // ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'SignPath', 'UserName'], as: 'CreatedUser', required: false,
            // include: [
            //     this.GetReference('Title')
            // ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'TitleId', 'UserName'], required: false,
            as: 'UserDoctor',
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.Encounter,
            attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
            required: false
        });
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        apiReq.Attributes = ['Id', 'BillAmount', 'BillDiscount',
            'PaidAmount', 'OutStandingAmount', 'FacilityId',
            'RefundAmount', 'BillAmount', 'BillDiscount', 'PatientBillStatusId', 'BillDateTime',
            'PatientId', 'BillTypeId', 'BillNumber', 'ReturnedAmount',
            'RoundOffValue', 'CGstAmount', 'SGstAmount', 'StoreMasterId',
            'GSTAmount', 'StoreMasterId', 'IsPharmacyBill', 'CreditApproved',
            'IsPaidFully', 'PharmacySaleTypeId', 'CreatedBy', 'NetInsuranceAmount',
            'TitleId', 'PatientName', 'GenderId', 'Age', 'DOB', 'Mobile', 'ReferralId',
            'EncounterId', 'DoctorId', 'TransferEncounterId', 'NonMedicalAmount',
            'WardId', 'BedId', 'RoomId', 'FSTypeId', 'GuarantorTypeId', 'CoPayAmount',
            'DiscountApprovedBy', 'BillDiscountModeId', 'NetPatientAmount', 'AgreementDiscountAmt',
            'NonMedicalAmount', 'Disallowed', 'Comments',
            'ParentBillId'

        ];
        apiReq.Attributes = apiReq.Attributes;
        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes });
    }
    // public async AddPatientPharmacyBills(req: BaseRequest): Promise<number> {
    //     const patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
    //     if (req.Data.Header.addPatient === true) {
    //         let memberData: any = {
    //             Data: {
    //                 Id: 0,
    //                 MRNTypeId: 1,
    //                 NationalityId: req.Data.Header.NationalityId,
    //                 TitleId: (req.Data.Header.TitleId) ? req.Data.Header.TitleId : -1,
    //                 GenderId: (req.Data.Header.GenderId) ? req.Data.Header.GenderId : -1,
    //                 FirstName: req.Data.Header.PatientName,
    //                 FacilityId: req.Data.Header.FacilityId,
    //                 PatientStatus: 'Active',
    //                 PatientStatusId: 2,
    //                 Mobile: req.Data.Header.Mobile,
    //                 AddressLine1: req.Data.Header.Address,
    //                 RegisteredDate: new Date(),
    //                 Age: req.Data.Header.Age,
    //                 DOB: req.Data.Header.DOB,
    //                 From: 'fromdirectsales'
    //             }
    //         };
    //         let memId: any = await patientBO.AddPatient(memberData);
    //         if (memId && memId > 0) {
    //             req.Data.Header.PatientId = memId;
    //         }
    //     }

    //     if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
    //         if (req.Data.Header.BillTypeId === 3) {
    //             /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
    //             //await this.PopulateInpatientBills(req);//Newly included 27/9/22
    //             req.Data.Header.CreditVocher = 0;
    //         } else if (req.Data.Header.BillTypeId === 4) {
    //             /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
    //             req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
    //         }

    //         req.Data.Header.BillDateTime = new Date();
    //         for (let i = 0, len = req.Data.Details.length; i < len; i++) {
    //             req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
    //         }
    //         if (req.Data.Header.EncounterTypeId && req.Data.Header.PharmacySaleTypeId) {
    //             if (req.Data.Header.EncounterTypeId === 2
    //                 && req.Data.Header.PharmacySaleTypeId === 6) {
    //                 req.Data.Header.ToStoreId = req.Data.Header.StoreMasterId;
    //                 let detailnew: any = [];
    //                 let perscreq: any = {
    //                     Data: {
    //                         Header: req.Data.Header,
    //                         Details: detailnew
    //                     }
    //                 };
    //                 let details = req.Data.Details;
    //                 for (let i = 0, len = details.length; i < len; i++) {
    //                     if (details[i].SubCategoryId) {
    //                         if (details[i].SubCategoryId === 1) {
    //                             details[i].RequestedQuantity = details[i].Quantity;
    //                             details[i].DrugId = details[i].ItemMasterId;
    //                             perscreq.Data.Details.push(details[i]);
    //                         }
    //                     }
    //                 }
    //                 if (perscreq.Data.Details.length > 0) {
    //                     let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
    //                     await prescriptionBO.CreatePrescription(perscreq);
    //                     for (let pre = 0, len = req.Data.Details.length; pre < len; pre++) {
    //                         req.Data.Details[pre].Id = 0;
    //                     }
    //                 }
    //             }
    //         }
    //     }
    //     let BillPaymentTypeId = -1;
    //     for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
    //         if (req.Data.paymentDetail[i].Id <= 0) {
    //             req.Data.paymentDetail[i].ReceiptNumber = null; /* await Sequence.Next(SequenceKeys.ReceiptNrId); */
    //             req.Data.paymentDetail[i].ReceiptDateTime = new Date();
    //             req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
    //             req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
    //             req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
    //             req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
    //             req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
    //             req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

    //             if (BillPaymentTypeId < 0)
    //                 BillPaymentTypeId = req.Data.paymentDetail[i].PaymentTypeId;
    //         }
    //     }
    //     // if (req.Data.Header.IsDirectDGBill === false || !req.Data.Header.IsDirectDGBill) {
    //     if (await this.IsAlreadyExist(req) <= -1) return -1;
    //     // }
    //     if (!req.Data.Header.BillGeneratedBy || req.Data.Header.BillGeneratedBy < 1) {
    //         req.Data.Header.BillGeneratedBy = this.Session.UserId;
    //     }
    //     if (!req.Data.Header.FacilityId || req.Data.Header.FacilityId < 1) req.Data.Header.FacilityId = this.Session.FacilityId;

    //     let result = await this.Save(req.Data.Header);


    //     const patient: any = await patientBO.GetById(req.Data.Header.PatientId);
    //     if (patient && patient.Mobile) {
    //         let eventTemplateBO = BoFactory.GetBo(userbo.EventTemplateBo, this.Request);
    //         let smsTemplateInfo =
    //             await eventTemplateBO.GetTemplateInfo('PharmacySms', 'PharmacySms', 1);
    //         if (smsTemplateInfo) {
    //             let smsmodel: any = {};
    //             if (SmsConfig['PROVIDER'] === 'HOSMAT') {
    //                 smsmodel = {
    //                     numbers: [patient.Mobile],
    //                     message: Template.Compile(smsTemplateInfo.TemplateContent,
    //                         {
    //                             patientName: patient.FirstName,
    //                             contactNo: this.Session.FacilityContact
    //                         })
    //                 };
    //             } else {
    //                 smsmodel = {
    //                     numbers: [patient.Mobile],
    //                     message: Template.Compile(smsTemplateInfo.TemplateContent,
    //                         {
    //                             patientName: patient.FirstName,
    //                             contactNo: this.Session.FacilityContact
    //                         })
    //                 };
    //             }
    //             let smsProvider = this.GetSmsProvider();
    //             if (smsProvider) {
    //                 let SMSStatus = await smsProvider.send(smsmodel);
    //                 let eventDashboardOutboundBo = BoFactory.GetBo(userbo.EventDashboardBo, this.Request);
    //                 await eventDashboardOutboundBo.ManageSMSOutBound(SMSStatus, smsmodel.message + ' To : ' + patient.Mobile);
    //             }
    //         }
    //     }
    //     if (result) {
    //         let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
    //         let PatientBillId = result.dataValues.Id;
    //         await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
    //         if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
    //             let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
    //             try {
    //                 await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
    //             } catch (ex) { throw { message: ex.message }; }

    //             // let TransactionId = Number(PatientBillId);
    //             // let groupInstance = await PaymentdetailBO.Find({
    //             //     where: { PatientBillId: TransactionId }
    //             // });
    //             // if (!groupInstance) {
    //             //     throw { message: 'Error in Generating Receipt' };
    //             // }

    //             let IsAdvanceAdjusting = 0;
    //             let PaidAgainstBill = 0;
    //             let AdjustedAgainstBill = 0;
    //             let DueTowardsBill = 0;
    //             if (req.Data.Header.BillTypeId === 4) {
    //                 if (req.Data.adjustmentDetail.length > 0) {
    //                     IsAdvanceAdjusting = 1;
    //                     AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

    //                     for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
    //                         if (req.Data.paymentDetail[i].Id > 0) {
    //                             for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
    //                                 req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
    //                             }
    //                         }
    //                     }

    //                     let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
    //                     await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
    //                 } else {
    //                     PaidAgainstBill = Number(req.Data.Header.PaidAmount);
    //                     DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
    //                 }
    //                 let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
    //                 let patientaccount = {};
    //                 patientaccount = {
    //                     Id: 0,
    //                     PatientId: req.Data.Header.PatientId,
    //                     EncounterId: req.Data.Header.EncounterId,
    //                     VisitNumber: req.Data.Header.VisitNumber,
    //                     TransactionTypeId: 2,
    //                     TransactionId: PatientBillId,
    //                     TransactionNumber: null,
    //                     TransactionDate: new Date(),
    //                     BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
    //                     PaidAmount: PaidAgainstBill,
    //                     IsAdvanceAdjusted: IsAdvanceAdjusting,
    //                     AdjustedAmount: AdjustedAgainstBill,
    //                     UnAdjustedAmount: 0,
    //                     DueAmount: DueTowardsBill,
    //                     DebitAmount: 0,
    //                     CreditAmount: 0
    //                 };
    //                 try {
    //                     await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
    //                 } catch (ex) { throw { message: 'Error in Updating Accounts' }; }
    //             }

    //             if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {
    //                 if (req.Data.Details.length > 0) {
    //                     for (let k = 0, len = req.Data.Details.length; k < len; k++) {
    //                         if (req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
    //                             req.Data.Details.splice(k, 1);
    //                         }
    //                     }
    //                 }

    //                 let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
    //                 try {
    //                     let stockResult = await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);
    //                     console.log('stock result');
    //                     console.log(stockResult);
    //                     console.log('stock result');
    //                     // if (stockResult && stockResult.length > 0) {
    //                     //     let errorMessages: any = [];
    //                     //     _.forEach(stockResult, (item: any) =>
    //                     // { errorMessages.push(item.error.ItemMasterId + ':' +
    //                     // item.error.name); });
    //                     //     throw { message: 'Stock Changes Happened for ' + errorMessages.join(',') };
    //                     // }
    //                 } catch (ex) {
    //                     let errorMessages: any = [];
    //                     _.forEach(ex.message, (item: any) => { errorMessages.push(item.ItemMasterId + ':' + item.name); });
    //                     throw { message: 'Stock Changes Happened for ' + errorMessages.join('$,$') };
    //                 }

    //                 let stockmovementBo = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
    //                 let TransactionId = Number(PatientBillId);
    //                 let groupInstance = await stockmovementBo.Find({
    //                     where: {
    //                         TransactionId: TransactionId,
    //                         TransactionTypeId: 21
    //                     }
    //                 });
    //                 if (!groupInstance) {
    //                     throw { message: 'Error in Updating Movement' };
    //                 }
    //                 // let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
    //                 // // try {
    //                 // let stockmovResult = await stockmovementBO.ManageStockMovements1(21, PatientBillId, req.Data);
    //                 // if (stockmovResult && stockmovResult.length > 0) {
    //                 //     let errorMessages: any = [];
    //                 //     _.forEach(stockmovResult, (item: any) => { errorMessages.push(item.error.name); });
    //                 //     // throw { message: 'Something went Wrong in ' + errorMessages.join(',') };
    //                 //     throw { message: 'Network Issue.. Please try again' };
    //                 // }
    //                 // } catch (ex) {
    //                 //console.log(ex);
    //                 // }


    //                 if (req.Data.Header.IsPrescription === 1) {
    //                     let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
    //                     await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
    //                 }
    //             }
    //         }

    //         if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;

    //         let billIdentifier: any = null;
    //         if (req.Data.Header.IsStoreSeparateSequence && req.Data.Header.IsStoreSeparateSequence === true) {
    //             if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
    //                 billIdentifier = this.getSepStoreSequenceIdentifier(SequenceKeys.PharmacyBillNumberId,
    //                     req.Data.Header.StoreMasterId
    //                 );

    //             }
    //         } else {
    //             if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
    //                 if (req.Data.Header.BillTypeId === 3) {
    //                     billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId,
    //                         req.Data.Header.FacilityId
    //                     );
    //                 } else if (req.Data.Header.BillTypeId === 4) {
    //                     if (req.Data.Header.EncounterTypeId === 2) {
    //                         billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyIPBillNumberId,
    //                             req.Data.Header.FacilityId
    //                         );
    //                     } else {
    //                         if (req.Data.Header.CreditVocher > 0) {
    //                             billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyDueBillNumberId,
    //                                 req.Data.Header.FacilityId
    //                             );
    //                         } else {
    //                             billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId,
    //                                 req.Data.Header.FacilityId
    //                             );
    //                         }

    //                     }

    //                 }


    //                 let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
    //                 let billingPreferencesData =
    //                     await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
    //                 let iPharmacyPaymodeSeqNr = 0;
    //                 try {
    //                     iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
    //                 } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
    //                 if (iPharmacyPaymodeSeqNr) {
    //                     if (BillPaymentTypeId > 0) {
    //                         if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
    //                             billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
    //                                 req.Data.Header.FacilityId
    //                             );
    //                         } else {
    //                             if (req.Data.Header.PharmacySaleTypeId === 4) { //Manual-Sales - MAN10001
    //                                 billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyManualBill,
    //                                     req.Data.Header.FacilityId
    //                                 );
    //                             } else { //Cash-Sales - CAS10001
    //                                 billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCashBill,
    //                                     req.Data.Header.FacilityId
    //                                 );
    //                             }
    //                         }
    //                     } else { //Credit-Sales- (outstanding) - CRT10001
    //                         billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
    //                             req.Data.Header.FacilityId
    //                         );
    //                     }
    //                 }

    //             }
    //         }
    //         if (billIdentifier) {
    //             const afterO: any = () => {
    //                 return ((bo, request, patientBillId) => {
    //                     return {
    //                         UpdateMovementInfo: async (code: string) => {
    //                             try {
    //                                 request.Data.Header.TransactionId = patientBillId;
    //                                 request.Data.Header.TransactionNumber = code;
    //                                 await bo.UpdateMovementInfo(request);
    //                             } catch (error) {
    //                                 console.error('Error in UpdateMovementInfo:', error);
    //                                 throw error;  // Handle the error as needed
    //                             }
    //                         },
    //                         updatePatientAccountsInfo: async (code: string) => {
    //                             try {
    //                                 request.Data.Header.PatientBillId = patientBillId;
    //                                 request.Data.Header.BillNumber = code;
    //                                 await bo.UpdatePatientAccountsInfo(request);
    //                             } catch (error) {
    //                                 console.error('Error in updatePatientAccountsInfo:', error);
    //                                 throw error;  // Handle the error as needed
    //                             }
    //                         },
    //                         SendBillDiscountSMS: async (code: string) => {
    //                             try {
    //                                 request.Data.Header.PatientBillId = patientBillId;
    //                                 request.Data.BillNum = code;
    //                                 await bo.SendBillDiscountSMS(request);
    //                             } catch (error) {
    //                                 console.error('Error in SendBillDiscountSMS:', error);
    //                                 throw error;  // Handle the error as needed
    //                             }
    //                         }
    //                     };
    //                 })(this, req, PatientBillId);
    //             };

    //             // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
    //             //     [
    //             //         afterO().UpdateMovementInfo,
    //             //         afterO().updatePatientAccountsInfo,
    //             //         afterO().SendBillDiscountSMS,
    //             //     ]);
    //             const handleDeferredExecution = async () => {
    //                 try {
    //                     // Assuming deferSequenceKey processes a list of functions
    //                     await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
    //                         [
    //                             afterO().UpdateMovementInfo,
    //                             afterO().updatePatientAccountsInfo,
    //                             afterO().SendBillDiscountSMS,
    //                         ]);
    //                 } catch (error) {
    //                     // console.error('Error in deferSequenceKey execution:', error);
    //                     throw error;
    //                 }
    //             };

    //             await handleDeferredExecution();

    //         }

    //         /*
    //         if (req.Data.ClaimDetails.length > 0) {
    //             await this.AddPatientPharmacyClaimBills(req);
    //         }
    //         */


    //         return PatientBillId;
    //     }

    //     return 0;
    // }

    public async AddPatientPharmacyBills(req: BaseRequest): Promise<number> {
        const patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
        if (req.Data.Header.addPatient && req.Data.Header.addPatient === true) {
            try {
                const patientId = await this.registerPatientIfNeeded(req, this.Session);
                if (patientId) req.Data.Header.PatientId = patientId;
            } catch (err) {
                // logger.error(err);
                // logger.info(err);
                // Optionally: rollback anything already done (e.g., undo stock, prescriptions)
                return Promise.reject({
                    message: 'Patient registration failed, cannot proceed with billing.',
                    error: err,
                });

            }
        }

        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
                //await this.PopulateInpatientBills(req);//Newly included 27/9/22
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 4) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
            if (req.Data.Header.EncounterTypeId && req.Data.Header.PharmacySaleTypeId) {
                if (req.Data.Header.EncounterTypeId === 2
                    && req.Data.Header.PharmacySaleTypeId === 6) {
                    req.Data.Header.ToStoreId = req.Data.Header.StoreMasterId;
                    let detailnew: any = [];
                    let perscreq: any = {
                        Data: {
                            Header: req.Data.Header,
                            Details: detailnew
                        }
                    };
                    let details = req.Data.Details;
                    for (let i = 0, len = details.length; i < len; i++) {
                        if (details[i].SubCategoryId) {
                            if (details[i].SubCategoryId === 1) {
                                details[i].RequestedQuantity = details[i].Quantity;
                                details[i].DrugId = details[i].ItemMasterId;
                                perscreq.Data.Details.push(details[i]);
                            }
                        }
                    }
                    if (perscreq.Data.Details.length > 0) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.CreatePrescription(perscreq);
                        for (let pre = 0, len = req.Data.Details.length; pre < len; pre++) {
                            req.Data.Details[pre].Id = 0;
                        }
                    }
                }
            }
        }
        let BillPaymentTypeId = -1;
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptNumber = null; /* await Sequence.Next(SequenceKeys.ReceiptNrId); */
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
                req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

                if (BillPaymentTypeId < 0)
                    BillPaymentTypeId = req.Data.paymentDetail[i].PaymentTypeId;
            }
        }
        // if (req.Data.Header.IsDirectDGBill === false || !req.Data.Header.IsDirectDGBill) {
        if (await this.IsAlreadyExist(req) <= -1) throw { error: 'Bill already generated' };
        // }
        if (!req.Data.Header.BillGeneratedBy || req.Data.Header.BillGeneratedBy < 1) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId || req.Data.Header.FacilityId < 1) req.Data.Header.FacilityId = this.Session.FacilityId;

        let result = await this.Save(req.Data.Header);


        const patient: any = await patientBO.GetById(req.Data.Header.PatientId);
        if (patient && patient.Mobile) {
            let eventTemplateBO = BoFactory.GetBo(userbo.EventTemplateBo, this.Request);
            let smsTemplateInfo =
                await eventTemplateBO.GetTemplateInfo('PharmacySms', 'PharmacySms', 1);
            if (smsTemplateInfo) {
                let smsmodel: any = {};
                if (SmsConfig['PROVIDER'] === 'HOSMAT') {
                    smsmodel = {
                        numbers: [patient.Mobile],
                        message: Template.Compile(smsTemplateInfo.TemplateContent,
                            {
                                patientName: patient.FirstName,
                                contactNo: this.Session.FacilityContact
                            })
                    };
                } else {
                    smsmodel = {
                        numbers: [patient.Mobile],
                        message: Template.Compile(smsTemplateInfo.TemplateContent,
                            {
                                patientName: patient.FirstName,
                                contactNo: this.Session.FacilityContact
                            })
                    };
                }
                let smsProvider = this.GetSmsProvider();
                if (smsProvider) {
                    let SMSStatus = await smsProvider.send(smsmodel);
                    let eventDashboardOutboundBo = BoFactory.GetBo(userbo.EventDashboardBo, this.Request);
                    await eventDashboardOutboundBo.ManageSMSOutBound(SMSStatus, smsmodel.message + ' To : ' + patient.Mobile);
                }
            }
        }
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                try {
                    await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                } catch (ex) { throw { message: ex.message }; }

                // let TransactionId = Number(PatientBillId);
                // let groupInstance = await PaymentdetailBO.Find({
                //     where: { PatientBillId: TransactionId }
                // });
                // if (!groupInstance) {
                //     throw { message: 'Error in Generating Receipt' };
                // }

                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.BillTypeId === 4) {
                    if (req.Data.adjustmentDetail.length > 0) {
                        IsAdvanceAdjusting = 1;
                        AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

                        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                            if (req.Data.paymentDetail[i].Id > 0) {
                                for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                    req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                }
                            }
                        }

                        let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                        await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                    } else {
                        PaidAgainstBill = Number(req.Data.Header.PaidAmount);
                        DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                    }
                    let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                    let patientaccount = {};
                    patientaccount = {
                        Id: 0,
                        PatientId: req.Data.Header.PatientId,
                        EncounterId: req.Data.Header.EncounterId,
                        VisitNumber: req.Data.Header.VisitNumber,
                        TransactionTypeId: 2,
                        TransactionId: PatientBillId,
                        TransactionNumber: null,
                        TransactionDate: new Date(),
                        BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
                        PaidAmount: PaidAgainstBill,
                        IsAdvanceAdjusted: IsAdvanceAdjusting,
                        AdjustedAmount: AdjustedAgainstBill,
                        UnAdjustedAmount: 0,
                        DueAmount: DueTowardsBill,
                        DebitAmount: 0,
                        CreditAmount: 0
                    };
                    try {
                        await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                    } catch (ex) { throw { message: 'Error in Updating Accounts' }; }
                }

                if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {
                    if (req.Data.Details.length > 0) {
                        for (let k = 0, len = req.Data.Details.length; k < len; k++) {
                            if (req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
                                req.Data.Details.splice(k, 1);
                            }
                        }
                    }

                    let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
                    try {
                        let stockResult = await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);
                        console.log('stock result');
                        console.log(stockResult);
                        console.log('stock result');
                        // if (stockResult && stockResult.length > 0) {
                        //     let errorMessages: any = [];
                        //     _.forEach(stockResult, (item: any) =>
                        // { errorMessages.push(item.error.ItemMasterId + ':' +
                        // item.error.name); });
                        //     throw { message: 'Stock Changes Happened for ' + errorMessages.join(',') };
                        // }
                    } catch (ex) {
                        let errorMessages: any = [];
                        _.forEach(ex.message, (item: any) => { errorMessages.push(item.ItemMasterId + ':' + item.name); });
                        throw { message: 'Stock Changes Happened for ' + errorMessages.join('$,$') };
                    }

                    let stockmovementBo = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
                    let TransactionId = Number(PatientBillId);
                    let groupInstance = await stockmovementBo.Find({
                        where: {
                            TransactionId: TransactionId,
                            TransactionTypeId: 21
                        }
                    });
                    if (!groupInstance) {
                        throw { message: 'Error in Updating Movement' };
                    }
                    // let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
                    // // try {
                    // let stockmovResult = await stockmovementBO.ManageStockMovements1(21, PatientBillId, req.Data);
                    // if (stockmovResult && stockmovResult.length > 0) {
                    //     let errorMessages: any = [];
                    //     _.forEach(stockmovResult, (item: any) => { errorMessages.push(item.error.name); });
                    //     // throw { message: 'Something went Wrong in ' + errorMessages.join(',') };
                    //     throw { message: 'Network Issue.. Please try again' };
                    // }
                    // } catch (ex) {
                    //console.log(ex);
                    // }


                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                }
            }

            if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;

            let billIdentifier: any = null;
            if (req.Data.Header.IsStoreSeparateSequence && req.Data.Header.IsStoreSeparateSequence === true) {
                if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                    billIdentifier = this.getSepStoreSequenceIdentifier(SequenceKeys.PharmacyBillNumberId,
                        req.Data.Header.StoreMasterId
                    );

                }
            } else {
                if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                    if (req.Data.Header.BillTypeId === 3) {
                        billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId,
                            req.Data.Header.FacilityId
                        );
                    } else if (req.Data.Header.BillTypeId === 4) {
                        if (req.Data.Header.EncounterTypeId === 2) {
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyIPBillNumberId,
                                req.Data.Header.FacilityId
                            );
                        } else {
                            if (req.Data.Header.CreditVocher > 0) {
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyDueBillNumberId,
                                    req.Data.Header.FacilityId
                                );
                            } else {
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId,
                                    req.Data.Header.FacilityId
                                );
                            }

                        }

                    }


                    let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                    let billingPreferencesData =
                        await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                    let iPharmacyPaymodeSeqNr = 0;
                    try {
                        iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                    } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
                    if (iPharmacyPaymodeSeqNr) {
                        if (BillPaymentTypeId > 0) {
                            if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                    req.Data.Header.FacilityId
                                );
                            } else {
                                if (req.Data.Header.PharmacySaleTypeId === 4) { //Manual-Sales - MAN10001
                                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyManualBill,
                                        req.Data.Header.FacilityId
                                    );
                                } else { //Cash-Sales - CAS10001
                                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCashBill,
                                        req.Data.Header.FacilityId
                                    );
                                }
                            }
                        } else { //Credit-Sales- (outstanding) - CRT10001
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                req.Data.Header.FacilityId
                            );
                        }
                    }

                }
            }
            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateMovementInfo: async (code: string) => {
                                try {
                                    request.Data.Header.TransactionId = patientBillId;
                                    request.Data.Header.TransactionNumber = code;
                                    await bo.UpdateMovementInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdateMovementInfo:', error);
                                    // logger.error('Error in UpdateMovementInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdatePatientAccountsInfo(request);
                                } catch (error) {
                                    // logger.error('Error in updatePatientAccountsInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            SendBillDiscountSMS: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.BillNum = code;
                                    await bo.SendBillDiscountSMS(request);
                                } catch (error) {
                                    // logger.error('Error in SendBillDiscountSMS:', error);
                                    throw error;  // Handle the error as needed
                                }
                            }
                        };
                    })(this, req, PatientBillId);
                };

                // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                //     [
                //         afterO().UpdateMovementInfo,
                //         afterO().updatePatientAccountsInfo,
                //         afterO().SendBillDiscountSMS,
                //     ]);
                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().UpdateMovementInfo,
                                afterO().updatePatientAccountsInfo,
                                afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        // logger.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                await handleDeferredExecution();

            }

            /*
            if (req.Data.ClaimDetails.length > 0) {
                await this.AddPatientPharmacyClaimBills(req);
            }
            */


            return PatientBillId;
        }

        return 0;
    }

    public async AddConsignmentBills(req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
                //await this.PopulateInpatientBills(req);//Newly included 27/9/22
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 4) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
            if (req.Data.Header.EncounterTypeId && req.Data.Header.PharmacySaleTypeId) {
                if (req.Data.Header.EncounterTypeId === 2
                    && req.Data.Header.PharmacySaleTypeId === 6) {
                    req.Data.Header.ToStoreId = req.Data.Header.StoreMasterId;
                    let detailnew: any = [];
                    let perscreq: any = {
                        Data: {
                            Header: req.Data.Header,
                            Details: detailnew
                        }
                    };
                    let details = req.Data.Details;
                    for (let i = 0, len = details.length; i < len; i++) {
                        if (details[i].SubCategoryId) {
                            if (details[i].SubCategoryId === 1) {
                                details[i].RequestedQuantity = details[i].Quantity;
                                details[i].DrugId = details[i].ItemMasterId;
                                perscreq.Data.Details.push(details[i]);
                            }
                        }
                    }
                    if (perscreq.Data.Details.length > 0) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.CreatePrescription(perscreq);
                        for (let pre = 0, len = req.Data.Details.length; pre < len; pre++) {
                            req.Data.Details[pre].Id = 0;
                        }
                    }
                }
            }
        }
        let BillPaymentTypeId = -1;
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptNumber = null; /* await Sequence.Next(SequenceKeys.ReceiptNrId); */
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
                req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

                if (BillPaymentTypeId < 0)
                    BillPaymentTypeId = req.Data.paymentDetail[i].PaymentTypeId;
            }
        }
        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            await detailBO.ManageIPatientBillDetails(PatientBillId, req.Data.Details);
            let encId = req.Data.Header.EncounterId;
            let IsPatientTransfer = false;
            await billSummBo.ManagePatBillSummary(IsPatientTransfer, encId, req.Data.Details);
            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                try {
                    await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                } catch (ex) {
                    throw { message: ex.message };
                }
                // let TransactionId = Number(PatientBillId);
                // let groupInstance = await PaymentdetailBO.Find({
                //     where: { PatientBillId: TransactionId }
                // });
                // if (!groupInstance) {
                //     throw { message: 'Error in Generating Receipt' };
                // }
                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.BillTypeId === 4) {
                    if (req.Data.adjustmentDetail.length > 0) {
                        IsAdvanceAdjusting = 1;
                        AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

                        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                            if (req.Data.paymentDetail[i].Id > 0) {
                                for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                    req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                }
                            }
                        }

                        let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                        await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                    } else {
                        PaidAgainstBill = Number(req.Data.Header.PaidAmount);
                        DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                    }
                    let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                    let patientaccount = {};
                    patientaccount = {
                        Id: 0,
                        PatientId: req.Data.Header.PatientId,
                        EncounterId: req.Data.Header.EncounterId,
                        VisitNumber: req.Data.Header.VisitNumber,
                        TransactionTypeId: 2,
                        TransactionId: PatientBillId,
                        TransactionNumber: null,
                        TransactionDate: new Date(),
                        BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
                        PaidAmount: PaidAgainstBill,
                        IsAdvanceAdjusted: IsAdvanceAdjusting,
                        AdjustedAmount: AdjustedAgainstBill,
                        UnAdjustedAmount: 0,
                        DueAmount: DueTowardsBill,
                        DebitAmount: 0,
                        CreditAmount: 0
                    };
                    await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                }

                if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {
                    if (req.Data.Details.length > 0) {
                        for (let k = 0, len = req.Data.Details.length; k < len; k++) {
                            if (req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
                                req.Data.Details.splice(k, 1);
                            }
                        }
                    }

                    let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
                    try {
                        await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);
                        //console.log(stockResult);
                    } catch (ex) {
                        let errorMessages: any = [];
                        _.forEach(ex.message, (item: any) => { errorMessages.push(item.ItemMasterId + ':' + item.name); });
                        throw { message: 'Stock Changes Happened for ' + errorMessages.join('$,$') };
                    }

                    // let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
                    // await stockmovementBO.ManageStockMovements1(21, PatientBillId, req.Data);

                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                }
            }

            if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;

            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId,
                        req.Data.Header.FacilityId
                    );
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId,
                        req.Data.Header.FacilityId
                    );
                }


                let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let billingPreferencesData =
                    await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                let iPharmacyPaymodeSeqNr = 0;
                try {
                    iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
                if (iPharmacyPaymodeSeqNr) {
                    if (BillPaymentTypeId > 0) {
                        if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                req.Data.Header.FacilityId
                            );
                        } else {
                            if (req.Data.Header.PharmacySaleTypeId === 4) { //Manual-Sales - MAN10001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyManualBill,
                                    req.Data.Header.FacilityId
                                );
                            } else { //Cash-Sales - CAS10001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCashBill,
                                    req.Data.Header.FacilityId
                                );
                            }
                        }
                    } else { //Credit-Sales- (outstanding) - CRT10001
                        billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                            req.Data.Header.FacilityId
                        );
                    }
                }

            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateMovementInfo: async (code: string) => {
                                try {
                                    request.Data.Header.TransactionId = patientBillId;
                                    request.Data.Header.TransactionNumber = code;
                                    await bo.UpdateMovementInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdateMovementInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdatePatientAccountsInfo(request);
                                } catch (error) {
                                    console.error('Error in updatePatientAccountsInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            SendBillDiscountSMS: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.BillNum = code;
                                    await bo.SendBillDiscountSMS(request);
                                } catch (error) {
                                    console.error('Error in SendBillDiscountSMS:', error);
                                    throw error;  // Handle the error as needed
                                }
                            }
                        };
                    })(this, req, PatientBillId);
                };

                // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                //     [
                //         afterO().UpdateMovementInfo,
                //         afterO().updatePatientAccountsInfo,
                //         afterO().SendBillDiscountSMS,
                //     ]);
                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().UpdateMovementInfo,
                                afterO().updatePatientAccountsInfo,
                                afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                handleDeferredExecution();


            }

            /*
            if (req.Data.ClaimDetails.length > 0) {
                await this.AddPatientPharmacyClaimBills(req);
            }
            */
            if (req.Data.poItemlines) {
                let purchaseOrderBO = BoFactory.GetBo(inventoryBO.PurchaseOrderBo, this.Request);
                req.Data.poItemlines.PoDate = new Date();
                req.Data.poItemlines.PatientBillId = PatientBillId;
                let POData: any = {
                    Data: {
                        Header: req.Data.poItemlines,
                        Details: req.Data.poItemlines.purchaseorderDetails
                    }
                };
                await purchaseOrderBO.AddPurchaseOrder(POData);
            }

            return PatientBillId;
        }

        return 0;
    }

    public async AddIPPatientPharmacyBills(req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
                //await this.PopulateInpatientBills(req);//Newly included 27/9/22
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 4) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }
            if (req.Data.Header.PatientStockRequestId) {
                let stockReqBO = BoFactory.GetBo(inpatientBO.PatientStockRequestsBo, this.Request);
                let patstockReqData = await stockReqBO.GetPatientStockRequestsById({ Id: req.Data.Header.PatientStockRequestId });
                let reqUpdate: any = {
                    Id: patstockReqData.Id,
                    PatientRequestStatusId: 5
                };
                await stockReqBO.Update(reqUpdate);
            }
            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
            if (req.Data.Header.EncounterTypeId && req.Data.Header.PharmacySaleTypeId) {
                if (req.Data.Header.EncounterTypeId === 2
                    && req.Data.Header.PharmacySaleTypeId === 6) {
                    req.Data.Header.ToStoreId = req.Data.Header.StoreMasterId;
                    let detailnew: any = [];
                    let perscreq: any = {
                        Data: {
                            Header: req.Data.Header,
                            Details: detailnew
                        }
                    };
                    let details = req.Data.Details;
                    for (let i = 0, len = details.length; i < len; i++) {
                        if (details[i].SubCategoryId) {
                            if (details[i].SubCategoryId === 1) {
                                details[i].RequestedQuantity = details[i].Quantity;
                                details[i].DrugId = details[i].ItemMasterId;
                                perscreq.Data.Details.push(details[i]);
                            }
                        }
                    }
                    if (perscreq.Data.Details.length > 0) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.CreatePrescription(perscreq);
                        for (let pre = 0, len = req.Data.Details.length; pre < len; pre++) {
                            req.Data.Details[pre].Id = 0;
                        }
                    }
                }
            }
        }
        let BillPaymentTypeId = -1;
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptNumber = null; /* await Sequence.Next(SequenceKeys.ReceiptNrId); */
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
                req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

                if (BillPaymentTypeId < 0)
                    BillPaymentTypeId = req.Data.paymentDetail[i].PaymentTypeId;
            }
        }
        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            await detailBO.ManageIPatientBillDetails(PatientBillId, req.Data.Details);
            let encId = req.Data.Header.EncounterId;
            let IsPatientTransfer = false;
            await billSummBo.ManagePatBillSummary(IsPatientTransfer, encId, req.Data.Details);
            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);

                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.BillTypeId === 4) {
                    if (req.Data.adjustmentDetail.length > 0) {
                        IsAdvanceAdjusting = 1;
                        AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

                        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                            if (req.Data.paymentDetail[i].Id > 0) {
                                for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                    req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                }
                            }
                        }

                        let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                        await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                    } else {
                        PaidAgainstBill = Number(req.Data.Header.PaidAmount);
                        DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                    }
                    let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                    let patientaccount = {};
                    patientaccount = {
                        Id: 0,
                        PatientId: req.Data.Header.PatientId,
                        EncounterId: req.Data.Header.EncounterId,
                        VisitNumber: req.Data.Header.VisitNumber,
                        TransactionTypeId: 2,
                        TransactionId: PatientBillId,
                        TransactionNumber: null,
                        TransactionDate: new Date(),
                        BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
                        PaidAmount: PaidAgainstBill,
                        IsAdvanceAdjusted: IsAdvanceAdjusting,
                        AdjustedAmount: AdjustedAgainstBill,
                        UnAdjustedAmount: 0,
                        DueAmount: DueTowardsBill,
                        DebitAmount: 0,
                        CreditAmount: 0
                    };
                    await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                }

                if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {
                    if (req.Data.Details.length > 0) {
                        for (let k = 0, len = req.Data.Details.length; k < len; k++) {
                            if (req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
                                req.Data.Details.splice(k, 1);
                            }
                        }
                    }

                    let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
                    try {
                        await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);
                    } catch (ex) {
                        let errorMessages: any = [];
                        _.forEach(ex.message, (item: any) => { errorMessages.push(item.ItemMasterId + ':' + item.name); });
                        throw { message: 'Stock Changes Happened for ' + errorMessages.join('$,$') };
                    }

                    // let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
                    // await stockmovementBO.ManageStockMovements1(21, PatientBillId, req.Data);

                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                }
            }

            if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
            let facilityId = (req.Data.Header.FacilityId) ? req.Data.Header.FacilityId : -1;
            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId,
                        req.Data.Header.FacilityId);
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId,
                        req.Data.Header.FacilityId);
                }


                let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let billingPreferencesData =
                    await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                let iPharmacyPaymodeSeqNr = 0;
                let iPharmacyCreditSeq = 0;
                try {
                    iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                    iPharmacyCreditSeq = parseInt(billingPreferencesData.ippharmcreditseq);
                } catch (ex) {
                    iPharmacyPaymodeSeqNr = 0;
                    iPharmacyCreditSeq = 0;
                }
                if (iPharmacyPaymodeSeqNr) {
                    if (BillPaymentTypeId > 0) {
                        if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                            // billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                            //     req.Data.Header.FacilityId);
                            if (iPharmacyCreditSeq) {
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditIPBill, facilityId);
                            } else {
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                    req.Data.Header.FacilityId);
                            }
                        } else {
                            if (req.Data.Header.PharmacySaleTypeId === 4) { //Manual-Sales - MAN10001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyManualBill,
                                    req.Data.Header.FacilityId);
                            } else { //Cash-Sales - CAS10001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCashBill,
                                    req.Data.Header.FacilityId);
                            }
                        }
                    } else { //Credit-Sales- (outstanding) - CRT10001
                        if (iPharmacyCreditSeq) {
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditIPBill, facilityId);
                        } else {
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                req.Data.Header.FacilityId);
                        }
                    }
                } else {
                    if (req.Data.Header.OutStandingAmount > 0) {
                        if (iPharmacyCreditSeq) {
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditIPBill, facilityId);
                        } else {
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                req.Data.Header.FacilityId);
                        }
                    }
                }

            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateMovementInfo: async (code: string) => {
                                try {
                                    request.Data.Header.TransactionId = patientBillId;
                                    request.Data.Header.TransactionNumber = code;
                                    await bo.UpdateMovementInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdateMovementInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdatePatientAccountsInfo(request);
                                } catch (error) {
                                    console.error('Error in updatePatientAccountsInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            SendBillDiscountSMS: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.BillNum = code;
                                    await bo.SendBillDiscountSMS(request);
                                } catch (error) {
                                    console.error('Error in SendBillDiscountSMS:', error);
                                    throw error;  // Handle the error as needed
                                }
                            }
                        };
                    })(this, req, PatientBillId);
                };

                // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                //     [
                //         afterO().UpdateMovementInfo,
                //         afterO().updatePatientAccountsInfo,
                //         afterO().SendBillDiscountSMS,
                //     ]);
                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().UpdateMovementInfo,
                                afterO().updatePatientAccountsInfo,
                                afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                handleDeferredExecution();


            }

            /*
            if (req.Data.ClaimDetails.length > 0) {
                await this.AddPatientPharmacyClaimBills(req);
            }
            */


            return PatientBillId;
        }

        return 0;
    }

    public async UpdatePatientPharmacyBills(req: BaseRequest): Promise<boolean> {
        if (req.Data.Header.Id > 0 && !req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 4) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
        }
        let BillPaymentTypeId = -1;
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptTypeId = req.Data.paymentDetail[i].ReceiptTypeId;
                req.Data.paymentDetail[i].ReceiptNumber = null; /* await Sequence.Next(SequenceKeys.ReceiptNrId); */
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;
                if (BillPaymentTypeId < 0)
                    BillPaymentTypeId = req.Data.paymentDetail[i].PaymentTypeId;
            }
        }
        if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 2) {
            if (req.Data.Header.CancelReason)
                var CancelReason: string = req.Data.Header.CancelReason;
            req.Data.Header = await this.GetPatientBillsById({ Id: req.Data.Header.Id });
            req.Data.Header.PatientBillStatusId = 2;
            if (CancelReason)
                req.Data.Header.CancelReason = CancelReason;
        }
        let result = await this.Update(req.Data.Header);
        let PatientBillId = req.Data.Header.Id;
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                try {
                    if (req.Data.paymentDetail && req.Data.paymentDetail.length > 0)
                        await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId,
                            req.Data.Header,
                            req.Data.paymentDetail);

                    // if(paymentresult && Number(paymentresult[0].Id) > 0) {
                    //     console.log('paymentresult');
                    //     console.log(paymentresult[0].Id);
                    // } else {
                    //     throw { message: 'Error in Generating Receipt' };
                    // }
                } catch (error) {
                    console.error('Final error handling:', error.message);
                    throw { message: error.message };
                }

                if (req.Data.Header.BillTypeId === 4) {
                    let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                    let patientaccount = {};
                    let IsAdvanceAdjusting = 0;
                    let PaidAgainstBill = 0;
                    let AdjustedAgainstBill = 0;
                    let DueTowardsBill = 0;
                    if (req.Data.Header.TotalDueAmount > 0) {
                        for (let j = 0, reclen = req.Data.paymentDetail.length; j < reclen; j++) {
                            if (req.Data.adjustmentDetail.length > 0) {
                                IsAdvanceAdjusting = 1;
                                AdjustedAgainstBill = Number(req.Data.Header.Received);

                                for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                    if (req.Data.paymentDetail[i].Id > 0) {
                                        for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                            req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                        }
                                    }
                                }

                                let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                                await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                            } else {
                                PaidAgainstBill = Number(req.Data.Header.Received);
                                DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                            }
                            patientaccount = {
                                Id: 0,
                                PatientId: req.Data.Header.PatientId,
                                EncounterId: req.Data.Header.EncounterId,
                                VisitNumber: req.Data.Header.VisitNumber,
                                TransactionTypeId: 2,
                                TransactionId: PatientBillId,
                                TransactionNumber: null,
                                TransactionDate: new Date(),
                                BillAmount: 0,
                                PaidAmount: PaidAgainstBill,
                                IsAdvanceAdjusted: IsAdvanceAdjusting,
                                AdjustedAmount: AdjustedAgainstBill,
                                UnAdjustedAmount: 0,
                                DueAmount: DueTowardsBill,
                                DebitAmount: 0,
                                CreditAmount: 0,
                                IsDueCollection: 1
                            };
                            await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                        }
                    } else {
                        for (let jj = 0, reclen1 = req.Data.paymentDetail.length; jj < reclen1; jj++) {
                            if (req.Data.adjustmentDetail.length > 0) {
                                IsAdvanceAdjusting = 1;
                                AdjustedAgainstBill = Number(req.Data.Header.Received);

                                for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                    if (req.Data.paymentDetail[i].Id > 0) {
                                        for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                            req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                        }
                                    }
                                }

                                let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                                await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                            } else {
                                PaidAgainstBill = Number(req.Data.Header.Received);
                                DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                            }
                            patientaccount = {
                                Id: 0,
                                PatientId: req.Data.Header.PatientId,
                                EncounterId: req.Data.Header.EncounterId,
                                VisitNumber: req.Data.Header.VisitNumber,
                                TransactionTypeId: 2,
                                TransactionId: PatientBillId,
                                TransactionNumber: null,
                                TransactionDate: new Date(),
                                BillAmount: 0,
                                PaidAmount: PaidAgainstBill,
                                AdjustedAmount: AdjustedAgainstBill,
                                UnAdjustedAmount: 0,
                                DueAmount: DueTowardsBill,
                                DebitAmount: 0,
                                CreditAmount: 0
                            };
                            await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                        }
                    }
                }

                if (req.Data.Header.IsPharmacyBill === 1 &&
                    (req.Data.Header.PharmacyBillStatusId === 1 || req.Data.Header.PharmacyBillStatusId === 0)) {
                    if (req.Data.Details.length > 0) {
                        for (let k = 0, len = req.Data.Details.length; k < len; k++) {
                            if (req.Data.Details[k].Id > 0 && req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
                                req.Data.Details.splice(k, 1);
                            }
                        }
                    }

                    let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
                    try {
                        await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);
                    } catch (ex) {
                        let errorMessages: any = [];
                        _.forEach(ex.message, (item: any) => { errorMessages.push(item.ItemMasterId + ':' + item.name); });
                        throw { message: 'Stock Changes Happened for ' + errorMessages.join('$,$') };
                    }

                    // let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
                    // await stockmovementBO.ManageStockMovements1(21, PatientBillId, req.Data);

                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                }
            }

            let billIdentifier: any = null;
            if (req.Data.Header.IsStoreSeparateSequence && req.Data.Header.IsStoreSeparateSequence === true) {
                if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                    billIdentifier = this.getSepStoreSequenceIdentifier(SequenceKeys.PharmacyBillNumberId,
                        req.Data.Header.StoreMasterId
                    );

                }
            } else {
                if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                    if (req.Data.Header.BillTypeId === 3) {
                        billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId,
                            req.Data.Header.FacilityId);
                    } else if (req.Data.Header.BillTypeId === 4) {
                        // billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyBillNumberId);
                        if (req.Data.Header.EncounterTypeId === 2) {
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyIPBillNumberId,
                                req.Data.Header.FacilityId);
                        } else {
                            billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId,
                                req.Data.Header.FacilityId);
                        }
                    }


                    let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                    let billingPreferencesData =
                        await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                    let iPharmacyPaymodeSeqNr = 0;
                    try {
                        iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                    } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
                    if (iPharmacyPaymodeSeqNr) {
                        if (BillPaymentTypeId > 0) {
                            if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                    req.Data.Header.FacilityId);
                            } else {
                                if (req.Data.Header.PharmacySaleTypeId === 4) { //Direct(Manual)-Sales - MAN0001
                                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyManualBill,
                                        req.Data.Header.FacilityId);
                                } else { //Cash-Sales - CAS0001
                                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCashBill,
                                        req.Data.Header.FacilityId);
                                }
                            }
                        } else {
                            if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                                    req.Data.Header.FacilityId);
                            }
                        }
                    }
                }
            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateMovementInfo: async (code: string) => {
                                try {
                                    request.Data.Header.TransactionId = patientBillId;
                                    request.Data.Header.TransactionNumber = code;
                                    await bo.UpdateMovementInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdateMovementInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = patientBillId;
                                    request.Data.Header.BillNumber = code;
                                    await bo.UpdatePatientAccountsInfo(request);
                                } catch (error) {
                                    console.error('Error in updatePatientAccountsInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            },
                        };
                    })(this, req, PatientBillId);
                };

                // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                //     [
                //         afterO().UpdateMovementInfo,
                //         afterO().updatePatientAccountsInfo,
                //     ]);
                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().UpdateMovementInfo,
                                afterO().updatePatientAccountsInfo,
                                // afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                handleDeferredExecution();


            }

            return PatientBillId;
        }
        return result;
    }

    public async AddPatientPharmacyClaimBills(req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 4) {
                req.Data.Header.BillAmount = req.Data.Header.ClaimBillAmount;
                req.Data.Header.BillDiscount = 0;
                req.Data.Header.BillDiscountTypeId = -1;
                req.Data.Header.DiscountApprovedBy = 0;
                req.Data.Header.DiscountModeValue = 0;
                req.Data.Header.PaidAmount = req.Data.Header.ClaimPaidAmount;
                req.Data.Header.IsPaidFully = 0;
                req.Data.Header.OutStandingAmount = req.Data.Header.ClaimOutStandingAmount;
                req.Data.Header.GSTAmount = req.Data.Header.ClaimGstAmount;
                req.Data.Header.InGstAmount = req.Data.Header.ClaimInGstAmount;
                req.Data.Header.CGstAmount = req.Data.Header.ClaimCGstAmount;
                req.Data.Header.SGstAmount = req.Data.Header.ClaimSGstAmount;
                req.Data.Header.CreditVocher = req.Data.Header.ClaimCreditVocher;
                req.Data.Header.PrivateDueId = -1;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.ClaimDetails.length; i < len; i++) {
                req.Data.ClaimDetails[i].BillDateTime = req.Data.Header.BillDateTime;
            }
            if (req.Data.Header.EncounterTypeId && req.Data.Header.PharmacySaleTypeId) {
                if (req.Data.Header.EncounterTypeId === 2
                    && req.Data.Header.PharmacySaleTypeId === 6) {
                    req.Data.Header.ToStoreId = req.Data.Header.StoreMasterId;
                    let detailnew: any = [];
                    let perscreq: any = {
                        Data: {
                            Header: req.Data.Header,
                            ClaimDetails: detailnew
                        }
                    };
                    let claimdetails = req.Data.ClaimDetails;
                    for (let i = 0, len = claimdetails.length; i < len; i++) {
                        if (claimdetails[i].SubCategoryId) {
                            if (claimdetails[i].SubCategoryId === 1) {
                                claimdetails[i].RequestedQuantity = claimdetails[i].Quantity;
                                claimdetails[i].DrugId = claimdetails[i].ItemMasterId;
                                perscreq.Data.ClaimDetails.push(claimdetails[i]);
                            }
                        }
                    }
                    if (perscreq.Data.ClaimDetails.length > 0) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.CreatePrescription(perscreq);
                        for (let pre = 0, len = req.Data.ClaimDetails.length; pre < len; pre++) {
                            req.Data.ClaimDetails[pre].Id = 0;
                        }
                    }
                }
            }
        }
        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.ClaimDetails);
            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {
                    if (req.Data.ClaimDetails.length > 0) {
                        for (let k = 0, len = req.Data.ClaimDetails.length; k < len; k++) {
                            if (req.Data.ClaimDetails[k].ItemMasterId > 0 && req.Data.ClaimDetails[k].Status === 2) {
                                req.Data.ClaimDetails.splice(k, 1);
                            }
                        }
                    }

                    /*
                    let stockitemBO = BoFactory.GetBo(inventoryBO.StockItemBo, this.Request);
                    let stockResult = await stockitemBO.ManageStockItems(21, PatientBillId, req.Data);

                    if (stockResult && stockResult.length > 0) {
                        let errorMessages: any = [];
                        _.forEach(stockResult, (item: any) => { errorMessages.push(item.error.name); });
                        throw { message: 'Stock Changes Happened for ' + errorMessages.join(',') };
                    }

                    let stockmovementBO = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
                    await stockmovementBO.ManageStockMovements(21, PatientBillId, req.Data);
                    */

                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                }
            }

            if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;

            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.IPBillNumberId);
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyBillClaimNumberId);
                }

                let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let billingPreferencesData =
                    await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                let iPharmacyPaymodeSeqNr = 0;
                try {
                    iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
                if (iPharmacyPaymodeSeqNr) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCreditBill);
                }
            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateMovementInfo: async (code: string) => {
                                try {
                                    request.Data.Header.TransactionId = patientBillId;
                                    req.Data.Header.TransactionNumber = code;
                                    await bo.UpdateMovementInfo(request);
                                } catch (error) {
                                    console.error('Error in UpdateMovementInfo:', error);
                                    throw error;  // Handle the error as needed
                                }
                            }
                        };
                    })(this, req, PatientBillId);
                };

                // this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier, [afterO().UpdateMovementInfo]);
                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().UpdateMovementInfo,
                                // afterO().updatePatientAccountsInfo,
                                // afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                handleDeferredExecution();

            }

            return PatientBillId;
        }

        return 0;
    }

    public async AddPatientDispenseBill(DispenseId: number, req: BaseRequest): Promise<boolean> {
        let BillInfo = req.Data.Header;
        let PatientBill: any = {};

        PatientBill = {
            Id: 0,
            // BillNumber: await Sequence.Next(SequenceKeys.PharmacyCreditBill),
            BillDateTime: new Date(),
            BillTypeId: 3,
            BillPriorityId: 1,
            BillAmount: BillInfo.DispensedValue,
            BillDiscount: 0,
            BillDiscountTypeId: 0,
            DiscountApprovedBy: 0,
            BillDiscountModeId: 0,
            RoundOffValue: 0,
            BilledCounter: 0,
            PaidAmount: 0,
            ServiceTax: 0,
            EducationCess: 0,
            GSTAmount: BillInfo.TotalGstAmount,
            InGstAmount: BillInfo.TotalInGstAmount,
            CGstAmount: BillInfo.TotalCGstAmount,
            SGstAmount: BillInfo.TotalSGstAmount,
            NetPatientAmount: BillInfo.NetPatientAmount,
            NetInsuranceAmount: BillInfo.NetInsuranceAmount,
            BillGeneratedBy: BillInfo.DispensedBy,
            BillApprovedBy: BillInfo.DispensedBy,
            PackageDiscount: 0,
            OrganizationId: 1,
            FacilityId: BillInfo.FacilityId,
            DepartmentId: BillInfo.DepartmentId,
            StoreMasterId: BillInfo.StoreMasterId,
            PatientId: BillInfo.PatientId,
            PatientTypeId: BillInfo.PatientTypeId,
            OTIdentifier: BillInfo.OTIdentifier,
            PatientName: BillInfo.PatientName,
            TitleId: BillInfo.TitleId,
            GenderId: BillInfo.GenderId,
            Age: BillInfo.Age,
            EncounterId: BillInfo.EncounterId,
            EncounterTypeId: BillInfo.EncounterTypeId,
            OTRegisterId: BillInfo.OTRegisterId,
            ProcedureId: BillInfo.ProcedureId,
            GuarantorId: BillInfo.GuarantorId,
            GuarantorTypeId: BillInfo.GuarantorTypeId,
            GuarantorName: BillInfo.GuarantorName,
            PrivateDueId: 0,
            GuarantorDueId: 0,
            DoctorId: BillInfo.DoctorId,
            DoctorName: BillInfo.DoctorName,
            RoomId: BillInfo.RoomId,
            BedId: BillInfo.BedId,
            WardId: BillInfo.WardId,
            OTRoomId: BillInfo.OTRoomId,
            PatientBillStatusId: 3,
            IsPharmacyBill: 1,
            PharmacySaleTypeId: 2,
            // ParentBillId: DispenseId
        };
        if (!PatientBill.BillGeneratedBy) {
            PatientBill.BillGeneratedBy = this.Session.UserId;
        }
        if (!PatientBill.FacilityId) PatientBill.FacilityId = this.Session.FacilityId;
        let result = await this.Save(PatientBill);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            for (let pbd = 0, pbdlen = req.Data.Details.length; pbd < pbdlen; pbd++) {
                req.Data.Details[pbd].Id = 0;
                req.Data.Details[pbd].BillDateTime = new Date();
            }
            await detailBO.ManagePatientDispenseBillDetails(PatientBillId, req.Data.Details);
            let IsPatientTransfer = false;
            await billSummBo.ManagePatBillSummary(IsPatientTransfer, BillInfo.EncounterId, req.Data.Details);

            let billIdentifier: any = null;
            if (!PatientBill.BillNumber && PatientBill.PatientBillStatusId === 3) {
                let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let billingPreferencesData =
                    await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);

                let iPharmacyCreditSeq = 0;
                try {
                    iPharmacyCreditSeq = parseInt(billingPreferencesData.ippharmcreditseq);
                } catch (ex) { iPharmacyCreditSeq = 0; }
                if (iPharmacyCreditSeq) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditIPBill, req.Data.Header.FacilityId);
                } else {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyCreditBill,
                        req.Data.Header.FacilityId);
                }

            }

            if (billIdentifier) {

                this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier);
            }
        }
        return true;
    }

    public async AddPatientDispenseReturnBill(DispenseReturnId: number, patientReturnId: number, req: BaseRequest): Promise<boolean> {
        let BillInfo = req.Data.Header;
        let PatientBill: any = {};


        PatientBill = {
            Id: 0,
            BillNumber: await Sequence.Next(SequenceKeys.IPBillNumberId),
            BillDateTime: new Date(),
            BillTypeId: 3,
            BillPriorityId: 1,
            BillAmount: BillInfo.ReceivedValue,
            BillDiscount: 0,
            BillDiscountTypeId: 0,
            DiscountApprovedBy: 0,
            BillDiscountModeId: 0,
            RoundOffValue: 0,
            BilledCounter: 0,
            PaidAmount: 0,
            ServiceTax: 0,
            EducationCess: 0,
            GSTAmount: BillInfo.TotalGstAmount,
            InGstAmount: BillInfo.TotalInGstAmount,
            CGstAmount: BillInfo.TotalCGstAmount,
            SGstAmount: BillInfo.TotalSGstAmount,
            NetPatientAmount: BillInfo.NetPatientAmount,
            NetInsuranceAmount: BillInfo.NetInsuranceAmount,
            BillGeneratedBy: BillInfo.ReturnReceivedBy,
            BillApprovedBy: BillInfo.ReturnReceivedBy,
            PackageDiscount: 0,
            OrganizationId: 0,
            FacilityId: BillInfo.FacilityId,
            DepartmentId: BillInfo.DepartmentId,
            StoreMasterId: BillInfo.StoreMasterId,
            PatientId: BillInfo.PatientId,
            PatientTypeId: BillInfo.PatientTypeId,
            PatientName: BillInfo.PatientName,
            TitleId: BillInfo.TitleId,
            GenderId: BillInfo.GenderId,
            Age: BillInfo.Age,
            EncounterId: BillInfo.EncounterId,
            EncounterTypeId: 2,
            GuarantorId: BillInfo.GuarantorId,
            GuarantorTypeId: BillInfo.GuarantorTypeId,
            GuarantorName: BillInfo.GuarantorName,
            PrivateDueId: 0,
            GuarantorDueId: 0,
            DoctorId: BillInfo.DoctorId,
            DoctorName: BillInfo.DoctorName,
            RoomId: BillInfo.RoomId,
            BedId: BillInfo.BedId,
            WardId: BillInfo.WardId,
            PatientBillStatusId: 3,
            IsPharmacyReturn: 1,
            PharmacyReturnTypeId: 2,
            // ParentBillId: DispenseReturnId,
            ParentReturnId: patientReturnId
        };
        if (!PatientBill.BillGeneratedBy) {
            PatientBill.BillGeneratedBy = this.Session.UserId;
        }
        if (!PatientBill.FacilityId) PatientBill.FacilityId = this.Session.FacilityId;
        let result = await this.Save(PatientBill);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            for (let pbd = 0, pbdlen = req.Data.Details.length; pbd < pbdlen; pbd++) {
                req.Data.Details[pbd].Id = 0;
                req.Data.Details[pbd].BillDateTime = new Date();
                req.Data.Details[pbd].Rate = -1 * req.Data.Details[pbd].Rate;
                req.Data.Details[pbd].Amount = -1 * req.Data.Details[pbd].Amount;
                req.Data.Details[pbd].GrossAmount = -1 * req.Data.Details[pbd].GrossAmount;
                req.Data.Details[pbd].NetAmountBeforeGst = -1 * req.Data.Details[pbd].NetAmountBeforeGst;
                req.Data.Details[pbd].NetAmount = -1 * req.Data.Details[pbd].NetAmount;
                req.Data.Details[pbd].InsNetAmount = -1 * req.Data.Details[pbd].InsNetAmount;
                req.Data.Details[pbd].PatNetAmount = -1 * req.Data.Details[pbd].PatNetAmount;
            }
            await detailBO.ManageIPatientBillDetails(PatientBillId, req.Data.Details);
            let IsPatientTransfer = false;
            IsPatientTransfer = req.Data.Header.IsPatientTransfer;
            await billSummBo.ManagePatBillSummary(IsPatientTransfer, BillInfo.EncounterId, req.Data.Details);
        }
        return true;
    }

    public async ManagePatientPharmacyReturns(PatientReturnId: number, req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientReturnStatusId === 3) {
            if (req.Data.Header.ReturnTypeId === 6) {

                req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId,
                    req.Data.Header.FacilityId
                );
                req.Data.Header.BillDateTime = new Date();
                req.Data.Header.ParentReturnId = PatientReturnId;
                req.Data.Header.BillAmount = -1 * req.Data.Header.BillAmount;
                req.Data.Header.NetPatientAmount = -1 * req.Data.Header.NetPatientAmount;
                req.Data.Header.NetInsuranceAmount = -1 * req.Data.Header.NetInsuranceAmount;
            }
        }
        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            let PatientReturnDetails: any = [];
            req.Data.Details.forEach((Detail: any) => {
                let PatientReturnDetail = Detail;
                PatientReturnDetail.Rate = -1 * PatientReturnDetail.Rate;
                PatientReturnDetail.Amount = -1 * PatientReturnDetail.Amount;
                PatientReturnDetail.GrossAmount = -1 * PatientReturnDetail.GrossAmount;
                PatientReturnDetail.NetAmountBeforeGst = -1 * PatientReturnDetail.NetAmountBeforeGst;
                PatientReturnDetail.NetAmount = -1 * PatientReturnDetail.NetAmount;
                PatientReturnDetail.PatNetAmount = -1 * PatientReturnDetail.PatNetAmount;
                PatientReturnDetail.InsNetAmount = -1 * PatientReturnDetail.InsNetAmount;
                PatientReturnDetails.push(PatientReturnDetail);
            });
            await detailBO.ManagePatientBillDetails(PatientBillId, PatientReturnDetails);
            await billSummBo.ManageReturnBillSummary(req.Data.Header.EncounterId, PatientReturnDetails);
            return PatientBillId;
        }

        return 0;
    }

    public async GetFindPharmacyBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let include: Array<IncludeOptions> = [];
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let storemasterId: -1;
        let order: Array<any> = [];
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, attributes: ['GstNumber'], required: false,
        });
        include.push({
            model: this.Models.StoreMaster, attributes: ['LicenseNo', 'TinNo'], required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender')
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['GuarantorName'], required: false,
        // });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'SignPath', 'UserName'], as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        include.push({
            model: this.Models.Encounter,
            attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate'],
            required: false,
            include: [
                this.GetReference('AdmissionStatus')
            ],
        });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }];
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatNumBil:
                        (where as any)['$or'] = [{ 'BillNumber': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'Mobile': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsDirectDGBill:
                        where['IsDirectDGBill'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.PatientGuarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        order.push(['PatientBillId', 'DESC']);
        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }

    public async GetFindDirectPharmacyBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let include: Array<IncludeOptions> = [];
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let storemasterId: -1;
        let order: Array<any> = [];
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, attributes: ['GstNumber'], required: false,
        });
        include.push({
            model: this.Models.StoreMaster, attributes: ['LicenseNo', 'TinNo'], required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        include.push({
            model: this.Models.Patient, attributes: ['FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender')
            ],
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['GuarantorName'], required: false,
        // });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'SignPath', 'UserName'], as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        // include.push({
        //     model: this.Models.Encounter,
        //     attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate'],
        //     required: false,
        //     include: [
        //         this.GetReference('AdmissionStatus')
        //     ],
        // });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }];
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    // case PatientBillsFilters.MRN:
                    //     patientQryJoin['where'] = {
                    //         '$or': [
                    //             { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                    //             { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                    //             { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                    //         ]
                    //     };
                    //     patientQryJoin['required'] = true;
                    //     break;
                    // case PatientBillsFilters.Mobile:
                    //     patientQryJoin['where'] = { 'Mobile': param.Value };
                    //     patientQryJoin['required'] = true;
                    //     break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatNumBil:
                        (where as any)['$or'] = [{ 'BillNumber': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'Mobile': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsDirectDGBill:
                        where['IsDirectDGBill'] = param.Value;
                        break;
                    case PatientBillsFilters.Mobile:
                        where['Mobile'] = { '$like': '%' + param.Value + '%' };
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientReturns,
            required: false,
            attributes: ['Id', 'PatientBillId', 'ReturnAmount', 'GrossAmount', 'RefundedAmount', 'RoundOffValue'],
            // include: [
            //     this.GetReference('PaymentType')]
        });
        // include.push(patientQryJoin);
        // include.push({
        //     model: this.Models.PatientGuarantor,
        //     as: 'Guarantor',
        //     attributes: ['GuarantorName', 'GuarantorId'],
        //     required: isGuarantorRequired,
        //     where: guarantorWhere
        // });
        order.push(['BillDateTime', 'DESC']);
        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }


    public async GetPatientPharmacyBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let include: Array<IncludeOptions> = [];
        let detailswhere: WhereOptions<any> = {};
        let isPatientDetailsRequired: any = false;
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let storemasterId: -1;
        let order: Array<any> = [];
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, attributes: ['GstNumber'], required: false,
        });
        include.push({
            model: this.Models.StoreMaster, attributes: ['LicenseNo', 'TinNo'], required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender')
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['GuarantorName'], required: false,
        // });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'SignPath', 'UserName'], as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        include.push({
            model: this.Models.Encounter, attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false,
            include: [
                this.GetReference('AdmissionStatus')
            ]
        });

        include.push({ model: this.Models.PatientPaymentDetails, required: false });
        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }];
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        detailswhere['DoctorShare'] = { '$gt': param.Value };
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientBillDetails,
            required: isPatientDetailsRequired,
            where: detailswhere,
            include: [{
                model: this.Models.ItemMaster,
                required: false,
                include: [
                    {
                        model: this.Models.StockItem,
                        required: false,
                        attributes: ['Id', 'StoreMasterId', 'ItemMasterId', 'ItemCode', 'ItemName', 'Quantity'],
                        where: { 'StoreMasterId': storemasterId },
                        include: [
                            {
                                model: this.Models.StockSerialItem,
                                required: false,
                                attributes: ['Id', 'StockItemId', 'StoreMasterId', 'ItemMasterId', 'BatchId',
                                    'Quantity', 'ExpiryDate', 'Ucp', 'Mrp', 'GstId', 'GstPercentage', 'InGstId',
                                    'InGstPercentage', 'CGstId', 'CGstPercentage', 'SGstId', 'SGstPercentage'],
                                where: { 'Quantity': { $gt: 0 } }
                            }
                        ]
                    }
                ]
            }]
        });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.PatientGuarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        order.push(['BillDateTime', 'DESC']);
        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }

    public async GetFindPatientPharmacyBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let include: Array<IncludeOptions> = [];
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let storemasterId: -1;
        let order: Array<any> = [];
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, attributes: ['GstNumber'], required: false,
        });
        include.push({
            model: this.Models.StoreMaster, attributes: ['LicenseNo', 'TinNo', 'StoreName'], required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender')
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['GuarantorName'], required: false,
        // });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'SignPath', 'UserName'], as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        include.push({
            model: this.Models.Encounter, attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false,
            include: [
                this.GetReference('AdmissionStatus')
            ]
        });

        include.push({ model: this.Models.PatientPaymentDetails, required: false });
        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }];
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.PatientGuarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        order.push(['BillDateTime', 'DESC']);
        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }

    public async AddPatientOpticalBills(req: BaseRequest): Promise<number> {
        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 4) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
            if (req.Data.Header.EncounterTypeId && req.Data.Header.PharmacySaleTypeId) {
                if (req.Data.Header.EncounterTypeId === 2
                    && req.Data.Header.PharmacySaleTypeId === 6) {
                    req.Data.Header.ToStoreId = req.Data.Header.StoreMasterId;
                    let detailnew: any = [];
                    let perscreq: any = {
                        Data: {
                            Header: req.Data.Header,
                            Details: detailnew
                        }
                    };
                    let details = req.Data.Details;
                    for (let i = 0, len = details.length; i < len; i++) {
                        if (details[i].SubCategoryId) {
                            if (details[i].SubCategoryId === 1) {
                                details[i].RequestedQuantity = details[i].Quantity;
                                details[i].DrugId = details[i].ItemMasterId;
                                perscreq.Data.Details.push(details[i]);
                            }
                        }
                    }
                    if (perscreq.Data.Details.length > 0) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.CreatePrescription(perscreq);
                        for (let pre = 0, len = req.Data.Details.length; pre < len; pre++) {
                            req.Data.Details[pre].Id = 0;
                        }
                    }
                }
            }
        }
        let BillPaymentTypeId = -1;
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptNumber = null; /* await Sequence.Next(SequenceKeys.ReceiptNrId); */
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].OutStandingAmount = req.Data.Header.NetAmount;
                req.Data.paymentDetail[i].DueAmount = req.Data.Header.OutStandingAmount;
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;

                if (BillPaymentTypeId < 0)
                    BillPaymentTypeId = req.Data.paymentDetail[i].PaymentTypeId;
            }
        }
        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let PatientBillId = result.dataValues.Id;
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);

                let IsAdvanceAdjusting = 0;
                let PaidAgainstBill = 0;
                let AdjustedAgainstBill = 0;
                let DueTowardsBill = 0;
                if (req.Data.Header.BillTypeId === 4) {
                    if (req.Data.adjustmentDetail.length > 0) {
                        IsAdvanceAdjusting = 1;
                        AdjustedAgainstBill = Number(req.Data.Header.PaidAmount);

                        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                            if (req.Data.paymentDetail[i].Id > 0) {
                                for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                    req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                }
                            }
                        }

                        let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                        await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                    } else {
                        PaidAgainstBill = Number(req.Data.Header.PaidAmount);
                        DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                    }
                    let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                    let patientaccount = {};
                    patientaccount = {
                        Id: 0,
                        PatientId: req.Data.Header.PatientId,
                        EncounterId: req.Data.Header.EncounterId,
                        VisitNumber: req.Data.Header.VisitNumber,
                        TransactionTypeId: 2,
                        TransactionId: PatientBillId,
                        TransactionNumber: null,
                        TransactionDate: new Date(),
                        BillAmount: Number(req.Data.Header.BillAmount) - Number(req.Data.Header.BillDiscount),
                        PaidAmount: PaidAgainstBill,
                        IsAdvanceAdjusted: IsAdvanceAdjusting,
                        AdjustedAmount: AdjustedAgainstBill,
                        UnAdjustedAmount: 0,
                        DueAmount: DueTowardsBill,
                        DebitAmount: 0,
                        CreditAmount: 0
                    };
                    await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                }

                if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 0) {
                    if (req.Data.Details.length > 0) {
                        for (let k = 0, len = req.Data.Details.length; k < len; k++) {
                            if (req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
                                req.Data.Details.splice(k, 1);
                            }
                        }
                    }

                    let stockitemBO = BoFactory.GetBo(inventoryBO.OpticalStockItemBo, this.Request);
                    await stockitemBO.ManageOpticalStockItems(21, PatientBillId, req.Data);
                    //console.log(stockResult);
                    // if (stockResult && stockResult.length > 0) {
                    //     let errorMessages: any = [];
                    //     _.forEach(stockResult, (item: any) => { errorMessages.push(item.error.name); });
                    //     throw { message: 'Stock Changes Happened for ' + errorMessages.join(',') };
                    // }

                    let stockmovementBO = BoFactory.GetBo(inventoryBO.OpticalStockMovementBo, this.Request);
                    await stockmovementBO.ManageOpticalStockMovements(21, PatientBillId, req.Data);

                    /*
                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                    */
                }
            }

            //pharmacypaymodeseqnr


            if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;

            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.IPBillNumberId);
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyBillNumberId);
                }


                let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let billingPreferencesData =
                    await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                let iPharmacyPaymodeSeqNr = 0;
                try {
                    iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
                if (iPharmacyPaymodeSeqNr) {
                    if (BillPaymentTypeId > 0) {
                        if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                            billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCreditBill);
                        } else {
                            if (req.Data.Header.PharmacySaleTypeId === 4) { //Manual-Sales - MAN10001
                                billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyManualBill);
                            } else { //Cash-Sales - CAS10001
                                billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCashBill);
                            }
                        }
                    } else { //Credit-Sales- (outstanding) - CRT10001
                        billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCreditBill);
                    }
                }

            }


            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateOpticalMovementInfo: async (code: string) => {
                                request.Data.Header.TransactionId = patientBillId;
                                request.Data.Header.TransactionNumber = code;
                                await bo.UpdateOpticalMovementInfo(request);
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                request.Data.Header.PatientBillId = patientBillId;
                                request.Data.Header.BillNumber = code;
                                await bo.UpdatePatientAccountsInfo(request);
                            },
                            SendBillDiscountSMS: async (code: string) => {
                                request.Data.Header.PatientBillId = patientBillId;
                                request.Data.BillNum = code;
                                await bo.SendBillDiscountSMS(request);
                            }
                        };
                    })(this, req, PatientBillId);
                };

                this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                    [
                        afterO().UpdateOpticalMovementInfo,
                        afterO().updatePatientAccountsInfo,
                        afterO().SendBillDiscountSMS,
                    ]);

            }

            return PatientBillId;
        }

        return 0;
    }

    public async UpdatePatientOpticalBills(req: BaseRequest): Promise<boolean> {
        if (req.Data.Header.Id > 0 && !req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 4) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
        }
        let BillPaymentTypeId = -1;
        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
            if (req.Data.paymentDetail[i].Id <= 0) {
                req.Data.paymentDetail[i].ReceiptTypeId = 3;
                req.Data.paymentDetail[i].ReceiptNumber = null; /* await Sequence.Next(SequenceKeys.ReceiptNrId); */
                req.Data.paymentDetail[i].ReceiptDateTime = new Date();
                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;
                if (BillPaymentTypeId < 0)
                    BillPaymentTypeId = req.Data.paymentDetail[i].PaymentTypeId;
            }
        }
        if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 2) {
            if (req.Data.Header.CancelReason)
                var CancelReason: string = req.Data.Header.CancelReason;
            req.Data.Header = await this.GetPatientBillsById({ Id: req.Data.Header.Id });
            req.Data.Header.PatientBillStatusId = 2;
            if (CancelReason)
                req.Data.Header.CancelReason = CancelReason;
        }
        let result = await this.Update(req.Data.Header);
        let PatientBillId = req.Data.Header.Id;
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
            if (req.Data.Header.Id > 0 && req.Data.Header.PatientBillStatusId === 3) {
                let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);

                if (req.Data.Header.BillTypeId === 4) {
                    let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
                    let patientaccount = {};
                    let IsAdvanceAdjusting = 0;
                    let PaidAgainstBill = 0;
                    let AdjustedAgainstBill = 0;
                    let DueTowardsBill = 0;
                    if (req.Data.Header.TotalDueAmount > 0) {
                        for (let j = 0, reclen = req.Data.paymentDetail.length; j < reclen; j++) {
                            if (req.Data.adjustmentDetail.length > 0) {
                                IsAdvanceAdjusting = 1;
                                AdjustedAgainstBill = Number(req.Data.Header.Received);

                                for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                    if (req.Data.paymentDetail[i].Id > 0) {
                                        for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                            req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                        }
                                    }
                                }

                                let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                                await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                            } else {
                                PaidAgainstBill = Number(req.Data.Header.Received);
                                DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                            }
                            patientaccount = {
                                Id: 0,
                                PatientId: req.Data.Header.PatientId,
                                EncounterId: req.Data.Header.EncounterId,
                                VisitNumber: req.Data.Header.VisitNumber,
                                TransactionTypeId: 2,
                                TransactionId: PatientBillId,
                                TransactionNumber: null,
                                TransactionDate: new Date(),
                                BillAmount: 0,
                                PaidAmount: PaidAgainstBill,
                                IsAdvanceAdjusted: IsAdvanceAdjusting,
                                AdjustedAmount: AdjustedAgainstBill,
                                UnAdjustedAmount: 0,
                                DueAmount: DueTowardsBill,
                                DebitAmount: 0,
                                CreditAmount: 0,
                                IsDueCollection: 1
                            };
                            await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                        }
                    } else {
                        for (let jj = 0, reclen1 = req.Data.paymentDetail.length; jj < reclen1; jj++) {
                            if (req.Data.adjustmentDetail.length > 0) {
                                IsAdvanceAdjusting = 1;
                                AdjustedAgainstBill = Number(req.Data.Header.Received);

                                for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                                    if (req.Data.paymentDetail[i].Id > 0) {
                                        for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                                            req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                                        }
                                    }
                                }

                                let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                                await PaymentadjustmentBO.ManagePatientPaymentAdjustments(PatientBillId, req.Data.adjustmentDetail);
                            } else {
                                PaidAgainstBill = Number(req.Data.Header.Received);
                                DueTowardsBill = Number(req.Data.Header.OutStandingAmount);
                            }
                            patientaccount = {
                                Id: 0,
                                PatientId: req.Data.Header.PatientId,
                                EncounterId: req.Data.Header.EncounterId,
                                VisitNumber: req.Data.Header.VisitNumber,
                                TransactionTypeId: 2,
                                TransactionId: PatientBillId,
                                TransactionNumber: null,
                                TransactionDate: new Date(),
                                BillAmount: 0,
                                PaidAmount: PaidAgainstBill,
                                AdjustedAmount: AdjustedAgainstBill,
                                UnAdjustedAmount: 0,
                                DueAmount: DueTowardsBill,
                                DebitAmount: 0,
                                CreditAmount: 0
                            };
                            await patientaccountsBO.ManagePatientAccounts(1, patientaccount as any);
                        }
                    }
                }

                if (req.Data.Header.IsPharmacyBill === 1 && req.Data.Header.PharmacyBillStatusId === 1) {
                    if (req.Data.Details.length > 0) {
                        for (let k = 0, len = req.Data.Details.length; k < len; k++) {
                            if (req.Data.Details[k].Id > 0 && req.Data.Details[k].ItemMasterId > 0 && req.Data.Details[k].Status === 2) {
                                req.Data.Details.splice(k, 1);
                            }
                        }
                    }

                    let stockitemBO = BoFactory.GetBo(inventoryBO.OpticalStockItemBo, this.Request);
                    let stockResult = await stockitemBO.ManageOpticalStockItems(21, PatientBillId, req.Data);
                    //console.log(stockResult);
                    if (stockResult && stockResult.length > 0) {
                        let errorMessages: any = [];
                        _.forEach(stockResult, (item: any) => { errorMessages.push(item.error.ItemMasterId + ':' + item.error.name); });
                        throw { message: 'Stock Changes Happened for ' + errorMessages.join('$,$') };
                    }

                    let stockmovementBO = BoFactory.GetBo(inventoryBO.OpticalStockMovementBo, this.Request);
                    await stockmovementBO.ManageOpticalStockMovements(21, PatientBillId, req.Data);

                    /*
                    if (req.Data.Header.IsPrescription === 1) {
                        let prescriptionBO = BoFactory.GetBo(emrbo.PrescriptionBo, this.Request);
                        await prescriptionBO.ManagePrescription(PatientBillId, req.Data);
                    }
                    */
                }
            }

            let billIdentifier: any = null;
            if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
                if (req.Data.Header.BillTypeId === 3) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.IPBillNumberId);
                } else if (req.Data.Header.BillTypeId === 4) {
                    billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyBillNumberId);
                }


                let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let billingPreferencesData =
                    await facilityPreferenceBO.GetPrintPreferences('billing', null, req.Data.Header.FacilityId);
                let iPharmacyPaymodeSeqNr = 0;
                try {
                    iPharmacyPaymodeSeqNr = parseInt(billingPreferencesData.pharmacypaymodeseqnr);
                } catch (ex) { iPharmacyPaymodeSeqNr = 0; }
                if (iPharmacyPaymodeSeqNr) {
                    if (BillPaymentTypeId > 0) {
                        if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                            billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCreditBill);
                        } else {
                            if (req.Data.Header.PharmacySaleTypeId === 4) { //Direct(Manual)-Sales - MAN0001
                                billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyManualBill);
                            } else { //Cash-Sales - CAS0001
                                billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCashBill);
                            }
                        }
                    } else {
                        if (req.Data.Header.OutStandingAmount > 0) { //Credit-Sales- (outstanding) - CRT0001
                            billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyCreditBill);
                        }
                    }
                }
            }

            if (billIdentifier) {
                const afterO: any = () => {
                    return ((bo, request, patientBillId) => {
                        return {
                            UpdateOpticalMovementInfo: async (code: string) => {
                                request.Data.Header.TransactionId = patientBillId;
                                request.Data.Header.TransactionNumber = code;
                                await bo.UpdateOpticalMovementInfo(request);
                            },
                            updatePatientAccountsInfo: async (code: string) => {
                                request.Data.Header.PatientBillId = patientBillId;
                                request.Data.Header.BillNumber = code;
                                await bo.UpdatePatientAccountsInfo(request);
                            },
                        };
                    })(this, req, PatientBillId);
                };

                this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                    [
                        afterO().UpdateOpticalMovementInfo,
                        afterO().updatePatientAccountsInfo,
                    ]);

            }

            return PatientBillId;
        }
        return result;
    }

    public async UpdatePatientFromStaffBills(req: BaseRequest): Promise<boolean> {
        if (req.Data.Header.Id > 0 && !req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 3) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId); */
                req.Data.Header.CreditVocher = 0;
            } else if (req.Data.Header.BillTypeId === 4) {
                /* req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId); */
                req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
            }

            req.Data.Header.BillDateTime = new Date();
            for (let i = 0, len = req.Data.Details.length; i < len; i++) {
                req.Data.Details[i].BillDateTime = req.Data.Header.BillDateTime;
            }
        }
        let result = await this.Update(req.Data.Header);
        return result;
    }


    public async ManageVirtualOrderPatientBills(orderId: number, Details: any, patientOrder: any,
        isDirectBill: boolean): Promise<number> {
        let response: number = 0;
        let billingAmount: number = 0;
        let billDiscountAmount: number = 0;
        let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
        let PatientBillDetailBo = BoFactory.GetBo(billingBO.PatientBillDetailsBo, this.Request);
        // let PatientOrderDetailsApiReq = {
        //     Id: 0,
        //     PageContext: { PageSize: 1000, PageNumber: 1 },
        //     Params: [{ Key: PatientOrderDetailFilters.PatientOrderId, Value: patientOrder.Id },
        //     { Key: PatientOrderDetailFilters.Ids, Value: detailIds },
        //     ]
        // };
        // let PatientOrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(PatientOrderDetailsApiReq);
        if (Details.length > 0) {
            let PatientBill: any = {};

            PatientBill = {
                BillTypeId: 1,
                BillNumber: '',
                BillDateTime: patientOrder.OrderRequestDate,
                PatientId: patientOrder.PatientId,
                DoctorId: patientOrder.DoctorId,
                OTRegisterId: patientOrder.SurgeryEntryId,
                OTIdentifier: patientOrder.SurgeryIdentifier,
                PatientBillStatusId: 1,
                FacilityId: patientOrder.FacilityId,
                PatientOrderId: orderId,
                IsDirectBill: isDirectBill,
                IsPaidFully: false,
                IsVirtualOrders: true,
                BillAmount: patientOrder.GrossAmount,
                OutStandingAmount: patientOrder.OrderTotal,
                PaidAmount: 0,
                BillGeneratedBy: this.Session.UserId
            };

            // PatientOrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(PatientOrderDetailsApiReq);
            if (!PatientBill.BillGeneratedBy) {
                PatientBill.BillGeneratedBy = this.Session.UserId;
            }
            if (!PatientBill.FacilityId) PatientBill.FacilityId = this.Session.FacilityId;
            let result = await this.Save(PatientBill);
            // await Promise.all(Details.map((DetailItem: any): Promise<void> => {
            //     return (async (detail): Promise<void> => {
            // if (!detail.IsPackage) {
            // let PatientOrderDetail: any = detail;
            for (let bdx in Details) {
                let detail = Details[bdx];
                let PatientBillDetail: any = {};
                // let ServiceItem: any = PatientOrderDetail.ServiceItem;
                let AliasId = null;
                let AliasName = null;
                PatientBillDetail.ServiceId = detail.ServiceId;
                PatientBillDetail.ServiceName = detail.ServiceName;
                PatientBillDetail.MasterName = detail.ServiceName;
                PatientBillDetail.ServiceCode = detail.ServiceCode;
                PatientBillDetail.ServiceCategoryId = detail.CategoryId;
                PatientBillDetail.AliasId = AliasId;
                PatientBillDetail.AliasName = AliasName;


                PatientBillDetail.DoctorClassId = detail.DoctorClassId;
                PatientBillDetail.DrShareTaxId = detail.DrShareTaxId;
                PatientBillDetail.DrTaxPercentage = detail.DrTaxPercentage;
                PatientBillDetail.TaxCode = detail.TaxCode;
                PatientBillDetail.DrTaxAmount = detail.DrTaxAmount;
                PatientBillDetail.DoctorShare = detail.DoctorShare;
                PatientBillDetail.NetAmount = detail.NetAmount;
                PatientBillDetail.EligiblePercentage = detail.EligiblePercentage;
                PatientBillDetail.SharePercentage = detail.SharePercentage;
                PatientBillDetail.ShareAmount = detail.ShareAmount;
                // Doctor Share Info

                PatientBillDetail.Quantity = detail.Quantity;
                PatientBillDetail.Amount = detail.TestPrice * detail.Quantity;
                PatientBillDetail.DiscountAmount = detail.Discount;
                PatientBillDetail.Rate = detail.TestPrice;
                PatientBillDetail.PatientBillId = patientOrder.PatientBillId;
                PatientBillDetail.DepartmentId = detail.DepartmentId;
                PatientBillDetail.GrossAmount = detail.GrossAmount;
                PatientBillDetail.NetAmount = detail.NetAmount;
                PatientBillDetail.BillDateTime = detail.RequestDate;
                PatientBillDetail.OrderDateTime = detail.CreatedAt;
                PatientBillDetail.MasterTypeId = detail.TestTypeId;
                PatientBillDetail.MasterItemId = detail.TestId;
                PatientBillDetail.PatientBillStatusId = 1;
                billingAmount += PatientBillDetail.Amount;
                billDiscountAmount += PatientBillDetail.DiscountAmount;
                PatientBillDetail.PatientBillId = result.dataValues.Id;
                PatientBillDetail.OrderStatusId = detail.OrderStatusId;
                let BillDetail: any = { Data: PatientBillDetail };
                let BillDetailId = await PatientBillDetailBo.AddPatientBillDetails(BillDetail);
                detail.PatientBillId = result.dataValues.Id;
                detail.PatientBillStatusId = 1;
                detail.PatientBillDetailId = BillDetailId;
                detail.Id = BillDetailId;
                await PatientOrderDetailBo.Update(detail);
                response = result.dataValues.Id;
                let PatientSavedBill = await this.GetPatientBillsById({ Id: result.dataValues.Id });
                PatientSavedBill.BillAmount = billingAmount;
                PatientSavedBill.BillDiscount = billDiscountAmount;
                await this.Update(PatientSavedBill);
            }
        }
        return response;
    }

    public async ManageTreatmenPlanBills(planId: number, PlanInfo: any, planDetails: any): Promise<number> {
        let EncounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        if (await EncounterBo.IsBillLocked({ Id: PlanInfo.EncounterId })) {
            throw { code: 'BILL_ALREADY_LOCKED' };
        }

        let response: number = 0;
        let billingAmount: number = 0;
        let billDiscountAmount: number = 0;
        let TreatmentPlanDetailBo = BoFactory.GetBo(emrbo.TreatmentPlanDetailBo, this.Request);
        let PatientBillDetailBo = BoFactory.GetBo(billingBO.PatientBillDetailsBo, this.Request);
        let servItmAliasBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemAliasBo, this.Request);
        let planDetailsApiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: TreatmentPlanDetailFilters.TreatmentPlanId, Value: PlanInfo.Id }
            ]
        };
        let TreatmentplanDetails = await TreatmentPlanDetailBo.GetTreatmentPlanDetails(planDetailsApiReq);
        if (TreatmentplanDetails.Data.length > 0) {
            let PatientBill: any = {};

            PatientBill = {
                BillAmount: 0,
                BillDiscount: 0,
                BillTypeId: 1,
                BillNumber: '',
                BillDateTime: PlanInfo.PlanRequestDate,
                PatientId: PlanInfo.PatientId,
                EncounterId: PlanInfo.EncounterId,
                EncounterTypeId: PlanInfo.Encounter.EncounterTypeId, //In Patient Encounter
                GuarantorId: PlanInfo.Encounter.GuarantorId,
                GuarantorTypeId: PlanInfo.Encounter.GuarantorTypeId,
                ServiceRateCategoryId: PlanInfo.Encounter.ServiceRateCategoryId,
                DoctorId: PlanInfo.DoctorId,
                // OTRegisterId: PlanInfo.SurgeryEntryId,
                // OTIdentifier: PlanInfo.SurgeryIdentifier,
                // NetPatientAmount: PlanInfo.NetPatientAmount,
                // NetInsuranceAmount: PlanInfo.NetInsuranceAmount,
                PatientBillStatusId: 1,
                FacilityId: PlanInfo.Encounter.FacilityId,
                DepartmentId: PlanInfo.Encounter.DepartmentId,
                OrganizationId: PlanInfo.Encounter.OrganizationId,
                BillGeneratedBy: this.Session.UserId
            };

            if (!PatientBill.BillGeneratedBy) {
                PatientBill.BillGeneratedBy = this.Session.UserId;
            }
            if (!PatientBill.FacilityId) PatientBill.FacilityId = this.Session.FacilityId;
            let result = await this.Save(PatientBill);

            let PlanDetail: any = TreatmentplanDetails.Data[0];

            let PatientBillDetail: any = {};
            if (PlanDetail.ServiceItem && PlanDetail.ServiceItem.Id > 0) {
                let ServiceItem: any = PlanDetail.ServiceItem;
                let AliasId = null;
                let AliasName = null;
                let apiServAliasReq = {
                    Id: 0,
                    Params: [
                        { Key: ServiceItemAliasFilters.ServiceItemId, Value: ServiceItem.Id },
                    ],
                    PageContext: { PageSize: -1, PageNumber: 1 }
                };
                let seritmaliasdata = await servItmAliasBo.GetServiceItemAliass(apiServAliasReq);
                if (seritmaliasdata.Data && seritmaliasdata.Data.length > 0) {
                    AliasId = seritmaliasdata.Data[0].AliasId;
                    AliasName = seritmaliasdata.Data[0].AliasName;
                }
                // }
                PatientBillDetail.ServiceId = ServiceItem.Id;
                PatientBillDetail.ServiceName = PlanDetail.ServiceName;
                PatientBillDetail.MasterName = PlanDetail.ServiceName;
                PatientBillDetail.ServiceCode = ServiceItem.ItemCode;
                PatientBillDetail.ServiceCategoryId = ServiceItem.CategoryId;
                PatientBillDetail.AliasId = AliasId;
                PatientBillDetail.AliasName = AliasName;


                // Doctor Share Info
                PatientBillDetail.DoctorClassId = PlanDetail.DoctorClassId;
                PatientBillDetail.DrShareTaxId = PlanDetail.DrShareTaxId;
                PatientBillDetail.DrTaxPercentage = PlanDetail.DrTaxPercentage;
                PatientBillDetail.TaxCode = PlanDetail.TaxCode;
                PatientBillDetail.DrTaxAmount = PlanDetail.DrTaxAmount;
                PatientBillDetail.DoctorShare = PlanDetail.DoctorShare;
                PatientBillDetail.NetAmount = PlanDetail.NetAmount;
                PatientBillDetail.EligiblePercentage = PlanDetail.EligiblePercentage;
                PatientBillDetail.SharePercentage = PlanDetail.SharePercentage;
                PatientBillDetail.ShareAmount = PlanDetail.ShareAmount;
                // Doctor Share Info

                PatientBillDetail.Quantity = TreatmentplanDetails.Data.length;
                PatientBillDetail.Amount = PlanDetail.ServicePrice * TreatmentplanDetails.Data.length;
                PatientBillDetail.DiscountAmount = PlanDetail.Discount || 0;
                PatientBillDetail.Rate = PlanDetail.ServicePrice;
                PatientBillDetail.DepartmentId = PlanDetail.DepartmentId;
                PatientBillDetail.GrossAmount = PlanDetail.ServicePrice * TreatmentplanDetails.Data.length;
                PatientBillDetail.NetAmount = PlanDetail.ServicePrice * TreatmentplanDetails.Data.length;
                PatientBillDetail.BillDateTime = PlanDetail.PlanScheduleDate;
                PatientBillDetail.OrderDateTime = PlanDetail.CreatedAt;
                PatientBillDetail.ServiceRateCategoryId = PlanInfo.Encounter.ServiceRateCategoryId;
                PatientBillDetail.EncounterId = PlanInfo.Encounter.Id;
                PatientBillDetail.DoctorId = PlanInfo.Encounter.DoctorId;
                PatientBillDetail.PatientBillStatusId = 1;
                PatientBillDetail.DoctorName = PlanInfo.Encounter.DoctorName;
                billingAmount += PatientBillDetail.Amount;
                billDiscountAmount += PatientBillDetail.DiscountAmount || 0;
                PatientBillDetail.PatientBillId = result.dataValues.Id;
                PatientBillDetail.OrderStatusId = PlanDetail.OrderStatusId;
                let BillDetail: any = { Data: PatientBillDetail };
                let BillDetailId = await PatientBillDetailBo.AddPatientBillDetails(BillDetail);
                for (let tdx in TreatmentplanDetails.Data) {
                    let Pldetail = TreatmentplanDetails.Data[tdx];
                    Pldetail.PatientBillId = result.dataValues.Id;
                    Pldetail.PatientBillStatusId = 1;
                    Pldetail.PatientBillDetailId = BillDetailId;
                    await TreatmentPlanDetailBo.Update(Pldetail);
                }
            }
            response = result.dataValues.Id;
            let PatientSavedBill = await this.GetPatientBillsById({ Id: result.dataValues.Id });
            PatientSavedBill.BillAmount = billingAmount;
            PatientSavedBill.BillDiscount = billDiscountAmount;
            await this.Update(PatientSavedBill);
        }
        return response;
    }

    public async ManageIPOrderPatientBills(orderId: number, detailIds: any,
        patientOrder: any, fromward: boolean, isDirectBill: boolean, orderStatus: number,
        OrdDetails: any): Promise<number> {
        let EncounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        if (await EncounterBo.IsBillLocked({ Id: patientOrder.EncounterId })) {
            throw { code: 'BILL_ALREADY_LOCKED' };
        }
        let DrShareDetailInfo = [];
        let DoctorClassId = -1;
        let DrIncludeTax = false;
        if (patientOrder && patientOrder.Encounter &&
            patientOrder.Encounter.DoctorId) {
            var DoctorId = patientOrder.Encounter.DoctorId;
            if (DoctorId) { // Doctor Share calculation
                let userinfoBo = BoFactory.GetBo(userbo.UserBo, this.Request);
                let doctorShareBo = BoFactory.GetBo(billingBO.DoctorShareBo, this.Request);
                let Drinfo = await userinfoBo.GetUserById({ Id: DoctorId });
                DoctorClassId = Drinfo.DoctorClassId;
                DrIncludeTax = Drinfo.IsIncludeTax;
                let apiDrShareReq = {
                    Id: 0,
                    Params: [
                        { Key: DoctorShareFilters.DoctorClassId, Value: DoctorClassId },
                        { Key: DoctorShareFilters.EncounterTypeId, Value: 2 }, // EncounterTypeId
                    ],
                    PageContext: { PageSize: -1, PageNumber: 1 }
                };
                let doctorShareData: any = await doctorShareBo.GetDoctorShare(apiDrShareReq);
                for (let idx in doctorShareData.Data) {
                    if (doctorShareData.Data[idx].DoctorShareDetails)
                        DrShareDetailInfo = doctorShareData.Data[idx].DoctorShareDetails;
                }

            }
        }

        let response: number = 0;
        let billingAmount: number = 0;
        let billDiscountAmount: number = 0;
        let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
        let PatientBillDetailBo = BoFactory.GetBo(billingBO.PatientBillDetailsBo, this.Request);

        // let patGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let servItmAliasBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemAliasBo, this.Request);

        let PatientOrderDetailsApiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientOrderDetailFilters.PatientOrderId, Value: patientOrder.Id },
            {
                Key: PatientOrderDetailFilters.IncludeServiceItem,
                Value: { 'ServiceRateCategoryId': patientOrder.Encounter.ServiceRateCategoryId }
            },
            { Key: PatientOrderDetailFilters.Ids, Value: detailIds },
            { Key: PatientOrderDetailFilters.IsPackage, Value: false },
            { Key: PatientOrderDetailFilters.IsDirectBill, Value: isDirectBill },
            { Key: PatientOrderDetailFilters.OrderStatusId, Value: orderStatus }
            ]
        };
        let PatientOrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(PatientOrderDetailsApiReq);
        if (PatientOrderDetails.Data.length > 0) {
            let PatientBill: any = {};

            PatientBill = {
                BillAmount: 0,
                BillDiscount: 0,
                BillTypeId: 3,
                BillNumber: await Sequence.Next(SequenceKeys.IPBillNumberId),
                BillDateTime: patientOrder.OrderRequestDate,
                PatientId: patientOrder.PatientId,
                EncounterId: patientOrder.EncounterId,
                EncounterTypeId: patientOrder.Encounter.EncounterTypeId, //In Patient Encounter
                GuarantorId: patientOrder.Encounter.GuarantorId,
                GuarantorTypeId: patientOrder.Encounter.GuarantorTypeId,
                ServiceRateCategoryId: patientOrder.Encounter.ServiceRateCategoryId,
                DoctorId: patientOrder.DoctorId,
                OTRegisterId: patientOrder.SurgeryEntryId,
                OTIdentifier: patientOrder.SurgeryIdentifier,
                NetPatientAmount: patientOrder.NetPatientAmount,
                NetInsuranceAmount: patientOrder.NetInsuranceAmount,
                PatientBillStatusId: 3,
                FacilityId: patientOrder.Encounter.FacilityId,
                DepartmentId: patientOrder.Encounter.DepartmentId,
                OrganizationId: patientOrder.Encounter.OrganizationId,
                PatientOrderId: orderId,
                IsDirectBill: isDirectBill,
                IsFromWard: fromward,
                BillGeneratedBy: this.Session.UserId
            };

            // let patguarantordata: any = null;

            // patguarantordata = await
            //     patGuarantorBo.GetPatientGuarantorById({ Id: patientOrder.Encounter.GuarantorId });

            if (DoctorClassId && DoctorClassId > 0 && DrShareDetailInfo.length > 0) {
                let gstbo = BoFactory.GetBo(inventoryBO.GstMasterBo, this.Request);
                for (let dtidx in PatientOrderDetails.Data) {
                    let detail: any = PatientOrderDetails.Data[dtidx];
                    if (!detail.IsPackage) {
                        let ServiceItem: any = detail.ServiceItem;
                        for (let idx in DrShareDetailInfo) {
                            let shareitem = DrShareDetailInfo[idx];
                            detail.DrShareTaxId = -1;
                            detail.BillDateTime = patientOrder.OrderRequestDate;
                            detail.DrTaxPercentage = 0;
                            if (ServiceItem.GstId > 0) {
                                let GstMaster = await gstbo.GetGstMasterById({ Id: ServiceItem.GstId });
                                if (GstMaster && GstMaster.Id)
                                    detail.DrShareTaxId = GstMaster.Id;
                                if (GstMaster && GstMaster.GstPercentage)
                                    detail.DrTaxPercentage = GstMaster.GstPercentage;
                                if (GstMaster && GstMaster.GstCode)
                                    detail.TaxCode = GstMaster.GstCode;
                            }
                            if (shareitem.SharingTypeId === 1) { // Category wise share
                                if (ServiceItem.ServiceCategoryId === shareitem.ServiceCategoryId) {
                                    let EligiblePerAmount = 0;
                                    let SharePerAmount = 0;
                                    let TaxPerAmount = 0;
                                    detail.DoctorClassId = DoctorClassId;
                                    detail.EligiblePercentage = shareitem.EligiblePercentage;
                                    detail.SharePercentage = shareitem.SharePercentage;
                                    detail.ShareAmount = shareitem.ShareAmount;
                                    try {
                                        EligiblePerAmount = detail.NetAmount * (detail.EligiblePercentage / 100);
                                        SharePerAmount = EligiblePerAmount * (detail.SharePercentage / 100);
                                        detail.DoctorShare = SharePerAmount;
                                    } catch (ex) { console.log(ex); }
                                    try {
                                        if (DrIncludeTax && detail.DrTaxPercentage > 0) {
                                            TaxPerAmount = SharePerAmount * (detail.DrTaxPercentage / 100);
                                            detail.DrTaxAmount = TaxPerAmount;
                                            detail.DoctorShare += detail.DrTaxAmount;
                                            detail.NetAmount += detail.DrTaxAmount;
                                        }
                                    } catch (ex) { console.log(ex); }
                                    await PatientOrderDetailBo.Update(detail);
                                    break;
                                }
                            } else if (shareitem.SharingTypeId === 2) { // Doctor wise share
                                if (ServiceItem.Id === shareitem.ServiceId) {
                                    let EligiblePerAmount = 0;
                                    let SharePerAmount = 0;
                                    let TaxPerAmount = 0;
                                    detail.DoctorClassId = DoctorClassId;
                                    detail.EligiblePercentage = shareitem.EligiblePercentage;
                                    detail.SharePercentage = shareitem.SharePercentage;
                                    detail.ShareAmount = shareitem.ShareAmount;
                                    try {
                                        EligiblePerAmount = detail.NetAmount * (detail.EligiblePercentage / 100);
                                        if (shareitem.SharePercentage === 0) {
                                            SharePerAmount = (EligiblePerAmount / 100) * shareitem.ShareAmount;
                                        } else {
                                            SharePerAmount = EligiblePerAmount * (detail.SharePercentage / 100);
                                        }
                                        detail.DoctorShare = SharePerAmount;
                                    } catch (ex) { console.log(ex); }
                                    try {
                                        if (DrIncludeTax && detail.DrTaxPercentage > 0) {
                                            TaxPerAmount = SharePerAmount * (detail.DrTaxPercentage / 100);
                                            detail.DrTaxAmount = TaxPerAmount;
                                            detail.DoctorShare += detail.DrTaxAmount;
                                            detail.NetAmount += detail.DrTaxAmount;
                                        }
                                    } catch (ex) { console.log(ex); }
                                    await PatientOrderDetailBo.Update(detail);
                                    break;
                                }
                            }
                        }
                    }
                }
            }

            PatientOrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(PatientOrderDetailsApiReq);
            if (!PatientBill.BillGeneratedBy) {
                PatientBill.BillGeneratedBy = this.Session.UserId;
            }
            if (!PatientBill.FacilityId) PatientBill.FacilityId = this.Session.FacilityId;
            let result = await this.Save(PatientBill);
            await Promise.all(PatientOrderDetails.Data.map((DetailItem: any): Promise<void> => {
                return (async (detail): Promise<void> => {
                    if (!detail.IsPackage) {
                        let PatientOrderDetail: any = detail;

                        let PatientBillDetail: any = {};
                        if (PatientOrderDetail.ServiceItem && PatientOrderDetail.ServiceItem.Id > 0) {
                            let ServiceItem: any = PatientOrderDetail.ServiceItem;
                            let AliasId = null;
                            let AliasName = null;
                            // if (patientOrder.Encounter.GuarantorTypeId > 1) {
                            let apiServAliasReq = {
                                Id: 0,
                                Params: [
                                    { Key: ServiceItemAliasFilters.ServiceItemId, Value: ServiceItem.Id },
                                    // { Key: ServiceItemAliasFilters.ExternalProviderId, Value: patguarantordata.GuarantorId }
                                ],
                                PageContext: { PageSize: -1, PageNumber: 1 }
                            };
                            let seritmaliasdata = await servItmAliasBo.GetServiceItemAliass(apiServAliasReq);
                            if (seritmaliasdata.Data && seritmaliasdata.Data.length > 0) {
                                AliasId = seritmaliasdata.Data[0].AliasId;
                                AliasName = seritmaliasdata.Data[0].AliasName;
                            }
                            // }
                            PatientBillDetail.ServiceId = ServiceItem.Id;
                            PatientBillDetail.ServiceName = detail.TestName;
                            PatientBillDetail.MasterName = detail.TestName;
                            PatientBillDetail.ServiceCode = ServiceItem.ItemCode;
                            PatientBillDetail.ServiceCategoryId = ServiceItem.CategoryId;
                            PatientBillDetail.AliasId = AliasId;
                            PatientBillDetail.AliasName = AliasName;


                            // Doctor Share Info
                            PatientBillDetail.DoctorClassId = detail.DoctorClassId;
                            PatientBillDetail.DrShareTaxId = detail.DrShareTaxId;
                            PatientBillDetail.DrTaxPercentage = detail.DrTaxPercentage;
                            PatientBillDetail.TaxCode = detail.TaxCode;
                            PatientBillDetail.DrTaxAmount = detail.DrTaxAmount;
                            PatientBillDetail.DoctorShare = detail.DoctorShare;
                            PatientBillDetail.NetAmount = detail.NetAmount;
                            PatientBillDetail.EligiblePercentage = detail.EligiblePercentage;
                            PatientBillDetail.SharePercentage = detail.SharePercentage;
                            PatientBillDetail.ShareAmount = detail.ShareAmount;
                            // Doctor Share Info

                            PatientBillDetail.Quantity = detail.Quantity;
                            PatientBillDetail.Amount = detail.TestPrice * detail.Quantity;
                            PatientBillDetail.DiscountAmount = detail.Discount;
                            PatientBillDetail.Rate = detail.TestPrice;
                            PatientBillDetail.UnitPrice = detail.TestPrice;
                            PatientBillDetail.PatientBillId = patientOrder.PatientBillId;
                            PatientBillDetail.DepartmentId = detail.DepartmentId;
                            // PatientBillDetail.MasterItemId = detail.MasterId;
                            PatientBillDetail.GrossAmount = detail.TestPrice;
                            PatientBillDetail.NetAmount = detail.NetAmount;
                            PatientBillDetail.InsNetAmount = detail.InsNetAmount;
                            PatientBillDetail.PatNetAmount = detail.PatNetAmount;
                            PatientBillDetail.BillDateTime = detail.RequestDate;
                            PatientBillDetail.OrderDateTime = detail.CreatedAt;
                            PatientBillDetail.DoctorId = detail.DoctorId;
                            PatientBillDetail.MasterTypeId = detail.TestTypeId;
                            PatientBillDetail.ServiceRateCategoryId = patientOrder.Encounter.ServiceRateCategoryId;
                            PatientBillDetail.MasterItemId = detail.TestId;
                            PatientBillDetail.EncounterId = patientOrder.Encounter.Id;
                            // PatientBillDetail.DoctorId = patientOrder.Encounter.DoctorId;
                            PatientBillDetail.PatientBillStatusId = 3;
                            // PatientBillDetail.DoctorName = patientOrder.Encounter.DoctorName;
                            billingAmount += PatientBillDetail.Amount;
                            billDiscountAmount += PatientBillDetail.DiscountAmount;
                            PatientBillDetail.PatientBillId = result.dataValues.Id;
                            PatientBillDetail.OrderStatusId = detail.OrderStatusId;
                            let BillDetail: any = { Data: PatientBillDetail };
                            let BillDetailId = await PatientBillDetailBo.AddPatientBillDetails(BillDetail);
                            detail.PatientBillId = result.dataValues.Id;
                            let docShareBO = BoFactory.GetBo(BillingBo.PatientDoctorShareDetailsBo, this.Request);
                            let docDetail: any = [];
                            if (detail.Id && detail.Id > 0 && detail.DoctorShare > 0) {
                                let billdetail: any = {};
                                if (detail.DoctorShare > 0) {
                                    if (detail.AllowIPDocShare === 0 || !detail.AllowIPDocShare) {
                                        billdetail = detail;
                                        let docData: any = {
                                            Id: 0,
                                            PatientBillId: detail.PatientBillId,
                                            PatientBillDetailId: BillDetailId,
                                            FacilityId: this.Session.FacilityId,
                                            BillDateTime: billdetail.BillDateTime,
                                            ServiceItemId: PatientBillDetail.ServiceId,
                                            ServiceCode: PatientBillDetail.ServiceCode,
                                            ServiceName: PatientBillDetail.ServiceName,
                                            ServiceAmount: detail.NetAmount,
                                            TeamId: detail.TeamId || 0,
                                            DoctorId: detail.DoctorId,
                                            DoctorName: detail.DoctorName,
                                            DoctorSharePercentage: detail.DoctorShareValue,
                                            DoctorShareAmount: detail.DoctorShare,
                                            IsInvoicedDoctorShare: false,
                                            PatientTypeId: detail.PatientTypeId,
                                            EncounterId: detail.EncounterId,
                                            ShareType: 1,
                                            DoctorShareStatusId: 1
                                        };
                                        docDetail.push(docData);
                                    }
                                    await docShareBO.ManagePatientDoctorShareDetails(detail.PatientBillId, BillDetailId, docDetail);
                                }
                            }
                            for (let odx in OrdDetails) {
                                let orderDetail = OrdDetails[odx];
                                if (orderDetail.TestId === PatientOrderDetail.TestId) {
                                    if (detail.Id && detail.Id > 0 && orderDetail.DocShareDetails &&
                                        orderDetail.DocShareDetails.length > 0) {
                                        let billdetail: any = {};
                                        let docDetail: any = [];
                                        let docShareDetail: any = {};
                                        for (let pdx in orderDetail.DocShareDetails) {
                                            docShareDetail = orderDetail.DocShareDetails[pdx];
                                            if (detail.Id && detail.Id > 0 && docShareDetail.PerformDoctorId > 0) {
                                                billdetail = detail;
                                                let docData: any = {
                                                    Id: 0,
                                                    PatientBillId: detail.PatientBillId,
                                                    PatientBillDetailId: BillDetailId,
                                                    FacilityId: this.Session.FacilityId,
                                                    BillDateTime: PatientBillDetail.BillDateTime,
                                                    ServiceItemId: PatientBillDetail.ServiceId,
                                                    ServiceCode: PatientBillDetail.ServiceCode,
                                                    ServiceName: PatientBillDetail.ServiceName,
                                                    ServiceAmount: billdetail.NetAmount,
                                                    TeamId: detail.TeamId || 0,
                                                    DoctorId: docShareDetail.PerformDoctorId,
                                                    DoctorName: docShareDetail.PerformDoctorName,
                                                    DoctorSharePercentage: docShareDetail.PerformDrShareValue,
                                                    DoctorShareAmount: docShareDetail.PerformDrShare,
                                                    PatientTypeId: docShareDetail.PatientTypeId,
                                                    EncounterId: docShareDetail.EncounterId,
                                                    IsInvoicedDoctorShare: false,
                                                    ShareType: 2,
                                                    DoctorShareStatusId: 1
                                                };
                                                docDetail.push(docData);
                                            }
                                        }
                                        await docShareBO.ManagePatientDoctorShareDetails(detail.PatientBillId, BillDetailId, docDetail);
                                    }
                                }
                            }
                            detail.PatientBillStatusId = 3;
                            detail.PatientBillDetailId = BillDetailId;
                            await PatientOrderDetailBo.Update(detail);
                        }
                    }
                })(DetailItem);
            }));
            response = result.dataValues.Id;
            let PatientSavedBill = await this.GetPatientBillsById({ Id: result.dataValues.Id });
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            await billSummBo.ManageOrderBillSummary(response);
            PatientSavedBill.BillAmount = billingAmount;
            PatientSavedBill.BillDiscount = billDiscountAmount;
            await this.Update(PatientSavedBill);

        }
        return response;
    }

    public async ManageOrderPatientDraftBills(orderId: number, detailIds: any,
        patientOrder: any, fromward: boolean, isDirectBill: boolean): Promise<number> {
        // let EncounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        // if (await EncounterBo.IsBillLocked({ Id: patientOrder.EncounterId })) {
        //     throw { code: 'BILL_ALREADY_LOCKED' };
        // }
        let DrShareDetailInfo = [];
        let DoctorClassId = -1;
        let DrIncludeTax = false;
        if (patientOrder && patientOrder.Encounter &&
            patientOrder.Encounter.DoctorId) {
            var DoctorId = patientOrder.Encounter.DoctorId;
            if (DoctorId) { // Doctor Share calculation
                let userinfoBo = BoFactory.GetBo(userbo.UserBo, this.Request);
                let doctorShareBo = BoFactory.GetBo(billingBO.DoctorShareBo, this.Request);
                let Drinfo = await userinfoBo.GetUserById({ Id: DoctorId });
                DoctorClassId = Drinfo.DoctorClassId;
                DrIncludeTax = Drinfo.IsIncludeTax;
                let apiDrShareReq = {
                    Id: 0,
                    Params: [
                        { Key: DoctorShareFilters.DoctorClassId, Value: DoctorClassId },
                        { Key: DoctorShareFilters.EncounterTypeId, Value: 2 }, // EncounterTypeId
                    ],
                    PageContext: { PageSize: -1, PageNumber: 1 }
                };
                let doctorShareData: any = await doctorShareBo.GetDoctorShare(apiDrShareReq);
                for (let idx in doctorShareData.Data) {
                    if (doctorShareData.Data[idx].DoctorShareDetails)
                        DrShareDetailInfo = doctorShareData.Data[idx].DoctorShareDetails;
                }

            }
        }

        let response: number = 0;
        let billingAmount: number = 0;
        let billDiscountAmount: number = 0;
        let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
        let PatientBillDetailBo = BoFactory.GetBo(billingBO.PatientBillDetailsBo, this.Request);

        // let patGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let servItmAliasBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemAliasBo, this.Request);

        let PatientOrderDetailsApiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientOrderDetailFilters.PatientOrderId, Value: patientOrder.Id },
            {
                Key: PatientOrderDetailFilters.IncludeServiceItem,
                Value: { 'ServiceRateCategoryId': patientOrder.Encounter.ServiceRateCategoryId }
            },
            { Key: PatientOrderDetailFilters.Ids, Value: detailIds },
            { Key: PatientOrderDetailFilters.IsPackage, Value: false },
                // { Key: PatientOrderDetailFilters.IsDirectBill, Value: isDirectBill }
            ]
        };
        let PatientOrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(PatientOrderDetailsApiReq);
        if (PatientOrderDetails.Data.length > 0) {
            let PatientBill: any = {};

            PatientBill = {
                BillAmount: 0,
                BillDiscount: 0,
                BillTypeId: 1,
                BillNumber: await Sequence.Next(SequenceKeys.OPBillNumberId),
                BillDateTime: patientOrder.OrderRequestDate,
                PatientId: patientOrder.PatientId,
                EncounterId: patientOrder.EncounterId,
                EncounterTypeId: patientOrder.Encounter.EncounterTypeId, //In Patient Encounter
                GuarantorId: patientOrder.Encounter.GuarantorId,
                GuarantorTypeId: patientOrder.Encounter.GuarantorTypeId,
                ServiceRateCategoryId: patientOrder.Encounter.ServiceRateCategoryId,
                DoctorId: patientOrder.DoctorId,
                OTRegisterId: patientOrder.SurgeryEntryId,
                OTIdentifier: patientOrder.SurgeryIdentifier,
                PatientBillStatusId: 1,
                FacilityId: patientOrder.Encounter.FacilityId,
                DepartmentId: patientOrder.Encounter.DepartmentId,
                OrganizationId: patientOrder.Encounter.OrganizationId,
                PatientOrderId: orderId,
                IsPaidFully: false,
                IsDirectBill: isDirectBill,
                IsFromWard: fromward,
                BillGeneratedBy: this.Session.UserId
            };

            // let patguarantordata: any = null;

            // patguarantordata = await
            //     patGuarantorBo.GetPatientGuarantorById({ Id: patientOrder.Encounter.GuarantorId });

            if (DoctorClassId && DoctorClassId > 0 && DrShareDetailInfo.length > 0) {
                let gstbo = BoFactory.GetBo(inventoryBO.GstMasterBo, this.Request);
                for (let dtidx in PatientOrderDetails.Data) {
                    let detail: any = PatientOrderDetails.Data[dtidx];
                    if (!detail.IsPackage) {
                        let ServiceItem: any = detail.ServiceItem;
                        for (let idx in DrShareDetailInfo) {
                            let shareitem = DrShareDetailInfo[idx];
                            detail.DrShareTaxId = -1;
                            detail.BillDateTime = patientOrder.OrderRequestDate;
                            detail.DrTaxPercentage = 0;
                            if (ServiceItem.GstId > 0) {
                                let GstMaster = await gstbo.GetGstMasterById({ Id: ServiceItem.GstId });
                                if (GstMaster && GstMaster.Id)
                                    detail.DrShareTaxId = GstMaster.Id;
                                if (GstMaster && GstMaster.GstPercentage)
                                    detail.DrTaxPercentage = GstMaster.GstPercentage;
                                if (GstMaster && GstMaster.GstCode)
                                    detail.TaxCode = GstMaster.GstCode;
                            }
                            if (shareitem.SharingTypeId === 1) { // Category wise share
                                if (ServiceItem.ServiceCategoryId === shareitem.ServiceCategoryId) {
                                    let EligiblePerAmount = 0;
                                    let SharePerAmount = 0;
                                    let TaxPerAmount = 0;
                                    detail.DoctorClassId = DoctorClassId;
                                    detail.EligiblePercentage = shareitem.EligiblePercentage;
                                    detail.SharePercentage = shareitem.SharePercentage;
                                    detail.ShareAmount = shareitem.ShareAmount;
                                    try {
                                        EligiblePerAmount = detail.NetAmount * (detail.EligiblePercentage / 100);
                                        SharePerAmount = EligiblePerAmount * (detail.SharePercentage / 100);
                                        detail.DoctorShare = SharePerAmount;
                                    } catch (ex) { console.log(ex); }
                                    try {
                                        if (DrIncludeTax && detail.DrTaxPercentage > 0) {
                                            TaxPerAmount = SharePerAmount * (detail.DrTaxPercentage / 100);
                                            detail.DrTaxAmount = TaxPerAmount;
                                            detail.DoctorShare += detail.DrTaxAmount;
                                            detail.NetAmount += detail.DrTaxAmount;
                                        }
                                    } catch (ex) { console.log(ex); }
                                    await PatientOrderDetailBo.Update(detail);
                                    break;
                                }
                            } else if (shareitem.SharingTypeId === 2) { // Doctor wise share
                                if (ServiceItem.Id === shareitem.ServiceId) {
                                    let EligiblePerAmount = 0;
                                    let SharePerAmount = 0;
                                    let TaxPerAmount = 0;
                                    detail.DoctorClassId = DoctorClassId;
                                    detail.EligiblePercentage = shareitem.EligiblePercentage;
                                    detail.SharePercentage = shareitem.SharePercentage;
                                    detail.ShareAmount = shareitem.ShareAmount;
                                    try {
                                        EligiblePerAmount = detail.NetAmount * (detail.EligiblePercentage / 100);
                                        if (shareitem.SharePercentage === 0) {
                                            SharePerAmount = (EligiblePerAmount / 100) * shareitem.ShareAmount;
                                        } else {
                                            SharePerAmount = EligiblePerAmount * (detail.SharePercentage / 100);
                                        }
                                        detail.DoctorShare = SharePerAmount;
                                    } catch (ex) { console.log(ex); }
                                    try {
                                        if (DrIncludeTax && detail.DrTaxPercentage > 0) {
                                            TaxPerAmount = SharePerAmount * (detail.DrTaxPercentage / 100);
                                            detail.DrTaxAmount = TaxPerAmount;
                                            detail.DoctorShare += detail.DrTaxAmount;
                                            detail.NetAmount += detail.DrTaxAmount;
                                        }
                                    } catch (ex) { console.log(ex); }
                                    await PatientOrderDetailBo.Update(detail);
                                    break;
                                }
                            }
                        }
                    }
                }
            }

            PatientOrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(PatientOrderDetailsApiReq);
            if (!PatientBill.BillGeneratedBy) {
                PatientBill.BillGeneratedBy = this.Session.UserId;
            }
            if (!PatientBill.FacilityId) PatientBill.FacilityId = this.Session.FacilityId;
            let result = await this.Save(PatientBill);
            await Promise.all(PatientOrderDetails.Data.map((DetailItem: any): Promise<void> => {
                return (async (detail): Promise<void> => {
                    if (!detail.IsPackage) {
                        let PatientOrderDetail: any = detail;
                        let PatientBillDetail: any = {};
                        if (PatientOrderDetail.ServiceItem && PatientOrderDetail.ServiceItem.Id > 0) {
                            let ServiceItem: any = PatientOrderDetail.ServiceItem;
                            let AliasId = null;
                            let AliasName = null;
                            // if (patientOrder.Encounter.GuarantorTypeId > 1) {
                            let apiServAliasReq = {
                                Id: 0,
                                Params: [
                                    { Key: ServiceItemAliasFilters.ServiceItemId, Value: ServiceItem.Id },
                                    // { Key: ServiceItemAliasFilters.ExternalProviderId, Value: patguarantordata.GuarantorId }
                                ],
                                PageContext: { PageSize: -1, PageNumber: 1 }
                            };
                            let seritmaliasdata = await servItmAliasBo.GetServiceItemAliass(apiServAliasReq);
                            if (seritmaliasdata.Data && seritmaliasdata.Data.length > 0) {
                                AliasId = seritmaliasdata.Data[0].AliasId;
                                AliasName = seritmaliasdata.Data[0].AliasName;
                            }
                            // }
                            PatientBillDetail.ServiceId = ServiceItem.Id;
                            PatientBillDetail.ServiceName = detail.TestName;
                            PatientBillDetail.MasterName = detail.TestName;
                            PatientBillDetail.ServiceCode = ServiceItem.ItemCode;
                            PatientBillDetail.ServiceCategoryId = ServiceItem.CategoryId;
                            PatientBillDetail.AliasId = AliasId;
                            PatientBillDetail.AliasName = AliasName;
                        }

                        // Doctor Share Info
                        PatientBillDetail.DoctorClassId = detail.DoctorClassId;
                        PatientBillDetail.DrShareTaxId = detail.DrShareTaxId;
                        PatientBillDetail.DrTaxPercentage = detail.DrTaxPercentage;
                        PatientBillDetail.TaxCode = detail.TaxCode;
                        PatientBillDetail.DrTaxAmount = detail.DrTaxAmount;
                        PatientBillDetail.DoctorShare = detail.DoctorShare;
                        PatientBillDetail.NetAmount = detail.NetAmount;
                        PatientBillDetail.EligiblePercentage = detail.EligiblePercentage;
                        PatientBillDetail.SharePercentage = detail.SharePercentage;
                        PatientBillDetail.ShareAmount = detail.ShareAmount;
                        // Doctor Share Info

                        PatientBillDetail.Quantity = detail.Quantity;
                        PatientBillDetail.Amount = detail.TestPrice * detail.Quantity;
                        PatientBillDetail.DiscountAmount = detail.Discount;
                        PatientBillDetail.Rate = detail.TestPrice;
                        PatientBillDetail.PatientBillId = patientOrder.PatientBillId;
                        PatientBillDetail.DepartmentId = detail.DepartmentId;
                        // PatientBillDetail.MasterItemId = detail.MasterId;
                        PatientBillDetail.GrossAmount = detail.TestPrice;
                        PatientBillDetail.NetAmount = detail.NetAmount;
                        PatientBillDetail.BillDateTime = detail.RequestDate;
                        PatientBillDetail.OrderDateTime = detail.CreatedAt;
                        // PatientBillDetail.DoctorId = detail.DoctorId;
                        PatientBillDetail.MasterTypeId = detail.TestTypeId;
                        PatientBillDetail.ServiceRateCategoryId = patientOrder.Encounter.ServiceRateCategoryId;
                        PatientBillDetail.MasterItemId = detail.TestId;
                        PatientBillDetail.EncounterId = patientOrder.Encounter.Id;
                        PatientBillDetail.DoctorId = patientOrder.Encounter.DoctorId;
                        PatientBillDetail.PatientBillStatusId = 3;
                        PatientBillDetail.DoctorName = patientOrder.Encounter.DoctorName;
                        billingAmount += PatientBillDetail.Amount;
                        billDiscountAmount += PatientBillDetail.DiscountAmount;
                        PatientBillDetail.PatientBillId = result.dataValues.Id;
                        PatientBillDetail.OrderStatusId = detail.OrderStatusId;
                        let BillDetail: any = { Data: PatientBillDetail };
                        let BillDetailId = await PatientBillDetailBo.AddPatientBillDetails(BillDetail);
                        detail.PatientBillId = result.dataValues.Id;
                        detail.PatientBillStatusId = 1;
                        detail.PatientBillDetailId = BillDetailId;
                        await PatientOrderDetailBo.Update(detail);
                    }
                })(DetailItem);
            }));
            response = result.dataValues.Id;
            let PatientSavedBill = await this.GetPatientBillsById({ Id: result.dataValues.Id });
            PatientSavedBill.BillAmount = billingAmount;
            PatientSavedBill.OutStandingAmount = billingAmount;
            PatientSavedBill.BillDiscount = billDiscountAmount;
            await this.Update(PatientSavedBill);
        }
        return response;
    }

    public async ManageIPCancelDietOrderPatientBills(DietorderId: number, detailIds: any, patientDietOrder: any, isDirectBill: boolean):
        Promise<number> {
        let response: number = 0;
        let billingAmount: number = 0;
        let billDiscountAmount: number = 0;
        let PatientDietOrderDetailBo = BoFactory.GetBo(emrbo.PatientDietOrderDetailBo, this.Request);
        let PatientBillDetailBo = BoFactory.GetBo(billingBO.PatientBillDetailsBo, this.Request);
        let patGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let servItmAliasBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemAliasBo, this.Request);
        let PatientDietOrderDetailsApiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientDietOrderDetailFilters.PatientDietOrderId, Value: patientDietOrder.Id },
            {
                Key: PatientDietOrderDetailFilters.IncludeServiceItem,
                Value: { 'ServiceRateCategoryId': patientDietOrder.Encounter.ServiceRateCategoryId }
            },
            { Key: PatientDietOrderDetailFilters.Ids, Value: detailIds },
            { Key: PatientDietOrderDetailFilters.IsDirectBill, Value: isDirectBill }
            ]
        };
        let PatientDietOrderDetails = await PatientDietOrderDetailBo.GetPatientDietOrderDetails(PatientDietOrderDetailsApiReq);
        if (PatientDietOrderDetails.Data.length > 0) {
            let PatientBill: any = {};
            PatientBill = {
                BillAmount: 0,
                BillDiscount: 0,
                BillTypeId: 3,
                BillNumber: await Sequence.Next(SequenceKeys.IPBillNumberId),
                BillDateTime: new Date(),
                PatientId: patientDietOrder.PatientId,
                EncounterId: patientDietOrder.EncounterId,
                EncounterTypeId: patientDietOrder.Encounter.EncounterTypeId, //In Patient Encounter
                GuarantorId: patientDietOrder.Encounter.GuarantorId,
                GuarantorTypeId: patientDietOrder.Encounter.GuarantorTypeId,
                ServiceRateCategoryId: patientDietOrder.Encounter.ServiceRateCategoryId,
                DoctorId: patientDietOrder.DoctorId,
                PatientBillStatusId: 3,
                FacilityId: patientDietOrder.Encounter.FacilityId,
                DepartmentId: patientDietOrder.Encounter.DepartmentId,
                OrganizationId: patientDietOrder.Encounter.OrganizationId,
                PatientDietOrderId: DietorderId,
                IsDirectBill: isDirectBill,
            };
            let patguarantordata: any = null;
            patguarantordata = await
                patGuarantorBo.GetPatientGuarantorById({ Id: patientDietOrder.Encounter.GuarantorId });
            if (!PatientBill.BillGeneratedBy) {
                PatientBill.BillGeneratedBy = this.Session.UserId;
            }
            if (!PatientBill.FacilityId) PatientBill.FacilityId = this.Session.FacilityId;
            let result = await this.Save(PatientBill);
            await Promise.all(PatientDietOrderDetails.Data.map((DetailItem): Promise<void> => {
                return (async (detail): Promise<void> => {
                    // if (!detail.IsPackage) {
                    let PatientDietOrderDetail: any = detail;
                    let PatientBillDetail: any = {};
                    if (PatientDietOrderDetail.ServiceItem && PatientDietOrderDetail.ServiceItem.Id > 0) {
                        let ServiceItem: any = PatientDietOrderDetail.ServiceItem;
                        let AliasId = null;
                        let AliasName = null;
                        if (patientDietOrder.Encounter.GuarantorTypeId > 1 && patguarantordata) {
                            let apiServAliasReq = {
                                Id: 0,
                                Params: [
                                    { Key: ServiceItemAliasFilters.ServiceItemId, Value: ServiceItem.Id },
                                    { Key: ServiceItemAliasFilters.ExternalProviderId, Value: patguarantordata.GuarantorId }
                                ],
                                PageContext: { PageSize: -1, PageNumber: 1 }
                            };
                            let seritmaliasdata = await servItmAliasBo.GetServiceItemAliass(apiServAliasReq);
                            if (seritmaliasdata.Data && seritmaliasdata.Data.length > 0) {
                                AliasId = seritmaliasdata.Data[0].AliasId;
                                AliasName = seritmaliasdata.Data[0].AliasName;
                            }
                        }
                        PatientBillDetail.ServiceId = ServiceItem.Id;
                        PatientBillDetail.ServiceName = detail.DietName;
                        PatientBillDetail.MasterName = detail.DietName;
                        PatientBillDetail.ServiceCode = ServiceItem.ItemCode;
                        PatientBillDetail.ServiceCategoryId = ServiceItem.CategoryId;
                        PatientBillDetail.AliasId = AliasId;
                        PatientBillDetail.AliasName = AliasName;
                    }
                    PatientBillDetail.Quantity = detail.Quantity;
                    PatientBillDetail.Amount = -1 * detail.NetAmount;
                    PatientBillDetail.DiscountAmount = -1 * detail.Discount;
                    PatientBillDetail.Rate = detail.Price;
                    PatientBillDetail.PatientBillId = patientDietOrder.PatientBillId;
                    // PatientBillDetail.DepartmentId = detail.DepartmentId;
                    // PatientBillDetail.MasterItemId = detail.MasterId;
                    PatientBillDetail.GrossAmount = -1 * detail.NetAmount;
                    PatientBillDetail.NetAmount = -1 * detail.NetAmount;
                    PatientBillDetail.BillDateTime = new Date();
                    PatientBillDetail.OrderDateTime = detail.CreatedAt;
                    // PatientBillDetail.DoctorId = detail.DoctorId;
                    PatientBillDetail.ServiceRateCategoryId = patientDietOrder.Encounter.ServiceRateCategoryId;
                    PatientBillDetail.EncounterId = patientDietOrder.Encounter.Id;
                    PatientBillDetail.DoctorId = patientDietOrder.Encounter.DoctorId;
                    PatientBillDetail.PatientBillStatusId = 3;
                    PatientBillDetail.DoctorName = patientDietOrder.Encounter.DoctorName;
                    billingAmount += PatientBillDetail.Amount;
                    billDiscountAmount += PatientBillDetail.DiscountAmount;
                    PatientBillDetail.PatientBillId = result.dataValues.Id;
                    PatientBillDetail.OrderStatusId = detail.OrderStatusId;
                    let BillDetail: any = { Data: PatientBillDetail };
                    let BillDetailId = await PatientBillDetailBo.AddPatientBillDetails(BillDetail);
                    detail.PatientBillId = result.dataValues.Id;
                    detail.PatientBillStatusId = 3;
                    detail.PatientBillDetailId = BillDetailId;
                    await PatientDietOrderDetailBo.Update(detail);
                    // }
                })(DetailItem);
            }));
            // await PatientBillDetailBo.ManagePatientBillDetails(result.dataValues.Id, PatientBillDetails);
            response = result.dataValues.Id;
            //  }
            let PatientSavedBill = await this.GetPatientBillsById({ Id: result.dataValues.Id });
            PatientSavedBill.BillAmount = billingAmount;
            PatientSavedBill.BillDiscount = billDiscountAmount;
            await this.Update(PatientSavedBill);
        }
        return response;
    }

    public async ManageIPDietOrderPatientBills(DietorderId: number, detailIds: any, patientDietOrder: any, isDirectBill: boolean):
        Promise<number> {
        let response: number = 0;
        let billingAmount: number = 0;
        let billDiscountAmount: number = 0;
        let PatientDietOrderDetailBo = BoFactory.GetBo(emrbo.PatientDietOrderDetailBo, this.Request);
        let PatientBillDetailBo = BoFactory.GetBo(billingBO.PatientBillDetailsBo, this.Request);
        let patGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let servItmAliasBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemAliasBo, this.Request);
        let PatientDietOrderDetailsApiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientDietOrderDetailFilters.PatientDietOrderId, Value: patientDietOrder.Id },
            {
                Key: PatientDietOrderDetailFilters.IncludeServiceItem,
                Value: { 'ServiceRateCategoryId': patientDietOrder.Encounter.ServiceRateCategoryId }
            },
            { Key: PatientDietOrderDetailFilters.Ids, Value: detailIds },
            { Key: PatientDietOrderDetailFilters.IsDirectBill, Value: isDirectBill }
            ]
        };
        let PatientDietOrderDetails = await PatientDietOrderDetailBo.GetPatientDietOrderDetails(PatientDietOrderDetailsApiReq);
        if (PatientDietOrderDetails.Data.length > 0) {
            let PatientBill: any = {};
            PatientBill = {
                BillAmount: 0,
                BillDiscount: 0,
                BillTypeId: 3,
                BillNumber: await Sequence.Next(SequenceKeys.IPBillNumberId),
                BillDateTime: new Date(),
                PatientId: patientDietOrder.PatientId,
                EncounterId: patientDietOrder.EncounterId,
                EncounterTypeId: patientDietOrder.Encounter.EncounterTypeId, //In Patient Encounter
                GuarantorId: patientDietOrder.Encounter.GuarantorId,
                GuarantorTypeId: patientDietOrder.Encounter.GuarantorTypeId,
                ServiceRateCategoryId: patientDietOrder.Encounter.ServiceRateCategoryId,
                DoctorId: patientDietOrder.DoctorId,
                PatientBillStatusId: 3,
                FacilityId: patientDietOrder.Encounter.FacilityId,
                DepartmentId: patientDietOrder.Encounter.DepartmentId,
                OrganizationId: patientDietOrder.Encounter.OrganizationId,
                PatientDietOrderId: DietorderId,
                IsDirectBill: isDirectBill,
            };
            let patguarantordata: any = null;
            patguarantordata = await
                patGuarantorBo.GetPatientGuarantorById({ Id: patientDietOrder.Encounter.GuarantorId });
            if (!PatientBill.BillGeneratedBy) {
                PatientBill.BillGeneratedBy = this.Session.UserId;
            }
            if (!PatientBill.FacilityId) PatientBill.FacilityId = this.Session.FacilityId;
            let result = await this.Save(PatientBill);
            await Promise.all(PatientDietOrderDetails.Data.map((DetailItem): Promise<void> => {
                return (async (detail): Promise<void> => {
                    // if (!detail.IsPackage) {
                    let PatientDietOrderDetail: any = detail;
                    let PatientBillDetail: any = {};
                    if (PatientDietOrderDetail.ServiceItem && PatientDietOrderDetail.ServiceItem.Id > 0) {
                        let ServiceItem: any = PatientDietOrderDetail.ServiceItem;
                        let AliasId = null;
                        let AliasName = null;
                        if (patientDietOrder.Encounter.GuarantorTypeId > 1 && patguarantordata) {
                            let apiServAliasReq = {
                                Id: 0,
                                Params: [
                                    { Key: ServiceItemAliasFilters.ServiceItemId, Value: ServiceItem.Id },
                                    { Key: ServiceItemAliasFilters.ExternalProviderId, Value: patguarantordata.GuarantorId }
                                ],
                                PageContext: { PageSize: -1, PageNumber: 1 }
                            };
                            let seritmaliasdata = await servItmAliasBo.GetServiceItemAliass(apiServAliasReq);
                            if (seritmaliasdata.Data && seritmaliasdata.Data.length > 0) {
                                AliasId = seritmaliasdata.Data[0].AliasId;
                                AliasName = seritmaliasdata.Data[0].AliasName;
                            }
                        }
                        PatientBillDetail.ServiceId = ServiceItem.Id;
                        PatientBillDetail.ServiceName = detail.DietName;
                        PatientBillDetail.MasterName = detail.DietName;
                        PatientBillDetail.ServiceCode = ServiceItem.ItemCode;
                        PatientBillDetail.ServiceCategoryId = ServiceItem.CategoryId;
                        PatientBillDetail.AliasId = AliasId;
                        PatientBillDetail.AliasName = AliasName;
                    }
                    PatientBillDetail.Quantity = detail.Quantity;
                    PatientBillDetail.Amount = detail.NetAmount;
                    PatientBillDetail.DiscountAmount = detail.Discount;
                    PatientBillDetail.Rate = detail.Price;
                    PatientBillDetail.PatientBillId = patientDietOrder.PatientBillId;
                    // PatientBillDetail.DepartmentId = detail.DepartmentId;
                    // PatientBillDetail.MasterItemId = detail.MasterId;
                    PatientBillDetail.GrossAmount = detail.NetAmount;
                    PatientBillDetail.NetAmount = detail.NetAmount;
                    PatientBillDetail.BillDateTime = new Date();
                    PatientBillDetail.OrderDateTime = detail.CreatedAt;
                    // PatientBillDetail.DoctorId = detail.DoctorId;
                    PatientBillDetail.ServiceRateCategoryId = patientDietOrder.Encounter.ServiceRateCategoryId;
                    PatientBillDetail.EncounterId = patientDietOrder.Encounter.Id;
                    PatientBillDetail.DoctorId = patientDietOrder.Encounter.DoctorId;
                    PatientBillDetail.PatientBillStatusId = 3;
                    PatientBillDetail.DoctorName = patientDietOrder.Encounter.DoctorName;
                    billingAmount += detail.NetAmount;
                    billDiscountAmount += PatientBillDetail.DiscountAmount;
                    PatientBillDetail.PatientBillId = result.dataValues.Id;
                    PatientBillDetail.OrderStatusId = detail.OrderStatusId;
                    let BillDetail: any = { Data: PatientBillDetail };
                    let BillDetailId = await PatientBillDetailBo.AddPatientBillDetails(BillDetail);
                    detail.PatientBillId = result.dataValues.Id;
                    detail.PatientBillStatusId = 3;
                    detail.PatientBillDetailId = BillDetailId;
                    await PatientDietOrderDetailBo.Update(detail);
                    // }
                })(DetailItem);
            }));
            // await PatientBillDetailBo.ManagePatientBillDetails(result.dataValues.Id, PatientBillDetails);
            response = result.dataValues.Id;
            //  }
            let PatientSavedBill = await this.GetPatientBillsById({ Id: result.dataValues.Id });
            PatientSavedBill.BillAmount = billingAmount;
            PatientSavedBill.BillDiscount = billDiscountAmount;
            await this.Update(PatientSavedBill);
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: result.dataValues.Id }]
            };
            let PatientBillDetailData = await PatientBillDetailBo.GetPatientBillDetails(apiReq);
            let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let IsPatientTransfer = false;
            await billSummBo.ManagePatBillSummary(IsPatientTransfer, patientDietOrder.Encounter.Id, PatientBillDetailData.Data);
        }
        return response;
    }

    public async CancelEncIPBills(req: BaseRequest): Promise<Boolean> {
        let ipbills = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Data.Id },
            { Key: PatientBillsFilters.BillType, Value: 3 }]
        };
        let data = await this.GetPatientBills(apiReq);
        ipbills = data.Data || [];
        if (ipbills.length > 0) {
            await this.ManageCancelledIPs(req.Id, ipbills);
        }

        return true;
    }

    public async ManageCancelledIPs(EncounterId: number, details: PatientBillsAttributes[]):
        Promise<boolean> {
        details = details || [];
        await Promise.all(details.map((DetailItem): Promise<void> => {
            return (async (detail): Promise<void> => {
                detail.Id = detail.Id || 0;
                detail.PatientBillStatusId = 2;
                if (detail.Status === 2 && detail.Id !== 0) {
                    await this.MarkAsDelete(detail.Id);
                } else if (detail.Id > 0) {
                    await this.Update(detail);
                }
            })(DetailItem);
        }));
        return true;
    }

    public async GetPatientBillsById(req: BaseRequest): Promise<PatientBillsAttributes> {
        let include: Array<IncludeOptions> = [];
        include.push({
            model: this.Models.PatientBillDetails, attributes: ['Id', 'PatientBillStatusId'],
            required: false,
        });
        include.push({
            model: this.Models.User, as: 'BillGeneratedUser', attributes: ['FirstName', 'LastName'], required: false,
            include: [this.GetReference('Title')]
        });
        let result = await this.GetById(req.Id, { include: include });
        return this.GetAttribute(result);
    }

    public async GetPatientBillsByEncounterId(req: BaseRequest): Promise<number> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: 50, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id }]
        };
        let responseData = await this.GetMinPatientBills(apiReq);
        if (responseData.Data.length > 0) {
            let data = responseData.Data[0];
            return data.Id;
        } else {
            return 0;
        }

    }

    public async GetPatientBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        let detailswhere: WhereOptions<any> = {};
        let isPatientDetailsRequired: any = false;
        let isPatientPaymentDetailsRequired: any = false;
        let paymentwhere: WhereOptions<any> = {};
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let admissionWhere: WhereOptions<any> = {};
        let isadmissionRequired: any = false;
        let issortbyoutstanding = false;
        let issortbydiscount = false;
        let storemasterId: -1;
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, required: false,
        });
        include.push({
            model: this.Models.StoreMaster, required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('BillType'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        include.push(this.GetReference('PatientType'));
        include.push(this.GetReference('PharmacySaleType'));
        include.push(this.GetReference('CancelReqRaisedStatus'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender'),
                { model: this.Models.Referral, required: false },
                { model: this.Models.AppointmentDisplay, attributes: ['TokenNo'], required: false }
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], as: 'Doctor', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false,
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['Id', 'GuarantorId', 'GuarantorName',
        //         'GuarantorTypeId', 'TpaId', 'GuarantorLetterNo', 'EmployeeId'], required: false,
        // });
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorDue', attributes: ['Code', 'GuarantorName'], required: false,
        });
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorMaster', attributes: ['Code', 'GuarantorName'], required: false,
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName', 'SignPath', 'UserName'],
            as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'DiscountApprovedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        // include.push({ model: this.Models.Encounter,
        //attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        // (where['BillDateTime'] as any)['$lte'] = param.Value + ' 23:59:59';
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        detailswhere['DoctorShare'] = { '$gt': param.Value };
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.ScheduleTypeId:
                        detailswhere['ScheduleTypeId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.From:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$gte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.To:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$lte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.AdmissionDate:
                        admissionWhere['AdmissionDate'] = { '$between': param.Value || '' };
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.OTRegisterId:
                        detailswhere['OTRegisterId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsRegCumBill:
                        where['IsRegCumBill'] = param.Value;
                        break;
                    case PatientBillsFilters.CreatedBy:
                        where['CreatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.PaymentTypeId:
                        paymentwhere['PaymentTypeId'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsBillDiscount:
                        where['BillDiscount'] = { '$gt': '0' };
                        issortbydiscount = true;
                        break;
                    case PatientBillsFilters.IsOutStandingDesc:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        issortbyoutstanding = true;
                        break;
                    case PatientBillsFilters.VisitIdentifier:
                        (admissionWhere as any)['$or'] = [{ 'VisitIdentifier': { '$like': '%' + (param.Value || '') + '%' } }];
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.IsFromWard:
                        where['IsFromWard'] = param.Value;
                        break;
                    case PatientBillsFilters.SurgeryentryId:
                        where['OTRegisterId'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.Staff:
                        where['Staff'] = param.Value;
                        break;
                    case PatientBillsFilters.Name:
                        (where as any)['$or'] = [{ 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.StaffId:
                        where['StaffId'] = param.Value;
                        break;
                    case PatientBillsFilters.UpdatedBy:
                        where['UpdatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsModified:
                        where['IsModified'] = param.Value;
                        break;
                    case PatientBillsFilters.IsFromOT:
                        where['IsFromOT'] = param.Value;
                        break;
                    case PatientBillsFilters.BillGuarantorId:
                        where['GuarantorId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsClinicalBills:
                        where['IsClinicalBills'] = param.Value;
                        break;
                    case PatientBillsFilters.NotinReferralId:
                        patientQryJoin['where'] = { 'ReferralId': { '$notIn': param.Value } };
                        patientQryJoin['required'] = true;
                        // where['ReferralId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.IsOpticalBill:
                        where['IsOpticalBill'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.RecFromDate:
                        paymentwhere['ReceiptDateTime'] = paymentwhere['ReceiptDateTime'] || {};
                        (paymentwhere['ReceiptDateTime'] as any)['$gte'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.RecToDate:
                        paymentwhere['ReceiptDateTime'] = paymentwhere['ReceiptDateTime'] || {};
                        (paymentwhere['ReceiptDateTime'] as any)['$lte'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.PatientId:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientTypeId:
                        where['PatientTypeId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientPaymentDetails,
            required: isPatientPaymentDetailsRequired,
            where: paymentwhere,
            include: [
                this.GetReference('ReceiptStatus'),
                this.GetReference('PaymentType'),
                {
                    model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName',
                        'SignPath', 'UserName'], as: 'CreatedUser', required: false,
                    include: [
                        this.GetReference('Title')
                    ]
                }
            ]
        });
        include.push({
            model: this.Models.PatientBillDetails,
            required: isPatientDetailsRequired,
            where: detailswhere,
            include: [

                { model: this.Models.ServiceItem, required: false },
                { model: this.Models.ServiceCategory, required: false, attributes: ['ServiceCategoryCode', 'ServiceCategoryName'] },
                {
                    model: this.Models.ItemMaster,
                    required: false,
                    include: [
                        { model: this.Models.DrugMaster, required: false },
                        {
                            model: this.Models.StockItem,
                            required: false,
                            attributes: ['Id', 'StoreMasterId', 'ItemMasterId', 'ItemCode', 'ItemName', 'Quantity', 'Rev'],
                            where: { 'StoreMasterId': storemasterId },
                            include: [
                                {
                                    model: this.Models.StockSerialItem,
                                    required: false,
                                    attributes: ['Id', 'StockItemId', 'StoreMasterId', 'ItemMasterId', 'BatchId',
                                        'Quantity', 'ExpiryDate', 'Ucp', 'Mrp', 'GstId', 'GstPercentage', 'InGstId',
                                        'InGstPercentage', 'CGstId', 'CGstPercentage', 'SGstId', 'SGstPercentage', 'Rev'],
                                    where: { 'Quantity': { $gt: 0 } }
                                }
                            ]
                        }
                    ]
                }]
        });
        include.push({
            model: this.Models.PatientRefund,
            required: false,
            attributes: ['Id', 'RefundDateTime', 'RefundIdentifier', 'RefundAmount', 'PatientBillId', 'PaymentTypeId'],
            include: [
                this.GetReference('PaymentType')]
        });
        include.push({
            model: this.Models.PatientReturns,
            required: false,
            attributes: ['Id', 'PatientBillId', 'ReturnAmount', 'GrossAmount', 'RefundedAmount', 'RoundOffValue'],
            // include: [
            //     this.GetReference('PaymentType')]
        });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.PatientGuarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        // include.push({
        //     model: this.Models.Encounter,
        //     attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
        //     include: [
        //         this.GetReference('AdmissionStatus')
        //     ],
        //     required: isadmissionRequired,
        //     where: admissionWhere
        // });
        include.push({
            model: this.Models.Encounter,
            attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
            include: [{ model: this.Models.EncounterGuarantor, required: false },
            this.GetReference('AdmissionStatus')],
            required: isadmissionRequired,
            where: admissionWhere
        });

        // if (!issortbydiscount && !issortbyoutstanding)
        //     order.push(['BillDateTime', 'DESC']);

        // if (issortbydiscount)
        //     order.push(['BillDiscount', 'DESC']);

        // if (issortbyoutstanding)
        //     order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }
    public async GetPatientBillsemrbillingservice(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        let issortbyoutstanding = false;
        let issortbydiscount = false;
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], as: 'Doctor', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push(this.GetReference('PatientBillStatus'));
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        // (where['BillDateTime'] as any)['$lte'] = param.Value + ' 23:59:59';
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.IsRegCumBill:
                        where['IsRegCumBill'] = param.Value;
                        break;
                    case PatientBillsFilters.CreatedBy:
                        where['CreatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsBillDiscount:
                        where['BillDiscount'] = { '$gt': '0' };
                        issortbydiscount = true;
                        break;
                    case PatientBillsFilters.IsOutStandingDesc:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        issortbyoutstanding = true;
                        break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.IsFromWard:
                        where['IsFromWard'] = param.Value;
                        break;
                    case PatientBillsFilters.SurgeryentryId:
                        where['OTRegisterId'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.Staff:
                        where['Staff'] = param.Value;
                        break;
                    case PatientBillsFilters.Name:
                        (where as any)['$or'] = [{ 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.StaffId:
                        where['StaffId'] = param.Value;
                        break;
                    case PatientBillsFilters.UpdatedBy:
                        where['UpdatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsModified:
                        where['IsModified'] = param.Value;
                        break;
                    case PatientBillsFilters.IsFromOT:
                        where['IsFromOT'] = param.Value;
                        break;
                    case PatientBillsFilters.BillGuarantorId:
                        where['GuarantorId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsClinicalBills:
                        where['IsClinicalBills'] = param.Value;
                        break;
                    case PatientBillsFilters.IsOpticalBill:
                        where['IsOpticalBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientId:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientTypeId:
                        where['PatientTypeId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        if (!issortbydiscount && !issortbyoutstanding)
            order.push(['BillDateTime', 'DESC']);

        if (issortbydiscount)
            order.push(['BillDiscount', 'DESC']);

        if (issortbyoutstanding)
            order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }
    public async GetPatientBillsforInsuranceupdate(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let issortbyoutstanding = false;
        let issortbydiscount = false;
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, required: false,
        });
        include.push({
            model: this.Models.StoreMaster, required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('BillType'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        include.push(this.GetReference('PatientType'));
        include.push(this.GetReference('PharmacySaleType'));
        include.push(this.GetReference('CancelReqRaisedStatus'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender'),
                { model: this.Models.Referral, required: false },
                { model: this.Models.AppointmentDisplay, attributes: ['TokenNo'], required: false }
            ],
        };
        include.push({
            model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false,
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['Id', 'GuarantorId', 'GuarantorName',
        //         'GuarantorTypeId', 'TpaId', 'GuarantorLetterNo', 'EmployeeId'], required: false,
        // });
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorDue', attributes: ['Code', 'GuarantorName'], required: false,
        });
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorMaster', attributes: ['Code', 'GuarantorName'], required: false,
        });
        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        // (where['BillDateTime'] as any)['$lte'] = param.Value + ' 23:59:59';
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.IsRegCumBill:
                        where['IsRegCumBill'] = param.Value;
                        break;
                    case PatientBillsFilters.CreatedBy:
                        where['CreatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsBillDiscount:
                        where['BillDiscount'] = { '$gt': '0' };
                        issortbydiscount = true;
                        break;
                    case PatientBillsFilters.IsOutStandingDesc:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        issortbyoutstanding = true;
                        break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.IsFromWard:
                        where['IsFromWard'] = param.Value;
                        break;
                    case PatientBillsFilters.SurgeryentryId:
                        where['OTRegisterId'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.Staff:
                        where['Staff'] = param.Value;
                        break;
                    case PatientBillsFilters.Name:
                        (where as any)['$or'] = [{ 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.StaffId:
                        where['StaffId'] = param.Value;
                        break;
                    case PatientBillsFilters.UpdatedBy:
                        where['UpdatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsModified:
                        where['IsModified'] = param.Value;
                        break;
                    case PatientBillsFilters.IsFromOT:
                        where['IsFromOT'] = param.Value;
                        break;
                    case PatientBillsFilters.BillGuarantorId:
                        where['GuarantorId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsClinicalBills:
                        where['IsClinicalBills'] = param.Value;
                        break;
                    case PatientBillsFilters.NotinReferralId:
                        patientQryJoin['where'] = { 'ReferralId': { '$notIn': param.Value } };
                        patientQryJoin['required'] = true;
                        // where['ReferralId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.IsOpticalBill:
                        where['IsOpticalBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientId:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientTypeId:
                        where['PatientTypeId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientGuarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        if (!issortbydiscount && !issortbyoutstanding)
            order.push(['BillDateTime', 'DESC']);

        if (issortbydiscount)
            order.push(['BillDiscount', 'DESC']);

        if (issortbyoutstanding)
            order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }
    public async GetPatientBillsforprint(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        let detailswhere: WhereOptions<any> = {};
        let isPatientDetailsRequired: any = false;
        let isPatientPaymentDetailsRequired: any = false;
        let paymentwhere: WhereOptions<any> = {};
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let admissionWhere: WhereOptions<any> = {};
        let isadmissionRequired: any = false;
        let issortbyoutstanding = false;
        let issortbydiscount = false;
        let storemasterId: -1;
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, required: false,
        });
        include.push({
            model: this.Models.StoreMaster, required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('BillType'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        include.push(this.GetReference('PatientType'));
        include.push(this.GetReference('PharmacySaleType'));
        include.push(this.GetReference('CancelReqRaisedStatus'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender'),
                { model: this.Models.Referral, required: false },
                { model: this.Models.AppointmentDisplay, attributes: ['TokenNo'], required: false }
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], as: 'Doctor', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false,
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['Id', 'GuarantorId', 'GuarantorName',
        //         'GuarantorTypeId', 'TpaId', 'GuarantorLetterNo', 'EmployeeId'], required: false,
        // });
        // include.push({
        //     model: this.Models.Guarantor, as: 'GuarantorDue', attributes: ['Code', 'GuarantorName'], required: false,
        // });
        // include.push({
        //     model: this.Models.Guarantor, as: 'GuarantorMaster', attributes: ['Code', 'GuarantorName'], required: false,
        // });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName', 'SignPath', 'UserName'],
            as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'DiscountApprovedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        // include.push({ model: this.Models.Encounter,
        //attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        // (where['BillDateTime'] as any)['$lte'] = param.Value + ' 23:59:59';
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        detailswhere['DoctorShare'] = { '$gt': param.Value };
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.ScheduleTypeId:
                        detailswhere['ScheduleTypeId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.From:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$gte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.To:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$lte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.AdmissionDate:
                        admissionWhere['AdmissionDate'] = { '$between': param.Value || '' };
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.OTRegisterId:
                        detailswhere['OTRegisterId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsRegCumBill:
                        where['IsRegCumBill'] = param.Value;
                        break;
                    case PatientBillsFilters.CreatedBy:
                        where['CreatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.PaymentTypeId:
                        paymentwhere['PaymentTypeId'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsBillDiscount:
                        where['BillDiscount'] = { '$gt': '0' };
                        issortbydiscount = true;
                        break;
                    case PatientBillsFilters.IsOutStandingDesc:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        issortbyoutstanding = true;
                        break;
                    case PatientBillsFilters.VisitIdentifier:
                        (admissionWhere as any)['$or'] = [{ 'VisitIdentifier': { '$like': '%' + (param.Value || '') + '%' } }];
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.IsFromWard:
                        where['IsFromWard'] = param.Value;
                        break;
                    case PatientBillsFilters.SurgeryentryId:
                        where['OTRegisterId'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.Staff:
                        where['Staff'] = param.Value;
                        break;
                    case PatientBillsFilters.Name:
                        (where as any)['$or'] = [{ 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.StaffId:
                        where['StaffId'] = param.Value;
                        break;
                    case PatientBillsFilters.UpdatedBy:
                        where['UpdatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsModified:
                        where['IsModified'] = param.Value;
                        break;
                    case PatientBillsFilters.IsFromOT:
                        where['IsFromOT'] = param.Value;
                        break;
                    case PatientBillsFilters.BillGuarantorId:
                        where['GuarantorId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsClinicalBills:
                        where['IsClinicalBills'] = param.Value;
                        break;
                    case PatientBillsFilters.NotinReferralId:
                        patientQryJoin['where'] = { 'ReferralId': { '$notIn': param.Value } };
                        patientQryJoin['required'] = true;
                        // where['ReferralId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.IsOpticalBill:
                        where['IsOpticalBill'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.RecFromDate:
                        paymentwhere['ReceiptDateTime'] = paymentwhere['ReceiptDateTime'] || {};
                        (paymentwhere['ReceiptDateTime'] as any)['$gte'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.RecToDate:
                        paymentwhere['ReceiptDateTime'] = paymentwhere['ReceiptDateTime'] || {};
                        (paymentwhere['ReceiptDateTime'] as any)['$lte'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.PatientId:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientTypeId:
                        where['PatientTypeId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientPaymentDetails,
            required: isPatientPaymentDetailsRequired,
            where: paymentwhere,
            include: [
                this.GetReference('ReceiptStatus'),
                this.GetReference('PaymentType'),
                {
                    model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName',
                        'SignPath', 'UserName'], as: 'CreatedUser', required: false,
                    include: [
                        this.GetReference('Title')
                    ]
                }
            ]
        });
        include.push({
            model: this.Models.PatientBillDetails,
            required: isPatientDetailsRequired,
            where: detailswhere,
            include: [

                { model: this.Models.ServiceItem, required: false },
                { model: this.Models.ServiceCategory, required: false, attributes: ['ServiceCategoryCode', 'ServiceCategoryName'] },
                {
                    model: this.Models.ItemMaster,
                    required: false,
                    include: [
                        { model: this.Models.DrugMaster, required: false },
                        {
                            model: this.Models.StockItem,
                            required: false,
                            attributes: ['Id', 'StoreMasterId', 'ItemMasterId', 'ItemCode', 'ItemName', 'Quantity', 'Rev'],
                            where: { 'StoreMasterId': storemasterId },
                            include: [
                                {
                                    model: this.Models.StockSerialItem,
                                    required: false,
                                    attributes: ['Id', 'StockItemId', 'StoreMasterId', 'ItemMasterId', 'BatchId',
                                        'Quantity', 'ExpiryDate', 'Ucp', 'Mrp', 'GstId', 'GstPercentage', 'InGstId',
                                        'InGstPercentage', 'CGstId', 'CGstPercentage', 'SGstId', 'SGstPercentage', 'Rev'],
                                    where: { 'Quantity': { $gt: 0 } }
                                }
                            ]
                        }
                    ]
                }]
        });
        include.push({
            model: this.Models.PatientRefund,
            required: false,
            attributes: ['Id', 'RefundDateTime', 'RefundIdentifier', 'RefundAmount', 'PatientBillId', 'PaymentTypeId'],
            include: [
                this.GetReference('PaymentType')]
        });
        include.push({
            model: this.Models.PatientReturns,
            required: false,
            attributes: ['Id', 'PatientBillId', 'ReturnAmount', 'GrossAmount', 'RefundedAmount', 'RoundOffValue'],
            // include: [
            //     this.GetReference('PaymentType')]
        });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.PatientGuarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        // include.push({
        //     model: this.Models.Encounter,
        //     attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
        //     include: [
        //         this.GetReference('AdmissionStatus')
        //     ],
        //     required: isadmissionRequired,
        //     where: admissionWhere
        // });
        // include.push({
        //     model: this.Models.Encounter,
        //     attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
        //     include: [{ model: this.Models.EncounterGuarantor, required: false },
        //     this.GetReference('AdmissionStatus')],
        //     required: isadmissionRequired,
        //     where: admissionWhere
        // });

        if (!issortbydiscount && !issortbyoutstanding)
            order.push(['BillDateTime', 'DESC']);

        if (issortbydiscount)
            order.push(['BillDiscount', 'DESC']);

        if (issortbyoutstanding)
            order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }
    public async GetPharmacyPatientBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        let detailswhere: WhereOptions<any> = {};
        let isPatientDetailsRequired: any = false;
        let isPatientPaymentDetailsRequired: any = false;
        let paymentwhere: WhereOptions<any> = {};
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let admissionWhere: WhereOptions<any> = {};
        let isadmissionRequired: any = false;
        let issortbyoutstanding = false;
        let issortbydiscount = false;
        let storemasterId: -1;
        // include.push({
        //     model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        // });
        include.push({
            model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false,
        });
        include.push({
            model: this.Models.Facility, required: false,
        });
        include.push({
            model: this.Models.StoreMaster, required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('BillType'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        include.push(this.GetReference('PatientType'));
        include.push(this.GetReference('PharmacySaleType'));
        include.push(this.GetReference('CancelReqRaisedStatus'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN',],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender'),
            ],
        };

        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], as: 'Doctor', required: false,
            include: [
                this.GetReference('Title')
            ]
        });

        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName', 'SignPath', 'UserName'],
            as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName', 'SignPath', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        detailswhere['DoctorShare'] = { '$gt': param.Value };
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.ScheduleTypeId:
                        detailswhere['ScheduleTypeId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.From:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$gte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.To:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$lte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.AdmissionDate:
                        admissionWhere['AdmissionDate'] = { '$between': param.Value || '' };
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.OTRegisterId:
                        detailswhere['OTRegisterId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsRegCumBill:
                        where['IsRegCumBill'] = param.Value;
                        break;
                    case PatientBillsFilters.CreatedBy:
                        where['CreatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.PaymentTypeId:
                        paymentwhere['PaymentTypeId'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsBillDiscount:
                        where['BillDiscount'] = { '$gt': '0' };
                        issortbydiscount = true;
                        break;
                    case PatientBillsFilters.IsOutStandingDesc:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        issortbyoutstanding = true;
                        break;
                    case PatientBillsFilters.VisitIdentifier:
                        (admissionWhere as any)['$or'] = [{ 'VisitIdentifier': { '$like': '%' + (param.Value || '') + '%' } }];
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.IsFromWard:
                        where['IsFromWard'] = param.Value;
                        break;
                    case PatientBillsFilters.SurgeryentryId:
                        where['OTRegisterId'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.Staff:
                        where['Staff'] = param.Value;
                        break;
                    case PatientBillsFilters.Name:
                        (where as any)['$or'] = [{ 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.StaffId:
                        where['StaffId'] = param.Value;
                        break;
                    case PatientBillsFilters.UpdatedBy:
                        where['UpdatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsModified:
                        where['IsModified'] = param.Value;
                        break;
                    case PatientBillsFilters.IsFromOT:
                        where['IsFromOT'] = param.Value;
                        break;
                    case PatientBillsFilters.BillGuarantorId:
                        where['GuarantorId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsClinicalBills:
                        where['IsClinicalBills'] = param.Value;
                        break;
                    case PatientBillsFilters.NotinReferralId:
                        patientQryJoin['where'] = { 'ReferralId': { '$notIn': param.Value } };
                        patientQryJoin['required'] = true;
                        // where['ReferralId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.IsOpticalBill:
                        where['IsOpticalBill'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.RecFromDate:
                        paymentwhere['ReceiptDateTime'] = paymentwhere['ReceiptDateTime'] || {};
                        (paymentwhere['ReceiptDateTime'] as any)['$gte'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.RecToDate:
                        paymentwhere['ReceiptDateTime'] = paymentwhere['ReceiptDateTime'] || {};
                        (paymentwhere['ReceiptDateTime'] as any)['$lte'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.PatientId:
                        where['PatientId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientPaymentDetails,
            required: isPatientPaymentDetailsRequired,
            where: paymentwhere,
            include: [
                this.GetReference('ReceiptStatus'),
                this.GetReference('PaymentType'),
                {
                    model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName',
                        'SignPath'], as: 'CreatedUser', required: false,
                    include: [
                        this.GetReference('Title')
                    ]
                }
            ]
        });
        include.push({
            model: this.Models.Encounter,
            attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
            include: [{ model: this.Models.EncounterGuarantor, required: false },
            this.GetReference('AdmissionStatus')],
            required: isadmissionRequired,
            where: admissionWhere
        });

        if (!issortbydiscount && !issortbyoutstanding)
            order.push(['BillDateTime', 'DESC']);

        if (issortbydiscount)
            order.push(['BillDiscount', 'DESC']);

        if (issortbyoutstanding)
            order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }
    public async checkBillFinalized(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];

        // include.push({
        //     model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        // });
        // include.push({
        //     model: this.Models.Facility, required: false,
        // });
        // include.push({
        //     model: this.Models.StoreMaster, required: false,
        // });

        // include.push(this.GetReference('BillPriority'));
        // include.push(this.GetReference('BillType'));
        // include.push(this.GetReference('PatientBillStatus'));
        // include.push(this.GetReference('GuarantorType'));
        // include.push(this.GetReference('Title'));
        // include.push(this.GetReference('Gender'));
        // include.push(this.GetReference('ChecklistStatus'));
        // include.push(this.GetReference('EncounterType'));
        // include.push(this.GetReference('PatientType'));
        // include.push(this.GetReference('PharmacySaleType'));
        // include.push(this.GetReference('CancelReqRaisedStatus'));
        // let patientQryJoin: any = {
        //     model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
        //         'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
        //         'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'],
        //     required: false,
        //     include: [
        //         this.GetReference('Title'), this.GetReference('Gender'),
        //         { model: this.Models.Referral, required: false },
        //         { model: this.Models.AppointmentDisplay, attributes: ['TokenNo'], required: false }
        //     ],
        // };
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
        //         'LicenseNo'], required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
        //         'LicenseNo'], as: 'Doctor', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'PrivateDue', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false,
        // });
        // // include.push({
        // //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['Id', 'GuarantorId', 'GuarantorName',
        // //         'GuarantorTypeId', 'TpaId', 'GuarantorLetterNo', 'EmployeeId'], required: false,
        // // });
        // include.push({
        //     model: this.Models.Guarantor, as: 'GuarantorDue', attributes: ['Code', 'GuarantorName'], required: false,
        // });
        // include.push({
        //     model: this.Models.Guarantor, as: 'GuarantorMaster', attributes: ['Code', 'GuarantorName'], required: false,
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName', 'SignPath'], as: 'CreatedUser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'Updateduser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'CancelledUser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'DiscountApprovedUser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
        //     include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        // });
        // include.push({
        //     model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
        //     include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        // });
        // include.push({
        //     model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        // });
        // include.push({ model: this.Models.Encounter,
        //attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    // case PatientBillsFilters.Guarantor:
                    //     guarantorWhere['GuarantorId'] = param.Value;
                    //     isGuarantorRequired = true;
                    //     break;
                    // case PatientBillsFilters.MultiGuarantorType:
                    //     guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                    //     isGuarantorRequired = true;
                    //     break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientId:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });

        // if (issortbyoutstanding)
        order.push(['OutStandingAmount', 'DESC']);
        apiReq.Attributes = ['Id', 'EncounterId', 'ToBeRefunded', 'PatientId', 'OutStandingAmount', 'PatientDue', 'GuarantorTypeId'];
        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }
    public async GetPatientTaxableBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        let detailswhere: WhereOptions<any> = {};
        let isPatientDetailsRequired: any = false;
        let isPatientPaymentDetailsRequired: any = false;
        let paymentwhere: WhereOptions<any> = {};
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let admissionWhere: WhereOptions<any> = {};
        let isadmissionRequired: any = false;
        let issortbyoutstanding = false;
        let issortbydiscount = false;
        let storemasterId: -1;
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, required: false,
        });
        include.push({
            model: this.Models.StoreMaster, required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('BillType'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        include.push(this.GetReference('PatientType'));
        include.push(this.GetReference('PharmacySaleType'));
        include.push(this.GetReference('CancelReqRaisedStatus'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender'),
                { model: this.Models.Referral, required: false },
                { model: this.Models.AppointmentDisplay, attributes: ['TokenNo'], required: false }
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], as: 'Doctor', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'PrivateDue', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false,
        // });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['Id', 'GuarantorId', 'GuarantorName',
        //         'GuarantorTypeId', 'TpaId', 'GuarantorLetterNo', 'EmployeeId'], required: false,
        // });
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorDue', attributes: ['Code', 'GuarantorName'], required: false,
        });
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorMaster', attributes: ['Code', 'GuarantorName'], required: false,
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName', 'SignPath', 'UserName'],
            as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'Updateduser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'CancelledUser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'DiscountApprovedUser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
        //     include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        // });
        // include.push({
        //     model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
        //     include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        // });
        // include.push({
        //     model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        // });
        // include.push({ model: this.Models.Encounter,
        //attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsTaxable:
                        detailswhere['GSTAmount'] = detailswhere['GSTAmount'] || {};
                        (detailswhere['GSTAmount'] as any)['$gt'] = 0;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        detailswhere['DoctorShare'] = { '$gt': param.Value };
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.ScheduleTypeId:
                        detailswhere['ScheduleTypeId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.From:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$gte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.To:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$lte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.AdmissionDate:
                        admissionWhere['AdmissionDate'] = { '$between': param.Value || '' };
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.OTRegisterId:
                        detailswhere['OTRegisterId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsRegCumBill:
                        where['IsRegCumBill'] = param.Value;
                        break;
                    case PatientBillsFilters.CreatedBy:
                        where['CreatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.PaymentTypeId:
                        paymentwhere['PaymentTypeId'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsBillDiscount:
                        where['BillDiscount'] = { '$gt': '0' };
                        issortbydiscount = true;
                        break;
                    case PatientBillsFilters.IsOutStandingDesc:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        issortbyoutstanding = true;
                        break;
                    case PatientBillsFilters.IsGuarantorRequired:
                        where['GuarantorId'] = { '$gt': '1' };
                        //isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.VisitIdentifier:
                        (admissionWhere as any)['$or'] = [{ 'VisitIdentifier': { '$like': '%' + (param.Value || '') + '%' } }];
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.IsFromWard:
                        where['IsFromWard'] = param.Value;
                        break;
                    case PatientBillsFilters.SurgeryentryId:
                        where['OTRegisterId'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.Staff:
                        where['Staff'] = param.Value;
                        break;
                    case PatientBillsFilters.Name:
                        (where as any)['$or'] = [{ 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.StaffId:
                        where['StaffId'] = param.Value;
                        break;
                    case PatientBillsFilters.UpdatedBy:
                        where['UpdatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsModified:
                        where['IsModified'] = param.Value;
                        break;
                    case PatientBillsFilters.IsFromOT:
                        where['IsFromOT'] = param.Value;
                        break;
                    case PatientBillsFilters.BillGuarantorId:
                        where['GuarantorId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsClinicalBills:
                        where['IsClinicalBills'] = param.Value;
                        break;
                    case PatientBillsFilters.NotinReferralId:
                        patientQryJoin['where'] = { 'ReferralId': { '$notIn': param.Value } };
                        patientQryJoin['required'] = true;
                        // where['ReferralId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.IsOpticalBill:
                        where['IsOpticalBill'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.RecFromDate:
                        paymentwhere['ReceiptDateTime'] = paymentwhere['ReceiptDateTime'] || {};
                        (paymentwhere['ReceiptDateTime'] as any)['$gte'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.RecToDate:
                        paymentwhere['ReceiptDateTime'] = paymentwhere['ReceiptDateTime'] || {};
                        (paymentwhere['ReceiptDateTime'] as any)['$lte'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.PatientId:
                        where['PatientId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientPaymentDetails,
            required: isPatientPaymentDetailsRequired,
            where: paymentwhere,
            include: [
                this.GetReference('ReceiptStatus'),
                {
                    model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName',
                        'SignPath'], as: 'CreatedUser', required: false,
                    include: [
                        this.GetReference('Title')
                    ]
                }
            ]
        });
        include.push({
            model: this.Models.PatientBillDetails,
            required: isPatientDetailsRequired,
            where: detailswhere,
            include: [
                { model: this.Models.ServiceItem, required: false },
                { model: this.Models.ServiceCategory, required: false, attributes: ['ServiceCategoryCode', 'ServiceCategoryName'] },
                {
                    model: this.Models.ItemMaster,
                    required: false,
                    include: [
                        { model: this.Models.DrugMaster, required: false },
                        {
                            model: this.Models.StockItem,
                            required: false,
                            attributes: ['Id', 'StoreMasterId', 'ItemMasterId', 'ItemCode', 'ItemName', 'Quantity', 'Rev'],
                            where: { 'StoreMasterId': storemasterId },
                            include: [
                                {
                                    model: this.Models.StockSerialItem,
                                    required: false,
                                    attributes: ['Id', 'StockItemId', 'StoreMasterId', 'ItemMasterId', 'BatchId',
                                        'Quantity', 'ExpiryDate', 'Ucp', 'Mrp', 'GstId', 'GstPercentage', 'InGstId',
                                        'InGstPercentage', 'CGstId', 'CGstPercentage', 'SGstId', 'SGstPercentage', 'Rev'],
                                    where: { 'Quantity': { $gt: 0 } }
                                }
                            ]
                        }
                    ]
                }]
        });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.Guarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            //where: guarantorWhere
        });
        // include.push({
        //     model: this.Models.PatientGuarantor,
        //     as: 'Guarantor',
        //     attributes: ['GuarantorName', 'GuarantorId'],
        //     required: isGuarantorRequired,
        //     //where: guarantorWhere
        // });
        // include.push({
        //     model: this.Models.Encounter,
        //     attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
        //     include: [
        //         this.GetReference('AdmissionStatus')
        //     ],
        //     required: isadmissionRequired,
        //     where: admissionWhere
        // });
        include.push({
            model: this.Models.Encounter,
            attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
            include: [{ model: this.Models.EncounterGuarantor, required: false },
            this.GetReference('AdmissionStatus')],
            required: isadmissionRequired,
            where: admissionWhere
        });

        if (!issortbydiscount && !issortbyoutstanding)
            order.push(['BillDateTime', 'DESC']);

        if (issortbydiscount)
            order.push(['BillDiscount', 'DESC']);

        if (issortbyoutstanding)
            order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }
    public async GetClinicalPatientBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        let detailswhere: WhereOptions<any> = {};
        let isPatientDetailsRequired: any = false;
        let isPatientPaymentDetailsRequired: any = false;
        let paymentwhere: WhereOptions<any> = {};
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let admissionWhere: WhereOptions<any> = {};
        let isadmissionRequired: any = false;
        let issortbyoutstanding = false;
        let issortbydiscount = false;
        let storemasterId: -1;
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, attributes: ['GstNumber'], required: false,
        });
        include.push({
            model: this.Models.StoreMaster, required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender'),
                { model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false },
                { model: this.Models.AppointmentDisplay, attributes: ['TokenNo'], required: false }
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], as: 'Doctor', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['Id', 'GuarantorId', 'GuarantorName',
        //         'GuarantorTypeId', 'TpaId', 'GuarantorLetterNo', 'EmployeeId'], required: false,
        // });
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorDue', attributes: ['Code', 'GuarantorName'], required: false,
        });
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorMaster', attributes: ['Code', 'GuarantorName'], required: false,
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName', 'SignPath'], as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'SignPath', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'DiscountApprovedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        include.push({
            model: this.Models.Appointment, required: false,
        });
        // include.push({ model: this.Models.Encounter,
        //attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        detailswhere['DoctorShare'] = { '$gt': param.Value };
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.ScheduleTypeId:
                        detailswhere['ScheduleTypeId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.From:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$gte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.To:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$lte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.AdmissionDate:
                        admissionWhere['AdmissionDate'] = { '$between': param.Value || '' };
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.OTRegisterId:
                        detailswhere['OTRegisterId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsRegCumBill:
                        where['IsRegCumBill'] = param.Value;
                        break;
                    case PatientBillsFilters.CreatedBy:
                        where['CreatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.PaymentTypeId:
                        paymentwhere['PaymentTypeId'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsBillDiscount:
                        where['BillDiscount'] = { '$gt': '0' };
                        issortbydiscount = true;
                        break;
                    case PatientBillsFilters.IsOutStandingDesc:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        issortbyoutstanding = true;
                        break;
                    case PatientBillsFilters.VisitIdentifier:
                        (admissionWhere as any)['$or'] = [{ 'VisitIdentifier': { '$like': '%' + (param.Value || '') + '%' } }];
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.IsFromWard:
                        where['IsFromWard'] = param.Value;
                        break;
                    case PatientBillsFilters.SurgeryentryId:
                        where['OTRegisterId'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.Staff:
                        where['Staff'] = param.Value;
                        break;
                    case PatientBillsFilters.Name:
                        (where as any)['$or'] = [{ 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.StaffId:
                        where['StaffId'] = param.Value;
                        break;
                    case PatientBillsFilters.UpdatedBy:
                        where['UpdatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.IsModified:
                        where['IsModified'] = param.Value;
                        break;
                    case PatientBillsFilters.IsFromOT:
                        where['IsFromOT'] = param.Value;
                        break;
                    case PatientBillsFilters.BillGuarantorId:
                        where['GuarantorId'] = param.Value;
                        break;
                    case PatientBillsFilters.AppointmentId:
                        where['AppointmentId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsClinicalBills:
                        where['IsClinicalBills'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientPaymentDetails,
            required: isPatientPaymentDetailsRequired,
            where: paymentwhere,
            include: [
                this.GetReference('ReceiptStatus'),
                {
                    model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName',
                        'SignPath'], as: 'CreatedUser', required: false,
                    include: [
                        this.GetReference('Title')
                    ]
                }
            ]
        });
        include.push({
            model: this.Models.PatientBillDetails,
            required: isPatientDetailsRequired,
            where: detailswhere,
            include: [
                { model: this.Models.ServiceCategory, required: false, attributes: ['ServiceCategoryCode', 'ServiceCategoryName'] },
                {
                    model: this.Models.ItemMaster,
                    required: false,
                    include: [
                        { model: this.Models.DrugMaster, required: false },
                        {
                            model: this.Models.StockItem,
                            required: false,
                            attributes: ['Id', 'StoreMasterId', 'ItemMasterId', 'ItemCode', 'ItemName', 'Quantity', 'Rev'],
                            where: { 'StoreMasterId': storemasterId },
                            include: [
                                {
                                    model: this.Models.StockSerialItem,
                                    required: false,
                                    attributes: ['Id', 'StockItemId', 'StoreMasterId', 'ItemMasterId', 'BatchId',
                                        'Quantity', 'ExpiryDate', 'Ucp', 'Mrp', 'GstId', 'GstPercentage', 'InGstId',
                                        'InGstPercentage', 'CGstId', 'CGstPercentage', 'SGstId', 'SGstPercentage', 'Rev'],
                                    where: { 'Quantity': { $gt: 0 } }
                                }
                            ]
                        }
                    ]
                }]
        });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.PatientGuarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        include.push({
            model: this.Models.Encounter,
            attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
            include: [
                this.GetReference('AdmissionStatus')
            ],
            required: isadmissionRequired,
            where: admissionWhere
        });

        if (!issortbydiscount && !issortbyoutstanding)
            order.push(['BillDateTime', 'DESC']);

        if (issortbydiscount)
            order.push(['BillDiscount', 'DESC']);

        if (issortbyoutstanding)
            order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }

    public async GetIPBillPatientBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        // let detailswhere: WhereOptions<any> = {};
        // let isPatientDetailsRequired: any = false;
        // let isPatientPaymentDetailsRequired: any = false;
        // let paymentwhere: WhereOptions<any> = {};
        // let guarantorWhere: WhereOptions<any> = {};
        // let isGuarantorRequired: any = false;
        // let admissionWhere: WhereOptions<any> = {};
        // let isadmissionRequired: any = false;
        // let issortbyoutstanding = false;
        // let issortbydiscount = false;
        // let storemasterId: -1;
        // include.push({
        //     model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        // });
        // include.push({
        //     model: this.Models.Facility, attributes: ['GstNumber'], required: false,
        // });
        // include.push({
        //     model: this.Models.StoreMaster, required: false,
        // });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('PatientBillStatus'));
        // include.push(this.GetReference('GuarantorType'));
        // include.push(this.GetReference('Title'));
        // include.push(this.GetReference('Gender'));
        // include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        // let patientQryJoin: any = {
        //     model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
        //         'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
        //         'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'],
        //     required: false,
        //     include: [
        //         this.GetReference('Title'), this.GetReference('Gender'),
        //         { model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false },
        //         { model: this.Models.AppointmentDisplay, attributes: ['TokenNo'], required: false }
        //     ],
        // };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], as: 'Doctor', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'PrivateDue', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['Id', 'GuarantorId', 'GuarantorName',
        //         'GuarantorTypeId', 'TpaId', 'GuarantorLetterNo', 'EmployeeId'], required: false,
        // });
        // include.push({
        //     model: this.Models.Guarantor, as: 'GuarantorDue', attributes: ['Code', 'GuarantorName'], required: false,
        // });
        // include.push({
        //     model: this.Models.Guarantor, as: 'GuarantorMaster', attributes: ['Code', 'GuarantorName'], required: false,
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CreatedUser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'Updateduser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName'], as: 'CancelledUser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'DiscountApprovedUser', required: false,
        //     include: [
        //         this.GetReference('Title')
        //     ]
        // });
        // include.push({
        //     model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
        //     include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        // });
        // include.push({
        //     model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
        //     include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        // });
        // include.push({
        //     model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        // });
        // include.push({ model: this.Models.Encounter,
        //attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false });

        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    // case PatientBillsFilters.Guarantor:
                    //     guarantorWhere['GuarantorId'] = param.Value;
                    //     isGuarantorRequired = true;
                    //     break;
                    // case PatientBillsFilters.MultiGuarantorType:
                    //     guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                    //     isGuarantorRequired = true;
                    //     break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    // case PatientBillsFilters.MRN:
                    //     patientQryJoin['where'] = {
                    //         '$or': [
                    //             { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                    //             { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                    //             { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                    //         ]
                    //     };
                    //     patientQryJoin['required'] = true;
                    //     break;
                    // case PatientBillsFilters.Mobile:
                    //     patientQryJoin['where'] = { 'Mobile': param.Value };
                    //     patientQryJoin['required'] = true;
                    //     break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    // case PatientBillsFilters.IsDoctorShare:
                    //     detailswhere['DoctorShare'] = { '$gt': param.Value };
                    //     isPatientDetailsRequired = true;
                    //     break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    // case PatientBillsFilters.StoreMasterId:
                    //     storemasterId = param.Value;
                    //     where['StoreMasterId'] = param.Value;
                    //     break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    // case PatientBillsFilters.ScheduleTypeId:
                    //     detailswhere['ScheduleTypeId'] = param.Value;
                    //     isPatientDetailsRequired = true;
                    //     break;
                    // case PatientBillsFilters.From:
                    //     admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                    //     (admissionWhere['AdmissionDate'] as any)['$gte'] = param.Value;
                    //     isadmissionRequired = true;
                    //     break;
                    // case PatientBillsFilters.To:
                    //     admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                    //     (admissionWhere['AdmissionDate'] as any)['$lte'] = param.Value;
                    //     isadmissionRequired = true;
                    //     break;
                    // case PatientBillsFilters.AdmissionDate:
                    //     admissionWhere['AdmissionDate'] = { '$between': param.Value || '' };
                    //     isadmissionRequired = true;
                    //     break;
                    // case PatientBillsFilters.OTRegisterId:
                    //     detailswhere['OTRegisterId'] = param.Value;
                    //     isPatientDetailsRequired = true;
                    //     break;
                    // case PatientBillsFilters.IsRegCumBill:
                    //     where['IsRegCumBill'] = param.Value;
                    //     break;
                    // case PatientBillsFilters.CreatedBy:
                    //     where['CreatedBy'] = param.Value;
                    //     break;
                    // case PatientBillsFilters.PaymentTypeId:
                    //     paymentwhere['PaymentTypeId'] = param.Value;
                    //     isPatientPaymentDetailsRequired = true;
                    //     break;
                    // case PatientBillsFilters.IsBillDiscount:
                    //     where['BillDiscount'] = { '$gt': '0' };
                    //     issortbydiscount = true;
                    //     break;
                    // case PatientBillsFilters.IsOutStandingDesc:
                    //     where['OutStandingAmount'] = { '$gt': '0' };
                    //     issortbyoutstanding = true;
                    //     break;
                    // case PatientBillsFilters.VisitIdentifier:
                    //     admissionWhere['$or'] = [{ 'VisitIdentifier': { '$like': '%' + (param.Value || '') + '%' } }];
                    //     isadmissionRequired = true;
                    //     break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.IsFromWard:
                        where['IsFromWard'] = param.Value;
                        break;
                    case PatientBillsFilters.SurgeryentryId:
                        where['OTRegisterId'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.Staff:
                        where['Staff'] = param.Value;
                        break;
                    case PatientBillsFilters.Name:
                        (where as any)['$or'] = [{ 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.StaffId:
                        where['StaffId'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        // include.push({
        //     model: this.Models.PatientPaymentDetails,
        //     required: isPatientPaymentDetailsRequired,
        //     where: paymentwhere,
        //     include: [
        //         this.GetReference('ReceiptStatus')
        //     ]
        // });
        // include.push({
        //     model: this.Models.PatientBillDetails,
        //     required: isPatientDetailsRequired,
        //     where: detailswhere,
        //     include: [
        //         { model: this.Models.ServiceCategory, required: false, attributes: ['ServiceCategoryCode', 'ServiceCategoryName'] },
        //         {
        //             model: this.Models.ItemMaster,
        //             required: false,
        //             include: [
        //                 { model: this.Models.DrugMaster, required: false },
        //                 {
        //                     model: this.Models.StockItem,
        //                     required: false,
        //                     attributes: ['Id', 'StoreMasterId', 'ItemMasterId', 'ItemCode', 'ItemName', 'Quantity', 'Rev'],
        //                     where: { 'StoreMasterId': storemasterId },
        //                     include: [
        //                         {
        //                             model: this.Models.StockSerialItem,
        //                             required: false,
        //                             attributes: ['Id', 'StockItemId', 'StoreMasterId', 'ItemMasterId', 'BatchId',
        //                                 'Quantity', 'ExpiryDate', 'Ucp', 'Mrp', 'GstId', 'GstPercentage', 'InGstId',
        //                                 'InGstPercentage', 'CGstId', 'CGstPercentage', 'SGstId', 'SGstPercentage', 'Rev'],
        //                             where: { 'Quantity': { $gt: 0 } }
        //                         }
        //                     ]
        //                 }
        //             ]
        //         }]
        // });
        // include.push(patientQryJoin);
        // include.push({
        //     model: this.Models.PatientGuarantor,
        //     as: 'Guarantor',
        //     attributes: ['GuarantorName', 'GuarantorId'],
        //     required: isGuarantorRequired,
        //     where: guarantorWhere
        // });
        // include.push({
        //     model: this.Models.Encounter,
        //     attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate', 'WardId'],
        //     include: [
        //         this.GetReference('AdmissionStatus')
        //     ],
        //     required: isadmissionRequired,
        //     where: admissionWhere
        // });

        // if (!issortbydiscount && !issortbyoutstanding)
        //     order.push(['BillDateTime', 'DESC']);

        // if (issortbydiscount)
        //     order.push(['BillDiscount', 'DESC']);

        // if (issortbyoutstanding)
        //     order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }

    public async GetPatientBillswithoutdetails(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let order: Array<any> = [];
        let include: Array<IncludeOptions> = [];
        let detailswhere: WhereOptions<any> = {};
        let isPatientDetailsRequired: any = false;
        let isPatientPaymentDetailsRequired: any = false;
        let paymentwhere: WhereOptions<any> = {};
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let admissionWhere: WhereOptions<any> = {};
        let isadmissionRequired: any = false;
        let issortbyoutstanding = false;
        let issortbydiscount = false;
        let storemasterId: -1;
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, attributes: ['GstNumber'], required: false,
        });
        include.push({
            model: this.Models.StoreMaster, required: false,
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'DiscountApprovedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        include.push(this.GetReference('DiscountApprovalStatus'));
        include.push(this.GetReference('CreditApprovalStatus'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender'),
                { model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false }
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['Id', 'GuarantorId', 'GuarantorName',
        //         'GuarantorTypeId', 'TpaId', 'GuarantorLetterNo', 'EmployeeId'], required: false,
        // });

        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName', 'SignPath'], as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'Qualification',
                'LicenseNo', 'UserName'], as: 'UserDoctor', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        // include.push({ model: this.Models.Encounter,
        //attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false });



        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.NotOutStanding:
                        where['OutStandingAmount'] = { '$eq': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        detailswhere['DoctorShare'] = { '$gt': param.Value };
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.ScheduleTypeId:
                        detailswhere['ScheduleTypeId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.From:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$gte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.To:
                        admissionWhere['AdmissionDate'] = admissionWhere['AdmissionDate'] || {};
                        (admissionWhere['AdmissionDate'] as any)['$lte'] = param.Value;
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.AdmissionDate:
                        admissionWhere['AdmissionDate'] = { '$between': param.Value || '' };
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.OTRegisterId:
                        detailswhere['OTRegisterId'] = param.Value;
                        isPatientDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsRegCumBill:
                        where['IsRegCumBill'] = param.Value;
                        break;
                    case PatientBillsFilters.CreatedBy:
                        where['CreatedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.PaymentTypeId:
                        paymentwhere['PaymentTypeId'] = param.Value;
                        isPatientPaymentDetailsRequired = true;
                        break;
                    case PatientBillsFilters.IsBillDiscount:
                        where['BillDiscount'] = { '$gt': '0' };
                        issortbydiscount = true;
                        break;
                    case PatientBillsFilters.IsOutStandingDesc:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        issortbyoutstanding = true;
                        break;
                    case PatientBillsFilters.VisitIdentifier:
                        (admissionWhere as any)['$or'] = [{ 'VisitIdentifier': { '$like': '%' + (param.Value || '') + '%' } }];
                        isadmissionRequired = true;
                        break;
                    case PatientBillsFilters.IsPaidFully:
                        where['IsPaidFully'] = param.Value;
                        break;
                    case PatientBillsFilters.BillDiscount:
                        where['BillDiscount'] = { '$gt': param.Value };
                        break;
                    case PatientBillsFilters.DepartmentId:
                        where['DepartmentId'] = param.Value;
                        break;
                    case PatientBillsFilters.DiscountApprovedBy:
                        where['DiscountApprovedBy'] = param.Value;
                        break;
                    case PatientBillsFilters.DiscountApprovalStatus:
                        where['DiscountApprovalStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.CreditApprovalStatus:
                        where['CreditApprovalStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.DiscountApprovalDate:
                        where['DiscountApprovalDate'] = param.Value;
                        break;
                    case PatientBillsFilters.WardId:
                        where['WardId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillGuarantorId:
                        where['GuarantorId'] = param.Value;
                        break;
                    case PatientBillsFilters.GreatrOutStanding:
                        where['OutStandingAmount'] = { '$gt': param.Value };
                        break;
                    default:
                        console.log(param.Key);
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.PatientPaymentDetails,
            required: isPatientPaymentDetailsRequired,
            where: paymentwhere,
            include: [
                this.GetReference('ReceiptStatus'),
                this.GetReference('PaymentType'),
            ]
        });
        // include.push({
        //     model: this.Models.PatientBillDetails,
        //     required: isPatientDetailsRequired,
        //     where: detailswhere,
        //     include: [
        //         { model: this.Models.ServiceCategory, required: false, attributes: ['ServiceCategoryCode', 'ServiceCategoryName'] },
        //         {
        //             model: this.Models.ItemMaster,
        //             required: false,
        //             include: [
        //                 { model: this.Models.DrugMaster, required: false },
        //                 {
        //                     model: this.Models.StockItem,
        //                     required: false,
        //                     attributes: ['Id', 'StoreMasterId', 'ItemMasterId', 'ItemCode', 'ItemName', 'Quantity', 'Rev'],
        //                     where: { 'StoreMasterId': storemasterId },
        //                     include: [
        //                         {
        //                             model: this.Models.StockSerialItem,
        //                             required: false,
        //                             attributes: ['Id', 'StockItemId', 'StoreMasterId', 'ItemMasterId', 'BatchId',
        //                                 'Quantity', 'ExpiryDate', 'Ucp', 'Mrp', 'GstId', 'GstPercentage', 'InGstId',
        //                                 'InGstPercentage', 'CGstId', 'CGstPercentage', 'SGstId', 'SGstPercentage', 'Rev'],
        //                             where: { 'Quantity': { $gt: 0 } }
        //                         }
        //                     ]
        //                 }
        //             ]
        //         }]
        // });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.Guarantor, as: 'GuarantorMaster',
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        // include.push({
        //     model: this.Models.PatientGuarantor,
        //     as: 'Guarantor',
        //     attributes: ['GuarantorName', 'GuarantorId'],
        //     required: isGuarantorRequired,
        //     where: guarantorWhere
        // });
        include.push({
            model: this.Models.Encounter,
            attributes: ['VisitIdentifier', 'AdmissionDate', 'AdmissionStatusId', 'DischargeDate'],
            include: [
                this.GetReference('AdmissionStatus')
            ],
            required: isadmissionRequired,
            where: admissionWhere
        });

        if (!issortbydiscount && !issortbyoutstanding)
            order.push(['BillDateTime', 'DESC']);

        if (issortbydiscount)
            order.push(['BillDiscount', 'DESC']);

        if (issortbyoutstanding)
            order.push(['OutStandingAmount', 'DESC']);

        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes, order: order });
    }

    public async GetFindPatientBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let include: Array<IncludeOptions> = [];
        let guarantorWhere: WhereOptions<any> = {};
        let isGuarantorRequired: any = false;
        let storemasterId: -1;
        include.push({
            model: this.Models.Department, attributes: ['DepartmentName'], required: false,
        });
        include.push({
            model: this.Models.Facility, attributes: ['GstNumber'], required: false,
        });
        include.push({
            model: this.Models.StoreMaster, attributes: ['LicenseNo', 'TinNo'], required: false,
        });

        include.push(this.GetReference('BillPriority'));
        include.push(this.GetReference('PatientBillStatus'));
        include.push(this.GetReference('GuarantorType'));
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        include.push(this.GetReference('ChecklistStatus'));
        include.push(this.GetReference('EncounterType'));
        let patientQryJoin: any = {
            model: this.Models.Patient, attributes: ['FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier'],
            required: false,
            include: [
                this.GetReference('Title'), this.GetReference('Gender')
            ],
        };
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'LicenseNo', 'UserName'], required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'PrivateDue', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        // include.push({
        //     model: this.Models.PatientGuarantor, as: 'PatientGuarantor', attributes: ['GuarantorName'], required: false,
        // });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'SignPath', 'UserName'], as: 'CreatedUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'Updateduser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'UserName'], as: 'CancelledUser', required: false,
            include: [
                this.GetReference('Title')
            ]
        });
        include.push({
            model: this.Models.WardRoomMaster, attributes: ['RoomNo'], required: false,
            include: [{ model: this.Models.RoomTypeMaster, attributes: ['RoomTypeName'], required: false }]
        });
        include.push({
            model: this.Models.WardRoomBedMaster, attributes: ['BedNo', 'Description', 'ServiceRateCategoryId'], required: false,
            include: [{ model: this.Models.WardRoomMaster, attributes: ['Id', 'RoomNo', 'Description'], required: false }]
        });
        include.push({
            model: this.Models.WardMaster, as: 'WardMaster', attributes: ['WardName'], required: false,
        });
        include.push({ model: this.Models.Encounter, attributes: ['VisitIdentifier', 'AdmissionDate', 'DischargeDate'], required: false });
        include.push({
            model: this.Models.Referral, attributes: ['ReferralCode', 'ReferralName'], required: false,
        });
        include.push({ model: this.Models.PatientPaymentDetails, required: false });
        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.FromDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$gte'] = param.Value;
                        break;
                    case PatientBillsFilters.ToDate:
                        where['BillDateTime'] = where['BillDateTime'] || {};
                        (where['BillDateTime'] as any)['$lte'] = param.Value;
                        break;
                    case PatientBillsFilters.PatBillDt:
                        where['BillDateTime'] = { '$between': param.Value || '' };
                        break;
                    case PatientBillsFilters.PatBillNr:
                        where['BillNumber'] = { '$like': '%' + ('' || param.Value || '') };
                        break;
                    case PatientBillsFilters.PatId:
                        (where as any)['$and'] = [{ 'PatientId': param.Value },
                        { 'PatientBillStatusId': 1 }]; /*{ 'BillNumber': { '$is': null } } */
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillPriority:
                        where['BillPriorityId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Doctor:
                        where['DoctorId'] = param.Value;
                        break;
                    case PatientBillsFilters.Facility:
                        where['FacilityId'] = param.Value;
                        break;
                    case PatientBillsFilters.GuarantorType:
                        where['GuarantorTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.Guarantor:
                        guarantorWhere['GuarantorId'] = param.Value;
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.MultiGuarantorType:
                        guarantorWhere['GuarantorTypeId'] = { '$notIn': param.Value };
                        isGuarantorRequired = true;
                        break;
                    case PatientBillsFilters.IsOutStanding:
                        where['OutStandingAmount'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.OnlyPID:
                        where['PatientId'] = param.Value;
                        break;
                    case PatientBillsFilters.MRN:
                        patientQryJoin['where'] = {
                            '$or': [
                                { 'MRN': { '$like': '%' + (param.Value) + '%' } },
                                { 'FirstName': { '$like': '%' + (param.Value || '') + '%' } },
                                { 'LastName': { '$like': '%' + (param.Value || '') + '%' } }
                            ]
                        };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.Mobile:
                        patientQryJoin['where'] = { 'Mobile': param.Value };
                        patientQryJoin['required'] = true;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientName:
                        where['PatientName'] = { '$like': (param.Value || '') + '%' };
                        break;
                    case PatientBillsFilters.EncounterTypeId:
                        where['EncounterTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.ExcludeEncounterType://$notIn
                        where['EncounterTypeId'] = { '$notIn': param.Value };
                        break;
                    case PatientBillsFilters.BillTypeIdInQ:
                        where['BillTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsDoctorShare:
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleTypeId:
                        where['PharmacySaleTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.PharmacySaleType:
                        where['PharmacySaleTypeId'] = { '$in': param.Value };
                        break;
                    case PatientBillsFilters.IsClaimed:
                        where['IsClaimed'] = param.Value;
                        break;
                    case PatientBillsFilters.ChecklistStatusId:
                        where['ChecklistStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.StoreMasterId:
                        storemasterId = param.Value;
                        where['StoreMasterId'] = param.Value;
                        break;
                    case PatientBillsFilters.PrivateDueId:
                        where['PrivateDueId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsConsolidatePay:
                        where['IsConsolidatePay'] = { '$gt': '0' };
                        break;
                    case PatientBillsFilters.BillingType:
                        if (param.Value) {
                            let paramArr: Array<number> = [];
                            if (param.Value.toString().indexOf(',') > -1) {
                                paramArr = param.Value.toString().split(',');
                            } else {
                                paramArr = [param.Value];
                            }
                            where['BillTypeId'] = { '$in': paramArr };
                        }
                        break;
                    case PatientBillsFilters.ScheduleTypeId:
                        break;
                    case PatientBillsFilters.IsModified:
                        where['IsModified'] = param.Value;
                        break;
                    case PatientBillsFilters.ReferralId:
                        where['ReferralId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsDirectDGBill:
                        where['IsDirectDGBill'] = param.Value;
                        break;
                    case PatientBillsFilters.PatNumBil:
                        (where as any)['$or'] = [{ 'BillNumber': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientName': { '$like': '%' + (param.Value || '') + '%' } },
                        { 'PatientMrn': { '$like': '%' + (param.Value || '') + '%' } }];
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push(patientQryJoin);
        include.push({
            model: this.Models.PatientGuarantor,
            as: 'Guarantor',
            attributes: ['GuarantorName', 'GuarantorId'],
            required: isGuarantorRequired,
            where: guarantorWhere
        });
        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes });
    }

    public async DeletePatientBills(req: BaseRequest): Promise<Boolean> {
        let DetailBo = BoFactory.GetBo(billingBO.PatientBillDetailsBo, this.Request);
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: req.Id }]
        };
        let PatientBillDeta = await DetailBo.GetPatientBillDetails(apiReq);
        PatientBillDeta.Data.forEach((detail) => {
            detail.Status = 2;
        });
        await DetailBo.ManagePatientBillDetails(req.Id, PatientBillDeta.Data);
        let billData: any = {
            Id: req.Id,
            PatientBillStatusId: 2,
            Status: 2
        };
        let res = await this.Update(billData);
        return res;
    }

    // public async GetPharmacySalewithReturn(req: BaseRequest): Promise<any> {
    //     let result: any = [];
    //     let returnBO = BoFactory.GetBo(BillingBo.PatientReturnDetailsBo, this.Request);
    //     result.push({ Key: 1, Value: await this.PharamcySaleInfo(req) });
    //     result.push({ Key: 2, Value: await returnBO.ReturnSummary(req) });
    //     return result;
    // }
    // public async PharamcySaleInfo(req: BaseRequest): Promise<any> {
    //     let SalesInfoResult: any = [];
    //     let opbillamountInstance: any = await this.Find({
    //         attributes: ['BillDateTime', 'BillDiscount',
    //             'RoundOffValue', 'BillAmount', 'BillGeneratedBy'],
    //         where: {
    //             BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
    //             BillTypeId: { '$in': [4, 6] },
    //             IsPharmacyBill: 1,
    //             PatientBillStatusId: 3,
    //             PharmacySaleTypeId: { '$gt': 0 },
    //             FacilityId: req.Data.FacilityId,
    //         },
    //         // include: [EncJoin]
    //     });

    //     if (opbillamountInstance) {
    //         let SaleInfo: number = 0;
    //         let Type: number = 0;
    //         let bill: any = this.GetAttribute(opbillamountInstance);
    //         SaleInfo = bill['BillAmount'];
    //         Type = 1;
    //         let info = {
    //             'SaleInfo': SaleInfo,
    //             'Type': Type,
    //         };
    //         SalesInfoResult.push(info);
    //     }
    //     return SalesInfoResult;
    // }
    public async getPharmacyBillsWithReturn(req: BaseRequest): Promise<any> {
        let PharmacyBillsWithReturn: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.OnlyPID, Value: req.Data.PatientId },
            { Key: PatientBillsFilters.IsPharmacyBill, Value: true },
            { Key: PatientBillsFilters.FromDate, Value: req.Data.FromDate },
            { Key: PatientBillsFilters.ToDate, Value: req.Data.ToDate },
            { Key: PatientBillsFilters.PharmacySaleTypeId, Value: req.Data.PharmacySaleTypeId },
            { Key: PatientBillsFilters.EncounterId, Value: req.Data.EncounterId },
            { Key: PatientBillsFilters.PrivateDueId, Value: req.Data.DueApprovedById },
            { Key: PatientBillsFilters.StoreMasterId, Value: req.Data.StoreMasterId }]
        };
        if (req.Data.IsOutStanding === true) {
            apiReq.Params.push({ Key: PatientBillsFilters.IsOutStanding, Value: req.Data.IsOutStanding });
        }
        if (req.Data.NotOutStanding === true) {
            apiReq.Params.push({ Key: PatientBillsFilters.NotOutStanding, Value: req.Data.NotOutStanding });
        }
        if (req.Data.VisitIdentifier) {
            apiReq.Params.push({ Key: PatientBillsFilters.VisitIdentifier, Value: req.Data.VisitIdentifier });
        }
        let PatientBillsData = await this.GetPatientBills(apiReq);
        let PatientBillIds = Array();
        if (PatientBillsData) {
            for (var i = 0; i < PatientBillsData.Data.length; i++) {
                let PatientBills = PatientBillsData.Data[i];
                PharmacyBillsWithReturn.push(PatientBills);
                PatientBillIds.push(PatientBillsData.Data[i].Id);
            }
        }

        let PatientReturnReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientReturnsFilters.OnlyPID, Value: req.Data.PatientId },
            { Key: PatientReturnsFilters.FromDate, Value: req.Data.FromDate },
            { Key: PatientReturnsFilters.ToDate, Value: req.Data.ToDate },
            { Key: PatientReturnsFilters.EncounterId, Value: req.Data.EncounterId },
            { Key: PatientReturnsFilters.PharmacyReturnType, Value: req.Data.PharmacySaleTypeId },
            { Key: PatientReturnsFilters.StoreMasterId, Value: req.Data.StoreMasterId }
            ]
        };
        if (req.Data.VisitIdentifier) {
            PatientReturnReq.Params.push({ Key: PatientReturnsFilters.VisitIdentifier, Value: req.Data.VisitIdentifier });
        }
        if (req.Data.IsOutStanding === true) {
            PatientReturnReq.Params.push({ Key: PatientReturnsFilters.PatientBillId, Value: PatientBillIds });
        }
        if (req.Data.NotOutStanding === true) {
            PatientReturnReq.Params.push({ Key: PatientReturnsFilters.PatientBillId, Value: PatientBillIds });
        }
        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);
        if (PatientReturnsData) {
            for (var j = 0; j < PatientReturnsData.Data.length; j++) {
                let PatientReturns = PatientReturnsData.Data[j];
                PharmacyBillsWithReturn.push(PatientReturns);
            }
        }
        return PharmacyBillsWithReturn;
    }

    public async getPharmacyClearanceBillsWithReturn(req: BaseRequest): Promise<any> {
        let PharmacyBillsWithReturn: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.OnlyPID, Value: req.Data.PatientId },
            { Key: PatientBillsFilters.IsPharmacyBill, Value: true },
            { Key: PatientBillsFilters.FromDate, Value: req.Data.FromDate },
            { Key: PatientBillsFilters.ToDate, Value: req.Data.ToDate },
            { Key: PatientBillsFilters.PharmacySaleTypeId, Value: req.Data.PharmacySaleTypeId },
            { Key: PatientBillsFilters.EncounterId, Value: req.Data.EncounterId },
            { Key: PatientBillsFilters.EncounterId, Value: req.Data.EncounterId },
            { Key: PatientBillsFilters.PrivateDueId, Value: req.Data.DueApprovedById }
            ]
        };
        if (req.Data.IsOutStanding === true) {
            apiReq.Params.push({ Key: PatientBillsFilters.IsOutStanding, Value: req.Data.IsOutStanding });
        }
        if (req.Data.NotOutStanding === true) {
            apiReq.Params.push({ Key: PatientBillsFilters.NotOutStanding, Value: req.Data.NotOutStanding });
        }
        let PatientBillsData = await this.GetPatientBills(apiReq);
        let PatientBillIds = Array();
        if (PatientBillsData) {
            for (var i = 0; i < PatientBillsData.Data.length; i++) {
                let PatientBills = PatientBillsData.Data[i];
                PharmacyBillsWithReturn.push(PatientBills);
                PatientBillIds.push(PatientBillsData.Data[i].Id);
            }
        }

        let PatientReturnReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientReturnsFilters.OnlyPID, Value: req.Data.PatientId },
            { Key: PatientReturnsFilters.FromDate, Value: req.Data.FromDate },
            { Key: PatientReturnsFilters.ToDate, Value: req.Data.ToDate },
            { Key: PatientReturnsFilters.PharmacyReturnType, Value: req.Data.PharmacySaleTypeId }
            ]
        };
        if (req.Data.IsOutStanding === true) {
            PatientReturnReq.Params.push({ Key: PatientReturnsFilters.PatientBillId, Value: PatientBillIds });
        }
        if (req.Data.NotOutStanding === true) {
            PatientReturnReq.Params.push({ Key: PatientReturnsFilters.PatientBillId, Value: PatientBillIds });
        }
        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);
        if (PatientReturnsData) {
            for (var j = 0; j < PatientReturnsData.Data.length; j++) {
                let PatientReturns = PatientReturnsData.Data[j];
                PharmacyBillsWithReturn.push(PatientReturns);
            }
        }

        let PatientRefundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientId, Value: req.Data.PatientId },
            { Key: PatientRefundFilters.EncounterId, Value: req.Data.EncounterId },
            { Key: PatientRefundFilters.RefundType, Value: 5 },
            ]
        };
        // if (req.Data.IsOutStanding === true) {
        //     PatientReturnReq.Params.push({ Key: PatientReturnsFilters.PatientBillId, Value: PatientBillIds });
        // }
        // if (req.Data.NotOutStanding === true) {
        //     PatientReturnReq.Params.push({ Key: PatientReturnsFilters.PatientBillId, Value: PatientBillIds });
        // }
        let PatientRefundsBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let PatientRefundData = await PatientRefundsBo.GetPatientRefund(PatientRefundReq);

        let outData = {
            BillswithReturn: PharmacyBillsWithReturn,
            RefundData: PatientRefundData
        };
        return outData;
    }

    public async PrintPharmacyBills(req: BaseRequest, IsWhataApp?: boolean): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills: any = data.Data[0];
        let patientinfo: any;
        if (PatientBills.PatientId !== 0) {
            let patReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientFilters.Id, Value: PatientBills.PatientId }]
            };
            let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
            let patientData = await patientBo.GetPatients(patReq);
            patientinfo = patientData.Data[0];
        }
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        BillDetailsData.Data.sort(function (a, b) {
            return a.Id - b.Id;
        });
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            TotalCgst += Detail.CGstPercentage;
            TotalSgst += Detail.SGstPercentage;
            BillDetails.push(PatientBillDetail);
        });

        let PatientPaymentDetailsData: any = [];
        if (req.Id && req.Id > 0) {
            let detailReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
                { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
            };
            let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
            PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        }

        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Data.EncId }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];

        // 2. Get all licenses (or just active & history)
        let licenses: any = {};
        // if(PatientBills)
        //     licenses = PatientBills.StoreMaster;
        function formatDateOnly(date: Date) {
            return date.toISOString().split('T')[0]; // "YYYY-MM-DD"
        }

        const billDateOnly = formatDateOnly(PatientBills.BillDateTime);
        let storemasterdetailBo = BoFactory.GetBo(invbo.StoreMasterDetailBo, this.Request);
        licenses = await storemasterdetailBo.Find({

            attributes: ['LicenseNo', 'ActiveFrom', 'ActiveTo', 'StoreMasterId'],
            where: {
                ActiveFrom: { [SStatic.Op.lte]: billDateOnly },
                activeTo: { [SStatic.Op.gte]: billDateOnly },
                StoreMasterId: PatientBills.StoreMasterId
            }
        });

        // // 3. Match license based on bill date
        // const licenseForBill = licenses.find((lic: { ActiveFrom: string; ActiveTo: string; LicenseNo: string }) => {
        //     const from = new Date(lic.ActiveFrom);
        //     const to = new Date(lic.ActiveTo);
        //     const billDate = new Date(PatientBills.BillDate);

        //     from.setHours(0, 0, 0, 0);
        //     to.setHours(0, 0, 0, 0);
        //     billDate.setHours(0, 0, 0, 0);

        //     return billDate >= from && billDate <= to;
        // });
        if (licenses) {
            console.log('License Details');
            console.log(licenses);
        }

        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBills.FacilityId, PatientBills.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;


        if (licenses) {
            console.log('License Details');
            console.log(licenses);
            if (printPreferencesData.pharmacyprintheader) {
                console.log('printPreferencesData');
                console.log(printPreferencesData.pharmacyprintheader);

                let html = printPreferencesData.pharmacyprintheader;

                // Replace only the text inside the <div class="address1 tst1">
                html = html.replace(/(<div[^>]*class="address1 tst1"[^>]*>)(.*?)(<\/div>)/i,
                    '$1DL : ' + licenses.LicenseNo
                );
                printPreferencesData.pharmacyprintheader = html;
            }
        }

        let storeReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: StoreMasterFilters.Id, Value: PatientBills.StoreMasterId }]
        };
        let storeBo = BoFactory.GetBo(StoreBo.StoreMasterBo, this.Request);
        let storeData = await storeBo.GetStoreMasters(storeReq);
        let Store = storeData.Data[0];
        let DueAmount: number = 0;
        if (PatientBills.OutStandingAmount > 0 && PatientBills.ReturnedAmount > 0) {
            DueAmount = PatientBills.OutStandingAmount - (PatientBills.ReturnedAmount || 0);
        }
        if (PatientBills.OutStandingAmount > 0) {
            DueAmount = PatientBills.OutStandingAmount;
        }
        let info = {
            Encounter: Encounter,
            PatientBills: PatientBills,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            BillDetails: BillDetails,
            NetAmount: Math.round((PatientBills.BillAmount - PatientBills.BillDiscount - (PatientBills.ReturnedAmount || 0))
                + PatientBills.RoundOffValue),
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            DueAmount: DueAmount,
            WithoutTax: Math.round(PatientBills.BillAmount - PatientBills.BillDiscount) -
                (PatientBills.CGstAmount + PatientBills.SGstAmount),
            Preferences: printPreferencesData,
            StoreData: Store,
            Flags: flags
        };

        let key = 'pharmacybill';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.PharmacySaleTypeId === 4) {
            key = 'directpharmacybill';
        }
        if (info.PatientBills.IsPharmacyBill === true
            && info.PatientBills.IsPaidFully === false
            && info.PatientBills.OutStandingAmount > 0
            && info.PatientBills.PharmacySaleTypeId !== 4) {
            key = 'pharmacybill';
        }
        if (info.PatientBills.IsPharmacyBill === true
            && info.PatientBills.IsPaidFully === false
            && info.PatientBills.OutStandingAmount > 0
            && info.PatientBills.PharmacySaleTypeId === 4) {
            key = 'directpharmacybill';
        }
        if (req.Data.isprint) {
            key = 'pharmacysmall';
            PrintTypeId = 1;
        }
        if (info.StoreData.ISSeparatePayCounter === true) {
            key = 'pharmacycontactlensbill';
        }
        let pdfOption: any = null;
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                //format: 'A5',
                height: '5.8in',        // allowed units: mm, cm, in, px 5.8 x 8.3 in
                width: '8.3in',            // allowed units: mm, cm, in, px Width x Height (inch)
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if (printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        if (IsWhataApp && IsWhataApp === true) {
            let filename = patientinfo.MRN + PatientBills.Id + '.pdf';
            return await Report.GeneratePdf(key, { header: {}, body: info, watermark: Watermark }, filename, null, pdfOption);
        } else {
            return await Report.Generate(key, { header: {}, body: info, watermark: Watermark }, null, pdfOption);
        }
        // // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }
    public async WhatsAppDocumentSent(req: BaseRequest): Promise<Boolean> {
        let result = true;
        await this.sendWhatsAppDocument(req);
        return result;
    }
    public async sendWhatsAppDocument(req: BaseRequest): Promise<any> {
        try {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
            };
            let billData = await this.GetPatientBills(apiReq);
            let PatientBills: any = billData.Data[0];

            if (!PatientBills) {
                console.log('No patient bill data found for the provided ID.');
                return false;
            }
            let patReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientFilters.Id, Value: PatientBills.PatientId }]
            };
            let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
            let patientData = await patientBo.GetPatients(patReq);
            let patientinfo: any = patientData.Data[0];

            if (!patientinfo) {
                console.log('No patient information found for the provided PatientId.');
                return false;
            }

            let appBO = BoFactory.GetBo(apnmtBO.AppointmentBo, this.Request);
            var PatientName = await appBO.getPatientName(patientinfo);
            // let faciliBO = BoFactory.GetBo(facilityBo.FacilityBo, this.Request);
            // let facilityData = await faciliBO.GetFacilityById({ Id: this.Session.FacilityId });
            // let landline = facilityData.LandLine;
            let storeReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: StoreMasterFilters.Id, Value: PatientBills.StoreMasterId }]
            };
            let storeBo = BoFactory.GetBo(StoreBo.StoreMasterBo, this.Request);
            let storeData = await storeBo.GetStoreMasters(storeReq);
            let Store = storeData.Data[0];
            let storeNumber = Store.Password;
            console.log('storenumber', storeNumber);
            if (WhatsAppConfig['PROVIDER'] && WhatsAppConfig['PROVIDER'] === 'JSS') {
                try {
                    let printData: any = {
                        Id: req.Id,
                        Data: req.Data
                    };
                    if (req.Data.Type === 'PahrmacyBill' && Store.IsWhatsapp === true) {
                        let resAttach = await this.PrintPharmacyBills(printData, true);
                        let filename = patientinfo.MRN + req.Id + '.pdf';
                        console.log(resAttach, 'here is your Attachment pharmacy');
                        let data = {
                            TemplateName: 'ssmpharmabill',
                            mobile: patientinfo.Mobile,
                            BodyParameter: [PatientName, storeNumber],
                            pdf: process.env.WHATSAPP_DOMAIN + 'docs/' + filename,
                            filename: filename
                        };
                        const whatsApp = new WhatsappNotificationService();
                        let msgRes = await whatsApp.sendMessage(data);
                        console.log(msgRes, 'here is jss pharmacy Attachment');
                    } else if (req.Data.Type === 'DirectPatientPharmacySale' && Store.IsWhatsapp === true) {
                        let resAttach = await this.PrintPharmacyBills1(printData, true);
                        let filename = patientinfo.MRN + req.Id + '.pdf';
                        console.log(resAttach, 'here is your Attachment Direct patient pharmacy');
                        let data = {
                            TemplateName: 'ssmpharmabill',
                            mobile: patientinfo.Mobile,
                            BodyParameter: [PatientName, storeNumber],
                            pdf: process.env.WHATSAPP_DOMAIN + 'docs/' + filename,
                            filename: filename
                        };
                        const whatsApp = new WhatsappNotificationService();
                        let msgRes = await whatsApp.sendMessage(data);
                        console.log(msgRes, 'here is jss pharmacy Attachment');
                    }
                } catch (error) {
                    console.log('Error Processing messages:', error);
                }
            }
            //SMS
            let smsProvider = this.GetSmsProvider();
            let eventTemplateBO = BoFactory.GetBo(userbo.EventTemplateBo, this.Request);
            let smsTemplateInfo =
                await eventTemplateBO.GetTemplateInfo('PharmacyInvoicejss', 'PharmacyInvoicejss', 1);
            let smsmodel: any = {};
            if (smsTemplateInfo) {
                try {
                    if (SmsConfig['PROVIDER'] === 'JSS') {
                        let printData: any = {
                            Id: req.Id,
                            Data: req.Data
                        };
                        if (req.Data.Type === 'PahrmacyBill' && Store.IsWhatsapp === true) {
                            let resAttach = await this.PrintPharmacyBills(printData, true);
                            let filename = patientinfo.MRN + req.Id + '.pdf';
                            console.log(resAttach, 'here is your Attachment pharmacy');
                            smsmodel = {
                                numbers: [patientinfo.Mobile],
                                message: Template.Compile(smsTemplateInfo.TemplateContent,
                                    {
                                        patientName: PatientName,
                                        url: process.env.SMS_DOMAIN + 'docs/' + filename,
                                        // url: 'jsshospitals.com/bill.pdf',
                                        facilitycontact: storeNumber
                                    }),
                                templateId: smsTemplateInfo.ModuleId
                            };
                        } else if (req.Data.Type === 'DirectPatientPharmacySale' && Store.IsWhatsapp === true) {
                            let resAttach = await this.PrintPharmacyBills1(printData, true);
                            let filename = patientinfo.MRN + req.Id + '.pdf';
                            console.log(resAttach, 'here is your Attachment pharmacy');
                            smsmodel = {
                                numbers: [patientinfo.Mobile],
                                message: Template.Compile(smsTemplateInfo.TemplateContent,
                                    {
                                        patientName: PatientName,
                                        url: process.env.SMS_DOMAIN + 'docs/' + filename,
                                        // url: 'jsshospitals.com/bill.pdf',
                                        facilitycontact: storeNumber
                                    }),
                                templateId: smsTemplateInfo.ModuleId
                            };
                        }
                    }
                    if (smsProvider) {
                        let SMSStatus = await smsProvider.send(smsmodel);
                        let eventDashboardOutboundBo = BoFactory.GetBo(userbo.EventDashboardBo, this.Request);
                        await eventDashboardOutboundBo.ManageSMSOutBound(SMSStatus, smsmodel.message + ' To : ' + patientinfo.Mobile);
                    }
                } catch (error) {
                    console.log('Error Processing SMS:', error);
                }
            }
            return true;
        } catch (error) {
            console.error('Error in sendWhatsAppDocument:', error);
            return false;
        }
    }
    public async PrintPharmacyBills1(req: BaseRequest, IsWhataApp?: boolean): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills: any = data.Data[0];
        let patientinfo: any;
        if (PatientBills.PatientId !== 0) {
            let patReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientFilters.Id, Value: PatientBills.PatientId }]
            };
            let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
            let patientData = await patientBo.GetPatients(patReq);
            patientinfo = patientData.Data[0];
        }
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        BillDetailsData.Data.sort(function (a, b) {
            return a.Id - b.Id;
        });
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            PatientBillDetail.Rate = (Detail.Rate).toFixed(2);
            PatientBillDetail.GSTAmount = (Detail.GSTAmount).toFixed(2);
            PatientBillDetail.NetAmount = (Detail.NetAmount).toFixed(2);
            TotalCgst += Detail.CGstPercentage;
            TotalSgst += Detail.SGstPercentage;
            BillDetails.push(PatientBillDetail);
        });

        let GstInfo: any = {};
        let GstgrpInfo: any = [];
        // if (PatientBills.EncounterTypeId === 1) {
        let gstGrp = _.groupBy(BillDetailsData.Data, 'GSTPercentage');
        for (let cdx in gstGrp) {
            let gstdata = gstGrp[cdx];
            let gstData: any = {
                gstRate: '',
                NetAmountBeforeGST: '',
                gstAmt: '',
                cgstRate: '',
                cgstAmt: '',
                sgstRate: '',
                sgstAmt: '',
            };
            let gstamt: any = 0;
            let cgstamt: any = 0;
            let sgstamt: any = 0;
            let beforegst: any = 0;
            for (let jx in gstdata) {
                GstInfo = gstdata[jx];
                gstData.gstRate = GstInfo.GSTPercentage;
                gstData.cgstRate = GstInfo.CGstPercentage;
                gstData.sgstRate = GstInfo.SGstPercentage;
                gstamt = gstamt + parseFloat(GstInfo.GSTAmount);
                cgstamt = cgstamt + parseFloat(GstInfo.CGstAmount);
                sgstamt = sgstamt + parseFloat(GstInfo.SGstAmount);
                beforegst += parseFloat(GstInfo.NetAmountBeforeGST);
                gstData.gstAmt = parseFloat(gstamt).toFixed(2);
                gstData.cgstAmt = parseFloat(cgstamt).toFixed(2);
                gstData.sgstAmt = parseFloat(sgstamt).toFixed(2);
                gstData.NetAmountBeforeGST = parseFloat(beforegst).toFixed(2);
            }
            GstgrpInfo.push(gstData);
        }
        // }
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Data.EncId }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];

        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBills.FacilityId, PatientBills.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;

        let storeReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: StoreMasterFilters.Id, Value: PatientBills.StoreMasterId }]
        };
        let storeBo = BoFactory.GetBo(StoreBo.StoreMasterBo, this.Request);
        let storeData = await storeBo.GetStoreMasters(storeReq);
        let Store = storeData.Data[0];
        let DueAmount: number = 0;
        if (PatientBills.OutStandingAmount > 0 && PatientBills.ReturnedAmount > 0) {
            DueAmount = PatientBills.OutStandingAmount - (PatientBills.ReturnedAmount || 0);
        }
        if (PatientBills.OutStandingAmount > 0) {
            DueAmount = PatientBills.OutStandingAmount;
        }
        let QrInfo: any;
        let PatName: '';
        let FacInfo: '';
        if (PatientBills.Patient) {
            if (PatientBills.Patient.Title) {
                PatName = PatientBills.Patient.Title.Description;
            }
            if (PatientBills.Patient.FirstName) {
                PatName += ' ' + PatientBills.Patient.FirstName;
            }
            if (PatientBills.Patient.LastName) {
                PatName += ' ' + PatientBills.Patient.LastName;
            }
        }
        if (PatientBills.Facility) {
            FacInfo = PatientBills.Facility.FacilityName;
        }
        if (PatientBills.Facility.AddressLine1) {
            FacInfo += ', ' + PatientBills.Facility.AddressLine1;
        }
        if (PatientBills.Facility.Mobile) {
            FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
        }
        if (PatientBills.Facility.Email) {
            FacInfo += ',Email: ' + PatientBills.Facility.Email;
        }
        if (PatientBills.Facility.GstNumber) {
            FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
        }

        QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
            PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        let info = {
            Encounter: Encounter,
            PatientBills: PatientBills,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            BillDetails: BillDetails,
            NetAmount: Math.round((PatientBills.BillAmount - PatientBills.BillDiscount - (PatientBills.ReturnedAmount || 0))
                + PatientBills.RoundOffValue),
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            DueAmount: DueAmount,
            WithoutTax: (PatientBills.BillAmount - PatientBills.BillDiscount) - (PatientBills.CGstAmount + PatientBills.SGstAmount),
            Preferences: printPreferencesData,
            StoreData: Store,
            GstgrpInfo: GstgrpInfo,
            QrInfo: QrInfo,
        };

        let key = 'pharmacybill1';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.PharmacySaleTypeId === 4) {
            key = 'directpharmacybill1';
        }
        if (info.PatientBills.IsPharmacyBill === true
            && info.PatientBills.IsPaidFully === false
            && info.PatientBills.OutStandingAmount > 0
            && info.PatientBills.PharmacySaleTypeId !== 4) {
            key = 'pharmacybill1';
        }
        if (info.PatientBills.IsPharmacyBill === true
            && info.PatientBills.IsPaidFully === false
            && info.PatientBills.OutStandingAmount > 0
            && info.PatientBills.PharmacySaleTypeId === 4) {
            key = 'directpharmacybill1';
        }
        if (req.Data.isprint) {
            key = 'pharmacysmall';
            PrintTypeId = 1;
        }
        if (info.StoreData.ISSeparatePayCounter === true) {
            key = 'pharmacycontactlensbill';
        }
        let pdfOption: any = null;
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '1in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                    // format: 'A5',
                    // height: '5.8in',        // allowed units: mm, cm, in, px 5.8 x 8.3 in
                    // width: '8.3in',            // allowed units: mm, cm, in, px Width x Height (inch)
                    // orientation: 'portrait',
                    // border: '0',
                    // header: {
                    //     height: '0.7in',
                    //     contents: '',
                    // },
                    // footer: {
                    //     height: '0.5in',
                    //     contents: {
                    //         first: '',
                    //         default: '',
                    //         last: '',
                    //     },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if (printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        if (IsWhataApp && IsWhataApp === true) {
            let filename = patientinfo.MRN + PatientBills.Id + '.pdf';
            return await Report.GeneratePdf(key, { header: {}, body: info, watermark: Watermark }, filename, null, pdfOption);
        } else {
            return await Report.Generate(key, { header: {}, body: info, watermark: Watermark }, null, pdfOption);
        }
        // // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }
    public async PrintIPPharmacyBills(req: BaseRequest): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBillsforprint(apiReq);
        let PatientBills: any = data.Data[0];

        // 2. Get all licenses (or just active & history)
        let licenses: any = {};
        // if(PatientBills)
        //     licenses = PatientBills.StoreMaster;
        function formatDateOnly(date: Date) {
            return date.toISOString().split('T')[0]; // "YYYY-MM-DD"
        }
        const billDateOnly = formatDateOnly(PatientBills.BillDateTime);
        let storemasterdetailBo = BoFactory.GetBo(invbo.StoreMasterDetailBo, this.Request);
        licenses = await storemasterdetailBo.Find({

            attributes: ['LicenseNo', 'ActiveFrom', 'ActiveTo', 'StoreMasterId'],
            where: {
                ActiveFrom: { [SStatic.Op.lte]: billDateOnly },
                activeTo: { [SStatic.Op.gte]: billDateOnly },
                StoreMasterId: PatientBills.StoreMasterId
            }
        });

        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetailsforprint(billdetailReq);
        let BillDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            TotalCgst += Detail.CGstPercentage;
            TotalSgst += Detail.SGstPercentage;
            BillDetails.push(PatientBillDetail);
        });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];

        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBills.FacilityId, PatientBills.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        if (licenses) {
            console.log('License Details');
            console.log(licenses);
            if (printPreferencesData.pharmacyprintheader) {
                console.log('printPreferencesData');
                console.log(printPreferencesData.pharmacyprintheader);

                let html = printPreferencesData.pharmacyprintheader;

                // Replace only the text inside the <div class="address1 tst1">
                html = html.replace(/(<div[^>]*class="address1 tst1"[^>]*>)(.*?)(<\/div>)/i,
                    '$1DL : ' + licenses.LicenseNo
                );
                printPreferencesData.pharmacyprintheader = html;
            }
        }
        let QrInfo: any;
        let PatName: '';
        let FacInfo: '';
        if (PatientBills.Patient.Title) {
            PatName = PatientBills.Patient.Title.Description;
        }
        if (PatientBills.Patient.FirstName) {
            PatName += ' ' + PatientBills.Patient.FirstName;
        }
        if (PatientBills.Patient.LastName) {
            PatName += ' ' + PatientBills.Patient.LastName;
        }
        if (PatientBills.Facility) {
            FacInfo = PatientBills.Facility.FacilityName;
        }
        if (PatientBills.Facility.AddressLine1) {
            FacInfo += ', ' + PatientBills.Facility.AddressLine1;
        }
        if (PatientBills.Facility.Mobile) {
            FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
        }
        if (PatientBills.Facility.Email) {
            FacInfo += ',Email: ' + PatientBills.Facility.Email;
        }
        if (PatientBills.Facility.GstNumber) {
            FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
        }

        QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
            PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        let info = {
            Encounter: Encounter,
            PatientBills: PatientBills,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            BillDetails: BillDetails,
            NetAmount: PatientBills.BillAmount - PatientBills.BillDiscount,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            WithoutTax: PatientBills.BillAmount - PatientBills.GSTAmount,
            Preferences: printPreferencesData,
            QrInfo: QrInfo,
            Flags: flags,
            isConsigmentBilling: req.Data.isConsigmentBilling
        };
        let key = 'ippharmacy';
        if (info.isConsigmentBilling === 1) {
            key = 'consignmentbilling';
        }
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.PharmacySaleTypeId === 4) {
            key = 'directpharmacybill';
        }
        if (req.Data.isprint) {
            key = 'ippharmacy';
            PrintTypeId = 1;

        }
        let pdfOption: any = null;
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '1in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                    // height: '5.8in',
                    // width: '8.3in',
                    // orientation: 'landscape',
                    // border: '0',
                    // header: {
                    //     height: '0.7in',
                    //     contents: '',
                    // },
                    // footer: {
                    //     height: '0.5in',
                    //     contents: {
                    //         first: '',
                    //         default: '',
                    //         last: '',
                    //     },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if (printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }

    public async PrintStaffCreditBillReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let StoreName = apiReq.Data.StoreName;
        let StaffName = apiReq.Data.StaffName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        let TotalDueAmount: number = 0;

        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            BillData.PatientInfo = '';
            if (Detail.Patient) {
                if (Detail.Patient.Title)
                    BillData.PatientInfo = Detail.Patient.Title.Description;
                if (Detail.Patient.FirstName)
                    BillData.PatientInfo += ' ' + Detail.Patient.FirstName;
                if (Detail.Patient.LastName)
                    BillData.PatientInfo += ' ' + Detail.Patient.LastName;
                if (Detail.Patient.MRN)
                    BillData.PatientInfo += '/' + Detail.Patient.MRN;
                if (Detail.Patient.Age)
                    BillData.PatientInfo += '/' + Detail.Patient.Age;
                if (Detail.Patient.Gender)
                    BillData.PatientInfo += '/' + Detail.Patient.Gender.Description;
            } else if (!Detail.Patient) {
                BillData.PatientInfo = Detail.PatientName + '/' + Detail.Age + '/' + Detail.Gender.Description;
            }
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];

            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
            TotalDueAmount += item.OutStandingAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBillsData.FacilityId, PatientBillsData.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            StaffName: StaffName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            TotalDueAmount: TotalDueAmount,
            StoreName: StoreName

        };
        let pdfOption: any = null;
        let key = 'staffcreditbillreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintStaffPendingPaymentReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let StoreName = apiReq.Data.StoreName;
        let UserName = apiReq.Data.UserName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalPaidAmount: number = 0;
        let TotalDueAmount: number = 0;

        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            BillData.PatientInfo = '';
            if (Detail.Patient) {
                if (Detail.Patient.Title)
                    BillData.PatientInfo = Detail.Patient.Title.Description;
                if (Detail.Patient.FirstName)
                    BillData.PatientInfo += ' ' + Detail.Patient.FirstName;
                if (Detail.Patient.LastName)
                    BillData.PatientInfo += ' ' + Detail.Patient.LastName;
                if (Detail.Patient.MRN)
                    BillData.PatientInfo += '/' + Detail.Patient.MRN;
                if (Detail.Patient.Age)
                    BillData.PatientInfo += '/' + Detail.Patient.Age;
                if (Detail.Patient.Gender)
                    BillData.PatientInfo += '/' + Detail.Patient.Gender.Description;
            } else if (!Detail.Patient) {
                BillData.PatientInfo = Detail.PatientName + '/' + Detail.Age + '/' + Detail.Gender.Description;
            }
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];

            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalNetAmount += item.NetAmount;
            TotalPaidAmount += item.PaidAmount;
            TotalDueAmount += item.OutStandingAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBillsData.FacilityId, PatientBillsData.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            UserName: UserName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalNetAmount: TotalNetAmount,
            TotalPaidAmount: TotalPaidAmount,
            TotalDueAmount: TotalDueAmount,
            StoreName: StoreName

        };
        let pdfOption: any = null;
        let key = 'staffpendingpaymentreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintDMIPPharmacyBills(req: BaseRequest): Promise<any> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data[0];
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            TotalCgst += Detail.CGstPercentage;
            TotalSgst += Detail.SGstPercentage;
            BillDetails.push(PatientBillDetail);
        });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: PatientBills.EncounterId }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];

        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogoForDM(PatientBills.FacilityId, PatientBills.StoreMasterId);
        let info = {
            Encounter: Encounter,
            PatientBills: PatientBills,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            BillDetails: BillDetails,
            NetAmount: PatientBills.BillAmount - PatientBills.BillDiscount,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            WithoutTax: PatientBills.BillAmount - PatientBills.GSTAmount,
            Preferences: printPreferencesData,
            PrintData: printStoreData
        };
        return info;
    }

    public async PrintOPPharmacyBillsforIP(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: req.Data.BillType },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }
            ]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills: any = data.Data;
        let SaleDetails: any = [];
        let ReturnDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        let RetTotalCgst: number = 0;
        let RetTotalSgst: number = 0;
        //let TotalAmount: number = 0;
        let DispenceAmount: number = 0;
        let ReturnAmount: number = 0;
        PatientBills.forEach((detail: any) => {
            let OldBillnumber: any = null;
            let BillNumber: any = detail.BillNumber;
            detail.PatientBillDetails.forEach((Detail: any) => {
                let PatientBillDetail = Detail;
                PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                TotalCgst += Detail.CGstPercentage;
                TotalSgst += Detail.SGstPercentage;
                PatientBillDetail.Headerinfo = 0;
                if (PatientBillDetail.IsPharmacySale) {
                    DispenceAmount += Detail.Amount;
                    if (OldBillnumber !== BillNumber) {
                        PatientBillDetail.Headerinfo = 1;
                        PatientBillDetail.BillNumber = BillNumber;
                        OldBillnumber = BillNumber;
                    }
                    SaleDetails.push(PatientBillDetail);
                }
            });
        });

        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let retapiReq: any = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientReturnsFilters.EncounterId, Value: req.Id },
                { Key: PatientReturnsFilters.ReturnType, Value: [1, 2, 3, 4] },
                { Key: PatientReturnsFilters.PatientReturnStatus, Value: 3 }
            ]
        };

        let retdata = await PatientReturnsBo.GetPatientReturns(retapiReq);
        let PatientRetBills: any = retdata.Data;
        PatientRetBills.forEach((detail: any) => {
            let OldReturnNumber: any = null;
            let ReturnNumber: any = detail.ReturnNumber;
            let BillNumber: any = detail.BillNumber;
            detail.PatientReturnDetails.forEach((Detail: any) => {
                let PatientReturnDetails = Detail;
                PatientReturnDetails.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                RetTotalCgst += Detail.CGstPercentage;
                RetTotalSgst += Detail.SGstPercentage;
                ReturnAmount += Detail.Amount;
                PatientReturnDetails.Headerinfo = 0;
                if (OldReturnNumber !== ReturnNumber) {
                    PatientReturnDetails.Headerinfo = 1;
                    PatientReturnDetails.BillNumber = BillNumber;
                    PatientReturnDetails.ReturnNumber = ReturnNumber;
                    OldReturnNumber = ReturnNumber;
                }
                PatientReturnDetails.BillDateTime = Detail.ReturnDateTime;
                ReturnDetails.push(PatientReturnDetails);
            });
        });

        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter: any = encounterData.Data[0];
        let patientData = Encounter.Patient;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let info = {
            Encounter: Encounter,
            Patient: patientData,
            PatientBills: PatientBills,
            SaleDetails: SaleDetails,
            ReturnDetails: ReturnDetails,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            RetTotalCgst: RetTotalCgst,
            RetTotalSgst: RetTotalSgst,
            TotalAmount: DispenceAmount - ReturnAmount,
            DispenceAmount: DispenceAmount,
            ReturnAmount: ReturnAmount,
            Preferences: printPreferencesData
        };
        let pdfOption: any = null;
        let key = 'opbillingpharmacyforip';
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                //format: 'A5',
                height: '5.8in',        // allowed units: mm, cm, in, px 5.8 x 8.3 in
                width: '8.3in',            // allowed units: mm, cm, in, px Width x Height (inch)
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }

    public async PrintOPConsolidate(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: [1, 4, 5] },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }
            ]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills: any = data.Data;
        let SaleDetails: any = [];
        let ReturnDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        let RetTotalCgst: number = 0;
        let RetTotalSgst: number = 0;
        //let TotalAmount: number = 0;
        let DispenceAmount: number = 0;
        let ReturnAmount: number = 0;
        PatientBills.forEach((detail: any) => {
            let OldBillnumber: any = null;
            let BillNumber: any = detail.BillNumber;
            detail.PatientBillDetails.forEach((Detail: any) => {
                let PatientBillDetail = Detail;
                PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                TotalCgst += Detail.CGstPercentage;
                TotalSgst += Detail.SGstPercentage;
                PatientBillDetail.Headerinfo = 0;
                DispenceAmount += Detail.Amount;
                if (OldBillnumber !== BillNumber) {
                    PatientBillDetail.Headerinfo = 1;
                    PatientBillDetail.BillNumber = BillNumber;
                    OldBillnumber = BillNumber;
                }
                SaleDetails.push(PatientBillDetail);
            });
        });

        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let retapiReq: any = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientReturnsFilters.EncounterId, Value: req.Id },
                { Key: PatientReturnsFilters.ReturnType, Value: [1, 2, 3, 4] },
                { Key: PatientReturnsFilters.PatientReturnStatus, Value: 3 }
            ]
        };

        let retdata = await PatientReturnsBo.GetPatientReturns(retapiReq);
        let PatientRetBills: any = retdata.Data;
        PatientRetBills.forEach((detail: any) => {
            let OldReturnNumber: any = null;
            let ReturnNumber: any = detail.ReturnNumber;
            let BillNumber: any = detail.BillNumber;
            detail.PatientReturnDetails.forEach((Detail: any) => {
                let PatientReturnDetails = Detail;
                PatientReturnDetails.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                RetTotalCgst += Detail.CGstPercentage;
                RetTotalSgst += Detail.SGstPercentage;
                ReturnAmount += Detail.Amount;
                PatientReturnDetails.Headerinfo = 0;
                if (OldReturnNumber !== ReturnNumber) {
                    PatientReturnDetails.Headerinfo = 1;
                    PatientReturnDetails.BillNumber = BillNumber;
                    PatientReturnDetails.ReturnNumber = ReturnNumber;
                    OldReturnNumber = ReturnNumber;
                }
                PatientReturnDetails.BillDateTime = Detail.ReturnDateTime;
                ReturnDetails.push(PatientReturnDetails);
            });
        });

        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter: any = encounterData.Data[0];
        let patientData = Encounter.Patient;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let info = {
            Encounter: Encounter,
            Patient: patientData,
            PatientBills: PatientBills,
            SaleDetails: SaleDetails,
            ReturnDetails: ReturnDetails,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            RetTotalCgst: RetTotalCgst,
            RetTotalSgst: RetTotalSgst,
            TotalAmount: DispenceAmount - ReturnAmount,
            DispenceAmount: DispenceAmount,
            ReturnAmount: ReturnAmount,
            Preferences: printPreferencesData
        };
        let pdfOption: any = null;
        let key = 'opconsolidatedprint';
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                //format: 'A5',
                height: '5.8in',        // allowed units: mm, cm, in, px 5.8 x 8.3 in
                width: '8.3in',            // allowed units: mm, cm, in, px Width x Height (inch)
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, },
            null, pdfOption);
    }

    public async PrintOPConsolidateWithoutPharmacy(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: [1, 5] },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }
            ]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills: any = data.Data;
        let SaleDetails: any = [];
        let ReturnDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        let RetTotalCgst: number = 0;
        let RetTotalSgst: number = 0;
        //let TotalAmount: number = 0;
        let DispenceAmount: number = 0;
        let ReturnAmount: number = 0;
        PatientBills.forEach((detail: any) => {
            let OldBillnumber: any = null;
            let BillNumber: any = detail.BillNumber;
            detail.PatientBillDetails.forEach((Detail: any) => {
                let PatientBillDetail = Detail;
                PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                TotalCgst += Detail.CGstPercentage;
                TotalSgst += Detail.SGstPercentage;
                PatientBillDetail.Headerinfo = 0;
                DispenceAmount += Detail.Amount;
                if (OldBillnumber !== BillNumber) {
                    PatientBillDetail.Headerinfo = 1;
                    PatientBillDetail.BillNumber = BillNumber;
                    OldBillnumber = BillNumber;
                }
                SaleDetails.push(PatientBillDetail);
            });
        });

        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let retapiReq: any = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientReturnsFilters.EncounterId, Value: req.Id },
                { Key: PatientReturnsFilters.ReturnType, Value: [1, 2, 3, 4] },
                { Key: PatientReturnsFilters.PatientReturnStatus, Value: 3 }
            ]
        };

        let retdata = await PatientReturnsBo.GetPatientReturns(retapiReq);
        let PatientRetBills: any = retdata.Data;
        PatientRetBills.forEach((detail: any) => {
            let OldReturnNumber: any = null;
            let ReturnNumber: any = detail.ReturnNumber;
            let BillNumber: any = detail.BillNumber;
            detail.PatientReturnDetails.forEach((Detail: any) => {
                let PatientReturnDetails = Detail;
                PatientReturnDetails.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                RetTotalCgst += Detail.CGstPercentage;
                RetTotalSgst += Detail.SGstPercentage;
                ReturnAmount += Detail.Amount;
                PatientReturnDetails.Headerinfo = 0;
                if (OldReturnNumber !== ReturnNumber) {
                    PatientReturnDetails.Headerinfo = 1;
                    PatientReturnDetails.BillNumber = BillNumber;
                    PatientReturnDetails.ReturnNumber = ReturnNumber;
                    OldReturnNumber = ReturnNumber;
                }
                PatientReturnDetails.BillDateTime = Detail.ReturnDateTime;
                ReturnDetails.push(PatientReturnDetails);
            });
        });

        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter: any = encounterData.Data[0];
        let patientData = Encounter.Patient;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let info = {
            Encounter: Encounter,
            Patient: patientData,
            PatientBills: PatientBills,
            SaleDetails: SaleDetails,
            ReturnDetails: ReturnDetails,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            RetTotalCgst: RetTotalCgst,
            RetTotalSgst: RetTotalSgst,
            TotalAmount: DispenceAmount - ReturnAmount,
            DispenceAmount: DispenceAmount,
            ReturnAmount: ReturnAmount,
            Preferences: printPreferencesData
        };
        let pdfOption: any = null;
        let key = 'opconsolidatedprint';
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                //format: 'A5',
                height: '5.8in',        // allowed units: mm, cm, in, px 5.8 x 8.3 in
                width: '8.3in',            // allowed units: mm, cm, in, px Width x Height (inch)
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, },
            null, pdfOption);
    }

    public async PrintIPBillingPharmacyBills(req: BaseRequest): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            payment: (req.Data.paymentDetail) ? req.Data.paymentDetail : 0,
            nonmedical: (req.Data.nonmedical) ? req.Data.nonmedical : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 3 },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }
            ]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills: any = data.Data;
        let SaleDetails: any = [];
        let ReturnDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        //let TotalAmount: number = 0;
        let DispenceAmount: number = 0;
        let ReturnAmount: number = 0;
        PatientBills.forEach((detail: any) => {
            detail.PatientBillDetails.forEach((Detail: any) => {
                let PatientBillDetail = Detail;
                PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                TotalCgst += Detail.CGstPercentage;
                TotalSgst += Detail.SGstPercentage;
                if (PatientBillDetail.IsPharmacySale) {
                    DispenceAmount += Detail.Amount;
                    SaleDetails.push(PatientBillDetail);
                    /*
                    if (PatientBillDetail.ReturnedQuantity > 0) {
                        TotalAmount += DispenceAmount - TotalReturnAmount;
                        TotalReturnAmount += Detail.ReturnedQuantity * Detail.Rate;
                        PatientBillDetail.ReturnAmount = Detail.ReturnedQuantity * Detail.Rate;
                        ReturnDetails.push(PatientBillDetail);
                    }
                    */
                }
                if (PatientBillDetail.IsPharmacyReturn) {
                    ReturnAmount += Detail.Amount;
                    ReturnDetails.push(PatientBillDetail);
                }
            });
        });

        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter: any = encounterData.Data[0];
        let patientData = Encounter.Patient;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        // let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        // let printStoreData =
        //     await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        //Preferences: printPreferencesData
        let info = {
            Encounter: Encounter,
            Patient: patientData,
            PatientBills: PatientBills,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            SaleDetails: SaleDetails,
            ReturnDetails: ReturnDetails,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            TotalAmount: DispenceAmount + ReturnAmount,
            DispenceAmount: DispenceAmount,
            ReturnAmount: ReturnAmount,
            Preferences: printPreferencesData,
            Flags: flags
            //TotalReturnAmount: TotalReturnAmount,
        };
        let pdfOption: any = null;
        let key = 'ipbillingpharmacy';
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                //format: 'A5',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }

    public async PrintOTBillingPharmacyBills(req: BaseRequest): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            payment: (req.Data.paymentDetail) ? req.Data.paymentDetail : 0,
            nonmedical: (req.Data.nonmedical) ? req.Data.nonmedical : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let SurgeryEntriesBo = BoFactory.GetBo(otmbo.SurgeryEntryBo, this.Request);

        let OTapiReq = {
            Id: 0,
            PageContext: { PageSize: 50, PageNumber: 1 },
            Params: [{ Key: SurgeryEntryFilters.Id, Value: req.Id }]
        };
        let OTRegisterData = await SurgeryEntriesBo.GetSurgeryEntrys(OTapiReq);

        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientBillDetailsFilters.EncounterId, Value: OTRegisterData.Data[0].EncounterId },
                // { Key: PatientBillDetailsFilters.OTRegisterId, Value: req.Id },
                { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 },
                { Key: PatientBillDetailsFilters.ServiceCategoryId, Value: 27 }
            ],
        };
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let data = await PatientBillDetailsBo.GetPatientBillDetails(apiReq);
        let OTRegisterDetails: any = OTRegisterData.Data[0];
        let PatientBills: any = [];
        let PatientBillDetails: any = data.Data;
        let PatientPaymentDetails: any = [];
        let SaleDetails: any = [];
        let ReturnDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        let DispenceAmount: number = 0;
        let ReturnAmount: number = 0;
        PatientBillDetails.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            TotalCgst += Detail.CGstPercentage;
            TotalSgst += Detail.SGstPercentage;
            if (PatientBillDetail.IsPharmacySale) {
                DispenceAmount += Detail.Amount;
                SaleDetails.push(PatientBillDetail);
            }
            if (PatientBillDetail.IsPharmacyReturn) {
                ReturnAmount += Detail.Amount;
                ReturnDetails.push(PatientBillDetail);
            }
        });

        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: OTRegisterDetails.EncounterId }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter: any = encounterData.Data[0];
        let info = {
            OtRegister: OTRegisterDetails,
            Encounter: Encounter,
            PatientBills: PatientBills,
            PatientPaymentDetails: PatientPaymentDetails,
            SaleDetails: SaleDetails,
            ReturnDetails: ReturnDetails,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            TotalAmount: DispenceAmount + ReturnAmount,
            DispenceAmount: DispenceAmount,
            ReturnAmount: ReturnAmount,
            Flags: flags
        };
        let pdfOption: any = null;
        let key = 'otbillingpharmacy';
        pdfOption = {
            format: 'A4',
            orientation: 'portrait',
            border: '0',
            header: {
                height: '1.7in',
                contents: '',
            },
            footer: {
                height: '0.5in',
                contents: {
                    first: '',
                    default: '',
                    last: '',
                },
            },
            type: 'pdf',
            base: 'file://' + join(__dirname, '/../../Templates/assets/')
        };

        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintOPPatientBills(req: BaseRequest): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let ipprint = req.Data.ipprint;
        let TPANAME = req.Data.tpa;
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let isduplicate: boolean = false;
        let CancelledAmt = 0;
        if (req.Data) {
            if (req.Data.IsDuplicate) {
                isduplicate = req.Data.IsDuplicate;
            }
            if (req.Data.CancelledAmount) {
                CancelledAmt = req.Data.CancelledAmount;
            }
        }
        let PatientBills: any = data.Data[0];
        if (PatientBills.OutStandingAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
            PatientBills.OutStandingAmount = PatientBills.OutStandingAmount - PatientBills.CNAmount;
        }
        // if (PatientBills.PaidAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
        //     PatientBills.PaidAmount = PatientBills.PaidAmount - PatientBills.CNAmount;
        // }
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let patReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientFilters.Id, Value: PatientBills.PatientId }]
        };
        let patientData = await patientBo.GetPatients(patReq);
        let patientinfo: any = {};
        patientinfo = patientData.Data[0];
        let Age = '';
        if (patientinfo.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientinfo.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientinfo.Age > 0) {
            Age = patientinfo.Age + 'y';
        }
        // let PatientGuarantorsReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: PatientGuarantorFilters.Id, Value: PatientBills.GuarantorId }]
        // };
        // let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        // let PatientGuarantorsData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorsReq);
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientinfo.ReferrerId) {
            ReferralId = patientinfo.ReferrerId;
        }
        // let ReferralsReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        // };
        // let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        // let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        // BillDetailsData.Data.forEach((Detail: any) => {
        for (let bdx in BillDetailsData.Data) {
            let PatientBillDetail: any = BillDetailsData.Data[bdx];
            if (PatientBillDetail.GrossAmount > 0) {
                PatientBillDetail.Amt = PatientBillDetail.InGstAmount -
                    (PatientBillDetail.CGstAmount + PatientBillDetail.SGstAmount);
                // let ordDetailReq = {
                //     Id: 0,
                //     PageContext: { PageSize: -1, PageNumber: 1 },
                //     Params: [{ Key: PatientOrderDetailFilters.PatientBillDetailId, Value: PatientBillDetail.Id }]
                // };
                // let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
                // let OrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(ordDetailReq);
                // if (OrderDetails.Data.length > 0) {
                //     for (let odx in OrderDetails.Data) {
                //         let ordData = OrderDetails.Data[odx];
                //         // let orderReq = {
                //         //     Id: 0,
                //         //     PageContext: { PageSize: -1, PageNumber: 1 },
                //         //     Params: [{ Key: PatientOrderFilters.Id, Value: ordData.PatientOrderId }]
                //         // };
                //         let PatientOrderBO = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
                //         let PatientOrder = await PatientOrderBO.GetPatientOrderById({ Id: ordData.PatientOrderId });
                //         if (PatientOrder) {
                //             PatientBillDetail.OrderNumber = PatientOrder.OrderNumber;
                //         }
                //     }

                // }
                BillDetails.push(PatientBillDetail);
            }
        }
        // let PatientBillDetail = Detail;
        // let ordDetailReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: PatientOrderDetailFilters.PatientBillDetailId, Value: PatientBillDetail.Id }]
        // };
        // let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
        // let OrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(ordDetailReq);

        // });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        // let ordReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: PatientOrderFilters.PatBillingId, Value: PatientBills.Id }]
        // };
        // let orderBo = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
        // let OrderData = await orderBo.GetPatientOrderWithoutDetails(ordReq);
        // let OrderInfo: any = OrderData.Data[0];
        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData
        let QrInfo: any;
        let PatName: '';
        let FacInfo: '';
        if (patientinfo.Title) {
            PatName = patientinfo.Title.Description;
        }
        if (patientinfo.FirstName) {
            PatName += ' ' + patientinfo.FirstName;
        }
        if (patientinfo.LastName) {
            PatName += ' ' + patientinfo.LastName;
        }
        if (PatientBills.Facility) {
            FacInfo = PatientBills.Facility.FacilityName;
        }
        if (PatientBills.Facility.AddressLine1) {
            FacInfo += ', ' + PatientBills.Facility.AddressLine1;
        }
        if (PatientBills.Facility.Mobile) {
            FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
        }
        if (PatientBills.Facility.Email) {
            FacInfo += ',Email: ' + PatientBills.Facility.Email;
        }
        if (PatientBills.Facility.GstNumber) {
            FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
        }

        QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
            PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        let PrintType;
        let printInfo = {
            ObjectId: req.Id
            , ObjectTypeId: 4 /*Order*/
            , Reason: req.Data ? req.Data.Reason : null
            , PrintTypeId: PrintType
        };
        let printtype = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        let entitydata = await printtype.ManagePrintHistory(printInfo);
        let guarantorId = PatientBills.GuarantorId;
        let guarantorBO = BoFactory.GetBo(generalMasterBO.GuarantorBo, this.Request);
        let guarantor = await guarantorBO.GetGuarantorById({ Id: guarantorId });
        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            // Referrals: ReferralsData.Data[0],
            NetAmount: (PatientBills.BillAmount + PatientBills.RoundOffValue) -
                (PatientBills.BillDiscount || 0) - (PatientBills.RefundAmount || 0) + (PatientBills.GSTAmount || 0),
            // PatientGuarantor: PatientGuarantorsData.Data[0],
            Age: Age,
            Preferences: printPreferencesData,
            QrInfo: QrInfo,
            // OrderInfo: OrderInfo,
            CancelledAmt: CancelledAmt,
            Flags: flags,
            entitydata: entitydata,
            guarantor: guarantor,
            ipprint: ipprint,
            TPAName: TPANAME
        };

        let key = 'outpatientbill';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'dgbilling';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        if (info.PatientBills.BillTypeId === 1
            && info.PatientBills.IsPaidFully === false && info.PatientBills.OutStandingAmount > 0) {
            key = 'outpatientbill';
        }
        let pdfOption: any = null;
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '1in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                    // height: '5.8in',
                    // width: '8.3in',
                    // orientation: 'landscape',
                    // border: '0',
                    // header: {
                    //     height: '0.7in',
                    //     contents: '',
                    // },
                    // footer: {
                    //     height: '0.5in',
                    //     contents: {
                    //         first: '',
                    //         default: '',
                    //         last: '',
                    //     },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if ((printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) && isduplicate) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if ((printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) && isduplicate) {
                Watermark = Watermark || 'DUPLICATE';
            }
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }
    public async PrintAllPharmacybilldetails(req: BaseRequest): Promise<FileInfo> {
        // let data = await this.getPharmacyBillsWithReturn(req);
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let PharmacyBillsWithReturn: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.OnlyPID, Value: req.Data.Id },
            { Key: PatientBillsFilters.IsPharmacyBill, Value: true },
            ]
        };
        let PatientBillsData = await this.GetPatientBills(apiReq);
        let PatientBillIds = Array();
        let PatientReturnIds = Array();
        if (PatientBillsData) {
            for (var i = 0; i < PatientBillsData.Data.length; i++) {
                let PatientBills = PatientBillsData.Data[i];
                PharmacyBillsWithReturn.push(PatientBills);
                PatientBillIds.push(PatientBillsData.Data[i].Id);
            }
        }
        let PatientReturnReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientReturnsFilters.OnlyPID, Value: req.Data.Id },
            ]
        };
        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);
        if (PatientReturnsData) {
            for (var j = 0; j < PatientReturnsData.Data.length; j++) {
                let PatientReturns = PatientReturnsData.Data[j];
                PharmacyBillsWithReturn.push(PatientReturns);
                PatientReturnIds.push(PatientReturnsData.Data[j].Id);
            }
        }
        let BillDetails = [];
        let ReturnDetails = [];
        // let AllDetails = [];
        let EncounterDetails: any = {};
        let BillData: any = {};
        let PatientDetails: any = {};
        let printPreferencesData: any = {};
        let TotBillAmount: number = 0;
        let BillDisc: number = 0;
        let PaidAmount: number = 0;
        let RefAmount: number = 0;
        let NetAmount: number = 0;
        let DueAmount: number = 0;
        let patientBillDetailsinfo: any;
        if (PharmacyBillsWithReturn) {
            let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);

            for (let j = 0, len = PatientBillIds.length; j < len; j++) {
                let BillId = PatientBillIds[j];

                let billreq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillsFilters.Id, Value: BillId }]
                };
                let BillInfo: any = await this.GetPatientBills(billreq);
                BillData = BillInfo.Data[0];
                TotBillAmount += BillData.BillAmount;
                BillDisc += BillData.BillDiscount;
                PaidAmount += BillData.PaidAmount;
                DueAmount += BillData.OutStandingAmount;
                RefAmount += BillData.RefundAmount;
                NetAmount += (BillData.BillAmount) - (BillData.BillDiscount);
                let billdetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: BillId }]
                };
                patientBillDetailsinfo = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
                for (let k = 0, pbdlen = patientBillDetailsinfo.Data.length; k < pbdlen; k++) {
                    BillDetails.push(patientBillDetailsinfo.Data[k]);
                }
            }
            // let PatientReturnReq = {
            //     Id: 0,
            //     PageContext: { PageSize: -1, PageNumber: 1 },
            //     Params: [{ Key: PatientReturnsFilters.PatientBillId, Value: BillId }]
            // };
            // let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
            // let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);
            for (let j = 0, len = PatientReturnIds.length; j < len; j++) {
                let ReturnId = PatientReturnIds[j];
                let retDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientReturnDetailsFilters.PatientReturnId, Value: ReturnId }]
                };
                let PatientRetDetailBo = BoFactory.GetBo(bo.PatientReturnDetailsBo, this.Request);
                let PatientReturnDetailsData: any = await PatientRetDetailBo.GetPatientReturnDetails(retDetailReq);

                for (let k = 0, pbdlen = PatientReturnDetailsData.Data.length; k < pbdlen; k++) {
                    ReturnDetails.push(PatientReturnDetailsData.Data[k]);
                }
            }
            let encReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: EncounterFilters.Id, Value: patientBillDetailsinfo.Data[0].EncounterId }]
            };
            let encounterData = await encounterBo.GetEncounters(encReq);

            EncounterDetails = encounterData.Data[0];

            let patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
            let patReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientFilters.Id, Value: BillData.PatientId }]
            };
            let PatData = await patientBO.GetPatients(patReq);
            PatientDetails = PatData.Data[0];

            let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
            printPreferencesData =
                await facilityPreferenceBO.GetFacilityPreferenceWithLogo(EncounterDetails.FacilityId);
        }
        let info = {
            BillDetails: BillDetails,
            ReturnDetails: ReturnDetails,
            BillData: BillData,
            // AllDetails: AllDetails,
            // EncounterDetails: EncounterDetails,
            PatientDetails: PatientDetails,
            Preferences: printPreferencesData,
            TotBillAmount: TotBillAmount,
            BillDisc: BillDisc,
            // PaidAmount: PaidAmount,
            // RefAmount: RefAmount,
            NetAmount: NetAmount,
            // SaleAmount: NetAmount - TotalReturnAmount,
            DueAmount: DueAmount,
            // TotalReturnAmount: TotalReturnAmount,
            Amount: PaidAmount - RefAmount,
            Flags: flags
        };

        let pdfOption: any = null;
        let key = 'pharmacysalesandreturndetails';
        if (req.Data) {
            key = 'pharmacysalesandreturndetails';
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.0in',
                    contents: '',
                },
                footer: {
                    height: '1.0in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',

                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };

        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async GetprintPatientBills(apiReq?: ApiRequest<PatientBillsFilters>):
        Promise<ApiResponse<PatientBillsAttributes[]>> {
        let where: WhereOptions<any> = {};
        let include: Array<IncludeOptions> = [];
        apiReq.Params.forEach((param) => {
            if (this.IsValidParam(param)) {
                switch (param.Key) {
                    case PatientBillsFilters.Id:
                        where['Id'] = param.Value;
                        break;
                    case PatientBillsFilters.PatientBillStatus:
                        where['PatientBillStatusId'] = param.Value;
                        break;
                    case PatientBillsFilters.BillType:
                        where['BillTypeId'] = param.Value;
                        break;
                    case PatientBillsFilters.EncounterId:
                        where['EncounterId'] = param.Value;
                        break;
                    case PatientBillsFilters.IsPharmacyBill:
                        where['IsPharmacyBill'] = param.Value;
                        break;
                    default:
                        throw 'Not Implemented';
                }
            }
        });
        include.push({
            model: this.Models.Patient, attributes: ['Id', 'FirstName', 'LastName', 'MRN', 'Age', 'Mobile',
                'MRNTypeId', 'DOB', 'TitleId', 'GenderId', 'AddressLine1',
                'AddressLine2', 'Pincode', 'Area', 'City', 'State', 'Country', 'NationalityIdentifier', 'ReferrerId'], required: false,
            include: [
                this.GetReference('Title'),
                this.GetReference('Gender')
            ]
        });
        include.push({
            model: this.Models.StoreMaster, attributes: ['Id', 'ISSeparatePayCounter'], required: false,
            // include: [
            //     this.GetReference('Title')
            // ]
        });
        include.push({
            model: this.Models.Facility, attributes: ['FacilityName', 'AddressLine1', 'Mobile',
                'Email', 'GstNumber'
            ], required: false,
            // include: [
            //     this.GetReference('Title')
            // ]
        });
        include.push({
            model: this.Models.Referral, attributes: ['Id', 'ReferralName'
            ], required: false,
            // include: [
            //     this.GetReference('Title')
            // ]
        });
        include.push({
            model: this.Models.User, attributes: ['FirstName', 'LastName', 'SignPath', 'UserName'], as: 'CreatedUser', required: false,
            // include: [
            //     this.GetReference('Title')
            // ]
        });
        include.push(this.GetReference('Title'));
        include.push(this.GetReference('Gender'));
        apiReq.Attributes = ['Id', 'BillAmount', 'BillDiscount',
            'PaidAmount', 'OutStandingAmount', 'FacilityId',
            'RefundAmount', 'BillAmount', 'BillDiscount', 'PatientBillStatusId', 'BillDateTime',
            'PatientId', 'BillTypeId', 'BillNumber', 'ReturnedAmount',
            'RoundOffValue', 'CGstAmount', 'SGstAmount', 'StoreMasterId',
            'GSTAmount', 'StoreMasterId', 'IsPharmacyBill',
            'IsPaidFully', 'PharmacySaleTypeId', 'CreatedBy',
            'TitleId', 'PatientName', 'GenderId', 'Age', 'DOB', 'Mobile', 'ReferralId',
            'CNAmount', 'ReferralName'
        ];
        apiReq.Attributes = apiReq.Attributes;
        return await this.FindAndCountAll(apiReq, { where: where, include: include, attributes: apiReq.Attributes });
    }
    public async ModPrintPatientBills(req: BaseRequest): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
        };
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetprintPatientBills(apiReq);
        let isduplicate: boolean = false;
        let CancelledAmt = 0;
        if (req.Data) {
            if (req.Data.IsDuplicate) {
                isduplicate = req.Data.IsDuplicate;
            }
            if (req.Data.CancelledAmount) {
                CancelledAmt = req.Data.CancelledAmount;
            }
        }
        let PatientBills: any = data.Data[0];
        if (PatientBills.OutStandingAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
            PatientBills.OutStandingAmount = PatientBills.OutStandingAmount - PatientBills.CNAmount;
        }
        // if (PatientBills.PaidAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
        //     PatientBills.PaidAmount = PatientBills.PaidAmount - PatientBills.CNAmount;
        // }
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let patReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientFilters.Id, Value: PatientBills.PatientId }]
        };
        let patientData = await patientBo.GetMinPatientSearch(patReq);
        let patientinfo: any = {};
        patientinfo = patientData.Data[0];
        let Age = '';
        if (patientinfo.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientinfo.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'Y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientinfo.Age > 0) {
            Age = patientinfo.Age + 'Y';
        }
        let PatientGuarantorsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.Id, Value: PatientBills.GuarantorId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorsData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorsReq);
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientinfo.ReferrerId) {
            ReferralId = patientinfo.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetMinPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        // BillDetailsData.Data.forEach((Detail: any) => {
        for (let bdx in BillDetailsData.Data) {
            let PatientBillDetail: any = BillDetailsData.Data[bdx];
            // if (PatientBillDetail.GrossAmount > 0) {
            PatientBillDetail.Amt = PatientBillDetail.InGstAmount -
                (PatientBillDetail.CGstAmount + PatientBillDetail.SGstAmount);
            // let ordDetailReq = {
            //     Id: 0,
            //     PageContext: { PageSize: -1, PageNumber: 1 },
            //     Params: [{ Key: PatientOrderDetailFilters.PatientBillDetailId, Value: PatientBillDetail.Id }]
            // };
            // let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
            // let OrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(ordDetailReq);
            // if (OrderDetails.Data.length > 0) {
            //     for (let odx in OrderDetails.Data) {
            //         let ordData = OrderDetails.Data[odx];
            // let orderReq = {
            //     Id: 0,
            //     PageContext: { PageSize: -1, PageNumber: 1 },
            //     Params: [{ Key: PatientOrderFilters.Id, Value: ordData.PatientOrderId }]
            // };
            //     let PatientOrderBO = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
            //     let PatientOrder = await PatientOrderBO.GetPatientOrderById({ Id: ordData.PatientOrderId });
            //     if (PatientOrder) {
            //         PatientBillDetail.OrderNumber = PatientOrder.OrderNumber;
            //     }
            // }

            // }
            BillDetails.push(PatientBillDetail);
            // }
        }
        // let PatientBillDetail = Detail;
        // let ordDetailReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: PatientOrderDetailFilters.PatientBillDetailId, Value: PatientBillDetail.Id }]
        // };
        // let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
        // let OrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(ordDetailReq);

        // });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        // let ordReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: PatientOrderFilters.PatBillingId, Value: PatientBills.Id }]
        // };
        // let orderBo = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
        // let OrderData = await orderBo.GetPatientOrderWithoutDetails(ordReq);
        // let OrderInfo: any = OrderData.Data[0];
        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData
        let QrInfo: any;
        let PatName: '';
        let FacInfo: '';
        if (patientinfo.Title) {
            PatName = patientinfo.Title.Description;
        }
        if (patientinfo.FirstName) {
            PatName += ' ' + patientinfo.FirstName;
        }
        if (patientinfo.LastName) {
            PatName += ' ' + patientinfo.LastName;
        }
        if (PatientBills.Facility) {
            FacInfo = PatientBills.Facility.FacilityName;
        }
        if (PatientBills.Facility.AddressLine1) {
            FacInfo += ', ' + PatientBills.Facility.AddressLine1;
        }
        if (PatientBills.Facility.Mobile) {
            FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
        }
        if (PatientBills.Facility.Email) {
            FacInfo += ',Email: ' + PatientBills.Facility.Email;
        }
        if (PatientBills.Facility.GstNumber) {
            FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
        }

        QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
            PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            Referrals: ReferralsData.Data[0],
            NetAmount: (PatientBills.BillAmount + PatientBills.RoundOffValue) -
                (PatientBills.BillDiscount || 0) - (PatientBills.RefundAmount || 0) + (PatientBills.GSTAmount || 0),
            PatientGuarantor: PatientGuarantorsData.Data[0],
            Age: Age,
            Preferences: printPreferencesData,
            QrInfo: QrInfo,
            // OrderInfo: OrderInfo,
            CancelledAmt: CancelledAmt,
            Flags: flags
        };

        let key = 'outpatientbill';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'dgbilling';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        if (info.PatientBills.BillTypeId === 1
            && info.PatientBills.IsPaidFully === false && info.PatientBills.OutStandingAmount > 0) {
            key = 'outpatientbill';
        }
        let pdfOption: any = null;
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '1in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                    // height: '5.8in',
                    // width: '8.3in',
                    // orientation: 'landscape',
                    // border: '0',
                    // header: {
                    //     height: '0.7in',
                    //     contents: '',
                    // },
                    // footer: {
                    //     height: '0.5in',
                    //     contents: {
                    //         first: '',
                    //         default: '',
                    //         last: '',
                    //     },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if ((printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) && isduplicate) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if ((printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) && isduplicate) {
                Watermark = Watermark || 'DUPLICATE';
            }
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }
    public async PrintPatientBills(req: BaseRequest): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let isduplicate: boolean = false;
        let CancelledAmt = 0;
        if (req.Data) {
            if (req.Data.IsDuplicate) {
                isduplicate = req.Data.IsDuplicate;
            }
            if (req.Data.CancelledAmount) {
                CancelledAmt = req.Data.CancelledAmount;
            }
        }
        let PatientBills: any = data.Data[0];
        if (PatientBills.OutStandingAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
            PatientBills.OutStandingAmount = PatientBills.OutStandingAmount - PatientBills.CNAmount;
        }
        // if (PatientBills.PaidAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
        //     PatientBills.PaidAmount = PatientBills.PaidAmount - PatientBills.CNAmount;
        // }
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let patReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientFilters.Id, Value: PatientBills.PatientId }]
        };
        let patientData = await patientBo.GetPatients(patReq);
        let patientinfo: any = {};
        patientinfo = patientData.Data[0];
        let Age = '';
        if (patientinfo.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientinfo.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientinfo.Age > 0) {
            Age = patientinfo.Age + 'y';
        }
        let PatientGuarantorsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.Id, Value: PatientBills.GuarantorId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorsData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorsReq);
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientinfo.ReferrerId) {
            ReferralId = patientinfo.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        // BillDetailsData.Data.forEach((Detail: any) => {
        for (let bdx in BillDetailsData.Data) {
            let PatientBillDetail: any = BillDetailsData.Data[bdx];
            if (PatientBillDetail.GrossAmount > 0) {
                PatientBillDetail.Amt = PatientBillDetail.InGstAmount -
                    (PatientBillDetail.CGstAmount + PatientBillDetail.SGstAmount);
                let ordDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientOrderDetailFilters.PatientBillDetailId, Value: PatientBillDetail.Id }]
                };
                let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
                let OrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(ordDetailReq);
                if (OrderDetails.Data.length > 0) {
                    for (let odx in OrderDetails.Data) {
                        let ordData = OrderDetails.Data[odx];
                        // let orderReq = {
                        //     Id: 0,
                        //     PageContext: { PageSize: -1, PageNumber: 1 },
                        //     Params: [{ Key: PatientOrderFilters.Id, Value: ordData.PatientOrderId }]
                        // };
                        let PatientOrderBO = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
                        let PatientOrder = await PatientOrderBO.GetPatientOrderById({ Id: ordData.PatientOrderId });
                        if (PatientOrder) {
                            PatientBillDetail.OrderNumber = PatientOrder.OrderNumber;
                        }
                    }

                }
                BillDetails.push(PatientBillDetail);
            }
        }
        // let PatientBillDetail = Detail;
        // let ordDetailReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: PatientOrderDetailFilters.PatientBillDetailId, Value: PatientBillDetail.Id }]
        // };
        // let PatientOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
        // let OrderDetails = await PatientOrderDetailBo.GetPatientOrderDetails(ordDetailReq);

        // });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        let ordReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientOrderFilters.PatBillingId, Value: PatientBills.Id }]
        };
        let orderBo = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
        let OrderData = await orderBo.GetPatientOrderWithoutDetails(ordReq);
        let OrderInfo: any = OrderData.Data[0];
        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData
        let QrInfo: any;
        let PatName: '';
        let FacInfo: '';
        if (patientinfo.Title) {
            PatName = patientinfo.Title.Description;
        }
        if (patientinfo.FirstName) {
            PatName += ' ' + patientinfo.FirstName;
        }
        if (patientinfo.LastName) {
            PatName += ' ' + patientinfo.LastName;
        }
        if (PatientBills.Facility) {
            FacInfo = PatientBills.Facility.FacilityName;
        }
        if (PatientBills.Facility.AddressLine1) {
            FacInfo += ', ' + PatientBills.Facility.AddressLine1;
        }
        if (PatientBills.Facility.Mobile) {
            FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
        }
        if (PatientBills.Facility.Email) {
            FacInfo += ',Email: ' + PatientBills.Facility.Email;
        }
        if (PatientBills.Facility.GstNumber) {
            FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
        }

        QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
            PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            Referrals: ReferralsData.Data[0],
            NetAmount: (PatientBills.BillAmount + PatientBills.RoundOffValue) -
                (PatientBills.BillDiscount || 0) - (PatientBills.RefundAmount || 0) + (PatientBills.GSTAmount || 0),
            PatientGuarantor: PatientGuarantorsData.Data[0],
            Age: Age,
            Preferences: printPreferencesData,
            QrInfo: QrInfo,
            OrderInfo: OrderInfo,
            CancelledAmt: CancelledAmt,
            Flags: flags
        };

        let key = 'outpatientbill';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'dgbilling';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        if (info.PatientBills.BillTypeId === 1
            && info.PatientBills.IsPaidFully === false && info.PatientBills.OutStandingAmount > 0) {
            key = 'outpatientbill';
        }
        let pdfOption: any = null;
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '1in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                    // height: '5.8in',
                    // width: '8.3in',
                    // orientation: 'landscape',
                    // border: '0',
                    // header: {
                    //     height: '0.7in',
                    //     contents: '',
                    // },
                    // footer: {
                    //     height: '0.5in',
                    //     contents: {
                    //         first: '',
                    //         default: '',
                    //         last: '',
                    //     },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if ((printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) && isduplicate) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if ((printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) && isduplicate) {
                Watermark = Watermark || 'DUPLICATE';
            }
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }
    public async PrintPatientBills1(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let isduplicate: boolean = false;
        let CancelledAmt = 0;
        if (req.Data) {
            if (req.Data.IsDuplicate) {
                isduplicate = req.Data.IsDuplicate;
            }
            if (req.Data.CancelledAmount) {
                CancelledAmt = req.Data.CancelledAmount;
            }
        }
        let PatientBills: any = data.Data[0];
        if (PatientBills.OutStandingAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
            PatientBills.OutStandingAmount = PatientBills.OutStandingAmount - PatientBills.CNAmount;
        }
        // if (PatientBills.PaidAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
        //     PatientBills.PaidAmount = PatientBills.PaidAmount - PatientBills.CNAmount;
        // }
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let patReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientFilters.Id, Value: PatientBills.PatientId }]
        };
        let patientData = await patientBo.GetPatients(patReq);
        let patientinfo: any = {};
        patientinfo = patientData.Data[0];
        let Age = '';
        if (patientinfo.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientinfo.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientinfo.Age > 0) {
            Age = patientinfo.Age + 'y';
        }
        let PatientGuarantorsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.Id, Value: PatientBills.GuarantorId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorsData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorsReq);
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientinfo.ReferrerId) {
            ReferralId = patientinfo.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            if (Detail.GrossAmount > 0) {
                PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                BillDetails.push(PatientBillDetail);
            }
        });
        let GstInfo: any = {};
        let GstgrpInfo: any = [];
        if (PatientBills.EncounterTypeId === 1) {
            // if (BillDetailsData.Data.GSTAmount > 0) {
            let gstGrp = _.groupBy(BillDetailsData.Data, 'GSTPercentage');
            for (let cdx in gstGrp) {
                if (cdx !== '0') {
                    let gstdata = gstGrp[cdx];
                    let gstData: any = {
                        gstRate: '',
                        gstAmt: '',
                        cgstRate: '',
                        cgstAmt: '',
                        sgstRate: '',
                        sgstAmt: '',
                    };
                    let gstamt: any = 0;
                    let cgstamt: any = 0;
                    let sgstamt: any = 0;

                    for (let jx in gstdata) {
                        GstInfo = gstdata[jx];
                        gstData.gstRate = GstInfo.GSTPercentage;
                        gstData.cgstRate = GstInfo.CGstPercentage;
                        gstData.sgstRate = GstInfo.SGstPercentage;
                        gstamt = gstamt + parseFloat(GstInfo.GSTAmount);
                        cgstamt = cgstamt + parseFloat(GstInfo.CGstAmount);
                        sgstamt = sgstamt + parseFloat(GstInfo.SGstAmount);
                        gstData.gstAmt = parseFloat(gstamt).toFixed(2);
                        gstData.cgstAmt = parseFloat(cgstamt).toFixed(2);
                        gstData.sgstAmt = parseFloat(sgstamt).toFixed(2);
                    }
                    GstgrpInfo.push(gstData);
                }
            }
        }
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);

        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData
        let QrInfo: any;
        let PatName: '';
        let FacInfo: '';
        if (patientinfo.Title) {
            PatName = patientinfo.Title.Description;
        }
        if (patientinfo.FirstName) {
            PatName += ' ' + patientinfo.FirstName;
        }
        if (patientinfo.LastName) {
            PatName += ' ' + patientinfo.LastName;
        }
        if (PatientBills.Facility) {
            FacInfo = PatientBills.Facility.FacilityName;
        }
        if (PatientBills.Facility.AddressLine1) {
            FacInfo += ', ' + PatientBills.Facility.AddressLine1;
        }
        if (PatientBills.Facility.Mobile) {
            FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
        }
        if (PatientBills.Facility.Email) {
            FacInfo += ',Email: ' + PatientBills.Facility.Email;
        }
        if (PatientBills.Facility.GstNumber) {
            FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
        }

        QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
            PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            Referrals: ReferralsData.Data[0],
            NetAmount: (PatientBills.BillAmount + PatientBills.RoundOffValue) -
                (PatientBills.BillDiscount || 0) - (PatientBills.RefundAmount || 0) + (PatientBills.GSTAmount || 0),
            PatientGuarantor: PatientGuarantorsData.Data[0],
            Age: Age,
            Preferences: printPreferencesData,
            QrInfo: QrInfo,
            GstgrpInfo: GstgrpInfo,
            CancelledAmt: CancelledAmt
        };

        let key = 'outpatientbill1';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'dgbilling1';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        if (info.PatientBills.BillTypeId === 1
            && info.PatientBills.IsPaidFully === false && info.PatientBills.OutStandingAmount > 0) {
            key = 'outpatientbill1';
        }
        let pdfOption: any = null;
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '1in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                    // height: '5.8in',
                    // width: '8.3in',
                    // orientation: 'landscape',
                    // border: '0',
                    // header: {
                    //     height: '0.7in',
                    //     contents: '',
                    // },
                    // footer: {
                    //     height: '0.5in',
                    //     contents: {
                    //         first: '',
                    //         default: '',
                    //         last: '',
                    //     },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if ((printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) && isduplicate) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }
    public async ThermalPrintPatientBills(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let isduplicate: boolean = false;
        if (req.Data) {
            if (req.Data.IsDuplicate) {
                isduplicate = req.Data.IsDuplicate;
            }
        }
        let PatientBills: any = data.Data[0];
        if (PatientBills.OutStandingAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
            PatientBills.OutStandingAmount = PatientBills.OutStandingAmount - PatientBills.CNAmount;
        }
        // if (PatientBills.PaidAmount > 0 && PatientBills.PatientBillStatusId !== 2) {
        //     PatientBills.PaidAmount = PatientBills.PaidAmount - PatientBills.CNAmount;
        // }
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let patReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientFilters.Id, Value: PatientBills.PatientId }]
        };
        let patientData = await patientBo.GetPatients(patReq);
        let patientinfo: any = {};
        patientinfo = patientData.Data[0];
        let Age = '';
        if (patientinfo.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientinfo.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientinfo.Age > 0) {
            Age = patientinfo.Age + 'y';
        }
        let PatientGuarantorsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.Id, Value: PatientBills.GuarantorId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorsData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorsReq);
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientinfo.ReferrerId) {
            ReferralId = patientinfo.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            if (Detail.GrossAmount > 0) {
                PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
                BillDetails.push(PatientBillDetail);
            }
        });
        let GstInfo: any = {};
        let GstgrpInfo: any = [];
        if (PatientBills.EncounterTypeId === 1) {
            // if (BillDetailsData.Data.GSTAmount > 0) {
            let gstGrp = _.groupBy(BillDetailsData.Data, 'GSTPercentage');
            for (let cdx in gstGrp) {
                if (cdx !== '0') {
                    let gstdata = gstGrp[cdx];
                    let gstData: any = {
                        gstRate: '',
                        gstAmt: '',
                        cgstRate: '',
                        cgstAmt: '',
                        sgstRate: '',
                        sgstAmt: '',
                    };
                    let gstamt: any = 0;
                    let cgstamt: any = 0;
                    let sgstamt: any = 0;

                    for (let jx in gstdata) {
                        GstInfo = gstdata[jx];
                        gstData.gstRate = GstInfo.GSTPercentage;
                        gstData.cgstRate = GstInfo.CGstPercentage;
                        gstData.sgstRate = GstInfo.SGstPercentage;
                        gstamt = gstamt + parseFloat(GstInfo.GSTAmount);
                        cgstamt = cgstamt + parseFloat(GstInfo.CGstAmount);
                        sgstamt = sgstamt + parseFloat(GstInfo.SGstAmount);
                        gstData.gstAmt = parseFloat(gstamt).toFixed(2);
                        gstData.cgstAmt = parseFloat(cgstamt).toFixed(2);
                        gstData.sgstAmt = parseFloat(sgstamt).toFixed(2);
                    }
                    GstgrpInfo.push(gstData);
                }
            }
        }
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);

        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData
        let QrInfo: any;
        let PatName: '';
        let FacInfo: '';
        if (patientinfo.Title) {
            PatName = patientinfo.Title.Description;
        }
        if (patientinfo.FirstName) {
            PatName += ' ' + patientinfo.FirstName;
        }
        if (patientinfo.LastName) {
            PatName += ' ' + patientinfo.LastName;
        }
        if (PatientBills.Facility) {
            FacInfo = PatientBills.Facility.FacilityName;
        }
        if (PatientBills.Facility.AddressLine1) {
            FacInfo += ', ' + PatientBills.Facility.AddressLine1;
        }
        if (PatientBills.Facility.Mobile) {
            FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
        }
        if (PatientBills.Facility.Email) {
            FacInfo += ',Email: ' + PatientBills.Facility.Email;
        }
        if (PatientBills.Facility.GstNumber) {
            FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
        }

        QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
            PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            Referrals: ReferralsData.Data[0],
            NetAmount: (PatientBills.BillAmount + PatientBills.RoundOffValue) -
                (PatientBills.BillDiscount || 0) - (PatientBills.RefundAmount || 0) + (PatientBills.GSTAmount || 0),
            PatientGuarantor: PatientGuarantorsData.Data[0],
            Age: Age,
            Preferences: printPreferencesData,
            QrInfo: QrInfo,
            GstgrpInfo: GstgrpInfo
        };

        let key = 'thermaloutpatientbill';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'thermaloutpatientbill';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        if (info.PatientBills.BillTypeId === 1
            && info.PatientBills.IsPaidFully === false && info.PatientBills.OutStandingAmount > 0) {
            key = 'thermaloutpatientbill';
        }
        let pdfOption: any = null;
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A7',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '1in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                    // height: '5.8in',
                    // width: '8.3in',
                    // orientation: 'landscape',
                    // border: '0',
                    // header: {
                    //     height: '0.7in',
                    //     contents: '',
                    // },
                    // footer: {
                    //     height: '0.5in',
                    //     contents: {
                    //         first: '',
                    //         default: '',
                    //         last: '',
                    //     },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if ((printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) && isduplicate) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }
    public async PrintPharmacyClearance(req: BaseRequest): Promise<FileInfo> {
        let data = await this.getPharmacyBillsWithReturn(req);
        let facilityId = req.Data.FacilityId;
        let SalesSerialNo = 0;
        let ReturnSerialNo = 0;
        let PatientPharmacyBillswithReturns = [];
        // let PatientPharmacyReturns = [];
        let PatientBillsWithReturn = data;
        let TotalAmount = 0;
        let TotalSalesAmount = 0;
        let TotalReturnAmount = 0;
        let TotalOutStandingAmount = 0;
        let TotalGrossAmount = 0;
        // let TotalPatientAmount = 0;
        let TotalPaidAmount = 0;
        let TotalRefundAmount = 0;
        let TotalDiscountAmount = 0;
        let TotalBalAmount = 0;
        let TotalDueAmount = 0;
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: req.Data.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let EncounterInfo = encounterData.Data[0];
        let patReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientFilters.Id, Value: req.Data.PatientId }]
        };
        let patientData = await patientBo.GetPatients(patReq);
        let patientinfo = patientData.Data[0];

        for (let i = 0; i < PatientBillsWithReturn.length; i++) {
            let item = PatientBillsWithReturn[i];
            if (item.ReturnNumber && item.ReturnNumber !== undefined &&
                item.ReturnNumber !== null && item.ReturnNumber !== '') {
                item.ReturnSerialNo = ReturnSerialNo + 1;
                if (item.ReturnAmount === 0 && item.GrossAmount > 0) {
                    item.ReturnAmount = item.GrossAmount;
                }
                item.NetReturnAmount = (item.ReturnAmount || 0) - (item.DiscountAmount || 0);
                item.NetReturnAmount = Math.round(item.NetReturnAmount);
                TotalReturnAmount = (TotalReturnAmount || 0) + (item.NetReturnAmount || 0);
                if (item.IsReturnUsed === 0) {
                    TotalReturnAmount = TotalReturnAmount + item.NetReturnAmount;
                    item.select1 = true;
                    item.IsReturnUsed = 1;
                }
                TotalRefundAmount = TotalRefundAmount + item.NetReturnAmount;
                PatientPharmacyBillswithReturns.push(item);
                ReturnSerialNo++;
            } else if (item.PharmacyReturnTypeId !== 6) {
                item.SalesSerialNo = SalesSerialNo + 1;
                item.NetAmount = (item.BillAmount || 0) - (item.BillDiscount || 0);
                // item.NetAmount = Math.round(item.NetAmount);
                if (!item.BillDiscount || 0) {
                    item.BillDiscount = 0;
                }
                if (item.PharmacySaleTypeId === 6) {
                    if (!item.PaidAmount || 0) {
                        item.PaidAmount = 0;
                    }
                    // item.DueAmount = (item.NetAmount || 0) - (item.PaidAmount || 0);
                    //item.BillAmount = item.BillAmount + item.NetInsuranceAmount;
                } else {
                    if (!item.PaidAmount || 0) {
                        item.PaidAmount = 0;
                    }
                    item.DueAmount = (item.NetAmount || 0) - (item.PaidAmount || 0);
                }
                TotalGrossAmount = TotalGrossAmount + (item.BillAmount || 0);
                TotalSalesAmount = TotalSalesAmount + (item.NetAmount || 0);
                TotalDiscountAmount = TotalDiscountAmount + (item.BillDiscount || 0);
                TotalOutStandingAmount = TotalOutStandingAmount + (item.OutStandingAmount || 0);
                TotalDueAmount = TotalDueAmount + (item.DueAmount || 0);
                // TotalPatientAmount = TotalPatientAmount + item.NetPatientAmount;
                // TotalInsuranceAmount = item.NetInsuranceAmount + TotalInsuranceAmount;
                TotalPaidAmount = TotalPaidAmount + (item.PaidAmount || 0);
                PatientPharmacyBillswithReturns.push(item);
                SalesSerialNo++;
            }
        }
        TotalAmount = (TotalGrossAmount || 0) - (TotalReturnAmount || 0);
        TotalBalAmount = (TotalAmount || 0) - (TotalPaidAmount || 0);
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        // let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(facilityId);
        // let printStoreData =
        //     await storePreferenceBO.GetStorePreferenceWithLogo(facilityId, PatientBills.StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.pharmacyprintfooter)
        //     printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;

        let info = {
            Encounter: EncounterInfo,
            Patient: patientinfo,
            PatientPharmacyBillswithReturns: PatientPharmacyBillswithReturns,
            // PatientPharmacyBills: PatientPharmacyBills,
            Preferences: printPreferencesData,
            TotalAmount: TotalAmount,
            TotalSalesAmount: TotalSalesAmount,
            TotalReturnAmount: TotalReturnAmount,
            TotalGrossAmount: TotalGrossAmount,
            TotalOutStandingAmount: TotalOutStandingAmount,
            TotalDueAmount: TotalDueAmount,
            // TotalPatientAmount: TotalPatientAmount,
            TotalPaidAmount: TotalPaidAmount,
            // TotalInsuranceAmount: TotalInsuranceAmount,
            TotalBalAmount: TotalBalAmount,
        };

        let pdfOption: any = null;
        let key = 'pharmacyclearance';
        if (req.Data) {
            key = 'pharmacyclearance';
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'landscape',
                    border: '0',
                    header: {
                        height: '1.5in',
                        contents: '',
                    },
                    footer: {
                        height: '0.5in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintStaffBills(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data[0];
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        BillDetailsData.Data.sort(function (a, b) {
            return a.Id - b.Id;
        });
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            TotalCgst += Detail.CGstPercentage;
            TotalSgst += Detail.SGstPercentage;
            BillDetails.push(PatientBillDetail);
        });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Data.EncId }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];

        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBills.FacilityId, PatientBills.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;

        let storeReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: StoreMasterFilters.Id, Value: PatientBills.StoreMasterId }]
        };
        let storeBo = BoFactory.GetBo(StoreBo.StoreMasterBo, this.Request);
        let storeData = await storeBo.GetStoreMasters(storeReq);
        let Store = storeData.Data[0];

        let info = {
            Encounter: Encounter,
            PatientBills: PatientBills,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            BillDetails: BillDetails,
            NetAmount: (PatientBills.BillAmount - PatientBills.BillDiscount) + PatientBills.RoundOffValue,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            WithoutTax: PatientBills.BillAmount - PatientBills.GSTAmount,
            Preferences: printPreferencesData,
            StoreData: Store
        };

        let key = 'staffbill';
        let Watermark = '';
        let PrintTypeId: number;
        // if (info.PatientBills.PatientBillStatusId === 2) {
        //     Watermark = 'CANCELLED';
        //     PrintTypeId = 2;
        // }
        // if (info.PatientBills.PharmacySaleTypeId === 4) {
        //     key = 'directpharmacybill';
        // }
        // if (info.PatientBills.IsPharmacyBill === true
        //     && info.PatientBills.IsPaidFully === false
        //     && info.PatientBills.OutStandingAmount > 0) {
        //     key = 'pharmacybill';
        // }
        // if (req.Data.isprint) {
        //     key = 'pharmacysmall';
        //     PrintTypeId = 1;
        // }
        // if (info.StoreData.ISSeparatePayCounter === true) {
        //     key = 'pharmacycontactlensbill';
        // }
        let pdfOption: any = null;
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A5',
                // height: '5.8in',        // allowed units: mm, cm, in, px 5.8 x 8.3 in
                // width: '8.3in',            // allowed units: mm, cm, in, px Width x Height (inch)
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if (printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
        // // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }

    public async PrintPatientBillsByPatient(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.PatId, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data[0];
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            BillDetails.push(PatientBillDetail);
        });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);

        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData

        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            NetAmount: PatientBills.BillAmount - PatientBills.BillDiscount,
            Preferences: printPreferencesData
        };

        let key = 'outpatientbill';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'dgbilling';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        let pdfOption: any = null;
        // let reportKey = 'ippharmacyjson';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '1in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if (printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
        // return await Report.Generate(key, { header: {}, body: info });
        // let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        // return await ephBO.PrintReport(key
        //     , { header: {}, body: info }
        //     , {
        //         ObjectId: req.Id
        //         , ObjectTypeId: 4 /*Order*/
        //         , Reason: req.Data ? req.Data.Reason : null
        //         , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
        //         , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
        //     });
    }

    public async PrintPatientBillsWithoutHeader(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data[0];
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            BillDetails.push(PatientBillDetail);
        });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);

        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData

        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            NetAmount: PatientBills.BillAmount - PatientBills.BillDiscount,
            Preferences: printPreferencesData
        };

        let key = 'outpatientbillwithoutheader';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'dgbilling';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        // if (info.PatientBills.BillTypeId === 4) {
        //     key = 'pharmacybill';
        //     if (info.PatientBills.PharmacySaleTypeId === 4) {
        //         key = 'directpharmacybill';
        //     }
        //     if (req.Data.isprint) {
        //         key = 'pharmacysmall';
        //         PrintTypeId = 1;
        //         pdfOption = {
        //             format: 'A4',
        //             orientation: 'portrait',
        //             border: '0',
        //             header: {
        //                 height: '0.40in',
        //                 contents: '',
        //             },
        //             footer: {
        //                 height: '0in',
        //                 contents: {
        //                     first: '',
        //                     default: '',
        //                     last: '',
        //                 },
        //             },
        //             type: 'pdf',
        //             base: 'file://' + join(__dirname, '/../../Templates/assets/')
        //         };
        //     }
        // }
        // return await Report.Generate(key, { header: {}, body: info });
        let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        return await ephBO.PrintReport(key
            , { header: {}, body: info }
            , {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            });
    }

    public async PrintPatientDGBillsWithoutHeader(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data[0];
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            BillDetails.push(PatientBillDetail);
        });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);

        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData

        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            NetAmount: PatientBills.BillAmount - PatientBills.BillDiscount,
            Preferences: printPreferencesData
        };

        let key = 'outpatientbillwithoutheader';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'dgbillingwithoutheader';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        // if (info.PatientBills.BillTypeId === 4) {
        //     key = 'pharmacybill';
        //     if (info.PatientBills.PharmacySaleTypeId === 4) {
        //         key = 'directpharmacybill';
        //     }
        //     if (req.Data.isprint) {
        //         key = 'pharmacysmall';
        //         PrintTypeId = 1;
        //         pdfOption = {
        //             format: 'A4',
        //             orientation: 'portrait',
        //             border: '0',
        //             header: {
        //                 height: '0.40in',
        //                 contents: '',
        //             },
        //             footer: {
        //                 height: '0in',
        //                 contents: {
        //                     first: '',
        //                     default: '',
        //                     last: '',
        //                 },
        //             },
        //             type: 'pdf',
        //             base: 'file://' + join(__dirname, '/../../Templates/assets/')
        //         };
        //     }
        // }
        // return await Report.Generate(key, { header: {}, body: info });
        let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        return await ephBO.PrintReport(key
            , { header: {}, body: info }
            , {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            });
    }

    public async Printopcreditbill(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data[0];
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            BillDetails.push(PatientBillDetail);
        });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);

        //Get print preferences
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        //Preferences: printPreferencesData

        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data,
            Encounter: encounterData.Data[0],
            NetAmount: PatientBills.BillAmount - PatientBills.BillDiscount,
            Preferences: printPreferencesData
        };

        let key = 'outpatientcreditbill';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        if (info.PatientBills.BillTypeId === 5) {
            key = 'dgbilling';
            if (info.PatientBills.PatientBillStatusId === 2) {
                Watermark = 'CANCELLED';
                PrintTypeId = 2;
            }
        }
        // if (info.PatientBills.BillTypeId === 4) {
        //     key = 'pharmacybill';
        //     if (info.PatientBills.PharmacySaleTypeId === 4) {
        //         key = 'directpharmacybill';
        //     }
        //     if (req.Data.isprint) {
        //         key = 'pharmacysmall';
        //         PrintTypeId = 1;
        //         pdfOption = {
        //             format: 'A4',
        //             orientation: 'portrait',
        //             border: '0',
        //             header: {
        //                 height: '0.40in',
        //                 contents: '',
        //             },
        //             footer: {
        //                 height: '0in',
        //                 contents: {
        //                     first: '',
        //                     default: '',
        //                     last: '',
        //                 },
        //             },
        //             type: 'pdf',
        //             base: 'file://' + join(__dirname, '/../../Templates/assets/')
        //         };
        //     }
        // }
        // return await Report.Generate(key, { header: {}, body: info });
        let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
        return await ephBO.PrintReport(key
            , { header: {}, body: info }
            , {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            });
    }

    public async PopulateInpatientBills(req: BaseRequest): Promise<any> {
        let isFinalized: boolean = false;
        let patientFinalBillApiReq = {
            Id: 0,
            PageContext: { PageSize: 10000, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
            { Key: PatientBillsFilters.BillType, Value: 2 },
            { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let Encounter = await encounterBo.GetEncounterById({ Id: req.Id });

        let FinalBill = await this.GetIPBillPatientBills(patientFinalBillApiReq);
        if (FinalBill.Data.length > 0) {
            isFinalized = true;
            return { IsPackageAssigned: Encounter.IsPackageAssigned, Refreshed: isFinalized };
        }

        if (Encounter && Encounter.IsPackageAssigned) {
            let encounterIpPackageBo = BoFactory.GetBo(encbo.EncounterIPPackageBo, this.Request);
            let refreshed = await encounterIpPackageBo.ManagePackageBillInfo(req);
            return { IsPackageAssigned: Encounter.IsPackageAssigned, Refreshed: refreshed };
        } else {
            await this.PopulateRoomCharges(req);
            let patientBillApiReq = {
                Id: 0,
                PageContext: { PageSize: 10000, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 3 },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
            };
            let splitBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let SummaryBill: any = [];
            let ServiceCategoryIds: Array<number> = [];
            let SummaryBillMap: any = {};
            let PatientBills = await this.GetIPBillPatientBills(patientBillApiReq);
            await Promise.all(PatientBills.Data.map((billItem): Promise<void> => {
                return (async (bill): Promise<void> => {
                    let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
                    let billdetailReq = {
                        Id: 0,
                        PageContext: { PageSize: 1000, PageNumber: 1 },
                        Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: bill.Id },
                        { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 }]
                    };
                    let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
                    BillDetailsData.Data.forEach((Detail) => {

                        let billDetail = SummaryBillMap[Detail.ServiceCategoryId];
                        if (!billDetail) {
                            ServiceCategoryIds.push(Detail.ServiceCategoryId);
                            billDetail = {
                                Id: 0,
                                EncounterId: req.Id,
                                ServiceCategoryId: Detail.ServiceCategoryId,
                                ActualAmount: 0,
                                IsPharmacySale: Detail.IsPharmacySale,
                                PatientBillSplit: []
                            };
                            SummaryBillMap[Detail.ServiceCategoryId] = billDetail;
                        }
                        let NetAmt: any;
                        let PatNetAmt: any;
                        if (!Detail.IsSupplementary) {
                            NetAmt = Detail.NetAmount || 0;
                            PatNetAmt = 0;
                        }
                        if (Detail.IsSupplementary) {
                            NetAmt = Detail.InsNetAmount || 0;
                            PatNetAmt = Detail.PatNetAmount || 0;
                        }
                        billDetail.PatientBillSplit.push({
                            PatientBillId: bill.Id,
                            PatientBillDetailId: Detail.Id,
                            EncounterId: req.Id,
                            VisitGuarantorId: bill.GuarantorId,
                            BillDateTime: new Date(),
                            ServiceId: Detail.ServiceId,
                            ItemGrossAmt: Detail.GrossAmount,
                            ItemAmount: NetAmt,
                            ItemDiscount: !Detail.IsSupplementary ? Detail.DiscountAmount : 0,
                            SplitItemAmount: PatNetAmt,
                            SplitItemDiscount: Detail.IsSupplementary ? Detail.DiscountAmount : 0,
                            DoctorId: bill.DoctorId,
                            IsSplit: false,
                            DepartmentId: Detail.DepartmentId,
                            ServiceName: Detail.ServiceName,
                            IsSupplementary: Detail.IsSupplementary,
                            AliasId: Detail.AliasId,
                            AliasName: Detail.AliasName,
                            IsPharmacySale: Detail.IsPharmacySale
                        });
                    });
                })(billItem);
            }));
            ServiceCategoryIds.forEach((ServiceCategoryId) => {
                SummaryBill.push(SummaryBillMap[ServiceCategoryId]);
            });

            for (let i = 0, len = SummaryBill.length; i < len; i++) {
                var NetTotalCategoryAmount = 0;
                var GrossTotalCategoryAmount = 0;
                let TotCatDiscount = 0;
                for (let j = 0, len = SummaryBill[i].PatientBillSplit.length; j < len; j++) {
                    var netlinetotal = 0;
                    var grosslinetotal = 0;
                    let lineDisc = 0;
                    grosslinetotal = grosslinetotal +
                        Number(SummaryBill[i].PatientBillSplit[j].ItemGrossAmt);
                    GrossTotalCategoryAmount = GrossTotalCategoryAmount + grosslinetotal;
                    netlinetotal = netlinetotal +
                        Number(SummaryBill[i].PatientBillSplit[j].ItemAmount) +
                        Number(SummaryBill[i].PatientBillSplit[j].SplitItemAmount);
                    NetTotalCategoryAmount = NetTotalCategoryAmount + netlinetotal;
                    lineDisc = lineDisc +
                        Number(SummaryBill[i].PatientBillSplit[j].ItemDiscount) +
                        Number(SummaryBill[i].PatientBillSplit[j].SplitItemDiscount);
                    TotCatDiscount = TotCatDiscount + lineDisc;
                }
                SummaryBill[i].ActualAmount = GrossTotalCategoryAmount;
                SummaryBill[i].ActualNetAmount = NetTotalCategoryAmount;
                SummaryBill[i].ActualDiscount = TotCatDiscount;
            }
            if (SummaryBill.length > 0) {
                let splitResult = await splitBo.ManagePatientBillSummary(req.Id, SummaryBill);
                return { IsPackageAssigned: false, Refreshed: splitResult };
            } else {
                return { IsPackageAssigned: false, Refreshed: false };
            }
        }

    }
    public async PopulateUpdateBills(req: BaseRequest, details: any): Promise<any> {
        let PatBillDetails = details;
        let isFinalized: boolean = false;
        let patientFinalBillApiReq = {
            Id: 0,
            PageContext: { PageSize: 10000, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
            { Key: PatientBillsFilters.BillType, Value: 2 },
            { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let Encounter = await encounterBo.GetEncounterById({ Id: req.Id });

        let FinalBill = await this.GetIPBillPatientBills(patientFinalBillApiReq);
        if (FinalBill.Data.length > 0) {
            isFinalized = true;
            return { IsPackageAssigned: Encounter.IsPackageAssigned, Refreshed: isFinalized };
        }

        if (Encounter && Encounter.IsPackageAssigned) {
            let encounterIpPackageBo = BoFactory.GetBo(encbo.EncounterIPPackageBo, this.Request);
            let refreshed = await encounterIpPackageBo.ManagePackageBillInfo(req);
            return { IsPackageAssigned: Encounter.IsPackageAssigned, Refreshed: refreshed };
        } else {
            await this.PopulateRoomCharges(req);
            let patientBillApiReq = {
                Id: 0,
                PageContext: { PageSize: 10000, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 3 },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
            };
            let splitBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let SummaryBill: any = [];
            let ServiceCategoryIds: Array<number> = [];
            let SummaryBillMap: any = {};
            let PatientBills = await this.GetIPBillPatientBills(patientBillApiReq);
            await Promise.all(PatientBills.Data.map((billItem): Promise<void> => {
                return (async (bill): Promise<void> => {
                    let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
                    let billdetailReq = {
                        Id: 0,
                        PageContext: { PageSize: 1000, PageNumber: 1 },
                        Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: bill.Id },
                        { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 }]
                    };
                    await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
                    PatBillDetails.forEach((Detail: any) => {
                        let billDetail = SummaryBillMap[Detail.ServiceCategoryId];
                        if (!billDetail) {
                            ServiceCategoryIds.push(Detail.ServiceCategoryId);
                            billDetail = {
                                Id: 0,
                                EncounterId: req.Id,
                                ServiceCategoryId: Detail.ServiceCategoryId,
                                ActualAmount: 0,
                                IsPharmacySale: Detail.IsPharmacySale,
                                PatientBillSplit: []
                            };
                            SummaryBillMap[Detail.ServiceCategoryId] = billDetail;
                        }
                        let NetAmt: any;
                        let PatNetAmt: any;
                        if (!Detail.IsSupplementary) {
                            NetAmt = Detail.NetAmount || 0;
                            PatNetAmt = 0;
                        }
                        if (Detail.IsSupplementary) {
                            NetAmt = Detail.InsNetAmount || 0;
                            PatNetAmt = Detail.PatNetAmount || 0;
                        }
                        billDetail.PatientBillSplit.push({
                            PatientBillId: bill.Id,
                            PatientBillDetailId: Detail.Id,
                            EncounterId: req.Id,
                            VisitGuarantorId: bill.GuarantorId,
                            BillDateTime: new Date(),
                            ServiceId: Detail.ServiceId,
                            ItemAmount: NetAmt,
                            ItemDiscount: !Detail.IsSupplementary ? Detail.DiscountAmount : 0,
                            SplitItemAmount: PatNetAmt,
                            SplitItemDiscount: Detail.IsSupplementary ? Detail.DiscountAmount : 0,
                            DoctorId: bill.DoctorId,
                            IsSplit: false,
                            DepartmentId: Detail.DepartmentId,
                            ServiceName: Detail.ServiceName,
                            IsSupplementary: Detail.IsSupplementary,
                            AliasId: Detail.AliasId,
                            AliasName: Detail.AliasName,
                            IsPharmacySale: Detail.IsPharmacySale
                        });
                    });
                })(billItem);
            }));
            ServiceCategoryIds.forEach((ServiceCategoryId) => {
                SummaryBill.push(SummaryBillMap[ServiceCategoryId]);
            });

            for (let i = 0, len = SummaryBill.length; i < len; i++) {
                var TotalCategoryAmount = 0;
                for (let j = 0, len = SummaryBill[i].PatientBillSplit.length; j < len; j++) {
                    var linetotal = 0;
                    linetotal = linetotal +
                        Number(SummaryBill[i].PatientBillSplit[j].ItemAmount) +
                        Number(SummaryBill[i].PatientBillSplit[j].SplitItemAmount);
                    TotalCategoryAmount = TotalCategoryAmount + linetotal;
                }
                SummaryBill[i].ActualAmount = TotalCategoryAmount;
            }

            if (SummaryBill.length > 0) {
                let splitResult = await splitBo.ManagePatientBillSummary(req.Id, SummaryBill);
                return { IsPackageAssigned: false, Refreshed: splitResult };
            } else {
                return { IsPackageAssigned: false, Refreshed: false };
            }
        }

    }

    public async PopulateRoomCharges(req: BaseRequest): Promise<boolean> {
        let EncounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let OccupancyBo = BoFactory.GetBo(inpatientBO.BedOccupancyHistoryBo, this.Request);
        let BedBo = BoFactory.GetBo(generalMasterBO.WardRoomBedMasterBo, this.Request);
        let Roombo = BoFactory.GetBo(generalMasterBO.WardRoomMasterBo, this.Request);

        let EncounterInfo = await EncounterBo.GetEncounterById({ Id: req.Id });
        let BedInfo = await BedBo.GetWardRoomBedMasterById({ Id: EncounterInfo.BedId });
        let RoomInfo = await Roombo.GetWardRoomMasterById({ Id: EncounterInfo.RoomId });

        let facilityprebo = BoFactory.GetBo(appbo.FacilityPreferenceBo, this.Request);
        let facilityPreferencesData =
            await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);

        let iRoomRentInHourly = 0;
        let iRoomRentGraceMins = 0;
        let CoPayPercent = '';

        // let iRoomRentGraceHour = 12; // 12 hours - Half day room charge
        let daywiseseparatebilling = 0;
        if (facilityPreferencesData && facilityPreferencesData.bedchargesforindividualdays) {
            try {
                daywiseseparatebilling = parseInt(facilityPreferencesData.bedchargesforindividualdays);
            } catch (ex) { daywiseseparatebilling = 0; }
        }

        if (facilityPreferencesData && facilityPreferencesData.roomrentinhourly) {
            try {
                iRoomRentInHourly = parseInt(facilityPreferencesData.roomrentinhourly);
            } catch (ex) { iRoomRentInHourly = 0; }
        }
        /////iRoomRentGraceMins = BedInfo.GracePeriod;
        iRoomRentGraceMins = RoomInfo.GracePeriod;
        if (!iRoomRentInHourly) iRoomRentInHourly = 0;
        if (!iRoomRentGraceMins) iRoomRentGraceMins = 0;


        let ServiceRateCategoryId: number = 0;
        ServiceRateCategoryId = EncounterInfo.ServiceRateCategoryId;
        let IsInsuranceTraiff: boolean = false;
        console.log(EncounterInfo.GuarantorId);
        if (EncounterInfo.GuarantorId > 0) {
            // let patientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
            // let patientGuarantorData = await patientGuarantorBo.GetPatientGuarantorById({ Id: EncounterInfo.GuarantorId });

            // if (patientGuarantorData) {
            let guarantorBO = BoFactory.GetBo(generalMasterBO.GuarantorBo, this.Request);
            let guarantor = await guarantorBO.GetGuarantorById({ Id: EncounterInfo.GuarantorId });
            let GuarantorId_ = 1000;
            if (EncounterInfo.GuarantorTypeId > 1) {
                CoPayPercent = guarantor.CoPayPercent;
            }
            await guarantorBO.GetCurrentGuarantorId().then(function (result) {
                GuarantorId_ = result;
            });
            // if (guarantor.Id !== GuarantorId_) {
            if ((guarantor.Id !== GuarantorId_) && (!guarantor.IsIPBedTariff)) {
                IsInsuranceTraiff = true;
                ServiceRateCategoryId = guarantor.ServiceRateCategoryId;
            }
            // }
            console.log(IsInsuranceTraiff);
            console.log(ServiceRateCategoryId);
        }

        let OccupancyApiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: BedOccupancyHistoryFilters.EncounterId, Value: req.Id },
            { Key: BedOccupancyHistoryFilters.OccupancyStatus, Value: 1 }]
        };

        let OccupancyHistoryDetails = await OccupancyBo.GetBedOccupancyHistorys(OccupancyApiReq);
        for (let ioccuhis = 0, lenoccuhis = OccupancyHistoryDetails.Data.length;
            ioccuhis < lenoccuhis; ioccuhis++) {
            let OccupancyHistory = OccupancyHistoryDetails.Data[ioccuhis];
            let EndDate: any = OccupancyHistory.BillingEndDate === null ?
                new Date() : OccupancyHistory.BillingEndDate;

            if (OccupancyHistory.BillingEndDate === null
                && OccupancyHistory.BillingStartDate) {
                EndDate = OccupancyHistory.BillingStartDate;
            }
            console.log(iRoomRentGraceMins);
            if (iRoomRentGraceMins > 0) { // add grace minutes...
                EndDate = moment(EndDate).add(iRoomRentGraceMins, 'minutes').toDate();
            }
            console.log('***********Start Date**********');
            // console.log(moment(StartDate));
            console.log(moment(EndDate));
            console.log('***********End Date**********');
            let NoofUnits = 0;
            let NewPatientBillId = OccupancyHistory.PatientBillId;
            try {
                let hours = moment(new Date()).diff(moment(EndDate), 'hours');
                console.log('*************No of Hours************');
                console.log(hours);
                if (hours >= 0 && hours < 3
                    && moment(new Date()).isSameOrAfter(moment(EndDate))) {
                    // NoofUnits = (iRoomRentInHourly > 0) ? 0.5 : 1;
                    if (iRoomRentInHourly > 0) {
                        NoofUnits = 1;
                    }
                } else if (hours >= 3) {
                    NoofUnits = hours / 24;
                    NoofUnits = parseFloat(NoofUnits.toFixed(1));
                    if (iRoomRentInHourly > 0) {
                        let vnoofdays = NoofUnits.toFixed(1);
                        let vaynoofdays = vnoofdays.split('.');
                        if (vaynoofdays.length > 0) {
                            if (vaynoofdays[0]) {
                                NoofUnits = parseInt(vaynoofdays[0]);
                            }
                        }
                        if (vaynoofdays.length > 1) {
                            if (vaynoofdays[1]) {
                                if (parseInt(vaynoofdays[1]) >= 5) {
                                    NoofUnits++;
                                } else {
                                    NoofUnits += 1;
                                }
                            }
                        }
                    }
                    NoofUnits = (iRoomRentInHourly > 0) ? NoofUnits : Math.ceil(NoofUnits); // No of days calculation check....
                }
            } catch (ex) { console.log('Error room charge calculation  : ' + ex); }
            console.log('*************No of Units************');
            console.log(NoofUnits);
            if (NoofUnits > 0) {
                let OccupancyData: any = { Data: OccupancyHistory };
                await OccupancyBo.ManageBedOccupancyHistory(OccupancyData);//Update the Bed Occupancy History
                let BillReq: any = {
                    Data: {
                        NoofDays: NoofUnits > 0 ? NoofUnits : 1,
                        RoomId: OccupancyHistory.RoomId,
                        WardId: OccupancyHistory.WardId,
                        BedId: OccupancyHistory.BedId,
                        DoctorId: EncounterInfo.DoctorId,
                        DepartmentId: EncounterInfo.DepartmentId,
                        PatientId: EncounterInfo.PatientId,
                        FacilityId: OccupancyHistory.FacilityId,
                        GuarantorId: EncounterInfo.GuarantorId,
                        GuarantorTypeId: EncounterInfo.GuarantorTypeId,
                        ServiceRateCategoryId: IsInsuranceTraiff ? ServiceRateCategoryId : BedInfo.ServiceRateCategoryId,
                        EncounterId: EncounterInfo.Id,
                        IsPrimaryBed: OccupancyHistory.IsPrimaryBed,
                        IsDoubleOccupancy: OccupancyHistory.IsDoubleOccupancy,
                        CoPayPercent: CoPayPercent,
                        BillingStartDate: OccupancyHistory.BillingStartDate
                    }
                };
                if (!EncounterInfo.IsBillLock) {
                    console.log('Occupancy history patient bill id****');
                    console.log(NewPatientBillId);
                    if (NewPatientBillId && Number(NewPatientBillId) > 0) {
                        //Update PatientBillDetails  Increase The Quantity....
                        BillReq.Data.PatientBillId = NewPatientBillId;
                        BillReq.Data.OccupancyHistoryId = OccupancyHistory.Id;//newly added on 14/5/2024
                        await EncounterBo.ManageBedChargesBillInfo(BillReq);
                    } else {
                        BillReq.Data.OccupancyHistoryId = OccupancyHistory.Id;
                        NewPatientBillId = await EncounterBo.ManageEncounterBillInfo(BillReq);
                        let OccupancyData: any = {
                            Data: {
                                Id: OccupancyHistory.Id,
                                PatientBillId: NewPatientBillId,
                            }
                        };
                        await OccupancyBo.ManageBedOccupancyHistory(OccupancyData);
                    }
                }
            }
        }


        return true;
    }

    public async PopulateIPBillsWithoutAutoCharge(req: BaseRequest): Promise<any> {
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let Encounter = await encounterBo.GetEncounterById({ Id: req.Id });
        if (Encounter && Encounter.IsPackageAssigned) {
            let encounterIpPackageBo = BoFactory.GetBo(encbo.EncounterIPPackageBo, this.Request);
            let refreshed = await encounterIpPackageBo.ManagePackageBillInfo(req);
            return { IsPackageAssigned: Encounter.IsPackageAssigned, Refreshed: refreshed };
        } else {
            let patientBillApiReq = {
                Id: 0,
                PageContext: { PageSize: 1000, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 3 },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
            };
            let splitBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
            let SummaryBill: any = [];
            let ServiceCategoryIds: Array<number> = [];
            let SummaryBillMap: any = {};
            let PatientBills = await this.GetPatientBills(patientBillApiReq);
            await Promise.all(PatientBills.Data.map((billItem): Promise<void> => {
                return (async (bill): Promise<void> => {
                    let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
                    let billdetailReq = {
                        Id: 0,
                        PageContext: { PageSize: 1000, PageNumber: 1 },
                        Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: bill.Id },
                        { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 }]
                    };
                    let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
                    BillDetailsData.Data.forEach((Detail) => {

                        let billDetail = SummaryBillMap[Detail.ServiceCategoryId];
                        if (!billDetail) {
                            ServiceCategoryIds.push(Detail.ServiceCategoryId);
                            billDetail = {
                                Id: 0,
                                EncounterId: req.Id,
                                ServiceCategoryId: Detail.ServiceCategoryId,
                                ActualAmount: 0,
                                PatientBillSplit: []
                            };
                            SummaryBillMap[Detail.ServiceCategoryId] = billDetail;
                        }
                        billDetail.PatientBillSplit.push({
                            PatientBillId: bill.Id,
                            PatientBillDetailId: Detail.Id,
                            EncounterId: req.Id,
                            VisitGuarantorId: bill.GuarantorId,
                            BillDateTime: new Date(),
                            ServiceId: Detail.ServiceId,
                            ItemAmount: !Detail.IsSupplementary ? Detail.GrossAmount : 0,
                            ItemDiscount: !Detail.IsSupplementary ? Detail.DiscountAmount : 0,
                            SplitItemAmount: Detail.IsSupplementary ? Detail.GrossAmount : 0,
                            SplitItemDiscount: Detail.IsSupplementary ? Detail.DiscountAmount : 0,
                            DoctorId: bill.DoctorId,
                            IsSplit: false,
                            DepartmentId: Detail.DepartmentId,
                            ServiceName: Detail.ServiceName,
                            IsSupplementary: Detail.IsSupplementary,
                            AliasId: Detail.AliasId,
                            AliasName: Detail.AliasName
                        });
                    });
                })(billItem);
            }));
            ServiceCategoryIds.forEach((ServiceCategoryId) => {
                SummaryBill.push(SummaryBillMap[ServiceCategoryId]);
            });

            for (let i = 0, len = SummaryBill.length; i < len; i++) {
                var TotalCategoryAmount = 0;
                for (let j = 0, len = SummaryBill[i].PatientBillSplit.length; j < len; j++) {
                    var linetotal = 0;
                    linetotal = linetotal +
                        Number(SummaryBill[i].PatientBillSplit[j].ItemAmount) +
                        Number(SummaryBill[i].PatientBillSplit[j].SplitItemAmount);
                    TotalCategoryAmount = TotalCategoryAmount + linetotal;
                }
                SummaryBill[i].ActualAmount = TotalCategoryAmount;
            }

            if (SummaryBill.length > 0) {
                let splitResult = await splitBo.ManagePatientBillSummary(req.Id, SummaryBill);
                return { IsPackageAssigned: false, Refreshed: splitResult };
            } else {
                return { IsPackageAssigned: false, Refreshed: false };
            }
        }

    }

    public async ManagePatientTransferedBill(PatientBillId: number, req: BaseRequest): Promise<number> {

        req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId);
        req.Data.Header.BillDateTime = new Date();
        req.Data.Header.BillTypeId = 3;
        req.Data.Header.BillAmount = req.Data.Header.TransferAmount;
        req.Data.Header.RoundOffValue = 0;
        req.Data.Header.PaidAmount = 0;
        req.Data.Header.IsPaidFully = 0;
        req.Data.Header.ParentBillId = PatientBillId;
        req.Data.Header.PatientId = req.Data.Header.TransferPatientId;
        req.Data.Header.PatientName = '';
        req.Data.Header.EncounterId = req.Data.Header.TransferEncounterId;
        req.Data.Header.FamilyLinkId = 0;
        req.Data.Header.TransferEncounterId = 0;
        req.Data.Header.TransferPatientId = 0;
        req.Data.Header.TransferAmount = 0;
        req.Data.Header.GuarantorId = 0;
        req.Data.Header.GuarantorTypeId = 0;
        req.Data.Header.GuarantorName = '';
        req.Data.Header.DoctorId = 0;
        req.Data.Header.FSTypeId = 0;
        req.Data.Header.RoomId = 0;
        req.Data.Header.BedId = 0;
        req.Data.Header.WardId = 0;
        let SummData: any = [];
        if (!req.Data.Header.BillGeneratedBy) {
            req.Data.Header.BillGeneratedBy = this.Session.UserId;
        }
        if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
        let result = await this.Save(req.Data.Header);
        if (result) {
            let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let PatientBillId = result.dataValues.Id;

            let ServiceItemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
            let ServiceItemApiReq = {
                Id: 0,
                PageContext: { PageSize: 1000, PageNumber: 1 },
                Params: [{ Key: ServiceItemFilters.Code, Value: 'BLT001' }]
            };
            let ServiceItemDetails = await ServiceItemBo.GetServiceItems(ServiceItemApiReq);
            if (ServiceItemDetails.Data.length > 0) {
                for (let sx in ServiceItemDetails.Data) {
                    let detail = ServiceItemDetails.Data[sx];
                    let PatientBillDetail: any = {};
                    PatientBillDetail.PatientBillId = PatientBillId;
                    PatientBillDetail.BillDateTime = new Date();
                    PatientBillDetail.ServiceId = detail.Id;
                    PatientBillDetail.ServiceCode = detail.ItemCode;
                    PatientBillDetail.ServiceName = detail.Name;
                    PatientBillDetail.ServiceCategoryId = detail.CategoryId;
                    PatientBillDetail.EncounterId = req.Data.Header.EncounterId;
                    PatientBillDetail.PatientBillStatusId = 3;
                    PatientBillDetail.Quantity = 1;
                    PatientBillDetail.Rate = req.Data.Header.BillAmount;
                    PatientBillDetail.Amount = req.Data.Header.BillAmount;
                    PatientBillDetail.GrossAmount = req.Data.Header.BillAmount;
                    PatientBillDetail.NetAmount = req.Data.Header.BillAmount;
                    PatientBillDetail.DepartmentId = detail.DepartmentId;
                    PatientBillDetail.Status = 1;
                    PatientBillDetail.CreatedBy = req.Data.Header.CreatedBy;
                    PatientBillDetail.CreatedAt = new Date();
                    PatientBillDetail.UpdatedBy = req.Data.Header.UpdatedBy;
                    PatientBillDetail.UpdatedAt = new Date();
                    await detailBO.Save(PatientBillDetail);
                    SummData.push(PatientBillDetail);
                }
                let billSummBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
                await billSummBo.ManagePatBillSummary(false, req.Data.Header.EncounterId, SummData);
                // await Promise.all(ServiceItemDetails.Data.map((DetailItem): Promise<void> => {
                //     return (async (detail): Promise<void> => {

                //     })(DetailItem);
                // }));
            }
            return PatientBillId;
        }

        return 0;
    }
    public async UpdateIpParentId(req: BaseRequest): Promise<boolean> {
        let BillApiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Data.EncounterId },
            { Key: PatientBillsFilters.BillType, Value: 3 }, { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
        };
        let PatientBills = await this.GetMinPatientBills(BillApiReq);
        await Promise.all(PatientBills.Data.map((billItem): Promise<void> => {
            return (async (bill): Promise<void> => {
                bill.ParentBillId = req.Data.ParentBillId;
                await this.Update(bill);
            })(billItem);
        }));
        return req.Data.ParentBillId > 0;
    }
    // public async ManageIPBills(req: BaseRequest): Promise<number> {
    //     let PatientBillId: number = 0;
    //     let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
    //     let EncounterInfo = await encounterBo.GetEncounterById({ Id: req.Data.Header.EncounterId });
    //     let IPAutoDischarge = 0;
    //     if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
    //         if (req.Data.Header.BillTypeId === 1) {
    //             req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.OPBillNumberId, req.Data.Header.FacilityId);
    //         } else if (req.Data.Header.BillTypeId === 2) {
    //             req.Data.Header.BillNumber = null;

    //             let IPCreditFinalBillNr = 0;

    //             let facilityprebo = BoFactory.GetBo(appbo.FacilityPreferenceBo, this.Request);
    //             let facilityPreferencesData =
    //                 await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
    //             if (facilityPreferencesData && facilityPreferencesData.ipcreditfinalbillnr) {
    //                 try {
    //                     IPCreditFinalBillNr = parseInt(facilityPreferencesData.ipcreditfinalbillnr);
    //                 } catch (ex) { IPCreditFinalBillNr = 0; }

    //                 try {
    //                     IPAutoDischarge = parseInt(facilityPreferencesData.ipautodischarge);
    //                 } catch (ex) { IPAutoDischarge = 0; }
    //             }

    //             if (req.Data.Header.GuarantorTypeId
    //                 && req.Data.Header.GuarantorTypeId !== 1
    //                 && req.Data.Header.CreditApproved > 0
    //                 && IPCreditFinalBillNr > 0) {
    //                 req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPCreditFinalBillNumberId, req.Data.Header.FacilityId);
    //             }

    //             if (!req.Data.Header.BillNumber && !req.Data.Header.IsDayCare) {
    //                 req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPFinalBillNumberId,
    // req.Data.Header.FacilityId);
    //             }
    //             if (!req.Data.Header.BillNumber && req.Data.Header.IsDayCare) {
    //                 req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.DayCareIPFinalBillNumberId,
    // req.Data.Header.FacilityId);
    //             }

    //             if (req.Data.Header.FSTypeId === 1) {
    //                 if (req.Data.Header.OutStandingAmount === 0) {
    //                     req.Data.Header.IsPaidFully = 1;
    //                 }
    //                 req.Data.Header.PaidAmount = Number(req.Data.Header.PaidAmount);

    //                 if (req.Data.Header.OutStandingAmount !== 0) { // Amount Paid zero No need to insert payment table.
    //                     // req.Data.paymentDetail = [];

    //                     for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
    //                         if (req.Data.paymentDetail[i].Id <= 0) {
    //                             req.Data.paymentDetail[i].ReceiptNumber =
    //                                 await Sequence.Next(SequenceKeys.ReceiptNrId, req.Data.Header.FacilityId);
    //                             // req.Data.paymentDetail[i].AmountPaid = req.Data.Header.PaidAmount || req.Data.Header.Received;
    //                             req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
    //                             req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
    //                             req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
    //                             req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;
    //                         }
    //                     }
    //                 }
    //                 if (req.Data.Header.OutStandingAmount !== 0) {
    //                     req.Data.Header.OutStandingAmount = req.Data.Header.OutStandingAmount;
    //                 } else {
    //                     req.Data.Header.OutStandingAmount = 0;
    //                 }
    //             } else if (req.Data.Header.FSTypeId === 2) {
    //                 req.Data.Header.IsPaidFully = 0;
    //                 req.Data.Header.RefundAmount = req.Data.Header.RefundedAmount;
    //                 req.Data.Header.RefundAmt = req.Data.Header.OutStandingAmount;
    //                 req.Data.Header.OutStandingAmount = 0;
    //                 if (req.Data.Header.GuarantorTypeId > 1) {
    //                     req.Data.Header.CreditVocher = req.Data.Header.CreditApproved;
    //                     req.Data.Header.OutStandingAmount = req.Data.Header.CreditApproved;
    //                 }
    //             } else if (req.Data.Header.FSTypeId === 3) {
    //                 req.Data.Header.IsPaidFully = 0;
    //                 if (req.Data.Header.GuarantorTypeId === 1) {
    //                     req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
    //                 }
    //                 if (req.Data.Header.GuarantorTypeId > 1) {
    //                     req.Data.Header.CreditVocher = req.Data.Header.NetInsuranceAmount;
    //                     req.Data.Header.OutStandingAmount = req.Data.Header.NetInsuranceAmount;
    //                 }
    //                 //req.Data.Header.OutStandingAmount = 0;
    //             } else if (req.Data.Header.FSTypeId === 4) {
    //                 req.Data.Header.IsPaidFully = 1;
    //                 req.Data.Header.ToBeRefunded = req.Data.Header.ToBeRefunded;
    //                 req.Data.Header.OutStandingAmount = 0;
    //                 if (req.Data.Header.GuarantorTypeId > 1) {
    //                     req.Data.Header.CreditVocher = req.Data.Header.NetInsuranceAmount;
    //                     req.Data.Header.OutStandingAmount = req.Data.Header.NetInsuranceAmount;
    //                     if (req.Data.Header.CreditApproved > 0 && req.Data.Header.changeCreditVoucher === 1) {
    //                         req.Data.Header.CreditVocher = req.Data.Header.CreditApproved;
    //                         req.Data.Header.OutStandingAmount = req.Data.Header.CreditApproved;
    //                     }
    //                     if (req.Data.Header.OutStandingAmount > 0) {
    //                         req.Data.Header.IsPaidFully = 0;
    //                     }
    //                 }

    //             } else if (req.Data.Header.FSTypeId === 5) {
    //                 req.Data.Header.IsPaidFully = 1;
    //                 req.Data.Header.PaidAmount = req.Data.Header.PaidAmount + Number(req.Data.Header.Received);

    //                 if (req.Data.Header.OutStandingAmount === 0) // Amount Paid zero No need to insert payment table.
    //                     req.Data.paymentDetail = [];

    //                 for (let ii = 0, len1 = req.Data.paymentDetail.length; ii < len1; ii++) {
    //                     if (req.Data.paymentDetail[ii].Id <= 0) {
    //                         req.Data.paymentDetail[ii].ReceiptNumber =
    //                             await Sequence.Next(SequenceKeys.ReceiptNrId, req.Data.Header.FacilityId);
    //                         req.Data.paymentDetail[ii].AmountPaid = req.Data.Header.Received;
    //                         req.Data.paymentDetail[ii].FacilityId = req.Data.Header.FacilityId;
    //                         req.Data.paymentDetail[ii].GuarantorId = req.Data.Header.GuarantorId;
    //                         req.Data.paymentDetail[ii].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
    //                         req.Data.paymentDetail[ii].GuarantorName = req.Data.Header.GuarantorName;
    //                     }
    //                 }
    //                 req.Data.Header.OutStandingAmount = 0;
    //             }
    //         } else if (req.Data.Header.BillTypeId === 3)
    //             req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId, req.Data.Header.FacilityId);
    //     }
    //     req.Data.Header.ReferralId = EncounterInfo.ReferralId;
    //     req.Data.Header.ReferralName = EncounterInfo.ReferralName;
    //     // req.Data.Header.GuarantorTypeId = EncounterInfo.GuarantorTypeId;

    //     if (req.Data.Header.Id === 0) {
    //         // if (req.Data.Header.IsDirectDGBill === false || !req.Data.Header.IsDirectDGBill) {
    //         if (await this.IsFinalBillAlreadyExist(req) <= -1) return -1;
    //         // }
    //         if (!req.Data.Header.BillGeneratedBy) {
    //             req.Data.Header.BillGeneratedBy = this.Session.UserId;
    //         }
    //         if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
    //         let result = await this.Save(req.Data.Header);
    //         PatientBillId = result.dataValues.Id;
    //         // let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
    //         // await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
    //     } else {
    //         let result = await this.Update(req.Data.Header);
    //         if (result)
    //             PatientBillId = req.Data.Header.Id;
    //         // let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
    //         // await detailBO.ManagePatientBillDetails(req.Data.Header.Id, req.Data.Details);
    //     }
    //     if (PatientBillId > 0) {
    //         let paymentInfo = req.Data.PaymentTypeInfo;
    //         if (req.Data.Header.FSTypeId === 1 || req.Data.paymentDetail.length > 0) {
    //             if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
    //                 let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
    //                 await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
    //             }
    //         } else if (req.Data.Header.FSTypeId === 5) {
    //             if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
    //                 let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
    //                 await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
    //             }
    //         }
    //         if (req.Data.Header.FSTypeId === 2) {
    //             let refundBo = BoFactory.GetBo(billingBO.PatientRefundBo, this.Request);
    //             let refundDetails: any = [];
    //             refundDetails.push({
    //                 Id: 0,
    //                 RefundDetailsDateTime: new Date(),
    //                 // PatientRefundId: number;
    //                 FacilityId: req.Data.Header.FacilityId,
    //                 // OrganizationId: number;
    //                 PatientId: req.Data.Header.PatientId,
    //                 EncounterId: req.Data.Header.EncounterId,
    //                 EncounerTypeId: 2,
    //                 RefundAmount: req.Data.Header.RefundAmt,
    //                 // DepartmentID: number;
    //                 // PaymentcounterID: number;
    //                 // DoctorId: number;
    //                 // ServiceId: number;
    //                 // ServiceName: string;
    //                 PatientBillId: PatientBillId,
    //                 // PharmacyReturnId: number;
    //                 // Comments: string;
    //                 // RoundOffValue: number;
    //             });
    //             let refundInfo: any = {
    //                 Data: {
    //                     Header: {
    //                         Id: 0,
    //                         IsFromFinalize: true,
    //                         RefundDateTime: new Date(),
    //                         //RefundIdentifier: string;
    //                         FacilityId: req.Data.Header.FacilityId,
    //                         //OrganizationId: number;
    //                         PatientId: req.Data.Header.PatientId,
    //                         RefundTypeId: 4,
    //                         EncounterId: req.Data.Header.EncounterId,
    //                         EncounterTypeId: 2,
    //                         PatientName: req.Data.Header.PatientName,
    //                         // PatientReceiptId: number;
    //                         RefundAmount: req.Data.Header.RefundAmt,
    //                         // DepartmentID: number;
    //                         //  PaymentcounterID: number;
    //                         // GuarantorId: number;
    //                         // GuarantorTypeId: number;
    //                         RefundGeneratedById: req.Data.Header.BillGeneratedBy,
    //                         RefundApprovedById: req.Data.Header.BillGeneratedBy,
    //                         // DoctorId: req.Dat;
    //                         PatientBillId: PatientBillId,
    //                         //PharmacyReturnId: number;
    //                         CardNumber: paymentInfo.CardNumber,
    //                         // CardDateTime: paymentInfo.CardDateTime,
    //                         //CardExpiryDate: paymentInfo.CardExpiryDate,
    //                         BankId: paymentInfo.BankId,
    //                         CardTypeId: paymentInfo.CardTypeId,
    //                         TerminalNoId: paymentInfo.TerminalNoId,
    //                         //PatientCreditNoteId: paymentInfo.PatientCreditNoteId,
    //                         // CardHolderName: string;
    //                         // AuthorizeNumber: number;
    //                         // GurantorName: string;
    //                         ChequeNo: paymentInfo.ChequeNo,
    //                         ChequeDate: paymentInfo.ChequeDate,
    //                         DDNumber: paymentInfo.DDNumber,
    //                         DDDate: paymentInfo.DDDate,
    //                         WireTransferId: paymentInfo.WireTransferId,
    //                         WireTransferDate: paymentInfo.WireTransferDate,
    //                         // Comments: string;
    //                         //CancelReason: string;
    //                         RefundStatusId: 1,
    //                         IsRefundApprove: (req.Data.Header.IsRefundApprove) ? req.Data.Header.IsRefundApprove : 0,
    //                         PaymentTypeId: paymentInfo.PaymentTypeId
    //                         // RoundOffValue: number;
    //                         //DebitNoteId: number;
    //                         // Status: number;
    //                         //  Rev: number;
    //                         //  CreatedBy: number;
    //                         // CreatedAt: Date;
    //                         // UpdatedBy: number;
    //                         // UpdatedAt: Date; },

    //                     },
    //                     Details: refundDetails
    //                 }
    //             };
    //             await refundBo.AddPatientRefund(refundInfo);
    //         }

    //         //Patient Doctor Share Details Update
    //         let ShareApiReq = {
    //             Id: 0,
    //             PageContext: { PageSize: 1000, PageNumber: 1 },
    //             Params: [{ Key: PatientDoctorShareDetailsFilters.EncounterId, Value: req.Data.Header.EncounterId },
    //                 //{ Key: PatientDoctorShareDetailsFilters.PatientBillId, Value: 3 }
    //             ]
    //         };
    //         let docShareBO = BoFactory.GetBo(BillingBo.PatientDoctorShareDetailsBo, this.Request);
    //         let docShareDetails = await docShareBO.GetPatientDoctorShareDetails(ShareApiReq);
    //         await Promise.all(docShareDetails.Data.map((shareItem): Promise<void> => {
    //             return (async (sbill): Promise<void> => {
    //                 sbill.ParentBillId = PatientBillId;
    //                 await docShareBO.Update(sbill);
    //             })(shareItem);
    //         }));
    //         // let BillApiReq = {
    //         //     Id: 0,
    //         //     PageContext: { PageSize: 1000, PageNumber: 1 },
    //         //     Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Data.Header.EncounterId },
    //         //     { Key: PatientBillsFilters.BillType, Value: 3 }, { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
    //         // };
    //         // let PatientBills = await this.GetPatientBills(BillApiReq);
    //         // await Promise.all(PatientBills.Data.map((billItem): Promise<void> => {
    //         //     return (async (bill): Promise<void> => {
    //         //         bill.ParentBillId = PatientBillId;
    //         //         await this.Update(bill);
    //         //     })(billItem);
    //         // }));

    //         if (req.Data.Header.EncounterId > 0) {

    //             let dischargeEventBO = BoFactory.GetBo(inpatientBO.PatientDischargeEventBo, this.Request);
    //             let dischargeEventApiReq = {
    //                 Id: 0,
    //                 PageContext: { PageNumber: 1, PageSize: 1000 },
    //                 Params: [{ Key: 1, Value: req.Data.Header.EncounterId }]
    //             };
    //             let dischargeEventInfo: any = {};
    //             let dischargeEventsInfo = await dischargeEventBO.GetPatientDischargeEvents(dischargeEventApiReq);
    //             if (dischargeEventsInfo.Data.length > 0) {
    //                 dischargeEventInfo = dischargeEventsInfo.Data[0];
    //                 dischargeEventInfo.AdmissionStatusId = 5;
    //                 if (IPAutoDischarge > 0)
    //                     dischargeEventInfo.AdmissionStatusId = 6;

    //                 let dischargeEvent: any = { Data: dischargeEventInfo };
    //                 await dischargeEventBO.UpdatePatientDischargeEvent(dischargeEvent);
    //             } else {
    //                 dischargeEventInfo.EncounterId = req.Data.Header.EncounterId;
    //                 dischargeEventInfo.PatientId = req.Data.Header.PatientId;
    //                 dischargeEventInfo.AdmissionStatusId = 5;
    //                 if (IPAutoDischarge > 0)
    //                     dischargeEventInfo.AdmissionStatusId = 6;

    //                 let dischargeEvent: any = { Data: dischargeEventInfo };
    //                 await dischargeEventBO.AddPatientDischargeEvent(dischargeEvent);
    //             }

    //         }
    //         if (req.Data.Header.FSTypeId === 5) {
    //             await this.ManagePatientTransferedBill(PatientBillId, req);
    //         }
    //     }
    //     if (req.Data.Header.TeamId && req.Data.Header.TeamId > 0) {
    //         await this.GetDischargeTeamUsers(req.Data.Header.EncounterId, req.Data.Header.TeamId);
    //     }
    //     if (req.Data.Header.BillNumber && req.Data.Header.BillDiscount) {
    //         req.Data.Header.PatientBillId = PatientBillId;
    //         await this.SendBillDiscountSMS(req);
    //     }
    //     return PatientBillId;
    // }

    public async ManageIPBills(req: BaseRequest): Promise<number> {
        let PatientBillId: number = 0;
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let EncounterInfo = await encounterBo.GetEncounterById({ Id: req.Data.Header.EncounterId });
        let IPAutoDischarge = 0;
        let IPCreditFinalBillNr = 0;

        let facilityprebo = BoFactory.GetBo(appbo.FacilityPreferenceBo, this.Request);
        let facilityPreferencesData =
            await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
        if (facilityPreferencesData && facilityPreferencesData.ipcreditfinalbillnr) {
            try {
                IPCreditFinalBillNr = parseInt(facilityPreferencesData.ipcreditfinalbillnr);
            } catch (ex) { IPCreditFinalBillNr = 0; }

            try {
                IPAutoDischarge = parseInt(facilityPreferencesData.ipautodischarge);
            } catch (ex) { IPAutoDischarge = 0; }
        }

        if (!req.Data.Header.BillNumber && req.Data.Header.PatientBillStatusId === 3) {
            if (req.Data.Header.BillTypeId === 1) {
                // req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.OPBillNumberId, req.Data.Header.FacilityId);
            } else if (req.Data.Header.BillTypeId === 2) {


                // if (req.Data.Header.GuarantorTypeId
                //     && req.Data.Header.GuarantorTypeId !== 1
                //     && req.Data.Header.CreditApproved > 0
                //     && IPCreditFinalBillNr > 0) {
                //     req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPCreditFinalBillNumberId,
                // req.Data.Header.FacilityId);
                // }

                // if (!req.Data.Header.BillNumber && !req.Data.Header.IsDayCare) {
                //     req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPFinalBillNumberId,
                // req.Data.Header.FacilityId);
                // }
                // if (!req.Data.Header.BillNumber && req.Data.Header.IsDayCare) {
                //     req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.DayCareIPFinalBillNumberId,
                // req.Data.Header.FacilityId);
                // }

                if (req.Data.Header.FSTypeId === 1) {
                    if (Number(req.Data.Header.OutStandingAmount) === 0) {
                        req.Data.Header.IsPaidFully = 1;
                    }
                    req.Data.Header.PaidAmount = Number(req.Data.Header.PaidAmount);

                    if (Number(req.Data.Header.OutStandingAmount) !== 0) { // Amount Paid zero No need to insert payment table.
                        // req.Data.paymentDetail = [];

                        for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                            if (req.Data.paymentDetail[i].Id <= 0) {
                                // req.Data.paymentDetail[i].ReceiptNumber =
                                // await Sequence.Next(SequenceKeys.ReceiptNrId, req.Data.Header.FacilityId);
                                // req.Data.paymentDetail[i].AmountPaid = req.Data.Header.PaidAmount || req.Data.Header.Received;
                                req.Data.paymentDetail[i].FacilityId = req.Data.Header.FacilityId;
                                req.Data.paymentDetail[i].GuarantorId = req.Data.Header.GuarantorId;
                                req.Data.paymentDetail[i].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                                req.Data.paymentDetail[i].GuarantorName = req.Data.Header.GuarantorName;
                            }
                        }
                    }
                    if (req.Data.Header.OutStandingAmount !== 0) {
                        req.Data.Header.OutStandingAmount = req.Data.Header.OutStandingAmount;
                    } else {
                        req.Data.Header.OutStandingAmount = 0;
                    }
                } else if (req.Data.Header.FSTypeId === 2) {
                    req.Data.Header.IsPaidFully = 0;
                    req.Data.Header.RefundAmount = req.Data.Header.RefundedAmount;
                    req.Data.Header.RefundAmt = req.Data.Header.OutStandingAmount;
                    req.Data.Header.OutStandingAmount = 0;
                    if (req.Data.Header.GuarantorTypeId > 1) {
                        req.Data.Header.CreditVocher = req.Data.Header.CreditApproved;
                        req.Data.Header.OutStandingAmount = req.Data.Header.CreditApproved;
                    }
                } else if (req.Data.Header.FSTypeId === 3) {
                    req.Data.Header.IsPaidFully = 0;
                    if (req.Data.Header.GuarantorTypeId === 1) {
                        req.Data.Header.CreditVocher = req.Data.Header.OutStandingAmount;
                    }
                    if (req.Data.Header.GuarantorTypeId > 1) {
                        req.Data.Header.CreditVocher = req.Data.Header.NetInsuranceAmount;
                        req.Data.Header.OutStandingAmount = req.Data.Header.NetInsuranceAmount;
                    }
                    //req.Data.Header.OutStandingAmount = 0;
                } else if (req.Data.Header.FSTypeId === 4) {
                    req.Data.Header.IsPaidFully = 1;
                    req.Data.Header.ToBeRefunded = Number(req.Data.Header.ToBeRefunded);
                    // req.Data.Header.OutStandingAmount = 0;
                    if (req.Data.Header.ToBeRefunded && req.Data.Header.ToBeRefunded > 0) {
                        if (req.Data.Header.GuarantorTypeId > 1) {
                            req.Data.Header.CreditVocher = Number(req.Data.Header.NetInsuranceAmount);
                            // req.Data.Header.OutStandingAmount = req.Data.Header.NetInsuranceAmount;
                            if (Number(req.Data.Header.CreditApproved) > 0 && req.Data.Header.changeCreditVoucher === 1) {
                                req.Data.Header.CreditVocher = Number(req.Data.Header.CreditApproved);
                                req.Data.Header.OutStandingAmount = Number(req.Data.Header.CreditApproved);
                            } else {
                                if (parseFloat(req.Data.Header.CreditApproved) !== 0) {
                                    req.Data.Header.OutStandingAmount = Number(req.Data.Header.NetInsuranceAmount);
                                }
                            }
                            if (Number(req.Data.Header.OutStandingAmount) > 0) {
                                req.Data.Header.IsPaidFully = 0;
                            }
                        }

                        // if (Number(req.Data.Header.OutStandingAmount) > 0) {
                        //     req.Data.Header.IsPaidFully = 0;
                        // }
                    }

                } else if (req.Data.Header.FSTypeId === 5) {
                    req.Data.Header.IsPaidFully = 1;
                    req.Data.Header.PaidAmount = req.Data.Header.PaidAmount + Number(req.Data.Header.Received);

                    if (req.Data.Header.OutStandingAmount === 0) // Amount Paid zero No need to insert payment table.
                        req.Data.paymentDetail = [];

                    for (let ii = 0, len1 = req.Data.paymentDetail.length; ii < len1; ii++) {
                        if (req.Data.paymentDetail[ii].Id <= 0) {
                            // req.Data.paymentDetail[ii].ReceiptNumber =
                            // await Sequence.Next(SequenceKeys.ReceiptNrId, req.Data.Header.FacilityId);
                            req.Data.paymentDetail[ii].AmountPaid = req.Data.Header.Received;
                            req.Data.paymentDetail[ii].FacilityId = req.Data.Header.FacilityId;
                            req.Data.paymentDetail[ii].GuarantorId = req.Data.Header.GuarantorId;
                            req.Data.paymentDetail[ii].GuarantorTypeId = req.Data.Header.GuarantorTypeId;
                            req.Data.paymentDetail[ii].GuarantorName = req.Data.Header.GuarantorName;
                        }
                    }
                    req.Data.Header.OutStandingAmount = 0;
                }
            }
            // else if (req.Data.Header.BillTypeId === 3)
            // req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.IPBillNumberId, req.Data.Header.FacilityId);
        }
        req.Data.Header.ReferralId = EncounterInfo.ReferralId;
        req.Data.Header.ReferralName = EncounterInfo.ReferralName;
        // req.Data.Header.GuarantorTypeId = EncounterInfo.GuarantorTypeId;

        if (req.Data.Header.Id === 0) {
            // if (req.Data.Header.IsDirectDGBill === false || !req.Data.Header.IsDirectDGBill) {
            if (await this.IsFinalBillAlreadyExist(req) <= -1) return -1;
            // }
            if (!req.Data.Header.BillGeneratedBy) {
                req.Data.Header.BillGeneratedBy = this.Session.UserId;
            }
            if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
            let result = await this.Save(req.Data.Header);
            PatientBillId = result.dataValues.Id;


            try {
                if (PatientBillId && PatientBillId > 0) {
                    let parentData: any = {
                        Data: {
                            EncounterId: req.Data.Header.EncounterId,
                            ParentBillId: PatientBillId
                        }
                    };
                    await this.UpdateIpParentId(parentData);
                } else
                    throw { message: 'Final Bill not generated' };
            } catch (ex) {
                console.log(ex);
            }
            // let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            // await detailBO.ManagePatientBillDetails(PatientBillId, req.Data.Details);
        } else {
            let result = await this.Update(req.Data.Header);
            if (result)
                PatientBillId = req.Data.Header.Id;
            // let detailBO = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            // await detailBO.ManagePatientBillDetails(req.Data.Header.Id, req.Data.Details);
        }
        if (PatientBillId > 0) {
            let paymentInfo = req.Data.PaymentTypeInfo;
            if (req.Data.Header.FSTypeId === 1 || req.Data.paymentDetail.length > 0) {
                if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                    let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                    await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                }
            } else if (req.Data.Header.FSTypeId === 5) {
                if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                    let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
                    await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
                }
            }

            if (req.Data.Header.FSTypeId === 2) {
                let refundBo = BoFactory.GetBo(billingBO.PatientRefundBo, this.Request);
                let refundDetails: any = [];
                refundDetails.push({
                    Id: 0,
                    RefundDetailsDateTime: new Date(),
                    // PatientRefundId: number;
                    FacilityId: req.Data.Header.FacilityId,
                    // OrganizationId: number;
                    PatientId: req.Data.Header.PatientId,
                    EncounterId: req.Data.Header.EncounterId,
                    EncounerTypeId: 2,
                    RefundAmount: req.Data.Header.RefundAmt,
                    // DepartmentID: number;
                    // PaymentcounterID: number;
                    // DoctorId: number;
                    // ServiceId: number;
                    // ServiceName: string;
                    PatientBillId: PatientBillId,
                    // PharmacyReturnId: number;
                    // Comments: string;
                    // RoundOffValue: number;
                });
                let refundInfo: any = {
                    Data: {
                        Header: {
                            Id: 0,
                            IsFromFinalize: true,
                            RefundDateTime: new Date(),
                            //RefundIdentifier: string;
                            FacilityId: req.Data.Header.FacilityId,
                            //OrganizationId: number;
                            PatientId: req.Data.Header.PatientId,
                            RefundTypeId: 4,
                            EncounterId: req.Data.Header.EncounterId,
                            EncounterTypeId: 2,
                            PatientName: req.Data.Header.PatientName,
                            // PatientReceiptId: number;
                            RefundAmount: req.Data.Header.RefundAmt,
                            // DepartmentID: number;
                            //  PaymentcounterID: number;
                            // GuarantorId: number;
                            // GuarantorTypeId: number;
                            RefundGeneratedById: req.Data.Header.BillGeneratedBy,
                            RefundApprovedById: req.Data.Header.BillGeneratedBy,
                            // DoctorId: req.Dat;
                            PatientBillId: PatientBillId,
                            //PharmacyReturnId: number;
                            CardNumber: paymentInfo.CardNumber,
                            // CardDateTime: paymentInfo.CardDateTime,
                            //CardExpiryDate: paymentInfo.CardExpiryDate,
                            BankId: paymentInfo.BankId,
                            CardTypeId: paymentInfo.CardTypeId,
                            TerminalNoId: paymentInfo.TerminalNoId,
                            //PatientCreditNoteId: paymentInfo.PatientCreditNoteId,
                            // CardHolderName: string;
                            // AuthorizeNumber: number;
                            // GurantorName: string;
                            ChequeNo: paymentInfo.ChequeNo,
                            ChequeDate: paymentInfo.ChequeDate,
                            DDNumber: paymentInfo.DDNumber,
                            DDDate: paymentInfo.DDDate,
                            WireTransferId: paymentInfo.WireTransferId,
                            WireTransferDate: paymentInfo.WireTransferDate,
                            // Comments: string;
                            //CancelReason: string;
                            RefundStatusId: 1,
                            IsRefundApprove: (req.Data.Header.IsRefundApprove) ? req.Data.Header.IsRefundApprove : 0,
                            PaymentTypeId: paymentInfo.PaymentTypeId
                            // RoundOffValue: number;
                            //DebitNoteId: number;
                            // Status: number;
                            //  Rev: number;
                            //  CreatedBy: number;
                            // CreatedAt: Date;
                            // UpdatedBy: number;
                            // UpdatedAt: Date; },

                        },
                        Details: refundDetails
                    }
                };
                await refundBo.AddPatientRefund(refundInfo);
            }

            //Patient Doctor Share Details Update
            let ShareApiReq = {
                Id: 0,
                PageContext: { PageSize: 1000, PageNumber: 1 },
                Params: [{ Key: PatientDoctorShareDetailsFilters.EncounterId, Value: req.Data.Header.EncounterId },
                    //{ Key: PatientDoctorShareDetailsFilters.PatientBillId, Value: 3 }
                ]
            };
            let docShareBO = BoFactory.GetBo(BillingBo.PatientDoctorShareDetailsBo, this.Request);
            let docShareDetails = await docShareBO.GetPatientDoctorShareDetails(ShareApiReq);
            await Promise.all(docShareDetails.Data.map((shareItem): Promise<void> => {
                return (async (sbill): Promise<void> => {
                    sbill.ParentBillId = PatientBillId;
                    await docShareBO.Update(sbill);
                })(shareItem);
            }));
            if (req.Data.Header.EncounterId > 0) {
                let dischargeEventBO = BoFactory.GetBo(inpatientBO.PatientDischargeEventBo, this.Request);
                let dischargeEventApiReq = {
                    Id: 0,
                    PageContext: { PageNumber: 1, PageSize: 1000 },
                    Params: [{ Key: 1, Value: req.Data.Header.EncounterId }]
                };
                let dischargeEventInfo: any = {};
                let dischargeEventsInfo = await dischargeEventBO.GetPatientDischargeEvents(dischargeEventApiReq);
                if (dischargeEventsInfo.Data.length > 0) {
                    dischargeEventInfo = dischargeEventsInfo.Data[0];
                    dischargeEventInfo.AdmissionStatusId = 5;
                    if (IPAutoDischarge > 0)
                        dischargeEventInfo.AdmissionStatusId = 6;

                    let dischargeEvent: any = { Data: dischargeEventInfo };
                    await dischargeEventBO.UpdatePatientDischargeEvent(dischargeEvent);
                } else {
                    dischargeEventInfo.EncounterId = req.Data.Header.EncounterId;
                    dischargeEventInfo.PatientId = req.Data.Header.PatientId;
                    dischargeEventInfo.AdmissionStatusId = 5;
                    if (IPAutoDischarge > 0)
                        dischargeEventInfo.AdmissionStatusId = 6;

                    let dischargeEvent: any = { Data: dischargeEventInfo };
                    await dischargeEventBO.AddPatientDischargeEvent(dischargeEvent);
                }

            }
            if (req.Data.Header.FSTypeId === 5) {
                await this.ManagePatientTransferedBill(PatientBillId, req);
            }
            //Bill Sequence Generation
            let billIdentifier: string | null = null;
            let facilityId: number = (req.Data.Header.FacilityId) ? req.Data.Header.FacilityId : this.Session.FacilityId;
            const isCredit = req.Data.Header.GuarantorTypeId && req.Data.Header.GuarantorTypeId !== 1 && req.Data.Header.CreditApproved > 0;

            if (req.Data.Header.BillTypeId === 1) {
                // billIdentifier = this.getSequenceIdentifier(SequenceKeys.OPBillNumberId);
                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.OPBillNumberId, facilityId);
            } else if (req.Data.Header.BillTypeId === 2) {
                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPFinalBillNumberId, facilityId);

                if (isCredit && IPCreditFinalBillNr > 0) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPCreditFinalBillNumberId, facilityId);
                } else if (!req.Data.Header.BillNumber && !req.Data.Header.IsDayCare) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPFinalBillNumberId, facilityId);
                } else if (!req.Data.Header.BillNumber && req.Data.Header.IsDayCare) {
                    billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.DayCareIPFinalBillNumberId, facilityId);
                }
            } else if (req.Data.Header.BillTypeId === 3) {
                billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId, facilityId);
            }

            if (billIdentifier) {
                // 2. Get DB max value
                //     const query = `SELECT MAX(CAST(SUBSTRING_INDEX(BillNumber, '/', -1) AS UNSIGNED)) AS MaxBillNumber
                // FROM patientbills
                //             WHERE BillNumber LIKE 'IPCR/2526/%'`;
                //     let maxnumber: any = await this.Dal.query(query);
                //     console.log(maxnumber);
                const afterO: any = () => {
                    return ((bo, request, PatientBillId) => {
                        return {

                            SendBillDiscountSMS: async (code: string) => {
                                try {
                                    request.Data.Header.PatientBillId = PatientBillId;
                                    request.Data.BillNum = code;
                                    await bo.SendBillDiscountSMS(request);
                                } catch (error) {
                                    console.error('Error in SendBillDiscountSMS:', error);
                                    throw error;  // Handle the error as needed
                                }
                            }
                        };
                    })(this, req, PatientBillId);
                };


                const handleDeferredExecution = async () => {
                    try {
                        // Assuming deferSequenceKey processes a list of functions
                        await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
                            [
                                afterO().SendBillDiscountSMS,
                            ]);
                    } catch (error) {
                        // console.error('Error in deferSequenceKey execution:', error);
                        throw error;
                    }
                };

                await handleDeferredExecution();

            }


        }
        if (req.Data.Header.TeamId && req.Data.Header.TeamId > 0) {
            await this.GetDischargeTeamUsers(req.Data.Header.EncounterId, req.Data.Header.TeamId);
        }
        // if (req.Data.Header.BillNumber && req.Data.Header.BillDiscount) {
        //     req.Data.Header.PatientBillId = PatientBillId;
        //     await this.SendBillDiscountSMS(req);
        // }
        return PatientBillId;
    }

    // private async generateBillNumber(header: any, CreditFinalBillNr: number, patientbillId: number): Promise<any> {

    //     let billIdentifier: string | null = null;
    //     const IPCreditFinalBillNr = CreditFinalBillNr;
    //     let facilityId: number = (header.FacilityId) ? header.FacilityId : this.Session.FacilityId;
    //     const isCredit = header.GuarantorTypeId && header.GuarantorTypeId !== 1 && header.CreditApproved > 0;


    //     if (header.BillTypeId === 1) {
    //         // billIdentifier = this.getSequenceIdentifier(SequenceKeys.OPBillNumberId);
    //         billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.OPBillNumberId, facilityId);
    //     } else if (header.BillTypeId === 2) {
    //         billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPFinalBillNumberId, facilityId);

    //         if (isCredit && IPCreditFinalBillNr > 0) {
    //             billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPCreditFinalBillNumberId, facilityId);
    //         } else if (!header.BillNumber && !header.IsDayCare) {
    //             billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPFinalBillNumberId, facilityId);
    //         } else if (!header.BillNumber && header.IsDayCare) {
    //             billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.DayCareIPFinalBillNumberId, facilityId);
    //         }
    //     } else if (header.BillTypeId === 3) {
    //         billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId, facilityId);
    //     }
    //     // else if (req.Data.Header.BillTypeId === 3) {
    //     //     billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.IPBillNumberId, facilityId);
    //     // } else if (req.Data.Header.BillTypeId === 4) {
    //     //     billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.PharmacyBillNumberId, facilityId);
    //     // } else if (req.Data.Header.BillTypeId === 5) {
    //     //     billIdentifier = this.getFacilitySequenceIdentifier(SequenceKeys.DGBillNumberId, facilityId);
    //     // }

    //     if (billIdentifier) {
    //         const afterO: any = () => {
    //             return ((bo, request, PatientBillId) => {
    //                 return {

    //                     SendBillDiscountSMS: async (code: string) => {
    //                         try {
    //                             request.Data.Header.PatientBillId = PatientBillId;
    //                             request.Data.BillNum = code;
    //                             await bo.SendBillDiscountSMS(request);
    //                         } catch (error) {
    //                             console.error('Error in SendBillDiscountSMS:', error);
    //                             throw error;  // Handle the error as needed
    //                         }
    //                     }
    //                 };
    //             })(this, req, patientbillId);
    //         };


    //         const handleDeferredExecution = async () => {
    //             try {
    //                 // Assuming deferSequenceKey processes a list of functions
    //                 await this.deferSequenceKey(PatientBillId, 'BillNumber', billIdentifier,
    //                     [
    //                         afterO().SendBillDiscountSMS,
    //                     ]);
    //             } catch (error) {
    //                 // console.error('Error in deferSequenceKey execution:', error);
    //                 throw error;
    //             }
    //         };

    //         handleDeferredExecution();

    //     }
    //     // return { BillNumber, IPAutoDischarge };
    // }

    public async GetDischargeTeamUsers(EncId: number, TeamId: number): Promise<boolean> {
        let userBo = BoFactory.GetBo(userbo.UserBo, this.Request);
        let usersbo = BoFactory.GetBo(userbo.UserTeamBo, this.Request);
        let TeamIdApiReq = {
            Id: 0,
            PageContext: { PageSize: 10000000, PageNumber: 1 },
            Params: [{ Key: 4, Value: TeamId }]
        };
        let TeamUserIdsInfo = await usersbo.GetUserTeams(TeamIdApiReq);
        if (TeamUserIdsInfo) {
            await Promise.all(TeamUserIdsInfo.map((TeamUserIds): Promise<void> => {
                return (async (Teamuserinfo): Promise<void> => {
                    if (Teamuserinfo && Teamuserinfo.UserId) {
                        let UserIdApiReq = {
                            Id: 0,
                            PageContext: { PageSize: 10000000, PageNumber: 1 },
                            Params: [{ Key: 0, Value: Teamuserinfo.UserId }]
                        };
                        let userdetailinfo = await userBo.GetUsers(UserIdApiReq);
                        if (userdetailinfo && userdetailinfo.Data && userdetailinfo.Data.length > 0) {
                            for (var idx in userdetailinfo.Data) {
                                let userdetail = userdetailinfo.Data[idx];
                                if (userdetail && userdetail.Mobile) {
                                    //console.log(userdetail.Mobile);
                                    let UserName = '';
                                    let Title = await this.GetTitle(userdetail.TitleId);
                                    if (Title) UserName += Title;
                                    if (userdetail.FirstName) UserName += ' ' + userdetail.FirstName;
                                    if (userdetail.LastName) UserName += ' ' + userdetail.LastName;
                                    let MobileNr = userdetail.Mobile;
                                    await this.SendDischargeSMS(EncId, MobileNr, UserName);
                                }
                            }
                        }
                    }
                })(TeamUserIds);
            }));
        }
        return true;
    }

    public async GetTitle(TitleId: number): Promise<string> {
        let refTitleBo = BoFactory.GetBo(userbo.ReferenceValueBo, this.Request);
        let vTitleName = '';
        let apiReqTitle = {
            Id: 0,
            PageContext: { PageSize: 50, PageNumber: 1 },
            Params: [
                { Key: ReferenceValueFilters.ReferenceGroupCode, Value: 'Title' },
                { Key: ReferenceValueFilters.ReferenceValueCodeId, Value: TitleId }
            ]
        };
        let TitleData = await refTitleBo.GetReferenceValues(apiReqTitle);
        if (TitleData.Data) {
            if (TitleData.Data.length > 0) {
                if (TitleData.Data[0].Description)
                    vTitleName = TitleData.Data[0].Description;
            }
        }
        return vTitleName;
    }

    public async SendDischargeSMS(EncId: number, MobileNr: string, UserName: string): Promise<boolean> {
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encdata = await encounterBo.GetEncounterById({ Id: EncId });
        if (encdata) {
            let PatientName = ''; let MRN = ''; let Age = 0; let Gender = '';
            let DisplayWard = ''; let DisplayRoom = ''; let DisplayBed = '';
            let DischargeDate = ''; let DischargeTime = '';
            let dateformat = 'DD/MM/YYYY';
            let timeformat = 'HH:mm:ss';
            if (encdata.DischargeDate === null) {
                let dischargedate = new Date();
                DischargeDate = moment(dischargedate).format(dateformat);
                DischargeTime = moment(dischargedate).format(timeformat);
            }
            MRN = encdata.PatientMrn;
            if (MobileNr) {
                let regBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
                let patientData = await regBo.GetPatientById({ Id: encdata.PatientId });
                Age = patientData.Age;
                let appBO = BoFactory.GetBo(apnmtBO.AppointmentBo, this.Request);
                PatientName = await appBO.getPatientName(patientData);
                Gender = await appBO.getPatientGender(patientData);
                let userBO = BoFactory.GetBo(userbo.UserBo, this.Request);
                let UserData = await userBO.GetUserById({ Id: encdata.DoctorId });
                let wardMasterBO = BoFactory.GetBo(generalMasterBo.WardMasterBo, this.Request);
                let warddata = await wardMasterBO.GetWardMasterById({ Id: encdata.WardId });
                DisplayWard = warddata.Description;
                let wardRoomMasterBO = BoFactory.GetBo(generalMasterBo.WardRoomMasterBo, this.Request);
                let roomData = await wardRoomMasterBO.GetWardRoomMasterById({ Id: encdata.RoomId });
                DisplayRoom = roomData.Description;
                let wardRoomBedMasterBO = BoFactory.GetBo(generalMasterBo.WardRoomBedMasterBo, this.Request);
                let bedData = await wardRoomBedMasterBO.GetWardRoomBedMasterById({ Id: encdata.BedId });
                DisplayBed = bedData.Description;
                let eventTemplateBO = BoFactory.GetBo(userbo.EventTemplateBo, this.Request);
                let smsTemplateInfo =
                    await eventTemplateBO.GetTemplateInfo('Discharge', 'PatientDischarge', 1);
                if (smsTemplateInfo) {
                    let vDocName = '';
                    vDocName = await this.getUserName(UserData);
                    let smsmodel = {
                        numbers: [MobileNr],
                        message: Template.Compile(smsTemplateInfo.TemplateContent,
                            {
                                userName: UserName,
                                doctorName: vDocName,
                                patientName: PatientName,
                                mrn: MRN,
                                age: Age, gender: Gender,
                                displayward: DisplayWard,
                                displayroom: DisplayRoom,
                                displaybed: DisplayBed,
                                displaydate: DischargeDate,
                                displaytime: DischargeTime
                            })
                    };
                    let smsProvider = this.GetSmsProvider();
                    if (smsProvider) {
                        let SMSStatus = await smsProvider.send(smsmodel);
                        let eventDashboardOutboundBo = BoFactory.GetBo(userbo.EventDashboardBo, this.Request);
                        await eventDashboardOutboundBo.ManageSMSOutBound(SMSStatus, smsmodel.message + ' To : ' + MobileNr);
                    }
                }
            }
        }
        return true;
    }

    public async getUserName(UserData: UserAttributes): Promise<string> {
        let vDocName = '';
        if (UserData) {
            if (UserData.FirstName) vDocName += UserData.FirstName;
            if (UserData.LastName) vDocName += ' ' + UserData.LastName;
            let apiReqTitle = {
                Id: 0,
                PageContext: { PageSize: 50, PageNumber: 1 },
                Params: [
                    { Key: ReferenceValueFilters.ReferenceGroupCode, Value: 'Title' },
                    { Key: ReferenceValueFilters.ReferenceValueCodeId, Value: UserData.TitleId }
                ]
            };
            let vTitleName = '';
            let refTitleBo = BoFactory.GetBo(userbo.ReferenceValueBo, this.Request);
            let TitleData = await refTitleBo.GetReferenceValues(apiReqTitle);
            if (TitleData.Data) {
                if (TitleData.Data.length > 0) {
                    if (TitleData.Data[0].Description)
                        vTitleName = TitleData.Data[0].Description;
                }
            }
            if (vTitleName)
                vDocName = vTitleName + '.' + vDocName;
        }
        return vDocName;
    }

    public async GetPendingOrders(Req: BaseRequest): Promise<any> {
        let PendingOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
        let apiReq = {
            Id: 0,
            PageContext: { PageNumber: 1, PageSize: 1000 },
            Params: [{ Key: PatientOrderDetailFilters.PatientOrderId, Value: Req.Id }]
        };
        let PendingOrders = await PendingOrderDetailBo.GetPatientOrderDetails(apiReq);
        let ServiceItems: any = [];
        await Promise.all(PendingOrders.Data.map((pendingitem): Promise<void> => {
            return (async (testitem): Promise<void> => {
                let serviceItemReq = {
                    Id: 0,
                    PageContext: { PageNumber: 1, PageSize: 1000 },
                    Params: [{ Key: ServiceItemFilters.MasterItemId, Value: testitem.TestId }]
                };
                let clinicalBO = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
                let ClinicalData = await clinicalBO.GetServiceItems(serviceItemReq);
                ServiceItems.push(ClinicalData.Data[0]);
            })(pendingitem);
        }));
        return ServiceItems;
    }

    public async GetPendingProcedureOrders(detailIds: any): Promise<any> {
        let PendingOrderDetailBo = BoFactory.GetBo(emrbo.ProcedureOrderDetailBo, this.Request);
        let apiReq = {
            Id: 0,
            PageContext: { PageNumber: 1, PageSize: 1000 },
            Params: [
                { Key: ProcedureOrderDetailFilters.ProcedureOrderId, Value: detailIds.Id }
            ]
        };
        let PendingOrders = await PendingOrderDetailBo.GetProcedureOrderDetails(apiReq);
        let ServiceItems: any = [];
        await Promise.all(PendingOrders.Data.map((pendingitem): Promise<void> => {
            return (async (serviceitem): Promise<void> => {
                let serviceItemReq = {
                    Id: 0,
                    PageContext: { PageNumber: 1, PageSize: 1000 },
                    Params: [{ Key: ServiceItemFilters.Id, Value: serviceitem.ServiceItemId }]
                };
                let clinicalBO = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
                let ClinicalData = await clinicalBO.GetServiceItems(serviceItemReq);
                // ClinicalData.Data[0].OrderId = Req.Id;
                // ClinicalData.Data[0].OrderDetailId = serviceitem.Id;
                // ClinicalData.Data[0].ToothNo = serviceitem.ToothNo;
                ServiceItems.push(ClinicalData.Data[0]);
            })(pendingitem);
        }));
        return ServiceItems;
    }

    public async GetMultiPendingOrders(detailIds: any): Promise<any> {
        let PendingOrderDetailBo = BoFactory.GetBo(emrbo.PatientOrderDetailBo, this.Request);
        let apiReq = {
            Id: 0,
            PageContext: { PageNumber: 1, PageSize: 1000 },
            Params: [{ Key: PatientOrderDetailFilters.PatientOrderId, Value: detailIds.Id }]
        };
        let PendingOrders = await PendingOrderDetailBo.GetPatientOrderDetails(apiReq);
        let ServiceItems: any = [];
        await Promise.all(PendingOrders.Data.map((pendingitem): Promise<void> => {
            return (async (testitem): Promise<void> => {
                let serviceItemReq = {
                    Id: 0,
                    PageContext: { PageNumber: 1, PageSize: 1000 },
                    Params: [{ Key: ServiceItemFilters.MasterItemId, Value: testitem.TestId }]
                };
                let clinicalBO = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
                let ClinicalData = await clinicalBO.GetServiceItems(serviceItemReq);
                ServiceItems.push(ClinicalData.Data[0]);
            })(pendingitem);
        }));
        return ServiceItems;
    }

    public async GetPendingPrescriptions(req: BaseRequest): Promise<any> {
        let PrescriptionDetailBo = BoFactory.GetBo(emrbo.PrescriptionDetailBo, this.Request);
        let apiReq = {
            Id: 0,
            PageContext: { PageNumber: 1, PageSize: 1000 },
            Params: [{ Key: PrescriptionDetailFilters.PrescriptionId, Value: req.Id }]
        };
        let Prescriptions = await PrescriptionDetailBo.GetPrescriptionDetails(apiReq);
        let PharmacyItems: any = [];
        await Promise.all(Prescriptions.Data.map((prescriptionitem): Promise<void> => {
            return (async (prescribeditem): Promise<void> => {
                let pharmacyItemReq = {
                    Id: 0,
                    PageContext: { PageNumber: 1, PageSize: 1000 },
                    Params: [{ Key: ItemMasterFilters.DrugId, Value: prescribeditem.DrugId }]
                };
                let itemmasterBO = BoFactory.GetBo(inventoryBO.ItemMasterBo, this.Request);
                let ItemMasterData = await itemmasterBO.GetItemMasters(pharmacyItemReq);
                PharmacyItems.push(ItemMasterData.Data[0]);
            })(prescriptionitem);
        }));
        return PharmacyItems;
    }

    public async ConsolidatePharmacyPayment(req: BaseRequest): Promise<boolean> {
        let BillDetails: Array<PatientBillsAttributes> = req.Data.Bills;
        let PaymentDetail: any = req.Data.PaymentDetail;
        let IsAdvanceAdjusting = 0;
        let AdjustedAgainstBill = 0;
        let PatientBillId = 0;
        let PaymentBo = BoFactory.GetBo(billingBO.PatientPaymentDetailsBo, this.Request);
        if (req.Data.Header.FSTypeId === 1) {
            if (req.Data.Header.Id === 0) {
                req.Data.Header.BillDateTime = new Date();
                req.Data.Header.BillNumber = await Sequence.Next(SequenceKeys.PharmacyBillNumberId,
                    req.Data.Header.FacilityId
                );
                if (!req.Data.Header.BillGeneratedBy) {
                    req.Data.Header.BillGeneratedBy = this.Session.UserId;
                }
                if (!req.Data.Header.FacilityId) req.Data.Header.FacilityId = this.Session.FacilityId;
                let result = await this.Save(req.Data.Header);
                PatientBillId = result.dataValues.Id;
                // if (req.Data.Header.BillTypeId === 4) {
                //     billIdentifier = this.getSequenceIdentifier(SequenceKeys.PharmacyBillNumberId);
                // }
            }

            await Promise.all(BillDetails.map((Bills): Promise<void> => {
                return (async (bill: any): Promise<void> => {
                    if (bill.DueAmount > 0) {
                        let OutStandingAmount = bill.DueAmount;
                        bill.OutStandingAmount = 0;
                        bill.IsPaidFully = true;
                        bill.PaidAmount = bill.PaidAmount + OutStandingAmount;
                        //bill.PaidAmount = Math.ceil(bill.PaidAmount);
                        bill.IsConsolidatePay = true;
                        bill.IsPharmacyAdjusted = true;
                        await this.Update(bill);
                    }
                })(Bills);
            }));
        }
        if (PatientBillId > 0) {
            // PaymentDetail = req.Data.PaymentDetail;
            if (req.Data.Header.FSTypeId === 1 || req.Data.paymentDetail.length > 0) {
                if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
                    // await PaymentBo.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, PaymentDetail);
                    let PatientPaymentDetail: any = {
                        Id: 0,
                        ReceiptDateTime: new Date(),
                        FacilityId: req.Data.Header.FacilityId,
                        OrganizationId: req.Data.Header.OrganizationId,
                        PatientId: req.Data.Header.PatientId,
                        ReceiptTypeId: 2,
                        EncounterId: req.Data.Header.EncounterId,
                        EncounterTypeId: req.Data.Header.EncounterTypeId,
                        PatientName: req.Data.Header.PatientName,
                        OutStandingAmount: 0,
                        AmountPaid: req.Data.Header.PaidAmount,
                        DueAmount: 0,
                        // DepartmentID: bill.DepartmentId,
                        // PaymentcounterID: 0,
                        // GuarantorId: bill.GuarantorId,
                        // GuarantorTypeId: bill.GuarantorTypeId,
                        // DoctorId: bill.DoctorId,
                        // ServiceId: bill.ServiceRateCategoryId,
                        // ServiceName: bill.ServiceRateCategoryName,
                        // PatientBillId: bill.Id,
                        // GurantorName: bill.GuarantorName,
                        ReceiptStatusId: 1,
                        TDSAmount: 0.00,
                        Disallowance: 0.00,
                        PaymentStatusId: 3,
                        CollectedOn: new Date(),
                        IsConsolidatePay: true,
                        IsPharmacyReceipt: false,
                        IsPharmacyClearance: true
                    };
                    PatientPaymentDetail.TerminalNoId = PaymentDetail.TerminalNoId;
                    PatientPaymentDetail.BankId = PaymentDetail.BankId;
                    PatientPaymentDetail.CardTypeId = PaymentDetail.CardTypeId;
                    PatientPaymentDetail.ChequeNo = PaymentDetail.ChequeNo;
                    PatientPaymentDetail.ChequeDate = PaymentDetail.ChequeDate;
                    PatientPaymentDetail.DDNumber = PaymentDetail.DDNumber;
                    PatientPaymentDetail.DDDate = PaymentDetail.DDDate;
                    PatientPaymentDetail.WireTransferId = PaymentDetail.WireTransferId;
                    PatientPaymentDetail.WireTransferDate = PaymentDetail.WireTransferDate;
                    PatientPaymentDetail.ReceiptGeneratedById = PaymentDetail.ReceiptGeneratedById;
                    PatientPaymentDetail.ReceiptApprovedById = PaymentDetail.ReceiptApprovedById;
                    PatientPaymentDetail.PaymentTypeId = PaymentDetail.PaymentTypeId;
                    PatientPaymentDetail.AuthorizedCode = PaymentDetail.AuthorizedCode;
                    PatientPaymentDetail.Comments = PaymentDetail.Comments;
                    if (PatientPaymentDetail.PaymentTypeId === 7) { //if Payment type is adjustment receipttype should be 6
                        PatientPaymentDetail.ReceiptTypeId = 6;
                    }
                    PatientPaymentDetail.ReceiptNumber = await Sequence.Next(SequenceKeys.ReceiptNrId,
                        req.Data.Header.FacilityId
                    );
                    await PaymentBo.Save(PatientPaymentDetail);
                }
            }
            // else if (req.Data.Header.FSTypeId === 2) {
            //     let refundBo = BoFactory.GetBo(billingBO.PatientRefundBo, this.Request);
            //     let refundDetails: any = [];
            //     refundDetails.push({
            //         Id: 0,
            //         RefundDetailsDateTime: new Date(),
            //         // PatientRefundId: number;
            //         FacilityId: req.Data.Header.FacilityId,
            //         // OrganizationId: number;
            //         PatientId: req.Data.Header.PatientId,
            //         EncounterId: req.Data.Header.EncounterId,
            //         EncounerTypeId: 2,
            //         RefundAmount: req.Data.Header.RefundAmount,
            //         // DepartmentID: number;
            //         // PaymentcounterID: number;
            //         // DoctorId: number;
            //         // ServiceId: number;
            //         // ServiceName: string;
            //         // PatientBillId: PatientBillId,
            //         // PharmacyReturnId: number;
            //         // Comments: string;
            //         // RoundOffValue: number;
            //     });
            //     let refundInfo: any = {
            //         Data: {
            //             Header: {
            //                 Id: 0,
            //                 IsFromFinalize: true,
            //                 RefundDateTime: new Date(),
            //                 //RefundIdentifier: string;
            //                 FacilityId: req.Data.Header.FacilityId,
            //                 //OrganizationId: number;
            //                 PatientId: req.Data.Header.PatientId,
            //                 RefundTypeId: 4,
            //                 EncounterId: req.Data.Header.EncounterId,
            //                 EncounterTypeId: 2,
            //                 PatientName: req.Data.Header.PatientName,
            //                 // PatientReceiptId: number;
            //                 RefundAmount: req.Data.Header.RefundAmount,
            //                 // DepartmentID: number;
            //                 //  PaymentcounterID: number;
            //                 // GuarantorId: number;
            //                 // GuarantorTypeId: number;
            //                 RefundGeneratedById: req.Data.Header.BillGeneratedBy,
            //                 RefundApprovedById: req.Data.Header.BillGeneratedBy,
            //                 // DoctorId: req.Dat;
            //                 PatientBillId: PatientBillId,
            //                 //PharmacyReturnId: number;
            //                 CardNumber: req.Data.Header.CardNumber,
            //                 // CardDateTime: paymentInfo.CardDateTime,
            //                 //CardExpiryDate: paymentInfo.CardExpiryDate,
            //                 BankId: req.Data.Header.BankId,
            //                 CardTypeId: req.Data.Header.CardTypeId,
            //                 TerminalNoId: req.Data.Header.TerminalNoId,
            //                 //PatientCreditNoteId: paymentInfo.PatientCreditNoteId,
            //                 // CardHolderName: string;
            //                 // AuthorizeNumber: number;
            //                 // GurantorName: string;
            //                 ChequeNo: req.Data.Header.ChequeNo,
            //                 ChequeDate: req.Data.Header.ChequeDate,
            //                 DDNumber: req.Data.Header.DDNumber,
            //                 DDDate: req.Data.Header.DDDate,
            //                 WireTransferId: req.Data.Header.WireTransferId,
            //                 WireTransferDate: req.Data.Header.WireTransferDate,
            //                 // Comments: string;
            //                 //CancelReason: string;
            //                 RefundStatusId: 1,

            //                 PaymentTypeId: req.Data.Header.PaymentTypeId
            //                 // RoundOffValue: number;
            //                 //DebitNoteId: number;
            //                 // Status: number;
            //                 //  Rev: number;
            //                 //  CreatedBy: number;
            //                 // CreatedAt: Date;
            //                 // UpdatedBy: number;
            //                 // UpdatedAt: Date; },

            //             },
            //             Details: refundDetails
            //         }
            //     };
            //     await refundBo.AddPatientRefund(refundInfo);
            // }
            // else if (req.Data.Header.FSTypeId === 5) {
            //     if (req.Data.Header.Id <= 0 && req.Data.Header.PatientBillStatusId === 3) {
            //         let PaymentdetailBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
            //         await PaymentdetailBO.ManagePatientPaymentDetails(PatientBillId, req.Data.Header, req.Data.paymentDetail);
            //     }
            // }

        }
        if (req.Data.Header.FSTypeId === 2) {
            let refundBo = BoFactory.GetBo(billingBO.PatientRefundBo, this.Request);
            let refundDetails: any = [];
            refundDetails.push({
                Id: 0,
                RefundDetailsDateTime: new Date(),
                // PatientRefundId: number;
                FacilityId: req.Data.Header.FacilityId,
                // OrganizationId: number;
                PatientId: req.Data.Header.PatientId,
                EncounterId: req.Data.Header.EncounterId,
                EncounerTypeId: 2,
                RefundAmount: req.Data.Header.RefundAmount,
                // DepartmentID: number;
                // PaymentcounterID: number;
                // DoctorId: number;
                // ServiceId: number;
                // ServiceName: string;
                // PatientBillId: PatientBillId,
                // PharmacyReturnId: number;
                // Comments: string;
                // RoundOffValue: number;
            });
            let refundInfo: any = {
                Data: {
                    Header: {
                        Id: 0,
                        IsFromFinalize: true,
                        RefundDateTime: new Date(),
                        //RefundIdentifier: string;
                        FacilityId: req.Data.Header.FacilityId,
                        //OrganizationId: number;
                        PatientId: req.Data.Header.PatientId,
                        RefundTypeId: 5,
                        EncounterId: req.Data.Header.EncounterId,
                        EncounterTypeId: 2,
                        PatientName: req.Data.Header.PatientName,
                        // PatientReceiptId: number;
                        RefundAmount: req.Data.Header.RefundAmount,
                        // DepartmentID: number;
                        //  PaymentcounterID: number;
                        // GuarantorId: number;
                        // GuarantorTypeId: number;
                        RefundGeneratedById: req.Data.Header.BillGeneratedBy,
                        RefundApprovedById: req.Data.Header.BillGeneratedBy,
                        // DoctorId: req.Dat;
                        PatientBillId: PatientBillId,
                        //PharmacyReturnId: number;
                        CardNumber: PaymentDetail.CardNumber,
                        // CardDateTime: paymentInfo.CardDateTime,
                        //CardExpiryDate: paymentInfo.CardExpiryDate,
                        BankId: PaymentDetail.BankId,
                        CardTypeId: PaymentDetail.CardTypeId,
                        TerminalNoId: PaymentDetail.TerminalNoId,
                        //PatientCreditNoteId: paymentInfo.PatientCreditNoteId,
                        // CardHolderName: string;
                        // AuthorizeNumber: number;
                        // GurantorName: string;
                        ChequeNo: PaymentDetail.ChequeNo,
                        ChequeDate: PaymentDetail.ChequeDate,
                        DDNumber: PaymentDetail.DDNumber,
                        DDDate: PaymentDetail.DDDate,
                        WireTransferId: PaymentDetail.WireTransferId,
                        WireTransferDate: PaymentDetail.WireTransferDate,
                        // Comments: string;
                        //CancelReason: string;
                        RefundStatusId: 1,

                        PaymentTypeId: PaymentDetail.PaymentTypeId
                        // RoundOffValue: number;
                        //DebitNoteId: number;
                        // Status: number;
                        //  Rev: number;
                        //  CreatedBy: number;
                        // CreatedAt: Date;
                        // UpdatedBy: number;
                        // UpdatedAt: Date; },

                    },
                    Details: refundDetails
                }
            };
            await refundBo.AddPatientRefund(refundInfo);
        }
        if (req.Data.ReturnBills) {
            if (req.Data.ReturnBills.length > 0) {
                let BillDetails: Array<PatientReturnsAttributes> = req.Data.ReturnBills;
                let PatientreturnBO = BoFactory.GetBo(billingBO.PatientReturnsBo, this.Request);
                //await PaymentadjustmentBO.ManagePatientPaymentAdjustments(BillDetails[0].Id, req.Data.adjustmentDetail);
                await Promise.all(BillDetails.map((Bills): Promise<void> => {
                    return (async (bill: any): Promise<void> => {
                        if (bill.IsReturnUsed === 1) {
                            await PatientreturnBO.Update(bill);
                        }
                    })(Bills);
                }));
            }
        }
        if (req.Data.adjustmentDetail) {
            if (req.Data.adjustmentDetail.length > 0) {
                IsAdvanceAdjusting = 1;
                AdjustedAgainstBill = Number(req.Data.PaymentDetail.Received);

                // for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                //     if (req.Data.paymentDetail[i].Id > 0) {
                //         for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                //             req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                //         }
                //     }
                // }

                let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                await PaymentadjustmentBO.ManagePatientPaymentAdjustments(BillDetails[0].Id, req.Data.adjustmentDetail);
            }
        }
        return true;
    }

    public async ConsolidatePayment(req: BaseRequest): Promise<boolean> {
        let BillDetails: Array<PatientBillsAttributes> = req.Data.Bills;
        let PaymentDetail: any = req.Data.PaymentDetail;
        let IsAdvanceAdjusting = 0;
        let AdjustedAgainstBill = 0;
        let PaymentBo = BoFactory.GetBo(billingBO.PatientPaymentDetailsBo, this.Request);
        await Promise.all(BillDetails.map((Bills): Promise<void> => {
            return (async (bill: any): Promise<void> => {
                if (bill.DueAmount > 0) {
                    let OutStandingAmount = bill.DueAmount;
                    bill.OutStandingAmount = 0;
                    bill.IsPaidFully = true;
                    bill.PaidAmount = bill.PaidAmount + OutStandingAmount;
                    //bill.PaidAmount = Math.ceil(bill.PaidAmount);
                    bill.IsConsolidatePay = true;
                    await this.Update(bill);
                    let PatientPaymentDetail: any = {
                        Id: 0,
                        ReceiptDateTime: new Date(),
                        FacilityId: bill.FacilityId,
                        OrganizationId: bill.OrganizationId,
                        PatientId: bill.PatientId,
                        ReceiptTypeId: 2,
                        EncounterId: bill.EncounterId,
                        EncounterTypeId: bill.EncounterTypeId,
                        PatientName: bill.PatientName,
                        OutStandingAmount: OutStandingAmount,
                        AmountPaid: OutStandingAmount,
                        DueAmount: 0,
                        DepartmentID: bill.DepartmentId,
                        PaymentcounterID: 0,
                        GuarantorId: bill.GuarantorId,
                        GuarantorTypeId: bill.GuarantorTypeId,
                        DoctorId: bill.DoctorId,
                        ServiceId: bill.ServiceRateCategoryId,
                        ServiceName: bill.ServiceRateCategoryName,
                        PatientBillId: bill.Id,
                        GurantorName: bill.GuarantorName,
                        ReceiptStatusId: 1,
                        TDSAmount: 0.00,
                        Disallowance: 0.00,
                        PaymentStatusId: 3,
                        CollectedOn: new Date(),
                        IsConsolidatePay: true,
                        IsPharmacyReceipt: true
                    };
                    PatientPaymentDetail.TerminalNoId = PaymentDetail.TerminalNoId;
                    PatientPaymentDetail.BankId = PaymentDetail.BankId;
                    PatientPaymentDetail.CardTypeId = PaymentDetail.CardTypeId;
                    PatientPaymentDetail.ChequeNo = PaymentDetail.ChequeNo;
                    PatientPaymentDetail.ChequeDate = PaymentDetail.ChequeDate;
                    PatientPaymentDetail.DDNumber = PaymentDetail.DDNumber;
                    PatientPaymentDetail.DDDate = PaymentDetail.DDDate;
                    PatientPaymentDetail.WireTransferId = PaymentDetail.WireTransferId;
                    PatientPaymentDetail.WireTransferDate = PaymentDetail.WireTransferDate;
                    PatientPaymentDetail.ReceiptGeneratedById = PaymentDetail.ReceiptGeneratedById;
                    PatientPaymentDetail.ReceiptApprovedById = PaymentDetail.ReceiptApprovedById;
                    PatientPaymentDetail.PaymentTypeId = PaymentDetail.PaymentTypeId;
                    PatientPaymentDetail.AuthorizedCode = PaymentDetail.AuthorizedCode;
                    PatientPaymentDetail.Comments = PaymentDetail.Comments;

                    PatientPaymentDetail.ReceiptNumber = await Sequence.Next(SequenceKeys.ReceiptNrId,
                        req.Data.Header.FacilityId
                    );
                    await PaymentBo.Save(PatientPaymentDetail);
                }
            })(Bills);
        }));
        if (req.Data.ReturnBills) {
            if (req.Data.ReturnBills.length > 0) {
                let BillDetails: Array<PatientReturnsAttributes> = req.Data.ReturnBills;
                let PatientreturnBO = BoFactory.GetBo(billingBO.PatientReturnsBo, this.Request);
                //await PaymentadjustmentBO.ManagePatientPaymentAdjustments(BillDetails[0].Id, req.Data.adjustmentDetail);
                await Promise.all(BillDetails.map((Bills): Promise<void> => {
                    return (async (bill: any): Promise<void> => {
                        if (bill.IsReturnUsed === 1) {
                            await PatientreturnBO.Update(bill);
                        }
                    })(Bills);
                }));
            }
        }
        if (req.Data.adjustmentDetail) {
            if (req.Data.adjustmentDetail.length > 0) {
                IsAdvanceAdjusting = 1;
                AdjustedAgainstBill = Number(req.Data.PaymentDetail.Received);

                // for (let i = 0, len = req.Data.paymentDetail.length; i < len; i++) {
                //     if (req.Data.paymentDetail[i].Id > 0) {
                //         for (let j = 0, len = req.Data.adjustmentDetail.length; j < len; j++) {
                //             req.Data.adjustmentDetail[j].PatientReceiptId = req.Data.paymentDetail[i].Id;
                //         }
                //     }
                // }

                let PaymentadjustmentBO = BoFactory.GetBo(billingBO.PatientPaymentAdjustmentsBo, this.Request);
                await PaymentadjustmentBO.ManagePatientPaymentAdjustments(BillDetails[0].Id, req.Data.adjustmentDetail);
            }
        }
        return true;
    }
    // public async ConsolidatePayment(req: BaseRequest): Promise<boolean> {
    //     let BillDetails: Array<PatientBillsAttributes> = req.Data.Bills;
    //     let PaymentDetail: any = req.Data.PaymentDetail;
    //     let PaymentBo = BoFactory.GetBo(billingBO.PatientPaymentDetailsBo, this.Request);
    //     await Promise.all(BillDetails.map((Bills): Promise<void> => {
    //         return (async (bill): Promise<void> => {
    //             let OutStandingAmount = bill.OutStandingAmount;
    //             bill.OutStandingAmount = 0;
    //             bill.IsPaidFully = true;
    //             bill.PaidAmount = bill.PaidAmount + OutStandingAmount;
    //             bill.PaidAmount = Math.ceil(bill.PaidAmount);
    //             bill.IsConsolidatePay = true;
    //             await this.Update(bill);


    //             let PatientPaymentDetail: any = {
    //                 Id: 0,
    //                 ReceiptDateTime: new Date(),
    //                 FacilityId: bill.FacilityId,
    //                 OrganizationId: bill.OrganizationId,
    //                 PatientId: bill.PatientId,
    //                 ReceiptTypeId: 2,
    //                 EncounterId: bill.EncounterId,
    //                 EncounterTypeId: bill.EncounterTypeId,
    //                 PatientName: bill.PatientName,
    //                 OutStandingAmount: OutStandingAmount,
    //                 AmountPaid: OutStandingAmount,
    //                 DueAmount: 0,
    //                 DepartmentID: bill.DepartmentId,
    //                 PaymentcounterID: 0,
    //                 GuarantorId: bill.GuarantorId,
    //                 GuarantorTypeId: bill.GuarantorTypeId,
    //                 DoctorId: bill.DoctorId,
    //                 ServiceId: bill.ServiceRateCategoryId,
    //                 ServiceName: bill.ServiceRateCategoryName,
    //                 PatientBillId: bill.Id,
    //                 GurantorName: bill.GuarantorName,
    //                 ReceiptStatusId: 1,
    //                 TDSAmount: 0.00,
    //                 Disallowance: 0.00,
    //                 PaymentStatusId: 3,
    //                 CollectedOn: new Date(),
    //                 IsConsolidatePay: true,
    //             };
    //             PatientPaymentDetail.TerminalNoId = PaymentDetail.TerminalNoId;
    //             PatientPaymentDetail.BankId = PaymentDetail.BankId;
    //             PatientPaymentDetail.CardTypeId = PaymentDetail.CardTypeId;
    //             PatientPaymentDetail.ChequeNo = PaymentDetail.ChequeNo;
    //             PatientPaymentDetail.ChequeDate = PaymentDetail.ChequeDate;
    //             PatientPaymentDetail.DDNumber = PaymentDetail.DDNumber;
    //             PatientPaymentDetail.DDDate = PaymentDetail.DDDate;
    //             PatientPaymentDetail.WireTransferId = PaymentDetail.WireTransferId;
    //             PatientPaymentDetail.WireTransferDate = PaymentDetail.WireTransferDate;
    //             PatientPaymentDetail.ReceiptGeneratedById = PaymentDetail.ReceiptGeneratedById;
    //             PatientPaymentDetail.ReceiptApprovedById = PaymentDetail.ReceiptApprovedById;
    //             PatientPaymentDetail.PaymentTypeId = PaymentDetail.PaymentTypeId;
    //             PatientPaymentDetail.AuthorizedCode = PaymentDetail.AuthorizedCode;
    //             PatientPaymentDetail.Comments = PaymentDetail.Comments;

    //             PatientPaymentDetail.ReceiptNumber = await Sequence.Next(SequenceKeys.ReceiptNrId);
    //             await PaymentBo.Save(PatientPaymentDetail);
    //         })(Bills);
    //     }));
    //     return true;
    // }

    public async PrintInpatientBills(req: BaseRequest): Promise<FileInfo> {

        let isFinalized: boolean = req.Data.isFinalized;
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            payment: (req.Data.paymentDetail) ? req.Data.paymentDetail : 0,
            nonmedical: (req.Data.nonmedical) ? req.Data.nonmedical : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let TotalMOUDisc = req.Data.MOUDisc;
        let PatientBills: any = {};
        if (isFinalized) {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 2 }, { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
                { Key: PatientBillsFilters.IsPharmacyBill, Value: false }]
            };
            let data = await this.GetPatientBills(apiReq);
            PatientBills = data.Data[0];
        }
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let patientData: any = {};
        patientData = await patientBo.GetPatientById({ Id: Encounter.PatientId });
        let Age = '';
        if (patientData.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientData.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientData.Age > 0) {
            Age = patientData.Age + 'y';
        }
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientData.ReferrerId) {
            ReferralId = patientData.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let TotalGrossAmount: number = 0;
        let TotalInsuranceAmount: number = 0;
        let TotalPatientAmount: number = 0;
        let TotalGSTAmount: number = 0;
        let TotalNetAmount: number = 0;
        let RoundOffValue: number = 0;
        let TotalDiscount: number = 0;
        let NetVal: number = 0;
        let TotSupplmDiscDrug: number = 0;
        let TotMouDisc: number = 0;
        let GstAmount = 0;
        let BillDetails: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillSummaryFilters.EncounterId, Value: req.Id },
            { Key: PatientBillSummaryFilters.ActualAmount, Value: '0' }]
        };
        let servicecategoryBo = BoFactory.GetBo(clinicalmasterBO.ServiceCategoryBo, this.Request);
        let billSummaryBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientBillSummary = await billSummaryBo.GetPatientBillSummarys(apiReq);

        await Promise.all(PatientBillSummary.Data.map((splitItem): Promise<void> => {
            return (async (bill): Promise<void> => {
                let summary: any = bill;
                if (req.Data.isSupplementary) {
                    TotalGrossAmount += bill.ActualPatAmount;
                } else {
                    TotalGrossAmount += bill.ActualAmount;
                }
                TotalDiscount += bill.DiscountAmount;
                TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount) + (bill.ActualPatAmount || 0);
                TotalGSTAmount += bill.TaxAmount;
                let summaryTotal: number = 0;
                let splitDetails: any = [];
                let billDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.ServiceCategoryId, Value: bill.ServiceCategoryId },
                    { Key: PatientBillDetailsFilters.EncounterId, Value: bill.EncounterId },
                    { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 },
                    { Key: PatientBillDetailsFilters.Quantity, Value: '0' }]
                };
                let DetailData = await PatientBillDetailsBo.GetPatientBillDetails(billDetailReq);
                if (DetailData.Data.length > 0) {
                    for (let bdx in DetailData.Data) {
                        let BillDetail: any = DetailData.Data[bdx];
                        if (BillDetail.PatientBill.BillTypeId !== 1) {
                            let discAmtSuppl: number = 0;
                            let discAmt: number = 0;
                            let moudiscamt: number = 0;
                            if (req.Data.isSupplementary) {
                                NetVal = BillDetail.PatNetAmount;
                            } else if (BillDetail.InsNetAmount > 0) {
                                NetVal = BillDetail.InsNetAmount;
                            } else {
                                NetVal = BillDetail.NetAmount;
                            }
                            if (req.Data.isSupplementary) {
                                if (BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmtSuppl = BillDetail.DiscountAmount - (BillDetail.AgreementDiscountAmt || 0);
                                }
                                if (BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    discAmtSuppl = BillDetail.AgreementDiscountAmt || 0;
                                }
                            } else {
                                if (!BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmt = BillDetail.DiscountAmount;
                                }
                                if (!BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    moudiscamt = BillDetail.AgreementDiscountAmt;
                                }
                            }

                            TotalDiscount += discAmt;
                            TotSupplmDiscDrug += discAmtSuppl;
                            TotMouDisc += moudiscamt;
                            TotalInsuranceAmount += (BillDetail.InsNetAmount);
                            TotalPatientAmount += (BillDetail.PatNetAmount);
                            if (req.Data.isSupplementary) {
                                summaryTotal = bill.ActualPatAmount;
                            } else {
                                summaryTotal = bill.ActualNetAmount + (bill.ActualPatAmount || 0);
                            }
                            if (req.Data.isSupplementary) {
                                if (BillDetail.IsSupplementary) {
                                    splitDetails.push(BillDetail);
                                }
                            } else {
                                splitDetails.push(BillDetail);
                            }
                        }
                    }
                }

                let ServiceCategoryInfo = await servicecategoryBo.GetServiceCategoryById({ Id: bill.ServiceCategoryId });
                summary.ServiceName = ServiceCategoryInfo.ServiceCategoryName;
                // let withpharmacysales: any = [];
                let orderbydatespldet = _.orderBy(splitDetails, ['ServiceId', 'BillDateTime']);
                // let roomGroups: any = {};
                let serviceitemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
                // for (let sdx in orderbydatespldet) {
                //     let detail = orderbydatespldet[sdx];
                //     if (detail.ServiceId > 0) {
                //         let bIsRoomCharge: boolean = false;
                //         let servReq = {
                //             Id: 0,
                //             PageContext: { PageSize: -1, PageNumber: 1 },
                //             Params: [{ Key: 0, Value: detail.ServiceId }]
                //         };
                //         let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                //         if (Servicedata.Data.length > 0) {
                //             let ServiceInfo: any = Servicedata.Data[0];
                //             detail.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                //             if (ServiceInfo) {
                //                 if (ServiceInfo.IsBedCharge) {
                //                     bIsRoomCharge = true;
                //                     let dateformat = 'DD/MM/YYYY';
                //                     if (roomGroups[detail.ServiceId]) {
                //                         let billdt = moment(detail.BillDateTime).format(dateformat);
                //                         roomGroups[detail.ServiceId].BTDt = billdt;
                //                         roomGroups[detail.ServiceId].Quantity += detail.Quantity;
                //                         roomGroups[detail.ServiceId].Amount += detail.GrossAmount;
                //                         roomGroups[detail.ServiceId].DiscountAmount += detail.DiscountAmount;
                //                         roomGroups[detail.ServiceId].AgreementDiscountAmt += detail.AgreementDiscountAmt;
                //                         roomGroups[detail.ServiceId].NetAmount +=
                //                             (detail.GrossAmount - detail.DiscountAmount - (detail.AgreementDiscountAmt || 0));
                //                     } else {
                //                         roomGroups[detail.ServiceId] = roomGroups[detail.ServiceId] || [];
                //                         let billdt1 = moment(detail.BillDateTime).format(dateformat);
                //                         roomGroups[detail.ServiceId].ServiceName = detail.ServiceName;
                //                         roomGroups[detail.ServiceId].BFDt = billdt1;
                //                         roomGroups[detail.ServiceId].BTDt = billdt1;
                //                         roomGroups[detail.ServiceId].User = detail.User;
                //                         roomGroups[detail.ServiceId].BillDateTime = detail.BillDateTime;
                //                         roomGroups[detail.ServiceId].Quantity = detail.Quantity;
                //                         roomGroups[detail.ServiceId].Rate = detail.Rate;
                //                         roomGroups[detail.ServiceId].Amount = detail.GrossAmount;
                //                         roomGroups[detail.ServiceId].DiscountAmount = detail.DiscountAmount;
                //                         roomGroups[detail.ServiceId].AgreementDiscountAmt += detail.AgreementDiscountAmt;
                //                         roomGroups[detail.ServiceId].NetAmount =
                //                             (detail.GrossAmount - detail.DiscountAmount - (detail.AgreementDiscountAmt || 0));
                //                         roomGroups[detail.ServiceId].Id = detail.Id;
                //                         detail.Amount = detail.GrossAmount;
                //                         detail.NetAmount =
                //                             detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //                         withpharmacysales.push(detail);
                //                     }
                //                 }
                //             }
                //         }
                //         if (!detail.IsDoctorDisplay && !bIsRoomCharge) {
                //             // detail.User = null;
                //             detail.Amount = detail.GrossAmount;
                //             detail.NetAmount =
                //                 detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //             withpharmacysales.push(detail);
                //         } else if (detail.IsDoctorDisplay && !bIsRoomCharge) {
                //             detail.Amount = detail.GrossAmount;
                //             detail.NetAmount =
                //                 detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //             withpharmacysales.push(detail);
                //         } else {
                //             for (let indx in withpharmacysales) {
                //                 if (withpharmacysales[indx].Id === roomGroups[detail.ServiceId].Id) {
                //                     withpharmacysales[indx].ServiceName = roomGroups[detail.ServiceId].ServiceName;
                //                     withpharmacysales[indx].Quantity = roomGroups[detail.ServiceId].Quantity;
                //                     withpharmacysales[indx].Rate = roomGroups[detail.ServiceId].Rate;
                //                     withpharmacysales[indx].Amount = roomGroups[detail.ServiceId].Amount;
                //                     withpharmacysales[indx].DiscountAmount = roomGroups[detail.ServiceId].DiscountAmount;
                //                     withpharmacysales[indx].AgreementDiscountAmt = roomGroups[detail.ServiceId].AgreementDiscountAmt;
                //                     withpharmacysales[indx].NetAmount =
                //                         roomGroups[detail.ServiceId].NetAmount;
                //                 }
                //             }
                //         }
                //     }
                // }
                // await Promise.all(orderbydatespldet.map((orderbydatespldetItem: any): Promise<void> => {
                //     return (async (detail): Promise<void> => {

                //     })(orderbydatespldetItem);
                // }));
                let nondrugdetails: any = [];
                let facilityprebo = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let showDrugDetails = 0;
                for (let idx in orderbydatespldet) {
                    let drugitems = orderbydatespldet[idx];
                    let servReq = {
                        Id: 0,
                        PageContext: { PageSize: -1, PageNumber: 1 },
                        Params: [{ Key: 0, Value: drugitems.ServiceId }]
                    };
                    let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                    if (Servicedata.Data.length > 0) {
                        let ServiceInfo: any = Servicedata.Data[0];
                        drugitems.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                    }
                    let printPreferencesData = await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
                    if (printPreferencesData && printPreferencesData.showdrugdetails) {
                        try {
                            showDrugDetails = parseInt(printPreferencesData.showdrugdetails);
                        } catch (ex) { showDrugDetails = 0; }
                    }
                    if (req.Data.ShowAliasInfo) {
                        if (drugitems.ServiceName) {
                            if (drugitems.AliasName) {
                                drugitems.ServiceName += '(' + drugitems.AliasName + ')';
                            }
                        }
                    }
                    if (showDrugDetails === 0 || !showDrugDetails) {
                        if (!drugitems.IsPharmacySale && !drugitems.IsPharmacyReturn) {
                            if (req.Data.isSupplementary) {
                                drugitems.NetValue = drugitems.PatNetAmount;
                            } else if (drugitems.InsNetAmount > 0) {
                                drugitems.NetValue = drugitems.InsNetAmount;
                            } else {
                                drugitems.NetValue = drugitems.NetAmount;
                            }
                            nondrugdetails.push(drugitems);
                        }
                    } else if (showDrugDetails === 1) {
                        if (req.Data.IsSupplementary) {
                            drugitems.NetValue = drugitems.PatNetAmount;
                        } else if (drugitems.InsNetAmount > 0) {
                            drugitems.NetValue = drugitems.InsNetAmount;
                        } else {
                            drugitems.NetValue = drugitems.NetAmount;
                        }
                        nondrugdetails.push(drugitems);
                    }
                }
                let Date_sort = function (a: any, b: any) {
                    if (a.BillDateTime < b.BillDateTime) {
                        return -1;
                    }
                    if (a.BillDateTime > b.BillDateTime) {
                        return 1;
                    }
                    return 0;
                };
                nondrugdetails.sort(Date_sort);
                summary['SplitedValue'] = nondrugdetails;
                summary['Amount'] = summaryTotal;
                if (splitDetails.length > 0)
                    BillDetails.push(summary);


                let custom_sort = function (a: any, b: any) {
                    return parseInt(a.ServiceCategory.DisplayOrder) - parseInt(b.ServiceCategory.DisplayOrder);
                };
                BillDetails.sort(custom_sort);
            })(splitItem);
        }));
        let DueCollect: number = 0;
        let PaidAmount: number = 0;
        let ReceiptAmount: number = 0;
        let Advance: number = 0;
        let ReceiptDetails = [];
        let encType: any;
        if (Encounter.IsDayCare) {
            encType = 5;
        } else {
            encType = 2;
        }
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.EncounterId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.EncounterTypeId, Value: encType },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: false },
            { Key: PatientPaymentDetailsFilters.IsConsolidatePay, Value: false },
            { Key: PatientPaymentDetailsFilters.StatusOfReceipts, Value: [1, 4] }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        for (let idx in PatientPaymentDetailsData.Data) {
            let ReceiptItem = PatientPaymentDetailsData.Data[idx];
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1) {
                PaidAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1 &&
                PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 2) {
                ReceiptAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 1) {
                Advance += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 3)
                DueCollect += PatientPaymentDetailsData.Data[idx].AmountPaid;
            ReceiptDetails.push(ReceiptItem);
        }
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.EncounterId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 },
            { Key: PatientRefundFilters.EncounterTypeId, Value: encType },
            { Key: PatientRefundFilters.IsCashToCredit, Value: false }]
        };
        let RefundAmount: number = 0;
        let PartialRefundAmount: number = 0;
        let RefundDetails = [];
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        for (var idx1 in PatientRefundData.Data) {
            var RefundItem = PatientRefundData.Data[idx1];
            if (PatientRefundData.Data[idx1].RefundStatusId === 1) {
                RefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            }
            if (PatientRefundData.Data[idx1].RefundTypeId === 1 || PatientRefundData.Data[idx1].RefundTypeId === 4)
                PartialRefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            RefundDetails.push(RefundItem);
        }
        let UserReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: UserFilters.Id, Value: req.Data.PrintUser }]
        };
        let UsersBo = BoFactory.GetBo(userbo.UserBo, this.Request);
        let PrintUser = await UsersBo.GetUsers(UserReq);
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        // let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        // let printStoreData =
        //     await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let GuarantorId = Encounter.GuarantorId;
        let GuarantorTypeId = Encounter.GuarantorTypeId;
        let PatientGuarantorReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.GuarantorId, Value: GuarantorId },
            { Key: PatientGuarantorFilters.PatientId, Value: Encounter.PatientId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorReq);
        let PatientGuarantor = PatientGuarantorData.Data[0];
        if (PatientBills.RoundOffValue) {
            RoundOffValue = Math.round(PatientBills.RoundOffValue);
        } else {
            RoundOffValue = 0;
        }
        let BalanceAmount: number = 0;
        if (!isFinalized) {
            TotalNetAmount = Math.round(TotalNetAmount) + Math.round(TotalGSTAmount);
            if (GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }
        }
        let QrInfo: any;
        if (isFinalized) {
            TotalNetAmount = Math.round(TotalGrossAmount - PatientBills.BillDiscount);
            if (PatientBills.GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }


            let PatName: '';
            let FacInfo: '';

            if (patientData.Title) {
                PatName = patientData.Title.Description;
            }
            if (patientData.FirstName) {
                PatName += ' ' + patientData.FirstName;
            }
            if (patientData.LastName) {
                PatName += ' ' + patientData.LastName;
            }
            if (PatientBills.Facility) {
                FacInfo = PatientBills.Facility.FacilityName;
            }
            if (PatientBills.Facility.AddressLine1) {
                FacInfo += ', ' + PatientBills.Facility.AddressLine1;
            }
            if (PatientBills.Facility.Mobile) {
                FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
            }
            if (PatientBills.Facility.Email) {
                FacInfo += ',Email: ' + PatientBills.Facility.Email;
            }
            if (PatientBills.Facility.GstNumber) {
                FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
            }
            QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
                PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        }
        // console.log('********11*******');
        let info = {
            PatientBills: PatientBills,
            Encounter: Encounter,
            Patient: patientData,
            BillDetails: BillDetails,
            Age: Age,
            PatientPaymentDetails: ReceiptDetails,
            TotalGrossAmount: Math.round(TotalGrossAmount),
            TotalGSTAmount: Math.round(TotalGSTAmount),
            TotalNetAmount: Math.round(TotalNetAmount),
            TotalDiscount: Math.round(TotalDiscount),
            TotSupplmDiscDrug: TotSupplmDiscDrug,
            TotalInsuranceAmount: Math.round(TotalInsuranceAmount),
            TotalPatientAmount: Math.round(TotalPatientAmount),
            DueCollect: Math.round(DueCollect),
            PaidAmount: Math.round(PaidAmount),
            ReceiptAmount: Math.round(ReceiptAmount),
            Advance: Math.round(Advance),
            NetAmount: (PatientBills.BillAmount + RoundOffValue) - PatientBills.BillDiscount,
            BalanceAmount: Math.round(BalanceAmount),
            currentdate: new Date(),
            PrintUser: PrintUser.Data[0],
            PatientRefund: PatientRefundData.Data,
            RefundAmount: Math.round(RefundAmount),
            PartialRefundAmount: Math.round(PartialRefundAmount),
            Preferences: printPreferencesData,
            PatientGuarantor: PatientGuarantor,
            Referrals: ReferralsData.Data[0],
            QrInfo: QrInfo,
            GstAmount: GstAmount,
            TotMouDisc: TotMouDisc,
            Flags: flags,
            TotalMOUDisc: TotalMOUDisc
        };
        let key = 'InpatientBillDetail(i)';
        let pdfOption: any = null;
        {
            if (isFinalized) {
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientBillDetail';
                }
            }
            if (req.Data.isGuarantor) {
                key = 'InpatientbillDetailsGuarantor(i)';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientbillDetailsGuarantor';
                }
            }
            if (req.Data.isSupplementary) {
                key = 'Supplementry';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2
                    && info.BillDetails.IsSupplementary === true) {
                    key = 'Supplementry';
                }
            }
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'portrait',
                    border: '0',
                    header: {
                        height: '1in',
                        contents: '',
                    },
                    footer: {
                        height: '1in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
                console.log(pdfOption);
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async getInpatientBillDetails(req: BaseRequest): Promise<any> {

        let isFinalized: boolean = req.Data.isFinalized;
        // let flags = {
        //     header: (req.Data.withHeader) ? req.Data.withHeader : 0,
        //     woheader: (req.Data.withoutHeader) ? req.Data.withoutHeader : 0,
        //     payment: (req.Data.paymentDetail) ? req.Data.paymentDetail : 0,
        //     nonmedical: (req.Data.nonmedical) ? req.Data.nonmedical : 0,
        //     patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
        //     insurancebill: (req.Data.insuranceBill) ? req.Data.insuranceBill : 0,
        // };
        let PatientBills: any = {};
        if (isFinalized) {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 2 },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
                { Key: PatientBillsFilters.IsPharmacyBill, Value: false }]
            };
            let data = await this.GetPatientBills(apiReq);
            PatientBills = data.Data[0];
        }
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let patientData: any = {};
        patientData = await patientBo.GetPatientById({ Id: Encounter.PatientId });
        let Age = '';
        if (patientData.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientData.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientData.Age > 0) {
            Age = patientData.Age + 'y';
        }
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientData.ReferrerId) {
            ReferralId = patientData.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let TotalGrossAmount: number = 0;
        let TotalInsuranceAmount: number = 0;
        let TotalPatientAmount: number = 0;
        let TotalGSTAmount: number = 0;
        let TotalNetAmount: number = 0;
        let RoundOffValue: number = 0;
        let TotalDiscount: number = 0;
        let NetVal: number = 0;
        let TotSupplmDiscDrug: number = 0;
        let TotMouDisc: number = 0;
        let GstAmount = 0;
        let BillDetails: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillSummaryFilters.EncounterId, Value: req.Id }]
        };
        let servicecategoryBo = BoFactory.GetBo(clinicalmasterBO.ServiceCategoryBo, this.Request);
        let billSummaryBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientBillSummary = await billSummaryBo.GetPatientBillSummarys(apiReq);

        await Promise.all(PatientBillSummary.Data.map((splitItem): Promise<void> => {
            return (async (bill: any): Promise<void> => {
                let summary: any = bill;
                if (req.Data.isSupplementary) {
                    TotalGrossAmount += bill.ActualPatAmount;
                } else {
                    if (!req.Data.nonmedical) {
                        TotalGrossAmount += bill.ActualAmount;
                    } else {
                        if (req.Data.nonmedical) {
                            TotalGrossAmount += bill.ActualAmount;
                        } else {
                            TotalGrossAmount += bill.ActualNetAmount;
                        }
                    }
                }
                // TotalDiscount += bill.DiscountAmount;
                // TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount) + (bill.ActualPatAmount || 0);
                if (req.Data.nonmedical) {
                    TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount);
                } else {
                    TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount);
                }

                TotalGSTAmount += bill.TaxAmount;
                let summaryTotal: any = 0;
                let summarywoMedicalTotal: any = 0;
                let splitDetails: any = [];
                let billDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.ServiceCategoryId, Value: bill.ServiceCategoryId },
                    { Key: PatientBillDetailsFilters.EncounterId, Value: bill.EncounterId },
                    { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 },
                    { Key: PatientBillDetailsFilters.Quantity, Value: '0' },
                    { Key: PatientBillDetailsFilters.IsTempIPBill, Value: true },]
                };
                let DetailData = await PatientBillDetailsBo.GetPatientBillDetails(billDetailReq);
                if (DetailData.Data.length > 0) {
                    for (let bdx in DetailData.Data) {
                        let BillDetail: any = DetailData.Data[bdx];
                        if (BillDetail.PatientBill.BillTypeId !== 1) {
                            let discAmtSuppl: number = 0;
                            let discAmt: number = 0;
                            let moudiscamt: number = 0;
                            if (req.Data.isSupplementary) {
                                NetVal = BillDetail.PatNetAmount;
                            } else if (BillDetail.InsNetAmount > 0) {
                                NetVal = BillDetail.InsNetAmount;
                            } else {
                                NetVal = BillDetail.NetAmount;
                            }
                            if (req.Data.isSupplementary) {
                                if (BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmtSuppl = BillDetail.DiscountAmount - (BillDetail.AgreementDiscountAmt || 0);
                                }
                                if (BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    discAmtSuppl = BillDetail.AgreementDiscountAmt || 0;
                                }
                            } else {
                                if (!BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmt = BillDetail.DiscountAmount;
                                }
                                if (!BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    moudiscamt = BillDetail.AgreementDiscountAmt;
                                }
                            }

                            TotalDiscount += discAmt;
                            TotSupplmDiscDrug += discAmtSuppl;
                            TotMouDisc += moudiscamt;
                            TotalInsuranceAmount += (BillDetail.InsNetAmount);
                            TotalPatientAmount += (BillDetail.PatNetAmount);
                            if (req.Data.isSupplementary) {
                                summaryTotal = parseFloat(bill.ActualPatAmount);
                            } else {
                                if (!req.Data.nonmedical) {
                                    summaryTotal = parseFloat(bill.ActualNetAmount);
                                } else {
                                    summaryTotal = parseFloat(bill.ActualNetAmount) + (parseFloat(bill.ActualPatAmount) || 0);
                                    summarywoMedicalTotal = parseFloat(BillDetail.InsNetAmount)
                                        + (parseFloat(BillDetail.PatNetAmount) || 0);
                                }
                            }
                            if (req.Data.isSupplementary) {
                                // if (BillDetail.IsSupplementary) {
                                //     splitDetails.push(BillDetail);
                                // }
                                splitDetails.push(BillDetail);
                            } else {
                                if (req.Data.nonmedical) {
                                    splitDetails.push(BillDetail);
                                } else {
                                    if (!BillDetail.IsSupplementary) {
                                        splitDetails.push(BillDetail);
                                    }
                                }
                            }
                        }
                    }
                }
                let ServiceCategoryInfo = await servicecategoryBo.GetServiceCategoryById({ Id: bill.ServiceCategoryId });
                summary.ServiceName = ServiceCategoryInfo.ServiceCategoryName;
                // let withpharmacysales: any = [];

                let orderbydatespldet = _.orderBy(splitDetails, ['ServiceId', 'BillDateTime']);
                // // let roomGroups: any = {};
                // let serviceitemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
                // for (let sdx in orderbydatespldet) {
                //     let detail = orderbydatespldet[sdx];
                //     if (detail.ServiceId > 0) {
                //         let bIsRoomCharge: boolean = false;
                //         let servReq = {
                //             Id: 0,
                //             PageContext: { PageSize: -1, PageNumber: 1 },
                //             Params: [{ Key: 0, Value: detail.ServiceId }]
                //         };
                //         let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                //         if (Servicedata.Data.length > 0) {
                //             let ServiceInfo: any = Servicedata.Data[0];
                //             detail.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                //             if (ServiceInfo) {
                //                 if (ServiceInfo.IsBedCharge) {
                //                     bIsRoomCharge = true;
                //                     let dateformat = 'DD/MM/YYYY';
                //                     if (roomGroups[detail.ServiceId]) {
                //                         let billdt = moment(detail.BillDateTime).format(dateformat);
                //                         roomGroups[detail.ServiceId].BTDt = billdt;
                //                         roomGroups[detail.ServiceId].Quantity += detail.Quantity;
                //                         roomGroups[detail.ServiceId].Amount += detail.GrossAmount;
                //                         roomGroups[detail.ServiceId].DiscountAmount += detail.DiscountAmount;
                //                         roomGroups[detail.ServiceId].AgreementDiscountAmt += detail.AgreementDiscountAmt;
                //                         roomGroups[detail.ServiceId].NetAmount +=
                //                             (detail.GrossAmount - detail.DiscountAmount - (detail.AgreementDiscountAmt || 0));
                //                     } else {
                //                         roomGroups[detail.ServiceId] = roomGroups[detail.ServiceId] || [];
                //                         let billdt1 = moment(detail.BillDateTime).format(dateformat);
                //                         roomGroups[detail.ServiceId].ServiceName = detail.ServiceName;
                //                         roomGroups[detail.ServiceId].BFDt = billdt1;
                //                         roomGroups[detail.ServiceId].BTDt = billdt1;
                //                         roomGroups[detail.ServiceId].User = detail.User;
                //                         roomGroups[detail.ServiceId].BillDateTime = detail.BillDateTime;
                //                         roomGroups[detail.ServiceId].Quantity = detail.Quantity;
                //                         roomGroups[detail.ServiceId].Rate = detail.Rate;
                //                         roomGroups[detail.ServiceId].Amount = detail.GrossAmount;
                //                         roomGroups[detail.ServiceId].DiscountAmount = detail.DiscountAmount;
                //                         roomGroups[detail.ServiceId].AgreementDiscountAmt += detail.AgreementDiscountAmt;
                //                         roomGroups[detail.ServiceId].NetAmount =
                //                             (detail.GrossAmount - detail.DiscountAmount - (detail.AgreementDiscountAmt || 0));
                //                         roomGroups[detail.ServiceId].Id = detail.Id;
                //                         detail.Amount = detail.GrossAmount;
                //                         detail.NetAmount =
                //                             detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //                         withpharmacysales.push(detail);
                //                     }
                //                 }
                //             }
                //         }
                //         if (!detail.IsDoctorDisplay && !bIsRoomCharge) {
                //             // detail.User = null;
                //             detail.Amount = detail.GrossAmount;
                //             detail.NetAmount =
                //                 detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //             withpharmacysales.push(detail);
                //         } else if (detail.IsDoctorDisplay && !bIsRoomCharge) {
                //             detail.Amount = detail.GrossAmount;
                //             detail.NetAmount =
                //                 detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //             withpharmacysales.push(detail);
                //         } else {
                //             for (let indx in withpharmacysales) {
                //                 if (withpharmacysales[indx].Id === roomGroups[detail.ServiceId].Id) {
                //                     withpharmacysales[indx].ServiceName = roomGroups[detail.ServiceId].ServiceName;
                //                     withpharmacysales[indx].Quantity = roomGroups[detail.ServiceId].Quantity;
                //                     withpharmacysales[indx].Rate = roomGroups[detail.ServiceId].Rate;
                //                     withpharmacysales[indx].Amount = roomGroups[detail.ServiceId].Amount;
                //                     withpharmacysales[indx].DiscountAmount = roomGroups[detail.ServiceId].DiscountAmount;
                //                     withpharmacysales[indx].AgreementDiscountAmt = roomGroups[detail.ServiceId].AgreementDiscountAmt;
                //                     withpharmacysales[indx].NetAmount =
                //                         roomGroups[detail.ServiceId].NetAmount;
                //                 }
                //             }
                //         }
                //     }
                // }
                // await Promise.all(orderbydatespldet.map((orderbydatespldetItem: any): Promise<void> => {
                //     return (async (detail): Promise<void> => {

                //     })(orderbydatespldetItem);
                // }));
                let nondrugdetails: any = [];
                let facilityprebo = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let showDrugDetails = 0;
                let printPreferencesData = await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
                if (printPreferencesData && printPreferencesData.showdrugdetails) {
                    try {
                        showDrugDetails = parseInt(printPreferencesData.showdrugdetails);
                    } catch (ex) { showDrugDetails = 0; }
                }
                for (let idx in orderbydatespldet) {
                    let drugitems = orderbydatespldet[idx];
                    // let servReq = {
                    //     Id: 0,
                    //     PageContext: { PageSize: -1, PageNumber: 1 },
                    //     Params: [{ Key: 0, Value: drugitems.ServiceId }]
                    // };
                    if (drugitems.ServiceItem) {
                        console.log(drugitems.ServiceItem.IsDoctorDisplay);
                        drugitems.IsDoctorDisplay = (drugitems.ServiceItem.IsDoctorDisplay) ? 1 : 0;
                        console.log(drugitems.IsDoctorDisplay);
                    } else {
                        drugitems.IsDoctorDisplay = 0;
                    }
                    // drugitems.IsDoctorDisplay = 1;//Done on 17/2/24 by jothi -- Confirmed by abcd Sir
                    // let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                    // if (Servicedata.Data.length > 0) {
                    //     let ServiceInfo: any = Servicedata.Data[0];
                    //     drugitems.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                    // }
                    if (showDrugDetails === 1) {
                        if (req.Data.IsSupplementary) {
                            drugitems.NetValue = drugitems.PatNetAmount;
                        } else if (drugitems.InsNetAmount > 0) {
                            drugitems.NetValue = drugitems.InsNetAmount;
                        } else {
                            drugitems.NetValue = drugitems.NetAmount;
                        }
                        nondrugdetails.push(drugitems);
                    } else if (showDrugDetails === 0 || !showDrugDetails) {
                        if (!drugitems.IsPharmacySale && !drugitems.IsPharmacyReturn) {
                            if (req.Data.isSupplementary) {
                                drugitems.NetValue = drugitems.PatNetAmount;
                            } else if (drugitems.InsNetAmount > 0) {
                                drugitems.NetValue = drugitems.InsNetAmount;
                            } else {
                                drugitems.NetValue = drugitems.NetAmount;
                            }
                            nondrugdetails.push(drugitems);
                        }
                    }
                }
                let Date_sort = function (a: any, b: any) {
                    if (a.BillDateTime < b.BillDateTime) {
                        return -1;
                    }
                    if (a.BillDateTime > b.BillDateTime) {
                        return 1;
                    }
                    return 0;
                };
                nondrugdetails.sort(Date_sort);

                summary['SplitedValue'] = nondrugdetails;
                summary['Amount'] = summaryTotal;
                summary['WoNonMedicalAmount'] = summarywoMedicalTotal;
                if (splitDetails.length > 0)
                    BillDetails.push(summary);


                let custom_sort = function (a: any, b: any) {
                    return parseInt(a.ServiceCategory.DisplayOrder) - parseInt(b.ServiceCategory.DisplayOrder);
                };
                BillDetails.sort(custom_sort);
            })(splitItem);
        }));
        let DueCollect: number = 0;
        let PaidAmount: number = 0;
        let Advance: number = 0;
        let ReceiptDetails = [];
        let RefundAmount: number = 0;
        let PartialRefundAmount: number = 0;
        let PatientRefundData: any;
        let PatientRefundInfo: any;

        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.EncounterId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.EncounterTypeId, Value: 2 },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: false },
            { Key: PatientPaymentDetailsFilters.IsConsolidatePay, Value: false },
            { Key: PatientPaymentDetailsFilters.StatusOfReceipts, Value: [1, 4] }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        for (let idx in PatientPaymentDetailsData.Data) {
            let ReceiptItem = PatientPaymentDetailsData.Data[idx];
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1) {
                PaidAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 1) {
                Advance += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 3)
                DueCollect += PatientPaymentDetailsData.Data[idx].AmountPaid;
            ReceiptDetails.push(ReceiptItem);
        }
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.EncounterId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 },
            { Key: PatientRefundFilters.EncounterTypeId, Value: 2 },
            { Key: PatientRefundFilters.IsCashToCredit, Value: false }]
        };

        let RefundDetails = [];
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        PatientRefundInfo = PatientRefundData.Data;

        for (var idx1 in PatientRefundData.Data) {
            var RefundItem = PatientRefundData.Data[idx1];
            if (PatientRefundData.Data[idx1].RefundStatusId === 1) {
                RefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            }
            if (PatientRefundData.Data[idx1].RefundTypeId === 1 || PatientRefundData.Data[idx1].RefundTypeId === 4)
                PartialRefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            RefundDetails.push(RefundItem);
        }

        let UserReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: UserFilters.Id, Value: req.Data.PrintUser }]
        };
        let UsersBo = BoFactory.GetBo(userbo.UserBo, this.Request);
        let PrintUser = await UsersBo.GetUsers(UserReq);
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        // let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        // let printStoreData =
        //     await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let GuarantorId = Encounter.GuarantorId;
        let GuarantorTypeId = Encounter.GuarantorTypeId;
        let PatientGuarantorReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.Id, Value: GuarantorId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorReq);
        let PatientGuarantor = PatientGuarantorData.Data[0];
        if (PatientBills.RoundOffValue) {
            RoundOffValue = PatientBills.RoundOffValue;
        } else {
            RoundOffValue = 0;
        }
        let BalanceAmount: number = 0;
        if (!isFinalized) {
            TotalNetAmount = Math.round(TotalNetAmount) + Math.round(TotalGSTAmount);
            if (GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }
        }
        let QrInfo: any;
        if (isFinalized) {
            TotalNetAmount = Math.round(TotalGrossAmount - PatientBills.BillDiscount);
            if (PatientBills.GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }


            let PatName: '';
            let FacInfo: '';

            if (patientData.Title) {
                PatName = patientData.Title.Description;
            }
            if (patientData.FirstName) {
                PatName += ' ' + patientData.FirstName;
            }
            if (patientData.LastName) {
                PatName += ' ' + patientData.LastName;
            }
            if (PatientBills.Facility) {
                FacInfo = PatientBills.Facility.FacilityName;
            }
            if (PatientBills.Facility.AddressLine1) {
                FacInfo += ', ' + PatientBills.Facility.AddressLine1;
            }
            if (PatientBills.Facility.Mobile) {
                FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
            }
            if (PatientBills.Facility.Email) {
                FacInfo += ',Email: ' + PatientBills.Facility.Email;
            }
            if (PatientBills.Facility.GstNumber) {
                FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
            }
            QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
                PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        }
        // console.log('********11*******');
        let info = {
            PatientBills: PatientBills,
            Encounter: Encounter,
            Patient: patientData,
            BillDetails: BillDetails,
            Age: Age,
            PatientPaymentDetails: ReceiptDetails,
            TotalGrossAmount: Math.round(TotalGrossAmount),
            TotalGSTAmount: Math.round(TotalGSTAmount),
            TotalNetAmount: Math.round(TotalNetAmount),
            TotalDiscount: Math.round(TotalDiscount),
            TotSupplmDiscDrug: TotSupplmDiscDrug,
            TotalInsuranceAmount: Math.round(TotalInsuranceAmount),
            TotalPatientAmount: Math.round(TotalPatientAmount),
            DueCollect: Math.round(DueCollect),
            PaidAmount: Math.round(PaidAmount),
            Advance: Math.round(Advance),
            NetAmount: (PatientBills.BillAmount + RoundOffValue) - PatientBills.BillDiscount,
            BalanceAmount: Math.round(BalanceAmount),
            currentdate: new Date(),
            PrintUser: PrintUser.Data[0],
            PatientRefund: PatientRefundInfo,
            RefundAmount: Math.round(RefundAmount),
            PartialRefundAmount: Math.round(PartialRefundAmount),
            Preferences: printPreferencesData,
            PatientGuarantor: PatientGuarantor,
            Referrals: ReferralsData.Data[0],
            QrInfo: QrInfo,
            GstAmount: GstAmount,
            TotMouDisc: TotMouDisc,
            billtype: req.Data.ids,
            CoPayAmount: Math.round(req.Data.CoPayAmount),
            NonMedicalAmount: Math.round(req.Data.NonMedicalAmount)

        };
        let key = 'InpatientBillDetailxl(i)';
        if (isFinalized) {
            if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                key = 'InpatientBillDetailxl';
            }
        }
        let pdfOption: any = null;
        // {
        //     if (isFinalized) {
        //         if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
        //             key = 'InpatientBillDetail';
        //         }
        //     }
        //     if (req.Data.isGuarantor) {
        //         key = 'InpatientbillDetailsGuarantor(i)';
        //         if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
        //             key = 'InpatientbillDetailsGuarantor';
        //         }
        //     }
        //     if (req.Data.isSupplementary) {
        //         key = 'Supplementry';
        //         if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2
        //             && info.BillDetails.IsSupplementary === true) {
        //             key = 'Supplementry';
        //         }
        //     }
        //     let pdfOptionJSON = await Report.GetPdfOption(key);
        //     if (!pdfOptionJSON) {
        //         pdfOption = {
        //             format: 'A4',
        //             orientation: 'portrait',
        //             border: '0',
        //             header: {
        //                 height: '1in',
        //                 contents: '',
        //             },
        //             footer: {
        //                 height: '1in',
        //                 contents: {
        //                     first: '',
        //                     default: '',
        //                     last: '',
        //                 },
        //             },
        //             type: 'pdf',
        //             base: 'file://' + join(__dirname, '/../../Templates/assets/')
        //         };
        //     } else {
        //         pdfOption = JSON.parse(pdfOptionJSON);
        //         pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        //         console.log(pdfOption);
        //     }
        // }
        return await Report.GenerateHtml(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintNonMedical(req: BaseRequest): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withoutHeader) ? req.Data.withoutHeader : 0,
            payment: (req.Data.paymentDetail) ? req.Data.paymentDetail : 0,
            nonmedical: (req.Data.nonmedical) ? req.Data.nonmedical : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let isFinalized: boolean = req.Data.isFinalized;
        let PatientBills: any = {};
        if (isFinalized) {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 2 }, { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
            };
            let data = await this.GetPatientBills(apiReq);
            PatientBills = data.Data[0];
        }
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let patientData: any = {};
        patientData = await patientBo.GetPatientById({ Id: Encounter.PatientId });
        let Age = '';
        if (patientData.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientData.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientData.Age > 0) {
            Age = patientData.Age + 'y';
        }
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientData.ReferrerId) {
            ReferralId = patientData.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let TotalGrossAmount: number = 0;
        let TotalInsuranceAmount: number = 0;
        let TotalPatientAmount: number = 0;
        let TotalGSTAmount: number = 0;
        let TotalNetAmount: number = 0;
        let RoundOffValue: number = 0;
        let TotalDiscount: number = 0;
        let NetVal: number = 0;
        let TotSupplmDiscDrug: number = 0;
        let GstAmount = 0;
        let BillDetails: any = [];
        let PatientBillSummary: any = [];
        let billSummaryBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
        let billpackSummaryBo = BoFactory.GetBo(billingBO.PatientBillPackageSummaryBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let servicecategoryBo = BoFactory.GetBo(clinicalmasterBO.ServiceCategoryBo, this.Request);
        if (!Encounter.IsPackageAssigned) {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillSummaryFilters.EncounterId, Value: req.Id }]
            };
            PatientBillSummary = await billSummaryBo.GetPatientBillSummarys(apiReq);
        }
        if (Encounter.IsPackageAssigned) {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillPackageSummaryFilters.EncounterId, Value: req.Id }]
            };
            PatientBillSummary = await billpackSummaryBo.GetPatientBillPackageSummarys(apiReq);
        }
        await Promise.all(PatientBillSummary.Data.map((splitItem: any): Promise<void> => {
            return (async (bill): Promise<void> => {
                let summary: any = bill;
                if (req.Data.isSupplementary) {
                    TotalGrossAmount += bill.ActualPatAmount;
                } else {
                    TotalGrossAmount += bill.ActualAmount;
                }
                TotalDiscount += bill.DiscountAmount;
                TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount) + (bill.ActualPatAmount || 0);
                TotalGSTAmount += bill.TaxAmount;
                let summaryTotal: number = 0;
                let splitDetails: any = [];
                let billDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.ServiceCategoryId, Value: bill.ServiceCategoryId },
                    { Key: PatientBillDetailsFilters.EncounterId, Value: bill.EncounterId },
                    { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 },
                    { Key: PatientBillDetailsFilters.Quantity, Value: '0' },
                    { Key: PatientBillDetailsFilters.IsSupplementary, Value: true },
                    { Key: PatientBillDetailsFilters.IsTempIPBill, Value: true },]
                };
                let DetailData = await PatientBillDetailsBo.GetPatientBillDetails(billDetailReq);
                if (DetailData.Data.length > 0) {
                    for (let bdx in DetailData.Data) {
                        let BillDetail: any = DetailData.Data[bdx];
                        if (BillDetail.PatientBill.BillTypeId !== 1) {
                            let discAmtSuppl: number = 0;
                            let discAmt: number = 0;
                            if (req.Data.isSupplementary) {
                                NetVal = BillDetail.PatNetAmount;
                            } else if (BillDetail.InsNetAmount > 0) {
                                NetVal = BillDetail.InsNetAmount;
                            } else {
                                NetVal = BillDetail.NetAmount;
                            }
                            if (req.Data.isSupplementary) {
                                if (BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmtSuppl = BillDetail.DiscountAmount - (BillDetail.AgreementDiscountAmt || 0);
                                }
                                if (BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    discAmtSuppl = BillDetail.AgreementDiscountAmt || 0;
                                }
                            } else {
                                if (!BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmt = BillDetail.DiscountAmount;
                                }
                                if (!BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    discAmt = BillDetail.AgreementDiscountAmt;
                                }
                            }

                            TotalDiscount += discAmt;
                            TotSupplmDiscDrug += discAmtSuppl;
                            TotalInsuranceAmount += (BillDetail.InsNetAmount);
                            TotalPatientAmount += (BillDetail.PatNetAmount);
                            if (req.Data.isSupplementary) {
                                summaryTotal = bill.ActualPatAmount;
                            } else {
                                summaryTotal = bill.ActualNetAmount + (bill.ActualPatAmount || 0);
                            }
                            if (req.Data.isSupplementary) {
                                if (BillDetail.IsSupplementary) {
                                    splitDetails.push(BillDetail);
                                }
                            } else {
                                splitDetails.push(BillDetail);
                            }
                        }
                    }
                }

                let ServiceCategoryInfo = await servicecategoryBo.GetServiceCategoryById({ Id: bill.ServiceCategoryId });
                summary.ServiceName = ServiceCategoryInfo.ServiceCategoryName;
                // let withpharmacysales: any = [];
                let orderbydatespldet = _.orderBy(splitDetails, ['ServiceId', 'BillDateTime']);
                // let roomGroups: any = {};
                let serviceitemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
                // for (let sdx in orderbydatespldet) {
                //     let detail = orderbydatespldet[sdx];
                //     if (detail.ServiceId > 0) {
                //         let bIsRoomCharge: boolean = false;
                //         let servReq = {
                //             Id: 0,
                //             PageContext: { PageSize: -1, PageNumber: 1 },
                //             Params: [{ Key: 0, Value: detail.ServiceId }]
                //         };
                //         let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                //         if (Servicedata.Data.length > 0) {
                //             let ServiceInfo: any = Servicedata.Data[0];
                //             detail.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                //             if (ServiceInfo) {
                //                 if (ServiceInfo.IsBedCharge) {
                //                     bIsRoomCharge = true;
                //                     let dateformat = 'DD/MM/YYYY';
                //                     if (roomGroups[detail.ServiceId]) {
                //                         let billdt = moment(detail.BillDateTime).format(dateformat);
                //                         roomGroups[detail.ServiceId].BTDt = billdt;
                //                         roomGroups[detail.ServiceId].Quantity += detail.Quantity;
                //                         roomGroups[detail.ServiceId].Amount += detail.GrossAmount;
                //                         roomGroups[detail.ServiceId].DiscountAmount += detail.DiscountAmount;
                //                         roomGroups[detail.ServiceId].AgreementDiscountAmt += detail.AgreementDiscountAmt;
                //                         roomGroups[detail.ServiceId].NetAmount +=
                //                             (detail.GrossAmount - detail.DiscountAmount - (detail.AgreementDiscountAmt || 0));
                //                     } else {
                //                         roomGroups[detail.ServiceId] = roomGroups[detail.ServiceId] || [];
                //                         let billdt1 = moment(detail.BillDateTime).format(dateformat);
                //                         roomGroups[detail.ServiceId].ServiceName = detail.ServiceName;
                //                         roomGroups[detail.ServiceId].BFDt = billdt1;
                //                         roomGroups[detail.ServiceId].BTDt = billdt1;
                //                         roomGroups[detail.ServiceId].User = detail.User;
                //                         roomGroups[detail.ServiceId].BillDateTime = detail.BillDateTime;
                //                         roomGroups[detail.ServiceId].Quantity = detail.Quantity;
                //                         roomGroups[detail.ServiceId].Rate = detail.Rate;
                //                         roomGroups[detail.ServiceId].Amount = detail.GrossAmount;
                //                         roomGroups[detail.ServiceId].DiscountAmount = detail.DiscountAmount;
                //                         roomGroups[detail.ServiceId].AgreementDiscountAmt += detail.AgreementDiscountAmt;
                //                         roomGroups[detail.ServiceId].NetAmount =
                //                             (detail.GrossAmount - detail.DiscountAmount - (detail.AgreementDiscountAmt || 0));
                //                         roomGroups[detail.ServiceId].Id = detail.Id;
                //                         detail.Amount = detail.GrossAmount;
                //                         detail.NetAmount =
                //                             detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //                         withpharmacysales.push(detail);
                //                     }
                //                 }
                //             }
                //         }
                //         if (!detail.IsDoctorDisplay && !bIsRoomCharge) {
                //             // detail.User = null;
                //             detail.Amount = detail.GrossAmount;
                //             detail.NetAmount =
                //                 detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //             withpharmacysales.push(detail);
                //         } else if (detail.IsDoctorDisplay && !bIsRoomCharge) {
                //             detail.Amount = detail.GrossAmount;
                //             detail.NetAmount =
                //                 detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //             withpharmacysales.push(detail);
                //         } else {
                //             for (let indx in withpharmacysales) {
                //                 if (withpharmacysales[indx].Id === roomGroups[detail.ServiceId].Id) {
                //                     withpharmacysales[indx].ServiceName = roomGroups[detail.ServiceId].ServiceName;
                //                     withpharmacysales[indx].Quantity = roomGroups[detail.ServiceId].Quantity;
                //                     withpharmacysales[indx].Rate = roomGroups[detail.ServiceId].Rate;
                //                     withpharmacysales[indx].Amount = roomGroups[detail.ServiceId].Amount;
                //                     withpharmacysales[indx].DiscountAmount = roomGroups[detail.ServiceId].DiscountAmount;
                //                     withpharmacysales[indx].AgreementDiscountAmt = roomGroups[detail.ServiceId].AgreementDiscountAmt;
                //                     withpharmacysales[indx].NetAmount =
                //                         roomGroups[detail.ServiceId].NetAmount;
                //                 }
                //             }
                //         }
                //     }
                // }
                // await Promise.all(orderbydatespldet.map((orderbydatespldetItem: any): Promise<void> => {
                //     return (async (detail): Promise<void> => {

                //     })(orderbydatespldetItem);
                // }));
                let nondrugdetails: any = [];
                // let facilityprebo = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                // let showDrugDetails = 0;
                for (let idx in orderbydatespldet) {
                    let drugitems = orderbydatespldet[idx];
                    let servReq = {
                        Id: 0,
                        PageContext: { PageSize: -1, PageNumber: 1 },
                        Params: [{ Key: 0, Value: drugitems.ServiceId }]
                    };
                    let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                    if (Servicedata.Data.length > 0) {
                        let ServiceInfo: any = Servicedata.Data[0];
                        drugitems.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                    }
                    // let printPreferencesData = await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
                    // if (printPreferencesData && printPreferencesData.showdrugdetails) {
                    //     try {
                    //         showDrugDetails = parseInt(printPreferencesData.showdrugdetails);
                    //     } catch (ex) { showDrugDetails = 0; }
                    // }
                    // if (showDrugDetails === 0 || !showDrugDetails) {
                    //     if (!drugitems.IsPharmacySale && !drugitems.IsPharmacyReturn) {
                    //         if (req.Data.isSupplementary) {
                    //             drugitems.NetValue = drugitems.PatNetAmount;
                    //         } else if (drugitems.InsNetAmount > 0) {
                    //             drugitems.NetValue = drugitems.InsNetAmount;
                    //         } else {
                    //             drugitems.NetValue = drugitems.NetAmount;
                    //         }
                    //         nondrugdetails.push(drugitems);
                    //     }
                    // } else if (showDrugDetails === 1) {
                    if (req.Data.IsSupplementary) {
                        drugitems.NetValue = drugitems.PatNetAmount;
                    } else if (drugitems.InsNetAmount > 0) {
                        drugitems.NetValue = drugitems.InsNetAmount;
                    } else {
                        drugitems.NetValue = drugitems.NetAmount;
                    }
                    nondrugdetails.push(drugitems);
                    // }
                }
                let Date_sort = function (a: any, b: any) {
                    if (a.BillDateTime < b.BillDateTime) {
                        return -1;
                    }
                    if (a.BillDateTime > b.BillDateTime) {
                        return 1;
                    }
                    return 0;
                };
                nondrugdetails.sort(Date_sort);
                summary['SplitedValue'] = nondrugdetails;
                summary['Amount'] = summaryTotal;
                if (splitDetails.length > 0)
                    BillDetails.push(summary);


                let custom_sort = function (a: any, b: any) {
                    return parseInt(a.ServiceCategory.DisplayOrder) - parseInt(b.ServiceCategory.DisplayOrder);
                };
                BillDetails.sort(custom_sort);
            })(splitItem);
        }));
        let DueCollect: number = 0;
        let PaidAmount: number = 0;
        let ReceiptAmount: number = 0;
        let Advance: number = 0;
        let ReceiptDetails = [];
        let encType: any;
        if (Encounter.IsDayCare) {
            encType = 5;
        } else {
            encType = 2;
        }
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.EncounterId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.EncounterTypeId, Value: encType },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: false },
            { Key: PatientPaymentDetailsFilters.IsConsolidatePay, Value: false },
            { Key: PatientPaymentDetailsFilters.StatusOfReceipts, Value: [1, 4] }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        for (let idx in PatientPaymentDetailsData.Data) {
            let ReceiptItem = PatientPaymentDetailsData.Data[idx];
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1) {
                PaidAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1 &&
                PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 2) {
                ReceiptAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 1) {
                Advance += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 3)
                DueCollect += PatientPaymentDetailsData.Data[idx].AmountPaid;
            ReceiptDetails.push(ReceiptItem);
        }
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.EncounterId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 },
            { Key: PatientRefundFilters.EncounterTypeId, Value: encType },
            { Key: PatientRefundFilters.IsCashToCredit, Value: false }]
        };
        let RefundAmount: number = 0;
        let PartialRefundAmount: number = 0;
        let RefundDetails = [];
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        for (var idx1 in PatientRefundData.Data) {
            var RefundItem = PatientRefundData.Data[idx1];
            if (PatientRefundData.Data[idx1].RefundStatusId === 1) {
                RefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            }
            if (PatientRefundData.Data[idx1].RefundTypeId === 1 || PatientRefundData.Data[idx1].RefundTypeId === 4)
                PartialRefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            RefundDetails.push(RefundItem);
        }
        let UserReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: UserFilters.Id, Value: req.Data.PrintUser }]
        };
        let UsersBo = BoFactory.GetBo(userbo.UserBo, this.Request);
        let PrintUser = await UsersBo.GetUsers(UserReq);
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        // let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        // let printStoreData =
        //     await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let GuarantorId = Encounter.GuarantorId;
        let GuarantorTypeId = Encounter.GuarantorTypeId;
        let PatientGuarantorReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.Id, Value: GuarantorId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorReq);
        let PatientGuarantor = PatientGuarantorData.Data[0];
        if (PatientBills.RoundOffValue) {
            RoundOffValue = Math.round(PatientBills.RoundOffValue);
        } else {
            RoundOffValue = 0;
        }
        let BalanceAmount: number = 0;
        if (!isFinalized) {
            TotalNetAmount = Math.round(TotalNetAmount) + Math.round(TotalGSTAmount);
            if (GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }
        }
        let QrInfo: any;
        if (isFinalized) {
            TotalNetAmount = Math.round(TotalGrossAmount - PatientBills.BillDiscount);
            if (PatientBills.GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }


            let PatName: '';
            let FacInfo: '';

            if (patientData.Title) {
                PatName = patientData.Title.Description;
            }
            if (patientData.FirstName) {
                PatName += ' ' + patientData.FirstName;
            }
            if (patientData.LastName) {
                PatName += ' ' + patientData.LastName;
            }
            if (PatientBills.Facility) {
                FacInfo = PatientBills.Facility.FacilityName;
            }
            if (PatientBills.Facility.AddressLine1) {
                FacInfo += ', ' + PatientBills.Facility.AddressLine1;
            }
            if (PatientBills.Facility.Mobile) {
                FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
            }
            if (PatientBills.Facility.Email) {
                FacInfo += ',Email: ' + PatientBills.Facility.Email;
            }
            if (PatientBills.Facility.GstNumber) {
                FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
            }
            QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
                PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        }
        // console.log('********11*******');
        let info = {
            PatientBills: PatientBills,
            Encounter: Encounter,
            Patient: patientData,
            BillDetails: BillDetails,
            Age: Age,
            PatientPaymentDetails: ReceiptDetails,
            TotalGrossAmount: Math.round(TotalGrossAmount),
            TotalGSTAmount: Math.round(TotalGSTAmount),
            TotalNetAmount: Math.round(TotalNetAmount),
            TotalDiscount: Math.round(TotalDiscount),
            TotSupplmDiscDrug: TotSupplmDiscDrug,
            TotalInsuranceAmount: Math.round(TotalInsuranceAmount),
            TotalPatientAmount: Math.round(TotalPatientAmount),
            DueCollect: Math.round(DueCollect),
            PaidAmount: Math.round(PaidAmount),
            ReceiptAmount: Math.round(ReceiptAmount),
            Advance: Math.round(Advance),
            NetAmount: (PatientBills.BillAmount + RoundOffValue) - PatientBills.BillDiscount,
            BalanceAmount: Math.round(BalanceAmount),
            currentdate: new Date(),
            PrintUser: PrintUser.Data[0],
            PatientRefund: PatientRefundData.Data,
            RefundAmount: Math.round(RefundAmount),
            PartialRefundAmount: Math.round(PartialRefundAmount),
            Preferences: printPreferencesData,
            PatientGuarantor: PatientGuarantor,
            Referrals: ReferralsData.Data[0],
            QrInfo: QrInfo,
            GstAmount: GstAmount,
            Flags: flags
        };
        let key = 'InpatientBillDetail(i)';
        let pdfOption: any = null;
        {
            if (isFinalized) {
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientBillDetail';
                }
            }
            if (req.Data.isGuarantor) {
                key = 'InpatientbillDetailsGuarantor(i)';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientbillDetailsGuarantor';
                }
            }
            if (req.Data.isSupplementary) {
                key = 'Supplementry';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2
                    && info.BillDetails.IsSupplementary === true) {
                    key = 'Supplementry';
                }
            }
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'portrait',
                    border: '0',
                    header: {
                        height: '1in',
                        contents: '',
                    },
                    footer: {
                        height: '1in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
                console.log(pdfOption);
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async NewPrintInpatientBills(req: BaseRequest): Promise<FileInfo> {
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withHeader) ? req.Data.withoutHeader : 0,
            payment: (req.Data.paymentDetail) ? req.Data.paymentDetail : 0,
            nonmedical: (req.Data.nonmedical) ? req.Data.nonmedical : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.patientBill) ? req.Data.insuranceBill : 0,
        };
        let isFinalized: boolean = req.Data.isFinalized;
        let PatientBills: any = {};
        if (isFinalized) {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 2 }, { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
                { Key: PatientBillsFilters.IsPharmacyBill, Value: false }]
            };
            let data = await this.GetPatientBills(apiReq);
            PatientBills = data.Data[0];
        }
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let patientData: any = {};
        patientData = await patientBo.GetPatientById({ Id: Encounter.PatientId });
        let Age = '';
        if (patientData.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientData.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientData.Age > 0) {
            Age = patientData.Age + 'y';
        }
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientData.ReferrerId) {
            ReferralId = patientData.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let TotalGrossAmount: number = 0;
        let TotalInsuranceAmount: number = 0;
        let TotalPatientAmount: number = 0;
        let TotalGSTAmount: number = 0;
        let TotalNetAmount: number = 0;
        let RoundOffValue: number = 0;
        let TotalDiscount: number = 0;
        let NetVal: number = 0;
        let TotSupplmDiscDrug: number = 0;
        let TotMouDisc: number = 0;
        let GstAmount = 0;
        let BillDetails: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillSummaryFilters.EncounterId, Value: req.Id }]
        };
        let facilityprebo = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData = await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
        let showDrugDetails = 0;
        if (printPreferencesData && printPreferencesData.showdrugdetails) {
            try {
                showDrugDetails = parseInt(printPreferencesData.showdrugdetails);
            } catch (ex) { showDrugDetails = 0; }
        }
        let servicecategoryBo = BoFactory.GetBo(clinicalmasterBO.ServiceCategoryBo, this.Request);
        let billSummaryBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientBillSummary = await billSummaryBo.GetPatientBillSummarys(apiReq);

        await Promise.all(PatientBillSummary.Data.map((splitItem): Promise<void> => {
            return (async (bill): Promise<void> => {
                let summary: any = bill;
                if (req.Data.isSupplementary) {
                    TotalGrossAmount += bill.ActualPatAmount;
                } else {
                    TotalGrossAmount += bill.ActualAmount;
                }
                TotalDiscount += bill.DiscountAmount;
                TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount) + (bill.ActualPatAmount || 0);
                TotalGSTAmount += bill.TaxAmount;
                let summaryTotal: number = 0;
                let splitDetails: any = [];
                let billDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.ServiceCategoryId, Value: bill.ServiceCategoryId },
                    { Key: PatientBillDetailsFilters.EncounterId, Value: bill.EncounterId },
                    { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 },
                    { Key: PatientBillDetailsFilters.Quantity, Value: '0' }]
                };
                let DetailData = await PatientBillDetailsBo.GetPatientBillDetails(billDetailReq);
                if (DetailData.Data.length > 0) {
                    for (let bdx in DetailData.Data) {
                        let BillDetail: any = DetailData.Data[bdx];
                        if (BillDetail.PatientBill.BillTypeId !== 1) {
                            let discAmtSuppl: number = 0;
                            let discAmt: number = 0;
                            let moudiscamt: number = 0;
                            if (req.Data.isSupplementary) {
                                NetVal = BillDetail.PatNetAmount;
                            } else if (BillDetail.InsNetAmount > 0) {
                                NetVal = BillDetail.InsNetAmount;
                            } else {
                                NetVal = BillDetail.NetAmount;
                            }
                            if (req.Data.isSupplementary) {
                                if (BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmtSuppl = BillDetail.DiscountAmount - (BillDetail.AgreementDiscountAmt || 0);
                                }
                                if (BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    discAmtSuppl = BillDetail.AgreementDiscountAmt || 0;
                                }
                            } else {
                                if (!BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmt = BillDetail.DiscountAmount;
                                }
                                if (!BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    moudiscamt = BillDetail.AgreementDiscountAmt;
                                }
                            }

                            TotalDiscount += discAmt;
                            TotSupplmDiscDrug += discAmtSuppl;
                            TotMouDisc += moudiscamt;
                            TotalInsuranceAmount += (BillDetail.InsNetAmount);
                            TotalPatientAmount += (BillDetail.PatNetAmount);
                            if (req.Data.isSupplementary) {
                                summaryTotal = bill.ActualPatAmount;
                            } else {
                                summaryTotal = bill.ActualNetAmount + (bill.ActualPatAmount || 0);
                            }
                            if (req.Data.isSupplementary) {
                                if (BillDetail.IsSupplementary) {
                                    splitDetails.push(BillDetail);
                                }
                            } else {
                                splitDetails.push(BillDetail);
                            }
                        }
                    }
                }

                let ServiceCategoryInfo = await servicecategoryBo.GetServiceCategoryById({ Id: bill.ServiceCategoryId });
                summary.ServiceName = ServiceCategoryInfo.ServiceCategoryName;
                // let withpharmacysales: any = [];
                let orderbydatespldet = _.orderBy(splitDetails, ['ServiceId', 'BillDateTime']);
                // let roomGroups: any = {};
                // let serviceitemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);

                let nondrugdetails: any = [];

                for (let idx in orderbydatespldet) {
                    let drugitems = orderbydatespldet[idx];
                    // let servReq = {
                    //     Id: 0,
                    //     PageContext: { PageSize: -1, PageNumber: 1 },
                    //     Params: [{ Key: 0, Value: drugitems.ServiceId }]
                    // };
                    // let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                    // if (Servicedata.Data.length > 0) {
                    //     let ServiceInfo: any = Servicedata.Data[0];
                    //     drugitems.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                    // }
                    if (showDrugDetails === 0 || !showDrugDetails) {
                        if (!drugitems.IsPharmacySale && !drugitems.IsPharmacyReturn) {
                            if (req.Data.isSupplementary) {
                                drugitems.NetValue = drugitems.PatNetAmount;
                            } else if (drugitems.InsNetAmount > 0) {
                                drugitems.NetValue = drugitems.InsNetAmount;
                            } else {
                                drugitems.NetValue = drugitems.NetAmount;
                            }
                            nondrugdetails.push(drugitems);
                        }
                    } else if (showDrugDetails === 1) {
                        if (req.Data.IsSupplementary) {
                            drugitems.NetValue = drugitems.PatNetAmount;
                        } else if (drugitems.InsNetAmount > 0) {
                            drugitems.NetValue = drugitems.InsNetAmount;
                        } else {
                            drugitems.NetValue = drugitems.NetAmount;
                        }
                        nondrugdetails.push(drugitems);
                    }
                }
                let Date_sort = function (a: any, b: any) {
                    if (a.BillDateTime < b.BillDateTime) {
                        return -1;
                    }
                    if (a.BillDateTime > b.BillDateTime) {
                        return 1;
                    }
                    return 0;
                };
                nondrugdetails.sort(Date_sort);
                summary['SplitedValue'] = nondrugdetails;
                summary['Amount'] = summaryTotal;
                if (splitDetails.length > 0)
                    BillDetails.push(summary);


                let custom_sort = function (a: any, b: any) {
                    return parseInt(a.ServiceCategory.DisplayOrder) - parseInt(b.ServiceCategory.DisplayOrder);
                };
                BillDetails.sort(custom_sort);
            })(splitItem);
        }));
        let DueCollect: number = 0;
        let PaidAmount: number = 0;
        let ReceiptAmount: number = 0;
        let Advance: number = 0;
        let ReceiptDetails = [];
        let encType: any;
        if (Encounter.IsDayCare) {
            encType = 5;
        } else {
            encType = 2;
        }
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.EncounterId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.EncounterTypeId, Value: encType },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: false },
            { Key: PatientPaymentDetailsFilters.IsConsolidatePay, Value: false },
            { Key: PatientPaymentDetailsFilters.StatusOfReceipts, Value: [1, 4] }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        for (let idx in PatientPaymentDetailsData.Data) {
            let ReceiptItem = PatientPaymentDetailsData.Data[idx];
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1) {
                PaidAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1 &&
                PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 2) {
                ReceiptAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 1) {
                Advance += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 3)
                DueCollect += PatientPaymentDetailsData.Data[idx].AmountPaid;
            ReceiptDetails.push(ReceiptItem);
        }
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.EncounterId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 },
            { Key: PatientRefundFilters.EncounterTypeId, Value: encType },
            { Key: PatientRefundFilters.IsCashToCredit, Value: false }]
        };
        let RefundAmount: number = 0;
        let PartialRefundAmount: number = 0;
        let RefundDetails = [];
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        for (var idx1 in PatientRefundData.Data) {
            var RefundItem = PatientRefundData.Data[idx1];
            if (PatientRefundData.Data[idx1].RefundStatusId === 1) {
                RefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            }
            if (PatientRefundData.Data[idx1].RefundTypeId === 1 || PatientRefundData.Data[idx1].RefundTypeId === 4)
                PartialRefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            RefundDetails.push(RefundItem);
        }
        let UserReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: UserFilters.Id, Value: req.Data.PrintUser }]
        };
        let UsersBo = BoFactory.GetBo(userbo.UserBo, this.Request);
        let PrintUser = await UsersBo.GetUsers(UserReq);
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        // let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        // let printStoreData =
        //     await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let GuarantorId = Encounter.GuarantorId;
        let GuarantorTypeId = Encounter.GuarantorTypeId;
        let PatientGuarantorReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.Id, Value: GuarantorId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorReq);
        let PatientGuarantor = PatientGuarantorData.Data[0];
        if (PatientBills.RoundOffValue) {
            RoundOffValue = Math.round(PatientBills.RoundOffValue);
        } else {
            RoundOffValue = 0;
        }
        let BalanceAmount: number = 0;
        if (!isFinalized) {
            TotalNetAmount = Math.round(TotalNetAmount) + Math.round(TotalGSTAmount);
            if (GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }
        }
        let QrInfo: any;
        if (isFinalized) {
            TotalNetAmount = Math.round(TotalGrossAmount - PatientBills.BillDiscount);
            if (PatientBills.GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }


            let PatName: '';
            let FacInfo: '';

            if (patientData.Title) {
                PatName = patientData.Title.Description;
            }
            if (patientData.FirstName) {
                PatName += ' ' + patientData.FirstName;
            }
            if (patientData.LastName) {
                PatName += ' ' + patientData.LastName;
            }
            if (PatientBills.Facility) {
                FacInfo = PatientBills.Facility.FacilityName;
            }
            if (PatientBills.Facility.AddressLine1) {
                FacInfo += ', ' + PatientBills.Facility.AddressLine1;
            }
            if (PatientBills.Facility.Mobile) {
                FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
            }
            if (PatientBills.Facility.Email) {
                FacInfo += ',Email: ' + PatientBills.Facility.Email;
            }
            if (PatientBills.Facility.GstNumber) {
                FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
            }
            QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
                PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        }
        // console.log('********11*******');
        let info = {
            PatientBills: PatientBills,
            Encounter: Encounter,
            Patient: patientData,
            BillDetails: BillDetails,
            Age: Age,
            PatientPaymentDetails: ReceiptDetails,
            TotalGrossAmount: Math.round(TotalGrossAmount),
            TotalGSTAmount: Math.round(TotalGSTAmount),
            TotalNetAmount: Math.round(TotalNetAmount),
            TotalDiscount: Math.round(TotalDiscount),
            TotSupplmDiscDrug: TotSupplmDiscDrug,
            TotalInsuranceAmount: Math.round(TotalInsuranceAmount),
            TotalPatientAmount: Math.round(TotalPatientAmount),
            DueCollect: Math.round(DueCollect),
            PaidAmount: Math.round(PaidAmount),
            ReceiptAmount: Math.round(ReceiptAmount),
            Advance: Math.round(Advance),
            NetAmount: (PatientBills.BillAmount + RoundOffValue) - PatientBills.BillDiscount,
            BalanceAmount: Math.round(BalanceAmount),
            currentdate: new Date(),
            PrintUser: PrintUser.Data[0],
            PatientRefund: PatientRefundData.Data,
            RefundAmount: Math.round(RefundAmount),
            PartialRefundAmount: Math.round(PartialRefundAmount),
            Preferences: printPreferencesData,
            PatientGuarantor: PatientGuarantor,
            Referrals: ReferralsData.Data[0],
            QrInfo: QrInfo,
            GstAmount: GstAmount,
            TotMouDisc: TotMouDisc,
            Flags: flags
        };
        let key = 'InpatientBillDetail(i)';
        let pdfOption: any = null;
        {
            if (isFinalized) {
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientBillDetail';
                }
            }
            if (req.Data.isGuarantor) {
                key = 'InpatientbillDetailsGuarantor(i)';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientbillDetailsGuarantor';
                }
            }
            if (req.Data.isSupplementary) {
                key = 'Supplementry';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2
                    && info.BillDetails.IsSupplementary === true) {
                    key = 'Supplementry';
                }
            }
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'portrait',
                    border: '0',
                    header: {
                        height: '1in',
                        contents: '',
                    },
                    footer: {
                        height: '1in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
                console.log(pdfOption);
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintInsuranceCreditSummary(req: BaseRequest): Promise<any> {
        let FromDate = req.Data.FromDate;
        let ToDate = req.Data.ToDate;
        let FacilityId = req.Data.FacilityId;
        let FacilityName = req.Data.FacilityName;
        let InsuranceName = req.Data.InsuranceName;
        let InsuranceCollection: any = [];
        let InsReq = req;
        InsuranceCollection = await this.GetInsuranceCreditSummary(InsReq);
        // DoctorData = DoctorData;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(FacilityId);
        let InsCollection = [];
        if (InsuranceCollection) {
            let inscreditcollections = [];
            if (InsuranceCollection.length > 0) {
                inscreditcollections = InsuranceCollection[0].Value;
            }

            for (let idx in inscreditcollections) {
                let insname = inscreditcollections[idx];
                let Key = '';
                let OPCredit = 0;
                let IPCredit = 0;
                let DueAmt = 0;
                // let DueCollectAmt = 0;
                for (let ix in insname) {
                    InsuranceName = '';
                    if (insname[ix].GuarantorName) {
                        InsuranceName = insname[ix].GuarantorName;
                    }
                    if (insname[ix].OPCredit) {
                        OPCredit = insname[ix].OPCredit;
                    }
                    if (insname[ix].IPCredit) {
                        IPCredit = insname[ix].IPCredit;
                    }
                    if (insname[ix].TotalOutstanding) {
                        DueAmt = insname[ix].TotalOutstanding;
                    }
                    Key = InsuranceName;
                    OPCredit = OPCredit;
                    IPCredit = IPCredit;
                    DueAmt = DueAmt;
                    // DueCollectAmt = DueCollectAmt;
                }
                InsCollection.push({
                    'Key': Key,
                    'Value': {
                        'OPCredit': OPCredit,
                        'IPCredit': IPCredit,
                        'DueAmt': DueAmt
                    }
                });
            }
        }
        let Totopcredit = 0;
        let Totipcredit = 0;
        let Totdueamt = 0;
        for (let jdx in InsCollection) {
            let insdata = InsCollection[jdx];
            Totopcredit += insdata.Value.OPCredit;
            Totipcredit += insdata.Value.IPCredit;
            Totdueamt += insdata.Value.DueAmt;
        }

        let info = {
            FromDate: FromDate,
            ToDate: ToDate,
            Preferences: printPreferencesData,
            InsuranceName: InsuranceName,
            FacilityName: FacilityName,
            InsCollection: InsCollection,
            Totopcredit: Totopcredit,
            Totipcredit: Totipcredit,
            Totdueamt: Totdueamt

        };
        let pdfOption: any = null;
        let key = 'insurancecreditsummary';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintInsuranceOutstandingSummary(req: BaseRequest): Promise<any> {
        let FromDate = req.Data.FromDate;
        let ToDate = req.Data.ToDate;
        let FacilityId = req.Data.FacilityId;
        let FacilityName = req.Data.FacilityName;
        let InsuranceName = req.Data.InsuranceName;
        let InsuranceCollection: any = [];
        let InsReq = req;
        InsuranceCollection = await this.GetInsuranceOutstandingSummary(InsReq);
        // DoctorData = DoctorData;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(FacilityId);
        let InsCollection = [];


        if (InsuranceCollection) {
            let inscreditcollections = [];
            if (InsuranceCollection.length > 0) {
                inscreditcollections = InsuranceCollection[0].Value;
            }

            for (let idx in inscreditcollections) {
                let insname = inscreditcollections[idx];
                let Key = '';
                let InsuranceType = '';
                let OPOutstanding = 0;
                let IPOutstanding = 0;
                let DueAmt = 0;
                // let DueCollectAmt = 0;
                for (let ix in insname) {
                    InsuranceName = '';
                    if (insname[ix].GuarantorName) {
                        InsuranceName = insname[ix].GuarantorName;
                    }
                    if (insname[ix].GuarantorType) {
                        InsuranceType = insname[ix].GuarantorType;
                    }
                    if (insname[ix].OPOutstanding) {
                        OPOutstanding = insname[ix].OPOutstanding;
                    }
                    if (insname[ix].IPOutstanding) {
                        IPOutstanding = insname[ix].IPOutstanding;
                    }
                    if (insname[ix].TotalOutstanding) {
                        DueAmt = insname[ix].TotalOutstanding;
                    }
                    Key = InsuranceName;
                    OPOutstanding = OPOutstanding;
                    IPOutstanding = IPOutstanding;
                    DueAmt = DueAmt;
                    // DueCollectAmt = DueCollectAmt;
                }
                InsCollection.push({
                    'Key': Key,
                    'InsuranceType': InsuranceType,
                    'OPOutstanding': OPOutstanding,
                    'IPOutstanding': IPOutstanding,
                    'DueAmt': DueAmt
                });
            }
        }
        let Totopoutstanding = 0;
        let Totipoutstanding = 0;
        let Totdueamt = 0;
        for (let jdx in InsCollection) {
            let insdata = InsCollection[jdx];
            Totopoutstanding += insdata.OPOutstanding;
            Totipoutstanding += insdata.IPOutstanding;
            Totdueamt += insdata.DueAmt;
        }

        let info = {
            FromDate: FromDate,
            ToDate: ToDate,
            Preferences: printPreferencesData,
            InsuranceName: InsuranceName,
            FacilityName: FacilityName,
            InsCollection: InsCollection,
            Totopoutstanding: Totopoutstanding,
            Totipoutstanding: Totipoutstanding,
            Totdueamt: Totdueamt

        };
        let pdfOption: any = null;
        let key = 'insuranceoutstandingsummary';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintStaffCreditSummary(req: BaseRequest): Promise<any> {
        let dateformat = 'DD/MM/YYYY';
        let timeformat = 'HH:mm:ss';
        let fromdate = moment(req.Data.FromDate).format(dateformat);
        let FromTime = moment(req.Data.FromDate).format(timeformat);
        let FromDate = fromdate + ' ' + FromTime;

        let todate = moment(req.Data.ToDate).format(dateformat);
        let totime = moment(req.Data.ToDate).format(timeformat);
        let ToDate = todate + ' ' + totime;

        let FacilityId = req.Data.FacilityId;
        let FacilityName = req.Data.FacilityName;
        let StaffName = req.Data.StaffName;
        let StaffCollection: any = [];
        let StaffReq = req;
        StaffCollection = await this.GetStaffCreditSummary(StaffReq);
        // DoctorData = DoctorData;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(FacilityId);
        let StaffSummary = [];


        if (StaffCollection) {
            let staffcreditcollections = [];
            if (StaffCollection.length > 0) {
                staffcreditcollections = StaffCollection[0].Value;
            }

            for (let idx in staffcreditcollections) {
                let StaffCol = staffcreditcollections[idx];
                let Key = '';
                let BillAmount = 0;
                let PaidAmount = 0;
                let DueAmt = 0;
                // let DueCollectAmt = 0;
                for (let ix in StaffCol) {
                    StaffName = '';
                    if (StaffCol[ix].StaffName) {
                        if (StaffCol[ix].StaffName.Title)
                            StaffName = StaffCol[ix].StaffName.Title.Description;
                        if (StaffCol[ix].StaffName.FirstName)
                            StaffName += ' ' + StaffCol[ix].StaffName.FirstName;
                        if (StaffCol[ix].StaffName.LastName)
                            StaffName += ' ' + StaffCol[ix].StaffName.LastName;
                    }
                    if (StaffCol[ix].BillAmount) {
                        BillAmount = StaffCol[ix].BillAmount;
                    }
                    if (StaffCol[ix].PaidAmount) {
                        PaidAmount = StaffCol[ix].PaidAmount;
                    }
                    if (StaffCol[ix].TotalOutstanding) {
                        DueAmt = StaffCol[ix].TotalOutstanding;
                    }
                    Key = StaffName;
                    BillAmount = BillAmount;
                    PaidAmount = PaidAmount;
                    DueAmt = DueAmt;
                    // DueCollectAmt = DueCollectAmt;
                }
                StaffSummary.push({
                    'Key': Key,
                    'Value': {
                        'BillAmount': BillAmount,
                        'PaidAmount': PaidAmount,
                        'DueAmt': DueAmt
                    }
                });
            }
        }
        let TotBillAmount = 0;
        let TotPaidAmount = 0;
        let Totdueamt = 0;
        for (let jdx in StaffSummary) {
            let staffdata = StaffSummary[jdx];
            TotBillAmount += staffdata.Value.BillAmount;
            TotPaidAmount += staffdata.Value.PaidAmount;
            Totdueamt += staffdata.Value.DueAmt;
        }

        let info = {
            FromDate: FromDate,
            ToDate: ToDate,
            Preferences: printPreferencesData,
            StaffName: StaffName,
            FacilityName: FacilityName,
            StaffSummary: StaffSummary,
            TotBillAmount: TotBillAmount,
            TotPaidAmount: TotPaidAmount,
            Totdueamt: Totdueamt

        };
        let pdfOption: any = null;
        let key = 'staffcreditsummary';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintInpatientBillDetails(req: BaseRequest): Promise<FileInfo> {

        let isFinalized: boolean = req.Data.isFinalized;
        let flags = {
            header: (req.Data.withHeader) ? req.Data.withHeader : 0,
            woheader: (req.Data.withoutHeader) ? req.Data.withoutHeader : 0,
            payment: (req.Data.paymentDetail) ? req.Data.paymentDetail : 0,
            nonmedical: (req.Data.nonmedical) ? req.Data.nonmedical : 0,
            patientbill: (req.Data.patientBill) ? req.Data.patientBill : 0,
            insurancebill: (req.Data.insuranceBill) ? req.Data.insuranceBill : 0,
            drugBill: (req.Data.drugBill) ? req.Data.drugBill : 0,
        };
        let TotalMOUDisc = req.Data.MOUDisc;
        let PatientBills: any = {};
        if (isFinalized) {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 2 },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
                { Key: PatientBillsFilters.IsPharmacyBill, Value: false }]
            };
            let data = await this.GetPatientBills(apiReq);
            PatientBills = data.Data[0];
        }
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let patientData: any = {};
        patientData = await patientBo.GetPatientById({ Id: Encounter.PatientId });
        let Age = '';
        if (patientData.Age === 0) {
            let diffDuration = moment.duration(moment().diff(patientData.DOB));
            let ageresult = '';
            let years = diffDuration.years();
            let months = diffDuration.months();
            let days = diffDuration.days();
            if (years > 0) {
                ageresult = diffDuration.years() + 'y ';
            } else if (years === 0) {
                if (months > 0) {
                    ageresult += diffDuration.months() + 'M ';
                }
                if (days > 0) {
                    ageresult += diffDuration.days() + 'D ';
                }
            }
            Age = ageresult;
        } else if (patientData.Age > 0) {
            Age = patientData.Age + 'y';
        }
        let ReferralId = 0;
        if (PatientBills.ReferralId) {
            ReferralId = PatientBills.ReferralId;
        }
        if (patientData.ReferrerId) {
            ReferralId = patientData.ReferrerId;
        }
        let ReferralsReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: ReferralFilters.Id, Value: ReferralId }]
        };
        let ReferralBo = BoFactory.GetBo(generalMasterBO.ReferralBo, this.Request);
        let ReferralsData = await ReferralBo.GetReferrals(ReferralsReq);
        let TotalGrossAmount: number = 0;
        let TotalInsuranceAmount: number = 0;
        let TotalPatientAmount: number = 0;
        let TotalGSTAmount: number = 0;
        let TotalNetAmount: number = 0;
        let RoundOffValue: number = 0;
        let TotalDiscount: number = 0;
        let NetVal: number = 0;
        let TotSupplmDiscDrug: number = 0;
        let TotMouDisc: number = 0;
        let GstAmount = 0;
        let BillDetails: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientBillSummaryFilters.EncounterId, Value: req.Id },
                { Key: PatientBillSummaryFilters.NotInServiceCategoryId, Value: [0] }
            ]
        };
        if (req.Data.drugBill === true) {
            apiReq.Params.push({ Key: PatientBillSummaryFilters.NotInServiceCategoryId, Value: [19, 20, 21, 22, 23, 24, 25, 26] });
        }
        let drugApiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientBillSummaryFilters.EncounterId, Value: req.Id },
                { Key: PatientBillSummaryFilters.ServiceCategoryId, Value: [19, 20, 21, 22, 23, 24, 25, 26] }
            ]
        };
        let servicecategoryBo = BoFactory.GetBo(clinicalmasterBO.ServiceCategoryBo, this.Request);
        let billSummaryBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientBillSummary = await billSummaryBo.GetPatientBillSummarys(apiReq);
        let PatientDrugBillSummary = await billSummaryBo.GetPatientBillSummarys(drugApiReq);

        await Promise.all(PatientBillSummary.Data.map((splitItem): Promise<void> => {
            return (async (bill: any): Promise<void> => {
                let summary: any = bill;
                if (req.Data.isSupplementary) {
                    TotalGrossAmount += bill.ActualPatAmount;
                } else {
                    if (!req.Data.nonmedical) {
                        TotalGrossAmount += bill.ActualAmount;
                    } else {
                        if (req.Data.nonmedical) {
                            TotalGrossAmount += bill.ActualAmount;
                        } else {
                            TotalGrossAmount += bill.ActualNetAmount;
                        }
                    }
                }
                // TotalDiscount += bill.DiscountAmount;
                // TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount) + (bill.ActualPatAmount || 0);
                if (req.Data.nonmedical) {
                    TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount);
                } else {
                    TotalNetAmount += (bill.ActualAmount - bill.DiscountAmount);
                }

                // TotalGSTAmount += bill.GSTAmount;
                TotalGSTAmount += bill.TaxAmount;
                let summaryTotal: any = 0;
                let summarywoMedicalTotal: any = 0;
                let splitDetails: any = [];
                let billDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.EncounterId, Value: bill.EncounterId },
                    { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 },
                    { Key: PatientBillDetailsFilters.Quantity, Value: '0' },
                    { Key: PatientBillDetailsFilters.IsTempIPBill, Value: true },
                    {
                        Key: PatientBillDetailsFilters.ServiceCategoryId,
                        Value: bill.ServiceCategoryId
                    }]
                };
                let DetailData = await PatientBillDetailsBo.GetPatientBillDetails(billDetailReq);
                if (DetailData.Data.length > 0) {
                    for (let bdx in DetailData.Data) {
                        let BillDetail: any = DetailData.Data[bdx];
                        if (BillDetail.PatientBill.BillTypeId !== 1) {
                            let discAmtSuppl: number = 0;
                            let discAmt: number = 0;
                            let moudiscamt: number = 0;
                            if (req.Data.isSupplementary) {
                                NetVal = BillDetail.PatNetAmount;
                            } else if (BillDetail.InsNetAmount > 0) {
                                NetVal = BillDetail.InsNetAmount;
                            } else {
                                NetVal = BillDetail.NetAmount;
                            }
                            if (req.Data.isSupplementary) {
                                if (BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmtSuppl = BillDetail.DiscountAmount - (BillDetail.AgreementDiscountAmt || 0);
                                }
                                if (BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    discAmtSuppl = BillDetail.AgreementDiscountAmt || 0;
                                }
                            } else {
                                if (!BillDetail.IsSupplementary && BillDetail.DiscountAmount > 0) {
                                    discAmt = BillDetail.DiscountAmount;
                                }
                                if (!BillDetail.IsSupplementary && BillDetail.AgreementDiscountAmt > 0) {
                                    moudiscamt = BillDetail.AgreementDiscountAmt;
                                }
                            }

                            TotalDiscount += discAmt;
                            TotSupplmDiscDrug += discAmtSuppl;
                            TotMouDisc += moudiscamt;
                            TotalInsuranceAmount += (BillDetail.InsNetAmount);
                            TotalPatientAmount += (BillDetail.PatNetAmount);
                            if (req.Data.isSupplementary) {
                                summaryTotal = parseFloat(bill.ActualPatAmount);
                            } else {
                                if (!req.Data.nonmedical) {
                                    summaryTotal = parseFloat(bill.ActualNetAmount);
                                } else {
                                    summaryTotal = parseFloat(bill.ActualNetAmount) + (parseFloat(bill.ActualPatAmount) || 0);
                                    summarywoMedicalTotal = parseFloat(BillDetail.InsNetAmount)
                                        + (parseFloat(BillDetail.PatNetAmount) || 0);
                                }
                            }
                            if (req.Data.isSupplementary) {
                                // if (BillDetail.IsSupplementary) {
                                //     splitDetails.push(BillDetail);
                                // }
                                splitDetails.push(BillDetail);
                            } else {
                                if (req.Data.nonmedical) {
                                    splitDetails.push(BillDetail);
                                } else {
                                    if (!BillDetail.IsSupplementary) {
                                        splitDetails.push(BillDetail);
                                    }
                                }
                            }
                        }
                    }
                }
                let ServiceCategoryInfo = await servicecategoryBo.GetServiceCategoryById({ Id: bill.ServiceCategoryId });
                summary.ServiceName = ServiceCategoryInfo.ServiceCategoryName;
                // let withpharmacysales: any = [];

                let orderbydatespldet = _.orderBy(splitDetails, ['ServiceId', 'BillDateTime']);
                // // let roomGroups: any = {};
                // let serviceitemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
                // for (let sdx in orderbydatespldet) {
                //     let detail = orderbydatespldet[sdx];
                //     if (detail.ServiceId > 0) {
                //         let bIsRoomCharge: boolean = false;
                //         let servReq = {
                //             Id: 0,
                //             PageContext: { PageSize: -1, PageNumber: 1 },
                //             Params: [{ Key: 0, Value: detail.ServiceId }]
                //         };
                //         let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                //         if (Servicedata.Data.length > 0) {
                //             let ServiceInfo: any = Servicedata.Data[0];
                //             detail.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                //             if (ServiceInfo) {
                //                 if (ServiceInfo.IsBedCharge) {
                //                     bIsRoomCharge = true;
                //                     let dateformat = 'DD/MM/YYYY';
                //                     if (roomGroups[detail.ServiceId]) {
                //                         let billdt = moment(detail.BillDateTime).format(dateformat);
                //                         roomGroups[detail.ServiceId].BTDt = billdt;
                //                         roomGroups[detail.ServiceId].Quantity += detail.Quantity;
                //                         roomGroups[detail.ServiceId].Amount += detail.GrossAmount;
                //                         roomGroups[detail.ServiceId].DiscountAmount += detail.DiscountAmount;
                //                         roomGroups[detail.ServiceId].AgreementDiscountAmt += detail.AgreementDiscountAmt;
                //                         roomGroups[detail.ServiceId].NetAmount +=
                //                             (detail.GrossAmount - detail.DiscountAmount - (detail.AgreementDiscountAmt || 0));
                //                     } else {
                //                         roomGroups[detail.ServiceId] = roomGroups[detail.ServiceId] || [];
                //                         let billdt1 = moment(detail.BillDateTime).format(dateformat);
                //                         roomGroups[detail.ServiceId].ServiceName = detail.ServiceName;
                //                         roomGroups[detail.ServiceId].BFDt = billdt1;
                //                         roomGroups[detail.ServiceId].BTDt = billdt1;
                //                         roomGroups[detail.ServiceId].User = detail.User;
                //                         roomGroups[detail.ServiceId].BillDateTime = detail.BillDateTime;
                //                         roomGroups[detail.ServiceId].Quantity = detail.Quantity;
                //                         roomGroups[detail.ServiceId].Rate = detail.Rate;
                //                         roomGroups[detail.ServiceId].Amount = detail.GrossAmount;
                //                         roomGroups[detail.ServiceId].DiscountAmount = detail.DiscountAmount;
                //                         roomGroups[detail.ServiceId].AgreementDiscountAmt += detail.AgreementDiscountAmt;
                //                         roomGroups[detail.ServiceId].NetAmount =
                //                             (detail.GrossAmount - detail.DiscountAmount - (detail.AgreementDiscountAmt || 0));
                //                         roomGroups[detail.ServiceId].Id = detail.Id;
                //                         detail.Amount = detail.GrossAmount;
                //                         detail.NetAmount =
                //                             detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //                         withpharmacysales.push(detail);
                //                     }
                //                 }
                //             }
                //         }
                //         if (!detail.IsDoctorDisplay && !bIsRoomCharge) {
                //             // detail.User = null;
                //             detail.Amount = detail.GrossAmount;
                //             detail.NetAmount =
                //                 detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //             withpharmacysales.push(detail);
                //         } else if (detail.IsDoctorDisplay && !bIsRoomCharge) {
                //             detail.Amount = detail.GrossAmount;
                //             detail.NetAmount =
                //                 detail.GrossAmount - (detail.DiscountAmount || 0) - (detail.AgreementDiscountAmt || 0);
                //             withpharmacysales.push(detail);
                //         } else {
                //             for (let indx in withpharmacysales) {
                //                 if (withpharmacysales[indx].Id === roomGroups[detail.ServiceId].Id) {
                //                     withpharmacysales[indx].ServiceName = roomGroups[detail.ServiceId].ServiceName;
                //                     withpharmacysales[indx].Quantity = roomGroups[detail.ServiceId].Quantity;
                //                     withpharmacysales[indx].Rate = roomGroups[detail.ServiceId].Rate;
                //                     withpharmacysales[indx].Amount = roomGroups[detail.ServiceId].Amount;
                //                     withpharmacysales[indx].DiscountAmount = roomGroups[detail.ServiceId].DiscountAmount;
                //                     withpharmacysales[indx].AgreementDiscountAmt = roomGroups[detail.ServiceId].AgreementDiscountAmt;
                //                     withpharmacysales[indx].NetAmount =
                //                         roomGroups[detail.ServiceId].NetAmount;
                //                 }
                //             }
                //         }
                //     }
                // }
                // await Promise.all(orderbydatespldet.map((orderbydatespldetItem: any): Promise<void> => {
                //     return (async (detail): Promise<void> => {

                //     })(orderbydatespldetItem);
                // }));
                let nondrugdetails: any = [];
                let facilityprebo = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let showDrugDetails = 0;
                let printPreferencesData = await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
                if (printPreferencesData && printPreferencesData.showdrugdetails) {
                    try {
                        showDrugDetails = parseInt(printPreferencesData.showdrugdetails);
                    } catch (ex) { showDrugDetails = 0; }
                }
                for (let idx in orderbydatespldet) {
                    let drugitems = orderbydatespldet[idx];
                    // let servReq = {
                    //     Id: 0,
                    //     PageContext: { PageSize: -1, PageNumber: 1 },
                    //     Params: [{ Key: 0, Value: drugitems.ServiceId }]
                    // };
                    if (drugitems.ServiceItem) {
                        console.log(drugitems.ServiceItem.IsDoctorDisplay);
                        drugitems.IsDoctorDisplay = (drugitems.ServiceItem.IsDoctorDisplay) ? 1 : 0;
                        console.log(drugitems.IsDoctorDisplay);
                    } else {
                        drugitems.IsDoctorDisplay = 0;
                    }
                    // drugitems.IsDoctorDisplay = 1;//Done on 17/2/24 by jothi -- Confirmed by abcd Sir
                    // let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                    // if (Servicedata.Data.length > 0) {
                    //     let ServiceInfo: any = Servicedata.Data[0];
                    //     drugitems.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                    // }
                    if (showDrugDetails === 1) {
                        if (req.Data.IsSupplementary) {
                            drugitems.NetValue = drugitems.PatNetAmount;
                        } else if (drugitems.InsNetAmount > 0) {
                            drugitems.NetValue = drugitems.InsNetAmount;
                        } else {
                            drugitems.NetValue = drugitems.NetAmount;
                        }
                        nondrugdetails.push(drugitems);
                    } else if (showDrugDetails === 0 || !showDrugDetails) {
                        if (!drugitems.IsPharmacySale && !drugitems.IsPharmacyReturn) {
                            if (req.Data.isSupplementary) {
                                drugitems.NetValue = drugitems.PatNetAmount;
                            } else if (drugitems.InsNetAmount > 0) {
                                drugitems.NetValue = drugitems.InsNetAmount;
                            } else {
                                drugitems.NetValue = drugitems.NetAmount;
                            }
                            nondrugdetails.push(drugitems);
                        }
                    }
                }
                let Date_sort = function (a: any, b: any) {
                    if (a.BillDateTime < b.BillDateTime) {
                        return -1;
                    }
                    if (a.BillDateTime > b.BillDateTime) {
                        return 1;
                    }
                    return 0;
                };
                nondrugdetails.sort(Date_sort);

                summary['SplitedValue'] = nondrugdetails;
                summary['Amount'] = summaryTotal;
                summary['WoNonMedicalAmount'] = summarywoMedicalTotal;
                if (splitDetails.length > 0)
                    BillDetails.push(summary);


                let custom_sort = function (a: any, b: any) {
                    return parseInt(a.ServiceCategory.DisplayOrder) - parseInt(b.ServiceCategory.DisplayOrder);
                };
                BillDetails.sort(custom_sort);
            })(splitItem);
        }));

        let TotalPharmacyGrossAmount: number = 0;
        let TotalPharmacyInsuranceAmount: number = 0;
        let TotalPharmacyPatientAmount: number = 0;
        let TotalPharmacyGSTAmount: number = 0;
        let TotalPharmacyNetAmount: number = 0;
        let TotalPharmacyDiscount: number = 0;
        let PharmacyNetVal: number = 0;
        let TotDrugSupplmDisc: number = 0;
        let TotDrugMouDisc: number = 0;
        let DrugBillDetail: any = [];
        await Promise.all(PatientDrugBillSummary.Data.map((splitItem): Promise<void> => {
            return (async (bill: any): Promise<void> => {
                let drugsummary: any = bill;
                if (req.Data.isSupplementary) {
                    TotalPharmacyGrossAmount += bill.ActualPatAmount;
                } else {
                    if (!req.Data.nonmedical) {
                        TotalPharmacyGrossAmount += bill.ActualAmount;
                    } else {
                        if (req.Data.nonmedical) {
                            TotalPharmacyGrossAmount += bill.ActualAmount;
                        } else {
                            TotalPharmacyGrossAmount += bill.ActualNetAmount;
                        }
                    }
                }
                if (req.Data.nonmedical) {
                    TotalPharmacyNetAmount += (bill.ActualAmount - bill.DiscountAmount);
                } else {
                    TotalPharmacyNetAmount += (bill.ActualAmount - bill.DiscountAmount);
                }

                TotalPharmacyGSTAmount += bill.TaxAmount;
                let drugsummaryTotal: any = 0;
                let drugsummarywoMedicalTotal: any = 0;
                let drugsplitDetail: any = [];
                let billDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.EncounterId, Value: bill.EncounterId },
                    { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 },
                    { Key: PatientBillDetailsFilters.Quantity, Value: '0' },
                    { Key: PatientBillDetailsFilters.IsTempIPBill, Value: true },
                    {
                        Key: PatientBillDetailsFilters.ServiceCategoryId,
                        Value: bill.ServiceCategoryId
                    }]
                };
                let DrugDetailData = await PatientBillDetailsBo.GetPatientBillDetails(billDetailReq);
                if (DrugDetailData.Data.length > 0) {
                    for (let bdx in DrugDetailData.Data) {
                        let DrugBillDetail: any = DrugDetailData.Data[bdx];
                        if (DrugBillDetail.PatientBill.BillTypeId !== 1) {
                            let discAmtSuppl: number = 0;
                            let discAmt: number = 0;
                            let moudiscamt: number = 0;
                            if (req.Data.isSupplementary) {
                                PharmacyNetVal = DrugBillDetail.PatNetAmount;
                            } else if (DrugBillDetail.InsNetAmount > 0) {
                                PharmacyNetVal = DrugBillDetail.InsNetAmount;
                            } else {
                                PharmacyNetVal = DrugBillDetail.NetAmount;
                            }
                            if (req.Data.isSupplementary) {
                                if (DrugBillDetail.IsSupplementary && DrugBillDetail.DiscountAmount > 0) {
                                    discAmtSuppl = DrugBillDetail.DiscountAmount - (DrugBillDetail.AgreementDiscountAmt || 0);
                                }
                                if (DrugBillDetail.IsSupplementary && DrugBillDetail.AgreementDiscountAmt > 0) {
                                    discAmtSuppl = DrugBillDetail.AgreementDiscountAmt || 0;
                                }
                            } else {
                                if (!DrugBillDetail.IsSupplementary && DrugBillDetail.DiscountAmount > 0) {
                                    discAmt = DrugBillDetail.DiscountAmount;
                                }
                                if (!DrugBillDetail.IsSupplementary && DrugBillDetail.AgreementDiscountAmt > 0) {
                                    moudiscamt = DrugBillDetail.AgreementDiscountAmt;
                                }
                            }

                            TotalPharmacyDiscount += discAmt;
                            TotDrugSupplmDisc += discAmtSuppl;
                            TotDrugMouDisc += moudiscamt;
                            TotalPharmacyInsuranceAmount += (DrugBillDetail.InsNetAmount);
                            TotalPharmacyPatientAmount += (DrugBillDetail.PatNetAmount);
                            if (req.Data.isSupplementary) {
                                drugsummaryTotal = parseFloat(bill.ActualPatAmount);
                            } else {
                                if (!req.Data.nonmedical) {
                                    drugsummaryTotal = parseFloat(bill.ActualNetAmount);
                                } else {
                                    drugsummaryTotal = parseFloat(bill.ActualNetAmount) + (parseFloat(bill.ActualPatAmount) || 0);
                                    drugsummarywoMedicalTotal = parseFloat(DrugBillDetail.InsNetAmount)
                                        + (parseFloat(DrugBillDetail.PatNetAmount) || 0);
                                }
                            }
                            if (req.Data.isSupplementary) {
                                drugsplitDetail.push(DrugBillDetail);
                            } else {
                                if (req.Data.nonmedical) {
                                    drugsplitDetail.push(DrugBillDetail);
                                } else {
                                    if (!DrugBillDetail.IsSupplementary) {
                                        drugsplitDetail.push(DrugBillDetail);
                                    }
                                }
                            }
                        }
                    }
                }
                let ServiceCategoryInfo = await servicecategoryBo.GetServiceCategoryById({ Id: bill.ServiceCategoryId });
                drugsummary.ServiceName = ServiceCategoryInfo.ServiceCategoryName;
                let orderbydatespldet = _.orderBy(drugsplitDetail, ['ServiceId', 'BillDateTime']);
                let nondrugdetails: any = [];
                let facilityprebo = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                let showDrugDetails = 0;
                let printPreferencesData = await facilityprebo.GetPrintPreferences('billing', null, this.Session.FacilityId);
                if (printPreferencesData && printPreferencesData.showdrugdetails) {
                    try {
                        showDrugDetails = parseInt(printPreferencesData.showdrugdetails);
                    } catch (ex) { showDrugDetails = 0; }
                }
                for (let idx in orderbydatespldet) {
                    let drugitems = orderbydatespldet[idx];
                    if (drugitems.ServiceItem) {
                        console.log(drugitems.ServiceItem.IsDoctorDisplay);
                        drugitems.IsDoctorDisplay = (drugitems.ServiceItem.IsDoctorDisplay) ? 1 : 0;
                        console.log(drugitems.IsDoctorDisplay);
                    } else {
                        drugitems.IsDoctorDisplay = 0;
                    }
                    if (showDrugDetails === 1) {
                        if (req.Data.IsSupplementary) {
                            drugitems.PharmacyNetVal = drugitems.PatNetAmount;
                        } else if (drugitems.InsNetAmount > 0) {
                            drugitems.PharmacyNetVal = drugitems.InsNetAmount;
                        } else {
                            drugitems.PharmacyNetVal = drugitems.NetAmount;
                        }
                        nondrugdetails.push(drugitems);
                    } else if (showDrugDetails === 0 || !showDrugDetails) {
                        if (!drugitems.IsPharmacySale && !drugitems.IsPharmacyReturn) {
                            if (req.Data.isSupplementary) {
                                drugitems.PharmacyNetVal = drugitems.PatNetAmount;
                            } else if (drugitems.InsNetAmount > 0) {
                                drugitems.PharmacyNetVal = drugitems.InsNetAmount;
                            } else {
                                drugitems.PharmacyNetVal = drugitems.NetAmount;
                            }
                            nondrugdetails.push(drugitems);
                        }
                    }
                }
                let Date_sort = function (a: any, b: any) {
                    if (a.BillDateTime < b.BillDateTime) {
                        return -1;
                    }
                    if (a.BillDateTime > b.BillDateTime) {
                        return 1;
                    }
                    return 0;
                };
                nondrugdetails.sort(Date_sort);

                drugsummary['SplitedValue'] = nondrugdetails;
                drugsummary['Amount'] = drugsummaryTotal;
                drugsummary['WoNonMedicalAmount'] = drugsummarywoMedicalTotal;
                if (drugsplitDetail.length > 0)
                    DrugBillDetail.push(drugsummary);


                let custom_sort = function (a: any, b: any) {
                    return parseInt(a.ServiceCategory.DisplayOrder) - parseInt(b.ServiceCategory.DisplayOrder);
                };
                DrugBillDetail.sort(custom_sort);
            })(splitItem);
        }));
        let DueCollect: number = 0;
        let ActualPaidAmt: number = 0;
        let PaidAmount: number = 0;
        let Advance: number = 0;
        let Adjustment: number = 0;
        let DrugAdvAmount: number = 0;
        let ReceiptDetails = [];
        let RefundAmount: number = 0;
        let PartialRefundAmount: number = 0;
        let PatientRefundData: any;
        let PatientRefundInfo: any;

        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.EncounterId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.EncounterTypeId, Value: 2 },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: false },
            { Key: PatientPaymentDetailsFilters.IsConsolidatePay, Value: false },
            { Key: PatientPaymentDetailsFilters.StatusOfReceipts, Value: [1, 4] }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        for (let idx in PatientPaymentDetailsData.Data) {
            let ReceiptItem = PatientPaymentDetailsData.Data[idx];
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1) {
                ActualPaidAmt += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 6) {
                Adjustment += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 1) {
                Advance += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 7) {
                DrugAdvAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 3)
                DueCollect += PatientPaymentDetailsData.Data[idx].AmountPaid;
            ReceiptDetails.push(ReceiptItem);
        }
        PaidAmount = Math.round(ActualPaidAmt) - Math.round(Adjustment);
        console.log(PaidAmount, 'PaidAmount');
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.EncounterId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 },
            { Key: PatientRefundFilters.EncounterTypeId, Value: 2 },
            { Key: PatientRefundFilters.IsCashToCredit, Value: false }]
        };

        let RefundDetails = [];
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        PatientRefundInfo = PatientRefundData.Data;

        for (var idx1 in PatientRefundData.Data) {
            var RefundItem = PatientRefundData.Data[idx1];
            if (PatientRefundData.Data[idx1].RefundStatusId === 1) {
                RefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            }
            if (PatientRefundData.Data[idx1].RefundTypeId === 1 || PatientRefundData.Data[idx1].RefundTypeId === 4)
                PartialRefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            RefundDetails.push(RefundItem);
        }

        let UserReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: UserFilters.Id, Value: req.Data.PrintUser }]
        };
        let UsersBo = BoFactory.GetBo(userbo.UserBo, this.Request);
        let PrintUser = await UsersBo.GetUsers(UserReq);
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        // let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        // let printStoreData =
        //     await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let GuarantorId = Encounter.GuarantorId;
        let GuarantorTypeId = Encounter.GuarantorTypeId;
        let PatientGuarantorReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientGuarantorFilters.Id, Value: GuarantorId }]
        };
        let PatientGuarantorBo = BoFactory.GetBo(regbo.PatientGuarantorBo, this.Request);
        let PatientGuarantorData = await PatientGuarantorBo.GetPatientGuarantors(PatientGuarantorReq);
        let PatientGuarantor = PatientGuarantorData.Data[0];
        if (PatientBills.RoundOffValue) {
            RoundOffValue = PatientBills.RoundOffValue;
        } else {
            RoundOffValue = 0;
        }
        let BalanceAmount: number = 0;
        if (!isFinalized) {
            TotalNetAmount = Math.round(TotalNetAmount) + Math.round(TotalGSTAmount);
            if (GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
            }
        }
        let QrInfo: any;
        let InsNetAmount: number = 0;
        if (isFinalized) {
            TotalNetAmount = Math.round(TotalGrossAmount - PatientBills.BillDiscount);

            if (PatientBills.GuarantorTypeId === 1) {
                BalanceAmount = ((TotalNetAmount) - PaidAmount) + PartialRefundAmount;
            } else {
                BalanceAmount = ((TotalNetAmount - TotalInsuranceAmount) - PaidAmount) + PartialRefundAmount;
                InsNetAmount = Math.round(TotalNetAmount - PatientBills.CreditApproved || 0);
            }


            let PatName: '';
            let FacInfo: '';

            if (patientData.Title) {
                PatName = patientData.Title.Description;
            }
            if (patientData.FirstName) {
                PatName += ' ' + patientData.FirstName;
            }
            if (patientData.LastName) {
                PatName += ' ' + patientData.LastName;
            }
            if (PatientBills.Facility) {
                FacInfo = PatientBills.Facility.FacilityName;
            }
            if (PatientBills.Facility.AddressLine1) {
                FacInfo += ', ' + PatientBills.Facility.AddressLine1;
            }
            if (PatientBills.Facility.Mobile) {
                FacInfo += ',Phone: ' + PatientBills.Facility.Mobile;
            }
            if (PatientBills.Facility.Email) {
                FacInfo += ',Email: ' + PatientBills.Facility.Email;
            }
            if (PatientBills.Facility.GstNumber) {
                FacInfo += ',GSTIN No: ' + PatientBills.Facility.GstNumber;
            }
            QrInfo = PatientBills.BillNumber + ' , ' + PatientBills.BillAmount + ' , ' +
                PatientBills.BillDateTime + ' , ' + PatName + ' , ' + FacInfo;
        }
        // console.log('********11*******');
        let info = {
            PatientBills: PatientBills,
            Encounter: Encounter,
            Patient: patientData,
            BillDetails: BillDetails,
            DrugBillDetail: DrugBillDetail,
            Age: Age,
            PatientPaymentDetails: ReceiptDetails,
            TotalGrossAmount: Math.round(TotalGrossAmount),
            TotalGSTAmount: Math.round(TotalGSTAmount),
            TotalNetAmount: Math.round(TotalNetAmount),
            TotalDiscount: Math.round(TotalDiscount),
            TotSupplmDiscDrug: TotSupplmDiscDrug,
            TotalInsuranceAmount: Math.round(TotalInsuranceAmount),
            TotalPatientAmount: Math.round(TotalPatientAmount),
            DueCollect: Math.round(DueCollect),
            PaidAmount: Math.round(PaidAmount),
            Advance: Math.round(Advance),
            DrugAdvAmount: Math.round(DrugAdvAmount),
            NetAmount: (PatientBills.BillAmount + RoundOffValue) - PatientBills.BillDiscount,
            BalanceAmount: Math.round(BalanceAmount),
            currentdate: new Date(),
            PrintUser: PrintUser.Data[0],
            PatientRefund: PatientRefundInfo,
            RefundAmount: Math.round(RefundAmount),
            PartialRefundAmount: Math.round(PartialRefundAmount),
            InsNetAmount: Math.round(InsNetAmount),
            Preferences: printPreferencesData,
            PatientGuarantor: PatientGuarantor,
            Referrals: ReferralsData.Data[0],
            QrInfo: QrInfo,
            GstAmount: GstAmount,
            TotMouDisc: TotMouDisc,
            Flags: flags,
            billtype: req.Data.ids,
            CoPayAmount: Math.round(req.Data.CoPayAmount),
            NonMedicalAmount: Math.round(req.Data.NonMedicalAmount),
            isFinalized: isFinalized,
            TotalPharmacyGrossAmount: Math.round(TotalPharmacyGrossAmount),
            TotalPharmacyInsuranceAmount: Math.round(TotalPharmacyInsuranceAmount),
            TotalPharmacyPatientAmount: Math.round(TotalPharmacyPatientAmount),
            TotalPharmacyGSTAmount: Math.round(TotalPharmacyGSTAmount),
            TotalPharmacyNetAmount: Math.round(TotalPharmacyNetAmount),
            TotalPharmacyDiscount: Math.round(TotalPharmacyDiscount),
            TotalMOUDisc: TotalMOUDisc

        };
        let key = 'InpatientBillDetail(i)';
        let pdfOption: any = null;
        {
            if (isFinalized) {
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientBillDetail';
                }
            }
            if (req.Data.isGuarantor) {
                key = 'InpatientbillDetailsGuarantor(i)';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientbillDetailsGuarantor';
                }
            }
            if (req.Data.isSupplementary) {
                key = 'Supplementry';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2
                    && info.BillDetails.IsSupplementary === true) {
                    key = 'Supplementry';
                }
            }
            if (info.Flags.drugBill) {
                key = 'InpatientBillDetailph';
            }
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'portrait',
                    border: '0',
                    header: {
                        height: '1in',
                        contents: '',
                    },
                    footer: {
                        height: '1in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
                console.log(pdfOption);
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async getInitCapString(String: string) {
        String = String.toLowerCase();
        var SplitString = String.split(' ');

        for (var i = 0; i < SplitString.length; i++) {
            SplitString[i] = SplitString[i].charAt(0).toUpperCase() + SplitString[i].slice(1);
        }

        var InitString = SplitString.join(' ');

        return await InitString;
    }

    public async PrintDailyInpatientBills(req: BaseRequest): Promise<FileInfo> {
        let isFinalized: boolean = req.Data.isFinalized;
        let PatientBills: any = {};
        if (isFinalized) {
            let apiReq = {
                Id: 0,
                PageContext: { PageSize: -1, PageNumber: 1 },
                Params: [{ Key: PatientBillsFilters.EncounterId, Value: req.Id },
                { Key: PatientBillsFilters.BillType, Value: 2 }, { Key: PatientBillsFilters.PatientBillStatus, Value: 3 }]
            };
            let data = await this.GetPatientBills(apiReq);
            PatientBills = data.Data[0];
        }
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];
        let patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let patientData = await patientBo.GetPatientById({ Id: Encounter.PatientId });
        let TotalGrossAmount: number = 0;
        let TotalNetAmount: number = 0;
        let TotalDiscount: number = 0;
        let BillDetails: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillSummaryFilters.EncounterId, Value: req.Id }]
        };
        let servicecategoryBo = BoFactory.GetBo(clinicalmasterBO.ServiceCategoryBo, this.Request);
        let billSummaryBo = BoFactory.GetBo(billingBO.PatientBillSummaryBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientBillSummary = await billSummaryBo.GetPatientBillSummarys(apiReq);
        await Promise.all(PatientBillSummary.Data.map((splitItem): Promise<void> => {
            return (async (bill): Promise<void> => {
                let summary: any = bill;
                let summaryTotal: number = 0;
                let splitDetails: any = [];
                // let PatientBillSplitDetailsBo = BoFactory.GetBo(billingBO.PatientBillSplitDetailsBo, this.Request);
                // let splitReq = {
                //     Id: 0,
                //     PageContext: { PageSize: -1, PageNumber: 1 },
                //     Params: [{ Key: PatientBillSplitDetailsFilters.PatientBillSummaryId, Value: bill.Id }]
                // };
                // if (req.Data.isSupplementary)
                //     splitReq.Params.push({ Key: PatientBillSplitDetailsFilters.IsSupplementary, Value: req.Data.isSupplementary });
                // let BillSplitDetailsData = await PatientBillSplitDetailsBo.GetPatientBillSplitDetails(splitReq);
                // await Promise.all(BillSplitDetailsData.Data.map((billItem): Promise<void> => {
                //     return (async (split): Promise<void> => {
                let billDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.ServiceCategoryId, Value: bill.ServiceCategoryId },
                    { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 },
                    { Key: PatientBillDetailsFilters.FromDate, Value: req.Data.FromDate },
                    { Key: PatientBillDetailsFilters.ToDate, Value: req.Data.ToDate }]
                };
                let DetailData = await PatientBillDetailsBo.GetPatientBillDetails(billDetailReq);
                if (DetailData.Data.length > 0) {
                    let BillDetail: any = DetailData.Data[0];
                    TotalGrossAmount += BillDetail.GrossAmount;
                    TotalDiscount += BillDetail.DiscountAmount;
                    TotalNetAmount += BillDetail.GrossAmount - BillDetail.DiscountAmount;
                    summaryTotal += BillDetail.GrossAmount - BillDetail.DiscountAmount;
                    splitDetails.push(BillDetail);
                }
                //     })(billItem);
                // }));
                let ServiceCategoryInfo = await servicecategoryBo.GetServiceCategoryById({ Id: bill.ServiceCategoryId });
                summary.ServiceName = ServiceCategoryInfo.ServiceCategoryName;

                //let withoutpharmacysales: any = [];
                let withpharmacysales: any = [];

                let orderbydatespldet = _.orderBy(splitDetails, ['ServiceId', 'BillDateTime']);
                let roomGroups: any = {};
                let serviceitemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
                await Promise.all(orderbydatespldet.map((orderbydatespldetItem: any): Promise<void> => {
                    return (async (detail): Promise<void> => {
                        //console.log(detail);
                        if (detail.ServiceId > 0) {
                            let bIsRoomCharge: boolean = false;
                            let servReq = {
                                Id: 0,
                                PageContext: { PageSize: -1, PageNumber: 1 },
                                Params: [{ Key: 0, Value: detail.ServiceId }]
                            };
                            let Servicedata = await serviceitemBo.GetServiceItems(servReq);
                            if (Servicedata.Data.length > 0) {
                                let ServiceInfo: any = Servicedata.Data[0];
                                detail.IsDoctorDisplay = ServiceInfo.IsDoctorDisplay;
                                if (ServiceInfo) {
                                    if (ServiceInfo.IsBedCharge) {
                                        bIsRoomCharge = true;
                                        let dateformat = 'DD/MM/YYYY';
                                        if (roomGroups[detail.ServiceId]) {
                                            let billdt = moment(detail.BillDateTime).format(dateformat);
                                            roomGroups[detail.ServiceId].BTDt = billdt;
                                            roomGroups[detail.ServiceId].Quantity += detail.Quantity;
                                            roomGroups[detail.ServiceId].Amount += detail.GrossAmount;
                                            roomGroups[detail.ServiceId].DiscountAmount += detail.DiscountAmount;
                                            roomGroups[detail.ServiceId].NetAmount += (detail.GrossAmount - detail.DiscountAmount);
                                            detail.Amount = detail.GrossAmount;
                                            detail.NetAmount = detail.GrossAmount - (detail.DiscountAmount || 0);
                                        } else {
                                            roomGroups[detail.ServiceId] = roomGroups[detail.ServiceId] || [];
                                            let billdt1 = moment(detail.BillDateTime).format(dateformat);
                                            roomGroups[detail.ServiceId].ServiceName = detail.ServiceName;
                                            roomGroups[detail.ServiceId].BFDt = billdt1;
                                            roomGroups[detail.ServiceId].BTDt = billdt1;
                                            roomGroups[detail.ServiceId].User = detail.User;
                                            roomGroups[detail.ServiceId].BillDateTime = detail.BillDateTime;
                                            roomGroups[detail.ServiceId].Quantity = detail.Quantity;
                                            roomGroups[detail.ServiceId].Rate = detail.Rate;
                                            roomGroups[detail.ServiceId].Amount = detail.GrossAmount;
                                            roomGroups[detail.ServiceId].DiscountAmount = detail.DiscountAmount;
                                            roomGroups[detail.ServiceId].NetAmount = (detail.GrossAmount - detail.DiscountAmount);
                                            roomGroups[detail.ServiceId].Id = detail.Id;
                                            detail.Amount = detail.GrossAmount;
                                            detail.NetAmount = detail.GrossAmount - (detail.DiscountAmount || 0);
                                            //withoutpharmacysales.push(detail);
                                            withpharmacysales.push(detail);
                                        }
                                    }
                                }
                            }
                            //if (!detail.IsDoctorDisplay && !bIsRoomCharge && !detail.IsPharmacySale && !detail.IsPharmacyReturn) {
                            if (!detail.IsDoctorDisplay && !bIsRoomCharge) {
                                //console.log('normal');
                                // detail.User = null;
                                detail.Amount = detail.GrossAmount;
                                detail.NetAmount = detail.GrossAmount - (detail.DiscountAmount || 0);
                                //withoutpharmacysales.push(detail);
                                withpharmacysales.push(detail);
                            } else if (detail.IsDoctorDisplay && !bIsRoomCharge) {
                                //else if (detail.IsDoctorDisplay && !bIsRoomCharge && !detail.IsPharmacySale && !detail.IsPharmacyReturn) {
                                //console.log('only dr');
                                //withoutpharmacysales.push(detail);
                                detail.Amount = detail.GrossAmount;
                                detail.NetAmount = detail.GrossAmount - (detail.DiscountAmount || 0);
                                withpharmacysales.push(detail);
                                //console.log(withoutpharmacysales);
                            } else { // Room Charges
                                //console.log('only Room');
                                /*
                                for (let indx in withoutpharmacysales) {
                                    if (withoutpharmacysales[indx].Id === roomGroups[detail.ServiceId].Id) {
                                        withoutpharmacysales[indx].ServiceName = roomGroups[detail.ServiceId].ServiceName + ' (' +
                                            roomGroups[detail.ServiceId].BFDt + ' - ' + roomGroups[detail.ServiceId].BTDt + ' )';
                                        withoutpharmacysales[indx].Quantity = roomGroups[detail.ServiceId].Quantity;
                                        withoutpharmacysales[indx].Rate = roomGroups[detail.ServiceId].Rate;
                                        withoutpharmacysales[indx].Amount = roomGroups[detail.ServiceId].Amount;
                                    }
                                }
                                */
                                for (let indx in withpharmacysales) {
                                    if (withpharmacysales[indx].Id === roomGroups[detail.ServiceId].Id) {
                                        withpharmacysales[indx].ServiceName = roomGroups[detail.ServiceId].ServiceName;
                                        withpharmacysales[indx].Quantity = roomGroups[detail.ServiceId].Quantity;
                                        withpharmacysales[indx].Rate = roomGroups[detail.ServiceId].Rate;
                                        withpharmacysales[indx].Amount = roomGroups[detail.ServiceId].Amount;
                                        withpharmacysales[indx].Amount = roomGroups[detail.ServiceId].Amount;
                                        withpharmacysales[indx].DiscountAmount = roomGroups[detail.ServiceId].DiscountAmount;
                                        withpharmacysales[indx].NetAmount =
                                            roomGroups[detail.ServiceId].NetAmount;
                                    }
                                }
                            }
                        }
                    })(orderbydatespldetItem);
                }));
                let nondrugdetails: any = [];
                for (let idx in withpharmacysales) {
                    let drugitems = withpharmacysales[idx];
                    if (!drugitems.IsPharmacySale && !drugitems.IsPharmacyReturn) {
                        if (req.Data.IsSupplementary) {
                            drugitems.NetValue = drugitems.PatNetAmount;
                        } else if (drugitems.InsNetAmount > 0) {
                            drugitems.NetValue = drugitems.InsNetAmount;
                        } else {
                            drugitems.NetValue = drugitems.NetAmount;
                        }
                        nondrugdetails.push(drugitems);
                    }
                }
                //summary['SplitedValue'] = splitDetails;
                //summary['SplitedValue'] = withoutpharmacysales;
                summary['SplitedValue'] = withpharmacysales;
                summary['Amount'] = summaryTotal;
                if (splitDetails.length > 0)
                    BillDetails.push(summary);

                let custom_sort = function (a: any, b: any) {
                    return parseInt(a.ServiceCategory.DisplayOrder) - parseInt(b.ServiceCategory.DisplayOrder);
                };
                BillDetails.sort(custom_sort);
            })(splitItem);
        }));

        // let billdetailReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: PatientBillDetailsFilters.EncounterId, Value: req.Id },
        //     { Key: PatientBillDetailsFilters.PatientBillStatus, Value: 3 }]
        // };
        //
        // let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        // for (let idx in BillDetailsData.Data) {
        //     let BillItem = BillDetailsData.Data[idx];
        //     TotalGrossAmount += BillDetailsData.Data[idx].GrossAmount;
        //     TotalNetAmount += BillDetailsData.Data[idx].GrossAmount - BillDetailsData.Data[idx].DiscountAmount;
        //     TotalDiscount += BillDetailsData.Data[idx].DiscountAmount;
        //     BillDetails.push(BillItem);
        // }
        let DueCollect: number = 0;
        let PaidAmount: number = 0;
        let ReceiptDetails = [];
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.EncounterId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.EncounterTypeId, Value: 2 },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: false },
            { Key: PatientPaymentDetailsFilters.IsConsolidatePay, Value: false },
            { Key: PatientPaymentDetailsFilters.StatusOfReceipts, Value: [1, 4] }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        for (let idx in PatientPaymentDetailsData.Data) {
            let ReceiptItem = PatientPaymentDetailsData.Data[idx];
            if (PatientPaymentDetailsData.Data[idx].ReceiptStatusId === 1) {
                PaidAmount += PatientPaymentDetailsData.Data[idx].AmountPaid;
            }
            if (PatientPaymentDetailsData.Data[idx].ReceiptTypeId === 3)
                DueCollect += PatientPaymentDetailsData.Data[idx].AmountPaid;
            ReceiptDetails.push(ReceiptItem);
        }
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.EncounterId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 },
            { Key: PatientRefundFilters.EncounterTypeId, Value: 2 }]
        };
        let RefundAmount: number = 0;
        let PartialRefundAmount: number = 0;
        let RefundDetails = [];
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        for (var idx1 in PatientRefundData.Data) {
            var RefundItem = PatientRefundData.Data[idx1];
            if (PatientRefundData.Data[idx1].RefundStatusId === 1) {
                RefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            }
            if (PatientRefundData.Data[idx1].RefundTypeId === 1)
                PartialRefundAmount += PatientRefundData.Data[idx1].RefundAmount;
            RefundDetails.push(RefundItem);
        }
        let UserReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: UserFilters.Id, Value: req.Data.PrintUser }]
        };
        let UsersBo = BoFactory.GetBo(userbo.UserBo, this.Request);
        let PrintUser = await UsersBo.GetUsers(UserReq);
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(Encounter.FacilityId);
        if (PatientBills.StoreMasterId) {
            let printStoreData =
                await storePreferenceBO.GetStorePreferenceWithLogo(Encounter.FacilityId, PatientBills.StoreMasterId);
            if (printStoreData && printStoreData.pharmacyprintheader)
                printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
            if (printStoreData && printStoreData.StoreLogo)
                printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        }
        let info = {
            PatientBills: PatientBills,
            Encounter: Encounter,
            Patient: patientData,
            BillDetails: BillDetails,
            PatientPaymentDetails: ReceiptDetails,
            TotalGrossAmount: TotalGrossAmount,
            TotalNetAmount: TotalNetAmount,
            TotalDiscount: TotalDiscount,
            DueCollect: DueCollect,
            PaidAmount: PaidAmount,
            NetAmount: (PatientBills.BillAmount + PatientBills.RoundOffValue) - PatientBills.BillDiscount,
            BalanceAmount: (TotalNetAmount - PaidAmount) + PartialRefundAmount,
            currentdate: new Date(),
            PrintUser: PrintUser.Data[0],
            PatientRefund: PatientRefundData.Data,
            RefundAmount: RefundAmount,
            PartialRefundAmount: PartialRefundAmount,
            Preferences: printPreferencesData
        };
        let key = 'dailywisebreakup';
        let pdfOption: any = null;
        {
            if (isFinalized) {
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientBillDetail';
                }
            }
            if (req.Data.isGuarantor) {
                key = 'InpatientbillDetailsGuarantor(i)';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2) {
                    key = 'InpatientbillDetailsGuarantor';
                }
            }
            if (req.Data.isSupplementary) {
                key = 'Supplementry';
                if (info.PatientBills.BillTypeId && info.PatientBills.BillTypeId === 2
                    && info.BillDetails.IsSupplementary === true) {
                    key = 'Supplementry';
                }
            }
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'portrait',
                    border: '0',
                    header: {
                        height: '1in',
                        contents: '',
                    },
                    footer: {
                        height: '1in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
                //console.log(pdfOption);
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async isRoomCharges(x: any): Promise<boolean> {
        let res = false;
        let serviceitemBo = BoFactory.GetBo(clinicalmasterBO.ServiceItemBo, this.Request);
        let ServiceInfo = await serviceitemBo.GetServiceItemById({ Id: x.ServiceId });
        if (ServiceInfo && ServiceInfo.IsBedCharge) {
            console.log(x.ServiceId + ' ' + x.ServiceCode + ' ' + x.ServiceName + ' ' + ServiceInfo.IsBedCharge);
            res = true;
        }

        return res;
    }

    public async DMPrintPatientBills(req: BaseRequest): Promise<any> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data[0];
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: PatientBills.PatientId }]
        };
        let encounterData = await encounterBo.GetEncounters(encReq);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            BillDetails.push(PatientBillDetail);
        });
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.PatientBillId, Value: req.Id },
            { Key: PatientRefundFilters.RefundStatus, Value: 1 }]
        };
        let PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);

        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogoForDM(PatientBills.FacilityId, PatientBills.StoreMasterId);
        //Preferences: printPreferencesData
        let info = {
            PatientBills: PatientBills,
            BillDetails: BillDetails,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            PatientRefund: PatientRefundData.Data[0],
            Encounter: encounterData.Data[0],
            NetAmount: PatientBills.BillAmount - PatientBills.BillDiscount,
            NetAmountInWords: await this.AmountInWord(PatientBills.BillAmount - PatientBills.BillDiscount),
            Preferences: printPreferencesData,
            PrintData: printStoreData
        };
        return info;
    }

    public async DMPrintPharmacyBillsWithReturn(req: BaseRequest): Promise<any> {

        let PatientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let PatientData = await PatientBo.GetPatientById({ Id: req.Data.PatientId });

        let EncounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.PatientId, Value: req.Data.PatientId }]
        };
        let EncounterData = await EncounterBo.GetEncounters(encReq);
        let Encounter = EncounterData.Data[0];

        let PharmacyBillsDetails: any = [];
        let PharmacyReturnDetails: any = [];
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: 1000, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Data.PatientBillIds }]
        };
        let PatientBillsData = await this.GetPatientBills(apiReq);
        if (PatientBillsData) {
            for (var i = 0; i < PatientBillsData.Data.length; i++) {
                let PatientBills = PatientBillsData.Data[i];
                PharmacyBillsDetails.push(PatientBills);
            }
        }

        let PatientReturnReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientReturnsFilters.Id, Value: req.Data.PatientReturnIds }
            ]
        };
        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);
        if (PatientReturnsData) {
            for (var j = 0; j < PatientReturnsData.Data.length; j++) {
                let PatientReturns = PatientReturnsData.Data[j];
                PharmacyReturnDetails.push(PatientReturns);
            }
        }

        let info = {
            PatientData: PatientData,
            Encounter: Encounter,
            PharmacyBillsDetails: PharmacyBillsDetails,
            PharmacyReturnDetails: PharmacyReturnDetails
        };
        return info;
    }

    public async AmountInWord(amt: number): Promise<string> {
        let amount: number = amt;
        let lang = 'enIndian';
        let writtenNumber = require('written-number');
        let stramt = amount.toFixed(2);
        let ActualAmts = stramt.split('.');
        let writtenNumber1 = '';
        let writtenNumber2 = '';
        if (ActualAmts.length > 0) {
            let damt1: number = +ActualAmts[0];
            writtenNumber1 = writtenNumber(damt1, { lang: lang });
        }
        if (ActualAmts.length > 1) {
            let damt2 = +ActualAmts[1];
            writtenNumber2 = writtenNumber(damt2, { lang: lang });
        }
        return writtenNumber1 + ' and ' + writtenNumber2 + ' Paise';
    }

    public async GetLastBillInfo(req: BaseRequest): Promise<any> {
        let BillInfo = await this.Find({
            where: {
                BillTypeId: 4,
                BillDateTime: { '$between': [req.Data.FromDate, req.Data.ToDate] }
            },
            order: [['Id', 'DESC']],
        });
        return BillInfo;
    }

    public GetModel(): SStatic.Model<PatientBillsInstance, PatientBillsAttributes> {
        return this.Models.PatientBills;
    }

    public async GetPharmacyDetails(req: BaseRequest): Promise<any> {
        let result: any = [];
        let returnBO = BoFactory.GetBo(BillingBo.PatientReturnsBo, this.Request);
        result.push({ Key: 1, Value: await this.SaleDetails(req) });
        result.push({ Key: 2, Value: await returnBO.ReturnDetails(req) });
        return result;
    }
    public async SaleDetails(req: BaseRequest): Promise<any> {
        let SaleDetailsResult: any = [];
        SaleDetailsResult['DisplayOrder'] = 1;
        let EncJoin: any = {
            model: this.Models.Encounter,
            attributes: ['Id', 'EncounterTypeId'],
            required: true,
            where: {
                EncounterTypeId: 1,
            }
        };
        let IpEncJoin: any = {
            model: this.Models.Encounter,
            attributes: ['Id', 'EncounterTypeId'],
            required: true,
            where: {
                EncounterTypeId: 2,
            }
        };
        let storeId: any;
        if (req.Data.StoreMasterId > 0) {
            storeId = req.Data.StoreMasterId;
        } else {
            storeId = { '$gt': 0 };
        }
        let opbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'BillAmount'],
            ],
            where: {
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                BillTypeId: 4,
                IsPharmacyBill: 1,
                IsDirectDGBill: false,
                PatientBillStatusId: 3,
                PharmacySaleTypeId: { '$gt': 0 },
                FacilityId: req.Data.FacilityId,
                StoreMasterId: storeId,
            },
            include: [EncJoin]
        });

        if (opbillamountInstance) {
            let OPSale: number = 0;
            let Type: number = 0;
            let bill: any = this.GetAttribute(opbillamountInstance);
            OPSale = bill['BillAmount'];
            Type = 1;
            // SaleDetailsResult['OPSale'] = bill['BillAmount'];
            // SaleDetailsResult['Type'] = bill[1];
            let info = {
                'OPSale': OPSale,
                'Type': Type,
            };
            SaleDetailsResult.push(info);
        }
        let opbillamountIPInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'BillAmount'],
            ],
            where: {
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                BillTypeId: 4,
                IsPharmacyBill: 1,
                PatientBillStatusId: 3,
                PharmacySaleTypeId: { '$gt': 0 },
                FacilityId: req.Data.FacilityId,
                StoreMasterId: storeId,

            },
            include: [IpEncJoin]
        });

        if (opbillamountIPInstance) {
            let IPSale: number = 0;
            let Type: number = 0;
            let bill: any = this.GetAttribute(opbillamountIPInstance);
            IPSale = bill['BillAmount'];
            Type = 2;
            // SaleDetailsResult['IPSale'] = bill['BillAmount'];
            // SaleDetailsResult['Type'] = bill[2];
            let info = {
                'IPSale': IPSale,
                'Type': Type,
            };
            SaleDetailsResult.push(info);
        }

        let opbillamountstaffInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'BillAmount'],
            ],
            where: {
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                BillTypeId: 6,
                IsPharmacyBill: 1,
                PatientBillStatusId: 3,
                PharmacySaleTypeId: { '$gt': 0 },
                FacilityId: req.Data.FacilityId,
                StoreMasterId: storeId,

            },
        });
        if (opbillamountstaffInstance) {
            let StaffCreditSale: number = 0;
            let Type: number = 0;
            let bill: any = this.GetAttribute(opbillamountstaffInstance);
            // SaleDetailsResult['StaffCreditSale'] = bill['BillAmount'];
            // SaleDetailsResult['Type'] = bill[3];
            StaffCreditSale = bill['BillAmount'];
            Type = 3;
            let info = {
                'StaffCreditSale': StaffCreditSale,
                'Type': Type,
            };
            SaleDetailsResult.push(info);
        }
        let opdirectbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'BillAmount'],
            ],
            where: {
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                BillTypeId: 4,
                IsPharmacyBill: 1,
                IsDirectDGBill: true,
                PatientBillStatusId: 3,
                PharmacySaleTypeId: { '$gt': 0 },
                FacilityId: req.Data.FacilityId,
                StoreMasterId: storeId,

            },
        });

        if (opdirectbillamountInstance) {
            let DirectSale: number = 0;
            let Type: number = 0;
            let bill: any = this.GetAttribute(opdirectbillamountInstance);
            DirectSale = bill['BillAmount'];
            Type = 4;
            // SaleDetailsResult['DirectSale'] = bill['BillAmount'];
            // SaleDetailsResult['Type'] = bill[1];
            let info = {
                'DirectSale': DirectSale,
                'Type': Type,
            };
            SaleDetailsResult.push(info);
        }
        return SaleDetailsResult;
    }

    public async GetRevenueDetals(req: BaseRequest): Promise<any> {
        let result: any = [];
        result.push({ Key: 'OP', Value: await this.OPRevenueDetails(req) });
        result.push({ Key: 'IP', Value: await this.IPRevenueDetails(req) });
        return result;
    }


    public async OPRevenueDetails(req: BaseRequest): Promise<any> {
        let OPBillResult: any = {};
        let netamt = 0;
        OPBillResult['DisplayOrder'] = 1;
        let opbillamountInstance: any = await this.FindAll({
            attributes: ['BillAmount', 'BillDiscount', 'DepartmentId'],
            where: {
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                BillTypeId: { '$in': [1, 5] },
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountInstance) {
            for (let i = 0; i < opbillamountInstance.length; i++) {
                let bills: any = this.GetAttribute(opbillamountInstance[i]);
                // let DeptId = bills.DepartmentId;
                let billamt = bills.BillAmount;
                let discbill = bills.BillDiscount;
                let NetAmount = billamt - discbill;
                netamt += NetAmount;
            }
            // let bill: any = this.GetAttribute(opbillamountInstance);
            OPBillResult['BillAmt'] = netamt;
        }
        return OPBillResult;
    }
    public async IPRevenueDetails(req: BaseRequest): Promise<any> {
        let OPBillResult: any = {};
        let netamt = 0;
        OPBillResult['DisplayOrder'] = 1;
        let opbillamountInstance: any = await this.FindAll({
            attributes: ['BillAmount', 'BillDiscount', 'DepartmentId'],
            where: {
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                BillTypeId: 2,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountInstance) {
            for (let i = 0; i < opbillamountInstance.length; i++) {
                let bills: any = this.GetAttribute(opbillamountInstance[i]);
                // let DeptId = bills.DepartmentId;
                let billamt = bills.BillAmount;
                let discbill = bills.BillDiscount;
                let NetAmount = billamt - discbill;
                netamt += NetAmount;
            }
            // let bill: any = this.GetAttribute(opbillamountInstance);
            OPBillResult['BillAmt'] = netamt;
        }
        return OPBillResult;
    }


    public async GetFacilityInfoDashBoard(req: BaseRequest): Promise<any> {
        let result: any = [];
        result.push({ Key: 'OP-Private', Value: await this.OPPrivateDueDetails(req) });
        result.push({ Key: 'OP-Insurance', Value: await this.OPInsuranceDueDetails(req) });
        result.push({ Key: 'IP-Cash', Value: await this.IPPharmacyDueDetails(req) });
        result.push({ Key: 'IP-Credit', Value: await this.IPCreditPharmacyDueDetails(req) });
        return result;
    }

    public async OPPrivateDueDetails(req: BaseRequest): Promise<any> {
        let OPPrivateDue: any = {};
        OPPrivateDue['DisplayOrder'] = 1;
        let EncJoin: any = {
            model: this.Models.Encounter,
            attributes: ['Id', 'EncounterTypeId'],
            required: true,
            where: {
                EncounterTypeId: 1,
            }
        };
        let storeId: any;
        if (req.Data.StoreMasterId > 0) {
            storeId = req.Data.StoreMasterId;
        } else {
            storeId = { '$gt': 0 };
        }
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('OutStandingAmount')), 'OPPrivateDue'],
            ],
            where: {
                BillTypeId: 4,
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                GuarantorTypeId: 1,
                PharmacySaleTypeId: { '$gt': 0 },
                OutStandingAmount: { '$gt': 0 },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                FacilityId: req.Data.FacilityId,
                StoreMasterId: storeId,
            },
            include: [EncJoin]
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            OPPrivateDue['OutStandingAmount'] = bill['OPPrivateDue'];
        }

        return OPPrivateDue;
    }

    public async OPInsuranceDueDetails(req: BaseRequest): Promise<any> {
        let OPInsuranceDue: any = {};
        OPInsuranceDue['DisplayOrder'] = 2;
        let EncJoin: any = {
            model: this.Models.Encounter,
            attributes: ['Id', 'EncounterTypeId'],
            required: true,
            where: {
                EncounterTypeId: 1,
            }
        };
        let storeId: any;
        if (req.Data.StoreMasterId > 0) {
            storeId = req.Data.StoreMasterId;
        } else {
            storeId = { '$gt': 0 };
        }
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('OutStandingAmount')), 'OPInsuranceDue'],
            ],
            where: {
                BillTypeId: 4,
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                GuarantorTypeId: { '$gt': 1 },
                PharmacySaleTypeId: { '$gt': 0 },
                OutStandingAmount: { '$gt': 0 },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                FacilityId: req.Data.FacilityId,
                StoreMasterId: storeId,
            },
            include: [EncJoin]
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            OPInsuranceDue['OutStandingAmount'] = bill['OPInsuranceDue'];
        }

        return OPInsuranceDue;
    }

    public async IPPharmacyDueDetails(req: BaseRequest): Promise<any> {
        let IPCashDue: any = {};
        IPCashDue['DisplayOrder'] = 3;
        let EncJoin: any = {
            model: this.Models.Encounter,
            attributes: ['Id', 'EncounterTypeId'],
            required: true,
            where: {
                EncounterTypeId: 2,
            }
        };
        let storeId: any;
        if (req.Data.StoreMasterId > 0) {
            storeId = req.Data.StoreMasterId;
        } else {
            storeId = { '$gt': 0 };
        }
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('OutStandingAmount')), 'IPCashDue'],
            ],
            where: {
                BillTypeId: 4,
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                GuarantorTypeId: 1,
                PharmacySaleTypeId: { '$gt': 0 },
                OutStandingAmount: { '$gt': 0 },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                FacilityId: req.Data.FacilityId,
                StoreMasterId: storeId,
            },
            include: [EncJoin]
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            IPCashDue['OutStandingAmount'] = bill['IPCashDue'];
        }

        return IPCashDue;
    }

    public async IPCreditPharmacyDueDetails(req: BaseRequest): Promise<any> {
        let IPInsuranceDue: any = {};
        IPInsuranceDue['DisplayOrder'] = 4;
        let EncJoin: any = {
            model: this.Models.Encounter,
            attributes: ['Id', 'EncounterTypeId'],
            required: true,
            where: {
                EncounterTypeId: 2,
            }
        };
        let storeId: any;
        if (req.Data.StoreMasterId > 0) {
            storeId = req.Data.StoreMasterId;
        } else {
            storeId = { '$gt': 0 };
        }
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('OutStandingAmount')), 'IPInsuranceDue'],
            ],
            where: {
                BillTypeId: 4,
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                GuarantorTypeId: { '$gt': 1 },
                PharmacySaleTypeId: { '$gt': 0 },
                OutStandingAmount: { '$gt': 0 },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                FacilityId: req.Data.FacilityId,
                StoreMasterId: storeId,
            },
            include: [EncJoin]
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            IPInsuranceDue['OutStandingAmount'] = bill['IPInsuranceDue'];
        }

        return IPInsuranceDue;
    }

    public async GetFacilityCollectionDashBoard(req: BaseRequest): Promise<any> {
        let result: any = [];
        result.push({ Key: 'OP Discount', Value: await this.OPBillDiscount(req) });
        result.push({ Key: 'IP Discount', Value: await this.IPBillDiscount(req) });
        return result;
    }
    public async OPBillDiscount(req: BaseRequest): Promise<any> {
        let OPBillResult: any = {};
        OPBillResult['DisplayOrder'] = 12;
        let opbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'BillDiscount'],
            ],
            where: {
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                BillTypeId: { '$in': [1, 5] },
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountInstance) {
            let bill: any = this.GetAttribute(opbillamountInstance);
            OPBillResult['BillDiscount'] = bill['BillDiscount'];
        }
        return OPBillResult;
    }

    public async IPBillDiscount(req: BaseRequest): Promise<any> {
        let OPBillResult: any = {};
        OPBillResult['DisplayOrder'] = 13;
        let opbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'BillDiscount'],
            ],
            where: {
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                EncounterTypeId: 2,
                BillTypeId: 2,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountInstance) {
            let bill: any = this.GetAttribute(opbillamountInstance);
            OPBillResult['BillDiscount'] = bill['BillDiscount'];
        }
        return OPBillResult;
    }

    public async GetOPDDashBoardInfo(req: BaseRequest): Promise<any> {
        let OPBillCount = await this.Items.count({
            where: {
                'Status': 1,
                'BillTypeId': 1,
                'PatientBillStatusId': 3,
                'CreatedAt': { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                'FacilityId': req.Data.FacilityId,
            }
        });
        let DGBillCount = await this.Items.count({
            where: {
                'Status': 1,
                'BillTypeId': 5,
                'PatientBillStatusId': 3,
                'CreatedAt': { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                'FacilityId': req.Data.FacilityId,
            }
        });
        return {
            'OPBillCount': OPBillCount,
            'DGBillCount': DGBillCount
        };
    }

    public async CancelIPPatientBill(req: BaseRequest): Promise<boolean> {
        let patientbill = await this.GetPatientBillsById({ Id: req.Id });
        patientbill.PatientBillStatusId = 2;
        patientbill.Status = 2;
        let result = await this.Update(patientbill);
        if (result) {
            let encounterBO = BoFactory.GetBo(encbo.EncounterBo, this.Request);
            await encounterBO.ReOpenEncounterAdmission(req);

            let refundBO = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
            await refundBO.CancelIPBillRefunds(req);

            let paymentBO = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
            req.Data.PatientBill = patientbill;
            await paymentBO.CancelIPBillPayments(req);

            let bedOccupancyBO = BoFactory.GetBo(inpatientBO.BedOccupancyHistoryBo, this.Request);
            await bedOccupancyBO.ReOccupyBed(req);

            let wardroombedBO = BoFactory.GetBo(generalMasterBO.WardRoomBedMasterBo, this.Request);
            await wardroombedBO.ManageBedStatus(req);
        }

        return result;
    }

    public async PrintconsumerBills(req: BaseRequest): Promise<FileInfo> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.Id, Value: req.Id }]
        };
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data[0];
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let billdetailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: PatientBills.Id }]
        };
        let BillDetailsData = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);
        let BillDetails: any = [];
        let TotalCgst: number = 0;
        let TotalSgst: number = 0;
        BillDetailsData.Data.sort(function (a, b) {
            return a.Id - b.Id;
        });
        let BillDetailsAmt = 0;
        let RetAmt = 0;
        let RetCgst = 0;
        let RetSgst = 0;
        BillDetailsData.Data.forEach((Detail: any) => {
            let PatientBillDetail = Detail;
            if (PatientBillDetail.ReturnedQuantity > 0) {
                PatientBillDetail.Quantity -= PatientBillDetail.ReturnedQuantity;
                RetAmt += PatientBillDetail.Rate * PatientBillDetail.ReturnedQuantity;
                RetCgst += PatientBillDetail.UnitCGstAmount * PatientBillDetail.ReturnedQuantity;
                RetSgst += PatientBillDetail.UnitSGstAmount * PatientBillDetail.ReturnedQuantity;
                PatientBillDetail.GSTAmount = PatientBillDetail.UnitGSTAmount * PatientBillDetail.Quantity;
                PatientBillDetail.Amount = PatientBillDetail.Rate * PatientBillDetail.Quantity;
                PatientBillDetail.NetAmountBeforeGST = PatientBillDetail.Amount - PatientBillDetail.GSTAmount;
            }
            BillDetailsAmt += PatientBillDetail.Amount;
            PatientBillDetail.Amt = Detail.InGstAmount - (Detail.CGstAmount + Detail.SGstAmount);
            TotalCgst += Detail.CGstPercentage;
            TotalSgst += Detail.SGstPercentage;
            BillDetails.push(PatientBillDetail);
        });
        if (BillDetailsAmt === 0) {
            PatientBills.BillAmount = 0;
            PatientBills.RoundOffValue = 0;
            PatientBills.BillDiscount = 0;
            PatientBills.GSTAmount = 0;
            PatientBills.PaidAmount = 0;
        } else {
            PatientBills.BillAmount -= RetAmt;
            //PatientBills.BillDiscount = 0;
            PatientBills.PaidAmount -= RetAmt;
            PatientBills.CGstAmount -= RetCgst;
            PatientBills.SGstAmount -= RetSgst;
        }
        let detailReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.PatientBillId, Value: req.Id },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 }]
        };
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(detailReq);
        let encReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: EncounterFilters.Id, Value: req.Id }]
        };
        let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let encounterData = await encounterBo.GetEncounters(encReq);
        let Encounter = encounterData.Data[0];
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBills.FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(PatientBills.FacilityId, PatientBills.StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        let info = {
            Encounter: Encounter,
            PatientBills: PatientBills,
            PatientPaymentDetails: PatientPaymentDetailsData.Data,
            BillDetails: BillDetails,
            NetAmount: (PatientBills.BillAmount - PatientBills.BillDiscount) + PatientBills.RoundOffValue,
            TotalCgst: TotalCgst,
            TotalSgst: TotalSgst,
            WithoutTax: PatientBills.BillAmount - PatientBills.GSTAmount,
            Preferences: printPreferencesData
        };

        let key = 'consumerbill';
        let Watermark = '';
        let PrintTypeId: number;
        if (info.PatientBills.PatientBillStatusId === 2) {
            Watermark = 'CANCELLED';
            PrintTypeId = 2;
        }
        let pdfOption: any = null;
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                //format: 'A5',
                height: '5.8in',        // allowed units: mm, cm, in, px 5.8 x 8.3 in
                width: '8.3in',            // allowed units: mm, cm, in, px Width x Height (inch)
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
            let printInfo = {
                ObjectId: req.Id
                , ObjectTypeId: 4 /*Order*/
                , Reason: req.Data ? req.Data.Reason : null
                , Watermark: Watermark  /* Provide watermark, if needed other than DUPLICATE */
                , PrintTypeId: PrintTypeId /* Provide 2, if needed watermark all time i.e., CONFIDENTIAL */
            };
            let ephBO = BoFactory.GetBo(generalBO.EntityPrintHistoryBo, this.Request);
            let entityPrintHistoryData = await ephBO.ManagePrintHistory(printInfo);
            if (printInfo.PrintTypeId === 2 || entityPrintHistoryData.PrintTypeId === 2) {
                Watermark = Watermark || 'DUPLICATE';
            }
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            // console.log(pdfOption);
        }
        return await Report.Generate(key, { header: {}, body: info, watermark: Watermark },
            null, pdfOption);
    }
    public async PrintCancelReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let PatientBillsData = data.Data[0];
        let PatientDailyBills: any = [];
        let TotalBillAmount: number = 0;
        let TotalDisAmount: number = 0;
        let TotalPaidAmount: number = 0;
        PatientBills.forEach((Detail: any) => {
            let BillData = Detail;
            BillData.NetAmount = parseInt(Detail.BillAmount) - parseInt(Detail.BillDiscount);
            PatientDailyBills.push(BillData);
        });
        for (let idx in PatientDailyBills) {
            let item = PatientDailyBills[idx];
            TotalBillAmount += item.BillAmount;
            TotalDisAmount += item.BillDiscount;
            TotalPaidAmount += item.PaidAmount;
        }
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientDailyBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            TotalBillAmount: TotalBillAmount,
            TotalDisAmount: TotalDisAmount,
            TotalPaidAmount: TotalPaidAmount,
        };
        let pdfOption: any = null;
        let key = 'cancelreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintIPInsuranceReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBillswithoutdetails(apiReq);
        let Patientbills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let GuarantorName = apiReq.Data.GuarantorName;
        let DoctorName = apiReq.Data.DoctorName;
        let WardName = apiReq.Data.WardName;
        let PatientbillsData = data.Data[0];
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientbillsData.FacilityId);
        let info = {
            Patientbills: Patientbills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName,
            GuarantorName: GuarantorName,
            DoctorName: DoctorName,
            WardName: WardName
        };
        let pdfOption: any = null;
        let key = 'ipinsurancereport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintIPCancelReport(apiReq?: ApiRequest<PatientBillsFilters>): Promise<any> {
        let data = await this.GetPatientBills(apiReq);
        let PatientBills = data.Data;
        let FromDate = apiReq.Data.FromDate;
        let ToDate = apiReq.Data.ToDate;
        let FacilityName = apiReq.Data.FacilityName;
        let PatientBillsData = data.Data[0];
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(PatientBillsData.FacilityId);
        let info = {
            PatientBills: PatientBills,
            Preferences: printPreferencesData,
            FromDate: FromDate,
            ToDate: ToDate,
            FacilityName: FacilityName
        };
        let pdfOption: any = null;
        let key = 'ipcancelreport';
        pdfOption = {
            format: 'A4',
            orientation: 'landscape',
            border: '0',
            header: {
                height: '1.5in',
                contents: '',
            },
            footer: {
                height: '0.5in',
                contents: {
                    first: '',
                    default: '',
                    last: '',
                },
            },
            type: 'pdf',
            base: 'file://' + join(__dirname, '/../../Templates/')
        };
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintCollectionReportByCashier(req: BaseRequest): Promise<any> {
        // let data = await this.GetPatientBills(apiReq);
        // let PatientBills = data.Data;
        // let datetimeformat = 'DD/MM/YYYY HH:mm:ss';
        // // let timeformat = 'HH:mm:ss';
        // let FromDate = moment(req.Data.FromDate).format(datetimeformat);
        // let FromTime = moment(req.Data.FromDate).format(timeformat);
        let dateformat = 'DD/MM/YYYY';
        let timeformat = 'HH:mm:ss';
        let fromdate = moment(req.Data.FromDate).format(dateformat);
        let FromTime = moment(req.Data.FromDate).format(timeformat);
        let FromDate = fromdate + ' ' + FromTime;

        let todate = moment(req.Data.ToDate).format(dateformat);
        let totime = moment(req.Data.ToDate).format(timeformat);
        let ToDate = todate + ' ' + totime;

        let FacilityId = req.Data.FacilityId;
        let PaymentTypeId = req.Data.PaymentTypeId;
        let FacilityName = req.Data.FacilityName;
        let UserName = req.Data.UserName;
        // let PatientBillsData = data.Data[0];

        let PatientBillData: any = [];
        let PatientBillOutstanding: any = [];
        let AdjBillData: any = [];
        let PatientRefundData: any = [];
        let PatientCollectionBills: any = [];
        let PatientCollectionAdvanceAdj: any = [];
        let CollectionSalescancel: any = [];
        let CollectionSalesOutstanding: any = [];
        // let DoctorPaymentData: any = [];
        let RefundDetail: any = [];
        let DoctorPayments: any = [];
        let ReceiptNumber: string = '';
        let AmountAdjusted: number = 0;
        let RefundAmount: number = 0;

        let TotalCardCancel: number = 0;
        let TotalCashCancel: number = 0;
        let TotalChequeOthersCancel: number = 0;
        let TotalNetBankingCancel: number = 0;
        let TotalUPICancel: number = 0;
        let TotalAffordCancel: number = 0;
        let TotalCashRefund: number = 0;
        let TotalCardRefund: number = 0;
        let TotalChequeOthersRefund: number = 0;
        let TotalNetBankingRefund: number = 0;
        let TotalUPIRefund: number = 0;
        let TotalAffordRefund: number = 0;
        let TotalRefund: number = 0;
        let TotalCancel: number = 0;

        let TotalCashInvPay: number = 0;
        let TotalCardInvPay: number = 0;
        let TotalChequeOtherInvPay: number = 0;
        let TotalNetBankingInvPay: number = 0;
        let TotalUPIInvPay: number = 0;
        let TotalAffordInvPay: number = 0;
        let TotalInvPay: number = 0;

        let TotalCashAdj: number = 0;
        let TotalCardAdj: number = 0;
        let TotalChequeOthersAdj: number = 0;
        let TotalNetBankingAdj: number = 0;
        let TotalUPIAdj: number = 0;
        let TotalAffordAdj: number = 0;
        let TotalAdj: number = 0;

        let TotalCashSales: number = 0;
        let TotalCardSales: number = 0;
        let TotalChequeOthersSales: number = 0;
        let TotalNetBankingSales: number = 0;
        let TotalUPISales: number = 0;
        let TotalAffordSales: number = 0;
        let TotalSales: number = 0;
        let billReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.From, Value: req.Data.FromDate },
            { Key: PatientPaymentDetailsFilters.To, Value: req.Data.ToDate },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 },
            { Key: PatientPaymentDetailsFilters.NeqPaymentTypeId, Value: 7 },
            { Key: PatientPaymentDetailsFilters.ReceiptType, Value: [1, 2, 3, 8] },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: false },
            { Key: PatientPaymentDetailsFilters.IsPharmacyClearance, Value: false },
            { Key: PatientPaymentDetailsFilters.MultiUser, Value: req.Data.UserId },
            { Key: PatientPaymentDetailsFilters.FacilityId, Value: req.Data.FacilityId },
            ]
        };
        if (PaymentTypeId > 0) {
            billReq.Params.push({ Key: PatientPaymentDetailsFilters.PaymentTypeId, Value: PaymentTypeId });
        }
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        PatientBillData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(billReq);
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(FacilityId);
        for (let idx in PatientBillData.Data) {
            let patientbillinfo = PatientBillData.Data[idx];
            let Cash: number = 0;
            let Card: number = 0;
            let ChequeOthers: number = 0;
            let NetBanking: number = 0;
            let UPI: number = 0;
            let Afford: number = 0;
            let BillDate: any = '';
            let BillNumber: any = '';
            let Mrn = '';
            // let PatientName = patientbillinfo.Patient.Title.Description + ' ' +
            //     patientbillinfo.Patient.FirstName + ' ' + patientbillinfo.Patient.LastName;
            if (patientbillinfo.PatientBill && patientbillinfo.PatientBill.BillNumber) {
                BillNumber = patientbillinfo.PatientBill.BillNumber;
            }
            if (patientbillinfo.PatientBill && patientbillinfo.PatientBill.BillDateTime) {
                BillDate = patientbillinfo.PatientBill.BillDateTime;
            }
            let PatientName = '';
            if (patientbillinfo.Patient) {
                if (patientbillinfo.Patient && patientbillinfo.Patient.Title && patientbillinfo.Patient.Title.Description) {
                    PatientName += patientbillinfo.Patient.Title.Description;
                }
                if (patientbillinfo.Patient && patientbillinfo.Patient.FirstName) {
                    PatientName += ' ' + patientbillinfo.Patient.FirstName;
                }
                if (patientbillinfo.Patient && patientbillinfo.Patient.LastName) {
                    PatientName += ' ' + patientbillinfo.Patient.LastName;
                }
                if (patientbillinfo.Patient && patientbillinfo.Patient.MRN) {
                    Mrn = patientbillinfo.Patient.MRN;
                }
            }
            // let DoctorName = patientbillinfo.DoctorName;
            let DoctorName = '';
            if (patientbillinfo.User && patientbillinfo.User.Title && patientbillinfo.User.Title.Description) {
                DoctorName += patientbillinfo.User.Title.Description;
            }
            if (patientbillinfo.User && patientbillinfo.User.FirstName) {
                DoctorName += ' ' + patientbillinfo.User.FirstName;
            }
            if (patientbillinfo.User && patientbillinfo.User.LastName) {
                DoctorName += ' ' + patientbillinfo.User.LastName;
            }
            ReceiptNumber = patientbillinfo.ReceiptNumber;
            if ((patientbillinfo.PaymentTypeId === 1) && (patientbillinfo.ReceiptStatusId === 1)) {
                Cash = patientbillinfo.AmountPaid;
                TotalCashSales += patientbillinfo.AmountPaid;
                TotalSales += patientbillinfo.AmountPaid;
            }
            if ((patientbillinfo.PaymentTypeId === 5 || patientbillinfo.PaymentTypeId === 6) &&
                patientbillinfo.ReceiptStatusId === 1) {
                Card = patientbillinfo.AmountPaid;
                TotalCardSales += patientbillinfo.AmountPaid;
                TotalSales += patientbillinfo.AmountPaid;
            }
            if ((patientbillinfo.PaymentTypeId !== 1 && patientbillinfo.PaymentTypeId !== 5 &&
                patientbillinfo.PaymentTypeId !== 6 &&
                patientbillinfo.PaymentTypeId !== 7 && patientbillinfo.PaymentTypeId !== 10
                && patientbillinfo.PaymentTypeId !== 11 && patientbillinfo.PaymentTypeId !== 12)
                && patientbillinfo.ReceiptStatusId === 1) {
                ChequeOthers = patientbillinfo.AmountPaid;
                TotalChequeOthersSales += patientbillinfo.AmountPaid;
                TotalSales += patientbillinfo.AmountPaid;
            }
            if ((patientbillinfo.PaymentTypeId === 10) && patientbillinfo.ReceiptStatusId === 1) {
                NetBanking = patientbillinfo.AmountPaid;
                TotalNetBankingSales += patientbillinfo.AmountPaid;
                TotalSales += patientbillinfo.AmountPaid;
            }
            if ((patientbillinfo.PaymentTypeId === 11) && patientbillinfo.ReceiptStatusId === 1) {
                UPI = patientbillinfo.AmountPaid;
                TotalUPISales += patientbillinfo.AmountPaid;
                TotalSales += patientbillinfo.AmountPaid;
            } if ((patientbillinfo.PaymentTypeId === 12) && patientbillinfo.ReceiptStatusId === 1) {
                Afford = patientbillinfo.AmountPaid;
                TotalAffordSales += patientbillinfo.AmountPaid;
                TotalSales += patientbillinfo.AmountPaid;
            }
            // }
            if (Cash > 0 || Card > 0 || ChequeOthers > 0 || NetBanking > 0 || UPI > 0 || Afford > 0) {
                let paysalesData: any = {
                    BillDate: BillDate,
                    BillNumber: BillNumber,
                    ReceiptNumber: ReceiptNumber,
                    PatientName: PatientName,
                    Mrn: Mrn,
                    DoctorName: DoctorName,
                    Cash: Cash,
                    Card: Card,
                    ChequeOthers: ChequeOthers,
                    NetBanking: NetBanking,
                    UPI: UPI,
                    Afford: Afford,
                    FacilityName: FacilityName,
                    // TotalCashSales: TotalCashSales,
                    // TotalCardSales: TotalCardSales,
                    // TotalChequeOthersSales: TotalChequeOthersSales,
                    // TotalSales: TotalSales
                };
                PatientCollectionBills.push(paysalesData);
            }
        }

        let adjReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.From, Value: req.Data.FromDate },
            { Key: PatientPaymentDetailsFilters.To, Value: req.Data.ToDate },
            { Key: PatientPaymentDetailsFilters.StatusOfReceipts, Value: [1, 4] },
            { Key: PatientPaymentDetailsFilters.ReceiptType, Value: [5] },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: false },
            { Key: PatientPaymentDetailsFilters.CreatedBy, Value: req.Data.UserId },
            { Key: PatientPaymentDetailsFilters.FacilityId, Value: req.Data.FacilityId },
            { Key: PatientPaymentDetailsFilters.PaymentTypeId, Value: req.Data.PaymentTypeId },
            ]
        };
        AdjBillData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(adjReq);
        for (let idx in AdjBillData.Data) {
            let patientbillinfo = AdjBillData.Data[idx];
            let Cash: number = 0;
            let Card: number = 0;
            let ChequeOthers: number = 0;
            let NetBanking: number = 0;
            let UPI: number = 0;
            let Afford: number = 0;
            // let BillDate: any = '';
            // let BillNumber: any = '';
            // let PatientName = patientbillinfo.Patient.Title.Description + ' ' +
            //     patientbillinfo.Patient.FirstName + ' ' + patientbillinfo.Patient.LastName;
            // if (patientbillinfo.PatientBill && patientbillinfo.PatientBill.BillNumber) {
            //     BillNumber = patientbillinfo.PatientBill.BillNumber;
            // }
            // if (patientbillinfo.PatientBill && patientbillinfo.PatientBill.BillDateTime) {
            //     BillDate = patientbillinfo.PatientBill.BillDateTime;
            // }
            let Mrn = '';
            let PatientName = '';
            if (patientbillinfo.Patient) {
                if (patientbillinfo.Patient && patientbillinfo.Patient.Title && patientbillinfo.Patient.Title.Description) {
                    PatientName += patientbillinfo.Patient.Title.Description;
                }
                if (patientbillinfo.Patient && patientbillinfo.Patient.FirstName) {
                    PatientName += ' ' + patientbillinfo.Patient.FirstName;
                }
                if (patientbillinfo.Patient && patientbillinfo.Patient.LastName) {
                    PatientName += ' ' + patientbillinfo.Patient.LastName;
                }
                if (patientbillinfo.Patient && patientbillinfo.Patient.MRN) {
                    Mrn = patientbillinfo.Patient.MRN;
                }
            }
            // let DoctorName = patientbillinfo.DoctorName;
            let DoctorName = '';
            if (patientbillinfo.User && patientbillinfo.User.Title && patientbillinfo.User.Title.Description) {
                DoctorName += patientbillinfo.User.Title.Description;
            }
            if (patientbillinfo.User && patientbillinfo.User.FirstName) {
                DoctorName += ' ' + patientbillinfo.User.FirstName;
            }
            if (patientbillinfo.User && patientbillinfo.User.LastName) {
                DoctorName += ' ' + patientbillinfo.User.LastName;
            }
            ReceiptNumber = patientbillinfo.ReceiptNumber;
            AmountAdjusted = patientbillinfo.AmountAdjusted;
            RefundAmount = patientbillinfo.RefundAmount;
            if (patientbillinfo.PaymentTypeId === 1) {
                Cash = patientbillinfo.AmountPaid;
                TotalCashAdj += patientbillinfo.AmountPaid;
                TotalAdj += patientbillinfo.AmountPaid;
            }
            if (patientbillinfo.PaymentTypeId === 5 || patientbillinfo.PaymentTypeId === 6) {
                Card = patientbillinfo.AmountPaid;
                TotalCardAdj += patientbillinfo.AmountPaid;
                TotalAdj += patientbillinfo.AmountPaid;
            }
            if (patientbillinfo.PaymentTypeId !== 1 && patientbillinfo.PaymentTypeId !== 5 &&
                patientbillinfo.PaymentTypeId !== 6 && patientbillinfo.PaymentTypeId !== 10
                && patientbillinfo.PaymentTypeId !== 11 && patientbillinfo.PaymentTypeId !== 12) {
                ChequeOthers = patientbillinfo.AmountPaid;
                TotalChequeOthersAdj += patientbillinfo.AmountPaid;
                TotalAdj += patientbillinfo.AmountPaid;
            }
            if (patientbillinfo.PaymentTypeId === 10) {
                NetBanking = patientbillinfo.AmountPaid;
                TotalNetBankingAdj += patientbillinfo.AmountPaid;
                TotalAdj += patientbillinfo.AmountPaid;
            }
            if (patientbillinfo.PaymentTypeId === 11) {
                UPI = patientbillinfo.AmountPaid;
                TotalUPIAdj += patientbillinfo.AmountPaid;
                TotalAdj += patientbillinfo.AmountPaid;
            }
            if (patientbillinfo.PaymentTypeId === 12) {
                Afford = patientbillinfo.AmountPaid;
                TotalAffordAdj += patientbillinfo.AmountPaid;
                TotalAdj += patientbillinfo.AmountPaid;
            }
            // }
            if (Cash > 0 || Card > 0 || ChequeOthers > 0 || NetBanking > 0 || UPI > 0 || Afford > 0) {
                let paysalesData: any = {
                    // BillDate: BillDate,
                    // BillNumber: BillNumber,
                    ReceiptNumber: ReceiptNumber,
                    PatientName: PatientName,
                    Mrn: Mrn,
                    DoctorName: DoctorName,
                    Cash: Cash,
                    Card: Card,
                    ChequeOthers: ChequeOthers,
                    NetBanking: NetBanking,
                    UPI: UPI,
                    Afford: Afford,
                    FacilityName: FacilityName,
                    AmountAdjusted: AmountAdjusted,
                    RefundAmount: RefundAmount
                    // TotalCashSales: TotalCashSales,
                    // TotalCardSales: TotalCardSales,
                    // TotalChequeOthersSales: TotalChequeOthersSales,
                    // TotalSales: TotalSales
                };
                PatientCollectionAdvanceAdj.push(paysalesData);
            }
        }
        let OutstandingReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.FromDate, Value: req.Data.FromDate },
            { Key: PatientBillsFilters.ToDate, Value: req.Data.ToDate },
            { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
            { Key: PatientBillsFilters.IsOutStandingDesc, Value: '0' },
            { Key: PatientBillsFilters.IsPaidFully, Value: false },
            { Key: PatientBillsFilters.CreatedBy, Value: req.Data.UserId },
            { Key: PatientBillsFilters.Facility, Value: req.Data.FacilityId },
            { Key: PatientBillsFilters.PaymentTypeId, Value: req.Data.PaymentTypeId },
            ]
        };
        PatientBillOutstanding = await this.GetPatientBills(OutstandingReq);
        for (let idx in PatientBillOutstanding.Data) {
            let Outstandinginfo = PatientBillOutstanding.Data[idx];
            let BillDate = Outstandinginfo.BillDateTime;
            let BillNumber = Outstandinginfo.BillNumber;
            // let PatientName = Outstandinginfo.Patient.Title.Description + ' ' +
            //     Outstandinginfo.Patient.FirstName + ' ' + Outstandinginfo.Patient.LastName;
            let Mrn = '';
            let PatientName = '';
            if (Outstandinginfo.Patient && Outstandinginfo.Patient.Title && Outstandinginfo.Patient.Title.Description) {
                PatientName += Outstandinginfo.Patient.Title.Description;
            }
            if (Outstandinginfo.Patient && Outstandinginfo.Patient.FirstName) {
                PatientName += ' ' + Outstandinginfo.Patient.FirstName;
            }
            if (Outstandinginfo.Patient && Outstandinginfo.Patient.LastName) {
                PatientName += ' ' + Outstandinginfo.Patient.LastName;
            }
            if (Outstandinginfo.Patient && Outstandinginfo.Patient.MRN) {
                Mrn = Outstandinginfo.Patient.MRN;
            }
            let DoctorName = Outstandinginfo.DoctorName;
            let DueAmt = Outstandinginfo.OutStandingAmount;
            let payoutstandingData: any = {
                BillDate: BillDate,
                BillNumber: BillNumber,
                PatientName: PatientName,
                Mrn: Mrn,
                DoctorName: DoctorName,
                DueAmt: DueAmt,
            };
            CollectionSalesOutstanding.push(payoutstandingData);
        }

        let CancelReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.FromDate, Value: req.Data.FromDate },
            { Key: PatientBillsFilters.ToDate, Value: req.Data.ToDate },
            { Key: PatientBillsFilters.PatientBillStatus, Value: 2 },
            { Key: PatientBillsFilters.BillingType, Value: [1, 5] },
            { Key: PatientBillsFilters.UpdatedBy, Value: req.Data.UserId },
            { Key: PatientBillsFilters.Facility, Value: req.Data.FacilityId },
            { Key: PatientBillsFilters.PaymentTypeId, Value: req.Data.PaymentTypeId },
            ]
        };
        PatientBillData = await this.GetPatientBills(CancelReq);
        for (let idx in PatientBillData.Data) {
            let patientbillinfo = PatientBillData.Data[idx];
            let Cash: number = 0;
            let Card: number = 0;
            let ChequeOthers: number = 0;
            let NetBanking: number = 0;
            let UPI: number = 0;
            let Afford: number = 0;
            let BillDate = patientbillinfo.BillDateTime;
            let BillNumber = patientbillinfo.BillNumber;
            // let PatientName = patientbillinfo.Patient.Title.Description + ' ' +
            //     patientbillinfo.Patient.FirstName + ' ' + patientbillinfo.Patient.LastName;
            let Mrn = '';
            let PatientName = '';
            if (patientbillinfo.Patient && patientbillinfo.Patient.Title && patientbillinfo.Patient.Title.Description) {
                PatientName += patientbillinfo.Patient.Title.Description;
            }
            if (patientbillinfo.Patient && patientbillinfo.Patient.FirstName) {
                PatientName += ' ' + patientbillinfo.Patient.FirstName;
            }
            if (patientbillinfo.Patient && patientbillinfo.Patient.LastName) {
                PatientName += ' ' + patientbillinfo.Patient.LastName;
            }
            if (patientbillinfo.Patient && patientbillinfo.Patient.MRN) {
                Mrn = patientbillinfo.Patient.MRN;
            }
            let DoctorName = patientbillinfo.DoctorName;
            let patientcancelData = patientbillinfo.PatientPaymentDetails;
            for (let payidx in patientcancelData) {
                let canceldetailData = patientcancelData[payidx];
                ReceiptNumber = canceldetailData.ReceiptNumber;
                if (canceldetailData.PaymentTypeId === 1 && canceldetailData.ReceiptStatusId === 3) {
                    Cash = canceldetailData.AmountPaid;
                    TotalCashCancel += canceldetailData.AmountPaid;
                    TotalCancel += canceldetailData.AmountPaid;
                }
                if ((canceldetailData.PaymentTypeId === 5 || canceldetailData.PaymentTypeId === 6)
                    && canceldetailData.ReceiptStatusId === 3) {
                    Card = canceldetailData.AmountPaid;
                    TotalCardCancel += canceldetailData.AmountPaid;
                    TotalCancel += canceldetailData.AmountPaid;
                }
                if ((canceldetailData.PaymentTypeId !== 1 && canceldetailData.PaymentTypeId !== 5
                    && canceldetailData.PaymentTypeId !== 6 && canceldetailData.PaymentTypeId !== 10
                    && canceldetailData.PaymentTypeId !== 11 && canceldetailData.PaymentTypeId !== 12)
                    && canceldetailData.ReceiptStatusId === 3) {
                    ChequeOthers = canceldetailData.AmountPaid;
                    TotalChequeOthersCancel += canceldetailData.AmountPaid;
                    TotalCancel += canceldetailData.AmountPaid;
                }
                if ((canceldetailData.PaymentTypeId === 10) && canceldetailData.ReceiptStatusId === 3) {
                    NetBanking = canceldetailData.AmountPaid;
                    TotalNetBankingCancel += canceldetailData.AmountPaid;
                    TotalCancel += canceldetailData.AmountPaid;
                }
                if ((canceldetailData.PaymentTypeId === 11) && canceldetailData.ReceiptStatusId === 3) {
                    UPI = canceldetailData.AmountPaid;
                    TotalUPICancel += canceldetailData.AmountPaid;
                    TotalCancel += canceldetailData.AmountPaid;
                }
                if ((canceldetailData.PaymentTypeId === 12) && canceldetailData.ReceiptStatusId === 3) {
                    Afford = canceldetailData.AmountPaid;
                    TotalAffordCancel += canceldetailData.AmountPaid;
                    TotalCancel += canceldetailData.AmountPaid;
                }
            }
            let paycancelData: any = {
                BillDate: BillDate,
                BillNumber: BillNumber,
                ReceiptNumber: ReceiptNumber,
                PatientName: PatientName,
                Mrn: Mrn,
                DoctorName: DoctorName,
                Cash: Cash,
                Card: Card,
                ChequeOthers: ChequeOthers,
                NetBanking: NetBanking,
                UPI: UPI,
                Afford: Afford,
            };
            CollectionSalescancel.push(paycancelData);
        }
        let PatientRefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let refundReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientRefundFilters.RefundStatus, Value: 1 },
                { Key: PatientRefundFilters.RefundDateTime, Value: [req.Data.FromDate, req.Data.ToDate] },
                { Key: PatientRefundFilters.UpdatedBy, Value: req.Data.UserId },
                { Key: PatientRefundFilters.FacilityId, Value: req.Data.FacilityId },
            ]
        };
        PatientRefundData = await PatientRefundBo.GetPatientRefund(refundReq);
        for (let idx in PatientRefundData.Data) {
            let refData = PatientRefundData.Data[idx];
            if (!refData.PharmacyReturnId || refData.PharmacyReturnId === null) {
                let Cash: number = 0;
                let Card: number = 0;
                let ChequeOthers: number = 0;
                let NetBanking: number = 0;
                let UPI: number = 0;
                let Afford: number = 0;
                let RefundDate = refData.RefundDateTime;
                let RefundNumber = refData.RefundIdentifier;
                let BillNum = ''; let BillDate = '';
                let ReceiptNumber = ''; let ReceiptDateTime = ''; let DoctorName = '';
                let Mrn = '';
                let PatientName = '';
                if (refData.PatientPaymentDetail) {
                    ReceiptNumber = refData.PatientPaymentDetail.ReceiptNumber;
                    ReceiptDateTime = refData.PatientPaymentDetail.ReceiptDateTime;

                }
                if (refData.PatientBill) {
                    BillNum = refData.PatientBill.BillNumber;
                    BillDate = refData.PatientBill.BillDateTime;
                    // DoctorName = refData.PatientBill.DoctorName;
                    if (refData.PatientBill.User && refData.PatientBill.User.Title && refData.PatientBill.User.Title.Description) {
                        DoctorName += refData.PatientBill.User.Title.Description;
                    }
                    if (refData.PatientBill.User && refData.PatientBill.User.FirstName) {
                        DoctorName += ' ' + refData.PatientBill.User.FirstName;
                    }
                    if (refData.PatientBill.User && refData.PatientBill.User.LastName) {
                        DoctorName += ' ' + refData.PatientBill.User.LastName;
                    }
                }
                if (refData.Patient) {
                    if (refData.Patient && refData.Patient.Title && refData.Patient.Title.Description) {
                        PatientName += refData.Patient.Title.Description;
                    }
                    if (refData.Patient && refData.Patient.FirstName) {
                        PatientName += ' ' + refData.Patient.FirstName;
                    }
                    if (refData.Patient && refData.Patient.LastName) {
                        PatientName += ' ' + refData.Patient.LastName;
                    }
                    if (refData.Patient && refData.Patient.MRN) {
                        Mrn = refData.Patient.MRN;
                    }
                }


                if (refData.PaymentTypeId === 1 && refData.RefundStatusId === 1) {
                    Cash = refData.RefundAmount;
                    TotalCashRefund += refData.RefundAmount;
                    TotalRefund += refData.RefundAmount;
                }
                if ((refData.PaymentTypeId === 5 || refData.PaymentTypeId === 6) &&
                    refData.RefundStatusId === 1) {
                    Card = refData.RefundAmount;
                    TotalCardRefund += refData.RefundAmount;
                    TotalRefund += refData.RefundAmount;
                }
                if ((refData.PaymentTypeId !== 1 && refData.PaymentTypeId !== 5 &&
                    refData.PaymentTypeId !== 6 && refData.PaymentTypeId !== 10 && refData.PaymentTypeId !== 11
                    && refData.PaymentTypeId !== 12)
                    && refData.RefundStatusId === 1) {
                    ChequeOthers = refData.RefundAmount;
                    TotalChequeOthersRefund += refData.RefundAmount;
                    TotalRefund += refData.RefundAmount;
                }
                if ((refData.PaymentTypeId === 10) && refData.RefundStatusId === 1) {
                    NetBanking = refData.RefundAmount;
                    TotalNetBankingRefund += refData.RefundAmount;
                    TotalRefund += refData.RefundAmount;
                }
                if ((refData.PaymentTypeId === 11) && refData.RefundStatusId === 1) {
                    UPI = refData.RefundAmount;
                    TotalUPIRefund += refData.RefundAmount;
                    TotalRefund += refData.RefundAmount;
                }
                if ((refData.PaymentTypeId === 12) && refData.RefundStatusId === 1) {
                    Afford = refData.RefundAmount;
                    TotalAffordRefund += refData.RefundAmount;
                    TotalRefund += refData.RefundAmount;
                }
                let refundData: any = {
                    RefundDate: RefundDate,
                    RefundNumber: RefundNumber,
                    BillNum: BillNum,
                    BillDate: BillDate,
                    ReceiptNumber: ReceiptNumber,
                    ReceiptDateTime: ReceiptDateTime,
                    PatientName: PatientName,
                    Mrn: Mrn,
                    DoctorName: DoctorName,
                    Cash: Cash,
                    Card: Card,
                    ChequeOthers: ChequeOthers,
                    NetBanking: NetBanking,
                    UPI: UPI,
                    Afford: Afford,

                };
                RefundDetail.push(refundData);
            }
        }
        // let DoctorPaymentBo = BoFactory.GetBo(docbo.DoctorPaymentBo, this.Request);
        // let docpayReq = {
        //     Id: 0,
        //     PageContext: { PageSize: -1, PageNumber: 1 },
        //     Params: [{ Key: DoctorPaymentFilters.PaymentStatusId, Value: 3 },
        //     { Key: DoctorPaymentFilters.PaymentDate, Value: [req.Data.FromDate, req.Data.ToDate] },
        //     { Key: DoctorPaymentFilters.UpdatedBy, Value: req.Data.UserId }]
        // };
        // DoctorPaymentData = await DoctorPaymentBo.GetDoctorPayments(docpayReq);

        // for (let idx in DoctorPaymentData.Data) {
        //     let DocPayment = DoctorPaymentData.Data[idx];
        //     if (DocPayment.UserId > 0) {
        //         if (DocPayment.CreatedUser.Title)
        //             UserName = DocPayment.CreatedUser.Title.Description;
        //         if (DocPayment.CreatedUser.FirstName)
        //             UserName += ' ' + DocPayment.CreatedUser.FirstName;
        //         if (DocPayment.CreatedUser.LastName)
        //             UserName += ' ' + DocPayment.CreatedUser.LastName;
        //     } else {
        //         UserName = '';
        //     }
        //     // if (DocPayment.PaymentTypeId > 0) {
        //     //      PaymentType = DocPayment.PaymentType.Description;
        //     // }
        //     let paymentno = DocPayment.DoctorPaymentIdentifier;
        //     let paymentdate = DocPayment.PaymentDateTime;
        //     let DoctorName = '';
        //     let Cash = 0;
        //     let Card = 0;
        //     let ChequeOthers = 0;
        //     if (DocPayment.Doctor && DocPayment.Doctor.Title && DocPayment.Doctor.Title.Description) {
        //         DoctorName += DocPayment.Doctor.Title.Description;
        //     }
        //     if (DocPayment.Doctor && DocPayment.Doctor.FirstName) {
        //         DoctorName += ' ' + DocPayment.Doctor.FirstName;
        //     }
        //     if (DocPayment.Doctor && DocPayment.Doctor.LastName) {
        //         DoctorName += ' ' + DocPayment.Doctor.LastName;
        //     }
        //     if (!DoctorName) {
        //         DoctorName = DocPayment.DoctorName;
        //     }


        //     if (DocPayment.PaymentTypeId === 1) {
        //         Cash = DocPayment.DoctorPaymentAmount;
        //         TotalCashInvPay += DocPayment.DoctorPaymentAmount;
        //         TotalInvPay += DocPayment.DoctorPaymentAmount;
        //     } else if (DocPayment.PaymentTypeId === 5 || DocPayment.PaymentTypeId === 6) {
        //         Card = DocPayment.DoctorPaymentAmount;
        //         TotalCardInvPay += DocPayment.DoctorPaymentAmount;
        //         TotalInvPay += DocPayment.DoctorPaymentAmount;
        //     } else if (DocPayment.PaymentTypeId === 2 || DocPayment.PaymentTypeId === 3 || DocPayment.PaymentTypeId === 4) {
        //         ChequeOthers = DocPayment.DoctorPaymentAmount;
        //         TotChequeOtherInvPay += DocPayment.DoctorPaymentAmount;
        //         TotalInvPay += DocPayment.DoctorPaymentAmount;
        //     }

        //     if (DocPayment.PaymentTypeId === 1) { // CASH
        //         Card = 0;
        //         ChequeOthers = 0;
        //     } else if (DocPayment.PaymentTypeId === 5 || DocPayment.PaymentTypeId === 6) { //CARD
        //         Cash = 0;
        //         ChequeOthers = 0;
        //     } else if (DocPayment.PaymentTypeId === 2 || DocPayment.PaymentTypeId === 3 || DocPayment.PaymentTypeId === 4) { // CHEQUE
        //         Cash = 0;
        //         Card = 0;
        //     }

        //     if (Cash > 0 || Card > 0 || ChequeOthers > 0) {
        //         //TotalSales += (Cash + Card + ChequeOthers);
        //         let DrPaymentCollectionModel = {
        //             PaymentDate: paymentdate,
        //             PaymentNo: paymentno,
        //             DoctorName: DoctorName,
        //             Cash: Cash,
        //             Card: Card,
        //             ChequeOthers: ChequeOthers
        //         };
        //         DoctorPayments.push(DrPaymentCollectionModel);
        //     }
        // }

        let TotalNetCash = TotalCashSales + TotalCashAdj - TotalCashRefund - TotalCashInvPay;
        let TotalNetCard = TotalCardSales + TotalCardAdj - TotalCardRefund - TotalCardInvPay;
        let TotalNetCheque = TotalChequeOthersSales + TotalChequeOthersAdj -
            TotalChequeOthersRefund - TotalChequeOtherInvPay;
        let TotalNetNetBanking = TotalNetBankingSales + TotalNetBankingAdj
            - TotalNetBankingRefund - TotalNetBankingInvPay;
        let TotalNetUPI = TotalUPISales + TotalUPIAdj - TotalUPIRefund - TotalUPIInvPay;
        let TotalNetAfford = TotalAffordSales + TotalAffordAdj - TotalAffordRefund - TotalAffordInvPay;
        let NetSubmission = TotalSales + TotalAdj - TotalRefund - TotalInvPay;


        let info = {
            PatientBillData: PatientBillData,
            RefundDetail: RefundDetail,
            PatientCollectionBills: PatientCollectionBills,
            PatientBillOutstanding: PatientBillOutstanding,
            PatientCollectionAdvanceAdj: PatientCollectionAdvanceAdj,
            CollectionSalescancel: CollectionSalescancel,
            Preferences: printPreferencesData,
            TotalCashSales: TotalCashSales,
            TotalCardSales: TotalCardSales,
            TotalChequeOthersSales: TotalChequeOthersSales,
            TotalNetBankingSales: TotalNetBankingSales,
            TotalUPISales: TotalUPISales,
            TotalAffordSales: TotalAffordSales,
            FromDate: FromDate,
            ToDate: ToDate,
            TotalCashRefund: TotalCashRefund,
            TotalCardRefund: TotalCardRefund,
            TotalChequeOthersRefund: TotalChequeOthersRefund,
            TotalNetBankingRefund: TotalNetBankingRefund,
            TotalUPIRefund: TotalUPIRefund,
            TotalAffordRefund: TotalAffordRefund,
            TotalRefund: TotalRefund,
            TotalCashCancel: TotalCashCancel,
            TotalCardCancel: TotalCardCancel,
            TotalChequeOthersCancel: TotalChequeOthersCancel,
            TotalNetBankingCancel: TotalNetBankingCancel,
            TotalUPICancel: TotalUPICancel,
            TotalAffordCancel: TotalAffordCancel,
            TotalCancel: TotalCancel,
            TotalSales: TotalSales,
            FacilityName: FacilityName,
            TotalNetCash: TotalNetCash,
            TotalNetCard: TotalNetCard,
            TotalNetCheque: TotalNetCheque,
            TotalNetNetBanking: TotalNetNetBanking,
            TotalNetUPI: TotalNetUPI,
            TotalNetAfford: TotalNetAfford,
            UserName: UserName,
            NetSubmission: NetSubmission,
            TotalCashInvPay: TotalCashInvPay,
            TotalCardInvPay: TotalCardInvPay,
            TotChequeOtherInvPay: TotalChequeOtherInvPay,
            TotNetBankingInvPay: TotalNetBankingInvPay,
            TotUPIInvPay: TotalUPIInvPay,
            TotalAffordInvPay: TotalAffordInvPay,
            TotalInvPay: TotalInvPay,
            DoctorPayments: DoctorPayments,
            TotalCashAdj: TotalCashAdj,
            TotalCardAdj: TotalCardAdj,
            TotalChequeOthersAdj: TotalChequeOthersAdj,
            TotalNetBankingAdj: TotalNetBankingAdj,
            TotalUPIAdj: TotalUPIAdj,
            TotalAffordAdj: TotalAffordAdj,
            TotalAdj: TotalAdj,
            CollectionSalesOutstanding: CollectionSalesOutstanding,

        };
        let pdfOption: any = null;
        let key = 'collectiondetailcashierreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintPharmacyCollectionSummaryReport(req: BaseRequest): Promise<any> {
        let dateformat = 'DD/MM/YYYY';
        let timeformat = 'HH:mm:ss';
        let fromdate = moment(req.Data.FromDate).format(dateformat);
        let FromTime = moment(req.Data.FromDate).format(timeformat);
        let FromDate = fromdate + ' ' + FromTime;

        let todate = moment(req.Data.ToDate).format(dateformat);
        let totime = moment(req.Data.ToDate).format(timeformat);
        let ToDate = todate + ' ' + totime;
        let FacilityId = req.Data.FacilityId;
        let StoreMasterId = req.Data.StoreMasterId;
        let UserName = req.Data.UserName;

        let PatientBillData: any = [];
        let PaymentOPSales: any = [];
        let PatientPaymentDetailsData: any = [];
        let PatientReturnData: any = [];
        let PaymentOPDueCollections: any = [];
        let PaymentOPAdvanceCollections: any = [];
        let PaymentOPReturn: any = [];
        let TotCash: number = 0;
        let TotCard: number = 0;
        let TotChequeOthers: number = 0;
        let TotUPI: number = 0;
        let TotSales: number = 0;
        let TotCashDue: number = 0;
        let TotCardDue: number = 0;
        let TotChequeOthersDue: number = 0;
        let TotUPIDue: number = 0;
        let TotDues: number = 0;
        let TotCashAdvance: number = 0;
        let TotCardAdvance: number = 0;
        let TotChequeOthersAdvance: number = 0;
        let TotUPIAdvance: number = 0;
        let TotAdvances: number = 0;
        let TotCashReturn: number = 0;
        let TotCardReturn: number = 0;
        let TotChequeOthersReturn: number = 0;
        let TotUPIReturn: number = 0;
        let TotReturn: number = 0;

        let billReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.FromDate, Value: req.Data.FromDate },
            { Key: PatientBillsFilters.ToDate, Value: req.Data.ToDate },
            { Key: PatientBillsFilters.StoreMasterId, Value: req.Data.StoreMasterId },
            { Key: PatientBillsFilters.CreatedBy, Value: req.Data.UserId },
            { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
            { Key: PatientBillsFilters.BillingType, Value: 4 }]
        };
        PatientBillData = await this.GetPatientBills(billReq);
        // let billdata = PatientBillData.Data[0];
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(FacilityId, StoreMasterId);
        for (let idx in PatientBillData.Data) {
            let PatientBill = PatientBillData.Data[idx];
            let BillDate = PatientBill.BillDateTime;
            let BillNumber = PatientBill.BillNumber;
            let ReceiptNumber = '';
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let ChequeOthers = 0;
            let UPI = 0;

            if (PatientBill.User && PatientBill.User.Title && PatientBill.User.Title.Description)
                DoctorName += PatientBill.User.Title.Description;

            if (PatientBill.User && PatientBill.User.FirstName)
                DoctorName += ' ' + PatientBill.User.FirstName;

            if (PatientBill.User && PatientBill.User.LastName)
                DoctorName += ' ' + PatientBill.User.LastName;

            if (PatientBill.Patient && PatientBill.Patient.Title && PatientBill.Patient.Title.Description)
                PatientName += PatientBill.Patient.Title.Description;

            if (PatientBill.Patient && PatientBill.Patient.FirstName)
                PatientName += ' ' + PatientBill.Patient.FirstName;

            if (PatientBill.Patient && PatientBill.Patient.LastName)
                PatientName += ' ' + PatientBill.Patient.LastName;

            if (PatientBill.Patient && PatientBill.Patient.MRN)
                PatientName += ' ' + PatientBill.Patient.MRN;

            if (!PatientName)
                PatientName = PatientBill.PatientName;

            for (var payidx in PatientBill.PatientPaymentDetails) {
                var PatientPaymentDetail = PatientBill.PatientPaymentDetails[payidx];
                ReceiptNumber = PatientPaymentDetail.ReceiptNumber;
                if (PatientPaymentDetail.ReceiptTypeId === 2) {
                    if (PatientPaymentDetail.PaymentTypeId === 1 && PatientPaymentDetail.ReceiptStatusId === 1) {
                        Cash = PatientPaymentDetail.AmountPaid;
                        TotCash += PatientPaymentDetail.AmountPaid;
                        TotSales += PatientPaymentDetail.AmountPaid;
                    } else if ((PatientPaymentDetail.PaymentTypeId === 5 || PatientPaymentDetail.PaymentTypeId === 6) &&
                        PatientPaymentDetail.ReceiptStatusId === 1) {
                        Card = PatientPaymentDetail.AmountPaid;
                        TotCard += PatientPaymentDetail.AmountPaid;
                        TotSales += PatientPaymentDetail.AmountPaid;
                    } else if ((PatientPaymentDetail.PaymentTypeId === 11) && PatientPaymentDetail.ReceiptStatusId === 1) {
                        UPI = PatientPaymentDetail.AmountPaid;
                        TotUPI += PatientPaymentDetail.AmountPaid;
                        TotSales += PatientPaymentDetail.AmountPaid;
                    } else if ((PatientPaymentDetail.PaymentTypeId !== 1 || PatientPaymentDetail.PaymentTypeId !== 5 ||
                        PatientPaymentDetail.PaymentTypeId !== 6 ||
                        PatientPaymentDetail.PaymentTypeId !== 11) && PatientPaymentDetail.ReceiptStatusId === 1) {
                        ChequeOthers = PatientPaymentDetail.AmountPaid;
                        TotChequeOthers += PatientPaymentDetail.AmountPaid;
                        TotSales += PatientPaymentDetail.AmountPaid;
                    }
                    if (PatientPaymentDetail.PaymentTypeId === 1) { // CASH
                        Card = 0;
                        ChequeOthers = 0;
                        UPI = 0;
                    } else if (PatientPaymentDetail.PaymentTypeId === 5 || PatientPaymentDetail.PaymentTypeId === 6) { //CARD
                        Cash = 0;
                        ChequeOthers = 0;
                        UPI = 0;
                    } else if (PatientPaymentDetail.PaymentTypeId === 11) { //UPI
                        Cash = 0;
                        ChequeOthers = 0;
                        Card = 0;
                    } else if (PatientPaymentDetail.PaymentTypeId !== 1 || PatientPaymentDetail.PaymentTypeId !== 5 ||
                        PatientPaymentDetail.PaymentTypeId !== 6 ||
                        PatientPaymentDetail.PaymentTypeId !== 11) { // CHEQUE
                        Cash = 0;
                        Card = 0;
                        UPI = 0;
                    }

                    if (Cash > 0 || Card > 0 || ChequeOthers > 0 || UPI > 0) {
                        //$scope.TotSales += (Cash + Card + ChequeOthers);
                        let PaymentCollectionModel = {
                            BillDate: BillDate,
                            BillNumber: BillNumber,
                            ReceiptNumber: ReceiptNumber,
                            DoctorName: DoctorName,
                            PatientName: PatientName,
                            Cash: Cash,
                            Card: Card,
                            ChequeOthers: ChequeOthers,
                            UPI: UPI,

                        };
                        PaymentOPSales.push(PaymentCollectionModel);
                    }
                }
            }
        }
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let DueReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.ReceiptType, Value: 3 },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: 1 },
            { Key: PatientPaymentDetailsFilters.StoreMasterId, Value: req.Data.StoreMasterId },
            { Key: PatientPaymentDetailsFilters.CreatedBy, Value: req.Data.UserId },
            { Key: PatientPaymentDetailsFilters.ReceiptDatetime, Value: [req.Data.FromDate, req.Data.ToDate] }]
        };
        PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(DueReq);

        for (let idx in PatientPaymentDetailsData.Data) {
            let PatientDue = PatientPaymentDetailsData.Data[idx];
            let ReceiptDate = PatientDue.ReceiptDateTime;
            let ReceiptNumber = PatientDue.ReceiptNumber;
            let BillNumber = PatientDue.PatientBill.BillNumber;
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let ChequeOthers = 0;
            let UPI = 0;

            if (PatientDue.User && PatientDue.User.Title && PatientDue.User.Title.Description)
                DoctorName += PatientDue.User.Title.Description;

            if (PatientDue.User && PatientDue.User.FirstName)
                DoctorName += ' ' + PatientDue.User.FirstName;

            if (PatientDue.User && PatientDue.User.LastName)
                DoctorName += ' ' + PatientDue.User.LastName;

            if (PatientDue.Patient && PatientDue.Patient.Title && PatientDue.Patient.Title.Description)
                PatientName += PatientDue.Patient.Title.Description;

            if (PatientDue.Patient && PatientDue.Patient.FirstName)
                PatientName += ' ' + PatientDue.Patient.FirstName;

            if (PatientDue.Patient && PatientDue.Patient.LastName)
                PatientName += ' ' + PatientDue.Patient.LastName;

            if (PatientDue.Patient && PatientDue.Patient.MRN)
                PatientName += ' ' + PatientDue.Patient.MRN;

            if (!PatientName)
                PatientName = PatientDue.PatientName;

            if (PatientDue.ReceiptTypeId === 3 && PatientDue.PatientBill) {
                if (PatientDue.PaymentTypeId === 1 && PatientDue.ReceiptStatusId === 1) {
                    Cash = PatientDue.AmountPaid;
                    TotCashDue += PatientDue.AmountPaid;
                    TotDues += PatientDue.AmountPaid;
                } else if (PatientDue.PaymentTypeId === 5 || PatientDue.PaymentTypeId === 6
                    && PatientDue.ReceiptStatusId === 1) {
                    Card = PatientDue.AmountPaid;
                    TotCardDue += PatientDue.AmountPaid;
                    TotDues += PatientDue.AmountPaid;
                } else if ((PatientDue.PaymentTypeId === 11)
                    && PatientDue.ReceiptStatusId === 1) {
                    UPI = PatientDue.AmountPaid;
                    TotUPIDue += PatientDue.AmountPaid;
                    TotDues += PatientDue.AmountPaid;
                } else if (PatientDue.PaymentTypeId !== 1 || PatientDue.PaymentTypeId !== 5 ||
                    PatientDue.PaymentTypeId !== 6 ||
                    PatientDue.PaymentTypeId !== 11
                    && PatientDue.ReceiptStatusId === 1) {
                    ChequeOthers = PatientDue.AmountPaid;
                    TotChequeOthersDue += PatientDue.AmountPaid;
                    TotDues += PatientDue.AmountPaid;
                }
                if (PatientDue.PaymentTypeId === 1) { // CASH
                    Card = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (PatientDue.PaymentTypeId === 5
                    || PatientDue.PaymentTypeId === 6) { //CARD
                    Cash = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (PatientDue.PaymentTypeId === 11) { //UPI
                    Cash = 0;
                    ChequeOthers = 0;
                    Card = 0;
                } else if (PatientDue.PaymentTypeId !== 1 || PatientDue.PaymentTypeId !== 5 ||
                    PatientDue.PaymentTypeId !== 6 ||
                    PatientDue.PaymentTypeId !== 11) { // CHEQUE
                    Cash = 0;
                    Card = 0;
                    UPI = 0;
                }
                if (Cash > 0 || Card > 0 || ChequeOthers > 0 || UPI > 0) {
                    //$scope.TotDues += (Cash + Card + ChequeOthers);
                    let PaymentCollectionModel = {
                        ReceiptDate: ReceiptDate,
                        ReceiptNumber: ReceiptNumber,
                        BillNumber: BillNumber,
                        DoctorName: DoctorName,
                        PatientName: PatientName,
                        Cash: Cash,
                        Card: Card,
                        ChequeOthers: ChequeOthers,
                        UPI: UPI,
                    };
                    PaymentOPDueCollections.push(PaymentCollectionModel);
                }

            }
        }
        let AdvPatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let AdvanceReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.ReceiptType, Value: 7 },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 },
            { Key: PatientPaymentDetailsFilters.StoreMasterId, Value: req.Data.StoreMasterId },
            { Key: PatientPaymentDetailsFilters.IsPharmacyReceipt, Value: 1 },
            { Key: PatientPaymentDetailsFilters.CreatedBy, Value: req.Data.UserId },
            // { Key: PatientPaymentDetailsFilters.BillType, Value: 4 },
            // { Key: PatientPaymentDetailsFilters.CreatedBy, Value: UserId },
            // { Key: PatientPaymentDetailsFilters.StoreMasterId, Value: StoreMasterId },
            { Key: PatientPaymentDetailsFilters.ReceiptDatetime, Value: [req.Data.FromDate, req.Data.ToDate] }]
        };
        PatientPaymentDetailsData = await AdvPatientPaymentDetailsBo.GetPatientPaymentDetails(AdvanceReq);

        for (let idx in PatientPaymentDetailsData.Data) {
            let PatientAdvance = PatientPaymentDetailsData.Data[idx];
            let BillNumber = '';
            let ReceiptDate = PatientAdvance.ReceiptDateTime;
            let ReceiptNumber = PatientAdvance.ReceiptNumber;
            if (PatientAdvance.PatientBill) {
                BillNumber = PatientAdvance.PatientBill.BillNumber;
            }
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let ChequeOthers = 0;
            let UPI = 0;




            if (PatientAdvance.User && PatientAdvance.User.Title && PatientAdvance.User.Title.Description)
                DoctorName += PatientAdvance.User.Title.Description;

            if (PatientAdvance.User && PatientAdvance.User.FirstName)
                DoctorName += ' ' + PatientAdvance.User.FirstName;

            if (PatientAdvance.User && PatientAdvance.User.LastName)
                DoctorName += ' ' + PatientAdvance.User.LastName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.Title && PatientAdvance.Patient.Title.Description)
                PatientName += PatientAdvance.Patient.Title.Description;

            if (PatientAdvance.Patient && PatientAdvance.Patient.FirstName)
                PatientName += ' ' + PatientAdvance.Patient.FirstName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.LastName)
                PatientName += ' ' + PatientAdvance.Patient.LastName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.MRN)
                PatientName += ' ' + PatientAdvance.Patient.MRN;

            if (!PatientName)
                PatientName = PatientAdvance.PatientName;

            if (PatientAdvance.ReceiptTypeId === 7) {
                if (PatientAdvance.PaymentTypeId === 1 && PatientAdvance.ReceiptStatusId === 1) {
                    Cash = PatientAdvance.AmountPaid;
                    TotCashAdvance += PatientAdvance.AmountPaid;
                    TotAdvances += PatientAdvance.AmountPaid;
                } else if (PatientAdvance.PaymentTypeId === 5 || PatientAdvance.PaymentTypeId === 6
                    && PatientAdvance.ReceiptStatusId === 1) {
                    Card = PatientAdvance.AmountPaid;
                    TotCardAdvance += PatientAdvance.AmountPaid;
                    TotAdvances += PatientAdvance.AmountPaid;
                } else if ((PatientAdvance.PaymentTypeId === 11) && PatientAdvance.ReceiptStatusId === 1) {
                    UPI = PatientAdvance.AmountPaid;
                    TotUPIAdvance += PatientAdvance.AmountPaid;
                    TotAdvances += PatientAdvance.AmountPaid;
                } else if ((PatientAdvance.PaymentTypeId !== 1 || PatientAdvance.PaymentTypeId !== 5 ||
                    PatientAdvance.PaymentTypeId !== 6 ||
                    PatientAdvance.PaymentTypeId !== 11) && PatientAdvance.ReceiptStatusId === 1) {
                    ChequeOthers = PatientAdvance.AmountPaid;
                    TotChequeOthersAdvance += PatientAdvance.AmountPaid;
                    TotAdvances += PatientAdvance.AmountPaid;
                }
                if (PatientAdvance.PaymentTypeId === 1) { // CASH
                    Card = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (PatientAdvance.PaymentTypeId === 5
                    || PatientAdvance.PaymentTypeId === 6) { //CARD
                    Cash = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (PatientAdvance.PaymentTypeId === 11) { // UPI
                    Cash = 0;
                    Card = 0;
                    ChequeOthers = 0;
                } else if (PatientAdvance.PaymentTypeId !== 1 || PatientAdvance.PaymentTypeId !== 5 ||
                    PatientAdvance.PaymentTypeId !== 6 ||
                    PatientAdvance.PaymentTypeId !== 11) { // CHEQUE
                    Cash = 0;
                    Card = 0;
                    UPI = 0;
                }
                if (Cash > 0 || Card > 0 || ChequeOthers > 0 || UPI > 0) {
                    //$scope.TotAdvances += (Cash + Card + ChequeOthers);
                    let PaymentCollectionModel = {
                        ReceiptDate: ReceiptDate,
                        ReceiptNumber: ReceiptNumber,
                        BillNumber: BillNumber,
                        DoctorName: DoctorName,
                        PatientName: PatientName,
                        Cash: Cash,
                        Card: Card,
                        ChequeOthers: ChequeOthers,
                        UPI: UPI,
                    };
                    PaymentOPAdvanceCollections.push(PaymentCollectionModel);
                }

            }
        }
        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let returnReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientReturnsFilters.TypeOfReturn, Value: [1, 2, 3, 4, 5] },
            { Key: PatientReturnsFilters.PatientReturnStatus, Value: 3 },
            { Key: PatientReturnsFilters.PatReturnDt, Value: [req.Data.FromDate, req.Data.ToDate] },
            { Key: PatientReturnsFilters.StoreMasterId, Value: req.Data.StoreMasterId },
            { Key: PatientReturnsFilters.CreatedBy, Value: req.Data.UserId },
            ]
        };
        PatientReturnData = await PatientReturnsBo.GetPatientReturns(returnReq);
        for (let idx in PatientReturnData.Data) {
            let PatientReturnBill = PatientReturnData.Data[idx];
            let ReturnDate = PatientReturnBill.ReturnDateTime;
            let ReturnNumber = PatientReturnBill.ReturnNumber;
            let BillDate = null;
            let BillNumber = '';
            let DoctorName = '';
            if (PatientReturnBill.PatientBill) {
                BillDate = PatientReturnBill.PatientBill.BillDateTime;
                BillNumber = PatientReturnBill.PatientBill.BillNumber;
            }
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let ChequeOthers = 0;
            let UPI = 0;

            if (PatientReturnBill.Patient && PatientReturnBill.Patient.Title && PatientReturnBill.Patient.Title.Description)
                PatientName += PatientReturnBill.Patient.Title.Description;

            if (PatientReturnBill.Patient && PatientReturnBill.Patient.FirstName)
                PatientName += ' ' + PatientReturnBill.Patient.FirstName;

            if (PatientReturnBill.Patient && PatientReturnBill.Patient.LastName)
                PatientName += ' ' + PatientReturnBill.Patient.LastName;

            if (PatientReturnBill.Patient && PatientReturnBill.Patient.MRN)
                PatientName += ' ' + PatientReturnBill.Patient.MRN;

            if (!PatientName)
                PatientName = PatientReturnBill.PatientName;

            if (PatientReturnBill.User && PatientReturnBill.User.Title && PatientReturnBill.User.Title.Description)
                DoctorName += PatientReturnBill.User.Title.Description;

            if (PatientReturnBill.User && PatientReturnBill.User.FirstName)
                DoctorName += ' ' + PatientReturnBill.User.FirstName;

            if (PatientReturnBill.User && PatientReturnBill.User.LastName)
                DoctorName += ' ' + PatientReturnBill.User.LastName;

            let refnumber = '';
            if (PatientReturnBill.PatientRefunds && PatientReturnBill.PatientRefunds.length > 0) {
                for (let rfdx in PatientReturnBill.PatientRefunds) {
                    var refundDetail = PatientReturnBill.PatientRefunds[rfdx];
                    refnumber = refundDetail.RefundNumber;
                    if (refundDetail.PaymentTypeId === 1 && refundDetail.RefundStatusId === 1) {
                        Cash = refundDetail.RefundAmount;
                        TotCashReturn += refundDetail.RefundAmount;
                        TotReturn += refundDetail.RefundAmount;
                    } else if ((refundDetail.PaymentTypeId === 5 ||
                        refundDetail.PaymentTypeId === 6) && refundDetail.RefundStatusId === 1) {
                        Card = refundDetail.RefundAmount;
                        TotCardReturn += refundDetail.RefundAmount;
                        TotReturn += refundDetail.RefundAmount;
                    } else if ((refundDetail.PaymentTypeId === 11) && refundDetail.RefundStatusId === 1) {
                        UPI = refundDetail.RefundAmount;
                        TotUPIReturn += refundDetail.RefundAmount;
                        TotReturn += refundDetail.RefundAmount;
                    } else if ((refundDetail.PaymentTypeId !== 1 || refundDetail.PaymentTypeId !== 5 ||
                        refundDetail.PaymentTypeId !== 6 ||
                        refundDetail.PaymentTypeId !== 11) && refundDetail.RefundStatusId === 1) {
                        ChequeOthers = refundDetail.RefundAmount;
                        TotChequeOthersReturn += refundDetail.RefundAmount;
                        TotReturn += refundDetail.RefundAmount;
                    }

                }
                // Cash = PatientReturnBill.ReturnAmount;
                // $scope.TotCashReturn += PatientReturnBill.ReturnAmount;
                // $scope.TotReturn += PatientReturnBill.ReturnAmount;

                if (refundDetail.PaymentTypeId === 1) { // CASH
                    Card = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (refundDetail.PaymentTypeId === 5 || refundDetail.PaymentTypeId === 6) { //CARD
                    Cash = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (refundDetail.PaymentTypeId === 11) { // UPI
                    Cash = 0;
                    Card = 0;
                    ChequeOthers = 0;
                } else if (refundDetail.PaymentTypeId !== 1 || refundDetail.PaymentTypeId !== 5 ||
                    refundDetail.PaymentTypeId !== 6 ||
                    refundDetail.PaymentTypeId !== 11) { // CHEQUE
                    Cash = 0;
                    Card = 0;
                    UPI = 0;
                }
            }
            if (Cash > 0 || Card > 0 || ChequeOthers > 0 || UPI > 0) {
                let PaymentCollectionModel = {
                    BillDate: BillDate,
                    ReturnDate: ReturnDate,
                    ReturnNumber: ReturnNumber,
                    BillNumber: BillNumber,
                    DoctorName: DoctorName,
                    PatientName: PatientName,
                    Cash: Cash,
                    Card: Card,
                    ChequeOthers: ChequeOthers,
                    UPI: UPI,
                };
                PaymentOPReturn.push(PaymentCollectionModel);
            }
        }
        let TotalNetCash = (TotCash + TotCashAdvance + TotCashDue) - TotCashReturn;
        let TotalNetCard = (TotCard + TotCardAdvance + TotCardDue) - TotCardReturn;
        let TotalNetCheque = (TotChequeOthers + TotChequeOthersAdvance + TotChequeOthersDue) - TotChequeOthersReturn;
        let TotalNetUPI = (TotUPI + TotUPIAdvance + TotUPIDue) - TotUPIReturn;
        let NetSubmission = (TotSales + TotAdvances + TotDues) - TotReturn;

        let info = {
            PatientBillData: PatientBillData,
            PaymentOPSales: PaymentOPSales,
            PaymentOPDueCollections: PaymentOPDueCollections,
            PaymentOPAdvanceCollections: PaymentOPAdvanceCollections,
            TotCash: TotCash,
            TotCard: TotCard,
            TotChequeOthers: TotChequeOthers,
            TotUPI: TotUPI,
            FromDate: FromDate,
            ToDate: ToDate,
            TotSales: TotSales,
            TotCashDue: TotCashDue,
            TotCardDue: TotCardDue,
            TotChequeOthersDue: TotChequeOthersDue,
            TotUPIDue: TotUPIDue,
            TotCashAdvance: TotCashAdvance,
            TotCardAdvance: TotCardAdvance,
            TotChequeOthersAdvance: TotChequeOthersAdvance,
            TotUPIAdvance: TotUPIAdvance,
            TotAdvances: TotAdvances,
            TotDues: TotDues,
            TotCashReturn: TotCashReturn,
            TotCardReturn: TotCardReturn,
            TotChequeOthersReturn: TotChequeOthersReturn,
            TotUPIReturn: TotUPIReturn,
            TotReturn: TotReturn,
            PaymentOPReturn: PaymentOPReturn,
            TotalNetCash: TotalNetCash,
            TotalNetCard: TotalNetCard,
            TotalNetCheque: TotalNetCheque,
            TotalNetUPI: TotalNetUPI,
            NetSubmission: NetSubmission,
            Preferences: printPreferencesData,
            StorePreferences: printStoreData,
            UserName: UserName,
        };
        let pdfOption: any = null;
        let key = 'pharmacycollectionsummaryreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintPharmacyCollectionReport(req: BaseRequest): Promise<any> {
        let dateformat = 'DD/MM/YYYY';
        let timeformat = 'HH:mm:ss';
        let fromdate = moment(req.Data.FromDate).format(dateformat);
        let FromTime = moment(req.Data.FromDate).format(timeformat);
        let FromDate = fromdate + ' ' + FromTime;

        let todate = moment(req.Data.ToDate).format(dateformat);
        let totime = moment(req.Data.ToDate).format(timeformat);
        let ToDate = todate + ' ' + totime;
        let PaymentType = req.Data.PaymentType;
        let FacilityId = req.Data.FacilityId;
        let StoreMasterId = req.Data.StoreMasterId;
        let UserName = req.Data.UserName;
        let StoreMaster = req.Data.StoreMaster;
        let UserId = req.Data.UserId;

        let PatientBillData: any = [];
        let PaymentOPSales: any = [];
        let PatientPaymentDetailsData: any = [];
        let PatientReturnData: any = [];
        let PaymentOPDueCollections: any = [];
        let PaymentOPAdvanceCollections: any = [];
        let PaymentOPReturn: any = [];
        let TotCash: number = 0;
        let TotCard: number = 0;
        let TotChequeOthers: number = 0;
        let TotUPI: number = 0;
        let TotSales: number = 0;
        let TotCashDue: number = 0;
        let TotCardDue: number = 0;
        let TotUPIDue: number = 0;
        let TotChequeOthersDue: number = 0;
        let TotDues: number = 0;
        let TotCashAdvance: number = 0;
        let TotCardAdvance: number = 0;
        let TotChequeOthersAdvance: number = 0;
        let TotUPIAdvance: number = 0;
        let TotAdvances: number = 0;
        let TotCashReturn: number = 0;
        let TotCardReturn: number = 0;
        let TotChequeOthersReturn: number = 0;
        let TotUPIReturn: number = 0;
        let TotReturn: number = 0;

        let billReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.FromDate, Value: req.Data.FromDate },
            { Key: PatientBillsFilters.ToDate, Value: req.Data.ToDate },
            { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
            { Key: PatientBillsFilters.BillingType, Value: [4, 6] },
            { Key: PatientBillsFilters.IsPharmacyBill, Value: true },
            { Key: PatientBillsFilters.StoreMasterId, Value: StoreMasterId },
            { Key: PatientBillsFilters.CreatedBy, Value: UserId }]
        };
        PatientBillData = await this.GetPatientBills(billReq);
        // let billdata = PatientBillData.Data[0];
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(FacilityId, StoreMasterId);
        if (printStoreData && printStoreData.pharmacyprintheader)
            printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        if (printStoreData && printStoreData.pharmacyprintfooter)
            printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        if (printStoreData && printStoreData.StoreLogo)
            printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        for (let idx in PatientBillData.Data) {
            let PatientBill = PatientBillData.Data[idx];
            let BillDate = PatientBill.BillDateTime;
            let BillNumber = PatientBill.BillNumber;
            let ReceiptNumber = '';
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let UPI = 0;
            let ChequeOthers = 0;
            let billedby = '';

            if (PatientBill.User && PatientBill.User.Title && PatientBill.User.Title.Description)
                DoctorName += PatientBill.User.Title.Description;

            if (PatientBill.User && PatientBill.User.FirstName)
                DoctorName += ' ' + PatientBill.User.FirstName;

            if (PatientBill.User && PatientBill.User.LastName)
                DoctorName += ' ' + PatientBill.User.LastName;

            if (PatientBill.Patient && PatientBill.Patient.Title && PatientBill.Patient.Title.Description)
                PatientName += PatientBill.Patient.Title.Description;

            if (PatientBill.Patient && PatientBill.Patient.FirstName)
                PatientName += ' ' + PatientBill.Patient.FirstName;

            if (PatientBill.Patient && PatientBill.Patient.LastName)
                PatientName += ' ' + PatientBill.Patient.LastName;

            if (PatientBill.Patient && PatientBill.Patient.MRN)
                PatientName += ' ' + PatientBill.Patient.MRN;

            if (!PatientName)
                PatientName = PatientBill.PatientName;

            for (var payidx in PatientBill.PatientPaymentDetails) {
                var PatientPaymentDetail = PatientBill.PatientPaymentDetails[payidx];
                ReceiptNumber = PatientPaymentDetail.ReceiptNumber;

                if (PatientPaymentDetail.CreatedUser && PatientPaymentDetail.CreatedUser.Title
                    && PatientPaymentDetail.CreatedUser.Title.Description)
                    billedby += PatientPaymentDetail.CreatedUser.Title.Description;

                if (PatientPaymentDetail.CreatedUser && PatientPaymentDetail.CreatedUser.FirstName)
                    billedby += ' ' + PatientPaymentDetail.CreatedUser.FirstName;

                if (PatientPaymentDetail.CreatedUser && PatientPaymentDetail.CreatedUser.LastName)
                    billedby += ' ' + PatientPaymentDetail.CreatedUser.LastName;


                if (PatientPaymentDetail.ReceiptTypeId === 2) {
                    if (UserId === -1 || UserId === PatientPaymentDetail.CreatedBy) {
                        if (PatientPaymentDetail.PaymentTypeId === 1 && PatientPaymentDetail.ReceiptStatusId === 1) {
                            Cash = PatientPaymentDetail.AmountPaid;
                            TotCash += PatientPaymentDetail.AmountPaid;
                            TotSales += PatientPaymentDetail.AmountPaid;
                        } else if ((PatientPaymentDetail.PaymentTypeId === 5 || PatientPaymentDetail.PaymentTypeId === 6) &&
                            PatientPaymentDetail.ReceiptStatusId === 1) {
                            Card = PatientPaymentDetail.AmountPaid;
                            TotCard += PatientPaymentDetail.AmountPaid;
                            TotSales += PatientPaymentDetail.AmountPaid;
                        } else if ((PatientPaymentDetail.PaymentTypeId === 11) &&
                            PatientPaymentDetail.ReceiptStatusId === 1) {
                            UPI = PatientPaymentDetail.AmountPaid;
                            TotUPI += PatientPaymentDetail.AmountPaid;
                            TotSales += PatientPaymentDetail.AmountPaid;
                        } else if ((PatientPaymentDetail.PaymentTypeId !== 1 || PatientPaymentDetail.PaymentTypeId !== 5 ||
                            PatientPaymentDetail.PaymentTypeId !== 6 ||
                            PatientPaymentDetail.PaymentTypeId !== 11) && PatientPaymentDetail.ReceiptStatusId === 1) {
                            ChequeOthers = PatientPaymentDetail.AmountPaid;
                            TotChequeOthers += PatientPaymentDetail.AmountPaid;
                            TotSales += PatientPaymentDetail.AmountPaid;
                        }
                        if (PatientPaymentDetail.PaymentTypeId === 1) { // CASH
                            Card = 0;
                            UPI = 0;
                            ChequeOthers = 0;
                        } else if (PatientPaymentDetail.PaymentTypeId === 5 || PatientPaymentDetail.PaymentTypeId === 6) { //CARD
                            Cash = 0;
                            ChequeOthers = 0;
                            UPI = 0;
                        } else if (PatientPaymentDetail.PaymentTypeId === 11) { //UPI
                            Cash = 0;
                            ChequeOthers = 0;
                            Card = 0;
                        } else if (PatientPaymentDetail.PaymentTypeId !== 1 || PatientPaymentDetail.PaymentTypeId !== 5 ||
                            PatientPaymentDetail.PaymentTypeId !== 6 || PatientPaymentDetail.PaymentTypeId !== 11) { // CHEQUE
                            Cash = 0;
                            Card = 0;
                            UPI = 0;
                        }

                        if (Cash > 0 || Card > 0 || UPI > 0 || ChequeOthers > 0) {
                            //$scope.TotSales += (Cash + Card + ChequeOthers);
                            let PaymentCollectionModel = {
                                BillDate: BillDate,
                                BillNumber: BillNumber,
                                ReceiptNumber: ReceiptNumber,
                                DoctorName: DoctorName,
                                Billedby: billedby,
                                PatientName: PatientName,
                                Cash: Cash,
                                Card: Card,
                                UPI: UPI,
                                ChequeOthers: ChequeOthers,

                            };
                            PaymentOPSales.push(PaymentCollectionModel);
                        }
                    }
                }
            }
        }
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let DueReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.ReceiptType, Value: 3 },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 },
            { Key: PatientPaymentDetailsFilters.BillType, Value: 4 },
            { Key: PatientPaymentDetailsFilters.CreatedBy, Value: UserId },
            { Key: PatientPaymentDetailsFilters.StoreMasterId, Value: StoreMasterId },
            { Key: PatientPaymentDetailsFilters.ReceiptDatetime, Value: [req.Data.FromDate, req.Data.ToDate] }]
        };
        PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(DueReq);

        for (let idx in PatientPaymentDetailsData.Data) {
            let PatientDue = PatientPaymentDetailsData.Data[idx];
            let BillNumber = '';
            let ReceiptDate = PatientDue.ReceiptDateTime;
            let ReceiptNumber = PatientDue.ReceiptNumber;
            if (PatientDue.PatientBill) {
                BillNumber = PatientDue.PatientBill.BillNumber;
            }
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let UPI = 0;
            let ChequeOthers = 0;
            let billedby = '';


            if (PatientDue.CreatedUser && PatientDue.CreatedUser.Title && PatientDue.CreatedUser.Title.Description)
                billedby += PatientDue.CreatedUser.Title.Description;

            if (PatientDue.CreatedUser && PatientDue.CreatedUser.FirstName)
                billedby += ' ' + PatientDue.CreatedUser.FirstName;

            if (PatientDue.CreatedUser && PatientDue.CreatedUser.LastName)
                billedby += ' ' + PatientDue.CreatedUser.LastName;


            if (PatientDue.User && PatientDue.User.Title && PatientDue.User.Title.Description)
                DoctorName += PatientDue.User.Title.Description;

            if (PatientDue.User && PatientDue.User.FirstName)
                DoctorName += ' ' + PatientDue.User.FirstName;

            if (PatientDue.User && PatientDue.User.LastName)
                DoctorName += ' ' + PatientDue.User.LastName;

            if (PatientDue.Patient && PatientDue.Patient.Title && PatientDue.Patient.Title.Description)
                PatientName += PatientDue.Patient.Title.Description;

            if (PatientDue.Patient && PatientDue.Patient.FirstName)
                PatientName += ' ' + PatientDue.Patient.FirstName;

            if (PatientDue.Patient && PatientDue.Patient.LastName)
                PatientName += ' ' + PatientDue.Patient.LastName;

            if (PatientDue.Patient && PatientDue.Patient.MRN)
                PatientName += ' ' + PatientDue.Patient.MRN;

            if (!PatientName)
                PatientName = PatientDue.PatientName;

            if (PatientDue.ReceiptTypeId === 3 && PatientDue.PatientBill) {
                if (UserId === -1 || UserId === PatientDue.CreatedBy) {
                    if (PatientDue.PaymentTypeId === 1 && PatientDue.ReceiptStatusId === 1) {
                        Cash = PatientDue.AmountPaid;
                        TotCashDue += PatientDue.AmountPaid;
                        TotDues += PatientDue.AmountPaid;
                    } else if (PatientDue.PaymentTypeId === 5 || PatientDue.PaymentTypeId === 6
                        && PatientDue.ReceiptStatusId === 1) {
                        Card = PatientDue.AmountPaid;
                        TotCardDue += PatientDue.AmountPaid;
                        TotDues += PatientDue.AmountPaid;
                    } else if (PatientDue.PaymentTypeId === 11
                        && PatientDue.ReceiptStatusId === 1) {
                        UPI = PatientDue.AmountPaid;
                        TotUPIDue += PatientDue.AmountPaid;
                        TotDues += PatientDue.AmountPaid;
                    } else if (PatientDue.PaymentTypeId !== 1 || PatientDue.PaymentTypeId !== 5 ||
                        PatientDue.PaymentTypeId !== 6 || PatientDue.PaymentTypeId !== 11 && PatientDue.ReceiptStatusId === 1) {
                        ChequeOthers = PatientDue.AmountPaid;
                        TotChequeOthersDue += PatientDue.AmountPaid;
                        TotDues += PatientDue.AmountPaid;
                    }
                    if (PatientDue.PaymentTypeId === 1) { // CASH
                        Card = 0;
                        UPI = 0;
                        ChequeOthers = 0;
                    } else if (PatientDue.PaymentTypeId === 5
                        || PatientDue.PaymentTypeId === 6) { //CARD
                        Cash = 0;
                        UPI = 0;
                        ChequeOthers = 0;
                    } else if (PatientDue.PaymentTypeId === 11) { //UPI
                        Cash = 0;
                        Card = 0;
                        ChequeOthers = 0;
                    } else if (PatientDue.PaymentTypeId !== 1 || PatientDue.PaymentTypeId !== 5 ||
                        PatientDue.PaymentTypeId !== 6 || PatientDue.PaymentTypeId !== 11) { // CHEQUE
                        Cash = 0;
                        Card = 0;
                        UPI = 0;
                    }
                    if (Cash > 0 || Card > 0 || UPI > 0 || ChequeOthers > 0) {
                        //$scope.TotDues += (Cash + Card + ChequeOthers);
                        let PaymentCollectionModel = {
                            ReceiptDate: ReceiptDate,
                            ReceiptNumber: ReceiptNumber,
                            BillNumber: BillNumber,
                            DoctorName: DoctorName,
                            Billedby: billedby,
                            PatientName: PatientName,
                            Cash: Cash,
                            Card: Card,
                            UPI: UPI,
                            ChequeOthers: ChequeOthers,
                        };
                        PaymentOPDueCollections.push(PaymentCollectionModel);
                    }
                }
            }
        }
        let AdvPatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let AdvanceReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.ReceiptType, Value: 7 },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 },
            // { Key: PatientPaymentDetailsFilters.BillType, Value: 4 },
            { Key: PatientPaymentDetailsFilters.CreatedBy, Value: UserId },
            // { Key: PatientPaymentDetailsFilters.StoreMasterId, Value: StoreMasterId },
            { Key: PatientPaymentDetailsFilters.ReceiptDatetime, Value: [req.Data.FromDate, req.Data.ToDate] }]
        };
        PatientPaymentDetailsData = await AdvPatientPaymentDetailsBo.GetPatientPaymentDetails(AdvanceReq);

        for (let idx in PatientPaymentDetailsData.Data) {
            let PatientAdvance = PatientPaymentDetailsData.Data[idx];
            let BillNumber = '';
            let ReceiptDate = PatientAdvance.ReceiptDateTime;
            let ReceiptNumber = PatientAdvance.ReceiptNumber;
            if (PatientAdvance.PatientBill) {
                BillNumber = PatientAdvance.PatientBill.BillNumber;
            }
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let UPI = 0;
            let ChequeOthers = 0;
            let billedby = '';


            if (PatientAdvance.CreatedUser && PatientAdvance.CreatedUser.Title && PatientAdvance.CreatedUser.Title.Description)
                billedby += PatientAdvance.CreatedUser.Title.Description;

            if (PatientAdvance.CreatedUser && PatientAdvance.CreatedUser.FirstName)
                billedby += ' ' + PatientAdvance.CreatedUser.FirstName;

            if (PatientAdvance.CreatedUser && PatientAdvance.CreatedUser.LastName)
                billedby += ' ' + PatientAdvance.CreatedUser.LastName;


            if (PatientAdvance.User && PatientAdvance.User.Title && PatientAdvance.User.Title.Description)
                DoctorName += PatientAdvance.User.Title.Description;

            if (PatientAdvance.User && PatientAdvance.User.FirstName)
                DoctorName += ' ' + PatientAdvance.User.FirstName;

            if (PatientAdvance.User && PatientAdvance.User.LastName)
                DoctorName += ' ' + PatientAdvance.User.LastName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.Title && PatientAdvance.Patient.Title.Description)
                PatientName += PatientAdvance.Patient.Title.Description;

            if (PatientAdvance.Patient && PatientAdvance.Patient.FirstName)
                PatientName += ' ' + PatientAdvance.Patient.FirstName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.LastName)
                PatientName += ' ' + PatientAdvance.Patient.LastName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.MRN)
                PatientName += ' ' + PatientAdvance.Patient.MRN;

            if (!PatientName)
                PatientName = PatientAdvance.PatientName;

            if (PatientAdvance.ReceiptTypeId === 7) {
                if (UserId === -1 || UserId === PatientAdvance.CreatedBy) {
                    if (PatientAdvance.PaymentTypeId === 1 && PatientAdvance.ReceiptStatusId === 1) {
                        Cash = PatientAdvance.AmountPaid;
                        TotCashAdvance += PatientAdvance.AmountPaid;
                        TotAdvances += PatientAdvance.AmountPaid;
                    } else if (PatientAdvance.PaymentTypeId === 5 || PatientAdvance.PaymentTypeId === 6
                        && PatientAdvance.ReceiptStatusId === 1) {
                        Card = PatientAdvance.AmountPaid;
                        TotCardAdvance += PatientAdvance.AmountPaid;
                        TotAdvances += PatientAdvance.AmountPaid;
                    } else if (PatientAdvance.PaymentTypeId === 11
                        && PatientAdvance.ReceiptStatusId === 1) {
                        UPI = PatientAdvance.AmountPaid;
                        TotUPIAdvance += PatientAdvance.AmountPaid;
                        TotAdvances += PatientAdvance.AmountPaid;
                    } else if (PatientAdvance.PaymentTypeId !== 1 || PatientAdvance.PaymentTypeId !== 5 ||
                        PatientAdvance.PaymentTypeId !== 6 || PatientAdvance.PaymentTypeId !== 11 && PatientAdvance.ReceiptStatusId === 1) {
                        ChequeOthers = PatientAdvance.AmountPaid;
                        TotChequeOthersAdvance += PatientAdvance.AmountPaid;
                        TotAdvances += PatientAdvance.AmountPaid;
                    }
                    if (PatientAdvance.PaymentTypeId === 1) { // CASH
                        Card = 0;
                        UPI = 0;
                        ChequeOthers = 0;
                    } else if (PatientAdvance.PaymentTypeId === 5
                        || PatientAdvance.PaymentTypeId === 6) { //CARD
                        Cash = 0;
                        ChequeOthers = 0;
                        UPI = 0;
                    } else if (PatientAdvance.PaymentTypeId === 11) { //UPI
                        Cash = 0;
                        ChequeOthers = 0;
                        Card = 0;
                    } else if (PatientAdvance.PaymentTypeId !== 1 || PatientAdvance.PaymentTypeId !== 5 ||
                        PatientAdvance.PaymentTypeId !== 6 || PatientAdvance.PaymentTypeId !== 11) { // CHEQUE
                        Cash = 0;
                        Card = 0;
                        UPI = 0;
                    }
                    if (Cash > 0 || Card > 0 || UPI > 0 || ChequeOthers > 0) {
                        //$scope.TotAdvances += (Cash + Card + ChequeOthers);
                        let PaymentCollectionModel = {
                            ReceiptDate: ReceiptDate,
                            ReceiptNumber: ReceiptNumber,
                            BillNumber: BillNumber,
                            DoctorName: DoctorName,
                            Billedby: billedby,
                            PatientName: PatientName,
                            Cash: Cash,
                            Card: Card,
                            UPI: UPI,
                            ChequeOthers: ChequeOthers,
                        };
                        PaymentOPAdvanceCollections.push(PaymentCollectionModel);
                    }
                }
            }
        }
        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let returnReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientReturnsFilters.TypeOfReturn, Value: [1, 2, 3, 4, 5] },
            { Key: PatientReturnsFilters.PatientReturnStatus, Value: 3 },
            { Key: PatientReturnsFilters.StoreMasterId, Value: StoreMasterId },
            { Key: PatientReturnsFilters.CreatedBy, Value: UserId },
            { Key: PatientReturnsFilters.PatReturnDt, Value: [req.Data.FromDate, req.Data.ToDate] }]
        };
        PatientReturnData = await PatientReturnsBo.GetPatientReturns(returnReq);
        for (let idx in PatientReturnData.Data) {
            let PatientReturnBill = PatientReturnData.Data[idx];
            if (UserId === -1 || UserId === PatientReturnBill.CreatedBy) {
                let ReturnDate = PatientReturnBill.ReturnDateTime;
                let ReturnNumber = PatientReturnBill.ReturnNumber;
                let BillDate = null;
                let BillNumber = '';
                let DoctorName = '';
                let billedby = '';
                if (PatientReturnBill.PatientBill) {
                    BillDate = PatientReturnBill.PatientBill.BillDateTime;
                    BillNumber = PatientReturnBill.PatientBill.BillNumber;
                }
                let PatientName = '';
                let Cash = 0;
                let Card = 0;
                let UPI = 0;
                let ChequeOthers = 0;

                if (PatientReturnBill.CreatedUser && PatientReturnBill.CreatedUser.Title && PatientReturnBill.CreatedUser.Title.Description)
                    billedby += PatientReturnBill.CreatedUser.Title.Description;

                if (PatientReturnBill.CreatedUser && PatientReturnBill.CreatedUser.FirstName)
                    billedby += ' ' + PatientReturnBill.CreatedUser.FirstName;

                if (PatientReturnBill.CreatedUser && PatientReturnBill.CreatedUser.LastName)
                    billedby += ' ' + PatientReturnBill.CreatedUser.LastName;


                if (PatientReturnBill.Patient && PatientReturnBill.Patient.Title && PatientReturnBill.Patient.Title.Description)
                    PatientName += PatientReturnBill.Patient.Title.Description;

                if (PatientReturnBill.Patient && PatientReturnBill.Patient.FirstName)
                    PatientName += ' ' + PatientReturnBill.Patient.FirstName;

                if (PatientReturnBill.Patient && PatientReturnBill.Patient.LastName)
                    PatientName += ' ' + PatientReturnBill.Patient.LastName;

                if (PatientReturnBill.Patient && PatientReturnBill.Patient.MRN)
                    PatientName += ' ' + PatientReturnBill.Patient.MRN;

                if (!PatientName)
                    PatientName = PatientReturnBill.PatientName;

                if (PatientReturnBill.User && PatientReturnBill.User.Title && PatientReturnBill.User.Title.Description)
                    DoctorName += PatientReturnBill.User.Title.Description;

                if (PatientReturnBill.User && PatientReturnBill.User.FirstName)
                    DoctorName += ' ' + PatientReturnBill.User.FirstName;

                if (PatientReturnBill.User && PatientReturnBill.User.LastName)
                    DoctorName += ' ' + PatientReturnBill.User.LastName;

                var refnumber = '';
                if (PatientReturnBill.PatientRefunds && PatientReturnBill.PatientRefunds.length > 0) {
                    for (var rfdx in PatientReturnBill.PatientRefunds) {
                        var refundDetail = PatientReturnBill.PatientRefunds[rfdx];
                        refnumber = refundDetail.RefundNumber;
                        if (UserId === -1 || UserId === refundDetail.CreatedBy) {
                            if (refundDetail.PaymentTypeId === 1 && refundDetail.RefundStatusId === 1) {
                                Cash = refundDetail.RefundAmount;
                                TotCashReturn += refundDetail.RefundAmount;
                                TotReturn += refundDetail.RefundAmount;
                            } else if ((refundDetail.PaymentTypeId === 5 ||
                                refundDetail.PaymentTypeId === 6) && refundDetail.RefundStatusId === 1) {
                                Card = refundDetail.RefundAmount;
                                TotCardReturn += refundDetail.RefundAmount;
                                TotReturn += refundDetail.RefundAmount;
                            } else if ((refundDetail.PaymentTypeId === 11) && refundDetail.RefundStatusId === 1) {
                                UPI = refundDetail.RefundAmount;
                                TotUPIReturn += refundDetail.RefundAmount;
                                TotReturn += refundDetail.RefundAmount;
                            } else if ((refundDetail.PaymentTypeId !== 1 || refundDetail.PaymentTypeId !== 5 ||
                                refundDetail.PaymentTypeId !== 6 || refundDetail.PaymentTypeId !== 11)
                                && refundDetail.RefundStatusId === 1) {
                                ChequeOthers = refundDetail.RefundAmount;
                                TotChequeOthersReturn += refundDetail.RefundAmount;
                                TotReturn += refundDetail.RefundAmount;
                            }
                        }
                    }
                    // Cash = PatientReturnBill.ReturnAmount;
                    // $scope.TotCashReturn += PatientReturnBill.ReturnAmount;
                    // $scope.TotReturn += PatientReturnBill.ReturnAmount;

                    if (refundDetail.PaymentTypeId === 1) { // CASH
                        Card = 0;
                        UPI = 0;
                        ChequeOthers = 0;
                    } else if (refundDetail.PaymentTypeId === 5 || refundDetail.PaymentTypeId === 6) { //CARD
                        Cash = 0;
                        UPI = 0;
                        ChequeOthers = 0;
                    } else if (refundDetail.PaymentTypeId === 11) { //UPI
                        Cash = 0;
                        ChequeOthers = 0;
                        Card = 0;
                    } else if (refundDetail.PaymentTypeId !== 1 || refundDetail.PaymentTypeId !== 5 ||
                        refundDetail.PaymentTypeId !== 6 || refundDetail.PaymentTypeId !== 11) { // CHEQUE
                        Cash = 0;
                        ChequeOthers = 0;
                        UPI = 0;
                    }
                }

                if (Cash > 0 || Card > 0 || UPI > 0 || ChequeOthers > 0) {
                    let PaymentCollectionModel = {
                        BillDate: BillDate,
                        ReturnDate: ReturnDate,
                        ReturnNumber: ReturnNumber,
                        BillNumber: BillNumber,
                        DoctorName: DoctorName,
                        Billedby: billedby,
                        PatientName: PatientName,
                        Cash: Cash,
                        Card: Card,
                        UPI: UPI,
                        ChequeOthers: ChequeOthers,
                    };
                    PaymentOPReturn.push(PaymentCollectionModel);
                }
            }
        }
        let TotalNetCash = (TotCash + TotCashAdvance + TotCashDue) - TotCashReturn;
        let TotalNetCard = (TotCard + TotCardAdvance + TotCardDue) - TotCardReturn;
        let TotalNetUPI = (TotUPI + TotUPIAdvance + TotUPIDue) - TotCardReturn;
        let TotalNetCheque = (TotChequeOthers + TotChequeOthersAdvance + TotChequeOthersDue) - TotChequeOthersReturn;
        let NetSubmission = (TotSales + TotAdvances + TotDues) - TotReturn;

        let info = {
            PatientBillData: PatientBillData,
            PaymentOPSales: PaymentOPSales,
            PaymentOPDueCollections: PaymentOPDueCollections,
            PaymentOPAdvanceCollections: PaymentOPAdvanceCollections,
            TotCash: TotCash,
            TotCard: TotCard,
            TotChequeOthers: TotChequeOthers,
            TotUPI: TotUPI,
            FromDate: FromDate,
            ToDate: ToDate,
            PaymentType: PaymentType,
            TotSales: TotSales,
            TotCashDue: TotCashDue,
            TotCardDue: TotCardDue,
            TotChequeOthersDue: TotChequeOthersDue,
            TotUPIDue: TotUPIDue,
            TotDues: TotDues,
            TotCashAdvance: TotCashAdvance,
            TotCardAdvance: TotCardAdvance,
            TotChequeOthersAdvance: TotChequeOthersAdvance,
            TotUPIAdvance: TotUPIAdvance,
            TotAdvances: TotAdvances,
            TotCashReturn: TotCashReturn,
            TotCardReturn: TotCardReturn,
            TotChequeOthersReturn: TotChequeOthersReturn,
            TotUPIReturn: TotUPIReturn,
            TotReturn: TotReturn,
            PaymentOPReturn: PaymentOPReturn,
            TotalNetCash: TotalNetCash,
            TotalNetCard: TotalNetCard,
            TotalNetCheque: TotalNetCheque,
            TotalNetUPI: TotalNetUPI,
            NetSubmission: NetSubmission,
            Preferences: printPreferencesData,
            StorePreferences: printStoreData,
            UserName: UserName,
            StoreMaster: StoreMaster
        };
        let pdfOption: any = null;
        let key = 'pharmacycollectionreport';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }

    public async PrintPharmacyCollectionAllCashier(req: BaseRequest): Promise<any> {
        let dateformat = 'DD/MM/YYYY';
        let timeformat = 'HH:mm:ss';
        let fromdate = moment(req.Data.FromDate).format(dateformat);
        let FromTime = moment(req.Data.FromDate).format(timeformat);
        let FromDate = fromdate + ' ' + FromTime;

        let todate = moment(req.Data.ToDate).format(dateformat);
        let totime = moment(req.Data.ToDate).format(timeformat);
        let ToDate = todate + ' ' + totime;
        let PaymentType = req.Data.PaymentType;
        let FacilityId = req.Data.FacilityId;
        let UserId = req.Data.UserId;
        let UserName = req.Data.UserName;
        let StoreMaster = req.Data.StoreMaster;
        let StoreMasterId = req.Data.StoreMasterId;

        let PatientBillData: any = [];
        let PaymentOPSales: any = [];
        let PatientPaymentDetailsData: any = [];
        let PatientReturnData: any = [];
        let PaymentOPDueCollections: any = [];
        let PaymentOPAdvanceCollections: any = [];
        let PaymentOPReturn: any = [];
        let TotCash: number = 0;
        let TotCard: number = 0;
        let TotChequeOthers: number = 0;
        let TotUPI: number = 0;
        let TotSales: number = 0;
        let TotCashDue: number = 0;
        let TotCardDue: number = 0;
        let TotChequeOthersDue: number = 0;
        let TotUPIDue: number = 0;
        let TotDues: number = 0;
        let TotCashAdvance: number = 0;
        let TotCardAdvance: number = 0;
        let TotChequeOthersAdvance: number = 0;
        let TotUPIAdvance: number = 0;
        let TotAdvances: number = 0;
        let TotCashReturn: number = 0;
        let TotCardReturn: number = 0;
        let TotChequeOthersReturn: number = 0;
        let TotUPIReturn: number = 0;
        let TotReturn: number = 0;

        let billReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientBillsFilters.RecFromDate, Value: req.Data.FromDate },
            { Key: PatientBillsFilters.RecToDate, Value: req.Data.ToDate },
            { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
            { Key: PatientBillsFilters.BillType, Value: [4, 6] },
            { Key: PatientBillsFilters.IsPharmacyBill, Value: true },
            { Key: PatientBillsFilters.CreatedBy, Value: UserId },
            { Key: PatientBillsFilters.StoreMasterId, Value: StoreMasterId },]
        };
        PatientBillData = await this.GetPharmacyPatientBills(billReq);
        // let billdata = PatientBillData.Data[0];
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(FacilityId);
        let printStoreData =
            await storePreferenceBO.GetStorePreferenceWithLogo(FacilityId, StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.printheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;
        for (let idx in PatientBillData.Data) {
            let PatientBill = PatientBillData.Data[idx];
            let BillDate = PatientBill.BillDateTime;
            let BillNumber = PatientBill.BillNumber;
            let ReceiptNumber = '';
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let UPI = 0;
            let ChequeOthers = 0;
            let billedby = '';

            if (PatientBill.User && PatientBill.User.Title && PatientBill.User.Title.Description)
                DoctorName += PatientBill.User.Title.Description;

            if (PatientBill.User && PatientBill.User.FirstName)
                DoctorName += ' ' + PatientBill.User.FirstName;

            if (PatientBill.User && PatientBill.User.LastName)
                DoctorName += ' ' + PatientBill.User.LastName;

            if (PatientBill.Patient && PatientBill.Patient.Title && PatientBill.Patient.Title.Description)
                PatientName += PatientBill.Patient.Title.Description;

            if (PatientBill.Patient && PatientBill.Patient.FirstName)
                PatientName += ' ' + PatientBill.Patient.FirstName;

            if (PatientBill.Patient && PatientBill.Patient.LastName)
                PatientName += ' ' + PatientBill.Patient.LastName;

            if (PatientBill.Patient && PatientBill.Patient.MRN)
                PatientName += ' ' + PatientBill.Patient.MRN;

            if (!PatientName)
                PatientName = PatientBill.PatientName;

            for (var payidx in PatientBill.PatientPaymentDetails) {
                var PatientPaymentDetail = PatientBill.PatientPaymentDetails[payidx];
                ReceiptNumber = PatientPaymentDetail.ReceiptNumber;

                if (PatientPaymentDetail.CreatedUser && PatientPaymentDetail.CreatedUser.Title
                    && PatientPaymentDetail.CreatedUser.Title.Description)
                    billedby += PatientPaymentDetail.CreatedUser.Title.Description;

                if (PatientPaymentDetail.CreatedUser && PatientPaymentDetail.CreatedUser.FirstName)
                    billedby += ' ' + PatientPaymentDetail.CreatedUser.FirstName;

                if (PatientPaymentDetail.CreatedUser && PatientPaymentDetail.CreatedUser.LastName)
                    billedby += ' ' + PatientPaymentDetail.CreatedUser.LastName;


                if (PatientPaymentDetail.ReceiptTypeId === 2) {
                    if (PatientPaymentDetail.PaymentTypeId === 1 && PatientPaymentDetail.ReceiptStatusId === 1) {
                        Cash = PatientPaymentDetail.AmountPaid;
                        TotCash += PatientPaymentDetail.AmountPaid;
                        TotSales += PatientPaymentDetail.AmountPaid;
                    } else if ((PatientPaymentDetail.PaymentTypeId === 5 || PatientPaymentDetail.PaymentTypeId === 6) &&
                        PatientPaymentDetail.ReceiptStatusId === 1) {
                        Card = PatientPaymentDetail.AmountPaid;
                        TotCard += PatientPaymentDetail.AmountPaid;
                        TotSales += PatientPaymentDetail.AmountPaid;
                    } else if ((PatientPaymentDetail.PaymentTypeId === 11) &&
                        PatientPaymentDetail.ReceiptStatusId === 1) {
                        UPI = PatientPaymentDetail.AmountPaid;
                        TotUPI += PatientPaymentDetail.AmountPaid;
                        TotSales += PatientPaymentDetail.AmountPaid;
                    } else if ((PatientPaymentDetail.PaymentTypeId !== 1 || PatientPaymentDetail.PaymentTypeId !== 5
                        || PatientPaymentDetail.PaymentTypeId !== 6 || PatientPaymentDetail.PaymentTypeId !== 11)
                        && PatientPaymentDetail.ReceiptStatusId === 1) {
                        ChequeOthers = PatientPaymentDetail.AmountPaid;
                        TotChequeOthers += PatientPaymentDetail.AmountPaid;
                        TotSales += PatientPaymentDetail.AmountPaid;
                    }
                    if (PatientPaymentDetail.PaymentTypeId === 1) { // CASH
                        Card = 0;
                        ChequeOthers = 0;
                        UPI = 0;
                    } else if (PatientPaymentDetail.PaymentTypeId === 5 || PatientPaymentDetail.PaymentTypeId === 6) { //CARD
                        Cash = 0;
                        ChequeOthers = 0;
                        UPI = 0;
                    } else if (PatientPaymentDetail.PaymentTypeId === 11) { //UPI
                        Cash = 0;
                        ChequeOthers = 0;
                        Card = 0;
                    } else if (PatientPaymentDetail.PaymentTypeId !== 1 || PatientPaymentDetail.PaymentTypeId !== 5
                        || PatientPaymentDetail.PaymentTypeId !== 6 || PatientPaymentDetail.PaymentTypeId !== 11) { // CHEQUE
                        Cash = 0;
                        Card = 0;
                        UPI = 0;
                    }

                    if (Cash > 0 || Card > 0 || UPI > 0 || ChequeOthers > 0) {
                        //$scope.TotSales += (Cash + Card + ChequeOthers);
                        let PaymentCollectionModel = {
                            BillDate: BillDate,
                            BillNumber: BillNumber,
                            ReceiptNumber: ReceiptNumber,
                            DoctorName: DoctorName,
                            Billedby: billedby,
                            PatientName: PatientName,
                            Cash: Cash,
                            Card: Card,
                            UPI: UPI,
                            ChequeOthers: ChequeOthers,

                        };
                        PaymentOPSales.push(PaymentCollectionModel);
                    }
                }
            }
        }
        let PatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let DueReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.ReceiptType, Value: 3 },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 },
            { Key: PatientPaymentDetailsFilters.BillType, Value: [4, 6] },
            { Key: PatientPaymentDetailsFilters.ReceiptDatetime, Value: [req.Data.FromDate, req.Data.ToDate] },
            { Key: PatientPaymentDetailsFilters.StoreMasterId, Value: StoreMasterId },
            { Key: PatientPaymentDetailsFilters.CreatedBy, Value: UserId }]
        };
        PatientPaymentDetailsData = await PatientPaymentDetailsBo.GetPatientPaymentDetails(DueReq);

        for (let idx in PatientPaymentDetailsData.Data) {
            let PatientDue = PatientPaymentDetailsData.Data[idx];
            let BillNumber = '';
            let ReceiptDate = PatientDue.ReceiptDateTime;
            let ReceiptNumber = PatientDue.ReceiptNumber;
            if (PatientDue.PatientBill) {
                BillNumber = PatientDue.PatientBill.BillNumber;
            }
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let UPI = 0;
            let ChequeOthers = 0;
            let billedby = '';


            if (PatientDue.CreatedUser && PatientDue.CreatedUser.Title && PatientDue.CreatedUser.Title.Description)
                billedby += PatientDue.CreatedUser.Title.Description;

            if (PatientDue.CreatedUser && PatientDue.CreatedUser.FirstName)
                billedby += ' ' + PatientDue.CreatedUser.FirstName;

            if (PatientDue.CreatedUser && PatientDue.CreatedUser.LastName)
                billedby += ' ' + PatientDue.CreatedUser.LastName;


            if (PatientDue.User && PatientDue.User.Title && PatientDue.User.Title.Description)
                DoctorName += PatientDue.User.Title.Description;

            if (PatientDue.User && PatientDue.User.FirstName)
                DoctorName += ' ' + PatientDue.User.FirstName;

            if (PatientDue.User && PatientDue.User.LastName)
                DoctorName += ' ' + PatientDue.User.LastName;

            if (PatientDue.Patient && PatientDue.Patient.Title && PatientDue.Patient.Title.Description)
                PatientName += PatientDue.Patient.Title.Description;

            if (PatientDue.Patient && PatientDue.Patient.FirstName)
                PatientName += ' ' + PatientDue.Patient.FirstName;

            if (PatientDue.Patient && PatientDue.Patient.LastName)
                PatientName += ' ' + PatientDue.Patient.LastName;

            if (PatientDue.Patient && PatientDue.Patient.MRN)
                PatientName += ' ' + PatientDue.Patient.MRN;

            if (!PatientName)
                PatientName = PatientDue.PatientName;

            if (PatientDue.ReceiptTypeId === 3 && PatientDue.PatientBill) {
                if (PatientDue.PaymentTypeId === 1 && PatientDue.ReceiptStatusId === 1) {
                    Cash = PatientDue.AmountPaid;
                    TotCashDue += PatientDue.AmountPaid;
                    TotDues += PatientDue.AmountPaid;
                } else if (PatientDue.PaymentTypeId === 5 || PatientDue.PaymentTypeId === 6
                    && PatientDue.ReceiptStatusId === 1) {
                    Card = PatientDue.AmountPaid;
                    TotCardDue += PatientDue.AmountPaid;
                    TotDues += PatientDue.AmountPaid;
                } else if (PatientDue.PaymentTypeId === 11
                    && PatientDue.ReceiptStatusId === 1) {
                    UPI = PatientDue.AmountPaid;
                    TotUPIDue += PatientDue.AmountPaid;
                    TotDues += PatientDue.AmountPaid;
                } else if (PatientDue.PaymentTypeId !== 1 || PatientDue.PaymentTypeId !== 5
                    || PatientDue.PaymentTypeId !== 6 || PatientDue.PaymentTypeId !== 11 && PatientDue.ReceiptStatusId === 1) {
                    ChequeOthers = PatientDue.AmountPaid;
                    TotChequeOthersDue += PatientDue.AmountPaid;
                    TotDues += PatientDue.AmountPaid;
                }
                if (PatientDue.PaymentTypeId === 1) { // CASH
                    Card = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (PatientDue.PaymentTypeId === 5
                    || PatientDue.PaymentTypeId === 6) { //CARD
                    Cash = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (PatientDue.PaymentTypeId === 11) { //UPI
                    Cash = 0;
                    ChequeOthers = 0;
                    Card = 0;
                } else if (PatientDue.PaymentTypeId !== 1 || PatientDue.PaymentTypeId !== 5
                    || PatientDue.PaymentTypeId !== 6 || PatientDue.PaymentTypeId !== 11) { // CHEQUE
                    Cash = 0;
                    Card = 0;
                    UPI = 0;
                }
                if (Cash > 0 || Card > 0 || UPI > 0 || ChequeOthers > 0) {
                    //$scope.TotDues += (Cash + Card + ChequeOthers);
                    let PaymentCollectionModel = {
                        ReceiptDate: ReceiptDate,
                        ReceiptNumber: ReceiptNumber,
                        BillNumber: BillNumber,
                        DoctorName: DoctorName,
                        Billedby: billedby,
                        PatientName: PatientName,
                        Cash: Cash,
                        Card: Card,
                        UPI: UPI,
                        ChequeOthers: ChequeOthers,
                    };
                    PaymentOPDueCollections.push(PaymentCollectionModel);
                }

            }
        }
        let AdvPatientPaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        let AdvanceReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.ReceiptType, Value: 7 },
            { Key: PatientPaymentDetailsFilters.ReceiptStatus, Value: 1 },
            // { Key: PatientPaymentDetailsFilters.BillType, Value: 4 },
            { Key: PatientPaymentDetailsFilters.CreatedBy, Value: UserId },
            // { Key: PatientPaymentDetailsFilters.StoreMasterId, Value: StoreMasterId },
            { Key: PatientPaymentDetailsFilters.ReceiptDatetime, Value: [req.Data.FromDate, req.Data.ToDate] }]
        };
        PatientPaymentDetailsData = await AdvPatientPaymentDetailsBo.GetPatientPaymentDetails(AdvanceReq);

        for (let idx in PatientPaymentDetailsData.Data) {
            let PatientAdvance = PatientPaymentDetailsData.Data[idx];
            let BillNumber = '';
            let ReceiptDate = PatientAdvance.ReceiptDateTime;
            let ReceiptNumber = PatientAdvance.ReceiptNumber;
            if (PatientAdvance.PatientBill) {
                BillNumber = PatientAdvance.PatientBill.BillNumber;
            }
            let DoctorName = '';
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let UPI = 0;
            let ChequeOthers = 0;
            let billedby = '';


            if (PatientAdvance.CreatedUser && PatientAdvance.CreatedUser.Title && PatientAdvance.CreatedUser.Title.Description)
                billedby += PatientAdvance.CreatedUser.Title.Description;

            if (PatientAdvance.CreatedUser && PatientAdvance.CreatedUser.FirstName)
                billedby += ' ' + PatientAdvance.CreatedUser.FirstName;

            if (PatientAdvance.CreatedUser && PatientAdvance.CreatedUser.LastName)
                billedby += ' ' + PatientAdvance.CreatedUser.LastName;


            if (PatientAdvance.User && PatientAdvance.User.Title && PatientAdvance.User.Title.Description)
                DoctorName += PatientAdvance.User.Title.Description;

            if (PatientAdvance.User && PatientAdvance.User.FirstName)
                DoctorName += ' ' + PatientAdvance.User.FirstName;

            if (PatientAdvance.User && PatientAdvance.User.LastName)
                DoctorName += ' ' + PatientAdvance.User.LastName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.Title && PatientAdvance.Patient.Title.Description)
                PatientName += PatientAdvance.Patient.Title.Description;

            if (PatientAdvance.Patient && PatientAdvance.Patient.FirstName)
                PatientName += ' ' + PatientAdvance.Patient.FirstName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.LastName)
                PatientName += ' ' + PatientAdvance.Patient.LastName;

            if (PatientAdvance.Patient && PatientAdvance.Patient.MRN)
                PatientName += ' ' + PatientAdvance.Patient.MRN;

            if (!PatientName)
                PatientName = PatientAdvance.PatientName;

            if (PatientAdvance.ReceiptTypeId === 7) {
                if (UserId === -1 || UserId === PatientAdvance.CreatedBy) {
                    if (PatientAdvance.PaymentTypeId === 1 && PatientAdvance.ReceiptStatusId === 1) {
                        Cash = PatientAdvance.AmountPaid;
                        TotCashAdvance += PatientAdvance.AmountPaid;
                        TotAdvances += PatientAdvance.AmountPaid;
                    } else if (PatientAdvance.PaymentTypeId === 5 || PatientAdvance.PaymentTypeId === 6
                        && PatientAdvance.ReceiptStatusId === 1) {
                        Card = PatientAdvance.AmountPaid;
                        TotCardAdvance += PatientAdvance.AmountPaid;
                        TotAdvances += PatientAdvance.AmountPaid;
                    } else if (PatientAdvance.PaymentTypeId === 11
                        && PatientAdvance.ReceiptStatusId === 1) {
                        UPI = PatientAdvance.AmountPaid;
                        TotUPIAdvance += PatientAdvance.AmountPaid;
                        TotAdvances += PatientAdvance.AmountPaid;
                    } else if (PatientAdvance.PaymentTypeId !== 1 || PatientAdvance.PaymentTypeId !== 5 ||
                        PatientAdvance.PaymentTypeId !== 6 ||
                        PatientAdvance.PaymentTypeId !== 11 && PatientAdvance.ReceiptStatusId === 1) {
                        ChequeOthers = PatientAdvance.AmountPaid;
                        TotChequeOthersAdvance += PatientAdvance.AmountPaid;
                        TotAdvances += PatientAdvance.AmountPaid;
                    }
                    if (PatientAdvance.PaymentTypeId === 1) { // CASH
                        Card = 0;
                        ChequeOthers = 0;
                        UPI = 0;
                    } else if (PatientAdvance.PaymentTypeId === 5
                        || PatientAdvance.PaymentTypeId === 6) { //CARD
                        Cash = 0;
                        ChequeOthers = 0;
                        UPI = 0;
                    } else if (PatientAdvance.PaymentTypeId === 11) { //UPI
                        Cash = 0;
                        ChequeOthers = 0;
                        Card = 0;
                    } else if (PatientAdvance.PaymentTypeId !== 1 || PatientAdvance.PaymentTypeId !== 5 ||
                        PatientAdvance.PaymentTypeId !== 6) { // CHEQUE
                        Cash = 0;
                        Card = 0;
                        UPI = 0;
                    }
                    if (Cash > 0 || Card > 0 || UPI > 0 || ChequeOthers > 0) {
                        //$scope.TotAdvances += (Cash + Card + ChequeOthers);
                        let PaymentCollectionModel = {
                            ReceiptDate: ReceiptDate,
                            ReceiptNumber: ReceiptNumber,
                            BillNumber: BillNumber,
                            DoctorName: DoctorName,
                            Billedby: billedby,
                            PatientName: PatientName,
                            Cash: Cash,
                            Card: Card,
                            UPI: UPI,
                            ChequeOthers: ChequeOthers,
                        };
                        PaymentOPAdvanceCollections.push(PaymentCollectionModel);
                    }
                }
            }
        }
        let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let returnReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientReturnsFilters.TypeOfReturn, Value: [1, 2, 3, 4, 5] },
            { Key: PatientReturnsFilters.PatientReturnStatus, Value: 3 },
            { Key: PatientReturnsFilters.PatReturnDt, Value: [req.Data.FromDate, req.Data.ToDate] },
            { Key: PatientReturnsFilters.CreatedBy, Value: UserId },
            { Key: PatientReturnsFilters.StoreMasterId, Value: StoreMasterId },]
        };
        PatientReturnData = await PatientReturnsBo.GetPatientReturns(returnReq);
        for (let idx in PatientReturnData.Data) {
            let PatientReturnBill = PatientReturnData.Data[idx];
            let ReturnDate = PatientReturnBill.ReturnDateTime;
            let ReturnNumber = PatientReturnBill.ReturnNumber;
            let BillDate = null;
            let BillNumber = '';
            let DoctorName = '';
            let billedby = '';
            if (PatientReturnBill.PatientBill) {
                BillDate = PatientReturnBill.PatientBill.BillDateTime;
                BillNumber = PatientReturnBill.PatientBill.BillNumber;
            }
            let PatientName = '';
            let Cash = 0;
            let Card = 0;
            let UPI = 0;
            let ChequeOthers = 0;

            if (PatientReturnBill.CreatedUser && PatientReturnBill.CreatedUser.Title && PatientReturnBill.CreatedUser.Title.Description)
                billedby += PatientReturnBill.CreatedUser.Title.Description;

            if (PatientReturnBill.CreatedUser && PatientReturnBill.CreatedUser.FirstName)
                billedby += ' ' + PatientReturnBill.CreatedUser.FirstName;

            if (PatientReturnBill.CreatedUser && PatientReturnBill.CreatedUser.LastName)
                billedby += ' ' + PatientReturnBill.CreatedUser.LastName;


            if (PatientReturnBill.Patient && PatientReturnBill.Patient.Title && PatientReturnBill.Patient.Title.Description)
                PatientName += PatientReturnBill.Patient.Title.Description;

            if (PatientReturnBill.Patient && PatientReturnBill.Patient.FirstName)
                PatientName += ' ' + PatientReturnBill.Patient.FirstName;

            if (PatientReturnBill.Patient && PatientReturnBill.Patient.LastName)
                PatientName += ' ' + PatientReturnBill.Patient.LastName;

            if (PatientReturnBill.Patient && PatientReturnBill.Patient.MRN)
                PatientName += ' ' + PatientReturnBill.Patient.MRN;

            if (!PatientName)
                PatientName = PatientReturnBill.PatientName;

            if (PatientReturnBill.User && PatientReturnBill.User.Title && PatientReturnBill.User.Title.Description)
                DoctorName += PatientReturnBill.User.Title.Description;

            if (PatientReturnBill.User && PatientReturnBill.User.FirstName)
                DoctorName += ' ' + PatientReturnBill.User.FirstName;

            if (PatientReturnBill.User && PatientReturnBill.User.LastName)
                DoctorName += ' ' + PatientReturnBill.User.LastName;

            let refnumber = '';
            if (PatientReturnBill.PatientRefunds && PatientReturnBill.PatientRefunds.length > 0) {
                for (let rfdx in PatientReturnBill.PatientRefunds) {
                    var refundDetail = PatientReturnBill.PatientRefunds[rfdx];
                    refnumber = refundDetail.RefundNumber;
                    if (UserId === -1 || UserId === refundDetail.CreatedBy) {
                        if (refundDetail.PaymentTypeId === 1 && refundDetail.RefundStatusId === 1) {
                            Cash = refundDetail.RefundAmount;
                            TotCashReturn += refundDetail.RefundAmount;
                            TotReturn += refundDetail.RefundAmount;
                        } else if ((refundDetail.PaymentTypeId === 5 ||
                            refundDetail.PaymentTypeId === 6) && refundDetail.RefundStatusId === 1) {
                            Card = refundDetail.RefundAmount;
                            TotCardReturn += refundDetail.RefundAmount;
                            TotReturn += refundDetail.RefundAmount;
                        } else if ((refundDetail.PaymentTypeId === 11) && refundDetail.RefundStatusId === 1) {
                            UPI = refundDetail.RefundAmount;
                            TotUPIReturn += refundDetail.RefundAmount;
                            TotReturn += refundDetail.RefundAmount;
                        } else if ((refundDetail.PaymentTypeId !== 1 || refundDetail.PaymentTypeId !== 5 ||
                            refundDetail.PaymentTypeId !== 6 ||
                            refundDetail.PaymentTypeId !== 11) && refundDetail.RefundStatusId === 1) {
                            ChequeOthers = refundDetail.RefundAmount;
                            TotChequeOthersReturn += refundDetail.RefundAmount;
                            TotReturn += refundDetail.RefundAmount;
                        }
                    }
                }
                // Cash = PatientReturnBill.ReturnAmount;
                // $scope.TotCashReturn += PatientReturnBill.ReturnAmount;
                // $scope.TotReturn += PatientReturnBill.ReturnAmount;

                if (refundDetail.PaymentTypeId === 1) { // CASH
                    Card = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (refundDetail.PaymentTypeId === 5 || refundDetail.PaymentTypeId === 6) { //CARD
                    Cash = 0;
                    ChequeOthers = 0;
                    UPI = 0;
                } else if (refundDetail.PaymentTypeId === 11) { //UPI
                    Cash = 0;
                    ChequeOthers = 0;
                    Card = 0;
                } else if (refundDetail.PaymentTypeId !== 1 || refundDetail.PaymentTypeId !== 5 ||
                    refundDetail.PaymentTypeId !== 6 || refundDetail.PaymentTypeId !== 6) { // CHEQUE
                    Cash = 0;
                    Card = 0;
                    UPI = 0;
                }
            }
            if (Cash > 0 || Card > 0 || UPI > 0 || ChequeOthers > 0) {
                let PaymentCollectionModel = {
                    BillDate: BillDate,
                    ReturnDate: ReturnDate,
                    ReturnNumber: ReturnNumber,
                    BillNumber: BillNumber,
                    DoctorName: DoctorName,
                    Billedby: billedby,
                    PatientName: PatientName,
                    Cash: Cash,
                    Card: Card,
                    UPI: UPI,
                    ChequeOthers: ChequeOthers,
                };
                PaymentOPReturn.push(PaymentCollectionModel);
            }
        }
        let TotalNetCash = (TotCash + TotCashDue) - TotCashReturn;
        let TotalNetCard = (TotCard + TotCardDue) - TotCardReturn;
        let TotalNetUPI = (TotUPI + TotUPIDue) - TotUPIReturn;
        let TotalNetCheque = (TotChequeOthers + TotChequeOthersDue) - TotChequeOthersReturn;
        let NetSubmission = (TotSales + TotDues) - TotReturn;

        let info = {
            PatientBillData: PatientBillData,
            PaymentOPSales: PaymentOPSales,
            PaymentOPDueCollections: PaymentOPDueCollections,
            PaymentOPAdvanceCollections: PaymentOPAdvanceCollections,
            TotCash: TotCash,
            TotCard: TotCard,
            TotUPI: TotUPI,
            TotChequeOthers: TotChequeOthers,
            FromDate: FromDate,
            ToDate: ToDate,
            PaymentType: PaymentType,
            TotSales: TotSales,
            TotCashDue: TotCashDue,
            TotCardDue: TotCardDue,
            TotUPIDue: TotUPIDue,
            TotChequeOthersDue: TotChequeOthersDue,
            TotCashAdvance: TotCashAdvance,
            TotCardAdvance: TotCardAdvance,
            TotUPIAdvance: TotUPIAdvance,
            TotChequeOthersAdvance: TotChequeOthersAdvance,
            TotAdvances: TotAdvances,
            TotDues: TotDues,
            TotCashReturn: TotCashReturn,
            TotCardReturn: TotCardReturn,
            TotUPIReturn: TotUPIReturn,
            TotChequeOthersReturn: TotChequeOthersReturn,
            TotReturn: TotReturn,
            PaymentOPReturn: PaymentOPReturn,
            TotalNetCash: TotalNetCash,
            TotalNetCard: TotalNetCard,
            TotalNetUPI: TotalNetUPI,
            TotalNetCheque: TotalNetCheque,
            NetSubmission: NetSubmission,
            Preferences: printPreferencesData,
            StorePreferences: printStoreData,
            UserName: UserName,
            StoreMaster: StoreMaster
        };
        let key = 'pharmacycollectionallcashier';
        let pdfOption: any = null;
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'landscape',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintConsolidatedPharmacybilldetails(req: BaseRequest): Promise<FileInfo> {
        let BillDetails = [];
        let ReturnDetails = [];
        let EncounterDetails: any = {};
        let PatientDetails: any = {};
        let BillData: any = {};
        let printPreferencesData: any = {};
        let TotBillAmount: number = 0;
        let BillDisc: number = 0;
        let PaidAmount: number = 0;
        let RefAmount: number = 0;
        let NetAmount: number = 0;
        let DueAmount: number = 0;
        if (req && req.Data) {
            let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);

            for (let j = 0, len = req.Data.length; j < len; j++) {
                let BillId = req.Data[j];

                let billreq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillsFilters.Id, Value: BillId }]
                };
                let BillInfo: any = await this.GetPatientBills(billreq);
                BillData = BillInfo.Data[0];
                TotBillAmount += BillData.BillAmount;
                BillDisc += BillData.BillDiscount;
                PaidAmount += BillData.PaidAmount;
                DueAmount += BillData.OutStandingAmount;
                RefAmount += BillData.RefundAmount;
                NetAmount += (BillData.BillAmount) - (BillData.BillDiscount);
                let billdetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: BillId }]
                };
                let patientBillDetailsinfo: any = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);

                // let PatientReturnReq = {
                //     Id: 0,
                //     PageContext: { PageSize: -1, PageNumber: 1 },
                //     Params: [{ Key: PatientReturnsFilters.PatientBillId, Value: BillId }]
                // };
                // let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
                // let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);

                let retDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientReturnDetailsFilters.PatientBillId, Value: BillId }]
                };
                let PatientRetDetailBo = BoFactory.GetBo(bo.PatientReturnDetailsBo, this.Request);
                let PatientReturnDetailsData: any = await PatientRetDetailBo.GetPatientReturnDetails(retDetailReq);

                for (let k = 0, pbdlen = PatientReturnDetailsData.Data.length; k < pbdlen; k++) {
                    ReturnDetails.push(PatientReturnDetailsData.Data[k]);
                }

                let encReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: EncounterFilters.Id, Value: patientBillDetailsinfo.Data[0].EncounterId }]
                };
                let encounterData = await encounterBo.GetEncounters(encReq);
                for (let k = 0, pbdlen = patientBillDetailsinfo.Data.length; k < pbdlen; k++) {
                    BillDetails.push(patientBillDetailsinfo.Data[k]);
                }
                EncounterDetails = encounterData.Data[0];

                let patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
                let patReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientFilters.Id, Value: BillData.PatientId }]
                };
                let PatData = await patientBO.GetPatients(patReq);
                PatientDetails = PatData.Data[0];

            }
            let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
            printPreferencesData =
                await facilityPreferenceBO.GetFacilityPreferenceWithLogo(EncounterDetails.FacilityId);
        }

        let info = {
            BillDetails: BillDetails,
            ReturnDetails: ReturnDetails,
            BillData: BillData,
            EncounterDetails: EncounterDetails,
            PatientDetails: PatientDetails,
            Preferences: printPreferencesData,
            TotBillAmount: TotBillAmount,
            BillDisc: BillDisc,
            PaidAmount: PaidAmount,
            RefAmount: RefAmount,
            NetAmount: NetAmount,
            DueAmount: DueAmount,
            Amount: NetAmount - RefAmount
        };

        let pdfOption: any = null;
        let key = 'pharmacyconsolidatebilldetails';
        if (req.Data) {
            key = 'pharmacyconsolidatebilldetails';
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'landscape',
                    border: '0',
                    header: {
                        height: '1.5in',
                        contents: '',
                    },
                    footer: {
                        height: '0.5in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintConsolidatedAllPharmacybilldetails(req: any): Promise<FileInfo> {
        let BillDetails = [];
        let ReturnDetails = [];
        let AllDetails = [];
        let EncounterDetails: any = {};
        let PatientDetails: any = {};
        let BillData: any = {};
        let printPreferencesData: any = {};
        let TotBillAmount: number = 0;
        let BillDisc: number = 0;
        let PaidAmount: number = 0;
        let RefAmount: number = 0;
        let NetAmount: number = 0;
        let DueAmount: number = 0;
        // let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);
        let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        let patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
        let patReq = {
            Id: 0,
            PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientFilters.Id, Value: req.Id }]
        };
        let PatData = await patientBO.GetMinPatientSearch(patReq);
        if (PatData.Data.length > 0) {
            PatientDetails = PatData.Data[0];
        }
        console.log('*********************');
        console.log(PatData);
        console.log('*****************');
        if (req && req.Data) {
            for (let j = 0, len = req.Data.length; j < len; j++) {
                let BillId = req.Data[j];

                let billreq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillsFilters.Id, Value: BillId }]
                };
                // let BillInfo: any = await this.GetPatientBills(billreq);
                let BillInfo: any = await this.GetMinPatientBills(billreq);


                BillData = BillInfo.Data[0];
                TotBillAmount += BillData.BillAmount;
                BillDisc += BillData.BillDiscount;
                PaidAmount += BillData.PaidAmount;
                DueAmount += BillData.OutStandingAmount;
                RefAmount += BillData.RefundAmount;
                NetAmount += (BillData.BillAmount) - (BillData.BillDiscount);
                let billdetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: BillId },
                    { Key: PatientBillDetailsFilters.IsPharmacySale, Value: true }]
                };
                let patientBillDetailsinfo: any = await PatientBillDetailsBo.GetMinPatientBillDetails(billdetailReq);

                // let PatientReturnReq = {
                //     Id: 0,
                //     PageContext: { PageSize: -1, PageNumber: 1 },
                //     Params: [{ Key: PatientReturnsFilters.PatientBillId, Value: BillId }]
                // };
                // let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
                // let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);


                // let encReq = {
                //     Id: 0,
                //     PageContext: { PageSize: -1, PageNumber: 1 },
                //     Params: [{ Key: EncounterFilters.Id, Value: patientBillDetailsinfo.Data[0].EncounterId }]
                // };
                // let encounterData = await encounterBo.GetEncounters(encReq);
                for (let k = 0, pbdlen = patientBillDetailsinfo.Data.length; k < pbdlen; k++) {
                    BillDetails.push(patientBillDetailsinfo.Data[k]);
                }
                // EncounterDetails = encounterData.Data[0];
                if (j === 0) {
                    let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
                    printPreferencesData =
                        await facilityPreferenceBO.GetFacilityPreferenceWithLogo(BillData.FacilityId);
                }

            }
        }

        if (req && req.RetData) {
            for (let s = 0, len = req.RetData.length; s < len; s++) {
                let ReturnId = req.RetData[s];
                let retDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientReturnDetailsFilters.PatientReturnId, Value: ReturnId }]
                };
                let PatientRetDetailBo = BoFactory.GetBo(bo.PatientReturnDetailsBo, this.Request);
                let PatientReturnDetailsData: any = await PatientRetDetailBo.GetPatientReturnDetails(retDetailReq);

                for (let k = 0, pbdlen = PatientReturnDetailsData.Data.length; k < pbdlen; k++) {
                    ReturnDetails.push(PatientReturnDetailsData.Data[k]);
                }
            }
        }

        if (BillDetails.length > 0) {
            for (let idx in BillDetails) {
                let billInfo = BillDetails[idx];
                let alldetailData: any = {
                    BillDateTime: '',
                    ServiceName: '',
                    Quantity: 0,
                    Rate: '',
                    Amount: 0
                };
                alldetailData.BillDateTime = billInfo.BillDateTime;
                alldetailData.ServiceName = billInfo.ServiceName;
                alldetailData.Quantity = billInfo.Quantity;
                alldetailData.Rate = billInfo.Rate;
                alldetailData.Amount = billInfo.Amount;
                AllDetails.push(alldetailData);
            }
        }
        let returnAmt = 0;
        let TotalReturnAmount = 0;
        if (ReturnDetails.length > 0) {
            for (let idx in ReturnDetails) {
                let returnInfo = ReturnDetails[idx];
                let alldetailData: any = {
                    BillDateTime: '',
                    ServiceName: '',
                    Quantity: 0,
                    Rate: '',
                    Amount: 0
                };
                alldetailData.BillDateTime = returnInfo.ReturnDateTime;
                alldetailData.ServiceName = returnInfo.ServiceName;
                alldetailData.Quantity = returnInfo.ReturnQuantity;
                alldetailData.Rate = '-' + returnInfo.Rate;
                alldetailData.Amount = '-' + returnInfo.Amount;
                returnAmt += returnInfo.Amount;
                AllDetails.push(alldetailData);
            }
        }
        TotalReturnAmount = returnAmt;
        let info = {
            BillDetails: BillDetails,
            ReturnDetails: ReturnDetails,
            BillData: BillData,
            AllDetails: AllDetails,
            EncounterDetails: EncounterDetails,
            PatientDetails: PatientDetails,
            Preferences: printPreferencesData,
            TotBillAmount: TotBillAmount,
            BillDisc: BillDisc,
            PaidAmount: PaidAmount,
            RefAmount: RefAmount,
            NetAmount: NetAmount,
            SaleAmount: NetAmount - TotalReturnAmount,
            DueAmount: DueAmount,
            TotalReturnAmount: TotalReturnAmount,
            BalanceAmount: (NetAmount || 0) - (TotalReturnAmount || 0) - (PaidAmount || 0),
            Amount: PaidAmount - RefAmount
        };

        let pdfOption: any = null;
        let key = 'allpharmacyconsolidatebilldetails';
        let pdfOptionJSON = await Report.GetPdfOption(key);
        if (!pdfOptionJSON) {
            pdfOption = {
                format: 'A4',
                orientation: 'portrait',
                border: '0',
                header: {
                    height: '1.5in',
                    contents: '',
                },
                footer: {
                    height: '0.5in',
                    contents: {
                        first: '',
                        default: '',
                        last: '',
                    },
                },
                type: 'pdf',
                base: 'file://' + join(__dirname, '/../../Templates/assets/')
            };
        } else {
            pdfOption = JSON.parse(pdfOptionJSON);
            pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);

    }
    public async PrintConsolidatedOPbilldetails(req: BaseRequest): Promise<FileInfo> {
        let BillDetails = [];
        let ReturnDetails = [];
        let EncounterDetails: any = {};
        let PatientDetails: any = {};
        let BillData: any = {};
        let printPreferencesData: any = {};
        let TotBillAmount: number = 0;
        let BillDisc: number = 0;
        let PaidAmount: number = 0;
        let RefAmount: number = 0;
        let NetAmount: number = 0;
        let DueAmount: number = 0;
        if (req && req.Data) {
            let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);

            for (let j = 0, len = req.Data.length; j < len; j++) {
                let BillId = req.Data[j];

                let billreq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillsFilters.Id, Value: BillId }]
                };
                let BillInfo: any = await this.GetPatientBills(billreq);
                BillData = BillInfo.Data[0];
                TotBillAmount += BillData.BillAmount;
                BillDisc += BillData.BillDiscount;
                PaidAmount += BillData.PaidAmount;
                DueAmount += BillData.OutStandingAmount;
                RefAmount += BillData.RefundAmount;
                NetAmount += (BillData.BillAmount) - (BillData.BillDiscount);
                let billdetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: BillId }]
                };
                let patientBillDetailsinfo: any = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);

                // let PatientReturnReq = {
                //     Id: 0,
                //     PageContext: { PageSize: -1, PageNumber: 1 },
                //     Params: [{ Key: PatientReturnsFilters.PatientBillId, Value: BillId }]
                // };
                // let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
                // let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);

                let retDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientReturnDetailsFilters.PatientBillId, Value: BillId }]
                };
                let PatientRetDetailBo = BoFactory.GetBo(bo.PatientReturnDetailsBo, this.Request);
                let PatientReturnDetailsData: any = await PatientRetDetailBo.GetPatientReturnDetails(retDetailReq);

                for (let k = 0, pbdlen = PatientReturnDetailsData.Data.length; k < pbdlen; k++) {
                    ReturnDetails.push(PatientReturnDetailsData.Data[k]);
                }

                let encReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: EncounterFilters.Id, Value: patientBillDetailsinfo.Data[0].EncounterId }]
                };
                let encounterData = await encounterBo.GetEncounters(encReq);
                for (let k = 0, pbdlen = patientBillDetailsinfo.Data.length; k < pbdlen; k++) {
                    BillDetails.push(patientBillDetailsinfo.Data[k]);
                }
                EncounterDetails = encounterData.Data[0];

                let patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
                let patReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientFilters.Id, Value: BillData.PatientId }]
                };
                let PatData = await patientBO.GetPatients(patReq);
                PatientDetails = PatData.Data[0];

            }
            let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
            printPreferencesData =
                await facilityPreferenceBO.GetFacilityPreferenceWithLogo(EncounterDetails.FacilityId);
        }

        let info = {
            BillDetails: BillDetails,
            ReturnDetails: ReturnDetails,
            BillData: BillData,
            EncounterDetails: EncounterDetails,
            PatientDetails: PatientDetails,
            Preferences: printPreferencesData,
            TotBillAmount: TotBillAmount,
            BillDisc: BillDisc,
            PaidAmount: PaidAmount,
            RefAmount: RefAmount,
            NetAmount: NetAmount,
            DueAmount: DueAmount,
            Amount: NetAmount - RefAmount

        };

        let pdfOption: any = null;
        let key = 'pharmacyconsolidateopbilldetails';
        if (req.Data) {
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'landscape',
                    border: '0',
                    header: {
                        height: '1.5in',
                        contents: '',
                    },
                    footer: {
                        height: '0.5in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintConsolidatedallbilldetails(req: BaseRequest): Promise<FileInfo> {
        let BillDetails = [];
        let ReturnDetails = [];
        let EncounterDetails: any = {};
        let PatientDetails: any = {};
        let BillData: any = {};
        let printPreferencesData: any = {};
        let TotBillAmount: number = 0;
        let BillDisc: number = 0;
        let PaidAmount: number = 0;
        let RefAmount: number = 0;
        let NetAmount: number = 0;
        let DueAmount: number = 0;
        if (req && req.Data) {
            let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
            let encounterBo = BoFactory.GetBo(encbo.EncounterBo, this.Request);

            for (let j = 0, len = req.Data.length; j < len; j++) {
                let BillId = req.Data[j];

                let billreq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillsFilters.Id, Value: BillId }]
                };
                let BillInfo: any = await this.GetPatientBills(billreq);
                BillData = BillInfo.Data[0];
                TotBillAmount += BillData.BillAmount;
                BillDisc += BillData.BillDiscount;
                PaidAmount += BillData.PaidAmount;
                DueAmount += BillData.OutStandingAmount;
                RefAmount += BillData.RefundAmount;
                NetAmount += (BillData.BillAmount) - (BillData.BillDiscount);
                let billdetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientBillDetailsFilters.PatientBillId, Value: BillId }]
                };
                let patientBillDetailsinfo: any = await PatientBillDetailsBo.GetPatientBillDetails(billdetailReq);

                // let PatientReturnReq = {
                //     Id: 0,
                //     PageContext: { PageSize: -1, PageNumber: 1 },
                //     Params: [{ Key: PatientReturnsFilters.PatientBillId, Value: BillId }]
                // };
                // let PatientReturnsBo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
                // let PatientReturnsData = await PatientReturnsBo.GetPatientReturns(PatientReturnReq);

                let retDetailReq = {
                    Id: 0,
                    PageContext: { PageSize: 1000, PageNumber: 1 },
                    Params: [{ Key: PatientReturnDetailsFilters.PatientBillId, Value: BillId }]
                };
                let PatientRetDetailBo = BoFactory.GetBo(bo.PatientReturnDetailsBo, this.Request);
                let PatientReturnDetailsData: any = await PatientRetDetailBo.GetPatientReturnDetails(retDetailReq);

                for (let k = 0, pbdlen = PatientReturnDetailsData.Data.length; k < pbdlen; k++) {
                    ReturnDetails.push(PatientReturnDetailsData.Data[k]);
                }

                let encReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: EncounterFilters.Id, Value: patientBillDetailsinfo.Data[0].EncounterId }]
                };
                let encounterData = await encounterBo.GetEncounters(encReq);
                for (let k = 0, pbdlen = patientBillDetailsinfo.Data.length; k < pbdlen; k++) {
                    BillDetails.push(patientBillDetailsinfo.Data[k]);
                }
                EncounterDetails = encounterData.Data[0];

                let patientBO = BoFactory.GetBo(regbo.PatientBo, this.Request);
                let patReq = {
                    Id: 0,
                    PageContext: { PageSize: -1, PageNumber: 1 },
                    Params: [{ Key: PatientFilters.Id, Value: BillData.PatientId }]
                };
                let PatData = await patientBO.GetPatients(patReq);
                PatientDetails = PatData.Data[0];

            }
            let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
            printPreferencesData =
                await facilityPreferenceBO.GetFacilityPreferenceWithLogo(EncounterDetails.FacilityId);
        }

        let info = {
            BillDetails: BillDetails,
            ReturnDetails: ReturnDetails,
            BillData: BillData,
            EncounterDetails: EncounterDetails,
            PatientDetails: PatientDetails,
            Preferences: printPreferencesData,
            TotBillAmount: TotBillAmount,
            BillDisc: BillDisc,
            PaidAmount: PaidAmount,
            RefAmount: RefAmount,
            NetAmount: NetAmount,
            DueAmount: DueAmount,
            Amount: NetAmount - RefAmount

        };

        let pdfOption: any = null;
        let key = 'pharmacyconsolidateallbilldetails';
        if (req.Data) {
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'portrait',
                    border: '0',
                    header: {
                        height: '1.5in',
                        contents: '',
                    },
                    footer: {
                        height: '0.5in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    public async PrintPharmacyConsolidatedbill(req: BaseRequest): Promise<FileInfo> {
        let data = await this.getPharmacyBillsWithReturn(req);
        let facilityId = req.Data.FacilityId;
        let SalesSerialNo = 0;
        let ReturnSerialNo = 0;
        let PatientPharmacyBills = [];
        let PatientPharmacyReturns = [];
        let PatientBillsWithReturn = data;
        let TotalAmount = 0;
        let TotalSalesAmount = 0;
        let TotalReturnAmount = 0;
        for (let i = 0; i < PatientBillsWithReturn.length; i++) {
            let item = PatientBillsWithReturn[i];
            if (item.ReturnNumber && item.ReturnNumber !== undefined &&
                item.ReturnNumber !== null && item.ReturnNumber !== '') {
                item.ReturnSerialNo = ReturnSerialNo + 1;
                if (item.ReturnAmount === 0 && item.GrossAmount > 0) {
                    item.ReturnAmount = item.GrossAmount;
                }
                item.NetReturnAmount = item.ReturnAmount - item.DiscountAmount;
                item.NetReturnAmount = Math.round(item.NetReturnAmount);
                TotalReturnAmount = TotalReturnAmount + item.NetReturnAmount;
                PatientPharmacyReturns.push(item);
                ReturnSerialNo++;
            } else if (item.PharmacyReturnTypeId !== 6) {
                item.SalesSerialNo = SalesSerialNo + 1;
                item.NetAmount = item.BillAmount - item.BillDiscount;
                item.NetAmount = Math.round(item.NetAmount);
                TotalSalesAmount = TotalSalesAmount + item.NetAmount;
                PatientPharmacyBills.push(item);
                SalesSerialNo++;
            }
        }
        TotalAmount = TotalSalesAmount - TotalReturnAmount;
        let facilityPreferenceBO = BoFactory.GetBo(userbo.FacilityPreferenceBo, this.Request);
        // let storePreferenceBO = BoFactory.GetBo(invbo.StorePreferenceBo, this.Request);
        let printPreferencesData =
            await facilityPreferenceBO.GetFacilityPreferenceWithLogo(facilityId);
        // let printStoreData =
        //     await storePreferenceBO.GetStorePreferenceWithLogo(facilityId, PatientBills.StoreMasterId);
        // if (printStoreData && printStoreData.pharmacyprintheader)
        //     printPreferencesData.pharmacyprintheader = printStoreData.pharmacyprintheader;
        // if (printStoreData && printStoreData.pharmacyprintfooter)
        //     printPreferencesData.pharmacyprintfooter = printStoreData.pharmacyprintfooter;
        // if (printStoreData && printStoreData.StoreLogo)
        //     printPreferencesData.Facilitylogo = printStoreData.StoreLogo;

        let info = {
            PatientPharmacyReturns: PatientPharmacyReturns,
            PatientPharmacyBills: PatientPharmacyBills,
            Preferences: printPreferencesData,
            TotalAmount: TotalAmount,
            TotalSalesAmount: TotalSalesAmount,
            TotalReturnAmount: TotalReturnAmount,
        };

        let pdfOption: any = null;
        let key = 'pharmacyconsolidatebillprint';
        if (req.Data) {
            key = 'pharmacyconsolidatebillprint';
            let pdfOptionJSON = await Report.GetPdfOption(key);
            if (!pdfOptionJSON) {
                pdfOption = {
                    format: 'A4',
                    orientation: 'landscape',
                    border: '0',
                    header: {
                        height: '1.5in',
                        contents: '',
                    },
                    footer: {
                        height: '0.5in',
                        contents: {
                            first: '',
                            default: '',
                            last: '',
                        },
                    },
                    type: 'pdf',
                    base: 'file://' + join(__dirname, '/../../Templates/assets/')
                };
            } else {
                pdfOption = JSON.parse(pdfOptionJSON);
                pdfOption.base = 'file://' + join(__dirname, '/../../Templates/assets/');
            }
        }
        return await Report.Generate(key, { header: {}, body: info }, null, pdfOption);
    }
    /* BIDeptRevneue */
    public async BIDeptRevneue(req: BaseRequest): Promise<any> {
        let BIRptDeptRevenue: any = {};
        for (let j = 0, len = req.Data.length; j < len; j++) {
            let frmdt = req.Data[j].DispDate;
            if (!BIRptDeptRevenue[frmdt]) BIRptDeptRevenue[frmdt] = {};
            let newreq: any = {
                Data: []
            };
            newreq.Data = req.Data[j];
            if (newreq && newreq.Data && newreq.Data.FromDate) {
                if (!BIRptDeptRevenue[frmdt]['OPDept']) {
                    BIRptDeptRevenue[frmdt]['OPDept'] = [];
                }
                if (!BIRptDeptRevenue[frmdt]['IPDept']) {
                    BIRptDeptRevenue[frmdt]['IPDept'] = [];
                }
                BIRptDeptRevenue[frmdt]['OPDept'].push(await this.OPDeptRevenue(newreq));
                BIRptDeptRevenue[frmdt]['IPDept'].push(await this.IPDeptRevenue(newreq));
            }
        }
        return BIRptDeptRevenue;
    }



    /* BIDoctorRevneue */
    public async BIDoctorRevneue(req: BaseRequest): Promise<any> {
        let BIRptDoctorRevenue: any = {};
        for (let j = 0, len = req.Data.length; j < len; j++) {
            let frmdt = req.Data[j].DispDate;
            if (!BIRptDoctorRevenue[frmdt]) BIRptDoctorRevenue[frmdt] = {};
            let newreq: any = {
                Data: []
            };
            newreq.Data = req.Data[j];
            if (newreq && newreq.Data && newreq.Data.FromDate) {
                if (!BIRptDoctorRevenue[frmdt]['OPDoctor']) {
                    BIRptDoctorRevenue[frmdt]['OPDoctor'] = [];
                }
                if (!BIRptDoctorRevenue[frmdt]['IPDoctor']) {
                    BIRptDoctorRevenue[frmdt]['IPDoctor'] = [];
                }
                BIRptDoctorRevenue[frmdt]['OPDoctor'].push(await this.OPDoctorRevenue(newreq));
                BIRptDoctorRevenue[frmdt]['IPDoctor'].push(await this.IPDoctorRevenue(newreq));
            }
        }
        return BIRptDoctorRevenue;
    }

    public async IPPharmacyBills(req: BaseRequest): Promise<any> {
        let UserGroup: { [id: number]: any[] } = {};
        let UserGroupJoin: any = {
            model: this.Models.User, as: 'CreatedUser',
            attributes: ['FirstName', 'LastName', 'UserName'],
            required: true,
            include: [
                this.GetReference('Title')
            ]
        };
        let totalbillamountInstance: any = await this.FindAll({
            attributes: ['BillAmount', 'BillGeneratedBy'],
            where: {
                // BillTypeId: 3,
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                Status: 1,
                FacilityId: req.Data.FacilityId,
                StoreMasterId: req.Data.StoreMasterId,
                BillGeneratedBy: { '$gt': 0 },
            },
            include: [UserGroupJoin]
        });
        if (totalbillamountInstance) {
            let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
            for (let i in groupbills) {
                let groupedBills = groupbills[i];
                let BillAmount: number = 0;
                let UserId: number = 0;
                let User: string = '';
                for (let i = 0; i < groupedBills.length; i++) {
                    let bills: any = groupedBills[i];
                    BillAmount += bills.BillAmount;
                    UserId = bills.BillGeneratedBy;
                    User = bills.CreatedUser;
                    UserGroup[UserId] = UserGroup[UserId] || [];
                }
                let info = {
                    'TotalBillAmt': BillAmount,
                    'UserId': UserId,
                    'UserName': User
                };
                UserGroup[UserId].push(info);
            }
        }
        let dueamountInstance: any = await this.FindAll({
            attributes: ['BillAmount', 'OutStandingAmount', 'BillGeneratedBy'],
            where: {
                // BillTypeId: 3,
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                Status: 1,
                FacilityId: req.Data.FacilityId,
                StoreMasterId: req.Data.StoreMasterId,
                BillGeneratedBy: { '$gt': 0 },
            },
            include: [UserGroupJoin]
        });
        if (dueamountInstance) {
            let groupbills = _.groupBy(dueamountInstance, 'BillGeneratedBy');
            for (let i in groupbills) {
                let groupedBills = groupbills[i];
                let DueAmount: number = 0;
                let UserId: number = 0;
                let User: string = '';
                for (let i = 0; i < groupedBills.length; i++) {
                    let bills: any = groupedBills[i];
                    DueAmount += bills.OutStandingAmount;
                    UserId = bills.BillGeneratedBy;
                    User = bills.CreatedUser;
                    UserGroup[UserId] = UserGroup[UserId] || [];
                }
                let info = {
                    'BalanceDue': DueAmount,
                    'UserId': UserId,
                    'UserName': User
                };
                UserGroup[UserId].push(info);
            }
        }
        let ipbillamountInstance: any = await this.FindAll({
            attributes: ['BillAmount', 'BillGeneratedBy'],
            where: {
                BillTypeId: 3,
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                Status: 1,
                FacilityId: req.Data.FacilityId,
                BillGeneratedBy: { '$gt': 0 },
                StoreMasterId: req.Data.StoreMasterId,
                PharmacySaleTypeId: 6
            },
            include: [UserGroupJoin]
        });
        if (ipbillamountInstance) {
            let groupbills = _.groupBy(ipbillamountInstance, 'BillGeneratedBy');
            for (let i in groupbills) {
                let groupedBills = groupbills[i];
                let BillAmount: number = 0;
                let UserId: number = 0;
                let User: string = '';
                for (let i = 0; i < groupedBills.length; i++) {
                    let bills: any = groupedBills[i];
                    BillAmount += bills.BillAmount;
                    UserId = bills.BillGeneratedBy;
                    User = bills.CreatedUser;
                    UserGroup[UserId] = UserGroup[UserId] || [];
                }
                let info = {
                    'IPSaleAmount': BillAmount,
                    'UserId': UserId,
                    'UserName': User
                };
                UserGroup[UserId].push(info);
            }
        }
        return UserGroup;
    }
    public async BillingBills(req: BaseRequest): Promise<any> {
        let UserGroup: { [id: number]: any[] } = {};
        let UserGroupJoin: any = {
            model: this.Models.User, as: 'CreatedUser',
            attributes: ['FirstName', 'LastName', 'UserName'],
            required: true,
            include: [
                this.GetReference('Title')
            ]
        };
        if (req.Data.UserId > 0) {
            let totalbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillGeneratedBy'],
                where: {
                    BillTypeId: { '$in': [1, 2, 5] },
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsPharmacyBill: 0,
                    Status: 1,
                    FacilityId: req.Data.FacilityId,
                    BillGeneratedBy: { '$eq': req.Data.UserId },
                },
                include: [UserGroupJoin]
            });
            if (totalbillamountInstance) {
                let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let UserId: number = 0;
                    let User: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        UserId = bills.BillGeneratedBy;
                        User = bills.CreatedUser;
                        UserGroup[UserId] = UserGroup[UserId] || [];
                    }
                    let info = {
                        'TotalBillAmt': BillAmount,
                        'UserId': UserId,
                        'UserName': User
                    };
                    UserGroup[UserId].push(info);
                }
            }
        } else if (req.Data.UserId === 0) {
            let totalbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillGeneratedBy'],
                where: {
                    BillTypeId: { '$in': [1, 2, 5] },
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsPharmacyBill: 0,
                    Status: 1,
                    FacilityId: req.Data.FacilityId,
                    BillGeneratedBy: { '$gt': req.Data.UserId },
                },
                include: [UserGroupJoin]
            });
            if (totalbillamountInstance) {
                let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let UserId: number = 0;
                    let User: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        UserId = bills.BillGeneratedBy;
                        User = bills.CreatedUser;
                        UserGroup[UserId] = UserGroup[UserId] || [];
                    }
                    let info = {
                        'TotalBillAmt': BillAmount,
                        'UserId': UserId,
                        'UserName': User
                    };
                    UserGroup[UserId].push(info);
                }
            }
        }
        return UserGroup;
    }

    public async OPBillingBills(req: BaseRequest): Promise<any> {
        let UserGroup: { [id: number]: any[] } = {};
        let UserGroupJoin: any = {
            model: this.Models.User, as: 'CreatedUser',
            attributes: ['FirstName', 'LastName', 'UserName'],
            required: true,
            include: [
                this.GetReference('Title')
            ]
        };
        if (req.Data.UserId > 0) {
            let totalbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillGeneratedBy'],
                where: {
                    BillTypeId: { '$in': [1, 5] },
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsPharmacyBill: 0,
                    Status: 1,
                    FacilityId: req.Data.FacilityId,
                    BillGeneratedBy: { '$eq': req.Data.UserId },
                },
                include: [UserGroupJoin]
            });
            if (totalbillamountInstance) {
                let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let UserId: number = 0;
                    let User: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        UserId = bills.BillGeneratedBy;
                        User = bills.CreatedUser;
                        UserGroup[UserId] = UserGroup[UserId] || [];
                    }
                    let info = {
                        'TotalBillAmt': BillAmount,
                        'UserId': UserId,
                        'UserName': User
                    };
                    UserGroup[UserId].push(info);
                }
            }
        } else if (req.Data.UserId === 0) {
            let totalbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillGeneratedBy'],
                where: {
                    BillTypeId: { '$in': [1, 5] },
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsPharmacyBill: 0,
                    Status: 1,
                    FacilityId: req.Data.FacilityId,
                    BillGeneratedBy: { '$gt': req.Data.UserId },
                },
                include: [UserGroupJoin]
            });
            if (totalbillamountInstance) {
                let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let UserId: number = 0;
                    let User: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        UserId = bills.BillGeneratedBy;
                        User = bills.CreatedUser;
                        UserGroup[UserId] = UserGroup[UserId] || [];
                    }
                    let info = {
                        'TotalBillAmt': BillAmount,
                        'UserId': UserId,
                        'UserName': User
                    };
                    UserGroup[UserId].push(info);
                }
            }
        }
        return UserGroup;
    }

    public async IPBillingBills(req: BaseRequest): Promise<any> {
        let UserGroup: { [id: number]: any[] } = {};
        let UserGroupJoin: any = {
            model: this.Models.User, as: 'CreatedUser',
            attributes: ['FirstName', 'LastName', 'UserName'],
            required: true,
            include: [
                this.GetReference('Title')
            ]
        };
        if (req.Data.UserId > 0) {
            let totalbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillGeneratedBy'],
                where: {
                    BillTypeId: { '$in': [2] },
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsPharmacyBill: 0,
                    Status: 1,
                    FacilityId: req.Data.FacilityId,
                    BillGeneratedBy: { '$eq': req.Data.UserId },
                },
                include: [UserGroupJoin]
            });
            if (totalbillamountInstance) {
                let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let UserId: number = 0;
                    let User: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        UserId = bills.BillGeneratedBy;
                        User = bills.CreatedUser;
                        UserGroup[UserId] = UserGroup[UserId] || [];
                    }
                    let info = {
                        'TotalBillAmt': BillAmount,
                        'UserId': UserId,
                        'UserName': User
                    };
                    UserGroup[UserId].push(info);
                }
            }
        } else if (req.Data.UserId === 0) {
            let totalbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillGeneratedBy'],
                where: {
                    BillTypeId: { '$in': [2] },
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsPharmacyBill: 0,
                    Status: 1,
                    FacilityId: req.Data.FacilityId,
                    BillGeneratedBy: { '$gt': 0 },
                },
                include: [UserGroupJoin]
            });
            if (totalbillamountInstance) {
                let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let UserId: number = 0;
                    let User: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        UserId = bills.BillGeneratedBy;
                        User = bills.CreatedUser;
                        UserGroup[UserId] = UserGroup[UserId] || [];
                    }
                    let info = {
                        'TotalBillAmt': BillAmount,
                        'UserId': UserId,
                        'UserName': User
                    };
                    UserGroup[UserId].push(info);
                }
            }
        }
        return UserGroup;
    }

    public async OPBillCollection(req: BaseRequest): Promise<any> {
        let OPBillResult: any = {};
        OPBillResult['DisplayOrder'] = 2;

        let opbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'TotalAmount'],
            ],
            where: {
                BillTypeId: { '$in': [1, 5] },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountInstance) {
            let bill: any = this.GetAttribute(opbillamountInstance);
            OPBillResult['BillAmount'] = bill['TotalAmount'];
        }

        let opbillamountdiscountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'TotalDis'],
            ],
            where: {
                BillTypeId: { '$in': [1, 5] },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountdiscountInstance) {
            let bill: any = this.GetAttribute(opbillamountdiscountInstance);
            OPBillResult['BillDiscount'] = bill['TotalDis'];
        }

        let opbillamountDueInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('OutStandingAmount')), 'TotalDue'],
            ],
            where: {
                BillTypeId: { '$in': [1, 5] },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                Status: 1,
                FacilityId: req.Data.FacilityId,
                IsPaidFully: 0
            }
        });
        if (opbillamountDueInstance) {
            let bill: any = this.GetAttribute(opbillamountDueInstance);
            OPBillResult['OutStandingAmount'] = bill['TotalDue'];
        }
        return OPBillResult;
    }
    public async IPBillCollection(req: BaseRequest): Promise<any> {
        let OPBillResult: any = {};
        OPBillResult['DisplayOrder'] = 5;

        let opbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'TotalAmount'],
            ],
            where: {
                BillTypeId: { '$in': [2] },
                EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountInstance) {
            let bill: any = this.GetAttribute(opbillamountInstance);
            OPBillResult['BillAmount'] = bill['TotalAmount'];
        }

        let opbillamountdiscountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'TotalDis'],
            ],
            where: {
                BillTypeId: { '$in': [2] },
                EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountdiscountInstance) {
            let bill: any = this.GetAttribute(opbillamountdiscountInstance);
            OPBillResult['BillDiscount'] = bill['TotalDis'];
        }

        let opbillamountDueInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('OutStandingAmount')), 'TotalDue'],
            ],
            where: {
                BillTypeId: { '$in': [2] },
                EncounterTypeId: { '$or': { '$eq': null, '$in': [2] } },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 0,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountDueInstance) {
            let bill: any = this.GetAttribute(opbillamountDueInstance);
            OPBillResult['OutStandingAmount'] = bill['TotalDue'];
        }
        return OPBillResult;
    }

    public async PharmacyBillCollection(req: BaseRequest): Promise<any> {
        let OPBillResult: any = {};
        OPBillResult['DisplayOrder'] = 8;

        let opbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'TotalAmount'],
            ],
            where: {
                BillTypeId: { '$in': [4] },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountInstance) {
            let bill: any = this.GetAttribute(opbillamountInstance);
            OPBillResult['BillAmount'] = bill['TotalAmount'];
        }

        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'TotalAmount'],
            ],
            where: {
                BillTypeId: { '$in': [3] },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                PharmacySaleTypeId: 6,
                IsPharmacyBill: 1,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            OPBillResult['IPBillAmount'] = bill['TotalAmount'];
        }
        let opbillamountdiscountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'TotalDis'],
            ],
            where: {
                BillTypeId: { '$in': [4] },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountdiscountInstance) {
            let bill: any = this.GetAttribute(opbillamountdiscountInstance);
            OPBillResult['BillDiscount'] = bill['TotalDis'];
        }

        let opbillamountroundoffInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('RoundOffValue')), 'TotalRoundoff'],
            ],
            where: {
                BillTypeId: { '$in': [4] },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountroundoffInstance) {
            let bill: any = this.GetAttribute(opbillamountroundoffInstance);
            OPBillResult['RoundOffValue'] = bill['TotalRoundoff'];
        }

        let opbillamountDueInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('OutStandingAmount')), 'TotalDue'],
            ],
            where: {
                BillTypeId: { '$in': [4] },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsPharmacyBill: 1,
                Status: 1,
                FacilityId: req.Data.FacilityId
            }
        });
        if (opbillamountDueInstance) {
            let bill: any = this.GetAttribute(opbillamountDueInstance);
            OPBillResult['OutStandingAmount'] = bill['TotalDue'];
        }
        return OPBillResult;
    }

    public async OverallBills(req: BaseRequest): Promise<any> {
        let UserGroup: { [id: number]: any[] } = {};
        let UserGroupJoin: any = {
            model: this.Models.User, as: 'CreatedUser',
            attributes: ['FirstName', 'LastName', 'UserName'],
            required: true,
            include: [
                this.GetReference('Title')
            ]
        };
        if (req.Data.UserId > 0) {
            let totalbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillGeneratedBy'],
                where: {
                    BillTypeId: { '$in': [1, 2, 4, 5] },
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    // IsPharmacyBill: 0,
                    Status: 1,
                    FacilityId: req.Data.FacilityId,
                    BillGeneratedBy: { '$eq': req.Data.UserId },
                },
                include: [UserGroupJoin]
            });
            if (totalbillamountInstance) {
                let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let UserId: number = 0;
                    let User: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        UserId = bills.BillGeneratedBy;
                        User = bills.CreatedUser;
                        UserGroup[UserId] = UserGroup[UserId] || [];
                    }
                    let info = {
                        'TotalBillAmt': BillAmount,
                        'UserId': UserId,
                        'UserName': User
                    };
                    UserGroup[UserId].push(info);
                }
            }
        } else if (req.Data.UserId === 0) {
            let totalbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillGeneratedBy'],
                where: {
                    BillTypeId: { '$in': [1, 2, 4, 5] },
                    BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    // IsPharmacyBill: 0,
                    Status: 1,
                    FacilityId: req.Data.FacilityId,
                    BillGeneratedBy: { '$gt': req.Data.UserId },
                },
                include: [UserGroupJoin]
            });
            if (totalbillamountInstance) {
                let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
                for (let i in groupbills) {
                    let groupedBills = groupbills[i];
                    let BillAmount: number = 0;
                    let UserId: number = 0;
                    let User: string = '';
                    for (let i = 0; i < groupedBills.length; i++) {
                        let bills: any = groupedBills[i];
                        BillAmount += bills.BillAmount;
                        UserId = bills.BillGeneratedBy;
                        User = bills.CreatedUser;
                        UserGroup[UserId] = UserGroup[UserId] || [];
                    }
                    let info = {
                        'TotalBillAmt': BillAmount,
                        'UserId': UserId,
                        'UserName': User
                    };
                    UserGroup[UserId].push(info);
                }
            }
        }
        return UserGroup;
    }

    public async GetRevenueDoctorSummary(req: BaseRequest): Promise<any> {
        let result: any = [];
        result.push({ Key: 1, Value: await this.OPDoctorRevenue(req) });
        result.push({ Key: 2, Value: await this.IPDoctorRevenue(req) });
        result.push({ Key: 3, Value: await this.DirectDoctorRevenue(req) });
        return result;
    }


    public async OPDoctorRevenue(req: BaseRequest): Promise<any> {
        let DoctorGroup: { [id: number]: any[] } = {};
        let DoctorGroupJoin: any = {
            model: this.Models.User,
            attributes: ['FirstName', 'LastName', 'UserName'],
            where: {
                UserTypeId: 2,
            },
            required: true,
            include: [
                this.GetReference('Title')
            ]
        };
        if (req.Data.DoctorId > 0) {
            let doctbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillDiscount', 'RefundAmount', 'DoctorId'],
                where: {
                    BillTypeId: { '$in': [1, 5] },
                    DoctorId: { '$eq': req.Data.DoctorId },
                    BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsConsolidatePay: false,
                    FacilityId: req.Data.FacilityId,
                },
                include: [DoctorGroupJoin]
            });
            if (doctbillamountInstance) {
                for (let i = 0; i < doctbillamountInstance.length; i++) {
                    let bills: any = this.GetAttribute(doctbillamountInstance[i]);
                    let DoctorId = bills.DoctorId;
                    let billamt = bills.BillAmount;
                    let discbill = bills.BillDiscount;
                    let refundamt = bills.RefundAmount;
                    let NetAmount = billamt - discbill;
                    let NetRevenue = (billamt || 0) - (discbill || 0) - (refundamt || 0);
                    DoctorGroup[DoctorId] = DoctorGroup[DoctorId] || [];
                    let info = {
                        'BillAmount': NetAmount,
                        'NetRevenue': NetRevenue,
                        'GrossAmount': billamt,
                        'BillDiscount': discbill,
                        'DoctorId': DoctorId,
                        'DoctorName': bills.User
                    };
                    DoctorGroup[DoctorId].push(info);
                }
            }
        } else if (req.Data.DoctorId === 0) {
            let doctbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillDiscount', 'RefundAmount', 'DoctorId'],
                where: {
                    BillTypeId: { '$in': [1, 5] },
                    DoctorId: { '$gt': 0 },
                    BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsConsolidatePay: false,
                    FacilityId: req.Data.FacilityId,
                },
                include: [DoctorGroupJoin]
            });
            if (doctbillamountInstance) {
                for (let i = 0; i < doctbillamountInstance.length; i++) {
                    let bills: any = this.GetAttribute(doctbillamountInstance[i]);
                    let DoctorId = bills.DoctorId;
                    let billamt = bills.BillAmount;
                    let discbill = bills.BillDiscount;
                    let refundamt = bills.RefundAmount;
                    let NetAmount = billamt - discbill;
                    let NetRevenue = (billamt || 0) - (discbill || 0) - (refundamt || 0);
                    DoctorGroup[DoctorId] = DoctorGroup[DoctorId] || [];
                    let info = {
                        'BillAmount': NetAmount,
                        'NetRevenue': NetRevenue,
                        'GrossAmount': billamt,
                        'BillDiscount': discbill,
                        'DoctorId': DoctorId,
                        'DoctorName': bills.User
                    };
                    DoctorGroup[DoctorId].push(info);
                }
            }
        }

        return DoctorGroup;
    }

    public async DirectDoctorRevenue(req: BaseRequest): Promise<any> {
        let DoctorGroup: { [id: number]: any[] } = {};
        let doctbillamountInstance: any = await this.FindAll({
            attributes: ['BillAmount', 'BillDiscount', 'RefundAmount', 'DoctorId'],
            where: {
                BillTypeId: { '$in': [1, 5] },
                DoctorId: { '$in': [-1, 0, null] },
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsConsolidatePay: false,
                FacilityId: req.Data.FacilityId,
            }
        });
        if (doctbillamountInstance) {
            for (let i = 0; i < doctbillamountInstance.length; i++) {
                let bills: any = this.GetAttribute(doctbillamountInstance[i]);
                let DoctorId = bills.DoctorId;
                let DocName = 'Others';
                let billamt = bills.BillAmount;
                let discbill = bills.BillDiscount;
                let refundamt = bills.RefundAmount;
                let NetAmount = billamt - discbill;
                let NetRevenue = (billamt || 0) - (discbill || 0) - (refundamt || 0);
                DoctorGroup[DoctorId] = DoctorGroup[DoctorId] || [];
                let info = {
                    'BillAmount': NetAmount,
                    'NetRevenue': NetRevenue,
                    'GrossAmount': billamt,
                    'BillDiscount': discbill,
                    'DoctorId': DoctorId,
                    'DoctorName': DocName
                };
                DoctorGroup[DoctorId].push(info);
            }
        }

        return DoctorGroup;
    }

    public async IPDoctorRevenue(req: BaseRequest): Promise<any> {
        let DoctorGroup: { [id: number]: any[] } = {};
        let DoctorGroupJoin: any = {
            model: this.Models.User,
            attributes: ['FirstName', 'LastName', 'UserName'],
            where: {
                UserTypeId: 2,
            },
            required: true,
            include: [
                this.GetReference('Title')
            ]
        };
        if (req.Data.DoctorId > 0) {
            let doctbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillDiscount', 'DoctorId'],
                where: {
                    BillTypeId: 2,
                    DoctorId: { '$eq': req.Data.DoctorId },
                    BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsConsolidatePay: false,
                    FacilityId: req.Data.FacilityId,
                },
                include: [DoctorGroupJoin]
            });
            if (doctbillamountInstance) {
                for (let i = 0; i < doctbillamountInstance.length; i++) {
                    let bills: any = this.GetAttribute(doctbillamountInstance[i]);
                    let DoctorId = bills.DoctorId;
                    let billamt = bills.BillAmount;
                    let discbill = bills.BillDiscount;
                    let NetAmount = billamt - discbill;
                    DoctorGroup[DoctorId] = DoctorGroup[DoctorId] || [];
                    let info = {
                        'BillAmount': NetAmount,
                        'GrossAmount': billamt,
                        'BillDiscount': discbill,
                        'DoctorId': DoctorId,
                        'DoctorName': bills.User
                    };
                    DoctorGroup[DoctorId].push(info);
                }
            }
        } else if (req.Data.DoctorId === 0) {
            let doctbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillDiscount', 'DoctorId'],
                where: {
                    BillTypeId: 2,
                    DoctorId: { '$gt': 0 },
                    BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsConsolidatePay: false,
                    FacilityId: req.Data.FacilityId,
                },
                include: [DoctorGroupJoin]
            });
            if (doctbillamountInstance) {
                for (let i = 0; i < doctbillamountInstance.length; i++) {
                    let bills: any = this.GetAttribute(doctbillamountInstance[i]);
                    let DoctorId = bills.DoctorId;
                    let billamt = bills.BillAmount;
                    let discbill = bills.BillDiscount;
                    let NetAmount = billamt - discbill;
                    DoctorGroup[DoctorId] = DoctorGroup[DoctorId] || [];
                    let info = {
                        'BillAmount': NetAmount,
                        'GrossAmount': billamt,
                        'BillDiscount': discbill,
                        'DoctorId': DoctorId,
                        'DoctorName': bills.User
                    };
                    DoctorGroup[DoctorId].push(info);
                }
            }
        }

        return DoctorGroup;
    }

    public async GetRevenueDepartmentSummary(req: BaseRequest): Promise<any> {
        let result: any = [];
        result.push({ Key: 1, Value: await this.OPDeptRevenue(req) });
        result.push({ Key: 2, Value: await this.IPDeptRevenue(req) });
        result.push({ Key: 3, Value: await this.DirectDeptRevenue(req) });
        return result;
    }

    public async OPDeptRevenue(req: BaseRequest): Promise<any> {
        let DeptGroup: { [id: number]: any[] } = {};
        let DeptGroupJoin: any = {
            model: this.Models.Department,
            attributes: ['DepartmentName'],
            required: false,
        };
        if (req.Data.DepartmentId > 0) {
            let deptbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillDiscount', 'RefundAmount', 'DepartmentId'],
                where: {
                    BillTypeId: { '$in': [1, 5] },
                    DepartmentId: { '$eq': req.Data.DepartmentId },
                    BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsConsolidatePay: false,
                    FacilityId: req.Data.FacilityId,
                },
                include: [DeptGroupJoin]
            });
            if (deptbillamountInstance) {
                for (let i = 0; i < deptbillamountInstance.length; i++) {
                    let bills: any = this.GetAttribute(deptbillamountInstance[i]);
                    let DeptId = bills.DepartmentId;
                    let billamt = bills.BillAmount;
                    let discbill = bills.BillDiscount;
                    let refundamt = bills.RefundAmount;
                    let NetAmount = billamt - discbill;
                    let NetRevenue = (billamt || 0) - (discbill || 0) - (refundamt || 0);
                    DeptGroup[DeptId] = DeptGroup[DeptId] || [];
                    let info = {
                        'BillAmount': NetAmount,
                        'NetRevenue': NetRevenue,
                        'GrossAmount': billamt,
                        'BillDiscount': discbill,
                        'DeptId': DeptId,
                        'DepartmentName': bills.Department.DepartmentName
                    };
                    DeptGroup[DeptId].push(info);
                }
            }
        } else if (req.Data.DepartmentId === 0) {
            let deptbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillDiscount', 'RefundAmount', 'DepartmentId'],
                where: {
                    BillTypeId: { '$in': [1, 5] },
                    DepartmentId: { '$gt': req.Data.DepartmentId },
                    BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsConsolidatePay: false,
                    FacilityId: req.Data.FacilityId,
                },
                include: [DeptGroupJoin]
            });
            if (deptbillamountInstance) {
                for (let i = 0; i < deptbillamountInstance.length; i++) {
                    let bills: any = this.GetAttribute(deptbillamountInstance[i]);
                    let DeptId = bills.DepartmentId;
                    let billamt = bills.BillAmount;
                    let discbill = bills.BillDiscount;
                    let refundamt = bills.RefundAmount;
                    let NetAmount = billamt - discbill;
                    let NetRevenue = (billamt || 0) - (discbill || 0) - (refundamt || 0);
                    DeptGroup[DeptId] = DeptGroup[DeptId] || [];
                    let info = {
                        'BillAmount': NetAmount,
                        'NetRevenue': NetRevenue,
                        'GrossAmount': billamt,
                        'BillDiscount': discbill,
                        'DeptId': DeptId,
                        'DepartmentName': bills.Department.DepartmentName
                    };
                    DeptGroup[DeptId].push(info);
                }
            }
        }
        return DeptGroup;
    }

    public async IPDeptRevenue(req: BaseRequest): Promise<any> {
        let DeptGroup: { [id: number]: any[] } = {};
        let DeptGroupJoin: any = {
            model: this.Models.Department,
            attributes: ['DepartmentName'],
            required: false,
        };
        if (req.Data.DepartmentId > 0) {
            let deptbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillDiscount', 'DepartmentId'],
                where: {
                    BillTypeId: 2,
                    DepartmentId: { '$eq': req.Data.DepartmentId },
                    BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsConsolidatePay: false,
                    FacilityId: req.Data.FacilityId,
                },
                include: [DeptGroupJoin]
            });
            if (deptbillamountInstance) {
                for (let i = 0; i < deptbillamountInstance.length; i++) {
                    let bills: any = this.GetAttribute(deptbillamountInstance[i]);
                    let DeptId = bills.DepartmentId;
                    let billamt = bills.BillAmount;
                    let discbill = bills.BillDiscount;
                    let NetAmount = billamt - discbill;
                    DeptGroup[DeptId] = DeptGroup[DeptId] || [];
                    let info = {
                        'BillAmount': NetAmount,
                        'GrossAmount': billamt,
                        'BillDiscount': discbill,
                        'DeptId': DeptId,
                        'DepartmentName': bills.Department.DepartmentName
                    };
                    DeptGroup[DeptId].push(info);
                }
            }
        } else if (req.Data.DepartmentId === 0) {
            let deptbillamountInstance: any = await this.FindAll({
                attributes: ['BillAmount', 'BillDiscount', 'DepartmentId'],
                where: {
                    BillTypeId: 2,
                    DepartmentId: { '$gt': req.Data.DepartmentId },
                    BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                    PatientBillStatusId: 3,
                    IsConsolidatePay: false,
                    FacilityId: req.Data.FacilityId,
                },
                include: [DeptGroupJoin]
            });
            if (deptbillamountInstance) {
                for (let i = 0; i < deptbillamountInstance.length; i++) {
                    let bills: any = this.GetAttribute(deptbillamountInstance[i]);
                    let DeptId = bills.DepartmentId;
                    let billamt = bills.BillAmount;
                    let discbill = bills.BillDiscount;
                    let NetAmount = billamt - discbill;
                    DeptGroup[DeptId] = DeptGroup[DeptId] || [];
                    let info = {
                        'BillAmount': NetAmount,
                        'GrossAmount': billamt,
                        'BillDiscount': discbill,
                        'DeptId': DeptId,
                        'DepartmentName': bills.Department.DepartmentName
                    };
                    DeptGroup[DeptId].push(info);
                }
            }
        }
        return DeptGroup;
    }

    public async DirectDeptRevenue(req: BaseRequest): Promise<any> {
        let DeptGroup: { [id: number]: any[] } = {};
        let deptbillamountInstance: any = await this.FindAll({
            attributes: ['BillAmount', 'BillDiscount', 'RefundAmount', 'DepartmentId'],
            where: {
                BillTypeId: { '$in': [1, 5] },
                DepartmentId: { '$in': [-1, 0, null] },
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                IsConsolidatePay: false,
                FacilityId: req.Data.FacilityId,
            }
        });
        if (deptbillamountInstance) {
            for (let i = 0; i < deptbillamountInstance.length; i++) {
                let bills: any = this.GetAttribute(deptbillamountInstance[i]);
                let DeptId = bills.DepartmentId;
                let DeptName = 'Others';
                let billamt = bills.BillAmount;
                let discbill = bills.BillDiscount;
                let refundamt = bills.RefundAmount;
                let NetAmount = billamt - discbill;
                let NetRevenue = (billamt || 0) - (discbill || 0) - (refundamt || 0);
                DeptGroup[DeptId] = DeptGroup[DeptId] || [];
                let info = {
                    'BillAmount': NetAmount,
                    'NetRevenue': NetRevenue,
                    'GrossAmount': billamt,
                    'BillDiscount': discbill,
                    'DeptId': DeptId,
                    'DepartmentName': DeptName
                };
                DeptGroup[DeptId].push(info);
            }
        }

        return DeptGroup;
    }


    /* BIDiscount */
    public async BIDiscountReport(req: BaseRequest): Promise<any> {
        let BIRptDeptRevenue: any = {};
        for (let j = 0, len = req.Data.length; j < len; j++) {
            let frmdt = req.Data[j].DispDate;
            if (!BIRptDeptRevenue[frmdt]) BIRptDeptRevenue[frmdt] = {};
            let newreq: any = {
                Data: []
            };
            newreq.Data = req.Data[j];
            if (newreq && newreq.Data && newreq.Data.FromDate) {
                if (!BIRptDeptRevenue[frmdt]['OPDiscount']) {
                    BIRptDeptRevenue[frmdt]['OPDiscount'] = [];
                }
                if (!BIRptDeptRevenue[frmdt]['IPDiscount']) {
                    BIRptDeptRevenue[frmdt]['IPDiscount'] = [];
                }
                if (!BIRptDeptRevenue[frmdt]['DGDiscount']) {
                    BIRptDeptRevenue[frmdt]['DGDiscount'] = [];
                }
                if (!BIRptDeptRevenue[frmdt]['PharDiscount']) {
                    BIRptDeptRevenue[frmdt]['PharDiscount'] = [];
                }
                if (!BIRptDeptRevenue[frmdt]['Top5DiscountPat']) {
                    BIRptDeptRevenue[frmdt]['Top5DiscountPat'] = [];
                }
                BIRptDeptRevenue[frmdt]['OPDiscount'].push(await this.OPDiscountReport(newreq));
                BIRptDeptRevenue[frmdt]['IPDiscount'].push(await this.IPDiscountReport(newreq));
                BIRptDeptRevenue[frmdt]['DGDiscount'].push(await this.DGDiscountReport(newreq));
                BIRptDeptRevenue[frmdt]['PharDiscount'].push(await this.PharDiscountReport(newreq));
                BIRptDeptRevenue[frmdt]['Top5DiscountPat'].push(await this.Top5DiscountPatients(newreq));

            }
        }
        return BIRptDeptRevenue;
    }

    public async OPDiscountReport(req: BaseRequest): Promise<any> {
        let Result = 0;
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'TotalOPDiscount'],
            ],
            where: {
                BillTypeId: 1,
                BillDiscount: { '$gt': 0 },
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                Status: 1,
            }
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            Result = bill['TotalOPDiscount'];
        }
        return Result;
    }

    public async IPDiscountReport(req: BaseRequest): Promise<any> {
        let Result = 0;
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'TotalIPDiscount'],
            ],
            where: {
                BillTypeId: 2,
                BillDiscount: { '$gt': 0 },
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                Status: 1,
            }
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            Result = bill['TotalIPDiscount'];
        }
        return Result;
    }

    public async DGDiscountReport(req: BaseRequest): Promise<any> {
        let Result = 0;
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'TotalDGDiscount'],
            ],
            where: {
                BillTypeId: 5,
                BillDiscount: { '$gt': 0 },
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                Status: 1,
            }
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            Result = bill['TotalDGDiscount'];
        }
        return Result;
    }

    public async PharDiscountReport(req: BaseRequest): Promise<any> {
        let Result = 0;
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillDiscount')), 'TotalPharDiscount'],
            ],
            where: {
                BillTypeId: 4,
                BillDiscount: { '$gt': 0 },
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                Status: 1,
            }
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            Result = bill['TotalPharDiscount'];
        }
        return Result;
    }

    public async Top5DiscountPatients(req: BaseRequest): Promise<any> {
        let apiReq = {
            Id: 0,
            PageContext: { PageSize: 5, PageNumber: 1 },
            Params: [
                { Key: PatientBillsFilters.BillType, Value: [1, 2, 4, 5] },
                {
                    Key: PatientBillsFilters.PatBillDt,
                    Value: [req.Data.FromDate, req.Data.ToDate]
                },
                { Key: PatientBillsFilters.IsBillDiscount, Value: true },
                { Key: PatientBillsFilters.PatientBillStatus, Value: 3 },
            ]
        };
        let Top5DisData: any = await this.GetPatientBills(apiReq);

        return Top5DisData;
    }

    /* BIDueReport */
    public async BIDueReport(req: BaseRequest): Promise<any> {
        let BIRptDeptRevenue: any = {};
        for (let j = 0, len = req.Data.length; j < len; j++) {
            let frmdt = req.Data[j].DispDate;
            if (!BIRptDeptRevenue[frmdt]) BIRptDeptRevenue[frmdt] = {};
            let newreq: any = {
                Data: []
            };
            newreq.Data = req.Data[j];
            if (newreq && newreq.Data && newreq.Data.FromDate) {
                if (!BIRptDeptRevenue[frmdt]['OPDue']) {
                    BIRptDeptRevenue[frmdt]['OPDue'] = [];
                }
                if (!BIRptDeptRevenue[frmdt]['IPDue']) {
                    BIRptDeptRevenue[frmdt]['IPDue'] = [];
                }
                BIRptDeptRevenue[frmdt]['OPDue'].push(await this.OPDueReport(newreq));
                BIRptDeptRevenue[frmdt]['IPDue'].push(await this.IPDueReport(newreq));
            }
        }
        return BIRptDeptRevenue;
    }

    public async OPDueReport(req: BaseRequest): Promise<any> {
        let Result: any = {};
        let GuarantorJoin: any = {
            model: this.Models.Guarantor, as: 'GuarantorMaster',
            attributes: ['GuarantorId', 'GuarantorName', 'GuarantorTypeId'],
            where: {
                Status: 1,
                ActiveStatusId: 2
            },
            required: true,
        };
        let opdueamountInstance: any = await this.FindAll({
            attributes: ['OutStandingAmount'],
            where: {
                BillTypeId: { '$in': [1, 5] },
                OutStandingAmount: { '$gt': 0 },
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                PatientBillStatusId: 3,
                Status: 1,
            },
            include: [GuarantorJoin]
        });
        if (opdueamountInstance) {
            for (let i = 0; i < opdueamountInstance.length; i++) {
                let bills: any = this.GetAttribute(opdueamountInstance[i]);
                let guarantorName = bills.GuarantorMaster.GuarantorName;
                let guarantorId = bills.GuarantorMaster.GuarantorId;
                let guarantorTypeId = bills.GuarantorMaster.GuarantorTypeId;
                Result[guarantorName] = Result[guarantorName] || [];
                let info = {
                    'OutStandingAmount': bills.OutStandingAmount,
                    'GuarantorId': guarantorId,
                    'GuarantorTypeId': guarantorTypeId,
                    'GuarantorName': guarantorName
                };
                Result[guarantorName].push(info);
            }
        }

        // let GuarantorJoin: any = {
        //     model: this.Models.Guarantor, as: 'GuarantorMaster',
        //     attributes: ['GuarantorId', 'GuarantorName', 'GuarantorTypeId'],
        //     where: {
        //         Status: 1,
        //         ActiveStatusId: 2
        //     },
        //     required: true,
        // };
        let opphardueInstance: any = await this.FindAll({
            attributes: ['OutStandingAmount'],
            where: {
                BillTypeId: 4,
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                OutStandingAmount: { '$gt': 0 },
                PatientBillStatusId: 3,
                Status: 1,
            },
            include: [GuarantorJoin]
        });
        if (opphardueInstance) {
            for (let i = 0; i < opphardueInstance.length; i++) {
                let bills: any = this.GetAttribute(opphardueInstance[i]);
                let guarantorName = bills.GuarantorMaster.GuarantorName;
                let guarantorId = bills.GuarantorMaster.GuarantorId;
                let guarantorTypeId = bills.GuarantorMaster.GuarantorTypeId;
                Result[guarantorName] = Result[guarantorName] || [];
                let info = {
                    'OutStandingAmount': bills.OutStandingAmount,
                    'GuarantorId': guarantorId,
                    'GuarantorTypeId': guarantorTypeId,
                    'GuarantorName': guarantorName
                };
                Result[guarantorName].push(info);
            }
        }
        return Result;
    }

    public async IPDueReport(req: BaseRequest): Promise<any> {
        let Result: any = {};
        let GuarantorJoin: any = {
            model: this.Models.Guarantor, as: 'GuarantorMaster',
            attributes: ['GuarantorId', 'GuarantorName', 'GuarantorTypeId'],
            where: {
                Status: 1,
                ActiveStatusId: 2
            },
            required: true,
        };
        let opdueamountInstance: any = await this.FindAll({
            attributes: ['OutStandingAmount'],
            where: {
                BillTypeId: 2,
                BillDateTime: { '$gte': req.Data.FromDate, '$lte': req.Data.ToDate },
                OutStandingAmount: { '$gt': 0 },
                PatientBillStatusId: 3,
                Status: 1,
            },
            include: [GuarantorJoin]
        });
        if (opdueamountInstance) {
            for (let i = 0; i < opdueamountInstance.length; i++) {
                let bills: any = this.GetAttribute(opdueamountInstance[i]);
                let guarantorName = bills.GuarantorMaster.GuarantorName;
                let guarantorId = bills.GuarantorMaster.GuarantorId;
                let guarantorTypeId = bills.GuarantorMaster.GuarantorTypeId;
                Result[guarantorName] = Result[guarantorName] || [];
                let info = {
                    'OutStandingAmount': bills.OutStandingAmount,
                    'GuarantorId': guarantorId,
                    'GuarantorTypeId': guarantorTypeId,
                    'GuarantorName': guarantorName
                };
                Result[guarantorName].push(info);
            }
        }
        return Result;
    }

    public async IPPharmacyPaymentDetails(req: BaseRequest): Promise<any> {
        let OPBillResult: any = {};
        OPBillResult['DisplayOrder'] = 100;
        let ipbillamountInstance: any = await this.Find({
            attributes: [
                [this.Dal.fn('SUM', this.Dal.col('BillAmount')), 'TotalAmountPaid'],
            ],
            where: {
                BillTypeId: 3,
                IsPharmacyBill: 1,
                PharmacySaleTypeId: { '$gt': 0 },
                BillDateTime: { '$gt': req.Data.FromDate, '$lte': req.Data.ToDate }
            }
        });
        if (ipbillamountInstance) {
            let bill: any = this.GetAttribute(ipbillamountInstance);
            OPBillResult['BillAmount'] = bill['TotalAmountPaid'];
        }

        OPBillResult['CashAmount'] = '0.00';
        OPBillResult['CardAmount'] = '0.00';
        OPBillResult['OtherAmount'] = '0.00';

        return OPBillResult;
    }

    public async ManageCashToCreditBill(req: BaseRequest): Promise<boolean> {
        //console.log(req);
        let refundbo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let GeneratedById = req.Data.GeneratedById;
        if (req.Data && req.Data.Bills) {
            for (let i = 0; i < req.Data.Bills.length; i++) {
                let Header = req.Data.Bills[i];
                Header.IsCashToCreditBill = true;
                if (!Header.RefundAmount) Header.RefundAmount = 0;

                Header.OldRefundAmount = 0;
                if (Header.RefundAmount > 0)
                    Header.OldRefundAmount = Header.RefundAmount;

                if (Header && Header.NetAmount > 0) {
                    let nulldata: any = null;
                    let PatientRefund: any = [];
                    let patientrefund = {
                        Id: 0,
                        RefundDateTime: Header.BillDateTime,
                        RefundIdentifier: nulldata,
                        FacilityId: Header.FacilityId,
                        OrganizationId: Header.OrganizationId,
                        PatientId: Header.PatientId,
                        RefundTypeId: '4',
                        EncounterId: Header.EncounterId,
                        EncounterTypeId: Header.EncounterTypeId,
                        PatientName: Header.PatientName,
                        RefundAmount: (Header.PaidAmount - Header.RefundAmount),
                        DepartmentID: Header.DepartmentId,
                        StoreMasterId: Header.StoreMasterId,
                        PaymentcounterID: nulldata,
                        GuarantorId: Header.GuarantorId,
                        GuarantorTypeId: Header.GuarantorTypeId,
                        RefundGeneratedById: GeneratedById,
                        RefundApprovedById: GeneratedById,
                        DoctorId: Header.DoctorId,
                        PatientBillId: Header.Id,
                        PharmacyReturnId: -1,
                        CardNumber: nulldata,
                        CardDateTime: nulldata,
                        CardExpiryDate: nulldata,
                        BankId: -1,
                        CardTypeId: -1,
                        CardHolderName: nulldata,
                        AuthorizeNumber: nulldata,
                        GurantorName: Header.GurantorName,
                        ChequeNo: nulldata,
                        ChequeDate: nulldata,
                        DDNumber: nulldata,
                        DDDate: nulldata,
                        WireTransferId: nulldata,
                        WireTransferDate: nulldata,
                        CancelReason: 'Cash2Credit',
                        RefundStatusId: 1,
                        RoundOffValue: Header.RoundOffValue,
                        PatientReturnId: -1,
                        DebitNoteId: -1,
                        PaymentTypeId: 1,
                        PatientCreditNoteId: '0',
                    };
                    PatientRefund.push(patientrefund);
                    await refundbo.ManageCashtoCreditRefund(Header.Id, PatientRefund);
                    Header.RefundAmount += Header.NetAmount;
                    await this.Update(Header);
                    Header.IsCashToCreditBill = false;
                    Header.CashToCreditRef = Header.Id;
                    Header.OutStandingAmount = Header.NetAmount;
                    Header.CreditVocher = Header.NetAmount;
                    Header.Id = 0;
                    Header.CNAmount = 0;
                    Header.RefundAmount = 0;
                    Header.IsPaidFully = 0;
                    Header.PaidAmount = 0;
                    Header.BillNumber = null;
                    Header.BillTypeId = 3;
                    Header.PharmacySaleTypeId = 6;
                    Header.BillGeneratedBy = GeneratedById;
                    for (let k = 0; k < Header.PatientBillDetails.length; k++) {
                        Header.PatientBillDetails[k].Id = 0;
                        Header.PatientBillDetails[k].PharmacySaleTypeId = 6;
                    }
                    let newreq: any = {
                        Data: {
                            Header: Header,
                            Details: Header.PatientBillDetails,
                            paymentDetail: [],
                        }
                    };
                    await this.AddPatientBills(newreq);
                }
            }
        }

        return true;
    }

    public async ManageIPCashToOPCreditBill(req: BaseRequest): Promise<boolean> {
        //console.log(req);
        let refundbo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        let retrunbo = BoFactory.GetBo(bo.PatientReturnsBo, this.Request);
        let retrundtsbo = BoFactory.GetBo(bo.PatientReturnDetailsBo, this.Request);
        let GeneratedById = req.Data.GeneratedById;
        let PrivateDueId = req.Data.PrivateDueId;
        if (req.Data && req.Data.Bills) {
            for (let i = 0; i < req.Data.Bills.length; i++) {
                let Header = req.Data.Bills[i];
                let OldPatientBillId: any = req.Data.Bills[i].Id;
                Header.IsCashToCreditBill = true;
                if (!Header.RefundAmount) Header.RefundAmount = 0;

                Header.OldRefundAmount = 0;
                if (Header.RefundAmount > 0)
                    Header.OldRefundAmount = Header.RefundAmount;

                if (Header && Header.NetAmount > 0) {
                    let nulldata: any = null;
                    let PatientRefund: any = [];
                    let patientrefund = {
                        Id: 0,
                        RefundDateTime: Header.BillDateTime,
                        RefundIdentifier: nulldata,
                        FacilityId: Header.FacilityId,
                        OrganizationId: Header.OrganizationId,
                        PatientId: Header.PatientId,
                        RefundTypeId: '4',
                        EncounterId: Header.EncounterId,
                        EncounterTypeId: Header.EncounterTypeId,
                        PatientName: Header.PatientName,
                        RefundAmount: (Header.PaidAmount - Header.RefundAmount),
                        DepartmentID: Header.DepartmentId,
                        StoreMasterId: Header.StoreMasterId,
                        PaymentcounterID: nulldata,
                        GuarantorId: Header.GuarantorId,
                        GuarantorTypeId: Header.GuarantorTypeId,
                        RefundGeneratedById: GeneratedById,
                        RefundApprovedById: GeneratedById,
                        DoctorId: Header.DoctorId,
                        PatientBillId: Header.Id,
                        PharmacyReturnId: -1,
                        CardNumber: nulldata,
                        CardDateTime: nulldata,
                        CardExpiryDate: nulldata,
                        BankId: -1,
                        CardTypeId: -1,
                        CardHolderName: nulldata,
                        AuthorizeNumber: nulldata,
                        GurantorName: Header.GurantorName,
                        ChequeNo: nulldata,
                        ChequeDate: nulldata,
                        DDNumber: nulldata,
                        DDDate: nulldata,
                        WireTransferId: nulldata,
                        WireTransferDate: nulldata,
                        CancelReason: 'Cash2Credit',
                        RefundStatusId: 1,
                        RoundOffValue: Header.RoundOffValue,
                        PatientReturnId: -1,
                        DebitNoteId: -1,
                        PaymentTypeId: 1,
                        PatientCreditNoteId: '0',
                        IsCashToCredit: true,
                    };
                    PatientRefund.push(patientrefund);
                    await refundbo.ManageCashtoCreditRefund(Header.Id, PatientRefund);
                    Header.RefundAmount += Header.NetAmount;
                    await this.Update(Header);
                    Header.IsCashToCreditBill = false;
                    Header.CashToCreditRef = Header.Id;
                    Header.OutStandingAmount = Header.NetAmount;
                    Header.CreditVocher = (Header.BillAmount + Header.RoundOffValue);
                    Header.Id = 0;
                    Header.CNAmount = 0;
                    Header.RefundAmount = 0;
                    Header.IsPaidFully = 0;
                    Header.PaidAmount = 0;
                    Header.BillNumber = null;
                    Header.BillTypeId = 4;
                    Header.PrivateDueId = PrivateDueId;
                    Header.PharmacySaleTypeId = 1;
                    Header.BillGeneratedBy = GeneratedById;
                    Header.CreatedBy = GeneratedById;
                    Header.CreatedAt = new Date();
                    for (let k = 0; k < Header.PatientBillDetails.length; k++) {
                        Header.PatientBillDetails[k].Id = 0;
                        Header.PatientBillDetails[k].PharmacySaleTypeId = 1;
                    }
                    let newreq: any = {
                        Data: {
                            Header: Header,
                            Details: Header.PatientBillDetails,
                            paymentDetail: [],
                        }
                    };
                    let NewPatientBillId = await this.AddPatientBills(newreq);
                    let NewPatientBillInfo = await this.GetPatientBillsById({ Id: NewPatientBillId });
                    if (OldPatientBillId > 0) {
                        let billReq = {
                            Id: 0,
                            PageContext: { PageSize: 1000, PageNumber: 1 },
                            Params: [{ Key: PatientReturnsFilters.PatientBillId, Value: OldPatientBillId }]
                        };
                        let PatientReturns: any = await retrunbo.GetPatientReturns(billReq);
                        if (PatientReturns && PatientReturns.Data
                            && PatientReturns.Data.length > 0) {
                            for (let rr = 0; rr < PatientReturns.Data.length; rr++) {
                                let patret = PatientReturns.Data[rr];
                                let billretdtReq = {
                                    Id: 0,
                                    PageContext: { PageSize: 10000, PageNumber: 1 },
                                    Params: [{ Key: PatientReturnDetailsFilters.PatientReturnId, Value: patret.Id }]
                                };
                                let patretdetails: any = await retrundtsbo.GetPatientReturnDetails(billretdtReq);
                                for (let rrd = 0; rrd < patretdetails.Data.length; rrd++) {
                                    patretdetails.Data[rrd].Id = 0;
                                    patretdetails.Data[rrd].PatientBillId = NewPatientBillId;
                                    patretdetails.Data[rrd].CreatedBy = GeneratedById;
                                    patretdetails.Data[rrd].CreatedAt = new Date();
                                    patretdetails.Data[rrd].ReturnDateTime = new Date();
                                }
                                patret.Id = 0;
                                patret.PatientBillId = NewPatientBillId;
                                patret.ReturnNumber = null;
                                patret.BillDateTime = NewPatientBillInfo.BillDateTime;
                                patret.CreatedBy = GeneratedById;
                                patret.CreatedAt = new Date();
                                patret.ReturnDateTime = new Date();
                                patret.ReturnAmount = 0; // Credit Return so zero
                                patret.RefundedAmount = 0; // Credit Return so zero
                                let RetReq: any = {
                                    Data: {
                                        Header: patret,
                                        Details: patretdetails.Data
                                    }
                                };
                                await retrunbo.AddCashToCreditReturns(RetReq);
                            }
                        }
                    }
                }
            }
        }

        return true;
    }

    // public async checkBillCashAmount(req: any): Promise<any> {
    //     // let billDate = new Date();
    //     // let FromDate = billDate.setMinutes(billDate.getMinutes() - 2);
    //     // let ToDate = billDate.setMinutes(billDate.getMinutes() + 2);
    //     // let FromDate = billDate.setSeconds(billDate.getSeconds() - 30);
    //     // let ToDate = billDate.setSeconds(billDate.getSeconds() + 30);
    //     // let frmDate = moment(FromDate);
    //     // let todate = moment(ToDate);
    //     let apiReq = {
    //         Id: 0,
    //         PageContext: { PageSize: -1, PageNumber: 1 },
    //         Params: [
    //             // { Key: PatientBillDetailsFilters.PatientId, Value: req.Data.Header.PatientId },
    //             { Key: PatientBillDetailsFilters.EncounterId, Value: req.Data.Header.EncounterId },
    //             { Key: PatientBillDetailsFilters.IsTempIPBill, Value: true },
    //             // { Key: PatientBillDetailsFilters.AdmissionStatusId, Value: [2, 3] },
    //             // { Key: PatientBillDetailsFilters.GuarantorId, Value: 1 }
    //         ]
    //     };
    //     let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
    //     let data = await PatientBillDetailsBo.GetMinPatientBillDetailsforLock(apiReq);

    //     var NetAmount = 0;
    //     var AmountPaid = 0;
    //     var RefundAmount = 0;

    //     if (data.Data && data.Data.length > 0) {
    //         var GrossAmount = 0;
    //         var DiscountAmount = 0;
    //         var ProportionateDiscount = 0;

    //         for (let k = 0; k < data.Data.length; k++) {
    //             let item: any = data.Data[k];
    //             GrossAmount = GrossAmount + item.GrossAmount;
    //             DiscountAmount = DiscountAmount + item.DiscountAmount;
    //             ProportionateDiscount = ProportionateDiscount + item.ProportionateDiscount;
    //         }

    //         // let billDetails: any = _.groupBy(data.Data, 'PatientBillId');
    //         // // let groupbills = _.groupBy(totalbillamountInstance, 'BillGeneratedBy');
    //         // for (let i in billDetails) {
    //         //     let groupedBills = billDetails[i];
    //         //     if (groupedBills[0].PatientPaymentDetails.length > 0) {
    //         //         groupedBills[0].PatientPaymentDetails.forEach((Detail: any) => {
    //         //             let paymentData = Detail;
    //         //             if (paymentData.ReceiptStatusId === 1 && paymentData.IsPharmacyReceipt === 0)
    //         //                 AmountPaid = AmountPaid + paymentData.AmountPaid;
    //         //         });
    //         //     }
    //         //     if (groupedBills[0].PatientRefunds.length > 0) {
    //         //         groupedBills[0].PatientRefunds.forEach((Detail: any) => {
    //         //             let refundData = Detail;
    //         //             if (refundData.PharmacyReturnId === null && refundData.RefundStatusId === 1)
    //         //                 RefundAmount = RefundAmount + refundData.RefundAmount;
    //         //         });
    //         //     }
    //         // }

    //         NetAmount = GrossAmount - (DiscountAmount + ProportionateDiscount);
    //     }

    //     let apiReq1 = {
    //         Id: 0,
    //         PageContext: { PageSize: -1, PageNumber: 1 },
    //         Params: [
    //             { Key: PatientPaymentDetailsFilters.EncounterId, Value: req.Data.Header.EncounterId },
    //         ]
    //     };

    //     let PatientpaymentDetailsBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
    //     let paymentData = await PatientpaymentDetailsBo.GetMinPatientPaymentDetails(apiReq1);
    //     if (paymentData.Data && paymentData.Data.length > 0) {
    //         for (let k = 0; k < paymentData.Data.length; k++) {
    //             let pdata: any = paymentData.Data[k];
    //             if (pdata.ReceiptStatusId === 1 && pdata.IsPharmacyReceipt === 0)
    //                 AmountPaid = AmountPaid + pdata.AmountPaid;

    //         }
    //     }

    //     let apiReq2 = {
    //         Id: 0,
    //         PageContext: { PageSize: -1, PageNumber: 1 },
    //         Params: [
    //             { Key: PatientRefundFilters.EncounterId, Value: req.Data.Header.EncounterId },
    //         ]
    //     };

    //     let RefundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
    //     let refundData = await RefundBo.GetPatientRefund(apiReq2);

    //     if (refundData.Data && refundData.Data.length > 0) {
    //         for (let k = 0; k < refundData.Data.length; k++) {
    //             let rdata: any = refundData.Data[k];
    //             if (rdata.PharmacyReturnId === null && rdata.RefundStatusId === 1)
    //                 RefundAmount = RefundAmount + rdata.RefundAmount;

    //         }
    //     }

    //     var BalanceAmount = NetAmount - (AmountPaid - RefundAmount);
    //     // return BalanceAmount;
    //     if (BalanceAmount > 0) {
    //         return BalanceAmount;
    //     } else return 0;
    // }
    public async checkBillCashAmount(req: any): Promise<any> {
        const encounterId = req.Data.Header.EncounterId;
        let NetAmount = 0, AmountPaid = 0, RefundAmount = 0;

        const billDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
        const billRes = await billDetailsBo.GetMinPatientBillDetailsforLock({
            Id: 0, PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [
                { Key: PatientBillDetailsFilters.EncounterId, Value: encounterId },
                { Key: PatientBillDetailsFilters.IsTempIPBill, Value: true }]
        });

        if (billRes.Data && billRes.Data.length > 0) {
            for (const item of billRes.Data) {
                if (item.PatientBillStatusId === 3) {
                    NetAmount += (item.GrossAmount || 0) - ((item.DiscountAmount || 0) + (item.ProportionateDiscount || 0));
                }
            }
        }

        const paymentBo = BoFactory.GetBo(bo.PatientPaymentDetailsBo, this.Request);
        const payRes = await paymentBo.GetMinPatientPaymentDetailsforLock({
            Id: 0, PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientPaymentDetailsFilters.EncounterId, Value: encounterId }]
        });

        if (payRes.Data && payRes.Data.length > 0) {
            for (const p of payRes.Data) {
                if (p.ReceiptStatusId === 1 && p.IsPharmacyReceipt === false)
                    AmountPaid += (p.AmountPaid || 0);
            }
        }

        const refundBo = BoFactory.GetBo(bo.PatientRefundBo, this.Request);
        const refundRes = await refundBo.GetPatientRefund({
            Id: 0, PageContext: { PageSize: -1, PageNumber: 1 },
            Params: [{ Key: PatientRefundFilters.EncounterId, Value: encounterId }]
        });

        if (refundRes.Data && refundRes.Data.length > 0) {
            for (const r of refundRes.Data) {
                if (r.PharmacyReturnId === null && r.RefundStatusId === 1)
                    RefundAmount += (r.RefundAmount || 0);
            }
        }

        console.log(NetAmount);
        console.log(AmountPaid);
        console.log(RefundAmount);
        const BalanceAmount = NetAmount - (AmountPaid - RefundAmount);
        // return BalanceAmount > 0 ? BalanceAmount : 0;
        return BalanceAmount;
    }

    // private async IsBillCashExceed(req: any): Promise<number> {
    //     // let billDate = new Date();
    //     // let FromDate = billDate.setMinutes(billDate.getMinutes() - 2);
    //     // let ToDate = billDate.setMinutes(billDate.getMinutes() + 2);
    //     // let FromDate = billDate.setSeconds(billDate.getSeconds() - 30);
    //     // let ToDate = billDate.setSeconds(billDate.getSeconds() + 30);
    //     // let frmDate = moment(FromDate);
    //     // let todate = moment(ToDate);
    //     let apiReq = {
    //         Id: 0,
    //         PageContext: { PageSize: -1, PageNumber: 1 },
    //         Params: [
    //             { Key: PatientBillDetailsFilters.PatientId, Value: req.Data.Header.PatientId },
    //             { Key: PatientBillDetailsFilters.EncounterId, Value: req.Data.Header.EncounterId },
    //             { Key: PatientBillDetailsFilters.IsTempIPBill, Value: true },
    //             { Key: PatientBillDetailsFilters.AdmissionStatusId, Value: [2, 3] },
    //             { Key: PatientBillDetailsFilters.GuarantorId, Value: 1 }]
    //     };
    //     // if (req.Data.Header.checkBill && req.Data.Header.checkBill === true) {
    //     //     apiReq.Params.push({
    //     //         Key: PatientBillsFilters.BillAmount, Value: req.Data.Header.BillAmount
    //     //     });
    //     //     apiReq.Params.push({
    //     //         Key: PatientBillsFilters.EncounterId, Value: req.Data.Header.EncounterId
    //     //     });
    //     // }
    //     let PatientBillDetailsBo = BoFactory.GetBo(bo.PatientBillDetailsBo, this.Request);
    //     let data = await PatientBillDetailsBo.GetMinPatientBillDetailsforLock(apiReq);
    //     if (data.Data && data.Data.length > 0) {
    //         var GrossAmount = 0;
    //         var DiscountAmount = 0;
    //         var ProportionateDiscount = 0;
    //         var NetAmount = 0;
    //         var AmountPaid = 0;
    //         var RefundAmount = 0;
    //         for (let k = 0; k < data.Data.length; k++) {
    //             let item: any = data.Data[k];
    //             GrossAmount = GrossAmount + item.GrossAmount;
    //             DiscountAmount = DiscountAmount + item.DiscountAmount;
    //             ProportionateDiscount = ProportionateDiscount + item.ProportionateDiscount;
    //             item.PatientPaymentDetails.forEach((Detail: any) => {
    //                 let paymentData = Detail;
    //                 if (paymentData.ReceiptStatusId === 1 && paymentData.IsPharmacyReceipt === 0)
    //                     AmountPaid = AmountPaid + paymentData.AmountPaid;
    //             });
    //             item.PatientRefund.forEach((Detail: any) => {
    //                 let refundData = Detail;
    //                 if (refundData.PharmacyReturnId === null && refundData.RefundStatusId === 1)
    //                     RefundAmount = RefundAmount + refundData.RefundAmount;
    //             });
    //         }
    //         NetAmount = GrossAmount - (DiscountAmount + ProportionateDiscount);
    //         var BalanceAmount = NetAmount - (AmountPaid - RefundAmount);
    //         if (BalanceAmount > 9999) {
    //             return -1;
    //         }
    //         // return (data.Data.length * -1);
    //     }
    //     return 1;
    // }
    private async registerPatientIfNeeded(req: BaseRequest, session: any): Promise<number | null> {
        const patientBo = BoFactory.GetBo(regbo.PatientBo, this.Request);
        const memberData: any = {
            Data: {
                Id: 0,
                MRNTypeId: 1,
                NationalityId: req.Data.Header.NationalityId,
                TitleId: req.Data.Header.TitleId || 0,
                PatientName: req.Data.Header.PatientName,
                FirstName: req.Data.Header.PatientName,
                DOB: req.Data.Header.DOB,
                GenderId: req.Data.Header.GenderId,
                Mobile: req.Data.Header.Mobile,
                Email: req.Data.Header.Email,
                IsActive: true,
                CreatedBy: session.UserId,
                CreatedOn: new Date(),
                IsEmergency: true,
                FacilityId: session.FacilityId,
                Address: req.Data.Header.Address,
                Age: req.Data.Header.Age,
                AgeUnitId: req.Data.Header.AgeUnitId,
                From: 'fromjssdirectsales',
                PatientStatus: 'Active'
            }
        };

        const patientId: any = await patientBo.AddPatient(memberData);
        return patientId > 0 ? patientId : null;
    }

    private async UpdatePlanInfo(req: any) {
        console.log('NEWLY GENERATED ' + req.Data.Header.BillNumber);
        let planBO = BoFactory.GetBo(emrbo.TreatmentPlanBo, this.Request);
        let orderUpdateBillNr: any = { BillNumber: req.Data.Header.BillNumber };
        await planBO.Update(orderUpdateBillNr, {
            fields: ['BillNumber'],
            where: {
                PatientBillId: req.Data.Header.PatientBillId
            }
        });

    }

    private async UpdateOrderInfo(req: any) {
        console.log('NEWLY GENERATED ' + req.Data.Header.BillNumber);
        let patorderBo = BoFactory.GetBo(emrbo.PatientOrderBo, this.Request);
        if (req.Data.Header.OrderIds) {
            if (req.Data.Header.OrderIds.length > 0) {
                for (let odx in req.Data.Header.OrderIds) {
                    let orderId = req.Data.Header.OrderIds[odx];
                    let PatientOrder = await patorderBo.GetPatientOrderById({ Id: orderId });
                    PatientOrder.BillingId = req.Data.Header.PatientBillId;
                    PatientOrder.BillNumber = req.Data.Header.BillNumber;
                    await patorderBo.Update(PatientOrder);
                }
            }
        } else {
            let orderUpdateBillNr: any = { BillNumber: req.Data.Header.BillNumber };
            await patorderBo.Update(orderUpdateBillNr, {
                fields: ['BillNumber'],
                where: {
                    BillingId: req.Data.Header.PatientBillId
                }
            });
        }
    }

    private async UpdatePatientAccountsInfo(req: any) {
        console.log('NEWLY GENERATED ' + req.Data.Header.BillNumber);
        let patientaccountsBO = BoFactory.GetBo(billingBO.PatientAccountsBo, this.Request);
        let accountUpdateBillNr: any = { TransactionNumber: req.Data.Header.BillNumber };
        await patientaccountsBO.Update(accountUpdateBillNr, {
            fields: ['TransactionNumber'],
            where: {
                TransactionTypeId: 2,
                TransactionId: req.Data.Header.PatientBillId,
                TransactionNumber: null,
            }
        });
    }

    private async UpdateMovementInfo(req: any) {
        let stockmovementBo = BoFactory.GetBo(inventoryBO.StockMovementBo, this.Request);
        let UpdateMovementTransactionNumber: any = { TransactionNumber: req.Data.Header.TransactionNumber };
        await stockmovementBo.Update(UpdateMovementTransactionNumber, {
            fields: ['TransactionNumber'],
            where: { TransactionId: req.Data.Header.TransactionId }
        });
    }

    private async UpdateOpticalMovementInfo(req: any) {
        let stockmovementBo = BoFactory.GetBo(inventoryBO.OpticalStockMovementBo, this.Request);
        let UpdateMovementTransactionNumber: any = { TransactionNumber: req.Data.Header.TransactionNumber };
        await stockmovementBo.Update(UpdateMovementTransactionNumber, {
            fields: ['TransactionNumber'],
            where: { TransactionId: req.Data.Header.TransactionId }
        });
    }
}
