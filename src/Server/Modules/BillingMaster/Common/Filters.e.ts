export enum ProcedureServicesFilters {
    Id,
    ProcedureId,
    ServiceId
}
export enum ServiceGroupRateMappingFilters {
    Id,
    ServiceGroup,
    BedTypeId,
    Amount,
    ActiveStatusId,
}
export enum PatientEstimationFilters {
    Id,
    SaveType,
    From,
    To
}
export enum PatientEstimationDetailsFilters {
    Id,
    PatientEstimationId
}
