export enum SequenceMastersFilters {
    Id,
    Name,
    GetAll,
    FacilityId
}
export enum EntityPrintHistoryFilters {
    Id,
    Name,
    Facility,
    PrintDate,
    ObjectType,
    From,
    To
}
